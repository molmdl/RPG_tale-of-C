# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-08-12)

**Core value:** The player experiences cellular respiration as a story with consequences — every choice and edit on the C14 hero either advances them toward a destiny (ATP [the hero's electrons harvested into energy via the ETC], storage, CO2, or catastrophe) or diverts them into a branch, with real PDB proteins as the cast and scientifically validated chemistry as the plot.
**Current focus:** Phase 5.4 — Cast & Hero Representation Design — COMPLETE ✓ (4/4 plans: 05.4-01 convention spec + 05.4-02 hero-highlight smoke + 05.4-03 scene-template/cast-reveal smoke + 05.4-04 human design-review checkpoint APPROVED-WITH-ADJUSTMENTS). Convention FROZEN for Phase 6/7/9/12 (cyan hero + magenta mutant + "YOU" label + default+warn+confirm selection rule + OQ-11 future note). Phase 5.3 COMPLETE ✓. Phase 5.2 COMPLETE ✓. Phase 5.1 COMPLETE (skeleton FROZEN at 55 nodes). Phase 5 COMPLETE ✓. Phase 4 COMPLETE + VERIFIED (198 tests). Next: Phase 6 (Qt UI MVP).

## Current Position

Phase: 6 of 16 (Qt UI + Minimal Playable MVP) — EXECUTED 14/14 plans; VERIFICATION: human_needed (GUI re-check round 2: SC2f fix + shuffle auto-resolve + Cancel-return pending; 2 user decisions RESOLVED 2026-08-30)
Plan: 14 of 14 complete in current phase (06-01..06-14 done; 06-14 Task 2 verdict + round-1 SC2 fixes + round-2 re-verify fixes landed: 68631dd SC2f + 245f88f seam hardening + 81b1a48 shuffle auto-resolve + f413dbb help note; 06-VERIFICATION.md status human_needed, score 60/60 machine-verifiable)
Status: Phase 5.4 COMPLETE — 05.4-04 (Plan 04, human design-review checkpoint) DELIVERED SC4 APPROVED-WITH-ADJUSTMENTS (5 overrides: OQ-2 magenta mutant, OQ-3 cyan hero + all-C-colored ball-and-stick + "YOU" label, OQ-4 "YOU", OQ-6 default+warn+confirm, OQ-11 future-note; 6 approve-rec). The convention `05.4-CONVENTION.md` is FROZEN (766 lines, 10 H2 sections §1-§8 + SC checklist; 5 overrides applied). Both smokes re-ran SMOKE_RESULT: PASS after the cyan/magenta/YOU palette swap (hero_highlight 29/29, scene_template 37/37). molops.py UNTOUCHED (OQ-1 dispatch deferred to Phase 6). The convention specifies: SC1 the C14 hero highlight (OQ-3 OVERRIDE: ALL carbons hero_cyan sticks + hero ball-and-stick sphere_scale 0.3 + "YOU" label + non-carbons keep element colors; OQ-6 OVERRIDE: multi-C fallback = default+warn+confirm gate; hybrid selection rule + 3 structural cases + persistence as pure function of on_enter + CO2-shed soul-transfer climax w/ anti-confusion); SC2 the scene-template library (6 scene TYPES mapping all 55 nodes as MolAction sequences NOT cmd.scene + 4 ending tier-templates); SC3 the cast-reveal (full-then-zoom + gray/green/magenta/orange palette + rep-redundancy colorblind-safe [magenta SPHERES vs green STICKS] + nameplate labels + 5.3 composition [5.4 BEFORE mutant magenta spheres + 5.3 AFTER WT green; fills <mutant_color>=magenta / <wt_color>=green]). 11 OQs ALL RESOLVED. No follow-up work owed in 5.4.
Last activity: 2026-08-30 — Re-verify ROUND 2 of 06-14 (post human-verify re-check): SC2f FIXED (root cause: the edit:offer→request_edit routing existed only in the mixed-weighted ChoicePanel mode; the Phase 5.1 replan added edit:offer to 13 pure-MC enzyme nodes — gly.pfk first — so the edit button advanced via generic choose(i) → edit.prompt with an empty stash → build_edit_intent RuntimeError; fix: _render_pure dual-predicate special-case → request_edit(_current_enzyme_id()), commit 68631dd, + graph-invariant test + formalized probe + smoke stage). Latent-seam hardening (245f88f): stale-stash cleared after successful apply (controller + MainWindow paths); EditDialog Cancel no longer strands the player (source-node stash + controller.return_to_edit_source + ChoicePanel.render_single_action "Return to the enzyme" button; NO skeleton change). Decision 1 APPLIED (81b1a48): tca.shuffle AUTO-RESOLVES at the controller _render choke point — trap AUTO-FIRES as a RESULT at visits>5 (bad.cycle_trap_host_death + bad_ending achievement), the aconitase edit is offered ONCE on the first entry via the new injected edit_offer_fn ("Edit aconitase before the wheel turns?"), else AUTO-SPIN choose(0) (engine RNG picks among the WEIGHTED only — one draw per spin); NO new RNG weights/outcomes; NO skeleton JSON change (55/21 untouched); entry methods return the POST-resolve turn. Decision 2 APPLIED (f413dbb): help expansion DEFERRED to Phase 11 — visible placeholder pointer added to help.json. Debug session committed (a0eb04d). 322 tests green; AST gate clean; smoke SMOKE_RESULT: PASS (60 checks); reachability 19/19. STILL human_needed until the user re-checks the round-2 GUI items (edit at gly.pfk opens the dialog; shuffle auto-spins without a Spin button; trap auto-fires as an ending; Cancel returns to the enzyme).

Progress: [█████████████████████████████████████████] 45 plans complete across phases 1-6 (Phase 5 COMPLETE 21/21; Phase 5.1: 16/16 DONE; Phase 5.2: 1/1 DONE; Phase 5.3: 2/2 DONE [SC1+SC2+SC3 APPROVED]; Phase 5.4: 4/4 DONE [SC1+SC2+SC3+SC4 APPROVED-WITH-ADJUSTMENTS — convention FROZEN]; Phase 6: EXECUTED ✓ — 06-01..06-14 ALL DONE, the Qt UI MVP is playable end-to-end, the FIRST human-verify milestone exercised (06-14 verdict: SC1/SC3/SC4/SC5 PASS; SC2 round-1 bugs fixed 2026-08-30; round-2 re-check fixes landed: SC2f + seam hardening + shuffle auto-resolve [user decision 1] + help deferral [user decision 2]); VERIFICATION (2026-08-30): human_needed — 60/60 machine-verifiable must-haves verified; pending: ONE GUI re-check of the round-2 fixes (edit at gly.pfk opens the dialog; shuffle auto-spins without a Spin button; trap auto-fires as an ending; Cancel returns to the enzyme) gating Phase 7 planning; phases 7-12 not yet planned)

### 06-01 update (2026-08-29)

Phase 6 (Qt UI MVP) IN PROGRESS — Plan 06-01 COMPLETE ✓ (3/3 tasks): added the 4 deferred molops dispatches owed from 5.3/5.4 — `set_color`/`label`/`set` (FROZEN-verbatim from 05.4-CONVENTION.md) + `align` (FROZEN-verbatim from 05.3-CONVENTION.md) as new elif branches in `c14/pymol_layer/molops.py` using `self._cmd` already injected (NO new constructor param, NO new dependency) — plus the `load` branch target-prefix fallback (`pdb:`/`cid:`/`sid:`/bare-filename) parsing `action.target` when no source/file/code/cid keys are present (fixes the KeyError on the 5.1 skeleton on_enter form; preserves the explicit-source backward-compat path — test_molops.py:220-251 stay green). 22 MockCmd unit tests green (14 op-dispatch + 8 load target-prefix/backward-compat; covers the quoted-string label footgun, cealign REVERSED arg-order normalization, align_sele composition, ValueError on unknown method, target-prefix parsing, no-object-stripped-code default). Headless smoke `tools/molops_deferred_dispatch_smoke.py` SMOKE_RESULT: PASS (13/13 stage checks): dispatching the 6-call hero-highlight + an align + a target-prefix load via `molops.apply` reproduces the direct-cmd smokes' post-conditions (count=1/elem C/rep spheres+sticks/color hero_cyan/label YOU; align moved mobile wt 8.85→1.46; load bare-filename→load_bundled hero2=3 NO KeyError; load pdb:→fetch_pdb mock NO network). AST gate clean; full suite green (no regression). 3 task commits caa1d40 + ff78d5b + 524362a + docs 21ffc18. Unblocks the controller (06-06) which forwards MolActions to molops.apply; unblocks SC2 (hero highlight) + the 5.3 WT-aligned reveal + the skeleton on_enter load form. DEVIATIONS: 2 parallel-staging races (Rule 3) — Task 3 commit 524362a bundled 2 concurrent 06-02/06-05 test files (test_engine.py +176 / test_state.py +29) that were pre-staged; the 06-01 SUMMARY was swept into the parallel 06-03 commit 335fdfa before its dedicated docs(06-01) commit 21ffc18 superseded it. Per the 05.1-14 precedent, history was NOT rewritten (parallel agents active — `git reset --soft` risks orphaning a parallel commit); attribution noise only, no data loss / incorrect code. Used path-limited commits (`git commit -m "..." -- <path>`) going forward to avoid sweeping parallel-staged files.

### 06-14 update (2026-08-30)

Phase 6 (Qt UI MVP) CAPSTONE — Plan 06-14 COMPLETE ✓ (2/2 tasks). Task 1: the headless integration smoke (details in the 06-14-SUMMARY). Task 2 (checkpoint:human-verify in a real Windows PyMOL 2.5.0 session): the human played the game end-to-end and reported the FIRST human-verify milestone verdict:

- **Verdict:** SC1 PASS (install + menu + main window); SC3 PASS (True + Bad endings reachable; save/load round-trips — camera restores; manual color/rep changes made before a save intentionally do NOT restore: the scene rebuilds from the node's on_enter replay and the saved view is CAMERA-only per 06-03); SC4 PASS (glucose starts instantly on bundled assets; the bulk-download prompt does not fire for the placeholder cast — mechanism verified headlessly); SC5 PASS (achievements unlock + persist across restarts; help pointers + wiki links work — the user prefers FULLER INLINE help, an OPEN decision; help.json NOT edited).
- **SC2 bugs fixed (same session, 3 path-scoped commits):** (a) the OQ-6 default sele `<obj> and elem C` matched ALL carbons -> BOTH got spheres + "YOU" on the 2-carbon `_smoke.pdb`; now `first (obj and elem C)` — EMPIRICALLY VERIFIED headlessly via the new `tools/probe_first_operator.py` (9/9 PASS: `first` supported in PyMOL 2.5.0, resolves to exactly 1 atom, picks C1 = the first carbon; the `index 1` fallback verified but unneeded) — commit `389ee3e` (also updates test_controller.py to the single-carbon sele + strengthens the smoke label check to EXACTLY ONE "YOU", post-fix `labels=['YOU','','']`). (b) orientation ("not sure which point it is") -> the status bar now shows `node=<id>  stage=<tag>  character=<c>  seed=<s>` — commit `3f483aa`. (f) clicking "Edit enzyme" crashed (`ValueError: too many values to unpack (expected 3)` at main_window.py:413) -> `EditDialog.selected_edit()` now returns the documented `(op, target, args)` 3-tuple (it was returning the raw stored 4-tuple `(label, op, target, args)`) — commit `c2d1008`.
- **Verified-correct, not a bug:** the `controller: molops.apply failed ... pdb:TBD_ACONITASE` console lines are the 06-06 Blocker 1 fix c defensive swallow (mid-game placeholders fail until Phase 7; the game keeps flowing); the `Error-fetch: unable to load 'tbd_aconitase'` line is PyMOL's own fetch print — NOT silenced. The optional status-bar placeholder hint was skipped (would need a new controller->view failure channel — over-engineering for Phase 6).
- **Post-fix gates all green:** py_compile (3 UI files) exit 0; 309 unit tests OK; check_imports clean; `controller_integration_smoke` SMOKE_RESULT: PASS; `hero_highlight_smoke` PASSED; `probe_first_operator` SMOKE_RESULT: PASS.
- **5 OPEN items recorded (do NOT resolve unilaterally):** (1) cycle-trap presented as an OPTION vs the user's expectation of a RESULT — design decision pending the user (FROZEN skeleton topology untouched: 55 nodes / 21 endings / cycle-trap as a choice); (2) fuller inline help vs links-only — pending the user; (3) citrate synthase dimer (biological assembly) loading — Phase 7 cast convention; (4) real glucose + story-like text — Phase 7; (5) per-node default color/focus (5.4 scene templates) — Phase 7; ending CG — Phase 12.

### 06-14 re-verify ROUND 2 update (2026-08-30)

The Phase 6 human-verify re-check found ONE remaining bug (SC2f) and the user RESOLVED the two open decisions (1 and 2 above). All implemented + gated green; full root cause in the committed debug session `.planning/debug/edit-prompt-empty-stash.md` (commit a0eb04d); details in 06-14-SUMMARY.md "Re-verify round 2".

- **SC2f FIXED (68631dd):** root cause — the edit:offer→request_edit routing existed ONLY in ChoicePanel._render_mixed (the mixed-weighted mode; designed when tca.shuffle was the only edit-offering node); the Phase 5.1 replan added edit:offer choices to 13 PURE-MC enzyme nodes without extending the panel, so gly.pfk's edit button advanced via generic choose(i) → edit.prompt with the enzyme stash never set → build_edit_intent RuntimeError. Fix: _render_pure special-cases the DUAL predicate (`edit:offer` tag OR goto == edit.prompt) → request_edit(_current_enzyme_id()), mirroring _render_mixed — the stash is set at the SOURCE node before the goto. Guard rails: formalized probe test at gly.pfk + NEW graph invariant (every edit-offering node carries edit:enzyme:<id>) + 4 smoke checks at gly.pfk.
- **Latent-seam hardening (245f88f):** (B1) _pending_edit_enzyme_id (+ new _pending_edit_source_node_id) cleared after a SUCCESSFUL apply — both the controller.apply_edit and the MainWindow inline post-apply paths — no stale stash can supply an enzyme for a future unrelated dialog. (B2) EditDialog Cancel no longer strands the player at the empty edit.prompt panel: request_edit stashes the SOURCE node id (controller-side attribute — NO skeleton change, NO save impact); on Cancel the MainWindow shows a status message + one "Return to the enzyme" button (ChoicePanel.render_single_action) wired to the new controller.return_to_edit_source (engine.goto of the stashed source, clearing both stashes). (B3) edit_dialog.submit() + the stale 06-08 deviation note updated to reality (apply_edit's intent.enzyme_id fallback landed; submit works but is unused by MainWindow).
- **Decision 1 APPLIED — tca.shuffle auto-resolve (81b1a48, feat):** user verbatim "decision 1 auto fire. also the soul jump shouldnt be a decision too, its rng jump success or keep on loop or out as CO2." Implemented in the CONTROLLER (engine stays pure domain) at the _render CHOKE POINT (all entry paths funnel through it: start/choose/take_choice/request_edit/apply_edit/load/return_to_edit_source) via _auto_resolve_shuffle with a re-entrancy guard; entry methods RETURN the POST-auto-resolve TurnResult; the superseded shuffle turn is NOT rendered (auto-resolves pre-render — the ChoicePanel mixed mode is dead-for-shuffle, kept). Flow: (1) trap cond met (visits > 5) → AUTO-FIRE take_choice(trap) as a RESULT → bad.cycle_trap_host_death + bad_ending achievement; (2) FIRST entry (visit_counts == 1 — survives save/load via GameState, NO new state) → the aconitase edit offered ONCE via the NEW injected edit_offer_fn (defaults to prompt_fn; MainWindow wrapper titled "The wheel is about to turn") → yes: request_edit("tca.aconitase"); (3) else AUTO-SPIN choose(0) — the engine RNG picks among the WEIGHTED choices ONLY (pick_choice ignores the index; debugger-verified 40 seeds landed exclusively on the approved 0.5/0.5 co2_turn outcomes; exactly one draw per spin). HARD CONSTRAINTS HONORED: NO new RNG outcomes/weights (TCA-RNG-WEIGHT-01 0.5/0.5 only); NO skeleton JSON change (55 nodes / 21 endings untouched; the trap choice stays in the JSON — the controller auto-takes it); co2_turnX nodes KEEP their player buttons. PHASE 7 CONTENT ITEM (recorded, not resolved): a shuffle → end.normal.co2 early-exit edge was considered and REJECTED this round (RNG weights are high-stakes per-claim approval); Phase 7 approves TCA weights and may add the edge then.
- **Decision 2 APPLIED — help expansion DEFERRED to Phase 11 (f413dbb):** user decision 2026-08-30 — the richer in-GUI step-by-step help is Phase 11's (documentation finalization); a visible placeholder pointer card was added to c14/data/help.json ("More detailed step-by-step guidance is planned for a later update (Phase 11 -- documentation finalization)."), rendered by HelpDialog with zero code change; the wiki links stay as further reference.
- **Round-2 gates all green:** py_compile (4 UI files) exit 0; **322 unit tests OK** (309 prior + 13 new: 1 probe + 1 graph invariant + 4 seam + 7 auto-resolve; the shuffle unit tests adapted to the auto-resolve semantics); check_imports clean; `controller_integration_smoke` SMOKE_RESULT: PASS (60 checks — stage 6 rewritten for auto-spin/auto-fire + same-seed determinism `co2_turn2 == co2_turn2`; stage 7 BFS walk records the shuffle milestone from the choice target, still reaches end.true in 28 steps); reachability 19/19 (topology untouched).
- **REMAINING round-2 GUI re-check (still human_needed until done):** (1) edit at gly.pfk opens the "Edit gly.pfk" dialog; (2) the shuffle auto-spins without a Spin button (one-time offer on the first entry); (3) the trap auto-fires as an ENDING after 6 wheel visits (no trap button); (4) Cancel on the EditDialog returns to the enzyme (no stranded empty panel). Open items 3-5 from round 1 are unchanged (Phase 7 content).

### 06-14 Task 1 update (2026-08-30)

Phase 6 (Qt UI MVP) CAPSTONE — Plan 06-14 Task 1 COMPLETE ✓ (Task 2 human-verify PENDING): the Phase 6 capstone headless integration smoke `tools/controller_integration_smoke.py` SMOKE_RESULT: PASS (58/58 stage checks) proving the FULL Controller path end-to-end against the REAL PyMOL 2.5.0 cmd + the REAL molops stack (AssetManager + EditOps + ProtonationManager + MolOps) + the REAL story_glucose graph + real EditRouter + real AchievementBoard, with a MockView (NO Qt). 10 stages: (4) start glucose seed=42 -> intro.preface; hero highlight dispatched (hero_atom=3 atoms from bundled _smoke.pdb via 06-01 load-branch fallback, hero_cyan color, YOU label via cmd.iterate; achievements glucose_tried+first_game); (5) walk to pyr.branch; 06-05 cond fix verified (aerobic eligible via `not flags.get('host_o2_low')`, anaerobic not) -> pyr.pdh; (6) walk to tca.shuffle; is_mixed_weighted_node True; spin+return 5x (visits=6); cycle-trap cond met; take_choice via engine.goto (06-06 Blocker B) -> bad.cycle_trap_host_death (Bad ending) + bad_ending achievement; (7) fresh controller seed=7; BFS-distance-guided walk (dist precomputed backward from end.true, cond-IGNORED; dist[intro.preface]=28) reaches end.true in 28 steps; milestone path asserted in-order (intro.preface -> ... -> tca.shuffle -> tca.co2_turn1 -> tca.isocitrate_dh -> ... -> etc.atp_synthase -> end.true) + true_ending achievement; (8) save/load round-trip (06-03) at gly.g6p; view captured (18 floats), current_node/seed/rng_state match, view applied, scene rebuilt; (9) bulk-download runner (06-10) missing_large_pdbs returns [] for placeholder cast. Mid-game pdb:XXX loads fail offline (network) — controller's defensive try/except (06-06 Blocker 1 fix c) degrades gracefully. DEVIATIONS: 2 auto-fixed bugs (Rule 1) — (1) HeroResolver pre-pass count crashes on not-yet-loaded hero object (count_fn called BEFORE on_enter load dispatches; bare cmd.count_atoms on non-existent object RAISES CmdException per 03-01; fix: try/except -> n=0 -> ambiguous -> prompt; c14/ui/controller.py 3a661f5); (2) plan's prescribed label selector `count_atoms('hero_atom and label')` invalid (`label` is atom PROPERTY not selection keyword; fix: cmd.iterate reading label property, mirrors hero_highlight_smoke.py:191-194). 309 tests green; AST gate clean. 2 task commits 3a661f5 (fix) + ad95068 (smoke) + docs 309343d (SUMMARY). Task 2 (checkpoint:human-verify) PENDING — requires real Windows PyMOL 2.5.0 session (SC1-5: install/menu/window, hero highlight, True+Bad endings+save/load, bulk-download, achievements+help); the WSL agent CANNOT run Qt/GUI per AGENTS.md.

### 06-13 update (2026-08-30)

Phase 6 (Qt UI MVP) IN PROGRESS — Plan 06-13 COMPLETE ✓ (2/2 tasks): built the in-game help dialog (DOC-03). `c14/data/help.json` = curated help content (FROZEN-verbatim from 06-RESEARCH-persistence-achievements.md Pattern 6): 4 editing pointers (Point mutation / Substrate edit / Protonation change / Restore safety net — the Phase 4 EDIT-01/02/03/05 mechanics in plain language) + 3 PyMOL wiki links (Alter, H_Add, Fetch). `c14/ui/help_dialog.py` = `HelpDialog(QtWidgets.QDialog)` that reads help.json via `c14.paths.data_path` (cwd-independent), renders the editing pointers as rich-text QLabels (word-wrapped) + the wiki links as QPushButtons opening the user's browser via `QtGui.QDesktopServices.openUrl` (the Qt way, cross-platform inside PyMOL — never os.system/webbrowser), wrapped in a QScrollArea + a Close button. Late-binding closure bug avoided via default-arg lambda capture (`lambda _=False, u=link["url"]: QtGui.QDesktopServices.openUrl(QtCore.QUrl(u))`); `link["note"]` shown as tooltip. Gate-EXEMPT (c14/ui/ in check_imports.py SKIP_DIRS — may import pymol.Qt); py_compile clean; AST gate clean (exit 0). Importing in WSL FAILS (no Qt) — EXPECTED; only py_compile is WSL-verifiable; the functional dialog (pointers render, links open in browser) is human-verify in 06-14. Webfetch re-confirmed all 3 wiki URLs resolve LIVE 2026-08-30 (Alter→Iterate page, H_Add, Fetch) — no fabricated URLs; no speculative additions (Show/Select/Sort NOT added — each would need its own live-fetch verification per Open Question 3). DOC-01/DOC-02 (dramatic cast list + slogan) NOT scoped — Phase 9 (cast not populated yet); help.json is ONLY editing pointers + wiki links. NO new dependencies (pymol.Qt + stdlib json, already approved). Python 3.6 compatible (no f-strings, `.format()`). 2 task commits cbafb49 (help.json) + 96dcc1a (help_dialog.py) + docs 5b91964 (SUMMARY); DEVIATIONS: none (plan executed exactly as written; `setWordWrap(True)` + QScrollArea NoFrame + resize(540,480) are within the plan's "UI simple/user-friendly" latitude + the explicit "wrap in QScrollArea if overflows" guidance — not behavior-changing). Unblocks 06-14 (functional human-verify) + the main-window controller (06-06+) which can construct `HelpDialog(parent=main_window)` and wire a Help button to it (self-contained: loads its own data, only `parent` arg).

### 06-04 update (2026-08-29)

Phase 6 (Qt UI MVP) IN PROGRESS — Plan 06-04 COMPLETE ✓ (2/2 tasks): built the pure-Python AchievementBoard domain module (ACH-01 + ACH-02). `c14/achievements.py` = `AchievementBoard` + `ACHIEVEMENT_CATALOG_V1` (6 v1 achievements: first_game, glucose_tried, true_ending, bad_ending, good_ending, normal_ending) + `TIER_ACHIEVEMENT_ID` (true/good/normal/bad -> tier achievement). `on_turn(turn_result, character, is_new_game)` detects endings (`node.is_ending` tier -> tier achievement + `endings_found` deduped by node_id), records `characters_tried` (deduped) + `branches_discovered` (deduped by node.id), unlocks `first_game` + `<character>_tried` on a new game; idempotent; auto-saves to `user_data_path("achievements.json")` (survives PyMOL restarts + plugin reinstalls — ACH-02; OUTSIDE the plugin install dir; indent=2 + trailing newline + parent-dir makedirs, mirrors SaveStore persist.py:38-65) ONLY when something changed. Collection-based NOT a leaderboard (ACH-01 — schema has NO score/rank/points; achievement entries have only id/name/description/date). `_unlock` returns bool for change-tracking (`self._unlock(...) or changed` ordering — NOT `changed or self._unlock(...)` — keeps the side effect live since Python `or` short-circuits the RIGHT operand). `_unlock` no-ops on unknown ids (catalog lookup misses) so Phase 8 fatty_acid_tried/alcohol_tried expansion = just add tuples to ACHIEVEMENT_CATALOG_V1, no on_turn/_unlock change; the character is still recorded in characters_tried regardless. Decoupled: the controller (06-06) calls board.on_turn; the engine stays pure-domain (06-RESEARCH Open Question 6). Forward-compatible .get merge on load (partial/older-schema JSON defaults missing keys to empty lists — no migration). FROZEN-verbatim from 06-RESEARCH-persistence-achievements.md Pattern 3 (lines 204-280) + plan-endorsed _unlock-returns-bool refinement. Pure-Python (json/os/datetime + c14.paths); AST gate clean (exit 0); py_compile clean. `tests/test_achievements.py` = `TestAchievementBoard` (15 tests): each unlock type (first_game/no-first-game-mid-playthrough/true/bad/good/normal/branches), 3 idempotency forms (same ending twice / same node twice / same character new game twice), unknown-character no-op (fatty_acid_tried NOT unlocked but character recorded), persistence round-trip, backward-compat partial JSON, parent-dir creation, collection-based-not-leaderboard, ISO-UTC date (3.6-compatible strptime with/without microseconds); FakeNode/FakeTurn duck-typed mocks + temp-dir paths (never touches real user data dir). Full suite green: 273 tests (258 prior + 15 new) — no regression; AST gate clean. 2 task commits 7eef2c9 (feat: achievements.py) + 4c20b92 (test: test_achievements.py) + docs 61c8113 (SUMMARY); DEVIATIONS: none (plan executed exactly as written; _unlock-returns-bool was explicitly plan-endorsed). Unblocks 06-06 (controller calls board.on_turn after each engine.start/choose/apply_player_edit) + 06-12 (achievements dialog reads the JSON / AchievementBoard.data; ACHIEVEMENT_CATALOG_V1 gives the full set for locked-vs-unlocked rendering); Phase 8 catalog expansion needs no on_turn/_unlock change. Depends on 06-02 user_data_path (DONE). No blockers.

### 06-10 update (2026-08-30)

Phase 6 (Qt UI MVP) IN PROGRESS — Plan 06-10 COMPLETE ✓ (3/3 tasks): built the one-time bulk-download prompt (CAST-04). `c14/data/cast.json` schema EXTENDED with `source` (bundled|download) + `pdb_id` + `character` per enzyme; fixture_enzyme_1 marked source=bundled; PLACEHOLDER_large_enzyme (source=download, pdb_id=PLACEHOLDER_PDB, character=glucose, claim_id=PLACEHOLDER_PHASE7) added as Phase 7 placeholder — NO fabricated PDB IDs (05.4 no-fabricated-science; the real per-enzyme large-PDB list stays MINIMAL/PLACEHOLDER for Phase 6; Phase 7 fills it). `c14/ui/bulk_download.py` = Qt-FREE runner (gate-EXEMPT dir but imports NO pymol.Qt — cmd injected like AssetManager; importable in pure WSL python3.6): `missing_large_pdbs(cmd, cast_path=None)` (PLACEHOLDER guard skips pdb_id.startswith("PLACEHOLDER"); Pitfall 3 lowercase filename check `pdb_id.lower()+".pdb"` against `c14.paths.data_path("data","assets","downloaded")`; idempotent-cache respected; returns [(pdb_id, object_name=enzyme_id, enzyme_id, character)]), `run_bulk_download(cmd, missing_codes, on_progress=None, on_cancel_check=None)` (per-file `AssetManager.fetch_pdb` loop — RuntimeError on count_atoms<=0 = failure signal; loop CONTINUES past a failure; cancel BETWEEN fetches via on_cancel_check — NOT mid-fetch; returns {completed, failed, canceled}), `characters_to_lock(failed, cast_path=None)` (Pattern 2 per-character lock — maps failed (pdb,enzyme,character,err) -> set of character ids; cast.json fallback if character is None), `expected_download_characters(cast_path=None)` (lock UI universe). Delegates to AssetManager.fetch_pdb (Pitfall 5 mitigations type=/async_=0/path=<abs> baked in); NEVER calls cmd.fetch directly (Anti-Pattern). `c14/ui/bulk_download_dialog.py` (gate-EXEMPT; `from pymol.Qt import QtCore, QtWidgets`) = `BulkDownloadDialog(QProgressDialog)` wrapping the Qt-free runner: per-file progress (label "Downloading <pdb_id> (i+1 of N)...") + `QApplication.processEvents()` BETWEEN fetches (NOT per-byte — cmd.fetch(async_=0) is sync + blocking with NO progress callback, importing.py:1386-1393 — Pitfall 1; per-file is CORRECT not a bug) + cancel-between-fetches (Pitfall 7; button label "Cancel (after current file)") + `maybe_run_bulk_download(controller, cmd, parent)` helper (one-time-prompt entry point for MainWindow._new_game 06-08: no-missing -> no prompt [game starts instantly; SC4 bundled]; on failed -> characters_to_lock + `_apply_lock` [duck-typed controller.lock_characters] — locks ONLY affected characters [glucose stays playable]; on canceled -> QMessageBox Retry? -> re-run with recomputed still-missing [free idempotent cache] OR lock still-missing characters [SC4]). `tests/test_bulk_download.py` = 11 pure-WSL MockCmd unit tests (no pymol/Qt import): PLACEHOLDER skip, missing/existing detection (Pitfall 3 lowercase via temp downloaded dir + monkeypatched c14.paths.data_path), success, failure-recorded-continues (middle obj fails), cancel-between-files, on_progress (i,total,pdb_id), characters_to_lock mapping+dedup+empty-when-no-failure (SC4), Qt-free import (runtime gate twin). Full suite green (284 tests); AST gate clean (exit 0); py_compile both UI files clean; importing bulk_download_dialog in WSL FAILS (no Qt) — EXPECTED (py_compile only); functional dialog is human-verify in 06-14. 3 task commits a173af1 (cast+runner) + 2616673 (tests) + 94f774d (dialog) + docs a234fbd (SUMMARY). DEVIATIONS: 3 (all Rule 2 missing-critical auto-additions for correct UX/SC4, no scope creep) — (1) added `self.show()`+processEvents at run() start [QProgressDialog auto-show delay would hide the bar during a fast fetch loop]; (2) implemented cancel-no-retry lock of still-missing characters [plan only specified Retry?-yes; SC4-correct: lock only affected characters]; (3) added bonus 11th test (characters_to_lock dedup — plan said 10). 1 inline test-assertion fix before commit (AssetManager.fetch_pdb RuntimeError formats with pdb_id `code` not object_name — asset_manager.py:125). Python 3.6 compatible (no f-strings, .format()). Unblocks 06-08 (MainWindow._new_game calls maybe_run_bulk_download) + 06-14 (human-verify) + Phase 7 (real per-enzyme large-PDB list; PLACEHOLDER guard auto-lets real entries through). object_name=enzyme_id convention is a Phase 7 decision point.

### 06-12 update (2026-08-30)

Phase 6 (Qt UI MVP) IN PROGRESS — Plan 06-12 COMPLETE ✓ (1/1 task): built the achievements board dialog (ACH-01/ACH-02 UI; SC5 renderer). `c14/ui/achievements_dialog.py` = `AchievementsDialog(QtWidgets.QDialog)` (gate-EXEMPT; `from pymol.Qt import QtCore, QtGui, QtWidgets` + `from c14.achievements import ACHIEVEMENT_CATALOG_V1` — that module is pure-Python/gate-clean so the AST gate stays clean). READ-ONLY renderer reading `AchievementBoard.data` (06-04) at `__init__` (snapshot at dialog-open; the board is kept current by the controller's `on_turn` during gameplay): summary line "Characters tried: {n}/3 | Endings found: {n} | Branches discovered: {n}" (collection COUNTS, NOT scores — ACH-01 anti-leaderboard) + "Unlocked (x of 6)" section cross-referencing `ACHIEVEMENT_CATALOG_V1` (unlocked entries show name + description + date; locked entries greyed "Name — Locked", description hidden — no spoilers; stable catalog display order) + "Endings found" section (Tier: node_id (character, date); tier capitalized) + "Characters tried" line (", ".join or "None yet.") + Close button; QScrollArea wrapper (overflow). NO unlock buttons (unlocks are gameplay-driven; the board auto-persists via 06-04); NO file I/O of its own. Cross-session (ACH-02): reads the board's persisted data (board constructed at MainWindow init [06-08] with default `user_data_path("achievements.json")` + `_load()` — prior-session unlocks show automatically). Python 3.6 compatible (no f-strings, `.format()`). py_compile clean; AST gate clean (exit 0). Importing in WSL FAILS (no Qt) — EXPECTED; functional board (unlocks show after playthroughs, persists across PyMOL restarts, greyed locked entries fill in) is human-verify in 06-14 (SC5). Phase 8 catalog expansion (fatty_acid_tried/alcohol_tried added to `ACHIEVEMENT_CATALOG_V1`) auto-renders with NO code change here. DOC-01/DOC-02 (dramatic cast list + slogan) NOT scoped — Phase 9. 1 task commit 1b3a4d2 + docs 0de16d6 (SUMMARY); DEVIATIONS: none (catalog cross-reference was an explicitly-allowed optional enhancement in the plan's `<action>`; `.get` defensive reads + `_date_only` helper + `resize(540,480)` match the HelpDialog sibling convention).

### 06-11 update (2026-08-30)

Phase 6 (Qt UI MVP) IN PROGRESS — Plan 06-11 COMPLETE ✓ (1/1 task): built the save/load dialogs (SAVE-01/02 UI + SC3 path collection). `c14/ui/save_load_dialogs.py` (gate-EXEMPT; `from pymol.Qt import QtWidgets` + `from c14.paths import user_data_path`) = THIN `QFileDialog` wrappers: `ask_save_path(parent, default_name="save.json")` (Save dialog defaulting to `user_data_path("saves")/save.json`, filter `RPG Save (*.json)`, returns path or None) + `ask_load_path(parent)` (Load dialog defaulting to `user_data_path("saves")`, same filter, returns path or None) + `_saves_dir()` helper (resolves `user_data_path("saves")` to str + idempotent `os.makedirs(exist_ok=True)` so the first save "just works"). The MainWindow (06-08) wires: Save button -> `path = ask_save_path(self); if path: self._controller.save(path)`; Load button -> `path = ask_load_path(self); if path: self._controller.load(path)`. The controller's save/load (06-06) call engine.save/load (06-03: captures view via view_provider, restores via view_applier AFTER on_enter replay). ANTI-PATTERNS AVOIDED: NO `.pse` session saved (Anti-Pattern 5 — persist.py:1-9; the scene rebuilds on load via on_enter replay, a pure function of game state); NO manual scene re-apply after `engine.load` (double-dispatch — 06-RESEARCH-ui-adapter.md Pitfall 3; the engine already replays on_enter); NO hardcoded save dir (uses `user_data_path` so saves survive reinstalls — 06-RESEARCH-persistence-achievements.md Pitfall 5); NO f-strings; does NOT block the GUI (fast JSON ops handled by controller/engine; the dialog is a native modal QFileDialog). Implemented Pattern 4 verbatim from 06-RESEARCH-persistence-achievements.md (lines 344-367); docstring expanded to match sibling dialogs (help_dialog.py / achievements_dialog.py conventions). Python 3.6 compatible (no f-strings, `.format()`, `# type:` comments). py_compile clean (exit 0); AST gate clean (exit 0); AST sanity confirms all 3 functions present. Importing in WSL FAILS (no Qt) — EXPECTED; only py_compile is WSL-verifiable; the functional round-trip (save mid-game, load restores exact session: story position + RNG + structures + view) is human-verify in 06-14 (SC3). Depends only on already-complete 06-02 (user_data_path) + 06-06 (controller.save/load). 1 task commit 7f4e5cd + docs bd3ea7c (SUMMARY); DEVIATIONS: none (plan executed exactly as written; docstring expansion is within "traceable, clean" convention, no behavioral change).

### 06-07 update (2026-08-30)

Phase 6 (Qt UI MVP) IN PROGRESS — Plan 06-07 COMPLETE ✓ (3/3 tasks): made `c14` loadable as a PyMOL 2.5.0 plugin (PLGN-01 + PLGN-02 packaging). `c14/__init__.py` (the ONE allowed modification to the pure-Python `c14/` root in Phase 6) gained a `# Version: 0.0.1 / # Author / # Citation-Required: No` metadata block (top of file; parsed by `PluginInfo.get_metadata` for the Plugin Manager Info dialog) + `__init_plugin__(pmgapp)` whose body is FROZEN-verbatim ONLY: `from .paths import selfcheck` / `selfcheck()` / `from .ui import plugin_entry` / `plugin_entry.init_plugin(pmgapp)` — the ONLY gate-clean path (ast.walk recurses into function bodies so even a function-local `import pymol` would be flagged; `paths`+`ui` are NOT in BANNED_TOP). `c14/ui/plugin_entry.py` (gate-EXEMPT; `c14/ui/` in SKIP_DIRS) = `init_plugin(pmgapp)` registering the "RPG: Tale of C" menu item via `pymol.plugins.addmenuitemqt` (plugins/__init__.py:100) + `_open_main_window()` lazily constructing + re-showing the `MainWindow` singleton on first click (all `pymol.Qt`/`pymol.plugins` imports deferred to function bodies — matches dynoplot.py:445-447; cheap menu registration at load, expensive QMainWindow construction deferred to click time). The `from .main_window import MainWindow` import is INSIDE `_open_main_window` so this file `py_compile`s even though `c14/ui/main_window.py` (06-08) does not yet exist — this plan executed BEFORE 06-08. `tools/build_plugin_zip.sh` = bash orchestrator producing `dist/c14-<version>.zip` with Case-1 layout (`c14/__init__.py` is the first entry; PLGN-02; plugins/installation.py:90-143), bundling `data/story_glucose/` into `c14/data/story_glucose/` (runtime resolves via `data_path`), excluding `__pycache__`/`*.pyc`/`c14/data/assets/downloaded/` (Pitfall 4), with a post-build sanity check asserting first entry == `c14/__init__.py` + required entries present + no forbidden entries + single top-level `c14/` dir. VERIFICATION all GREEN: py_compile (c14/__init__.py + c14/ui/plugin_entry.py) exit 0; AST gate `tools/check_imports.py` exit 0 (CRITICAL — the gate stays green; the only Import/ImportFrom nodes in c14/__init__.py are the 2 relative `from .paths`/`from .ui`); `import c14` in pure WSL works + `__init_plugin__` callable WITHOUT touching Qt (pymol/PyQt5/c14.ui.plugin_entry NOT in sys.modules after import — the function is DEFINED not CALLED); `bash tools/build_plugin_zip.sh` exit 0 producing `dist/c14-0.0.1-dev.zip` (46 entries, ~96 KB; version read from `__version__="0.0.1-dev"`); independent `python3.6 zipfile` re-check confirms all 5 plan-verify conditions (first=c14/__init__.py, 0 downloaded/, 0 .pyc, manifest.json present, plugin_entry.py present, ui/__init__.py present). The actual menu + window appearance is human-verify in 06-14 (Qt cannot run in WSL — no display; plugin_entry.py is py_compile-only in WSL per AGENTS.md). NO new dependencies (python3.6 stdlib only). Python 3.6 compatible (no f-strings). 4 task commits f223c8f (plugin_entry.py) + cd44ecd (c14/__init__.py __init_plugin__) + e914e78 (build_plugin_zip.sh) + c7f31b8 (chore: .gitignore dist/) + docs 3d6e88d (SUMMARY). DEVIATIONS: 2 (both auto-fixed): (1) Rule 3-Blocking — `zip`/`unzip` CLIs not installed in WSL (`which` exit 1; AGENTS.md forbids apt install) → build script delegates staging+zip+sanity to `python3.6` stdlib `shutil`+`zipfile` (PyMOL's installer `installation.py:extract_zipfile` is also Python zipfile → installer-native; no new deps; deliverables match the plan exactly, only the mechanism changed); (2) Rule 2-Missing Critical — `dist/` was untracked (`git check-ignore` exit 1) → added `dist/` to `.gitignore` (5-line block) to prevent accidental commit of the ~96 KB build artifact during the 3 parallel executor sessions (06-08/09/11 active). Unblocks 06-08 (MainWindow is the deferred-import target of `_open_main_window`) + 06-14 (human-verify install: Plugin → Plugin Manager → Install New Plugin → pick dist/c14-0.0.1-dev.zip → restart PyMOL → "RPG: Tale of C" menu appears → click → MainWindow opens).

### 06-09 update (2026-08-30)

Phase 6 (Qt UI MVP) IN PROGRESS — Plan 06-09 COMPLETE ✓ (1/1 task): built the EditDialog (SC1 "edit interface" + EDIT-01/02/03 UI). `c14/ui/edit_dialog.py` = `EditDialog(QtWidgets.QDialog)` (gate-EXEMPT; `from pymol.Qt import QtCore, QtGui, QtWidgets` + `import c14.paths`) — the curated edit-options dialog. CURATED, NOT free-form chemistry (PROJECT.md:80 lookup-table + bad-ending fallback): reads `c14/data/edits.json` via `c14.paths.data_path` (cwd-independent) for the current `enzyme_id`'s known edits (each entry's `signature` {op,target,args} + `branch_node`) -> labeled "Restore <target> to <new_res> (the correct fix)" radio options whose `(op,target,args)` reproduces the table signature EXACTLY (known -> branch via EditRouter) + a few plausible WRONG options hardcoded for Phase 6 (generic point mutations `resi 1->ALA` / `resi 2->GLY` using standard amino-acid codes — NOT fabricated disease mutations; don't match the table -> bad-ending pool, a valid game outcome). NO free-form residue-entry (would always route to bad pool — unfun + not the curated-table design). `selected_edit()` returns the selected `(op,target,args)` tuple or None; `submit(controller, enzyme_id, parent=None)` static helper bundles `controller.build_edit_intent(op,target,args)` + `controller.apply_edit(intent)` -> returns TurnResult (or None on cancel). Enzyme with NO known edits (Phase 7 fills them) shows an informational message ("No known edits for this enzyme yet (Phase 7 content). Your edit will route to the bad-ending pool.") + a single generic wrong option (routes to bad pool — valid outcome). Phase 6 placeholder `fixture_enzyme_1` (1 known edit: point_mutation `resi 1 -> GLY`, claim_id PLACEHOLDER_PHASE5) handled. `_label_for_known` covers all 3 EDIT op types (point_mutation/substrate_edit/protonation_change) for Phase 7 forward-compat. WARNING 4 file ownership honored: 06-09 owns ONLY edit_dialog.py; CONSUMES the 06-06 seam (`request_edit` + `_pending_edit_enzyme_id` + `build_edit_intent` + `apply_edit`) + the 06-08 seam (`render_turn` detects `edit.prompt` -> opens EditDialog) WITHOUT modifying controller.py or main_window.py (3 parallel wave-4 plans, non-overlapping files). VERIFICATION: `python3.6 -m py_compile c14/ui/edit_dialog.py` exit 0 (syntax); `python3.6 tools/check_imports.py` exit 0 (AST gate clean — c14/ui/ is in SKIP_DIRS so the pymol.Qt import is allowed). Importing in WSL FAILS (no Qt) — EXPECTED for a gate-EXEMPT Qt file; functional flow (curated options render, selection builds EditIntent whose signature() routes via EditRouter known->branch / unknown->bad-pool) is human-verify in 06-14. SEAM TENSION (documented, NOT a 06-09 deviation): 06-06's `Controller.apply_edit` reads enzyme_id from the CURRENT node's `edit:enzyme:<id>` tag + raises RuntimeError when absent (test_controller.py #6); at `edit.prompt` (tag `edit:prompt`, not `edit:enzyme:<id>`) it would raise. `build_edit_intent` DOES fall back to `_pending_edit_enzyme_id` at edit.prompt (test #17), but `apply_edit` does NOT. The 06-08/06-09 seam calls `apply_edit` at edit.prompt; resolving it needs a one-line fix in 06-06's controller.py (apply_edit falls back to the stash or uses edit_intent.enzyme_id) — OUT OF SCOPE for 06-09 (Warning 4). 06-09's `submit` calls the declared seam verbatim; the EditDialog + selected_edit API is correct either way. 1 task commit d6aec19 + docs af1b2e1 (SUMMARY). DEVIATIONS: none (plan executed exactly as written; the seam tension is a pre-existing cross-plan concern between 06-06 and 06-08, consumed not owned by 06-09).

### 06-08 update (2026-08-30)

Phase 6 (Qt UI MVP) IN PROGRESS — Plan 06-08 COMPLETE ✓ (2/2 tasks): built the SC1 core UI surface — `c14/ui/widgets.py` (StoryPanel + ChoicePanel) + `c14/ui/main_window.py` (MainWindow + StartDialog). Both gate-EXEMPT (`c14/ui/` in check_imports.py SKIP_DIRS; `from pymol.Qt import QtCore, QtGui, QtWidgets` + `from pymol import cmd`). StoryPanel = text_dramatic (14pt) + text_teaching (10pt gray, QScrollArea-wrapped) STACKED (not tabbed — both layers show at once) + an ending banner; handles empty/TBD dramatic text with a placeholder. ChoicePanel = a DUMB RENDERER over the 06-06 Controller (NO cmd.* / cond-eval / RNG-pick / QMessageBox — all delegated): 3 modes — ending ("Start a new game" button -> `new_game_requested` Qt signal -> MainWindow._new_game), mixed weighted+non-weighted (tca.shuffle: "Spin the wheel" -> `controller.choose(0)` RNG picks among weighted + edit:offer -> `controller.request_edit(controller._current_enzyme_id())` + cond-gated cycle-trap -> `controller.take_choice(choice)`, greyed-out until `visits > 5` — iterates ALL non-weighted so the trap shows greyed before its cond is met), pure-MC (one button per eligible choice -> `choose(index)`) / pure-weighted (single Spin + "(luck decides)" info label). Eligibility EXACTLY matches the engine's `pick_choice` filter (both call `interpreter._cond`), so `choose(index)` maps 1:1. Late-binding closure avoided via `lambda _=False, c=choice/i=index` default-arg capture (matches help_dialog.py:85-87). MainWindow (QMainWindow) constructs the molops stack (real cmd + AssetManager + EditOps + ProtonationManager + MolOps + EditRouter over `c14/data/edits.json` via `EditsTable.load`) + AchievementBoard (default `user_data_path("achievements.json")` for ACH-02 cross-session) + Controller (view=self); toolbar [New/Save/Load/Achievements/Help]; central StoryPanel+ChoicePanel; status bar (node/character/seed). `prompt_fn` = QMessageBox.question wrapper (the OQ-6 warn+confirm gate — 05.4 OQ-6 OVERRIDE; passed to the controller so molops stays pure); `count_fn`/`view_provider`/`view_applier` injected (cmd.count_atoms / cmd.get_view / cmd.set_view). StartDialog = modal; Glucose enabled (FA/Alcohol disabled "future phase" tooltip per 05.1 fa.stub/alc.stub); seed QLineEdit (blank=random/play; int=demo; invalid -> warn + veto accept). `render_turn` (the controller->view seam): ending -> render_ending + "Ending reached: <tier>" status; edit.prompt -> WARNING 4 seam (open EditDialog 06-09 with `controller._pending_edit_enzyme_id`, on accept `build_edit_intent` + route); normal -> render_node + choices + node/char/seed status. All 4 sibling dialogs (edit_dialog/save_load_dialogs/achievements_dialog/help_dialog) imported DEFERRED inside handlers so main_window.py py_compiles without them (parallel-execution safety; resolves at click time). DEVIATIONS: 2 — (1) [Rule 1 Bug] the plan prescribed `controller.apply_edit(intent)` at edit.prompt but it RAISES there (edit.prompt has no `edit:enzyme:` tag; `apply_edit` re-reads the enzyme_id from the current node — verified by `test_apply_edit_raises_when_no_enzyme_tag`; 06-09's EditDialog.submit docstring confirms the same bug + defers the real fix to 06-06's controller, out of scope for 06-08+06-09). Fixed in 06-08 by routing via `engine.apply_player_edit(intent, intent.enzyme_id)` + `_record_achievement` + `_render` directly (mirrors apply_edit's logic but uses the EditIntent's stashed enzyme_id; 06-06 controller + 06-09 dialog UNMODIFIED). (2) [Rule 3 Blocking] `_resolve_story_dir()` falls back to repo-root `data/story_glucose` in dev (c14/data/story_glucose is absent until the 06-07 zip build bundles it; without the fallback the MainWindow crashes in a bare checkout). VERIFICATION all GREEN: py_compile (widgets.py + main_window.py) PASS; AST gate (check_imports.py) exit 0; 308 tests green (no regression — controller untouched). Importing in WSL FAILS (no Qt/pymol) — EXPECTED; functional UI is human-verify in 06-14. Verified compatible with the now-committed 06-09 EditDialog (`EditDialog(controller, enzyme_id, parent)` + `selected_edit()` -> (op,target,args)) + 06-11 save_load_dialogs (`ask_save_path(parent)` + `ask_load_path(parent)`) APIs. 2 task commits 5e0845a (widgets) + f7b713b (main_window) + docs d6ac240 (SUMMARY).

## Performance Metrics

**Velocity:**
- Total plans completed: 44
- Average duration: ~25 min
- Total execution time: ~12 hours

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 1. Foundations & Citation Gate | 3/3 ✓ | 56 min | 19 min |
| 2. Story Engine Core | 5/5 ✓ | 78 min | 16 min |
| 3. PyMOL cmd Layer | 3/3 ✓ | 118 min | 39 min |
| 4. Editing + Protonation + Restore | 5/5 ✓ | 250 min | 50 min |
| 5. Pre-Content Key Decisions | 5/5 ✓ | 34 min | 7 min |
| 5.1 Story Graph Design (INSERTED) | 16/16 ✓ | ~121 min (early plans) | — |
| 5.2 Bad-Ending Extensibility (INSERTED) | 1/1 ✓ | 22 min | 22 min |
| 5.3 WT-Aligned Structure Load (INSERTED) | 2/2 ✓ | 6 min (01) + multi-session (02) | — |
| 5.4 Cast & Hero Representation (INSERTED) | 4/4 ✓ | 45 min (01) + 14 min (02) + 62 min (03) + 30 min (04) | — |

**Recent Trend:**
- Last 3 plans: 05.4-02 (14 min: hero-highlight smoke 25/25 PASS), 05.4-03 (62 min: scene-template smoke 37/37 PASS), 05.4-04 (30 min: human design-review checkpoint APPROVED-WITH-ADJUSTMENTS — 5 overrides applied, convention FROZEN, both smokes re-ran PASS)
- Trend: Phase 5.4 COMPLETE ✓ (4/4 plans). The convention `05.4-CONVENTION.md` is FROZEN (766 lines) with the 5 human overrides applied: OQ-2 magenta mutant (red confuses with oxygen), OQ-3 cyan hero + all-C-colored ball-and-stick + "YOU" label (gold confuses with sulfur; the hero is "one of them"; scoped-dim REMOVED), OQ-4 "YOU" label (player-facing), OQ-6 default+warn+confirm selection rule (the confirm IS the no-fabricated-science guard), OQ-11 future-note (3-sub-beat is a deferred future enrichment). 6 approve-rec (OQ-1/5/7/8/9/10). Both smokes re-ran SMOKE_RESULT: PASS after the cyan/magenta/YOU palette swap (hero 29/29, scene 37/37). molops.py UNTOUCHED (OQ-1 dispatch deferred to Phase 6). NO fabricated science (ZERO real PDB IDs; cyan/magenta RGB are PLACEHOLDERS pending Phase 7). Convention FROZEN for Phase 6/7/9/12. Next: Phase 6 (Qt UI MVP — implements the 3 deferred set_color/label/set dispatches + the 5.3 align dispatch + the OQ-6 warn+confirm Qt prompt).

*Updated after each plan completion*

## Accumulated Context

### Decisions

Decisions are logged in PROJECT.md Key Decisions table.
Recent decisions affecting current work:

- [Roadmap]: 12-phase comprehensive build order (10 integer milestones + 2 INSERTED decimal design phases at 5.1/5.2); Phase 5 (Key Decisions + source approval) runs parallel with Phases 2–4 and gates all content (Phases 6–9).
- [Roadmap]: Architecture = 3-tier testability layering (pure-Python domain → pymol.cmd headless layer → pymol.Qt human-verify layer); ink-inspired story graph as JSON we own; edit routing via lookup table + bad-ending fallback; citation registry as architectural no-fabricated-science gate.
- [Roadmap]: Two INSERTED design phases (5.1 Story Graph Design, 5.4 Cast & Hero Representation Design [formerly 5.2, renumbered 2026-08-22]) sit between Phase 5 decisions and Phase 6 MVP — they make the story graph topology + MC-choice/edit-node integration contracts and the 3D visual language explicit, reviewable artifacts before any Qt UI or cited content is built. Both own 0 requirements (design phases, per Phase 10 precedent); they enable downstream delivery.
- [Roadmap]: Phase 5.2 (Bad-Ending Extensibility + Flexibility, INSERTED 2026-08-22) + Phase 5.3 (WT-Aligned Experimental Structure Load Convention, INSERTED 2026-08-22) are 2 URGENT decimal phases inserted between Phase 5.1 and Phase 5.4 (formerly 5.2) per user feedback during the 05.1-06 human review checkpoint. Phase 5.2 demonstrates + tests the bad-ending extensibility convention (add "denature with a change in pH" + parametrized test). Phase 5.3 designs the WT-aligned structure load convention (restoration reveal after a correct edit, esp. for mutants with major conformational differences). Both own 0 requirements (convention/extensibility phases); old Phase 5.2 renumbered to Phase 5.4 (0 plans, no directory existed — safe renumber).
- [Roadmap]: Content is a marathon across Phases 7–9 (glucose → fatty acid + alcohol → anaerobic + full cast), dominated by per-claim approval throughput (Pitfall 7), not engineering difficulty.
- [Roadmap]: Content phases 7/8/9 are NOT split into more phases despite the heavy per-claim citation load. Instead each uses granular per-pathway-segment plans (5–7 for P7, 4–6 for P8, 4–5 for P9). Rationale: (a) per-claim approval is orthogonal to phase structure — the human approves claims individually regardless; (b) already at top of comprehensive depth (12 phases), splitting would exceed range; (c) content segments are sequential (continuity), so phases buy no parallelism; (d) phase-level verifier marginal value is low for content (citation gate already enforced at build time); (e) plans give the per-segment review cadence the user wants with less ceremony. See ROADMAP.md "Content Phase Plan Granularity".
- [Roadmap]: Phase 11 (Documentation Finalization & Verification) split OUT of Phase 10 per user feedback. Phase 10 keeps polish, playtest-driven edit-table expansion, accessibility, full manual GUI test matrix, pre-ship citation gate. Phase 11 owns: update README + in-game help to match final shipped content (cast list, slogan, PDB IDs + resolutions), verify all docs reflect shipped game, final docs verification as the last release gate. Phase 11 owns 0 new requirements (verifies/updates DOC-01, DOC-02 created in Phase 9, per the Phase 10 0-requirement precedent). Phase 11 depends on Phase 10 (content polish settled before docs finalization) — it is the LAST release gate before ship.
- [Execution 01-01]: AST-based import gate (tools/check_imports.py) adopted over grep — 0 false positives on comments/strings, catches aliased `import pymol.cmd as c`. Strict-ban policy: flags any pymol/PyQt5 Import/ImportFrom including inside TYPE_CHECKING guards. Directory location = tier (c14/ root scanned; pymol_layer/ + ui/ excluded).
- [Execution 01-01]: c14/__init__.py stays pure-Python in Phase 1 (zero pymol/PyQt5 imports); __init_plugin__ deferred to Phase 6 (lazy-delegate to c14/ui/plugin_entry.py per research Pattern 1).
- [Execution 01-01]: Python 3.6 stdlib only — unittest (pytest not installed); subprocess uses stdout/stderr=PIPE not capture_output (3.7+); py_compile + unittest paired (Pitfall 3: py_compile necessary but not sufficient).
- [Execution 01-03]: Citation gate predicate is `approval_status == "approved"` (strict equality), NOT `!= "pending"` — research Pitfall 6: `!= "pending"` would erroneously pass a `rejected` claim. A rejected claim fails identically to a pending one (no special case). Verified by the rejected fixture (exit 1, status 'rejected').
- [Execution 01-03]: Keep all three approval_status values {pending, approved, rejected} in the enum — `rejected` records provenance (why a claim was refused) rather than silently deleting. Phase 1 success criterion (names only pending/approved) fully satisfied by the `== "approved"` test.
- [Execution 01-03]: `object_pairs_hook` duplicate-key detection in CitationRegistry.load (~6 lines) — without it, json.load silently last-wins on duplicate claim_id keys, clobbering an approval/rejection. Verified raising on 3.6.9 (Pitfall 3).
- [Execution 01-03]: Three-way exit codes (0=pass / 1=missing-or-unapproved / 2=config-load-error) for the citation gate — lets CI distinguish "broken fixtures/tooling" (exit 2) from "genuinely unapproved science" (exit 1). Phase 1 success criterion only requires non-zero on fail; the split is a clean-to-have the plan adopted.
- [Execution 01-03]: `data/citations.json` lives at REPO ROOT `data/` (content source file, read at pre-ship time by the gate), NOT `c14/data/` (bundled runtime asset that ships in the plugin zip). Distinct directories with distinct roles — established early to avoid future confusion.
- [Execution 01-03]: Registry shipped as JSON object (`data/citations.json`), NOT JSONL (`data/claims.jsonl`) — research Pitfall 2: O(1) lookup, structural dup-key prevention, matches authoritative ARCHITECTURE.md. The `.jsonl` references in earlier research are superseded.
- [Execution 01-03]: Story-walker (`collect_referenced_claim_ids`) isolated in one function in `tools/check_citations.py` — Phase 2 swaps it for `c14.story.validate.collect_claim_ids("data/story/")` with zero changes to the gate's core logic (forward-compatible contract).
- [Execution 01-02]: c14.paths.data_path() is a pure `__file__`-relative resolver (NO existence check) — separation of concerns; callers handle FileNotFoundError; supports "compute where a future file would go". selfcheck() DOES check existence (raises FileNotFoundError) — the fail-loud layout invariant for plugin load (Pitfall 1 mitigation, Phase 6+ caller). Return type pathlib.Path; str(path) coerces for PyMOL cmd.* string-path APIs.
- [Execution 01-02]: CWD-independence proven via real `os.chdir(tempfile.mkdtemp())` in setUp — honest end-to-end proof, better than `mock.patch('os.getcwd')` which only proves the call site. Existence-of-fixture-from-foreign-CWD IS the proof; adopted near-verbatim from 01-RESEARCH-paths.md Pattern 2.
- [Execution 01-02]: README.md satisfies DOC-04 already (verified, NOT modified) — 01-RESEARCH-paths.md read the 57-line file in full; banner + description + TBD: Installation/References/Cast all present. Read-only verification task produced no commit (no changes; respects CITE-01 gate and later-phase content ownership).
- [Execution 01-02]: Bundled data lives inside `c14/data/` (ships in PyMOL Plugin Manager zip); distinct from repo-root `data/` (Plan 03's citations.json source/registry file, read at pre-ship time). Two directories, two roles — established early to avoid future confusion.
- [Execution 02-01]: Plain classes (NOT @dataclass) for Node/Choice/MolAction/RngEngine/GameState — 3.6.9 has no dataclasses module (Phase 1 Pitfall 1); matches CitationRegistry precedent. ARCHITECTURE.md shows @dataclass but the plan explicitly overrides for 3.6 compat; field names/semantics kept near-verbatim.
- [Execution 02-01]: MolAction is pure data (op/target/args) with NO pymol import — the testability-boundary carrier. The future c14/pymol_layer/molops.py (Phase 3/4) translates it to cmd.*. Domain tier never names a pymol type (ARCHITECTURE.md Pattern 1 / Anti-Pattern 1, enforced by the AST gate).
- [Execution 02-01]: RngEngine random-mode picks seed via `secrets.randbits(31)` (3.6-safe stdlib) and records it on .seed for replay. Single random.Random per playthrough; ALL stochastic draws go through it (Anti-Pattern 7). get_state/set_state/from_state convert tuples<->lists for JSON; verified the exact next draw survives JSON round-trip on 3.6.9.
- [Execution 02-01]: GameState stores seed + rng_state as plain data (does NOT hold a live RngEngine); the engine syncs rng.get_state() into state before save and rebuilds via RngEngine.from_state(seed, rng_state) on load. to_dict key order fixed to ARCHITECTURE.md GameState JSON for diff-stable saves; from_dict uses .get defaults for partial/older-save tolerance. finished=True (truthy bool) when ending reached, None while playing.
- [Execution 02-03]: Duck-typed _choices/_goto/_is_ending helpers (hasattr() dispatch) make check_reachability/validate_graph work on BOTH Node objects (node.choices/.goto/.is_ending) and raw JSON dicts (node.get('choices')/['goto']/['is_ending']) — single algorithm code path, no duplication. Lets the same validator serve the loader's Node objects and the gate's raw JSON fixtures.
- [Execution 02-03]: collect_claim_ids does its OWN minimal file loading (does NOT import c14.story.graph) so Plan 03 stays independent of Plan 02 for parallel execution. Accepts a file (backward-compat with Phase 1 fixtures) OR a directory (manifest.json + merge files). The minor manifest-reading overlap with graph.py is an acceptable trade for module independence.
- [Execution 02-03]: check_reachability skips edges to nonexistent nodes (graceful — can't reach what doesn't exist); validate_graph flags them as dangling_divert (structural). Separation of concerns: reachability checker reports reach; validator reports structure. Missing-start_id returns is_ok=False (graceful, no crash).
- [Execution 02-03]: Citation gate surgical refactor complete — inline collect_referenced_claim_ids replaced by c14.story.validate.collect_claim_ids; gate core logic (registry cross-ref, [MISSING]/[UNAPPROVED] report, 0/1/2 exit codes) UNCHANGED; --story accepts file OR directory. All 12 existing tests pass (zero regression — Phase 1 forward-compatible contract honored: the walker was isolated in one function precisely so this swap is surgical).
- [Execution 02-02]: StoryGraph.load(story_dir) injects the JSON object key as the node's `id` field before Node.from_dict (which requires d["id"]) — keeps the {nodes: {id: {claim_ids}}} shape the Phase 1 citation gate story-walker reads, without duplicating the id inside each node body. Loader is CWD-independent (explicit story_dir arg); all_nodes() returns the {id: Node} dict (NOT a list) as the Plan 03/05 contract.
- [Execution 02-02]: StoryInterpreter (ARCHITECTURE.md Pattern 2 near-verbatim) emits pure-data MolAction lists from enter_node — NEVER cmd.* (testability boundary proven by a full walk with a mock sink + `'pymol' not in sys.modules`). pick_choice: weighted -> single Choice via rng.random()*total + cumulative sum (single injected RngEngine, Anti-Pattern 7); non-weighted -> eligible list. _cond: restricted-namespace {flags,char,counters,visits} eval with no builtins, trusted-content model, fail-safe False on any exception.
- [Execution 02-02]: Phase 2 success criterion #1 PROVEN — minimal story (intro.start -> 2 weighted choices -> 2 endings) loads and the interpreter walks it (enter -> weighted pick -> advance -> enter ending) in pure Python with a mock MolAction sink and zero pymol import. RNG determinism through the walk holds (same seed -> same ending; diff seeds diverge) — criterion #2 partially proven (full save/load determinism in Plan 05).
- [Execution 02-04]: SaveStore is a thin pure-data layer (json.dump indent=2 + GameState.from_dict; parent-dir creation + trailing newline). It does NOT replay MolActions and does NOT save .pse (Anti-Pattern 5); the ENGINE owns the on_enter replay on load (Pattern 6). Human-readable, diff-friendly saves (Decision D2).
- [Execution 02-04]: GameEngine is the turn loop (start/choose/_enter/save/load) wiring StoryInterpreter + GameState + RngEngine. It emits on_enter MolActions PER-ACTION to an injected molaction_sink (a callable taking a single MolAction) — never cmd.* (the testability boundary; a plain list .append is the test mock). Chose per-action dispatch because the plan's test done-criteria (len(sink)==2, sink[0].op with sink.append) required it; the plan's key_links prose ("callable(list)") conflicted with its own tests, so the tests won. Phase 4+ molops.apply(action) receives one action per call (the natural unit).
- [Execution 02-04]: RNG-state sync — _enter() writes rng.get_state() into state after every entry and save() syncs before serializing; load() rebuilds the RngEngine via RngEngine.from_state(seed, rng_state) and replays the current node's on_enter with record_visit=False (no visit double-count). Exact-next-draw equivalence proven (test_rng_state_survives_save_load). Phase 2 success criterion #3 PROVEN (save/load restores an identical session by replaying on_enter); criterion #2 proven through the engine (the single seeded RngEngine carries through start->choose->save->load).
- [Execution 02-04]: interpreter.enter_node gained a backward-compatible record_visit=True param (2-line additive edit) so load-replay can skip the visit bump. All 12 existing interpreter tests still pass (zero regression — Plan 02 forward-compatible contract honored).
- [Execution 02-05]: Phase 2 GOAL ACHIEVED — 17-test integration suite (tests/test_integration.py) + runnable demo (tools/demo_playthrough.py) prove all 4 Phase 2 success criteria end-to-end in pure Python with zero PyMOL/Qt import. The integration test imports the FULL domain stack at module top (engine+graph+rng+state+persist+validate+model) and asserts 'pymol'/'PyQt5' not in sys.modules after a full playthrough — the testability boundary proven across the entire architecture, not just per-module. SC2 strongest proof: mid-playthrough save (before the weighted choice) -> load -> choose reaches the SAME ending as never saving (saved RNG position converges). SC4 orphaned-variant built via Node.from_dict on a copy of g.all_nodes(). The demo (seed 42, exits 0) is the human-visible proof twin. 97 tests pass; AST gate clean. No deviations (plan executed exactly as written; the per-action dispatch contract from 02-04 was pre-flagged and adapted correctly).
- [Execution 03-01]: Headless bridge verdict via STDOUT sentinel, NOT exit code — run-conda-pymol.bat ALWAYS returns 0 (`call conda deactivate` overwrites %ERRORLEVEL%; PyMOL's parsing.run_file swallows exceptions). SC #1's "exit code 0" reinterpreted as "SMOKE_RESULT: PASS sentinel present". The harness (tools/run_headless.sh) greps ^SMOKE_RESULT: PASS (presence=pass, absence=fail — stricter than grepping FAIL which falsely passes if the smoke crashes before printing the sentinel). Every later headless test (Plans 02+03) reuses this contract.
- [Execution 03-01]: cmd.create pitfall EMPIRICALLY CORRECTED — the "silent no-op" claim (PITFALLS.md Pitfall 3) did NOT reproduce for a new-target backup. Real gotchas: (a) create(bak,src,1,1) copies ONLY state 1 → incomplete multi-state backup (silent data loss); (b) create(obj,obj) self-copy is DESTRUCTIVE (raises CmdException + corrupts source); (c) default-args cmd.create(backup,source) (source_state=0,target_state=0=ALL states) is the working form — matches ARCHITECTURE.md:304. PITFALLS.md updated (5 targeted edits). Phase 4 restore uses default-args create.
- [Execution 03-01]: delete post-condition MUST use ?-prefix — bare count_atoms("deleted_obj") RAISES CmdException('Invalid selection name'); count_atoms("?"+name) returns 0 (safe). The ? is PyMOL's existing-objects-only selector prefix (used throughout source, e.g. creating.py:1001).
- [Execution 03-01]: Source-citation convention established — every cmd.* call carries `# src: tmp/pymol-src/modules/pymol/<file>.py:<line> cmd.<name>` on the line directly above it (22 citations in api_sanity_smoke.py; greppable via `grep -rn "# src: tmp/pymol-src"`). Line numbers pinned to PyMOL 2.5.0 (all 12 verified against tmp/pymol-src/modules/pymol/). Plans 02+03 must follow this for AssetManager + MolOps.
- [Execution 03-01]: Bundled (c14/data/assets/bundled/, committed, ships in plugin zip) vs Downloaded (c14/data/assets/downloaded/, gitignored, runtime cache) asset directory split. cmd.fetch skips download if file exists (importing.py:1211-1213) = free idempotent cache. Verified: check-ignore downloaded/ -> path; check-ignore bundled/ -> nothing.
- [Execution 03-02]: Inject-cmd testability pattern carried into the pymol layer — AssetManager imports only os + c14.paths (NO pymol at module top); cmd is constructor-injected (`def __init__(self, cmd): self._cmd = cmd`). The module is importable in pure WSL python3.6 with no pymol installed; MockCmd unit tests verify dispatch/path/args; the headless smoke (tools/asset_smoke.py) injects the REAL pymol.cmd to verify the actual API contract. Plan 03 MolOps reuses this pattern (MolOps(cmd, asset_manager)).
- [Execution 03-02]: Pitfall 5 mitigations baked into every fetch call — AssetManager.fetch_pubchem/fetch_pdb ALWAYS pass type= (NOT the CIF default — Pitfall 5a), async_=0 (sync — Pitfall 5c defense), path=<abs downloaded dir> (NOT cwd — Pitfall 5b). Downloads land in c14/data/assets/downloaded/ regardless of cwd (confirmed by the headless smoke's fetch_pubchem_file_landed stage: file at abs path). cmd.load returns None (importing.py:635) — count_atoms > 0 is the only reliable non-empty post-condition.
- [Execution 03-02]: MockCmd.count_atoms is an explicit method (NOT __getattr__ fallback) — so the post-condition probe is NOT recorded in self.calls; calls[0] is always the dispatched load/fetch op. Set mock._count=0 to exercise the RuntimeError branch; mock._count=3 for the success path. This MockCmd pattern is reusable for Plan 03 MolOps dispatch tests.
- [Execution 03-03]: MolOps.apply(action) takes ONE MolAction per call (the 02-04 per-action dispatch contract) — the engine emits `for action in actions: sink(action)`; the Phase 4+ controller calls molops.apply(action) per action. apply_all(actions) is a thin convenience loop. The unit boundary is per-action apply. 8 ops implemented (hide_all/show/show_as/select_focus/zoom/color/load/delete); load delegates to AssetManager (no direct cmd.* call); load without AssetManager raises RuntimeError.
- [Execution 03-03]: NotImplementedError for edit/protonate/restore = explicit Phase 4 boundary — these ops are NOT implemented in Phase 3; raising NotImplementedError (rather than silently no-opping) makes the boundary explicit so a stray MolAction with op='edit' fails loudly. Unknown ops also raise NotImplementedError. Phase 4 implements these by replacing the NotImplementedError branches. Unit-tested (4 tests assert the raise).
- [Execution 03-03]: `rep <name>` selection keyword is the headless "representation visible" post-condition — count_atoms('obj & rep sticks') > 0 after show(sticks). Empirically confirmed (C-backed, unverifiable from selector.py source). Headless smoke molops_scene_rep_visible (rep-sticks=3) proves SC #3: a queued MolAction list translates to the correct cmd.* sequence AND the resulting object has the expected representation visible.
- [Execution 03-03]: Phase 3 COMPLETE — all 4 success criteria delivered. SC #1 (Plan 01: api-sanity smoke + SMOKE_RESULT sentinel), SC #2 (Plan 02: AssetManager resolves bundled + fetched substrates cwd-independent), SC #3 (Plan 03: MolOps translates queued MolAction list to cmd.* sequence; headless smoke confirms rep visible), SC #4 (Plans 01/02/03: source-citation convention on every cmd.* call). 123 tests pass; AST gate clean.
- [Execution 04-01]: apply_edit(object_name, edit_steps) -> RestoreHandle is the settled signature 04-03 + 04-05 depend on. edit_steps is a LIST of step dicts (NOT edit_type+kwargs) so ProtonationManager (04-03) can compose multi-step sequences (removes + alter + adds) under ONE backup. Step-dict schema: {"op":"alter"|"h_add"|"remove"|"fuse", ...}. The 4 convenience methods (point_mutation, substrate_remove_group, substrate_add_group, protonation_change) build standard lists; ProtonationManager builds its own.
- [Execution 04-01]: _handles registry pattern — apply_edit registers the handle in self._handles (take_backup does NOT); restore(obj) looks up by object_name; restore_from_handle pops after restore. restore(obj) works after apply_edit but NOT after bare take_backup. restore_from_handle works with any explicit handle. This matches the usage pattern: apply_edit is the normal entry; take_backup is for ProtonationManager Mode (a) which manages its own handle.
- [Execution 04-01]: protonation_change h_ops partitioning — removes (OLD resn) BEFORE alter, adds (NEW resn) AFTER alter — matches 04-03 _apply_alter (Pitfall 2 ordering). The catalog authors selections with the right resn phase. The SC1 gate (04-04 check_alter_gate.py) is on cmd.alter ONLY; h_add is not alter, but the same sort-after discipline applies uniformly via apply_edit's always-sort+rebuild guard.
- [Execution 04-01]: MockCmd.count_atoms differentiates _bak_ prefix (returns _count_bak for backup names, _count for live objects) so the count-mismatch test can set them differently. MockCmd.iterate populates stored.list from configurable _residue_sig. Both are explicit methods (NOT __getattr__ fallback) so they are NOT recorded in self.calls — calls[0] is always the first real dispatch (extends the Phase 3 MockCmd precedent from test_molops.py:39-65).
- [Execution 04-01]: The alter->sort trap (Pitfall 6) is mechanically mitigated — apply_edit ALWAYS calls cmd.sort + cmd.rebuild after any alter/h_add/remove/fuse step (unit-tested for all 4 step ops). `self._cmd.alter` appears ONLY at edit_ops.py:129 inside apply_edit's step dispatch (the sole sanctioned path; 04-04's check_alter_gate.py will allowlist this module). 16 self._cmd.* call sites, 16 # src: citations (all 10 unique APIs verified against tmp/pymol-src/modules/pymol/ at the cited file:line). 140 tests pass (123 prior + 17 new); AST gate clean.
- [Execution 04-02]: EditIntent is a NEW plain class SEPARATE from MolAction — MolAction is the execution carrier (engine->molops, ops edit/protonate/restore); EditIntent is the routing carrier (player->router) and carries the enzyme_id lookup key MolAction lacks. Conflating them couples routing to execution and breaks data-flow directionality. EditIntent lives in c14/story/model.py alongside MolAction (pure data, 3.6-safe, from_dict/to_dict/__eq__/signature).
- [Execution 04-02]: Edit routing matching is EXACT dict equality on a normalized signature (op/target/args) — zero fuzzy/substring/similarity (smuggles in the out-of-scope chemistry engine). signature() lowercases+strips target, stringifies args via _norm_val. Dict equality is order-independent on 3.6.9 (verified). The signature is a CONTRACT: the UI produces EditIntents whose signature() matches the table; the table author writes the identical canonical form; the router compares with ==.
- [Execution 04-02]: Per-enzyme bad-ending pool uses OVERRIDE semantics (not merge) — if a per-enzyme pool is present + non-empty, use it; else fall back to global. validate_edits_table only flags the GLOBAL pool emptiness as an error (absent/empty per-enzyme pools fall back to global — NOT an error). EditRouter is stateless (RngEngine passed per route() call, mirroring StoryInterpreter). apply_player_edit reuses existing _enter + add_edit + molaction_sink (no new dispatch path). edit_router=None default keeps the constructor backward-compatible.
- [Execution 04-02]: SC3 PROVEN pure-Python — EditRouter routes known edits to defined branches (exact signature match) and unknown edits to the bad-ending pool (RngEngine-weighted, reproducible). 17 tests pass (TestEditRouter + TestGameEngineEditIntegration). NO PyMOL needed for routing (the SC3 demo uses a mock molaction_sink = list.append). The edit APPLICATION (backup + alter + sort) is the apply_edit helper's job (04-01), triggered by the routed branch's on_enter MolAction("edit",...); the EditRouter is pure routing (EditIntent in -> node_id out). 157 tests pass (123 prior + 17 04-01 + 17 04-02); AST gate clean (no pymol/random in edit_router.py).
- [Execution 04-03]: Catalog Split — pure-data variant CATALOG (c14/protonation_catalog.py, domain tier, AST-gate-clean) vs mechanism dispatcher (c14/pymol_layer/protonation.py, gate-exempt). The catalog is the DATA (which variants exist, which claim_id backs each); the ProtonationManager is the MECHANISM (how to apply a variant via cmd.*). Keeps the catalog unit-testable in pure WSL + the claim_id audit trail in the domain tier (where CITE-01 lives). Matches 04-RESEARCH-protonation.md Pattern 2.
- [Execution 04-03]: 4-dep injection with module-as-catalog — ProtonationManager(cmd, edit_ops, catalog, assets) where catalog is the c14.protonation_catalog MODULE (passed as a module reference). Unit tests use the REAL catalog (no mock needed for the data tier — it's pure + stable); mocks are only needed for cmd/edit_ops/assets (the mechanism tier). Mirrors the molops.py inject pattern but with the catalog as a 4th dep.
- [Execution 04-03]: h_ops ordering convention (Pitfall 2) encoded as catalog DATA — remove ops use OLD resn in sele (e.g. "resn HIS and name HE2"), MUST run BEFORE alter; add ops use NEW resn (e.g. "resn HID and name ND1"), MUST run AFTER alter. The catalog encodes this at the data level (test_h_ops_remove_uses_old_resn_add_uses_new_resn); _apply_alter partitions + reorders at the mechanism level (removes -> alter -> adds). The "add" -> "h_add" step-op translation happens in _apply_alter (catalog uses human-readable "add"; edit_ops step dict uses "h_add").
- [Execution 04-03]: Self-managed handle restore (Rule 3 deviation) — the plan said restore delegates to self._edit.restore(target), but take_backup (Mode a) does NOT register in edit_ops._handles (only apply_edit does — 04-01 SUMMARY), so edit_ops.restore(target) would raise "no backup handle registered" for Mode a. Used edit_ops.restore_from_handle(self._backup[target]) instead — the 04-01 contract for self-managed handles (works with any explicit handle, no _handles lookup). ProtonationManager tracks its own handle in self._backup for BOTH modes.
- [Execution 04-03]: AST-based no-direct-alter guard (Rule 1 deviation) — the plan said to text-scan protonation.py for "self._cmd.alter(" / "self._cmd.h_add(" but the module docstring DOCUMENTS the gate compliance (mentions the patterns in comments), causing naive substring false positives. Used AST to walk for actual Call nodes (self._cmd.alter/self._cmd.h_add) — mirrors the check_imports.py AST-gate precedent (Phase 1). Precise on actual calls, immune to comments/docstrings.
- [Execution 04-03]: SC4 PROVEN pure-Python (mechanics) — ProtonationManager applies a curated variant via edit_ops.apply_edit (Mode b: alter + h_ops) or AssetManager.load_bundled (Mode a: load pre-built); switch_variant records current_variant; restore clears state via edit_ops.restore_from_handle. NO direct self._cmd.alter/h_add (AST-verified — the alter gate allowlist stays at edit_ops.py only). 28 tests pass (10 catalog + 18 manager). 185 tests pass (157 prior + 28 new); AST gate clean. The REAL cmd.* contract is deferred to 04-05 headless smoke (3-tier testability pattern). Phase 4 boundary guards: test_phase4_placeholder_claim_ids (every claim_id == PLACEHOLDER_PHASE5; removed in Phase 5+) + test_no_pka_values_in_catalog (regex scan for pKa/pH/DOI — catches fabricated science).
- [Execution 04-04]: AST over grep for the alter gate — AST on `ast.Call.func ast.Attribute attr=="alter"` catches cmd.alter/self._cmd.alter/pymol.cmd.alter uniformly and does NOT false-positive on `"alter"` in comments/strings/dict-values (c14/protonation_catalog.py's `{"mode":"alter"}` is ast.Str, correctly ignored). A grep on `.alter(` would false-positive on those string values. The gate scans c14/ + tools/ INCLUDING pymol_layer/ + ui/ (unlike check_imports.py which SKIP_DIRS them) — the alter gate must see every .py; only edit_ops.py is allowlisted. Exit 0=clean / 1=violation / 2=ERROR (SyntaxError). SC1 is now a hard machine-checked invariant, not just unit-tested.
- [Execution 04-04]: find_violations(roots, allowlist) extracted as testable core — the scan loop was extracted from main() into find_violations(roots, allowlist) -> (violations, errors) so the exit-1 path is unit-testable in a temp dir without subprocess (mirrors check_citations.py:run_gate). Tests monkey-patch REPO_ROOT to a temp dir. 6-test self-test proves: 3 alter forms caught, no false positives on strings, allowlist honored, stray alter in molops.py FLAGGED, SyntaxError->errors (exit 2), real repo exits 0.
- [Execution 04-04]: Hand-written PDB fixtures over cmd.fab — cmd.fab uses one-letter amino-acid codes ("HIS" = H+I+S = 3 residues, not 1 HIS). Hand-writing ATOM records gives full deterministic control over atom names (esp. HD1/HE2 needed for SC4) with no network/fragment-library dependency. Geometry is approximate but valid enough for PyMOL load+alter resn+byres+count. _edit_smoke.pdb (ALA-GLY, 17 atoms, 2 residues) for SC1+SC2; _his_smoke.pdb (HIS with HD1+HE2, 14 atoms) for SC4. Both verified headlessly (SMOKE_RESULT: PASS). check_edit_coverage.py does inline JSON loading (no c14.edit_router import) keeping 04-04 depends_on:[04-01] honest.
- [Execution 04-05]: molops edit/restore/protonate delegation — the 3 NotImplementedError branches are replaced with delegation to EditOps (edit: dispatches on args["edit_type"] to point_mutation/substrate_remove_group/substrate_add_group/protonation_change; restore: editops.restore(target)) + ProtonationManager (protonate: pm.apply_variant(target, variant_id)). Constructor gains editops=None + protonation=None optional params (backward-compatible: MolOps(cmd) still works; missing helper -> RuntimeError, NOT NotImplementedError). Unknown ops STILL raise NotImplementedError (stray op fails loudly). Unknown edit_type raises ValueError. molops.py has NO cmd.alter (delegates; alter gate stays at edit_ops.py). 198 tests pass (191 prior + 7 net new: 11 new delegation tests - 4 removed NotImplementedError tests).
- [Execution 04-05]: h_ops selection scoping fix (Rule 1 bug) — ProtonationManager._apply_alter was NOT scoping h_ops selections to the target object, so cmd.remove("resn HIS and name HE2") also removed HE2 from the backup (_bak_HIS), corrupting it (restore atom-count mismatch). Fixed: _apply_alter now prepends target to each h_op sele ("{target} and {h_op_sele}"). This was NOT catchable by MockCmd unit tests (MockCmd records the sele string but doesn't execute it against real atoms/backups) — the headless smoke with REAL pymol.cmd surfaced it. Validates the 3-tier testability pattern: unit tests prove dispatch mapping, headless smokes prove the real API contract.
- [Execution 04-05]: Phase 4 COMPLETE — all 5 SCs delivered. SC1 (alter->sort mitigated + AST gate + headless byres proof: post-edit byres returns 10 atoms, no silent corruption), SC2 (backup/restore round-trip for point_mutation + substrate_remove_group + protonation_change, headlessly proven: atom count + residue signature restored), SC3 (EditRouter routing, 04-02), SC4 (ProtonationManager applies HID + switches HIE + restores HIS, headlessly proven: resn + H count + current_variant tracked), SC5 (per-enzyme coverage scan green). 198 tests pass; alter gate + import gate + coverage scan all GREEN; 2 headless smokes (edit + protonation) PASS.
- [Execution 05-01]: SC#1 soul-jump METAPHOR decided (human, checkpoint:decision) = option (b) sense-moves-with-electron, REFRAMED as metamorphosis (caterpillar→butterfly): the hero is the carbon and transforms (carbon body shed as CO2 = chrysalis stage), not dies; narrative POV/sense follows the electrons onward ("my electrons power the cell"); the "soul" is narrative attention, not a physical identity. Carbon-fate SCIENCE unchanged (electrons → ETC → ATP; carbon → CO2). SC#2 C14-decay DROP confirmed (drop=yes): radioactive decay removed as a bad-ending trigger; cycle-trap-until-host-death (spec.md line 18) is the sole timescale bad-ending; no NUBASE2020/C14-TIME-01 needed. Ending distribution documented: 1 True + several Normal/Good + many Bad per character (asymmetric, user-confirmed 2026-08-15) — affects Phase 5.1 graph topology. PROJECT.md rows 104 (metaphor) + 105 (RESOLVED via DROP) + new ending-distribution row + spec.md line 18 Pitfall 9 NOTE annotation. Downstream: Plan 05-02 invariant re-wording incorporates the distribution; Plan 05-04/05 must NOT populate/review NUBASE2020-C14 + C14-TIME-01; Phase 5.1 reachability checker must confirm ≥1 Bad-ending trigger reachable per character WITHOUT decay; True-ending text uses metamorphosis framing (not "hero dies").
- [Execution 05-02]: SC#3 anaerobic framing RESOLVED via option (d) CHOICE-FOR-GLUCOSE + BAD-FOR-FA/ALC (human, checkpoint:decision): glucose gets a 3-ending anaerobic branch (lactic=Good/retained, ethanolic=Normal/CO2, crisis=Bad); FA + alcohol get the Bad-ending trigger (O2-depletion energy crisis). Best pedagogical fit — reuses the glucose graph (no 4th character), invariant preserved aerobically. Invariant re-worded to aerobic-scoped (option i): aerobically 1 True + several Normal/Good + many Bad per character; anaerobically True unreachable (no O2/ETC), character-specific (glucose 3-ending branch; FA/ALC Bad-trigger only). Host = mammal/human (lactic fermentation: pyruvate → lactate via LDH; LDH in Phase 9; human typed "human" → interpreted as mammal). ETC-O2 finding acknowledged (settled chemistry — True biochemically unreachable anaerobically, verified against LibreTexts ETC + Biological Oxidation pages 2026-08-15). PROJECT.md rows 94 (host) + 95 (invariant re-worded) + 97 (framing RESOLVED via d) + new row 108 (ETC-O2 finding) + footer; REQUIREMENTS STORY-02 (v1-scope parenthetical) + STORY-05 (framing=(d)); spec.md line 14 Phase 5 NOTE annotation. Downstream: Phase 5.1 reachability checker configures aerobic-scoped invariant + anaerobic sub-graph (glucose 3-ending branch + FA/ALC Bad-trigger; True UNREACHABLE anaerobically — do NOT fabricate); Phase 9 +1 enzyme LDH (pyruvate decarboxylase NOT needed — host is mammal not yeast; ADH reused for alcohol aerobic path only); 5 LibreTexts anaerobic pages are a LATER batch per-claim gate (Phase 9), NOT approved here. Pending Phase 5 decisions reduced 2 → 1 (batch-vs-per-claim approval only).
- [Execution 05-03]: SC#4 source-approval WORKFLOW RESOLVED via option (c) HYBRID + user-requested routine-claim warning flag (human, checkpoint:decision): sources approved up front (batch), high-stakes claims individually reviewed, routine claims source-inherited (fast-track) + flagged with review_tier="routine" + inherits_source_approval=true for leisure spot-check (VISIBILITY mechanism, NOT a blocking gate — Phase 1 gate still passes routine claims via the source's approval). High-stakes taxonomy = advisory list as-is: HIGH-STAKES (individual per-claim review) = [RNG-weights, protonation-defaults, carbon-fate, contested]; ROUTINE (source-inherited fast-track + warning flag) = [enzyme-catalyzes-reaction-X, enzyme name/EC/cofactor, PDB existence/resolution/citation, pathway ordering]. Phase 1 gate UNCHANGED — approval_status == "approved" preserved (strict equality, c14/citations.py:109, Pitfall 6); loader (c14/citations.py:87-97) ignores extended fields (source_id, review_tier, inherits_source_approval, review_notes, claim_text); data/sources.json never read by gate. Throughput: glucose first batch ~1.5h; full content ~6-8h (vs ~33h strict per-claim). PROJECT.md row 106 (RESOLVED via c hybrid + taxonomy + warning flag + backward-compat + throughput) + row 102 (cross-ref) + footer. ALL 4 Phase 5 Key Decisions now RESOLVED. Downstream: Plan 05-04 CREATE data/sources.json + populate extended citations.json schema fields (review_tier/inherits_source_approval/source_id/review_notes/claim_text; routine=routine+true, high-stakes=high-stakes+false); Plan 05-05 approve sources batch → high-stakes individually (TCA-RNG-CITRATE-PROCHIRALITY-01, TCA-RNG-WEIGHT-01) → spot-check routine at leisure (GLY-PFK-01, CAST-PFK-PDB-01, CAST-CSC-PDB-01); NO NUBASE2020-C14/C14-TIME-01 (DROPPED).
- [Execution 05-04]: SC#4 registry STRUCTURE implemented + first batch populated (workflow = (c) HYBRID — data/sources.json CREATED, not skipped). data/sources.json: 5 PENDING candidate source records keyed by source_id [LIBRETEXTS-METAB-GLYCOLYSIS, LIBRETEXTS-METAB-TCA, LEHNINGER-CH-CITRIC_ACID_CYCLE, PDB-4PFK, PDB-1CSC]; 2 PDB IDs web-verified at rcsb.org 2026-08-15; bare JSON object; Phase 1 gate NEVER reads it. data/citations.json: 5 PENDING seed claims [GLY-PFK-01 routine, TCA-RNG-CITRATE-PROCHIRALITY-01 high-stakes, TCA-RNG-WEIGHT-01 high-stakes, CAST-PFK-PDB-01 routine, CAST-CSC-PDB-01 routine] carrying BOTH existing fields (claim/source_type/source; pdb_id+resolution_angstrom for PDB) AND new extended fields (claim_text/source_id/review_tier/inherits_source_approval/review_notes). data/citations.README.md extended (sources.json section + 5 new per-claim fields + warning-flag mechanism + corrected load-time-validation paragraph + backward-compat note + duplicate-key hook convention). Phase 1 gate backward-compatible: loader (c14/citations.py:87-97) ignores extended fields (loader OK 5 claims); gate exits 1 [MISSING] on fixture placeholder claims (loads extended registry without config error); 13 citation unit tests pass (no regression). NO entry auto-approved. NUBASE2020-C14 + C14-TIME-01 NOT populated (DROPPED in Plan 05-01); tests/fixtures/citations_pass.json UNCHANGED. Downstream: Plan 05-05 human first-batch approval checkpoint — approve 5 sources (batch) → 2 high-stakes claims individually (TCA-RNG-CITRATE-PROCHIRALITY-01, TCA-RNG-WEIGHT-01) → spot-check 3 routine claims at leisure (GLY-PFK-01, CAST-PFK-PDB-01, CAST-CSC-PDB-01); confirm direct [UNAPPROVED]-on-pending-seed-claim proof (Plan 05 Task 2 step 5 mirrors a seed claim_id into a temp fixture).
- [Execution 05-05]: PHASE 5 COMPLETE — first batch approved + SC#5 gate PROVEN operational on real data. Human reviewed + approved 5 first-batch claims (GLY-PFK-01, TCA-RNG-CITRATE-PROCHIRALITY-01, TCA-RNG-WEIGHT-01, CAST-PFK-PDB-01, CAST-CSC-PDB-01) + 4 sources (LIBRETEXTS-METAB-GLYCOLYSIS, LIBRETEXTS-METAB-TCA, PDB-4PFK, PDB-1CSC) on 2026-08-15. LEHNINGER-CH-CITRIC_ACID_CYCLE REJECTED per user license concern (avoid print-textbook license issues); provenance retained (rejected fails gate identically to pending — no Pitfall 6 regression). The 2 high-stakes TCA RNG claims (TCA-RNG-CITRATE-PROCHIRALITY-01, TCA-RNG-WEIGHT-01) source_id swapped LEHNINGER → LIBRETEXTS-METAB-TCA (Jakubowski & Flatt, Fundamentals of Biochemistry Vol. II, Ch 16 The Citric Acid Cycle, section 16.02; CC BY-SA 4.0; web-verified 2026-08-15 via webfetch — the page covers citrate synthase (S)-citryl-CoA stereospecificity + aconitase 180-degree flip = discrimination between citrate's two carboxymethyl groups; HONEST CAVEAT: the page does NOT explicitly use 'prochiral' for citrate or state the first-turn-vs-second-turn probability — recorded in review_notes for Phase 7 reconsideration). TCA-RNG-WEIGHT-01 weight=0.5/0.5 per user acceptance ('this is the probability in textbook'). High-stakes claims stay review_tier=high-stakes + inherits_source_approval=false after the swap (do NOT auto-inherit from the new source — per-claim safety guarantee preserved). SC#5 PROVEN: gate exits 0 'CITATION GATE PASSED: 5 claim reference(s) across 2 node(s) -- all approved' on a temp fixture referencing the 5 approved claims; existing placeholder fixture still exits 1 [MISSING] (no regression); 12 citation tests pass. Breakdown: 5 claims approved / 0 rejected / 0 pending; 4 sources approved / 1 rejected / 0 pending. ALL 5 Phase 5 SCs delivered: SC#1 (05-01), SC#2 (05-01), SC#3 (05-02), SC#4 (05-03+05-04+05-05), SC#5 (05-05). Downstream: Phase 5.1 (Story Graph Design) UNBLOCKED; Phase 6 (Qt UI MVP) UNBLOCKED (workflow operational); Phase 7 (Glucose content) UNBLOCKED (first-batch sources approved + the 2 high-stakes TCA RNG claims already approved here).
- [Execution 05.3-01]: Phase 5.3 IN PROGRESS (Plan 01 SC1 DELIVERED) — wrote the SC1 design artifact `05.3-CONVENTION.md` (415 lines, 10 H2 sections: SC checklist + §1-§8 + review checklist) — the WT-aligned structure load convention spec. KEY DECISIONS: (1) ADOPT the API researcher's MolAction naming (target=mobile, args["reference"]=fixed) over the design researcher's (args.mobile/args.target) — avoids BOTH the redundancy with MolAction.target AND the PyMOL target=fixed collision; (2) Option iii placement (branch-node on_enter AFTER reverse-mutation edit) — REJECT option i (narratively backwards) + option ii (pre-decides OQ-DM2 by converging only under approach a); (3) CORRECTED the 05.1-06 Note 2 imprecision — a correct edit routes to a branch node (NOT the restore safety net); the WT-aligned load is a branch-node on_enter concern; (4) cmd.super default (NOT cmd.align) for domain-shift mutants per fitting.py:316-320; (5) op="align" molops dispatch DEFERRED to Phase 6 (Q3 flagged for human override at Plan 02 — both 5.3-pure and 5.3-adds-op mechanically identical); (6) Convention is CONDITIONAL (approach × conformational_difference; OQ-DM2 honored — complements whichever approach Phase 7 picks); (7) Overlay default for major, replace fallback for minor/none/restore-shortcut; side-by-side deferred to Phase 6 UI; (8) align_sele + conformational_difference are per-enzyme Phase 7 metadata fields (source-approved); (9) 8 open questions Q1-Q8 + 7-criterion review checklist surfaced for Plan 02 checkpoint. NO fabricated science (only 6CFO/6CER as real PDB IDs, both marked pending Phase 7; all other PDB IDs/disease/align_sele are PLACEHOLDERS). NO code modified (c14/pymol_layer/molops.py NOT touched — pure design writing phase). MANDATORY hero-identity anti-confusion NOTE (enzyme health NOT carbon fate; pyr.pdh carbon shed as CO2, electrons continue via NADH→ETC→ATP). 2 atomic commits (ca50bed docs §1-§5 + a0bdb85 docs §6-§8 + review checklist). Zero deviations — plan executed exactly as written (minor ~5-line content expansion to meet the >=200-line minimum for Task 1, both substantive additions).
- [Execution 05.3-02]: Phase 5.3 COMPLETE (Plan 02 SC2 + SC3 DELIVERED) — wrote `tools/wt_align_smoke.py` (229 lines, 12 `# src: tmp/pymol-src` citations) + 2 hand-written PDB fixtures (`_wt_align_mut.pdb` 17-atom ALA-GLY mutant, `_wt_align_wt.pdb` 14-atom GLY-GLY WT +5 Å x offset) and ran the smoke headlessly via `bash tools/run_headless.sh tools/wt_align_smoke.py` with `SMOKE_RESULT: PASS` + all 8 stage checks green (load_mutant 17 atoms; apply_correct_edit RestoreHandle; edit_took resi1 GLY 10 atoms; load_wt 14 atoms; super_returns_finite_tuple RMSD=0.5497 Å 5 matched atoms; super_moved_mobile x:8.85→1.11; scoped_super_matched_ca 1 CA; scoped_super_moved_whole_wt x:6.11→1.46) — SC2: proves the `cmd.super` MECHANISM on a placeholder, NOT real science (placeholder coords arbitrary; real RMSD-quality gate is Phase 7). SC3: human design-review checkpoint APPROVED — human typed "approved"; the convention + the prototype + all 8 open questions (§8 Q1-Q8) accepted at research recommendations. KEY DECISIONS: (1) Q3 = DEFER the op="align" molops dispatch to Phase 6 (research rec accepted) — c14/pymol_layer/molops.py stays UNTOUCHED (no follow-up dispatch work owed in 5.3); the smoke calls cmd.super DIRECTLY; (2) Q1 conditional, Q2 overlay-major/replace-otherwise, Q4 per-enzyme align_sele Phase 7 field, Q5 side-by-side OUT of 5.3 (Phase 6), Q6 restore-suffices-under-a, Q7 PDB sourcing boundary, Q8 conformational_difference Phase 7 source-backed — all accepted at recs; (3) NO adjustments to 05.3-CONVENTION.md needed (approved as-is). AUTO-FIXED DEVIATION (Rule 1+3 bug/blocking): the plan prescribed cmd.iterate(..., "x,y,z") for coordinate capture but cmd.iterate raises NameError('x/y/z only available in iterate_state and alter_state') — coordinates live only in the iterate_state/alter_state namespace; fixed all 4 coordinate captures to cmd.iterate_state(1, ...) with the editing.py:1578 citation (verified against tmp/pymol-src); changes the smoke's coordinate-capture mechanics ONLY, NOT the convention, the post-conditions, or the §6 molops dispatch pseudocode. NO fabricated science (fixtures are placeholder peptides ALA-GLY/GLY-GLY; only 6CFO/6CER referenced, both already 5.1 CANDIDATE pending Phase 7). AST gate clean (exit 0). 2 atomic commits (95d6d39 feat fixtures + c5ba316 feat smoke) + 1 human-verify checkpoint (no commit, human-approved). Phase 5.3 COMPLETE (2/2 plans; SC1 + SC2 + SC3 all delivered). Next: Phase 5.4 (Cast & Hero Representation Design) → Phase 6 (Qt UI MVP).
- [Execution 05.2-01]: Phase 5.2 COMPLETE — bad-ending extensibility convention locked + tested (2026-08-22). Added exactly ONE new bad-ending node `bad.denature_ph_change` (pH-denaturation, 1a `edit:unknown`, `PLACEHOLDER_PHASE7_BAD`) to `data/story_glucose/bad_endings.json` + ONE structural choice (path L) to the `edit.prompt` stub (12→13 choices) — the FIRST intentional mutation of the frozen 5.1 skeleton (54→55 nodes, 20→21 endings, Bad 14→15 [1T+3G+2N+15B]). Generalized the hardcoded bad-ending test into a parametrized `subTest` convention proof (`test_all_edit_prompt_bad_endings_reachable_convention`) that dynamically iterates `edit.prompt.choices` + asserts each bad-ending target exists + is `is_ending="bad"` + carries a valid `edit:*` tag + full-graph reachability stays GREEN — auto-covers future Phase 7 additions WITHOUT test edits. Renamed `test_manifest_loads_all_54_nodes`→`_55_nodes` (assert 54→55); bumped `test_reachability_green_all_four_tiers` (20→21 endings, 14B→15B). Suite stays at 9 tests, all PASS. Updated `05.1-DESIGN.md` sections 1/3/4/5/6/7/8 + `05.1-GRAPH-DIAGRAM.md` + regenerated `05.1-graph.svg`/`05.1-graph.txt` (55 nodes | 21 endings 1T/3G/2N/15B | 14 edit-allowed | 1 RNG). KEY DECISIONS: (1) structural half only — `bad_endings.json` + `edit.prompt` stub; runtime `c14/data/edits.json` `bad_ending_pool` UNCHANGED (2 ids, Phase 7 territory); (2) path L appended at end of A-K global sequence (no renumbering); (3) generalized (replaced) the old hardcoded test, not added alongside — the dynamic subTest is strictly stronger; (4) `05.1-VERIFICATION.md` left UNCHANGED — the new frozen-at-55 state is established by a later 05.2-VERIFICATION. NO fabricated science (PLACEHOLDER_PHASE7_BAD only; text_dramatic narrates the general textbook principle of pH-denaturation, NOT a specific citation). NO new dependencies (Python 3.6.9 stdlib unittest subTest only). 3 atomic commits (b588e70 feat + 73f682f test + 41a3160 docs). Zero deviations — plan executed exactly as written.
- [Execution 05.1-06]: Phase 5.1 COMPLETE — human review checkpoint APPROVED (2026-08-22). The user approved the FINAL post-expansion 54-node glucose skeleton as-is (no fixes required; skeleton FROZEN at 54 nodes / 20 endings 1T+3G+2N+14B / intro.preface start / 14 edit-allowed / 0 single-Continue). 3 CRITICAL/HIGH disease-mutant decisions CONFIRMED AS DEFAULTS (user approved without override): (1) IDH3A M204I (DIS-IDH3A-01-cand) DEFAULT CONFIRMED for tca.isocitrate_dh — biochemically correct NAD+-dependent mitochondrial TCA enzyme (PDB 5GRF); IDH1 R132H alternative documented but NOT adopted (not the TCA enzyme); (2) pyr.pdh cast PDB 6CFO (WT, 2.70 Å) CAST CONFIRMED — mutant-reveal approach (a apply-edit vs b load-mutant-PDB [6CER]) DEFERRED TO PHASE 7; 1NI4 alternative documented but NOT adopted; (3) tca.citrate_synthase edit:structural REFRAME KEPT (edit:enzyme tag stays + edit:structural added; alternative [remove edit:enzyme] NOT adopted). 32 OQs disposed (user approved without adjustment = all accepted as placeholders / deferred to Phase 7 / confirmed as skeleton decisions): 13 original OQs deferred to Phase 7; 11 disease-mutant OQs — OQ-DM1/DM2/DM3 resolved by the 3 decisions above, OQ-DM4-DM11 deferred to Phase 7 (OGDH scan, PFKM mutant, Aconitase 1ACO homology, COX4I1 numbering, CYC1 choice, LDH not-promoted, ETC non-human-PDB caveat, DIS-*-cand candidate status); 8 bad-ending/preface OQs — OQ-BAD-1/2/3 deferred to Phase 7, OQ-PREFACE-1/3/5 confirmed as skeleton decisions, OQ-PREFACE-2 deferred to Phase 5.2, OQ-PREFACE-4 deferred to Phase 5.2/6. Pre-flight verification all green: 9 reachability tests PASS (54 nodes, 20 endings, intro.preface start, 14 edit-allowed, 0 single-Continue, pyr.pdh 6CFO, etc.complex_i DIS-NDUFS8-01-cand, 9 bad endings reachable from edit.prompt); 207 full suite PASS; skeleton spot-checks match DESIGN.md. SC1 human-review COMPLETE (branch-point mapping confirmed against spec.md + approved sources; no invented branches). SC4 design-review COMPLETE (14-row edit-node contract confirmed sound; CS structural exception; tca.shuffle shared edit point; 5-step data-flow; Phase 7 content boundary). SC2 + invariants CONFIRMED (9 tests PASS). Metadata-only continuation: NO skeleton/JSON/test/DESIGN.md/GRAPH-DIAGRAM.md changes. Pending follow-ups (NOT implemented in 05.1-06): (1) user requested bad-ending extensibility + a test with "denature with a change in pH" — orchestrator to route to a new plan (05.1-17) or defer to Phase 7; (2) user wants WT-aligned experimental structure load after a correct edit (esp for mutants with major conformational differences) — interacts with pyr.pdh mutant-reveal (OQ-DM2 Phase 7) + ProtonationManager restore path (Phase 4 SC2) + Phase 5.2 scene-template convention; NEW requirement not in skeleton/DESIGN.md; orchestrator to route to 05.1-17 or defer to Phase 5.2/6/7. Phase 5.2 + Phase 6 UNBLOCKED.
- [Execution 05.4-01]: Phase 5.4 IN PROGRESS (Plan 01 of 04) — Plan 05.4-01 delivered the SC1+SC2+SC3 convention spec `05.4-CONVENTION.md` (751 lines, 10 H2 sections §1-§8 + SC checklist) — the Cast & Hero Representation convention spec consumed by Phases 6/7/9/12 WITHOUT re-designing it. Pure design writing (0 code changes; `c14/pymol_layer/molops.py` + `c14/story/model.py` UNTOUCHED, confirmed via git diff --name-only). KEY DECISION — op-granularity RESOLUTION: ADOPT 3 minimal peer-primitive ops set_color/label/set (each wrapping ONE cmd.* call: set_color→cmd.set_color viewing.py:2107 idempotent; label→cmd.label viewing.py:1332 w/ quoted-string expr footgun encapsulated; set→cmd.set setting.py:183 per-atom) + SHORT MolAction sequences, NOT the composite op="hero_highlight" super-op the hero-design researcher proposed. Mirrors how 5.3 resolved its naming disagreement by adopting the API researcher's recommendation (05.3-CONVENTION.md §6 — 5.3 added ONE focused op align NOT a composite wt_reveal super-op; 5.4 adds 3 focused primitive wrappers because the representation/color/label surface is wider). The rejected super-op documented in §2.2 for the record (its pros: readable on_enter + footgun encapsulation + colorblind default baked in; its cons: couples concerns, one-op-many-cmds, harder per-step testing, breaks 5.3 precedent — lose). The `set` op is the 3rd primitive (justification for going beyond the API researcher's 2-ops rec: sphere_scale is a per-atom setting show_as/color don't cover; cmd.set is a distinct primitive; generic op=set reusable beyond sphere_scale). The 3 new-op molops dispatches (set_color/label/set elif branches + MockCmd unit tests) DEFER to Phase 6 (mirrors 5.3 §6/§8 Q3; OQ-1 flagged for the Plan 04 checkpoint — human may override to "add in 5.4" per the 04-05 precedent; if overridden, Plans 02/03 gain molops.py edit + unit test tasks). Shared foundation (§2): §2.1 op vocabulary (8 existing peer-primitive ops hide_all/show/show_as/select_focus/zoom/color/load/delete + 3 NEW set_color/label/set w/ their MolAction shapes + molops dispatch pseudocode SPEC-only); §2.2 the op-granularity RESOLUTION (4-point rationale: minimal+orthogonal dispatch, per-step headless-testability, 5.3 precedent, the set op is the 3rd primitive); §2.3 the shared named-color palette (hero_gold Okabe-Ito [0.90,0.62,0.0] PLACEHOLDER pending Phase 7 source approval spheres / gray cartoon / green sticks [FILLS 5.3 <wt_color>] / red spheres [FILLS 5.3 <mutant_color>, OQ-2 magenta alt] / orange sticks / gray80 scoped dim / bad-ending red cartoon — with the named-color confidence note: gray/green/red/orange/gray80 standard PyMOL named colors; hero_gold defined via cmd.set_color idempotent; do NOT assert a hardcoded color INDEX [session-allocated; assert the NAME via get_color_indices round-trip]). SC1 hero highlight (§3): §3.1 hybrid selection rule (option iv: hero_selector per-asset [Phase 7-authored deterministic] + single-C auto-derive [count_atoms==1] + multi-C-no-selector FAIL LOUDLY [no silent best-guess — the which-carbon-is-the-hero is a HIGH-STAKES pathway-science claim PROJECT.md row 109]); §3.2 3 structural cases (1 hero-alone [intro.preface, hero_atom single-atom obj] / 2 hero-in-substrate [glucose multi-C, hero_selector=glucose and name C1] / 3 hero-at-enzyme-cast [loaded obj is the ENZYME, hero in substrate not loaded] w/ 3 sub-options 3a co-load / 3b suspend / 3c hybrid RECOMMENDED; case 3 co-load decision is per-enzyme Phase 7, mirrors 5.3 OQ-DM2); §3.3 the 6-call semantic sequence (set_color hero_gold → show_as spheres → set sphere_scale 1.0 → color hero_gold → color gray80 scoped-dim [IN THE HERO-BEARING OBJECT ONLY, NOT the cast/enzyme — critical scoping] → label C14, expressed as MolActions NOT a super-op; for single-atom hero_atom case 1, step 5 dim omitted = 5 calls); §3.4 persistence as pure function of on_enter (re-derived each replay from the deterministic hero_selector rule; NO runtime marker alter/flag/b needed; cites engine.py:20-22 [pure function docstring], :163-194 [_enter runs on_enter], :203-213 [load replays on_enter], persist.py:50-51 [saves ONLY GameState JSON no .pse]); §3.5 CO2-shed soul-transfer climax (tca.shuffle → tca.co2_turn1/turn2 [hero carbon SHED as CO2, gold sphere shows ONE final time then FADES] → etc.entry onward [highlight TRANSFERS to NADH = the soul=electrons] → end.true [highlight on ATP = metamorphosis complete]; the soul-jump metamorphosis reframing PROJECT.md row 107; the hero-highlight carries a soul mode [highlight targets non-carbon NADH/ATP for post-shed nodes]); §3.6 MANDATORY anti-confusion note (BOXED: the highlight TRANSFER is a NARRATIVE/VISUAL DEVICE [soul follows electrons per metamorphosis reframing]; does NOT assert the carbon becomes ATP [WRONG — carbon shed as CO2 at TCA] / becomes NADH [WRONG — ELECTRONS carried by NADH not the carbon] / any C14-decay timescale [DROPPED PROJECT.md row 108]; the text_teaching contract at tca.co2_turn1/turn2, etc.entry, etc.atp_synthase, end.true MUST state the metamorphosis framing; keep carbon-fate + electron-soul framings SEPARATE — mirror 5.3 §5); §3.7 no fabricated science (PLACEHOLDERS only: pdb:TBD_*, <hero_selector>, <active_site_sele>, etc.; ZERO real PDB IDs unlike 5.3 which had 6CFO/6CER; palette RGB values placeholders pending Phase 7). SC2 scene-template library (§4): §4.1 scene templates are MolAction SEQUENCES NOT cmd.scene keys (cmd.scene is session state LOST on save/load [c14/persist.py:50-51 saves ONLY GameState JSON no .pse]; on_enter replays via engine.py:203-213 — cmd.scene is optional Phase 6 in-session PERFORMANCE CACHE only; 5.4 does NOT depend on cmd.scene); §4.2 the 6 scene TYPES mapping all 55 skeleton nodes (1 preface [intro.preface/select/shell_glucose, fa.stub, alc.stub] / 2 substrate-traversal [gly.start/g6p/fbp_to_pyruvate/pyruvate, tca.entry/co2_turn1/co2_turn2/akg_dh, etc.atp_synthase + 6 narrative-only edit-allowed conditional] / 3 active-site-reveal=cast-reveal [8 PDB-bearing disease-mutant edit-allowed gly.pfk/pyruvate_kinase/pyr.pdh/tca.aconitase/isocitrate_dh/etc.complex_iv + tca.citrate_synthase structural variant + 6 narrative-only conditional] / 4 branch-point [pyr.branch/anaer.entry/tca.divert_to_good/etc.entry] / 5 rng-shuffle [tca.shuffle the SOLE stochastic node] / 6 ending 4-tier [true=end.true; good=end.good.fatty_acid/amino_acid/anaer.lactic; normal=end.normal.co2/anaer.ethanolic; bad=15 bad.*]) + the 6 narrative-only edit-allowed sub-case (5.4 specs BOTH substrate-traversal [current skeleton] OR active-site-reveal IF Phase 7 sources a structure; Phase 7 decides per enzyme, mirrors 5.3 OQ-DM2; OQ-10); §4.3 per-type draft MolAction sequences + post-conditions (machine-checkable via count_atoms rep keyword + iterate color-name/label + named-sele residue count) for all 6 types incl 4 ending tier-templates (true=ATP hero_gold "your soul powers the cell"; good=retained hero_gold "your carbon lives on"; normal=CO2 gray "the everyday exit"; bad=broken red cartoon "the host fades") + honesty note (prototype proves the MECHANISM not real science; == expected_active_site_residue_count assertions use placeholder values, real gate is Phase 7). SC3 cast-reveal (§5): §5.1 full-then-zoom RECOMMENDED (the dramatic meet-the-cast-member beat — the RPG cast framing CAST-01; active-site-first = Phase 6 re-entry optimization [track visited enzyme_ids; first visit=full-then-zoom, re-entry=active-site-first] = OQ-7; exception: 6 narrative-only edit-allowed nodes no PDB → active-site-first moot); §5.2 the cast-reveal sequence (1 full gray cartoon → 2 zoom active site via select_focus → 3 green catalytic sticks → 4 red mutant SPHERES [rep redundancy=colorblind-safe — spheres vs sticks carries the signal redundantly with hue since red/green alone fails deuteranopia/protanopia] → 5 orange cofactor sticks → 6 nameplate+residue labels → 7 hero-highlight if substrate co-loaded) + the tca.citrate_synthase structural variant (edit:structural NO disease mutant → OMIT step 4); §5.3 two label layers (1 enzyme nameplate [cmd.label fixed string on one anchor CA, headless-verifiable via iterate label] + 2 residue labels [resn+'-'+resi on catalytic CAs = the "specific residue representations shown" CAST-05]; Phase 6 Qt overlay is polish ON TOP NOT a replacement — cmd.label is the floor [works headlessly + survives save/load via MolAction replay]); §5.4 the mutant→WT visual arc + 5.3 composition (5.4 OWNS BEFORE [mutant red spheres at edit-allowed on_enter] + the named color values [wt_color=green, mutant_color=red FILLING 5.3 <mutant_color>/<wt_color> slots 05.3-CONVENTION.md:318-319]; 5.3 OWNS AFTER [WT-aligned restoration reveal at branch on_enter]; 5.4 does NOT re-litigate the 5.3 align mechanism; the cast-reveal BEFORE + restoration reveal AFTER use the SAME palette so red→green = broken→restored; enzyme-health framing [cast-reveal] kept SEPARATE from carbon-fate framing — mirror 5.3 §5). Downstream contracts (§6): §6.1 Phase 6 (6 scene-type seqs + cast-reveal + hero-highlight + shared palette; UI must NOT invent colors/reps/labels; 3 new ops set_color/label/set + align deferred to Phase 6; Phase 6 MAY add cmd.scene cache, multi-viewport side-by-side, Qt overlays, first-visit-vs-re-entry branch); §6.2 Phase 7 (7-field enzyme metadata table [enzyme_pdb/active_site_sele/catalytic_residues_sele/mutant_residue_sele/cofactor_sele/label_anchor_sele/reveal_variant] + 4-field hero metadata table [hero_selector/hero_label/hero_color/soul]; selectors are structural BUT which-residue-is-catalytic/which-residue-is-the-disease-mutation/which-carbon-is-the-hero ARE cited science claims HIGH-STAKES PROJECT.md row 109; CO2-shed+soul-transfer nodes Phase 7 authors the on_enter sequences + anti-confusion text_teaching); §6.3 Phase 9 (cast-reveal + Type 3 template GUARANTEE cast consistency BY CONSTRUCTION — every enzyme enters via the SAME template+palette NOT ad-hoc; Phase 9 must NOT invent per-enzyme color schemes; large-enzyme visibility/zoom is Phase 9); §6.4 Phase 12 (Type 6 ending-templates compose with cmd.ray render — Phase 12 renders the scene the template produces, does NOT re-pose/re-color; 4 tier-templates give 4 distinct visual signatures). 11 open questions (§7): OQ-1 op-granularity+dispatch-landing [RESOLVED-in-spec-confirm] + OQ-2 mutant-color red-vs-magenta + OQ-3 hero-color hero_gold-vs-cyan + OQ-4 label-wording C14-vs-YOU + OQ-5 CO2-shed-visual fade+transfer-vs-fade-only + OQ-6 multi-C-fallback FAIL-LOUDLY-vs-heuristic + OQ-7 cast-reveal full-then-zoom-vs-active-site-first + OQ-8 branch-point side-by-side data-level-vs-mandate + OQ-9 ending-tier 4-templates-vs-1-parametric + OQ-10 narrative-only-edit-allowed both-options-vs-mandate-structure + OQ-11 sub-beats single-node-vs-3-sub-beat; plus 4 RESOLVED-by-research items recorded (fixture reuse / cmd.scene rejection / PubChem atom naming Phase 7 / palette RGB Phase 7). Review checklist (§8): 9 criteria for the Plan 04 checkpoint (hero-selection-rule / op-granularity / scene-template completeness / cast-reveal consistency / 5.3 composition / CO2-shed climax+anti-confusion / downstream contracts / 11 OQs / no-fabricated-science). 2 atomic commits (b37185e §1-§3 [325 lines] + f6697fd §4-§8 [426 lines added, 751 total]). DEVIATIONS: none (plan executed exactly as written; pure design writing against 3 already-complete HIGH-confidence research files; the op-granularity RESOLUTION was the key decision the plan required, made exactly as directed). Next: 05.4-02/03 (headless mechanism proofs — prototype cmd.* directly NOT via molops per OQ-1 deferral, like wt_align_smoke.py calls cmd.super directly) → 05.4-04 (human design-review checkpoint against the 9-criterion review checklist + 11 OQs) → Phase 6 (Qt UI MVP, which implements the 3 deferred set_color/label/set dispatches + the 5.3 align dispatch).
- [Execution 05.4-03]: Phase 5.4 IN PROGRESS (Plan 03 of 04) — Plan 05.4-03 delivered the SC2+SC3 headless mechanism proof `tools/scene_template_smoke.py` (516 lines, 99 `# src: tmp/pymol-src` citations) running headlessly with `SMOKE_RESULT: PASS` and all 37 stage checks green via `bash tools/run_headless.sh tools/scene_template_smoke.py`. Pure pymol.cmd.* (NO Qt); calls cmd.* DIRECTLY (NOT via molops.apply — the new-op set_color/label/set dispatches are deferred to Phase 6 per convention §2.2/OQ-1, mirrors wt_align_smoke.py calling cmd.super directly). Proves the SC2+SC3 MECHANISM on placeholder fixtures, NOT real science (real per-enzyme active-site/catalytic/mutant selections are Phase 7 source-approved content). The 6 scene TYPES each apply as a MolAction SEQUENCE via direct cmd.* (NOT cmd.scene — session state lost on save/load; convention §4.1): Type 1 preface (hero_atom spheres gold "C14" + 20-AA cast cartoon gray), Type 2 substrate-traversal (sticks gray + hero gold sphere on C1 via per-atom show_as scoping — rep-sticks=2, rep-spheres=1), Type 3 active-site-reveal=cast-reveal DISEASE-MUTANT variant (full gray cartoon → zoom active site → green catalytic sticks [show ADDS on top of cartoon — resi 2 has BOTH cartoon AND sticks] → red mutant SPHERES [rep redundancy=colorblind-safe] → "ENZ" nameplate label), Type 3b STRUCTURAL variant tca.citrate_synthase (SAME sequence MINUS mutant spheres — hide everything reset → no mutant spheres verified rep-spheres=0; CS nameplate), Type 4 branch-point (current gray sticks + alt_destiny hero_gold sticks), Type 5 rng-shuffle tca.shuffle (prochiral pair spheres + "? (RNG)" label), Type 6 ending 4 tier-templates (true=ATP hero_gold "ATP - soul"; good=retained hero_gold; normal=CO2 gray; bad=broken red cartoon "catastrophe"). Stage 7 the 5.3 composition: 5.4 OWNS BEFORE (mutant red spheres on enzyme resi 1) + 5.3 OWNS AFTER (WT-aligned reveal colored green via 5.4's <wt_color> contract — load _wt_align_wt.pdb as enzyme_wt, cartoon green, comp_after cartoon=14 color=green); red→green = broken→restored COLOR contract coheres; the 5.3 align MECHANISM is proven in tools/wt_align_smoke.py (NOT re-run here). Color-name round-trip at scale: color_name_for helper (cmd.iterate color-index [non-colliding collector cidx, RESEARCH-api §F Pitfall 1] → get_color_indices index→name [Pitfall 4: index is session-allocated, hero_gold=5388 this run; assert the NAME not a hardcoded index]) + label_text_for helper (cmd.iterate label [collector lbls]) used across every color/label check. ANTI-CONFUSION notes present: header (cast-reveal = enzyme health NOT carbon fate; hero is a CARBON, C14 is its tracking label; do NOT claim the carbon becomes ATP) + true-tier inline (the gold on atp = the hero's ELECTRONS/the soul NOT the carbon; carbon shed as CO2; the metamorphosis). Source-citation convention held: 99 # src: citations (set_color viewing.py:2107, get_color_index querying.py:851, load importing.py:635, create creating.py:960, show_as viewing.py:528, show viewing.py:491, hide viewing.py:568, color viewing.py:1858, zoom viewing.py:65, select selecting.py:48, label viewing.py:1332, count_atoms querying.py:1412, iterate editing.py:1490, get_color_indices querying.py:843), all line numbers verified against tmp/pymol-src/modules/pymol/. DEVIATIONS (3 auto-fixed, all Rule 1-Bug, no scope creep): (1) Type 5 prochiral selection operator-precedence leak — the plan's `citrate and name C1 or name C2` parses as `(citrate and name C1) or (name C2)` (OR binds looser than AND), leaking name C2 to other _smoke.pdb-derived objects (current/substrate also have a C2) → would resolve to 4 not 2; fixed to `citrate and (name C1 or name C2)` → prochiral=2 PASS; (2) Stage 7 BEFORE state clobbered — the plan re-asserts the Type 3 mutant red spheres as "already applied" but Type 3b did hide everything which removed them; re-applied the mutant red spheres (show spheres + color red on enzyme resi 1) before the BEFORE assertion → comp_before spheres=10 color=red PASS; (3) create citation line accuracy — the plan listed creating.py:1001 (inside the body) but the def is at creating.py:960; cited the def line 960 (matches the cite-the-def-line convention). The smoke passed on the FIRST headless run after these fixes were written in (no failed iterations). NO code modified outside tools/scene_template_smoke.py (c14/pymol_layer/molops.py + c14/story/model.py + 05.4-CONVENTION.md UNTOUCHED, confirmed via git diff --name-only); AST gate clean (python3.6 tools/check_imports.py exit 0); NO new fixture files (reused _smoke.pdb/_edit_smoke.pdb/_wt_align_wt.pdb); NO fabricated science (placeholder structures only; ZERO real PDB IDs/DOIs/disease claims). 2 atomic commits (eb4db34 feat Task 1 write smoke + 2f2d15a test Task 2 headless PASS marker [--allow-empty — smoke passed first run, no fixes]). Next: 05.4-02 (hero-highlight headless prototype, parallel wave 2) → 05.4-04 (human design-review checkpoint against the 9-criterion review checklist + 11 OQs) → Phase 6 (Qt UI MVP, which implements the 3 deferred set_color/label/set dispatches + the 5.3 align dispatch).
- [Execution 05.4-02]: Phase 5.4 IN PROGRESS (Plan 02 of 04) — Plan 05.4-02 delivered the SC1 hero-highlight headless mechanism proof `tools/hero_highlight_smoke.py` (393 lines, 49 `# src: tmp/pymol-src` citations) running headlessly with `SMOKE_RESULT: PASS` and all 25 stage checks green via `bash tools/run_headless.sh tools/hero_highlight_smoke.py`. Pure pymol.cmd.* (NO Qt); calls cmd.* DIRECTLY (NOT via molops.apply — the new-op set_color/label/set dispatches are deferred to Phase 6 per convention §2.2/OQ-1, mirrors wt_align_smoke.py calling cmd.super directly). Proves the SC1 MECHANISM on placeholder fixtures, NOT real science (real hero-selectors are Phase 7 source-approved content). Stage 0: hero_gold defined via cmd.set_color (Okabe-Ito [0.90,0.62,0.0] PLACEHOLDER RGB pending Phase 7) + the named-color round-trip verified (get_color_index name→idx + get_color_indices(all=1) idx→name — NOT a hardcoded index, Pitfall 4). Case 1 hero-alone (Strategy A — the hero as its OWN single-atom object): cmd.create("hero_atom","mol and name C1") → the §3.3 5-call sequence (show_as spheres → set sphere_scale 1.0 → color hero_gold → label "C14"; dim omitted — single atom) → post-conditions count==1 / elem C / rep spheres / color-name round-trip hero_gold / label C14. Case 2 hero-in-substrate (Strategy B — the hero as a deterministic sub-sele): show_as spheres scoped to `mol and name C1` (per-atom scoping PROOF: count_atoms("mol and not name C1 and rep spheres")==0 — O1/C2 untouched) + the scoped dim `color gray80 mol and not name C1` (scoped-dim PROOF: color_name_for==gray80). Case 3 hero-at-enzyme-cast (case 3a co-load): placeholder enzyme _edit_smoke.pdb (17-atom ALA-GLY) show_as cartoon color gray (the cast keeps its OWN color NOT dimmed — color_name_for("enzyme and name CA")=="gray") + co-loaded substrate (1-atom from mol's C1) highlighted hero_gold spheres "C14" (the dim scoped to the SUBSTRATE only, NOT the enzyme — convention §3.3 critical scoping). Stage 4 the CO2-shed soul-transfer climax (convention §3.5): co2 object highlighted hero_gold one final time ("C14 (farewell)") → FADED (color gray80 + label cleared "" → iterate label==[""]) → TRANSFER the soul to nadh object (hero_gold APPEARS "soul (electrons)") — with a MANDATORY inline ANTI-CONFUSION comment (the gold on nadh = the ELECTRONS/the soul NOT the carbon; the carbon body was shed+faded; the carbon does NOT become NADH or ATP — scientifically WRONG; C14-decay DROPPED). The color_name_for() helper (cmd.iterate color-index [non-colliding collector `cidx`, RESEARCH-api §F Pitfall 1] → get_color_indices(all=1) idx→name [Pitfall 4]) is the reusable color-verification primitive. Source-citation convention held: 49 # src: citations (set_color viewing.py:2107, get_color_index querying.py:851, get_color_indices querying.py:843, load importing.py:635, create creating.py:960, show_as viewing.py:528, set setting.py:183, color viewing.py:1858, label viewing.py:1332, count_atoms querying.py:1412, iterate editing.py:1490), all line numbers verified against tmp/pymol-src/modules/pymol/. DEVIATIONS (2 auto-fixed, both Rule 1-Bug, no scope creep): (1) gray80 color-name round-trip — the color_name_for() helper initially called get_color_indices() with default all=0 which returns only 178 user-facing colors and OMITS standard extended colors like gray80 (idx=4236 — the scoped-dim + fade color); the custom hero_gold happened to be in all=0 (so Cases 1/3 passed) but gray80 returned None (so case2_dim_gray80 + climax_co2_faded FAILED on first run); fixed to get_color_indices(all=1) [the complete 5388-color superset] in both the helper + the Stage 0 check → all 25 checks PASS; (2) create citation line accuracy — the plan listed creating.py:1001 (inside the body) but the def is at creating.py:960; cited the def line 960 (matches the cite-the-def-line convention + the 05.4-03 precedent). NO code modified outside tools/hero_highlight_smoke.py (c14/pymol_layer/molops.py + c14/story/model.py + 05.4-CONVENTION.md UNTOUCHED, confirmed via git diff --name-only); AST gate clean (python3.6 tools/check_imports.py exit 0); NO new fixture files (reused _smoke.pdb/_edit_smoke.pdb per RESEARCH-api §I OQ-5); NO fabricated science (placeholder structures only; ZERO real PDB IDs/DOIs/disease claims). 2 atomic commits (80c10ed feat Task 1 write smoke + d38ab80 test Task 2 headless PASS incl the gray80 fix). Next: 05.4-04 (human design-review checkpoint against the 9-criterion review checklist + 11 OQs) → Phase 6 (Qt UI MVP, which implements the 3 deferred set_color/label/set dispatches + the 5.3 align dispatch).
- [Execution 05.4-04]: Phase 5.4 COMPLETE (Plan 04 of 04 — SC4 DELIVERED) — Plan 05.4-04 delivered the SC4 human design-review checkpoint APPROVED-WITH-ADJUSTMENTS (5 overrides + 6 approve-rec). The human reviewed the convention (05.4-CONVENTION.md §1-§8 + 11 OQs) + both smoke outputs (hero_highlight 25/25 PASS + scene_template 37/37 PASS) against the §8 review checklist. 5 OVERRIDES applied: OQ-2 mutant red→magenta (red confuses with the red of oxygen atoms in the structure; magenta is pure-hue-safe; fills 5.3 <mutant_color>=magenta); OQ-3 hero hero_gold→hero_cyan [0.0,0.75,0.75] PLACEHOLDER + ALL-C-colored ball-and-stick design (gold confuses with the yellow of sulfur; ALL carbons in the hero-bearing molecule are cyan sticks — the hero is 'one of them'; the hero is distinguished by ball-and-stick [sticks + SMALL sphere sphere_scale=0.3 via cmd.show NOT show_as] + 'YOU' label; non-carbons keep element colors NOT dimmed — the old scoped-dim gray80 REMOVED, gray80 now ONLY for CO2-shed fade; RGB PLACEHOLDER pending Phase 7); OQ-4 label 'C14'→'YOU' (player-facing identity label); OQ-6 multi-C fallback FAIL LOUDLY→default+warn+confirm gate (NOT fail-loudly [endless-loop], NOT silent heuristic [fabricated-science risk]; the confirm IS the no-fabricated-science guard; Phase 6 UI contract — smokes use known-by-construction selectors, the warn+confirm path is NOT exercised headlessly); OQ-11 APPROVE rec + NOTE (single-node reveal for MVP; the 3-sub-beat [approach→narrative fade for catalysis→product] is a DEFERRED FUTURE ENRICHMENT, NOT an infinite skip — potential student project / future development; the real-coordinate version needing QM/literature is the harder research-grade variant). 6 APPROVE-rec: OQ-1 (3 minimal ops NOT super-op; dispatch DEFERRED to Phase 6 — molops.py UNTOUCHED), OQ-5 (fade+transfer), OQ-7 (full-then-zoom), OQ-8 (data-level 2-object shape), OQ-9 (4 tier-templates), OQ-10 (5.4 specs BOTH, Phase 7 decides). CO2-shed anti-confusion CONFIRMED (soul-transfer = narrative device NOT carbon→ATP/NADH claim; C14-decay DROPPED — sole timescale = cycle-trap-until-host-death). Convention revised: 05.4-CONVENTION.md 751→766 lines, FROZEN for Phase 6/7/9/12; §2.3 palette (hero_cyan/gray/green/magenta/orange/gray80/red-bad-ending); §3.3 hero sequence (set_color→show_as sticks ALL→color hero_cyan elem C→[non-C keep element colors]→show spheres hero→set sphere_scale 0.3→label 'YOU' — 6 calls, NO scoped-dim); §3.5 CO2-shed (cyan fade+transfer); §3.6 anti-confusion (cyan not gold); §5.2 cast-reveal (magenta mutant spheres); §5.4 composition (<mutant_color>=magenta); §3.1 selection rule (default+warn+confirm); §4.3/§6.5 OQ-11 future-note; §7 ALL 11 OQs RESOLVED; §8 checklist updated. Both smokes updated + re-ran SMOKE_RESULT: PASS (hero_highlight 29/29 [new checks: all_carbons_cyan, noncarbon_element_color O1='oxygen', all_carbons_sticks, rep_sticks ball-and-stick proof]; scene_template 37/37 [magenta mutant, cyan hero, 'YOU' labels]). molops.py + model.py UNTOUCHED (OQ-1 dispatch deferred to Phase 6 — confirmed via git diff --name-only); AST gate clean; NO new fixtures; ZERO real PDB IDs in convention. 3 commits (df3e55f convention revision + b116a0d smoke updates + metadata). No follow-up work owed in 5.4 (OQ-1 dispatch deferred to Phase 6; OQ-6 warn+confirm is a Phase 6 UI contract; OQ-11 3-sub-beat is a future enrichment). Phase 5.4 COMPLETE ✓ (4/4 plans). Next: Phase 6 (Qt UI MVP).
- [Execution 06-01]: Phase 6 (Qt UI MVP) IN PROGRESS — Plan 06-01 COMPLETE ✓ (3/3 tasks): delivered the 4 deferred molops dispatches owed from 5.3/5.4. Added 4 new elif branches in `c14/pymol_layer/molops.py` `MolOps.apply` — `set_color`→`cmd.set_color(name, rgb)` (viewing.py:2107), `label`→`cmd.label(sele, '"text"')` (viewing.py:1332; the quoted-string footgun encapsulated — bare `YOU` would eval the atom property), `set`→`cmd.set(name, value, sele)` (setting.py:183), `align`→`cmd.super/align/cealign` (fitting.py:242/306/27) per `args["method"]` (default "super"; cealign arg order REVERSED [target,mobile] normalized by the dispatch so callers always pass [mobile,reference]; `align_sele` composes "{target} and {sele}" for BOTH; unknown method raises ValueError) — all FROZEN-verbatim from 05.4-CONVENTION.md:125-139 + 05.3-CONVENTION.md:240-258, using `self._cmd` already injected (NO new constructor param, NO new dependency, NO super-op per 5.4 §2.2). ALSO extended the `load` branch with a target-prefix fallback parsing `action.target` (`pdb:`/`cid:`/`sid:`/bare-filename) when no source/file/code/cid keys are present (the 5.1 skeleton on_enter form) — fixes the KeyError on `intro.preface`'s `{"op":"load","target":"pdb:4PFK","args":{"object":"pfk"}}` — while preserving the explicit-source backward-compat path (checks `"file" in args` FIRST; test_molops.py:220-251 stay green). KEY DECISIONS: (1) 4 minimal peer-primitive ops NOT a composite super-op (mirrors 5.3/5.4); (2) label quoted-string footgun encapsulated at the dispatch layer; (3) cealign REVERSED arg order normalized at the dispatch; (4) load target-prefix fallback preserves backward-compat. 22 MockCmd unit tests green (14 op-dispatch + 8 load target-prefix/backward-compat: arg order, quoted-string label, cealign reversal, align_sele composition, ValueError on unknown method, target-prefix parsing, no-object-stripped-code default, + a backward-compat regression guard re-running the 4 explicit-source forms). Headless smoke `tools/molops_deferred_dispatch_smoke.py` SMOKE_RESULT: PASS (13/13 stage checks): dispatching the 6-call hero-highlight + an align + a target-prefix load via `molops.apply` reproduces the direct-cmd smokes' post-conditions (count=1/elem C/rep spheres+sticks/color hero_cyan/label YOU; align moved mobile wt 8.85→1.46 right arg order; load bare-filename→load_bundled hero2=3 NO KeyError; load pdb:→fetch_pdb mock NO network). AST gate clean; full suite green (no regression). Unblocks the controller (06-06) for SC2 + the 5.3 WT-aligned reveal + the skeleton on_enter load form. DEVIATIONS (2, both Rule 3 parallel-staging races, no scope creep): (1) Task 3 commit 524362a bundled 2 concurrent 06-02/06-05 test files (test_engine.py +176 / test_state.py +29) that were pre-staged in the git index — left as-is per the 05.1-14 precedent (do NOT rewrite history while parallel agents active; `git reset --soft` risks orphaning a parallel commit); (2) the 06-01 SUMMARY was swept into the parallel 06-03 commit 335fdfa before its dedicated docs(06-01) commit 21ffc18 superseded it — switched to path-limited commits (`git commit -m "..." -- <path>`) to avoid sweeping parallel-staged files. 4 commits (caa1d40 feat Task 1 + ff78d5b test Task 2 + 524362a feat Task 3 [bundled] + 21ffc18 docs). Next: 06-06 controller (forwards MolActions to molops.apply) + the rest of Phase 6 (Qt UI MVP).
- [Execution 05.1-EXPANSION]: Phase 5.1 IN PROGRESS (EXPANSION COMPLETE) — the user-approved tiered-completeness expansion grew the glucose skeleton from 34 to 43 nodes (+9 net) per Decisions 1-5: (1) tiered completeness = FULL nodes with PDB loads for HIGH-priority teaching points + NARRATIVE-only nodes to complete the cycle visibly; (2) α-KG DH = narrative-only (no PDB); (3) split Complex II + III + IV into separate nodes (Complex IV also split out); (4) expand the lactic anaerobic path into an LDH playable step; (5) add PDB load to tca.aconitase now. NEW nodes: gly.pyruvate_kinase (FULL, PDB 7FS3 candidate) + anaer.ldh (FULL, PDB 5W8J candidate) + tca.isocitrate_dh (FULL, PDB 5GRF candidate) + tca.akg_dh (NARRATIVE, no PDB per Decision 2) + tca.succinyl_coa_synthetase (NARRATIVE) + tca.fumarase (NARRATIVE) + tca.malate_dh (NARRATIVE, 2 choices loop/forward but SC5 'neither' = not tier-determining) + etc.complex_ii (NARRATIVE) + etc.complex_iii (NARRATIVE) + etc.complex_iv (FULL, PDB 1OCC candidate); REMOVED: etc.complex_ii_iii_iv (compressed). 4 FULL + 6 NARRATIVE new nodes = 10 added - 1 removed = +9 net. NEW PDB loads added to EXISTING nodes: pyr.pdh (PDB 2OZL candidate) + tca.aconitase (PDB TBD_ACONITASE — human mito ACO2 UniProt Q99798 has NO PDB structure, verified via UniProt REST: 0 cross-refs; bovine 1ACO at 2.05 Å is the classic Ogston-hypothesis teaching candidate for Phase 7 review; NO fabricated PDB ID). 6 PDB candidates total pending Phase 7 approval (7FS3, 2OZL, 5W8J, 5GRF, 1OCC, TBD_ACONITASE); NONE added to data/citations.json or data/sources.json (separate Phase 7 approval batch); all new nodes use PLACEHOLDER_PHASE7* claim_ids. Ending distribution UNCHANGED (1T+3G+2N+5B=11 — the expansion added path nodes, not endings). RNG shuffle stays the SOLE stochastic node (0.5/0.5 unchanged — the 5 new TCA enzymes are DOWNSTREAM of the shuffle, they embody its outcome, not new RNG). Topology: gly.fbp_to_pyruvate (now steps 4-9) -> gly.pyruvate_kinase -> gly.pyruvate; anaer.entry lactic choice -> anaer.ldh -> anaer.lactic; tca.co2_turn1/turn2 forward -> tca.isocitrate_dh -> tca.akg_dh -> tca.succinyl_coa_synthetase -> tca.fumarase -> tca.malate_dh -> (loop to tca.shuffle OR forward to tca.divert_to_good); etc.complex_i -> etc.complex_ii -> etc.complex_iii -> etc.complex_iv -> etc.atp_synthase. SC2 STILL GREEN (all 4 tiers reachable, 0 dangling diverts, both RED variants still RED). SC5 count updated: MC-choice=6 + both=6 + neither=31 + edit-allowed=0 = 43 (was 6+6+22=34; the 9 new nodes are all 'neither' — forced-advance narrative/PDB-load nodes, not branching choices, not edit-allowed). Edit-allowed nodes UNCHANGED (5 + tca.shuffle). 5 atomic commits: 91fb30c (glycolysis) + dc6f549 (LDH + PDH PDB) + 506f02b (TCA 5 enzymes + aconitase PDB) + a9811da (ETC split) + a61b74c (docs+tests). 05.1-DESIGN.md (43-row SC5 table, updated SC3/SC4 tables, updated reachability chains, updated OQ5/OQ6) + 05.1-GRAPH-DIAGRAM.md (mermaid diagram with 9 new nodes + new "New Nodes Added" table) + tests/test_glucose_reachability.py (renamed test_manifest_loads_all_34_nodes -> _43_nodes, count 34 -> 43) all updated. Full suite 203 tests pass (zero regression); AST gate clean. See 05.1-EXPANSION-SUMMARY.md. DEVIATIONS: (a) [Rule 3-Blocking] aconitase PDB ID does not exist for the human mitochondrial enzyme — used pdb:TBD_ACONITASE placeholder + documented bovine 1ACO candidate for Phase 7 review; (b) [Rule 3-Blocking] commit dc6f549 title wording inaccuracy (says "PDH/aconitase PDB loads" but only PDH load was added in that commit — aconitase load added in the next commit 506f02b; documented in summary, no amend). Downstream: 05.1-06 (human review checkpoint — review the 9 new nodes + 6 PDB candidates + the aconitase-no-PDB finding + the tca.malate_dh 2-choice loop framing); Phase 7 (replace 38 PLACEHOLDER_PHASE7* claim_ids with approved claims; approve/swap the 6 PDB candidates; decide the aconitase PDB strategy).
- [Execution 05.1-15]: Phase 5.1 IN PROGRESS (expansion Wave 2 COMPLETE) — Plan 05.1-15 updated 05.1-DESIGN.md (the authoritative companion design doc) for the post-expansion skeleton: 43->54 nodes, 11->20 endings (1T+3G+2N+14B), start intro.select->intro.preface. 8 sections updated in 1 file, 2 atomic commits (ea1763e + ca85c9b), doc-only zero test impact. (A) §1 Overview: 54-node inventory (was 43), 1T+3G+2N+14B=20 endings (was 11), expansion paragraph documenting Plans 05.1-12/13 (Req 1 +9 bad endings [6 1a edit:unknown + 3 1b edit:known] + Req 2 +2 preface nodes [intro.preface Beat A + intro.shell_glucose Beat B] + manifest start shift + gly.start hide-20-AA), 11 new node-inventory rows, "3 stubs + 2 preface nodes" clarification, "11 NEW nodes" note. (B) §3 File split: intro.json 3->5, bad_endings.json 5->14, total 43->54, manifest start intro.select->intro.preface, gly.start on_enter note. (C) §4 Choice-point contract: +2 preface MC-choice rows (intro.preface Continue+Observe->intro.select; intro.shell_glucose Continue+Observe->gly.start) + edit.prompt stub row updated (3->12 structural choices: 1 edit:known_critical + 8 edit:unknown + 3 edit:known). (D) §5 Edit-node contract: edit.prompt stub 12-choice description + 1a/1b distinction (1a edit:unknown RngEngine-weighted random pool 2->8; 1b edit:known deterministic, binding is Phase 7) + shared-pool decision (NOT per-enzyme for skeleton; per-enzyme is Phase 7) + edits.json pool expansion note (Phase 7 expands bad_ending_pool to include 6 new 1a ids; 1b ids NOT in random pool). (E) §6 SC5 table: 54-row table (+11 new rows: 2 preface MC-choice #1+#3 + 9 bad-ending neither #45-53) + updated counts MC-choice=18 (was 16; +2 preface), both=9 (UNCHANGED), edit-allowed=5 (UNCHANGED), neither=22 (was 13; +9 bad endings), total=54 + 20-ending distribution verification (1T+3G+2N+14B; Bad 5->14 via 05.1-12; True/Good/Normal UNCHANGED). (F) §7 Reachability: green-test code block updated (assert 20 endings, start intro.preface) + reachability chains table (+9 bad-ending rows all via edit.prompt structural path; True-ending chain prepended intro.preface->intro.select->intro.shell_glucose->gly.start; anaerobic paths start intro.preface->intro.select but skip intro.shell_glucose [glucose-only]) + tiered-completeness note 11->20 endings + red-test description (g.start_node() now intro.preface) + tests file subsection 5->9 tests (5 original + 3 replan-invariant + 1 bad-ending-reachability). (G) §8 Backward-compat: no-impact note (JSON + doc only; existing MolOps ops hide_all/load/show_as; pdb:TBD_* placeholders follow TBD_ACONITASE precedent; 198 prior tests unchanged; 8 glucose tests updated in Plan 14). (H) §9 Open questions: +8 new OQs (OQ-BAD-1 per-enzyme pools Phase 7 [LOW]; OQ-BAD-2 1b binding Phase 7 [MEDIUM]; OQ-BAD-3 bad-ending teaching text Phase 7 [INFO]; OQ-PREFACE-1 per-character shell DECIDED [LOW]; OQ-PREFACE-2 20-AA cartoon Phase 5.2 [MEDIUM]; OQ-PREFACE-3 gly.start hide-20-AA DECIDED [LOW]; OQ-PREFACE-4 hero-atom load Phase 5.2/6 [MEDIUM]; OQ-PREFACE-5 0-single-Continue DECIDED [INFO]) + still-pending-checkpoint note (3 CRITICAL disease-mutant decisions + 24 prior OQs STILL pending 05.1-06 re-review; Plans 12-13 implement ONLY the 2 new requirements) + footer 2026-08-22 expansion entry. KEY DECISIONS documented: shared pool (NOT per-enzyme); 1a/1b tag distinction; per-character shell node (NOT parametric); gly.start hide-20-AA (NO journey_start node); 0-single-Continue UNCHANGED via Observe 2nd choice; pdb:TBD_* placeholders (NO fabricated PDB IDs). Rule 3 deviation: concurrent-execution orphan — Task 2 commit d5a70a0 diverged from HEAD due to parallel Plans 14+16 committing on the same base; recommitted as ca85c9b on top of the concurrent branch. 05.1-DESIGN.md is now the accurate REVIEWABLE artifact for the 54-node skeleton. Downstream: ONLY 05.1-06 (human review of the FINAL post-expansion skeleton with 2 new requirements + 3 CRITICAL disease-mutant decisions + 32 OQs) remains. After 06, Phase 5.1 is COMPLETE and Phase 6 (Qt UI MVP) is UNBLOCKED.
- [Execution 05.1-16]: Phase 5.1 IN PROGRESS (expansion Wave 2 ESSENTIALLY COMPLETE) — Plan 05.1-16 regenerated the story-graph diagram (SVG + ASCII) from the 54-node post-expansion skeleton + updated 05.1-GRAPH-DIAGRAM.md (the visual-narrative artifact for the 05.1-06 re-review). 05.1-graph.svg + 05.1-graph.txt regenerated via tools/render_story_graph.py (exit 0; 54 nodes, 89 edges; SVG well-formed XML). The renderer auto-detected the 11 new nodes via id-prefix stage grouping (bad.* -> "bad" stage, intro.* -> "intro" stage) + edit:enzyme tags + is_ending — ZERO renderer code change. 14 #FFF8DC edit-allowed markers present (UNCHANGED). intro.preface is the BFS root/start (top of ASCII, first intro-stage node in SVG). edit.prompt -> 12 bad endings structural edges (3 existing + 9 new). 05.1-GRAPH-DIAGRAM.md updated: header 43->54 nodes / 11->20 endings; post-expansion paragraph; preface-node legend note; mermaid +11 nodes (IP + ISG + 9 bad endings) + new edges (IP->IS->ISG->GS + 9 EP structural) + badEnd class; Ending-Tier table Bad 5->14 (total 20; all 14 bad endings listed); Edit-Allowed UNCHANGED note; new "Bad-Ending + Preface Expansion (Plans 05.1-12/13)" section (11-node table + Decisions Locked + Pending 05.1-06 Re-review note); Key Design Decisions +2 (CG-collection + preface sequence). Doc-only, zero test impact (8 reachability tests OK + 206 full suite OK). Rule 3 deviation: a parallelization race swept Task 1's staged svg/txt into the concurrent Plan 05.1-14's feat commit 3a268c0 (deliverable committed + verified, just attributed to 05.1-14); Task 2 committed cleanly as 32c4dd5. Concurrent: 05.1-14 DONE (test assertions fixed, 3a268c0); 05.1-15 DONE (ea1763e + ca85c9b; DESIGN.md 8 sections updated for 54-node/20-ending/intro.preface-start skeleton).
- [Execution 05.1-14]: Phase 5.1 IN PROGRESS (expansion Wave 2 IN PROGRESS -> COMPLETE) — Plan 05.1-14 updated the SC2 reachability tests for the post-expansion skeleton (Plans 05.1-12 + 05.1-13): 3 stale assertions fixed (node 43->54, ending 11->20 [1T+3G+2N+5B -> 1T+3G+2N+14B], start intro.select->intro.preface) + 1 new invariant test (test_all_new_bad_endings_reachable_from_edit_prompt: all 9 Plan-12 bad endings reachable from edit.prompt via choice.goto + 1a edit:unknown / 1b edit:known tag distinction). test_manifest_loads_all_43_nodes renamed to _54_nodes. Module + class + 2 red-variant docstrings rewritten for the 05.1-12/13 expansion; 2 RED variants still RED from intro.preface (assertions UNCHANGED); 3 replan invariants (0 single-Continue, 14 edit-allowed, pdh cast-PDB fix) UNCHANGED; no-pymol test UNCHANGED. 9 reachability tests PASS (8 prior + 1 new); full suite 207 tests PASS (was 206; +1; zero regression). 2 atomic commits (3a268c0 feat Task 1 + 37fa3bb feat Task 2). KEY DECISIONS: (1) updated (not weakened) the 3 stale assertions to EXACT new final values (not ranges) so a future regression off the final count fails loudly; (2) added the bad-ending-reachability invariant as a NEW test (not a re-scope) so the 8 prior tests stay verbatim in logic; (3) did NOT rewrite git history after the parallel-race deviation (see below). DEVIATIONS: 2 (both Rule 3-Blocking, parallel-execution race from parallelization=true): (1) Task 1 commit 3a268c0 accidentally bundled 2 pre-staged diagram files (05.1-graph.svg/txt) left in the git index by an interrupted Plan 16 attempt -- the test content is correct + verified, the diagram content is correct (54-node version) just redundantly attributed; not rewritten (parallel agents active); (2) a `git reset --soft HEAD~1` intended to undo the bundled commit instead undid a parallel Plan 15 agent's commit (d5a70a0) because HEAD had moved via parallel commits -- restored via `git reset --soft d5a70a0`; no data loss. NOTE: the concurrent Plan 16 agent's STATE.md entry incorrectly attributes Plan 14 Task 2 to commit 32c4dd5 (actually Plan 16's own GRAPH-DIAGRAM commit); the TRUE Plan 14 Task 2 commit is 37fa3bb (this entry corrects the record). Next: 05.1-06 (human review of the FINAL post-expansion skeleton).
- [Execution 05.1-13]: Phase 5.1 IN PROGRESS (expansion Wave 2 IN PROGRESS) — Plan 05.1-13 added the story preface sequence (Requirement 2 from the 05.1-06 checkpoint feedback: Beat A hero blessing before character select + Beat B shell beat after character select before the journey). 2 new preface nodes in intro.json (3->5 nodes): intro.preface (Beat A, pre-character-select) — on_enter [hide_all + load pdb:TBD_HERO_CARBON hero_atom spheres + load pdb:TBD_AA_CAST_20 aa_cast cartoon] + 2 choices (Continue + Observe mc:observe, both -> intro.select); intro.shell_glucose (Beat B, post-character-select, pre-journey) — on_enter [hide_all + load pdb:TBD_GLUCOSE glucose sticks + show_as aa_cast cartoon] + 2 choices (Continue + Observe mc:observe, both -> gly.start). manifest start intro.select->intro.preface. intro.select glucose goto rerouted gly.start->intro.shell_glucose (FA/Alc unchanged — their shell nodes are Phase 8). gly.start on_enter [hide_all] -> [hide_all + show_as glucose sticks] (hide 20-AA cast, re-show hero molecule; glucose persists from intro.shell_glucose — hide_all hides but does not delete). KEY DECISIONS: (1) per-character shell node (NOT parametric) — less duplication for glucose; FA/Alc Phase 8; (2) hide-20-AA via gly.start on_enter (NO dedicated journey_start node — simpler); (3) 0-single-Continue UNCHANGED (NOT re-scoped) — each preface node gets Continue + Observe mc:observe (same-destination, different-experience, per Plans 07-09 pattern); (4) 20-AA cartoon: show_as rep "cartoon" (naturally hides terminals; exact cmd is Phase 5.2/6); (5) skeleton carries on_enter MolAction PLACEHOLDERS using EXISTING MolOps op vocabulary (hide_all/load/show_as); TBD_* PDB targets loud-fail until Phase 6/7 (NO fabricated PDB IDs); text is Phase 7 (PLACEHOLDER_PHASE7). NO fabricated science. Direct BFS GREEN from intro.preface: 52->54 nodes, ending count UNCHANGED 20 (1T+3G+2N+14B; preface nodes NOT endings), all 4 tiers reachable; 14 edit-allowed UNCHANGED; 0 single-Continue UNCHANGED. 2 atomic commits (6541648 + d28b8d5). DEVIATION: [Rule 3-Blocking] committed interrupted Plan 12's leftover metadata (05.1-12-SUMMARY.md + STATE.md + ROADMAP.md) as docs(05.1-12) commit 51b9bc2 so Plan 12 + 13 docs don't conflate. NOTE: 2 STALE-COUNT/START test assertions still fail as EXPECTED (43->54 nodes, 11->20 endings, start intro.select->intro.preface) — fixed in 05.1-14; other 6 reachability tests still PASS. Next: 05.1-14 (test count/start assertions update + new bad-ending-reachability invariant), 05.1-15 (DESIGN.md expansion update), 05.1-16 (diagram regen), then 05.1-06 (human review of the FINAL post-expansion skeleton).
- [Execution 05.1-12]: Phase 5.1 IN PROGRESS (expansion Wave 2 IN PROGRESS) — Plan 05.1-12 expanded the bad-ending pool for the CG-collection feel (Requirement 1 from the 05.1-06 checkpoint feedback: 1a randomize/expand the unknown-edit pool + 1b known-consequence bad endings). 9 new bad-ending nodes added to data/story_glucose/bad_endings.json (0 nodes removed): 6 1a unknown-pool [bad.enzyme_collapse, bad.wrong_substrate_trapped, bad.broken_active_site, bad.lost_in_cytosol, bad.proton_leak, bad.misfolded_aggregate — tagged edit:unknown; the RngEngine-weighted unknown-edit pool grows 2 -> 8 so the SAME wrong edit can produce DIFFERENT bad endings] + 3 1b known-consequence [bad.active_site_destroyed, bad.substrate_channel_blocked, bad.cofactor_lost — tagged edit:known, deterministic outcomes of specific known edits, distinct from the 1a random pool; the BINDING to specific edits is Phase 7 edits.json content]. edit.prompt stub structural choices expanded 3 -> 12 (8 edit:unknown + 3 edit:known + 1 existing edit:known_critical) so the BFS sees all 9 new bad endings reachable. All 9 new nodes: is_ending='bad' + claim_ids=['PLACEHOLDER_PHASE7_BAD'] + on_enter=[{hide_all}] + choices=[] (ending nodes). KEY DECISIONS: (1) shared pool (NOT per-enzyme) for the skeleton — variety via node count (8 unknown + 3 known), not new routing logic; per-enzyme pools are Phase 7 (Phase 4 EditRouter OVERRIDE semantics already support them); (2) 1b reachable via edit.prompt structural choices for the BFS — deterministic routing is Phase 7; (3) edit:known tag distinguishes 1b from 1a's edit:unknown (existing bad.critical_residue_break uses edit:known_critical, a third category). NO fabricated science (PLACEHOLDER_PHASE7_BAD only; no new PDB IDs or DIS-*-cand claims in data/citations.json). Direct BFS GREEN: 43->52 nodes, 11->20 endings (1T+3G+2N+14B; Bad tier 5->14), all 4 tiers reachable, no unreachable endings; 14 edit-allowed UNCHANGED; 0 new single-Continue; True/Good/Normal counts unchanged (1/3/2). 2 atomic commits (eab886b feat Task 1 [1a] + 68e2d68 feat Task 2 [1b]). NOTE: 2 STALE-COUNT test assertions now fail as EXPECTED (test_manifest_loads_all_43_nodes: 43->52; test_reachability_green_all_four_tiers: 11->20 endings) — fixed in 05.1-14; the other 6 reachability tests still PASS. Next: 05.1-13 (preface sequence +2 nodes -> 54 total), 05.1-14 (test count assertions update), 05.1-15 (DESIGN.md expansion update), 05.1-16 (diagram regen), then 05.1-06 (human review of the FINAL post-expansion skeleton).
- [Execution 05.1-10]: Phase 5.1 IN PROGRESS (replan Wave COMPLETE) — Plan 05.1-10 updated 05.1-DESIGN.md to match the post-replan skeleton exactly (Driver 3: doc + test updates). 3 section updates in 1 file, 2 atomic commits (eac6eb4 + 32746d7), 206 tests zero regression. (A) SC5 classification table (§6) reclassified for post-replan state: MC-choice=16, both=9, edit-allowed=5, neither=13 (was 6/6/31/0); 0 single-Continue nodes (15 Continue-to-MC conversions); 14 edit-allowed nodes = 12 disease-mutant (DIS-*-cand) + 1 structural (CS edit:structural, no DIS-*-cand) + 1 shared (tca.shuffle edit:enzyme:tca.aconitase, no DIS-*-cand of its own). Added DIS-*-cand annotations per node. The 5 "edit-allowed"-class nodes (gly.pfk, pyr.pdh, tca.aconitase, tca.citrate_synthase, etc.complex_i) have only 1 non-edit choice (NOT "both"); the 9 "both"-class nodes (8 promotions + tca.shuffle) have ≥2 non-edit choices + edit:offer. (B) Edit-node contract (§5) expanded from 6 rows to 14 rows: each documents enzyme_id + DIS-*-cand CANDIDATE claim + reverse-mutation signature (NAME ONLY — e.g. PKLR R479H→H479R, NOT the edits.json entry) + known-edit branch route (edit.prompt→EditRouter→branch, Phase 7) + unknown-edit bad-pool fallback. Documented CS structural-exploration exception (no disease mutant; restoration narrative does NOT apply), tca.shuffle shared edit point (same tca.aconitase EditRouter bucket), pyr.pdh 2OZL→6CFO cast-PDB fix (6CER option (b) = Phase 7 content decision), etc.complex_i PLACEHOLDER→DIS-NDUFS8-01-cand claim_id fix. Added Phase 7 CONTENT boundary subsection (skeleton = structure: edit:enzyme tags + edit:offer choices + DIS-*-cand claim references; edits.json + text + on_enter pre-edit MolActions = Phase 7). (C) §4 mc:observe note (Observe choices follow same SC3 contract; mechanism unchanged) + updated §4 edit-allowed choices table (5 "edit-allowed" rows, was 5 "both" rows). (D) §9 appended 11 new disease-mutant open questions (OQ-DM1-DM11) assembled from the 3 research docs, cross-referenced to node ids + claim_ids, with "05.1-06 checkpoint must resolve these before Phase 7" pointer. IDH3A-default/IDH1-alternative explicitly FLAGGED (OQ-DM1 CRITICAL — skeleton uses IDH3A M204I as DEFAULT; IDH1 R132H cancer-mutant alternative documented but NOT chosen unilaterally). pyr.pdh cast-PDB fix (OQ-DM2 CRITICAL), CS reframe (OQ-DM3 HIGH), OGDH scan (OQ-DM4), PFKM choice (OQ-DM5), Aconitase 1ACO homology (OQ-DM6), COX4I1 numbering (OQ-DM7), CYC1 choice (OQ-DM8), LDH not-promoted (OQ-DM9), ETC PDB-organism caveat (OQ-DM10), DIS-*-cand candidate status (OQ-DM11). Original 13 OQs preserved. Updated factual references across §1 (pyr.pdh 6CFO), §9 OQ6 (2OZL→6CFO), §9 OQ8 (0 single-Continue), §9 citation-gate summary (12 DIS-*-cand), doc footer. DEVIATION: [Rule 1-Bug] updated §4 edit-allowed table for factual accuracy (plan said preserve §4 unchanged, but the table listed "5 edit-allowed nodes all both" which was wrong post-replan — corrected to "edit-allowed" classes + pointer to §5 for full 14-node list; no structural change). 05.1-DESIGN.md is now the accurate REVIEWABLE artifact — the 05.1-06 checkpoint can read it + spot-check the JSON and find full consistency. Downstream: ONLY 05.1-06 (human review of the FINAL skeleton with all replan changes incl IDH3A-vs-IDH1 + akg_dh deeper-scan + CS edit:structural + bovine-1ACO homology caveat) remains. After 06, Phase 5.1 is COMPLETE and Phase 6 (Qt UI MVP) is UNBLOCKED.
- [Execution 05.1-08]: Phase 5.1 IN PROGRESS (replan Wave) — Plan 05.1-08 applied disease-mutant research + Continue-to-MC directive to tca.json: 8 node modifications, 0 nodes added/removed (43 total unchanged). 4 TCA enzymes PROMOTED to edit-allowed (each gained edit:enzyme:<id> tag + DIS-*-cand claim_id + edit:offer choice to edit.prompt): tca.isocitrate_dh [IDH3A M204I -> DIS-IDH3A-01-cand, RP90 OMIM 619007, ClinVar Pathogenic; PDB 5GRF is IDH3A — biochemically correct], tca.succinyl_coa_synthetase [SUCLG1 G85A -> DIS-SUCLG1-01-cand, MTDPS9 OMIM 245400; skeleton 6WCV is SUCLG1, no PDB swap], tca.fumarase [FH R233H -> DIS-FH-01-cand, HLRCC OMIM 150800; UniProt "catalytically inactive" + multi-submitter ClinVar Pathogenic — strongest candidate], tca.malate_dh [MDH2 G37R -> DIS-MDH2-01-cand, DEE51 OMIM 617339]. The 3 linear promoted nodes (isocitrate_dh/succinyl_coa_synthetase/fumarase) gained mc:observe Observe 2nd choices (3-choice: Continue + Observe + edit:offer); tca.malate_dh got edit:offer ONLY (already 2-choice loop/forward; adding Observe would dilute the branch point; 3 choices total: loop + forward + edit:offer; em-dash "Continue onward — the soul calls" label preserved VERBATIM). KEY DECISION: IDH3A is the DEFAULT (biochemically-correct TCA enzyme, NAD+-dependent mitochondrial, PDB 5GRF matches); the IDH1 R132H cancer-mutant alternative (real mutant PDB 3INM, glioma OMIM 137800, narratively powerful but NOT the TCA enzyme — cytosolic NADP+-dependent isoform) is FLAGGED for 05.1-06 human review, NOT chosen unilaterally (researcher's presentation, not a unilateral decision per AGENTS.md no-fabricated-science). tca.aconitase += DIS-ACO2-01-cand (S112R ICRD, ClinVar 29584 Pathogenic, OMIM 614559) alongside APPROVED TCA-RNG-CITRATE-PROCHIRALITY-01; NO MC change (already edit-allowed + 2 choices; bovine-1ACO homology + residue-numbering caveat is Phase 7 text_teaching content, NOT in skeleton). tca.entry + tca.akg_dh Continue-to-MC: appended mc:observe Observe 2nd choices (2 choices each); tca.akg_dh NOT promoted (no edit:enzyme tag, no edit:offer, no DIS-*-cand) — OGDH has NO confirmed pathogenic missense in first 14/163 ClinVar Oxoglutaricaciduria records examined; LOW-confidence negative flagged for deeper scan at 05.1-06 (not "OGDH has no pathogenic missense" — a complete scan may surface one). tca.citrate_synthase REFRAME: tags += edit:structural (CS has NO disease point mutant; 7 ClinVar Pathogenic records are ALL structural variants — inversions/CN-gains, not implementable via cmd.alter; UniProt O75390 disease field empty); keeps existing edit:enzyme tag + 2 choices unchanged; NO DIS-*-cand claim added (the edit:enzyme tag predates the restoration narrative — was for structural exploration; research Recommendation note a option i adopted; human may later remove the edit:enzyme tag — flagged at 05.1-06). All 5 DIS-*-cand claims (ACO2, IDH3A, SUCLG1, FH, MDH2) are CANDIDATE pending Phase 7 per-claim approval (skeleton REFERENCES candidates, does NOT assert disease as approved fact — AGENTS.md no-fabricated-science; citation gate NOT run during Phase 5.1). 2 atomic commits (eb4b71f feat Task 1 + 3e2a731 feat Task 2). Reachability GREEN (5 tests, 203 suite, zero regression; 43 nodes, 11 endings, 4 tiers; all 7 edit:offer choices in tca.json goto edit.prompt which exists in bad_endings.json — no orphaned endings). DEVIATIONS: none (plan executed exactly as written); minor plan misstatement noted (plan verification said "tca.json has 14 nodes" but actual is 12 — binding criterion "43 total unchanged" holds; no code impact). Downstream: 05.1-10/11 (DESIGN.md SC5 per-node classification table update for the replan promotions — edit-allowed grew from 5+shuffle to 9+shuffle across glycolysis/TCA/ETC; tests invariants), then 05.1-06 (human review of the FINAL skeleton with ALL replan changes incl the IDH3A-vs-IDH1 decision + akg_dh deeper-scan flag + CS edit:structural reframe + bovine-1ACO homology caveat).
- [Execution 05.1-09]: Phase 5.1 IN PROGRESS (replan Wave) — Plan 05.1-09 applied disease-mutant research + Continue-to-MC directive to etc_atp.json: 5 node modifications, 0 nodes added/removed (43 total unchanged). 3 ETC complexes PROMOTED to edit-allowed (etc.complex_ii SDHA W512R -> DIS-SDHA-01-cand [PMID 7550341 Bourgeron 1995, first nuclear resp-chain mutation]; etc.complex_iii CYC1 C12W -> DIS-CYC1-01-cand [PMID 23910460 Gaignard 2013, MC3DN6; W96C-vs-L215F caveat flagged for 05.1-06]; etc.complex_iv COX4I1 T130P -> DIS-COX4I1-01-cand [PMID 31290619 Pillai 2019, MC4DN16; COX4I1-not-SURF1 — SURF1 is assembly factor absent from 1OCC, COX4I1 is the regulatory subunit in 1OCC chain D; P152-vs-T130 numbering caveat flagged]); each now 3-choice (Continue + mc:observe Observe + edit:offer to edit.prompt). etc.complex_i claim_id FIX: PLACEHOLDER_PHASE7_ETC REPLACED by DIS-NDUFS8-01-cand (PMID 9837812 Loeffen 1998, first nuclear Complex I Leigh mutation; REPLACE not append per replan instruction; edit:enzyme:etc.complex_i tag + 2 choices preserved unchanged; ETC pathway-fact claim deferred to Phase 7). etc.atp_synthase Continue-to-MC: appended mc:observe Observe choice (both choices goto end.true; "Become ATP — the soul-jump" em-dash U+2014 label preserved VERBATIM; NOT edit-allowed — climax node, no disease mutant in scope). All 4 new DIS-*-cand claims are CANDIDATE pending Phase 7 per-claim approval (skeleton REFERENCES candidates, does NOT assert disease as approved fact — AGENTS.md no-fabricated-science; citation gate NOT run during Phase 5.1). 2 atomic commits (0225a99 feat + 5b7641d fix). Reachability GREEN (5 tests, 203 suite, zero regression; 43 nodes, 11 endings). Next: 05.1-08 (TCA replan) + 05.1-10/11 (DESIGN.md SC5 table + tests invariants), then 05.1-06 (human review of the FINAL skeleton with all replan changes).
- [Execution 05.1-07]: Phase 5.1 IN PROGRESS (replan Wave) — Plan 05.1-07 applied disease-mutant research + Continue-to-MC directive to glycolysis.json + pyruvate_branch.json: 8 node modifications, 0 nodes added/removed (43 total unchanged). gly.pyruvate_kinase PROMOTED to edit-allowed (7th edit-allowed enzyme: tags += edit:enzyme:gly.pyruvate_kinase; claim_ids += DIS-PKLR-01-cand; 3 choices = Continue + mc:observe Observe + edit:offer to edit.prompt; cast PDB 7FS3 unchanged). PKLR mutation is R479H (NOT R479W per REPLAN-CONTEXT) — CORRECTED by research (UniProt P30613 VAR_011480, Valentini 2002 PubMed:11960989, Kanno 1994 Amish PubMed:8161798, dbSNP rs118204085; reverse-mutation H479R = Phase 7 edits.json content). pyr.pdh cast PDB fixed: 2OZL (S264E phospho-mimic mutant, NOT wild-type per RCSB title "Human pyruvate dehydrogenase S264E variant") -> 6CFO (WT, 2.70 A, Whitley 2018 PubMed:29970614, same paper as the V138M mutant 6CER); claim_ids += DIS-PDHA1-01-cand (PDHA1 V138M=UniProt V167M, PDHAD MIM:312170) + CAST-PDH-WT-PDB-01-cand (6CFO). gly.pfk += DIS-PFKM-01-cand (PFKM G209D, GSD VII/Tarui MIM:232800) alongside APPROVED claims. 5 linear nodes (gly.start/g6p/fbp_to_pyruvate/pyruvate + anaer.ldh) gained mc:observe Observe 2nd choices (same destination, different experience — biochemistry fixed). anaer.ldh NOT promoted (GSD11 MIM:612933 is real but canonical Maekawa-1990 mutation is 20-bp frameshift not point mutation; Q100Ter pathogenic but nonsense (mechanically incompatible with cmd.alter); R171H is ClinVar VUS not pathogenic — per research recommendation). 6CER (CAST-PDH-MUTANT-PDB-01-cand, the V138M mutant PDB) NOT added to skeleton — skeleton loads ONE cast PDB per node; the mutant-reveal-vs-apply-edit design choice is Phase 7 on_enter-content. All 4 DIS-*-cand + 1 CAST-PDH-WT-PDB-01-cand are CANDIDATE pending Phase 7 per-claim approval (skeleton REFERENCES candidates, does NOT assert disease as approved fact — AGENTS.md no-fabricated-science). No text_dramatic/text_teaching changes; no edits.json entries; no on_enter content beyond the cast-PDB id swap. Reachability GREEN (5 tests, 43 nodes, 11 endings, zero regression). 2 atomic commits (24ff090 + 6d82a00). Downstream: 05.1-08+ (TCA+ETC+endings replan — same directive to remaining regions: tca.entry/isocitrate_dh/akg_dh/succinyl_coa_synthetase/fumarase/malate_dh, etc.complex_ii/iii/iv/atp_synthase, endings + edit.prompt stub), then 05.1-06 (human review of FINAL skeleton), then 05.1-09/10/11.
- [Execution 05.1-05]: Phase 5.1 IN PROGRESS (Wave 2 COMPLETE) — Plan 05.1-05 delivered 05.1-DESIGN.md (9 sections, 74,791 chars) — the REVIEWABLE design artifact in the phase directory (NOT shipped content) that makes the two gameplay-integration contracts + the per-node classification explicit, cross-referencing the ACTUAL 34 node ids in data/story_glucose/*.json. Section 1 Overview: locked Phase 5 decisions honored (soul-jump metamorphosis row 104, C14-decay DROP row 105, anaerobic option d row 97, TCA RNG 0.5/0.5, ending distribution 1T+3G+2N+5B row 107) + 34-node inventory (31 story + 3 stubs fa.stub/alc.stub/edit.prompt) + citation-gate coexistence (PLACEHOLDER_PHASE7* for 29 pending nodes; gate is pre-ship manually-invoked CLI tool NOT run on the skeleton during Phase 5.1; Phase 7 replaces placeholders). Section 2 SC1 node-type taxonomy: 5 types as a DERIVED property (is_ending/choices[].weight/len(choices)) + tags declaration edit:enzyme:<id> for edit-allowed — ZERO schema change; single-valued node_type field REJECTED (breaks multi-role nodes like tca.shuffle = rng-shuffle + choice + edit-allowed); classifier helper is documentation (interpreter already dispatches on these fields); on_enter_divert DORMANCY documented (never referenced by interpreter.py/engine.py — every non-ending node has ≥1 choice.goto). Section 3 File split: 7-file split + manifest.json conventions (version=1, default_seed=0, start='intro.select', files[]) + StoryGraph.load loader + location split (story JSON at repo-root data/story_glucose/ dev + c14/data/story_glucose/ runtime bundle; edits.json/cast.json STAY at c14/data/ — tooling-bound, tools/check_edit_coverage.py:41-42 hardcodes it). Section 4 SC3 choice-point contract: 5-facet spec (player options/Choice.label, edges/Choice.goto, traversal fires/choice.effects THEN target.on_enter, cond gating/_cond restricted-namespace eval, weight/RNG pick) + key distinctions (choice.effects are STATE mutations NOT MolActions; cond vs weight orthogonal; on_enter_divert NOT followed) + per-choice-node table for ALL choice/rng-shuffle nodes (intro.select, pyr.branch, anaer.entry, tca.divert_to_good, etc.entry, edit.prompt + tca.shuffle RNG + 5 edit-allowed nodes' Continue+edit:offer choices). Section 5 SC4 edit-node contract: 5-step data-flow (player edit -> EditIntent -> EditRouter.route(enzyme_id) -> node-id -> engine._enter -> on_enter MolAction(op=edit)) verified against engine.py:135-161 + edit_router.py:122-149 + 5-facet spec + per-edit-node table (5 edit-allowed: gly.pfk/pyr.pdh/tca.citrate_synthase/tca.aconitase/etc.complex_i + tca.shuffle which binds edit:enzyme:tca.aconitase) + edit:enzyme:<id> tag binding (Option 1 ADOPTED, zero schema change; Option 2 new field flagged but NOT adopted) + edit.prompt STRUCTURAL stub explanation (3 choice.goto edges to edit-related bad endings for BFS; at runtime EditRouter routes via apply_player_edit BYPASSING the stub; Phase 6+ real edit UI) + edits.json location (c14/data/edits.json tooling-bound; existing bad_ending_pool ids MATCH skeleton's bad-ending node ids EXACTLY — zero edits.json update; enzyme entries are Phase 7 content). Section 6 SC5 per-node classification table: 34 rows (one per real node), columns node_id/file/structural types/MC-choice?/edit-allowed?/SC5 class/enzyme_id/pathway branch point/ending tier/claim_ids; count verified MC-choice=6 (intro.select, pyr.branch, anaer.entry, tca.divert_to_good, etc.entry, edit.prompt) + both=6 (gly.pfk, pyr.pdh, tca.citrate_synthase, tca.aconitase, tca.shuffle, etc.complex_i) + neither=22 = 34; ending distribution 1T+3G+2N+5B=11; SC5 class is a gameplay DESIGN decision per node (a single-Continue node has structural type 'choice' but SC5 'neither'). Section 7 SC2 reachability: green test (all 11 endings reachable, all 4 tiers, is_ok) + red test (orphan end.true via etc.atp_synthase edge removal) + aerobic-only-True rule (cond gates RUNTIME eligibility; choice.goto gates STRUCTURAL reachability — the True ending is statically reachable via the aerobic choice.goto chain; the anaerobic guard is a runtime cond concern the BFS does NOT model; DO NOT model the anaerobic block as a missing structural edge) + tests/test_glucose_reachability.py 5 tests (authored in Plan 04). Section 8 Backward-compat: ZERO code changes for recommended approach (derived types + tags) — all 8 concerns (from_dict/to_dict/AST gate/citation gate/coverage gate/validate_edits_table/interpreter-engine/existing tests) verified NONE impact; 198 existing tests pass unchanged (toy fixture data/story/ untouched). Section 9 Open questions: 13 OQs surfaced for Plan 06 human review — OQ1 ethanolic anaerobic ending vs mammalian host (HIGH — research OQ1, the single most important open question; anaer.ethanolic is a yeast pathway, mammalian hosts don't do ethanolic fermentation; human decides at Phase 7: drop/counterfactual/host-variation), OQ2 Normal-ending narrative framing (MEDIUM — is etc.entry 'let go' the right branch point or retrospective framing?), OQ3 cycle-trap visit-cap N=5 (LOW — game-design parameter, human confirms at Phase 6 playtest, NOT a science claim), OQ4 cataplerotic-exit claims not in first-batch (MEDIUM — Phase 7 source approval), OQ5 ETC PDB structures 5LDW/1ZOY/1BGY/1OCC/1E79 CANDIDATE (MEDIUM — Phase 7 approval before on_enter loads wired), OQ6 PDH cast PDB not identified (LOW — Phase 9 cast), OQ7 char-setting mechanism for character-select (COORDINATION — controller sets state.character from choice index, not choice effects), OQ8 on_enter_divert dormancy (COORDINATION — skeleton uses Continue choices; if designer wants on_enter_divert BFS-traversable, that's a reachability-checker EXTENSION), OQ9 edits.json/cast.json location c14/data/ vs data/ (low-stakes — keep c14/data/ zero-work, ARCHITECTURE.md outdated), OQ10 edit-allowed binding tags vs new field (DECIDED=tags Option 1), OQ11 SC5 classification design-doc table vs JSON field (DECIDED=table — derivable, no drift), OQ12 node-level vs choice-level edit:offer tag (DECIDED=both coexist — node tag for binding, choice tag for UI trigger), OQ13 anaer.entry classification override (planner decision — the 3-way fermentation-outcome choice IS a real tier-determining branch, OVERRIDES research's tentative story/neither). The doc cross-references the Phase 2/4 engine/model/validator/edit_router source + the Phase 5 Key Decisions (PROJECT.md rows 104-108) + the two Phase 5.1 research tracks. No deviations — plan executed exactly as written. Prior: Plan 05.1-04 delivered the manifest + SC2 reachability green/red suite (SC2 machine-checked GREEN on the real 34-node skeleton). Downstream: 05.1-06 (human review checkpoint — reviews 05.1-DESIGN.md against spec.md + approved sources + the 13 open questions; the ONLY remaining plan in Phase 5.1). After 05.1-06, Phase 5.1 is COMPLETE and Phase 6 (Qt UI MVP) is UNBLOCKED.
- [Execution 05.1-04]: Phase 5.1 IN PROGRESS (Wave 2 manifest + SC2) — Plan 05.1-04 delivered data/story_glucose/manifest.json (the bundle index: version=1, default_seed=0, start='intro.select', files[] listing all 7 skeleton files in pathway order [intro, glycolysis, pyruvate_branch, tca, etc_atp, endings, bad_endings]) + tests/test_glucose_reachability.py (5-test SC2 green/red reachability suite). The manifest schema matches exactly what StoryGraph.load reads (graph.py: manifest.get for version/default_seed/files/start) + matches the toy fixture data/story/manifest.json. SC2 SATISFIED (machine-checked): StoryGraph.load('data/story_glucose/') returns 34 nodes (31 story + fa.stub + alc.stub + edit.prompt) with NO ValueError (no duplicate ids across the 7 files — the dotted knot.stitch convention ensures uniqueness); check_reachability(g.all_nodes(), 'intro.select') is GREEN (is_ok=True, unreachable_endings=[], all 4 ending tiers {true,good,normal,bad} reachable, exactly 11 endings = 1 True + 3 Good + 2 Normal + 5 Bad — matches user-confirmed asymmetric distribution PROJECT.md row 107). The BFS traverses choice.goto edges ONLY (confirmed by reading validate.py — does NOT traverse on_enter_divert); cond-gated edges (pyr.branch aerobic/anaerobic split; tca.shuffle cycle-trap -> bad.cycle_trap_host_death) ARE traversed regardless of cond, so both subtrees are structurally reachable (the anaerobic guard is a runtime cond concern, not structural). SC2 RED proven: variant 1 orphans end.true (the True ending) by rebuilding etc.atp_synthase (its sole inbound choice.goto edge) with a goto to end.normal.co2 -> is_ok False, end.true in unreachable_endings; variant 2 adds an extra orphaned ending -> is_ok False. Both mirror test_integration.py:348-362; both build the variant in-test on a dict(g.all_nodes()) copy (real skeleton never modified). KEY DECISION: manifest created in Wave 2 (this plan), NOT in parallel Wave 1 authoring plans — avoids manifest-coordination races between the 3 parallel Wave 1 plans (01/02/03). KEY DECISION: the SC2 GREEN test pins the EXACT ending distribution (11 = 1T+3G+2N+5B) + all 4 tiers reachable — makes the user-confirmed asymmetric distribution a machine-checked invariant. Full suite 203 tests PASS (198 existing + 5 new, zero regression — toy fixture data/story/ untouched; glucose skeleton at separate data/story_glucose/ dir); AST import gate clean (exit 0). Downstream: 05.1-05 (05.1-DESIGN.md can cite the SC2-green skeleton as validated topology), 05.1-06 (human review — reachability machine-green, review focuses on narrative/claims/edit.prompt structural-vs-runtime distinction).
- [Execution 05.1-03]: Phase 5.1 IN PROGRESS (Wave 1 COMPLETE) — Plan 05.1-03 delivered 8 ending/stub glucose story graph skeleton nodes (data/story_glucose/endings.json 3 + bad_endings.json 5) as reviewable JSON, completing Wave 1 skeleton authoring. endings.json = 3 aerobic endings: end.good.fatty_acid (citrate cataplerotic exit to fatty-acid synthesis, is_ending="good") + end.good.amino_acid (TCA intermediate -> amino-acid via transamination, is_ending="good") + end.normal.co2 (the aerobic "unremarkable CO2 exit" — the Normal-tier structural branch point off etc.entry's "let go" choice [Plan 02], is_ending="normal" w/ a NARRATIVE-FRAMING FLAG for human review at 05.1-06 — research OQ2). bad_endings.json = 4 bad-ending pool nodes (bad.lost_connection + bad.released_from_host [ids MATCH the Phase 4 EditRouter global bad_ending_pool in c14/data/edits.json EXACTLY — zero edits.json update] + bad.cycle_trap_host_death [reached via tca.shuffle's cond-gated choice visits > 5, authored in Plan 02; the SOLE timescale bad-ending per PROJECT.md row 105, C14-decay DROPPED; NOT an RNG node — a visit-count cond gate, Pitfall 8 cycle-cap, N=5 placeholder] + bad.critical_residue_break [known-critical-residue edit route for PFK/aconitase/Complex I/PDH/citrate synthase]) — all is_ending="bad" + empty choices; PLUS edit.prompt shared STRUCTURAL stub (NOT an ending — no is_ending; tags ["edit:prompt"]) with 3 choice.goto edges to the 3 edit-related bad endings (bad.critical_residue_break/lost_connection/released_from_host) so the BFS sees them reachable (SC2). CRITICAL design: edit.prompt is a STRUCTURAL placeholder — at runtime the EditRouter (c14/edit_router.py) routes via engine.apply_player_edit(intent, enzyme_id) [known -> branch_node, unknown -> bad-ending pool], bypassing the stub's choices entirely; the stub has NO cancel/return choice (Phase 6+ implements the real edit UI). All 5 bad endings structurally reachable: 3 via edit.prompt (this plan) + 1 via tca.shuffle cond-gate (Plan 02) + 1 via anaer.entry 3rd choice (Plan 01). The 34-node glucose skeleton is now COMPLETE across 7 files (intro 3 + glycolysis 5 + pyruvate_branch 6 + tca 7 + etc_atp 5 + endings 3 + bad_endings 5 = 34 incl. 3 stubs fa.stub/alc.stub/edit.prompt); ending-tier distribution = 1 True + 3 Good + 2 Normal + 5 Bad = 11 endings (matches user-confirmed asymmetric distribution, PROJECT.md row 107). Every forward path uses choice.goto; ZERO on_enter_divert across both new files. claim_ids use PLACEHOLDER_PHASE7_ENDINGS/_BAD/_EDIT variants. Verified: json.load OK + Node.from_dict OK + 3 good/good/normal + 4 bad tiers + edit.prompt 3 goto edges + fallback pool match + 34 unique ids no collisions + all dangling cross-file targets from 01+02 resolve. No deviations. Next: 05.1-04 (manifest.json + SC2 reachability green/red on the merged 34-node skeleton — needs 01+02+03, all done), 05.1-05 (05.1-DESIGN.md choice-point + edit-node contracts + SC5 per-node classification table), 05.1-06 (human review).
- [Execution 05.1-02]: Phase 5.1 IN PROGRESS (Wave 1) — Plan 05.1-02 delivered 12 TCA+ETC glucose story graph skeleton nodes (data/story_glucose/tca.json 7 + etc_atp.json 5) as reviewable JSON. tca.shuffle = the spec-mandated RNG-weighted citrate-prochirality shuffle (0.5/0.5 weights, citing the 2 APPROVED high-stakes claims TCA-RNG-WEIGHT-01 + TCA-RNG-CITRATE-PROCHIRALITY-01; spec.md line 13); carries tags [stage:tca, edit:enzyme:tca.aconitase, rng:weighted] (the shuffle's edit context = the aconitase active site — same enzyme_id as tca.aconitase, so both bind to enzyme_id "tca.aconitase" in the EditRouter). tca.citrate_synthase cites CAST-CSC-PDB-01 + TCA-RNG-CITRATE-PROCHIRALITY-01 + loads PDB 1CSC (the only PDB load in this plan; on_enter {hide_all, load pdb:1CSC}). tca.aconitase cites TCA-RNG-CITRATE-PROCHIRALITY-01 + NO PDB load (aconitase PDB pending Phase 9 cast). TCA loop (tca.co2_turn1/turn2 -> tca.shuffle via choice.goto) + forward path (-> tca.divert_to_good -> etc.entry / end.good.fatty_acid / end.good.amino_acid via cross-file choice.goto). Cycle-trap = cond-gated choice on tca.shuffle: cond "visits.get('tca.shuffle', 0) > 5" -> bad.cycle_trap_host_death (visit-cap N=5 placeholder game-design parameter — research OQ3, NOT a science claim; human confirms at Phase 6 playtest; the BFS IGNORES cond so the goto edge IS traversed, making bad.cycle_trap_host_death structurally reachable). ETC soul-jump arc: etc.entry (2 choices: follow-ETC -> etc.complex_i, let-go -> end.normal.co2 [the Normal-ending "let go" branch making the Normal tier structurally reachable — research OQ2 recommendation, flagged for human review at 05.1-06]) -> etc.complex_i (edit:enzyme:etc.complex_i + edit:offer; NO PDB load — 5LDW CANDIDATE) -> etc.complex_ii_iii_iv (NO PDB load — 1ZOY/1BGY/1OCC CANDIDATE) -> etc.atp_synthase (NO PDB load — 1E79 CANDIDATE; the soul-jump climax) -> end.true (is_ending="true", empty choices; metamorphosis framing: "you are ATP. Your electrons power the cell; your carbon body was shed as CO2. The metamorphosis is complete." + text_teaching "caterpillar -> butterfly; carbon -> CO2 = chrysalis; electrons -> ATP = the soul lives on"). ALL 5 ETC PDBs (5LDW/1ZOY/1BGY/1OCC/1E79) are CANDIDATE pending Phase 7 human approval (research OQ5) — ETC on_enter is {hide_all} only; real PDB loads are Phase 7. Soul-jump framing honored (PROJECT.md row 104): electrons -> ATP, carbon -> CO2; NO "carbon becomes ATP" (Pitfall 4 RESOLVED — verified by string scan). No C14-decay ending (DROPPED per PROJECT.md row 105 — cycle-trap-until-host-death is the SOLE timescale bad-ending; verified no decay/radioactive/half-life text). All forward paths use choice.goto (BFS traverses choice.goto, NOT on_enter_divert — c14/story/validate.py:198-200); 0 on_enter_divert occurrences across both files. JSON shape matches Node.from_dict / Choice.from_dict (id is the JSON key under {"nodes": {id: {...}}}, NOT in the body — matches the existing data/story/intro.json fixture + the c14/story/graph.py:85-87 loader contract). Text is PLACEHOLDER/TBD except where approved claims cover a node (the 3 approved-claim TCA nodes); all other nodes use stage-suffixed PLACEHOLDER_PHASE7_<stage> (PLACEHOLDER_PHASE7_TCA, PLACEHOLDER_PHASE7_ETC) for traceability — the citation gate will FAIL on these until Phase 7 replaces them (intended gate behavior; the gate is NOT run on the skeleton during Phase 5.1). 3 cross-file choice.goto edges pin the ending node ids for 05.1-03 to author: end.good.fatty_acid, end.good.amino_acid, end.normal.co2 (from tca.divert_to_good + etc.entry), bad.cycle_trap_host_death (from tca.shuffle), edit.prompt (from the 3 edit-allowed nodes). No deviations — plan executed exactly as written. Downstream: 05.1-01 (early-path) + 05.1-03 (endings) run in parallel in Wave 1; 05.1-04 (manifest + SC2 reachability) needs 01+02+03 merged; 05.1-05 (DESIGN.md contracts) needs 01+02+03; 05.1-06 (human review) needs 04+05 — open questions: N=5 visit-cap, Normal-ending "let go" branch point, Good-ending cataplerotic-exit claims (PLACEHOLDER_PHASE7), ethanolic anaerobic ending (05.1-01 scope).

- [Execution 05.1-01]: Phase 5.1 IN PROGRESS (Wave 1) — Plan 05.1-01 delivered 14 early-path glucose story graph skeleton nodes (data/story_glucose/intro.json 3 + glycolysis.json 5 + pyruvate_branch.json 6) as reviewable JSON. intro.select = character-select (3 choices -> gly.start / fa.stub / alc.stub; choices carry effects:{} explicitly per plan — the controller [Phase 6] owns character selection, not choice effects; fa.stub/alc.stub are Phase 8 placeholder stubs returning to intro.select, NOT endings). Glycolysis chain = single-option "Continue" choice.goto advances: gly.start -> gly.g6p -> gly.pfk -> gly.fbp_to_pyruvate -> gly.pyruvate -> pyr.branch. gly.pfk = the APPROVED-CLAIM anchor (cites GLY-PFK-01 + CAST-PFK-PDB-01; carries tags [stage:glycolysis, edit:enzyme:gly.pfk] — the edit:enzyme tag DECLARES edit-allowed + binds enzyme_id="gly.pfk" for the EditRouter, zero schema change per research Q3 Option 1; loads pdb:4PFK on_enter; 2 choices: Continue glycolysis -> gly.fbp_to_pyruvate, edit:offer -> edit.prompt [the edit.prompt stub node is authored in Plan 03's bad_endings.json — cross-file reference]). pyr.branch = THE central aerobic/anaerobic branch point (cond-gated: choice 1 cond "not flags.host_o2_low" -> pyr.pdh [aerobic]; choice 2 cond "flags.host_o2_low" -> anaer.entry [anaerobic] with effects {set: {anaerobic: true}}; the BFS IGNORES cond so BOTH goto edges are traversed — both subtrees structurally reachable). pyr.pdh = pyruvate dehydrogenase (edit:enzyme:pyr.pdh + edit:offer -> edit.prompt; -> tca.entry [cross-file to sibling 05.1-02 tca.json]; NO PDB load — PDH PDB pending Phase 9). anaer.entry = the 3-way fermentation-outcome choice (OVERRIDES the research's tentative story/neither classification — the skeleton needs a choice here for the 3 endings to be reachable): -> anaer.lactic [is_ending="good", lactic fermentation, carbon retained, AN-G-02 CANDIDATE] / anaer.ethanolic [is_ending="normal", ethanolic fermentation, CO2 released, AN-G-03 CANDIDATE, HOST-TENSION FLAG: mammalian host does not do ethanolic fermentation — yeast pathway; flagged for human review at 05.1-06/Phase 7] / anaer.crisis [is_ending="bad", anaerobic energy crisis -> host death, AN-ETC-02 CANDIDATE]. Cross-file consistency with sibling 05.1-02 adopted: (a) text_teaching included on ALL nodes ("" for PENDING nodes, "TBD — ..." for approved-claim nodes) + (b) stage:<stage> node tag on EVERY node (stage:intro / stage:glycolysis / stage:pyruvate / stage:anaerobic) — matches the sibling's universal convention for a consistent merged skeleton at Plan 04 manifest time. Every forward path uses choice.goto (single-option Continue for deterministic advances); ZERO on_enter_divert anywhere (Grep-verified absence — on_enter_divert is dormant: never referenced by interpreter.py or engine.py; the BFS does not traverse it). Placeholder claim_ids use domain-suffixed PLACEHOLDER_PHASE7 / PLACEHOLDER_PHASE7_GLYCOLYSIS / PLACEHOLDER_PHASE7_ANAEROBIC / PLACEHOLDER_PHASE8 (pending Phase 7 per-claim approval; the gate is NOT run on the skeleton during Phase 5.1); only gly.pfk cites the 2 approved first-batch claims. Cross-file integration verified: pyr.pdh -> tca.entry RESOLVES vs sibling 05.1-02 tca.json; the full 5-file skeleton (26 nodes: 14 mine + 12 sibling) has NO node-id collisions; only the expected Plan 03 stubs (edit.prompt, end.good.fatty_acid, end.good.amino_acid, end.normal.co2, bad.cycle_trap_host_death, bad.lost_connection, bad.released_from_host, bad.critical_residue_break) remain dangling. Open items flagged for later phases: (1) pyr.branch cond-syntax refinement — the plan's "not flags.host_o2_low" / "flags.host_o2_low" use dict attribute access which the interpreter's _cond (exposing flags as a dict) would evaluate to fail-safe False on AttributeError -> both choices hidden at runtime; Phase 6 (controller) should refine to flags.get('host_o2_low') dict-method form per research Q2 [RUNTIME concern, NOT structural — the skeleton's BFS reachability is unaffected]; (2) anaer.ethanolic mammal-vs-yeast host-tension flag for Phase 7 human review; (3) anaer.entry 3-way choice mechanism may be refined in Phase 7 (cond-gate by host type or RNG). Downstream: 05.1-03 (endings skeleton — authors the dangling cross-file targets), 05.1-04 (manifest + SC2 reachability green/red on the merged 26+ node skeleton), 05.1-05 (05.1-DESIGN.md choice-point + edit-node contracts + SC5 per-node classification table referencing these 14 + sibling 12 nodes concretely), 05.1-06 (human review checkpoint).

- [Execution 06-05]: Phase 6 IN PROGRESS (Plan 05 DONE ✓) — fixed the SC#3 pyr.branch cond-syntax bug + swapped the start-node crashing pdb:TBD_* loads for the bundled _smoke.pdb placeholder + added the 6-call hero-highlight sequence to intro.preface on_enter. CONTENT/DATA edits to the FROZEN 5.1 skeleton, NO topology change (55 nodes / 21 endings preserved; 9 existing reachability tests stay green). (1) pyruvate_branch.json: 2 choice conds fixed flags.host_o2_low -> flags.get('host_o2_low') (dict-method form; the interpreter._cond exposes flags as a DICT -> dict-attribute access raises AttributeError -> caught -> BOTH choices False -> player STUCK at pyr.branch -> SC#3 blocked; the dict-method form returns None when unset -> not None is True [aerobic eligible, player can reach TCA -> True ending] / bool(None) is False [anaerobic hidden until a Phase 7 flag-setter]; matches tca.shuffle's working sibling visits.get(...)). (2) intro.json: intro.preface on_enter 2 pdb:TBD_* loads -> _smoke.pdb (bundled 3-atom C1/O1/C2 test fixture at c14/data/assets/bundled/_smoke.pdb, NOT a cited PDB -> no fabricated science; no network fetch -> no crash at the manifest start node = Blocker 1 fix b) + the 6-call hero-highlight sequence inserted after the hero_atom load (set_color hero_cyan [0.0,0.75,0.75] + show_as sticks + color hero_cyan on elem C + show spheres + set sphere_scale 0.3 + label YOU — 05.4-CONVENTION.md §3.3; SC2 human-verifiable at Phase 6; dispatches via 06-01's molops set_color/label/set ops); intro.shell_glucose on_enter pdb:TBD_GLUCOSE -> _smoke.pdb (no hero-highlight — preface owns it). (3) tests/test_glucose_reachability.py: +2 test classes (TestPyrBranchRuntimeEligibility 5 tests [aerobic eligible when unset, anaerobic hidden when unset, flip when set, not-stuck regression guard, no-broken-dict-attribute-conds regex scan] + TestStartNodeOnEnterShape 5 tests [no pdb:TBD at start nodes, hero-highlight 6-op ordered sequence after the hero_atom load, shell_glucose glucose load uses _smoke.pdb, no start-node pdb: fetch, topology-unchanged 55/21 regression guard]). 19 reachability+eligibility+shape tests green; 258 full suite green. Commits: Task 1 cdafb3b (fix conds; NOTE — a parallelization race swept 06-03's staged c14/engine.py [view-matrix injection, 06-03's tested+sound work] into this commit alongside pyruvate_branch.json; history NOT rewritten per the 05.1-14 precedent since parallel agents 06-01/06-02 committed on top; my deliverable is correctly in the commit), Task 2 0887fc7 (test), Task 3 492dd15 (fix + shape tests), metadata d8cd4ed. DEVIATIONS: 2 auto-fixed (Rule 1 bug — the plan's prescribed regression-scan regex flags\\.[a-zA-Z_][a-zA-Z0-9_]*(?!\\() had a greedy-backtracking false-positive matching 'flags.ge' [a prefix of 'flags.get'] because the (?!\\() lookahead fails at '(' then the engine backtracks to 'ge' where the next char 't' is not '('; fixed by adding a (?![a-zA-Z0-9_]) word-boundary BEFORE (?!\\() so the full identifier matches before the '(' check; verified on 5 cases; Rule 3 blocking — the Task 1 parallelization race above). Integration with 06-01: this plan authored the DATA (MolAction sequences in JSON referencing op names); 06-01 authored the MECHANISM (molops.py dispatches); integration proven at 06-14 headless smoke. Next: 06-14 headless smoke + human-verify start at intro.preface (now non-crashing).

### 06-06 update (2026-08-30)

Phase 6 (Qt UI MVP) IN PROGRESS — Plan 06-06 COMPLETE ✓ (3/3 tasks): built the Qt-free Controller (mediator) + HeroResolver (OQ-6 gate) + the additive engine.goto/choice_cond_met methods. `c14/ui/controller.py` = `Controller` (Qt-FREE — imports ONLY c14.* domain modules + stdlib sys, NO pymol/PyQt5; importable in pure WSL python3.6 — the controller OPTS OUT of the c14/ui/ gate exemption per 06-RESEARCH Pitfall 6) + `HeroResolver` (OQ-6 multi-C warn+confirm gate). Controller wires `GameEngine(molaction_sink=self._dispatch_molaction, edit_router, view_provider, view_applier)` + forwards each MolAction the engine emits to `molops.apply`. Event handlers: `start_game/choose/take_choice/apply_edit/request_edit/save/load` — all route correctly + record achievement turns (is_new_game=True ONLY on start_game) + render via the injected view. `_dispatch_molaction` wraps the molops.apply forward in a defensive try/except (Blocker 1 fix c: a failed load logs to stderr + continues to the next on_enter action — graceful degradation for offline pdb:XXX fetches; per-action swallow). `take_choice(choice)` + `request_edit(enzyme_id)` route via the NEW `engine.goto` (NOT choose) — SC#3 Blocker B fix: tca.shuffle's 2 non-weighted choices (edit:offer -> edit.prompt, cycle-trap -> bad.cycle_trap_host_death) are now reachable (choose would RNG-pre-empt them). `request_edit` stashes `_pending_edit_enzyme_id` + gotos edit.prompt (Warning 4 edit-prompt seam: the MainWindow 06-08 reads the stash when render_turn sees edit.prompt to open the EditDialog 06-09 with the right enzyme); `build_edit_intent` falls back to `_pending_edit_enzyme_id` at edit.prompt (which has no edit:enzyme: tag). `is_mixed_weighted_node` detects tca.shuffle (>=1 weighted + >=1 non-weighted eligible) so the view renders Spin + Edit/conditional buttons. HeroResolver: detects the hero-highlight via `label` op text "YOU" (OQ-4 marker); single-C (count==1) no-op; multi-C (count!=1) prompts via injected prompt_fn + on confirm rewrites the hero ops' sele (label/show-spheres/set-sphere_scale) to the default first-C ("<hero_obj> and elem C"); show_as sticks + color KEEP object-wide sele. Monkey-patch+restore pre-pass (one-shot mutate target.on_enter = resolved, restore in finally — 06-RESEARCH Open Question 2 recommended; the engine re-reads node.on_enter each _enter so one-shot mutate is safe). `c14/engine.py` += `goto(node_id)` (directly enters a node bypassing choice resolution) + `choice_cond_met(choice)` (exposes interpreter._cond; fail-safe when state is None) — additive public methods (Phase 2 tests stay green; AST gate clean). `tests/test_engine.py` += `TestEngineGoto` (6 tests: enters/records-visit/ending/cond-none/delegates/before-start). `tests/test_controller.py` = 18 tests with MockMolOps + MockView + mock prompt_fn/count_fn + MockAchievementBoard + a real EditRouter (from c14/data/edits.json): engine wiring (bound-method ==), start/choose/take_choice/apply_edit/request_edit/save/load routing, tca.shuffle goto, request_edit seam, build_edit_intent fallback, _dispatch_molaction swallow (Blocker 1 fix c), achievement calls (is_new_game True on start / False on choose), HeroResolver 4 cases (single-C no-op / multi-C prompt+rewrite / rejected / no-highlight), is_mixed_weighted_node (tca.shuffle True / pure-MC + pure-weighted False), Qt-free import gate (no pymol/PyQt5 in sys.modules). KEY DECISIONS: (1) monkey-patch+restore over pending-queue (one-shot mutate + restore in finally; avoids sink-stash complexity); (2) hero op rewrite scope (label/show-spheres/set-sphere_scale sele rewritten; show_as/color keep object-wide); (3) count_fn counts atoms but message says "carbons" (plan literal; test 10 injects count_fn=3 + checks "3 carbons"); (4) take_choice/request_edit use engine.goto directly (choose stays for pure-MC); (5) _target_node_id_for_choose returns None for weighted nodes (RNG picks; no weighted target has a hero highlight in the current skeleton). DEVIATIONS: 1 (Rule 1 bug — test_engine_wiring_molaction_sink used assertIs on bound methods [fresh objects per access] -> fixed to assertEqual + __func__/__self__ identity; trivial test-assertion fix, no behavior change). 3 task commits f013734 (engine goto+choice_cond_met) + 3a8080a (Qt-free Controller+HeroResolver) + 92a8e6d (18 controller tests) + docs 6e90931 (SUMMARY). 24 new tests green (6 engine + 18 controller); full suite 308 green (was 284; +24; zero regression). AST gate clean; Qt-free import proven (`python3.6 -c "import c14.ui.controller"` succeeds; no pymol/PyQt5 in sys.modules). SC#3 FULLY unblocked: 06-05 fixed the aerobic path (True ending reachable) + 06-06 fixed the Bad path (cycle-trap reachable via engine.goto). Unblocks 06-08 (MainWindow consumes TurnResult + _pending_edit_enzyme_id + is_mixed_weighted_node), 06-09 (EditDialog calls build_edit_intent/apply_edit), 06-14 (integration human-verify). NO new dependencies (stdlib sys + c14.* domain only). Python 3.6 compatible (no f-strings/dataclasses).

### Roadmap Evolution

- Phase 12 added (2026-08-16): Ending Cutscene Rendering — for each ending, prepare a cutscene render (CG scene setup by agent, color/angle/lighting adjusted by human; default lighting 'XRay' in the PyMOL lighting plugin, render with 'Ray'). Depends on Phase 10 (settled content); owns 0 requirements (render phase producing visual artifacts from already-delivered content, per the Phase 10/11 0-requirement precedent). May run between Phase 10 and Phase 11 if the docs should reference the cutscene renders, or as the final phase after Phase 11. Research not needed (standard PyMOL `cmd.ray` + 'XRay' lighting, verifiable against `tmp/pymol-src/`; scene setup reuses the Phase 5.4 representation convention). Human-verify-heavy phase (GUI angle/lighting adjustment cannot be automated from WSL).

### Pending Todos

None yet.

### Blockers/Concerns

Issues that affect future work:

- [Phase 5]: COMPLETE — all 5 success criteria delivered. ALL 4 science-framing Key Decisions RESOLVED: Pitfall 4 (ATP/True-Ending carbon-fate reframing) RESOLVED (soul-jump reframing + metamorphosis metaphor — PROJECT.md row 104); Pitfall 9 (C14-decay) RESOLVED via DROP (PROJECT.md row 105); SC#3 anaerobic framing RESOLVED via option (d) (PROJECT.md rows 95/97/108); SC#4 source-approval WORKFLOW RESOLVED via option (c) HYBRID + routine-claim warning flag (PROJECT.md row 106). SC#4 first-batch approval DONE (Plan 05-05: 5 claims + 4 sources approved; LEHNINGER rejected per license concern + swapped to LibreTexts Jakubowski Ch 16 for the 2 high-stakes TCA RNG claims). SC#5 gate PROVEN operational on real data (Plan 05-05: exit 0 "CITATION GATE PASSED" on a fixture referencing the 5 approved claims). NUBASE2020-C14 + C14-TIME-01 eliminated (DROPPED). Phase 5.1 (Story Graph Design) is UNBLOCKED. Non-blocking note: the LibreTexts Jakubowski 16.02 page covers the prochirality MECHANISM (citrate synthase (S)-citryl-CoA + aconitase 180-degree flip) but does NOT explicitly use 'prochiral' for citrate — documented in the 2 high-stakes claims' review_notes for Phase 7 reconsideration if a more explicit source is desired.
- [Phase 5.1 / 5.4 (INSERTED)]: Two design phases gate Phase 6. Phase 5.1 (Story Graph Design) is COMPLETE (2026-08-22; 16/16 plans done; human review APPROVED in 05.1-06; skeleton FROZEN at 54 nodes / 20 endings 1T+3G+2N+14B / intro.preface start / 14 edit-allowed / 0 single-Continue; 3 disease-mutant decisions confirmed as defaults; 32 OQs disposed; 9 reachability tests + 207 full suite PASS). Phase 5.4 (Cast & Hero Representation Design, formerly 5.2, renumbered 2026-08-22) is UNBLOCKED — not yet planned. The replan Wave (plans 07-11) + expansion Wave 2 (plans 12-16) expanded the phase from 6 to 16 plans. 05.1-10 updated 05.1-DESIGN.md to match the post-replan skeleton exactly: SC5 table reclassified (MC-choice=16, both=9, edit-allowed=5, neither=13; 0 single-Continue; 14 edit-allowed = 12 disease-mutant + 1 structural + 1 shared); edit-node contract expanded 6→14 rows with DIS-*-cand CANDIDATE claims + reverse-mutation signatures (NAME ONLY) + Phase 7 content boundary; 11 new OQ-DM open questions with IDH3A-default/IDH1-alternative FLAGGED for 05.1-06; pyr.pdh 2OZL→6CFO + etc.complex_i PLACEHOLDER→DIS-NDUFS8-01-cand + CS edit:structural documented. 05.1-11 added 3 replan-invariant test methods (0 single-Continue, 14 edit-allowed, 6CFO+DIS-NDUFS8) + regenerated SVG/ASCII diagram (14 edit-allowed markers) + updated GRAPH-DIAGRAM.md. 05.1-DESIGN.md is now the accurate REVIEWABLE artifact. 206 tests pass (zero regression). 05.1-08 closed the TCA cycle for editing (4 TCA enzymes now edit-allowed: isocitrate_dh [IDH3A DEFAULT — IDH1 R132H alternative FLAGGED for human review] + succinyl_coa_synthetase [SUCLG1] + fumarase [FH] + malate_dh [MDH2]; aconitase += DIS-ACO2-01-cand; akg_dh NOT promoted [low-confidence negative]; citrate_synthase REFRAMED edit:structural [no disease point mutant]) — 5 DIS-*-cand CANDIDATEs pending Phase 7. 05.1-09 closed the ETC chain for editing (all 4 ETC complexes now edit-allowed: complex_i [NDUFS8] + complex_ii [SDHA] + complex_iii [CYC1] + complex_iv [COX4I1] — DIS-*-cand CANDIDATEs pending Phase 7) + fixed complex_i PLACEHOLDER->DIS-NDUFS8-01-cand + atp_synthase Continue-to-MC. 05.1-07 applied the disease-mutant research + Continue-to-MC to glycolysis.json + pyruvate_branch.json: gly.pyruvate_kinase promoted to edit-allowed (PKLR R479H [NOT R479W] -> DIS-PKLR-01-cand); pyr.pdh cast PDB fixed 2OZL [phospho-mimic] -> 6CFO [WT] + DIS-PDHA1-01-cand + CAST-PDH-WT-PDB-01-cand; gly.pfk += DIS-PFKM-01-cand; 5 linear nodes gained mc:observe Observe 2nd choices; anaer.ldh NOT promoted (GSD11 frameshift). All 12 DIS-*-cand + 1 CAST-PDH-WT-PDB-01-cand claims are CANDIDATE pending Phase 7 (skeleton REFERENCES, does NOT assert). Remaining: ONLY 05.1-06 (human review of the FINAL skeleton with all replan changes incl IDH3A-vs-IDH1 + akg_dh deeper-scan + CS edit:structural + bovine-1ACO homology caveat). Open items for later phases: pyr.branch cond-syntax refinement to flags.get('host_o2_low') for Phase 6 [runtime, not structural]; anaer.ethanolic mammal-vs-yeast host-tension flag for Phase 7 human review; anaer.entry 3-way choice refinement for Phase 7; the 2OZL->6CFO cast fix surfaces that TBD_ACONITASE + 2OZL were placeholder PDBs needing Phase 7 verification. Phase 5.4 (Representation Design) needs Phase 3 + 5.1; it produces the hero-highlight + scene-template convention. Both are review-checkpoints, not requirement-delivery phases.
- [Phase 5.3 (INSERTED)]: COMPLETE (2/2 plans; 2026-08-27) — SC1 convention spec (05.3-01) + SC2 headless prototype (05.3-02: `tools/wt_align_smoke.py` + 2 PDB fixtures, `SMOKE_RESULT: PASS`, all 8 stage checks green — proves the `cmd.super` MECHANISM on a placeholder) + SC3 human design-review checkpoint (05.3-02: APPROVED — convention + prototype + all 8 open questions accepted at research recommendations). The `op="align"` molops dispatch is DEFERRED to Phase 6 (Q3 resolved: human accepted the research rec to defer; `c14/pymol_layer/molops.py` UNTOUCHED, no follow-up dispatch work owed in 5.3). The convention is ready to be consumed by Phases 5.4/6/7 WITHOUT re-designing it. Auto-fixed: `cmd.iterate`→`cmd.iterate_state` (editing.py:1578) for coordinate capture — smoke mechanics only, no convention impact. NO fabricated science (only 6CFO/6CER, both already 5.1 CANDIDATE pending Phase 7). Hero-identity anti-confusion NOTE is in the convention. Next: Phase 5.4 (Cast & Hero Representation Design) → Phase 6 (Qt UI MVP, which implements the one deferred `op="align"` dispatch).
- [Phase 5.4 (INSERTED)]: IN PROGRESS (1/4 plans; 2026-08-29) — 05.4-01 delivered the SC1+SC2+SC3 convention spec `05.4-CONVENTION.md` (751 lines, 10 H2 sections §1-§8 + SC checklist). SC1 hero highlight (hybrid selection rule + 3 structural cases + 6-call sequence + persistence as pure function of on_enter + CO2-shed soul-transfer climax w/ MANDATORY anti-confusion). SC2 scene-template library (6 types mapping all 55 nodes as MolAction sequences NOT cmd.scene + 4 ending tier-templates). SC3 cast-reveal (full-then-zoom + gray/green/red/orange palette + rep-redundancy colorblind-safe + 5.3 composition filling <mutant_color>/<wt_color>). KEY DECISION: op-granularity RESOLVED — ADOPT 3 minimal peer-primitive ops set_color/label/set + SHORT sequences, NOT the rejected composite op="hero_highlight" super-op (mirrors 5.3 precedent). The 3 new-op dispatches DEFER to Phase 6 (OQ-1; mirrors 5.3 §6/§8 Q3). 11 open questions OQ-1 to OQ-11 surfaced for Plan 04. NO code modified (molops.py + model.py UNTOUCHED). NO fabricated science (ZERO real PDB IDs — only pdb:TBD_* placeholders, unlike 5.3 which had 6CFO/6CER; palette RGB values placeholders pending Phase 7). The convention is internally consistent across all 3 SCs (shared op vocabulary + shared named-color palette). Next: 05.4-02/03 (headless mechanism proofs — prototype cmd.* directly per OQ-1 deferral) → 05.4-04 (human design-review checkpoint against the 9-criterion review checklist + 11 OQs) → Phase 6 (Qt UI MVP, which implements the 3 deferred set_color/label/set dispatches + the 5.3 align dispatch). UPDATE 05.4-03 (2026-08-29): SC2+SC3 headless mechanism proof DELIVERED — `tools/scene_template_smoke.py` (516 lines, 99 # src: citations) runs `SMOKE_RESULT: PASS` with all 37 stage checks green (6 scene types + cast-reveal disease/structural + 4 ending tiers + 5.3 composition as MolAction SEQUENCES via direct cmd.* NOT cmd.scene on placeholder fixtures; 3 auto-fixes: prochiral selection parenthesized [OR-leak fix], Stage-7 BEFORE re-established [Type 3b had cleared it], create citation line 1001→960 [def-line accuracy]; molops.py UNTOUCHED; AST gate clean; NO new fixtures; smoke passed first run). Phase 5.4 now 3/4 (01 convention spec + 02 hero-highlight smoke + 03 scene-template/cast-reveal smoke); 05.4-04 (human review) pending.
- [Phase 4]: COMPLETE — all 5 SCs delivered. The `alter`→`sort` silent-corruption trap (Pitfall 6) is MECHANICALLY MITIGATED (04-01: apply_edit ALWAYS calls cmd.sort + cmd.rebuild) + MACHINE-CHECKED (04-04: AST gate enforces cmd.alter ONLY in edit_ops.py) + HEADLESSLY PROVEN (04-05: post-edit byres returns expected atoms, no silent corruption). The `cmd.create` pitfall (Pitfall 3) was EMPIRICALLY CORRECTED (03-01): default-args `cmd.create(backup, source)` (all states) is the working form. The h_ops selection scoping bug was FIXED (04-05: selections now scoped to target to prevent backup corruption). 198 tests pass; alter gate + import gate + coverage scan all GREEN; 2 headless smokes PASS. Phase 4 is the highest technical-risk phase — it is now COMPLETE.
- [Phase 6]: First human-verify milestone — Qt/GUI cannot be exercised from WSL (Pitfall 2). Manual GUI test matrix begins here. Now implements the reviewed Phase 5.1 + 5.2 design artifacts rather than inventing them inline. Plugin loader should also call `c14.paths.selfcheck()` at startup (Pitfall 1 mitigation — fail loud on broken bundled layout); the helper is designed for this and the Phase 1 test is the first caller.
- [Phase 11]: Documentation finalization + verification is the LAST release gate, after Phase 10's content polish. It depends on Phase 10 being complete (playtest-driven content changes settled) so docs reflect final shipped content. It owns 0 new requirements — verifies/updates DOC-01, DOC-02 against shipped reality.
- [Coverage]: REQUIREMENTS.md previously stated "34 total" v1 requirements; actual enumerated v1 set is 32 (PATH-01, STAT-01 are v2). Traceability uses 32. The 5.1/5.2/11 phases own 0 requirements — coverage unchanged at 32/32.

### Quick Tasks Completed

| # | Description | Date | Commit | Directory |
|---|-------------|------|--------|-----------|
| 001 | Resolve Pitfall 4 (C14/ATP carbon-fate conflict) — adopt soul-jump reframing (electrons-as-soul → ETC → ATP, tied to RNG TCA shuffle) + cleanup wrong claims across planning docs | 2026-08-13 | 7b6119c | [001-resolve-pitfall-4-c14-atp-carbon-fate-co](./quick/001-resolve-pitfall-4-c14-atp-carbon-fate-co/) |
| 002 | Narrative motivation: hero as "gifted/blessed" restorer of a sick host — reframes editing as restoration (on_enter pre-edit shows diseased enzyme; player's edit is the restoration attempt via EditRouter). Content-layer decision; no skeleton replan. Updates spec.md + PROJECT.md + 05.1-DESIGN.md | 2026-08-19 | (this commit) | [002-narrative-motivation-hero-as-restorer](./quick/002-narrative-motivation-hero-as-restorer/) |

## Session Continuity

Last session: 2026-08-30 (Phase 6 CAPSTONE 06-14 COMPLETE ✓ — Task 1 headless integration smoke SMOKE_RESULT: PASS [58/58 stage checks; commits 3a661f5 HeroResolver pre-pass count fix + ad95068 smoke] + Task 2 checkpoint:human-verify VERDICT RECORDED [SC1/SC3/SC4/SC5 PASS; SC2 bugs fixed: c2d1008 edit-dialog 3-tuple, 389ee3e single-carbon hero sele via empirically-verified `first` operator + tools/probe_first_operator.py, 3f483aa stage-tag status bar; 5 open items recorded for the user/Phase 7; 309 tests green; AST gate clean; smokes + probe PASS post-fix].)
Stopped at: Completed 06-14-PLAN.md (Phase 6, Plan 14 — 2/2 tasks; the FIRST human-verify milestone exercised end-to-end). Phase 6 COMPLETE. Next: user decisions on the 5 open items (cycle-trap option-vs-result; fuller inline help) + Phase 7 (content-glucose) planning.
06-03: COMPLETE ✓ 2026-08-30 — view-matrix injection closing the Phase 2 view-matrix gap for SC#3. GameState.view field (list of 18 floats or None; 3 sites __init__/to_dict/from_dict; default None via .get — old Phase-2 saves load cleanly, no version bump; added at END of to_dict for diff-stability). GameEngine view_provider/view_applier injection (default None = no-op; save captures via provider [failure-tolerant try/except → None, never blocks save]; load applies via applier AFTER the on_enter replay so a node's zoom MolAction does NOT override the saved view [saved view wins — 06-RESEARCH Pattern 4]; no-op if None or view None). engine.py + state.py stay stdlib-only (AST gate clean); mirrors molaction_sink injection (controller 06-06 injects lambda: list(cmd.get_view()) / lambda v: cmd.set_view(v)). 11 unit tests green (TestGameStateView 4 + TestEngineViewCallbacks 7 incl a single-shared-log ordering proof that set_view is the LAST entry after all replayed molactions); 253-test full suite green (no Phase 2 regression). Commits: Task 1 clean feat(06-03) 7f32e5e; Tasks 2-3 swept into concurrent 06-01/06-05 commits (cdafb3b engine.py, 524362a tests) by parallel git operations — code present + verified at HEAD, attribution noted in 06-03-SUMMARY (history rewrite declined as unsafe with an active concurrent session). DEVIATIONS: 2 Rule-1 auto-fixes (test_to_dict_keys expected-set +view for the schema change; used bound sink.append not the plan's broken unbound list.append) + 1 environmental (concurrent-session commit sweep). Next: 06-06 controller injects the view callbacks → 06-14 headless smoke for real cmd.get_view/set_view.
06-05: COMPLETE ✓ 2026-08-30 — pyr.branch cond-syntax fix (SC#3 Blocker A) + start-node _smoke.pdb swap (Blocker 1 fix b) + 6-call hero-highlight sequence in intro.preface on_enter (SC2 verifiable at Phase 6). CONTENT/DATA edits to the FROZEN 5.1 skeleton, NO topology change (55 nodes / 21 endings preserved; 9 existing reachability tests stay green). (1) pyruvate_branch.json: 2 choice conds flags.host_o2_low -> flags.get('host_o2_low') (dict-method form; interpreter._cond exposes flags as a DICT -> attribute access raises AttributeError -> caught -> BOTH choices False -> player stuck -> SC#3 blocked; dict-method returns None when unset -> not None is True [aerobic eligible] / bool(None) is False [anaerobic hidden until Phase 7 flag-setter]; matches tca.shuffle's working visits.get(...) sibling). (2) intro.json: intro.preface on_enter 2 pdb:TBD_* loads -> _smoke.pdb (bundled 3-atom test fixture, NOT a cited PDB -> no fabricated science; no network fetch -> no crash at the manifest start node) + 6-call hero-highlight sequence after the hero_atom load (set_color hero_cyan + show_as sticks + color elem C + show spheres + set sphere_scale 0.3 + label YOU — 05.4-CONVENTION.md §3.3; dispatches via 06-01's molops ops); intro.shell_glucose on_enter pdb:TBD_GLUCOSE -> _smoke.pdb. (3) tests/test_glucose_reachability.py: +10 tests (5 runtime-eligibility + 5 start-node shape). 19 reachability+eligibility+shape tests green; 258 full suite green. Commits: Task 1 cdafb3b (fix conds — NOTE: parallel race swept 06-03's staged c14/engine.py into this commit; history NOT rewritten per 05.1-14 precedent; 06-03's work tested+sound, attribution noted in 06-03-SUMMARY), Task 2 0887fc7 (test), Task 3 492dd15 (fix + shape tests), metadata d8cd4ed. DEVIATIONS: 2 auto-fixed (Rule 1 bug — the plan's prescribed regression-scan regex had a greedy-backtracking false-positive matching 'flags.ge' [prefix of 'flags.get']; fixed with a (?![a-zA-Z0-9_]) word-boundary before (?!\(); Rule 3 blocking — the Task 1 parallelization race). Next: 06-14 headless smoke + human-verify start at intro.preface (now non-crashing) → 06-06 controller.
Resume file: None
