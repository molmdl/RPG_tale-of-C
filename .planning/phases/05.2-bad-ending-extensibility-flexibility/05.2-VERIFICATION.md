---
phase: 05.2-bad-ending-extensibility-flexibility
verified: 2026-08-22T23:45:00Z
status: passed
score: 5/5 must-haves verified
re_verification: No — initial verification
---

# Phase 5.2: Bad-Ending Extensibility & Flexibility Verification Report

**Phase Goal:** The bad-ending pool is demonstrably extensible — a tested convention exists for adding new bad endings whenever needed (Phase 7 content, future expansions) without breaking the skeleton structure or the reachability invariants. The convention is proven by adding a yet-to-exist ending "denature with a change in pH" (a pH-denaturation bad ending not previously in the skeleton) and verifying all invariants hold.
**Verified:** 2026-08-22T23:45:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                                                                                                                                                                                              | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `bad.denature_ph_change` (pH-denaturation, `edit:unknown`, `PLACEHOLDER_PHASE7_BAD`) exists in `bad_endings.json` and is reachable from `edit.prompt` via a structural choice (structural path L)                                                                                                                                  | ✓ VERIFIED | Node at `bad_endings.json:146-156` with `is_ending: "bad"`, `tags: ["ending:bad", "edit:unknown"]`, `claim_ids: ["PLACEHOLDER_PHASE7_BAD"]`, `choices: []`. 13th structural choice (path L) at line 178: `{"label": "Make an unrecognized edit (structural path L)", "goto": "bad.denature_ph_change", "tags": ["edit:unknown"]}`. Runtime: `bad.denature_ph_change in edit.prompt targets = True`; reachability GREEN.                                                                                |
| 2   | A parametrized `subTest` extensibility convention proof exists: every bad ending reachable from the `edit.prompt` structural stub is a valid bad ending with a correct `edit:*` tag, and full-graph reachability stays GREEN — auto-covering future Phase 7 additions WITHOUT test edits                                              | ✓ VERIFIED | `test_all_edit_prompt_bad_endings_reachable_convention` at `tests/test_glucose_reachability.py:312-347`. Uses `with self.subTest(choice_goto=c.goto):` (parametrized). Dynamically iterates `edit_prompt.choices` (auto-covers future additions). Asserts `c.goto in g.all_nodes()`, `is_ending == "bad"`, valid `edit:*` tag, `assertGreaterEqual(len(bad_targets), 13)`, `check_reachability(...).is_ok == True`, `unreachable_endings == []`. Test PASSES (`Ran 9 tests ... OK`). |
| 3   | The 14 edit-allowed + 0 single-Continue invariants still hold (covered by the UNCHANGED `test_14_edit_allowed_nodes` + `test_no_single_continue_choice`)                                                                                                                                                                           | ✓ VERIFIED | `test_14_edit_allowed_nodes` at line 219 (`assertEqual(len(edit_allowed), 14)`); `test_no_single_continue_choice` passes. Runtime re-check (correct criterion: `edit:enzyme:` tag prefix): 14 edit-allowed, 0 single-Continue. Both tests in the GREEN `Ran 9 tests ... OK` run.                                                                                                                                                                                                                     |
| 4   | All count assertions are updated: 54→55 nodes, 20→21 endings, Bad tier 14→15 (1T+3G+2N+15B = 21)                                                                                                                                                                                                                                   | ✓ VERIFIED | `test_manifest_loads_all_55_nodes` (renamed from `_54_nodes`) at line 98: `assertEqual(len(nodes), 55)`. `test_reachability_green_all_four_tiers`: `assertEqual(len(all_endings), 21)` with comment "1T+3G+2N+15B". Runtime: 55 nodes, 21 endings, 15 bad endings.                                                                                                                                                                                                                                  |
| 5   | DESIGN.md, GRAPH-DIAGRAM.md, and the regenerated graph.svg/txt all reflect the new node + bumped counts (55 nodes / 21 endings / 15B)                                                                                                                                                                                              | ✓ VERIFIED | `05.1-DESIGN.md`: 13 occurrences of `bad.denature_ph_change`; "55-node inventory"; "1T + 3G + 2N + 15B = 21 endings"; SC5-table row 54; backward-compat note. `05.1-GRAPH-DIAGRAM.md`: 5 occurrences; Bad tier = 15 with `bad.denature_ph_change` listed; Phase 5.2 extensibility subsection + Key Design Decision. `05.1-graph.svg`: `bad.denature_ph_change` rendered as text node. `05.1-graph.txt`: summary "55 nodes \| 21 endings (1T/3G/2N/15B) \| 14 edit-allowed \| 1 RNG" + node box + structural edge.   |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact                                                                                                                              | Expected                                                                   | Status     | Details                                                                                                                                                                                                                                                                                                              |
| ------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `data/story_glucose/bad_endings.json`                                                                                                 | `bad.denature_ph_change` node + 13th edit.prompt structural choice (path L) | ✓ VERIFIED | EXISTS (182 lines, 7908 bytes). SUBSTANTIVE: well-formed node with `text_dramatic`/`text_teaching`/`claim_ids`/`tags`/`on_enter`/`is_ending`/`choices`. WIRED: loaded by `StoryGraph.load` in the test suite (line 321); node targeted by `edit.prompt` choice (line 178). Contains `bad.denature_ph_change` (2 occurrences: node def + choice goto). |
| `tests/test_glucose_reachability.py`                                                                                                  | renamed `_55_nodes` + generalized `subTest` convention proof + count bumps | ✓ VERIFIED | EXISTS (351 lines, 19801 bytes). SUBSTANTIVE: real test logic with `subTest`, `assertIn`, `assertEqual`, `check_reachability`. WIRED: imports `c14.story.graph.StoryGraph` + `c14.story.validate.check_reachability`; runs GREEN. Contains `test_all_edit_prompt_bad_endings_reachable_convention` (line 312) + `test_manifest_loads_all_55_nodes` (line 98). |
| `.planning/phases/05.1-.../05.1-DESIGN.md`                                                                                            | sections 1/3/4/5/6/7/8 updated for 55-node/21-ending/15B skeleton         | ✓ VERIFIED | EXISTS (131686 bytes). SUBSTANTIVE: 13 occurrences of `bad.denature_ph_change`; section 1 ending-distribution "1T + 3G + 2N + 15B = 21 endings"; SC5-table row 54; 55-node inventory section; backward-compat note (Phase 5.2 extensibility convention). Contains `bad.denature_ph_change`.                          |
| `.planning/phases/05.1-.../05.1-graph.svg`                                                                                            | regenerated graph diagram with 55 nodes / 21 endings                       | ✓ VERIFIED | EXISTS (50063 bytes). SUBSTANTIVE: contains rendered text node `>bad.denature_ph_change</text>`. Regenerated in commit `41a3160` (2-line diff = node + edge added). WIRED: visual companion to the test-verified graph.                                                                                              |
| `.planning/phases/05.1-.../05.1-graph.txt`                                                                                            | regenerated ASCII graph summary (55 nodes \| 21 endings \| 14 edit-allowed) | ✓ VERIFIED | EXISTS (25173 bytes). SUBSTANTIVE: summary line "55 nodes \| 21 endings (1T/3G/2N/15B) \| 14 edit-allowed \| 1 RNG"; `bad.denature_ph_change [BAD]` box in Unreached-nodes section; structural edge `edit.prompt ──► bad.denature_ph_change`. Regenerated in commit `41a3160`.                                        |
| `.planning/phases/05.1-.../05.1-GRAPH-DIAGRAM.md`                                                                                     | count bumps + bad.denature_ph_change listed + extensibility convention note | ✓ VERIFIED | EXISTS (35598 bytes). SUBSTANTIVE: 5 occurrences of `bad.denature_ph_change`; Bad tier row = 15 with the new node listed; Phase 5.2 extensibility subsection (lines 514-520); Key Design Decision entry (line 547). Contains `bad.denature_ph_change`.                                                              |

### Key Link Verification

| From                                                                        | To                                                | Via                                                                                                                                                                      | Status   | Details                                                                                                                                                                                                                                                                                                                                                          |
| --------------------------------------------------------------------------- | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `data/story_glucose/bad_endings.json:edit.prompt choices`                   | `data/story_glucose/bad_endings.json:nodes.bad.denature_ph_change` | the new structural-path-L choice.goto must target the new node id EXACTLY (a typo orphans it → reachability RED)                                                          | WIRED    | Choice at line 178: `"goto": "bad.denature_ph_change"`. Node key at line 146: `"bad.denature_ph_change"`. EXACT match (no typo). Runtime: `bad.denature_ph_change in [c.goto for c in edit_prompt.choices] = True`; `check_reachability(...).is_ok = True` (GREEN); `unreachable_endings = []`. A typo would have made reachability RED — it is GREEN.           |
| `tests/test_glucose_reachability.py:test_all_edit_prompt_bad_endings_reachable_convention` | `data/story_glucose/bad_endings.json`            | `StoryGraph.load(self._story_dir)` loads the REAL skeleton; `check_reachability` over the real post-addition graph must stay GREEN                                        | WIRED    | Test line 321: `g = StoryGraph.load(self._story_dir)` where `_story_dir = data/story_glucose` (the REAL skeleton). Line 343: `rep = check_reachability(g.all_nodes(), g.start_node())`. Line 344: `self.assertTrue(rep.is_ok, ...)`. Test PASSES → real post-addition graph reachability is GREEN. No mock/fixture substitution — the real directory is loaded. |

### Requirements Coverage

This phase owns **0 requirements** (it is a DESIGN/CONVENTION phase that enables STORY-03 bad-ending variety delivery in Phase 7). No requirements mapped to this phase; nothing to block.

### Anti-Patterns Found

| File                              | Line(s)   | Pattern                                                  | Severity | Impact                                                                                                                                                                                                                       |
| --------------------------------- | --------- | -------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `data/story_glucose/bad_endings.json` | 147-149   | `text_dramatic: "TBD — ..."`, `text_teaching: "TBD — Phase 7 teaching text."`, `claim_ids: ["PLACEHOLDER_PHASE7_BAD"]` | ℹ️ Info  | NOT a code stub. This is the **intentional Phase 7 content placeholder pattern** mandated by the plan (the structural convention is what 5.2 proves; the actual narrative text + verified citations are Phase 7 content). All 13 sibling bad endings use the same `PLACEHOLDER_PHASE7_BAD` / `TBD` pattern. Structural validity (`is_ending`, `tags`, `choices: []`, reachability) is fully implemented. No blocker. |

No real TODO/FIXME/not-implemented stubs, no empty implementations, no `console.log`-only handlers, no orphaned artifacts in any touched file.

### Human Verification Required

None. This is a structural/test/docs phase with no GUI, no real-time behavior, no external services, and no PyMOL/Qt. All checks are programmatic and pass:
- The full `unittest` suite runs GREEN (`Ran 9 tests ... OK`) under `python3.6` (3.6.9) per AGENTS.md.
- The structural wiring (choice.goto → node id) is verified by a runtime load + reachability check, not just grep.
- The runtime `c14/data/edits.json` `bad_ending_pool` is confirmed UNCHANGED (2 ids: `bad.lost_connection`, `bad.released_from_host`) — verified by both file inspection AND git history (edits.json not in the changed-files list of any phase-5.2 commit).
- `05.1-VERIFICATION.md` is confirmed UNCHANGED — not in the changed-files list of any phase-5.2 commit; last modified in the original 05.1 completion commit `98c4daa`.

### Gaps Summary

No gaps. All 5 observable truths are verified, all 6 artifacts pass existence + substantive + wired checks, both key links are wired (exact-id match proven by GREEN reachability; real-skeleton load proven by the passing test), the 14 edit-allowed + 0 single-Continue invariants hold, all count assertions are bumped (54→55 / 20→21 / 14B→15B), and all 4 doc/diagram files reflect the new node + counts. The full 9-test suite is GREEN.

The phase goal — "demonstrably extensible bad-ending pool via a tested convention, proven by adding `bad.denature_ph_change` and verifying all invariants hold" — is **achieved**:
- The convention is locked: +1 node (`bad.*` id-prefix, `edit:unknown`/`edit:known`/`edit:known_critical` tag, `PLACEHOLDER_PHASE7_BAD` claim) + +1 structural choice in the `edit.prompt` stub (continue the A-L global path sequence) + bump count assertions; the parametrized `subTest` auto-verifies future additions WITHOUT test edits.
- The proof is the new node itself: `bad.denature_ph_change` was not previously in the 54-node skeleton, was added following the convention, and all invariants (reachability GREEN, 14 edit-allowed, 0 single-Continue, 55/21/15B counts) still hold.
- The boundary is respected: the structural half (skeleton pool) is authored now; the runtime half (`edits.json` `bad_ending_pool`) is UNCHANGED for Phase 7.

---

_Verified: 2026-08-22T23:45:00Z_
_Verifier: OpenCode (gsd-verifier)_
