---
phase: 05.2-bad-ending-extensibility-flexibility
plan: 01
subsystem: testing
tags: [story-graph, reachability, unittest, subtest, bad-endings, extensibility, json]

# Dependency graph
requires:
  - phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts
    provides: the FROZEN 54-node glucose skeleton (bad_endings.json + edit.prompt stub + tests/test_glucose_reachability.py + 05.1-DESIGN.md + 05.1-GRAPH-DIAGRAM.md + diagram renderer)
provides:
  - bad.denature_ph_change node (pH-denaturation, 1a edit:unknown, PLACEHOLDER_PHASE7_BAD) in bad_endings.json
  - 13th edit.prompt structural choice (path L) for BFS reachability
  - test_all_edit_prompt_bad_endings_reachable_convention (parametrized subTest convention proof that auto-covers future Phase 7 additions)
  - Updated 05.1-DESIGN.md + 05.1-GRAPH-DIAGRAM.md + regenerated diagram reflecting 55 nodes / 21 endings / 15B
  - The locked bad-ending extensibility convention (structural half: bad_endings.json + edit.prompt stub; runtime half deferred to Phase 7)
affects: [07-content-bad-endings, 05.2-VERIFICATION, 06-qt-ui-mvp]

# Tech tracking
tech-stack:
  added: []  # no new libraries — Python 3.6.9 stdlib unittest only
  patterns:
    - "Parametrized subTest convention proof: dynamically iterate edit.prompt.choices so future additions auto-verify WITHOUT test edits"
    - "Structural vs runtime bad-ending pool boundary: skeleton pool (bad_endings.json + edit.prompt stub) is BFS-only + authored now; runtime pool (edits.json bad_ending_pool) is Phase 7 content"
    - "Global structural-path letter sequence (A-L): edit:unknown paths A-H + L, edit:known paths I-K — append at end, no renumbering"

key-files:
  created: []  # no new files created — all edits to existing files
  modified:
    - data/story_glucose/bad_endings.json  # +1 node (bad.denature_ph_change) + +1 edit.prompt structural choice (path L)
    - tests/test_glucose_reachability.py  # renamed _54_nodes->_55_nodes, count bumps, generalized subTest convention proof
    - .planning/phases/05.1-.../05.1-DESIGN.md  # sections 1/3/4/5/6/7/8 bumped for 55-node/21-ending/15B skeleton
    - .planning/phases/05.1-.../05.1-GRAPH-DIAGRAM.md  # count bumps + new node listed + extensibility convention note
    - .planning/phases/05.1-.../05.1-graph.svg  # regenerated (55 nodes / 21 endings)
    - .planning/phases/05.1-.../05.1-graph.txt  # regenerated ASCII summary

key-decisions:
  - "Generalized the hardcoded bad-ending test into a dynamic subTest convention proof (replaces, not adds — suite stays at 9 tests)"
  - "Structural half only: bad_endings.json + edit.prompt stub; runtime edits.json bad_ending_pool UNCHANGED (2 ids — Phase 7 territory)"
  - "Path L appended at end of the A-K global sequence (no renumbering of A-K) — edit:unknown continues the unknown-pool sequence"
  - "05.1-VERIFICATION.md left UNCHANGED — 5.2 is the FIRST intentional mutation of the frozen 5.1 skeleton (54->55, intentional + tested, NOT a regression); the new frozen-at-55 state is established by 05.2-VERIFICATION"

patterns-established:
  - "Bad-ending extensibility convention: +1 node in bad_endings.json (bad.* id-prefix, edit:unknown/known/known_critical tag) + +1 structural choice in edit.prompt stub (continue the A-L global path sequence) + bump count assertions + the parametrized subTest auto-verifies"
  - "subTest parametrization over edit.prompt.choices for reachability convention proofs — auto-covers future additions WITHOUT test edits"

# Metrics
duration: 22 min
completed: 2026-08-22
---

# Phase 5.2 Plan 01: Bad-Ending Extensibility Convention Summary

**Locked a tested structural convention for extending the bad-ending pool: +1 node (bad.denature_ph_change, pH-denaturation, edit:unknown) + +1 edit.prompt structural choice (path L), proven by a parametrized subTest that auto-covers future Phase 7 additions — 54→55 nodes, 20→21 endings, Bad 14→15**

## Performance

- **Duration:** 22 min
- **Started:** 2026-08-22T14:09:39Z
- **Completed:** 2026-08-22T14:31:42Z
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments

- Added `bad.denature_ph_change` (pH-denaturation, 1a `edit:unknown`, `PLACEHOLDER_PHASE7_BAD`) to `bad_endings.json` + 13th structural choice (path L) to the `edit.prompt` stub — the FIRST intentional mutation of the frozen 5.1 skeleton (54→55 nodes, 20→21 endings, Bad 14→15)
- Generalized the hardcoded bad-ending test into `test_all_edit_prompt_bad_endings_reachable_convention` — a parametrized `subTest` over `edit.prompt.choices` that dynamically asserts every bad-ending target exists + is `is_ending="bad"` + carries a valid `edit:*` tag + full-graph reachability stays GREEN — auto-covers future Phase 7 additions WITHOUT test edits
- Bumped all count assertions: `test_manifest_loads_all_54_nodes`→`_55_nodes` (54→55), `test_reachability_green_all_four_tiers` (20→21 endings, 14B→15B, 5→14→5→15)
- Updated `05.1-DESIGN.md` sections 1/3/4/5/6/7/8 + `05.1-GRAPH-DIAGRAM.md` + regenerated `05.1-graph.svg`/`05.1-graph.txt` — all reflect 55 nodes / 21 endings / 1T+3G+2N+15B / 13 structural choices / path L
- Confirmed runtime `c14/data/edits.json` `bad_ending_pool` is UNCHANGED (2 ids: `bad.lost_connection`, `bad.released_from_host`) — runtime-pool expansion is Phase 7 content
- Confirmed `05.1-VERIFICATION.md` UNCHANGED — the historical 5.1 record is left alone; the new frozen-at-55 state is established by a later 05.2-VERIFICATION

## Task Commits

Each task was committed atomically:

1. **Task 1: Add bad.denature_ph_change node + edit.prompt structural path L (skeleton JSON)** - `b588e70` (feat)
2. **Task 2: Generalize bad-ending test into subTest convention proof + bump counts** - `73f682f` (test)
3. **Task 3: Update 05.1-DESIGN.md + regenerate diagram + update 05.1-GRAPH-DIAGRAM.md** - `41a3160` (docs)

**Plan metadata:** (to be committed below)

## Files Created/Modified

- `data/story_glucose/bad_endings.json` — +1 node (`bad.denature_ph_change`, pH-denaturation, edit:unknown, PLACEHOLDER_PHASE7_BAD) inserted after `bad.cofactor_lost` + +1 `edit.prompt` structural choice (path L) appended (12→13 choices)
- `tests/test_glucose_reachability.py` — renamed `test_manifest_loads_all_54_nodes`→`_55_nodes` (assert 54→55); bumped `test_reachability_green_all_four_tiers` (20→21 endings, 14B→15B); replaced `test_all_new_bad_endings_reachable_from_edit_prompt` with `test_all_edit_prompt_bad_endings_reachable_convention` (parametrized subTest); module + class docstrings bumped with Phase 5.2 note
- `.planning/phases/05.1-.../05.1-DESIGN.md` — sections 1/3/4/5/6/7/8 bumped: 54→55 nodes, 20→21 endings, 14B→15B, 12→13 structural choices, +1 SC5-table row (row 54), +1 reachability-chain row, +1 backward-compat note (Phase 5.2 extensibility convention impact)
- `.planning/phases/05.1-.../05.1-GRAPH-DIAGRAM.md` — header 54→55-node; post-expansion paragraph bumped; Ending-Tier distribution 20→21 / 14→15 + bad.denature_ph_change listed; mermaid diagram +1 bad node (BDP) + 1 edge (EP→BDP) + badEnd class; +1 Phase 5.2 extensibility subsection; +1 Key Design Decision
- `.planning/phases/05.1-.../05.1-graph.svg` — regenerated (55 nodes, 90 edges)
- `.planning/phases/05.1-.../05.1-graph.txt` — regenerated ASCII summary (`55 nodes | 21 endings (1T/3G/2N/15B) | 14 edit-allowed | 1 RNG`)

## Decisions Made

- **Generalized (replaced), not added alongside:** the old hardcoded `test_all_new_bad_endings_reachable_from_edit_prompt` (which hardcoded 9 Plan-12 ids + sliced `new_bad[:6]/new_bad[6:]`) was replaced by the dynamic `test_all_edit_prompt_bad_endings_reachable_convention` (subTest over `edit.prompt.choices`). Keeping both is redundant — the dynamic test is strictly stronger (auto-covers the new node + future Phase 7 additions). Suite size stays at 9 (rename 1 + generalize-and-rename 1).
- **Path L appended at end (no renumbering):** the global A-K sequence (A-H edit:unknown, I-K edit:known) continues as "structural path L" for the new 1a edit:unknown ending, appended at the end. No renumbering of A-K (cosmetic-only for a structural stub; renumbering is churn that risks the existing test).
- **`assertGreaterEqual(..., 13)` shrinkage floor kept:** catches accidental removal; Phase 7 additions (14+) auto-pass since `>= 13`. The subTest loop + GREEN reachability alone is the convention proof; the floor is a regression guard.
- **05.1-VERIFICATION.md left UNCHANGED:** it is the historical record of the 5.1 approved/frozen state. Phase 5.2 is the FIRST intentional mutation (54→55) — the new frozen-at-55 state is established by a later 05.2-VERIFICATION, NOT by editing the 5.1 record.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required. This phase is JSON + test + docs only (no PyMOL/Qt, no new dependencies, no runtime changes).

## Next Phase Readiness

- **Phase 5.2 SC1 (new node + reachable):** DELIVERED — `bad.denature_ph_change` exists in `bad_endings.json` + reachable from `edit.prompt` via structural path L. Proven by the JSON cross-check + the GREEN reachability assertion (`rep.is_ok` True) + the subTest `assertIn(c.goto, g.all_nodes())`.
- **Phase 5.2 SC2 (parametrized extensibility test):** DELIVERED — `test_all_edit_prompt_bad_endings_reachable_convention` is a `subTest`-parametrized convention proof. Proven by `Ran 9 tests` + `OK`.
- **Phase 5.2 SC3 (all counts + docs + diagram updated):** DELIVERED — count assertions bumped 54→55 / 20→21 / Bad 14→15 in the test; `05.1-DESIGN.md` + `05.1-GRAPH-DIAGRAM.md` + regenerated `05.1-graph.svg`/`05.1-graph.txt` all reflect the new node + counts.
- **Runtime pool UNCHANGED:** `c14/data/edits.json` `bad_ending_pool` = 2 ids (Phase 7 territory). The structural half (skeleton pool) is the only half Phase 5.2 touches.
- **Frozen-at-55 state:** established by this plan + a later 05.2-VERIFICATION. The 05.1-VERIFICATION.md is the historical 5.1 record (left UNCHANGED).
- **Downstream:** Phase 5.2 is COMPLETE (1/1 plans). The next phases per ROADMAP: Phase 5.3 (WT-aligned structure load convention — the other urgent 5.1 checkpoint follow-up) + Phase 5.4 (Representation Design) + Phase 6 (Qt UI MVP) are UNBLOCKED.

---
*Phase: 05.2-bad-ending-extensibility-flexibility*
*Completed: 2026-08-22*
