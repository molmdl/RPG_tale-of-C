---
phase: 07.1-story-framework-node-editing-html-interface
plan: 12
type: execute
wave: 4
depends_on: ["07.1-04", "07.1-07"]
files_modified:
  - tools/story_editor_assets/70_claims.js
  - tests/test_story_editor_claims.py
autonomous: true
must_haves:
  truths:
    - "The Claims tab lists every registry claim with approval-status chips, review tier, claim_text, and its source records"
    - "Clicking a source shows reference/url/license/source_type (offline-safe display)"
    - "A reverse index shows which nodes reference each claim; a node-claim chip jumps to the claim"
    - "The panel is strictly READ-ONLY — approvals stay human-gated (spec.md non-negotiable)"
  artifacts:
    - path: "tools/story_editor_assets/70_claims.js"
      provides: "EDITOR.view('claims'): registry browser, claim detail, source resolution, reverse index, per-node warning chips"
      min_lines: 120
    - path: "tests/test_story_editor_claims.py"
      provides: "Structural battery: read-only contract, reverse index, status chips, multi-source handling"
  key_links:
    - from: "70_claims.js claim row"
      to: "bundle.sources[source_id]"
      via: "source_id resolution (str OR list-of-str — 40 claims are multi-source)"
      pattern: "source_id"
    - from: "70_claims.js reverse index"
      to: "bundle node claim_ids"
      via: "walk 57 claim_ids lists building claim → referencing nodes"
      pattern: "claim_ids"
---

<objective>
Build the claims/references browser (Requirement C): convenient checking of the reference and source behind every scientific content — claim → status/tier/text → sources (url, license) → reverse index (which nodes use it) → jump-to-node.

Purpose: C is read-and-jump, never edit — approvals are human-gated by policy (RESEARCH-UI "Reference/source checking (C)"; RESEARCH-DATA §1.11 defines exactly what to show per claim).
Output: tools/story_editor_assets/70_claims.js + structural tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-RESEARCH-DATA.md   # §1.10 registry schemas + §1.11 reference-check panel spec
@.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-RESEARCH-UI.md     # C section
@tools/story_editor_assets/00_core.js
@data/citations.json
@data/sources.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: 70_claims.js — registry browser + reverse index</name>
  <files>tools/story_editor_assets/70_claims.js</files>
  <action>
Write the claims asset (owns `#tab-claims`). IIFE registering `EDITOR.view('claims', ...)`:

1. **Claim table**: every entry of bundle.citations — row: claim_id, status chip (approved green / pending amber / rejected red; cite rpg/citations.py:100-109 strict `approved` semantics in a comment), review_tier badge (routine/high-stakes), short `claim` text. Filters: status, tier, "referenced only", text search.
2. **Claim detail** (click a row): full `claim_text`; `source_type`; for each source_id (STRING OR LIST — handle both, 40/77 are multi-source): the sources.json record — `reference`, `url` (rendered as a LINK — file:// pages may open https targets in a new tab; that is display-only, not a resource load), `pdb_doi`, `license`, source approval_status; `review_notes` if present; `pdb_id`/`resolution_angstrom` if present.
3. **Reverse index**: build claim → [node ids] by walking all nodes' claim_ids (≤3 each, 57 nodes — trivial); shown in the claim detail AND as a per-node warning chip when a node references an unapproved/unresolved claim (mirror of the lint's claim rules; clicking the chip opens the claim).
4. **Jump-to-node**: each referencing node id is a link calling `EDITOR.select(nodeId)` + switching to the graph tab.
5. **Unreferenced-claims view**: a filter showing the free pool (claims no node references — currently 8; useful for Phase 7-9 authoring).
6. **READ-ONLY contract**: no mutator calls anywhere in this asset; a header comment states approvals are human-gated (spec.md); the Sanctioned residual (PLACEHOLDER_PHASE8 on fa.stub/alc.stub) renders as an annotated special case, not an alarm.
7. Degrade gracefully if citations/sources did not load (warning panel pointing at the boot checklist). Register + init + CSS. ES5 only; header: ownership 07.1-12; cite RESEARCH-DATA §1.10/§1.11.
  </action>
  <verify>Regenerate; emitted HTML contains `EDITOR.view('claims'`, `claim_text`, `review_tier`, `source_id`, `pdb_doi`, `license`, reverse-index walking of `claim_ids`</verify>
  <done>Claims panel registered: table + detail + reverse index + jump-to-node + unreferenced filter; read-only</done>
</task>

<task type="auto">
  <name>Task 2: Structural claims tests</name>
  <files>tests/test_story_editor_claims.py</files>
  <action>
Create `tests/test_story_editor_claims.py`:
1. `test_read_only`: the asset contains NO `EDITOR.apply` / `nodeSet` / mutator calls (grep zero; the read-only contract is pinned).
2. `test_multi_source`: source_id handled as string AND array (grep both branches).
3. `test_reverse_index`: claim_ids walk present; warning chip for unapproved references present.
4. `test_status_chips_strict`: `approval_status` compared to `"approved"` exactly (no pending-shortcut).
5. `test_jump_to_node`: `EDITOR.select(` referenced for node jumps.
6. `test_claim_focus_hook`: consumes the `EDITOR.ui.claimFocus` hook from the form (07.1-09) — switching to the claims tab + opening the claim.
7. ES5 discipline; full suite green.
  </action>
  <verify>`python3.6 -m unittest tests.test_story_editor_claims -v` green; full suite green</verify>
  <done>Requirement C structurally pinned read-only; suite green</done>
</task>

</tasks>

<verification>
- Full suite green; emitted HTML contains the claims asset
- No forbidden files touched
</verification>

<success_criteria>
Requirement C delivered: a read-only claims/sources browser with live status chips, review tiers, reverse index, and jump-to-node, structurally pinned.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-12-SUMMARY.md`
</output>
