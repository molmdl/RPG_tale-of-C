---
phase: 07.1-story-framework-node-editing-html-interface
plan: 08
type: execute
wave: 3
depends_on: ["07.1-03", "07.1-04"]
files_modified:
  - tools/story_editor_assets/20_validate.js
  - tests/test_story_editor_validate_js.py
autonomous: true
must_haves:
  truths:
    - "The JS validator mirrors the Python lint rule-for-rule: same rule ids, same severity split (error vs notice), same sanctioned-residual encoding"
    - "The BFS follows goto edges only (cond/weight ignored, on_enter_divert never followed) exactly like rpg/story/validate.py"
    - "Count shifts produce acknowledgment-required notices naming the pinned tests + viewer constants"
    - "Validation runs after every mutation (hook) and gates the save panel"
  artifacts:
    - path: "tools/story_editor_assets/20_validate.js"
      provides: "EDITOR.validate(bundle) → {errors, notices, countShifts}; every rule carries a src: citation comment"
      min_lines: 200
    - path: "tests/test_story_editor_validate_js.py"
      provides: "Parity pins: every lint rule id present in the JS asset with its src citation; sanctioned residual encoded exactly"
  key_links:
    - from: "20_validate.js"
      to: "tools/story_editor_lint.py"
      via: "1:1 rule-id parity (the JS mirror of the Python-authoritative gate)"
      pattern: "dangling_divert|unreachable_ending|count_shift"
    - from: "EDITOR.hooks.validate"
      to: "00_core.js afterChange"
      via: "validator registered as a hook; save panel refuses while errors exist"
      pattern: "EDITOR\\.hooks\\.validate"
---

<objective>
Port the Python lint's rule catalog (07.1-03) 1:1 into the browser as `EDITOR.validate()` — pre-save validation inside the editor with the exact same rule ids, severities, BFS semantics, and the sanctioned citation residual encoded precisely.

Purpose: the editor must catch what the suite would catch BEFORE the human saves (Pitfall S4 drift-guard: each JS rule cites its Python source line, same as the repo's `# src:` convention).
Output: tools/story_editor_assets/20_validate.js + parity tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@tools/story_editor_lint.py      # the authoritative rule catalog + rule ids (07.1-03) — port 1:1
@.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-RESEARCH-SAFETY.md   # breakage map B1-B10 → JS rules with cited sources
@.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-RESEARCH-DATA.md     # §2.2 invariant inventory
@tools/story_editor_assets/00_core.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 20_validate.js — the rule-for-rule JS mirror</name>
  <files>tools/story_editor_assets/20_validate.js</files>
  <action>
Write the validator asset. IIFE attaching `EDITOR.validate`:

1. **BFS parity**: `bfsReachable(bundle)` follows `choice.goto` edges ONLY; cond/weight IGNORED; `on_enter_divert` NEVER followed (dormant, 05.1-DESIGN); start = manifest.start. Cite `rpg/story/validate.py:167 check_reachability` + the don't-hand-roll warning (RESEARCH-SAFETY: a JS BFS following conds would disagree with the Python gate → false green/red).
2. **Rules** — every rule id from tools/story_editor_lint.py, same severity:
   - ERRORS: `dangling_divert`, `unreachable_ending` (orphan ending / tier missing after edit), `router_only_incoming` (gly.pfk_restored/tca.aconitase_restored zero-incoming), `single_continue`, `text_layer_empty` (edit.prompt the ONLY exemption), `tbd_text`, `claim_missing`, `claim_unapproved` (`approval_status === "approved"` STRICT — never a `!== "pending"` shortcut; cite rpg/citations.py:100-109 Pitfall 6), `placeholder_misuse` (PLACEHOLDER_PHASE8 only on fa.stub/alc.stub — Pitfall S6 exact encoding), `offer_without_tag`, `weight_outside_shuffle`, `cond_attribute_form` (dict-method form mandatory — `flags.get(`/`visits.get(`), `empty_bad_ending_pool` (GLOBAL only; per-enzyme empty = legal fallback — cite edit_router.py:193-199), `pool_node_not_ending`, `dangling_pool_node`, `duplicate_signature` (normalized-dict equality: target strip+lowercase, args stringify — cite edit_router.py:122-149), `dangling_edit_branch`, `coverage_uncovered`, `relationship_pin`, `start_node_pdb_load` (+ hero-highlight ordered-subsequence warning per test_glucose_reachability.py:800-843), `unknown_op` (op outside the 10-op story vocabulary + molops extras; `set_view` = NOTICE with the Phase-10 flag text).
   - NOTICES: `count_shift` (nodes/endings-by-tier/edit-allowed old→new + the exact pinned-test names `test_manifest_loads_all_57_nodes`, `test_reachability_green_all_four_tiers`, `test_15_edit_allowed_nodes` + viewer pinned constants), sanctioned-residual info line, `set_view` Phase-10 flag, per-enzyme-empty-pool info.
3. **Sanctioned residual** encoded exactly as the lint does.
4. **Result shape**: `EDITOR.validate(bundle)` → `{errors: [{kind, node, detail, src}], notices: [...], countShifts: {...}}`; `EDITOR.validateCurrent()` runs on the live bundle. Register as an `EDITOR.hooks.validate` hook in 00_core (added via init; core iterates defensively). Every rule's code comment carries `src: <python path:line>` exactly like the lint.
5. **Gating API**: `EDITOR.saveBlocked()` → true when errors exist (the save panel, 07.1-13, consumes this).
6. ES5 only; header comment: ownership 07.1-08, parity source = tools/story_editor_lint.py + the cited test lines.
  </action>
  <verify>Regenerate; emitted HTML contains `EDITOR.validate`, `bfsReachable`, every rule-id string from the lint (spot-check ≥ 12 ids), `approval_status`, `PLACEHOLDER_PHASE8`, `count_shift` with the three test names; hooks.validate registered</verify>
  <done>JS validator mirrors the lint rule-for-rule with citations; gating API exposed; validation wired into afterChange</done>
</task>

<task type="auto">
  <name>Task 2: Parity pins JS ↔ lint</name>
  <files>tests/test_story_editor_validate_js.py</files>
  <action>
Create `tests/test_story_editor_validate_js.py`:
1. Import tools/story_editor_lint.py (sys.path tools) and enumerate its rule ids (expose `RULE_IDS` list in the lint if not already — if 07.1-03 named rules as string constants, import them; else parse the test file's rule-id list — prefer adding a `RULE_IDS` module constant to the LINT only if it does not already exist; do NOT modify the lint's logic, a constant addition is allowed under this plan's file scope? NO — the lint is 07.1-03's file. Parse the rule ids from tests/test_story_editor_lint.py instead, or hard-code the list here with a comment. Choose: parse `tests/test_story_editor_lint.py` for `rule ids` markers, fallback list embedded).
2. Assert every rule id string appears in the JS asset; assert the three pinned-test names appear in the count_shift notice; assert `approval_status === "approved"` strict comparison present and NO `!== "pending"`-style shortcut; assert PLACEHOLDER_PHASE8 + fa.stub + alc.stub co-occur.
3. Assert BFS comment cites `rpg/story/validate.py` and the asset contains no `on_enter_divert` FOLLOWING (the string may appear only in a "never followed" comment).
4. `EDITOR.saveBlocked` present.
5. ES5 discipline; full suite green.
  </action>
  <verify>`python3.6 -m unittest tests.test_story_editor_validate_js -v` green; full suite green</verify>
  <done>Rule-id parity mechanically pinned; sanctioned residual exact; suite green</done>
</task>

</tasks>

<verification>
- Full suite green
- Emitted HTML contains the validator between load (10_) and view assets (30_+)
- No forbidden files touched
</verification>

<success_criteria>
The browser validator is a rule-for-rule mirror of the Python-authoritative lint with mechanical parity pins — the editor now catches suite-breaking edits before save.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-08-SUMMARY.md`
</output>
