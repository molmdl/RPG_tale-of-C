---
phase: 07.1-story-framework-node-editing-html-interface
plan: 10
type: execute
wave: 4
depends_on: ["07.1-04", "07.1-06"]
files_modified:
  - tools/story_editor_assets/60_trace.js
  - tests/test_story_editor_trace.py
autonomous: true
must_haves:
  truths:
    - "A Trace tab renders any path from a chosen start node as an expandable nested list (full-story view)"
    - "Branches expand per choice; cycles are guarded with a ↩ marker instead of infinite recursion"
    - "Each node row shows id, kind/tier chip, and the choice labels that lead onward"
    - "Copy-as-text produces a readable indented plain-text story"
  artifacts:
    - path: "tools/story_editor_assets/60_trace.js"
      provides: "EDITOR.view('trace'): start-node select, DFS nested <ul> render, cycle guard, expand/collapse, copy-as-text"
      min_lines: 100
    - path: "tests/test_story_editor_trace.py"
      provides: "Structural battery: DFS guard, nested-list markup, copy helper, start default"
  key_links:
    - from: "60_trace.js DFS"
      to: "bundle choices[].goto"
      via: "structurally walks goto edges (mirrors the reachability BFS shape — goto only)"
      pattern: "goto"
    - from: "60_trace.js"
      to: "00_core.js EDITOR.select"
      via: "start picker defaults to manifest.start; selecting a node in the graph offers 'trace from here'"
      pattern: "manifest\\.start|EDITOR\\.select"
---

<objective>
Build the path-tracing tab (Requirement D): pick a start node, DFS the choice graph, render the traced flow as a nested list — the "view the full story" instrument.

Purpose: D is a first-class spec item ("a tab/button/panel to trace a path and show the traced flow as a nested list, to view as a full story"); the DFS mirrors the reachability BFS shape (goto-only) so traced structure always agrees with the Python gate (RESEARCH-UI "Path tracing (D)").
Output: tools/story_editor_assets/60_trace.js + structural tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-RESEARCH-UI.md   # "Path tracing (D) — fresh, simple DFS"
@.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-RESEARCH-DATA.md  # §1.6 choice schema (weight/cond semantics for display)
@tools/story_editor_assets/00_core.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 60_trace.js — DFS tracer + nested-list view</name>
  <files>tools/story_editor_assets/60_trace.js</files>
  <action>
Write the trace asset (owns `#tab-trace`). IIFE registering `EDITOR.view('trace', ...)`:

1. **Controls row**: start-node `<select>` (all node ids grouped by file; default `manifest.start` = intro.preface) + "Trace from selected graph node" button (consumes EDITOR.state.sel if set) + "Copy as text" + "Collapse all / Expand all".
2. **DFS render**: walk from the start following each choice's goto in choice order; render nested `<ul><li>`; each node row shows: node id, derived-kind/tier chip (reuse EDITOR.deriveKind/endingTier), and the choice label that leads to each child (e.g. `Continue → gly.g6p`); weighted choices show `⚖ weight 0.5`; conditional choices show `⌿ cond` with the cond string; edit:offer choices get a `wrench` chip. Depth bounded by the visited set — a revisit renders a `↩ already shown` marker instead of recursing (cycle guard REQUIRED once editors can create arbitrary edges; RESEARCH-UI D section).
3. **Expand/collapse**: per-branch toggle (details/summary or class toggle); state kept in module view-state across rerenders.
4. **Copy-as-text**: builds an indented plain-text rendering (2-space per level, `- [tier] node id — choice label`) and copies via a fallback-safe pattern (execCommand fallback since file:// clipboard may be restricted; show "copied" feedback or select-a-textarea fallback).
5. **Live rerender**: traces re-run on afterChange so in-progress edits reflect immediately; if the bundle isn't loaded show the placeholder.
6. Register + init + inject CSS. ES5 only; header comment: ownership 07.1-10; cite the goto-only BFS-shape note (agrees with rpg/story/validate.py:167 semantics — cond/weight never alter structure).
  </action>
  <verify>Regenerate; emitted HTML contains `EDITOR.view('trace'`, the visited-set guard, `manifest.start` default, copy-as-text helper; structural tests below pass</verify>
  <done>Trace tab registered with DFS + cycle guard + expand/collapse + copy-as-text</done>
</task>

<task type="auto">
  <name>Task 2: Structural trace tests</name>
  <files>tests/test_story_editor_trace.py</files>
  <action>
Create `tests/test_story_editor_trace.py`:
1. `test_registered`: `EDITOR.view('trace'` present.
2. `test_cycle_guard`: a visited-set/cycle marker exists (grep `visited` + the ↩ marker or equivalent constant).
3. `test_goto_only_walk`: the DFS reads `choice.goto`/`.goto` and NEVER consults `weight`/`cond` for structure (weight/cond appear only in DISPLAY code paths — assert they occur after the walk, or annotate: assert the walk function body contains no `weight`/`cond` tokens; keep the walk in a named function `traceWalk` to make the test precise).
4. `test_start_default`: `manifest.start` used as the default trace root.
5. `test_copy_helper`: copy-as-text function present with a fallback path.
6. ES5 discipline; full suite green.
  </action>
  <verify>`python3.6 -m unittest tests.test_story_editor_trace -v` green; full suite green</verify>
  <done>Trace tab structurally pinned; suite green</done>
</task>

</tasks>

<verification>
- Full suite green; emitted HTML contains the trace asset
- No forbidden files touched
</verification>

<success_criteria>
Requirement D delivered as a registered tab: nested-list full-story view with cycle guard, expand/collapse, and copy-as-text, structurally pinned.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-10-SUMMARY.md`
</output>
