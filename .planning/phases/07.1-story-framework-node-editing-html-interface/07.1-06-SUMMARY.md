---
phase: 07.1-story-framework-node-editing-html-interface
plan: 06
subsystem: tooling (offline authoring-tool infrastructure — SVG graph view)
tags: [svg, vanilla-js, es5, iife, graph-layout, zoom-pan, event-delegation, derived-kinds, story-editor, offline]

# Dependency graph
requires:
  - phase: 07.1-story-framework-node-editing-html-interface (plan 04)
    provides: the window.EDITOR contract this renderer codes against — EDITOR.view/init registration, EDITOR.select (the click→form channel), EDITOR.state.bundle/dirty read access, deriveKind/endingTier (the viewer-ported derived kinds)
  - phase: 07.1-story-framework-node-editing-html-interface (plan 01)
    provides: the sorted auto-inline asset pipeline (drop a .js file, zero generator edits), the #tab-graph/#graph-area shell mount, and the emitted committed repo-root story_editor.html
  - phase: 07.1-story-framework-node-editing-html-interface (plan 05)
    provides: the inline ordering precedent (serializer before view assets) this asset's ordering pin mirrors
provides:
  - tools/story_editor_assets/30_graph.js — EDITOR.view('graph', renderGraph): the editor's primary navigation surface — 7-column layered layout ported verbatim from the committed viewer (FILE_COL file→column, edit.prompt hub lift to column-5 row 0, stub/unmapped → column 6, row = insertion order; NO stored coordinates), derived kind/level coloring with the viewer's palette, solid choice.goto edges (merged annotations + arrowhead markers) + amber dashed overlay (edits.json branch routes + bad_ending_pool membership), wheel zoom 0.4–2.5× + drag pan in a module view state that SURVIVES rerender, delegated click/double-click → EDITOR.select, dirty-file dot markers, empty-state placeholder
  - The selection→form wiring the node form (07.1-09) consumes: every graph click/double-click lands in EDITOR.state.sel via the shared EDITOR.select, and double-click additionally scrolls/focuses the #node-form panel
  - tests/test_story_editor_graph.py — the 7-test structural battery (registration, FILE_COL-vs-manifest JSON-decoded pin, derived-kinds-only pin, selection wiring, no-persisted-coordinates pin, ES5 discipline, emitted-page inlining order)
affects: [07.1-09 (node form renders from EDITOR.state.sel set here), 07.1-10 (trace panel complements the graph), 07.1-11 + 07.1-19 (Firefox human-verify checkpoints exercise the graph UX), 07.1-16/17 (later panels co-exist on the same rerender dispatcher)]

# Tech tracking
tech-stack:
  added: [] (zero dependencies — hand-rolled ES5, one IIFE, classic script; SVG built via createElementNS with textContent — no markup-injection path)
  patterns:
    - "Viewer-parity layout port: the committed viewer's constants (NODE_W 190 / NODE_H 68 / X0 60 / Y0 90 / COL_PITCH 280 / ROW_PITCH 96), FILE_COL mapping, edit.prompt hub lift and edge geometry classes (fan/fwd/down/up/back, +56px perpendicular dashed offset) are ported with per-item line citations — the human's viewer mental model transfers 1:1 to the editor"
    - "View-state-transform pattern: zoom/pan live in a module-level {tx, ty, scale} that is NOT part of the rebuilt SVG contents — every rerender re-applies the surviving transform (RESEARCH-UI structural choice 3)"
    - "Delegated interaction with pan-suppression: ONE click/dblclick listener per svg keyed by data-node-id via EDITOR.delegate; a pan that moved the view swallows the trailing click so dragging never flips the selection"
    - "Read-only bundle contract in a view: the renderer reads EDITOR.state.bundle/.dirty and never assigns — layout coords are a local view model rebuilt per render, mutations belong to the 00_core mutators (grep-pinned)"

key-files:
  created:
    - tools/story_editor_assets/30_graph.js
    - tests/test_story_editor_graph.py
  modified:
    - story_editor.html (regenerated + committed with all five landed wave-1..3 assets)

key-decisions:
  - "The viewer's EXACT layout constants supersede the plan's approximate numbers ('box ~180x64, gaps ~24/~220'): the plan's first mandate is 'port the committed viewer's approach' and the must-have is that the derivation MATCHES the viewer so the mental model transfers; the plan's numbers are marked '~' and yield a 10px-smaller box with tighter rows for zero benefit (zoom/pan makes fit moot). Documented in-source at the constants."
  - "Node drag deliberately NOT implemented: the plan marks it OPTIONAL view-only, the data has no x/y, and any drag-persistence temptation is a no-stored-coordinates violation — view-only pan is the complete requirement."
  - "bad_ending_pool membership edges run FROM edit.prompt (the hub owns the unknown-edit pool picks per edit_router semantics), not from an abstract container; per-enzyme pool overrides (schema-supported, none in current data) render enzyme→member so the view stays honest if they ever appear."
  - "Ending-level color derives from EDITOR.endingTier(node) (is_ending ONLY, never the ending:* tag — the 3 anaerobic endings carry no tag), applied to BOTH box fill and left stripe; KIND_STYLE remains the fallback — the derived-tier requirement is exercised literally, not just via the kind string."
  - "The FILE_COL literal is written strict-JSON (quoted keys, no trailing comma) so the structural battery can decode it with the stdlib JSON parser and pin it against the live manifest — the same decode-then-compare trick the 07.1-05 battery established."

patterns-established:
  - "Per-item viewer citations: every ported block (constants, mapping, hub lift, edge geometry, defs, palette) cites its `git show HEAD:tools/story_graph_viewer.py` line range in a comment — future view assets (40_form, 60_trace) should keep the provenance discipline"
  - "Sibling-race deferral + self-resolution: story_editor.html regeneration committed only when the emitted page contains exclusively LANDED assets; deferred at Task-1 (07.1-08's in-flight 20_validate.js), self-resolved same-plan once the sibling's commits landed"

# Metrics
duration: 24 min
completed: 2026-09-05
---

# Phase 7.1 Plan 06: SVG Graph View (30_graph.js) Summary

**The editor's primary navigation surface — a 1123-line ES5 SVG renderer whose 7-column layout is ported line-for-line from the committed viewer (no stored coordinates), with derived kind/level coloring, solid choice edges + amber dashed edit/pool overlay, rerender-surviving zoom/pan, and click-to-select feeding the node form — pinned by a 7-test structural battery; suite 460→488 OK across the parallel wave's landings.**

## Performance

- **Duration:** 24 min
- **Started:** 2026-09-04T18:48:08Z (UTC; local 2026-09-05)
- **Completed:** 2026-09-04T19:11:38Z (UTC; local 2026-09-05)
- **Tasks:** 2/2 (+ 1 deferral-resolution commit)
- **Files modified:** 2 created + 1 regenerated/committed

## Accomplishments
- `tools/story_editor_assets/30_graph.js` (1123 lines, one ES5 IIFE, inlined after `05_json.js` by the sorted pipeline, owns `#graph-area`): registered via `EDITOR.view("graph", renderGraph)` + `EDITOR.init(initGraph)` per the 07.1-04 convention.
- **Layout derivation (viewer port, zero stored coordinates):** column = source file via `FILE_COL` (intro=0, glycolysis=1, pyruvate_branch=2, tca=3, etc_atp=4, endings AND bad_endings=5); Phase-8 stub ids (fa.stub/alc.stub — physically in intro.json) and any unmapped file → column 6; `edit.prompt` lifted to row 0 of column 5 (the hub the 15 solid edit:offer edges converge on); row = insertion order; constants `NODE_W 190 / NODE_H 68 / X0 60 / Y0 90 / COL_PITCH 280 / ROW_PITCH 96` ported verbatim with per-item `git show HEAD:tools/story_graph_viewer.py` line citations in the header.
- **Render:** `<g class="node kind-…" data-node-id=…>` per node (rect + left color stripe + truncated id text + kind badge chip + `<title>` tooltip with kind/file/col-row/cast label/dramatic-text prefix); edges as paths — solid for every deduped `choice.goto` (merged labels/weights/conds/choice-tags, arrowhead `marker-end`, RNG `w=0.5` / `cond` mid-labels) and amber dashed overlay for (1) the 13 edits.json enzyme→branch_node routes and (2) the 2-member global `bad_ending_pool` membership from edit.prompt (per-enzyme overrides rendered if ever present); column headers with derived counts; stub hatch pattern + grey/amber marker defs ported from `render_svg_defs`.
- **Derived kinds only:** kind via `EDITOR.deriveKind` (precedence port), ending-level color via `EDITOR.endingTier` (is_ending ONLY) driving box+stripe; palette hexes ported from the viewer CSS (true=#c9a227 gold, good=#2e8b57 green, normal=#4682b4 steel, bad=#b22222 red, restored purple double-border, edit-prompt amber, stub hatch, rng teal, edit-allowed violet, story grey-blue). Zero `node_type`/`tier` key access anywhere in the asset (grep-pinned).
- **Interactions:** wheel zoom bounded 0.4×–2.5× with zoom-at-cursor mapped through the viewBox "meet" scaling; drag pan (cursor grab/grabbing) with pan-suppression of the trailing click; transform kept in module `viewState` that SURVIVES every rerender and is re-applied after each rebuild; "Reset view" button restores the identity transform; delegated click → `EDITOR.select(id)` (selection highlight stroke #1f6feb); double-click additionally scrolls `#node-form` into view and focuses its first field (the form content itself is 07.1-09's).
- **Dirty/validity hints + empty state:** nodes whose file is dirty (reads `EDITOR.state.dirty`, presence-of-key) get a small dot marker; with no bundle the graph renders a "load data first" placeholder with a "Show boot panel" scroll button.
- `tests/test_story_editor_graph.py` (7 tests, all green): registration (`EDITOR.view("graph", renderGraph)` + `EDITOR.init`, ≥150 lines); `FILE_COL` decoded via stdlib JSON == the live manifest's 7-file mapping + column-6 fallback + stub/hub lift markers; derived-kinds-only (references `EDITOR.deriveKind`/`EDITOR.endingTier`, zero `node_type`/`.tier`/`['tier']` occurrences); selection wiring (delegated click AND dblclick on `data-node-id` calling `EDITOR.select(`); no persisted coordinates (no `state.bundle` assignment, no node-field writes, READ-ONLY header pinned); per-asset ES5 discipline (no `=>`/`let`/`const`/modules); emitted-page inlining (after `00_core.js` and `05_json.js`, before the bootstrap — boot anchor = LAST `EDITOR.runInits();` occurrence, the same definition-vs-call ambiguity class 07.1-04's Rule-3 fix resolved).

## Task Commits

Each task was committed atomically:

1. **Task 1: 30_graph.js — layout, render, interactions** - `ea01fab` (feat) — asset only; story_editor.html regeneration DEFERRED (sibling's in-flight 20_validate.js; see Issues)
2. **Task 2: Structural graph tests** - `ed75b3b` (test) — tests/test_story_editor_graph.py
3. **Deferral resolution:** - `b8400b7` (chore) — regenerated committed story_editor.html with all five landed wave-1..3 assets, once 07.1-07/07.1-08's commits had landed

**Plan metadata:** (this commit)

## Files Created/Modified
- `tools/story_editor_assets/30_graph.js` — the SVG graph view asset (created, 1123 lines)
- `tests/test_story_editor_graph.py` — the 7-test structural battery (created, 327 lines)
- `story_editor.html` — regenerated + committed (`b8400b7`): inlines 00_core.js, 05_json.js, 10_load.js (07.1-07), 20_validate.js (07.1-08), 30_graph.js (07.1-06) in pipeline order, before the bootstrap

## Decisions Made
See `key-decisions` in the frontmatter; the load-bearing ones:
- **Viewer's exact constants over the plan's "~" approximations** — the must-have ("matching the committed viewer's layout derivation") outranks approximate numbers; zoom/pan makes the box-size difference moot. Cited in-source at the constants block.
- **Ending-level color literally from `EDITOR.endingTier`** — the plan names both derived APIs; driving box+stripe from the tier value (not just the kind string) makes the 3 anaerobic endings (is_ending without the ending:* tag) color correctly by construction.
- **Pool membership edges FROM edit.prompt** — matches the router semantics (unknown edits route through the hub to the pool), keeps the overlay meaningful next to the hub's solid fan edges, and the +56px dashed offset keeps the coincident pairs visible.
- **No node drag** — optional per plan, and the only drag worth having (persisted positions) is forbidden by the no-stored-coordinates rule.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Renderer initially referenced only `EDITOR.deriveKind`; the plan's own pin caught the missing `EDITOR.endingTier` usage**

- **Found during:** Task 2 (first run of tests/test_story_editor_graph.py)
- **Issue:** The first asset draft colored endings purely via the `ending-<tier>` kind string from `EDITOR.deriveKind` and never called `EDITOR.endingTier` — under-implementing the plan's explicit "Derived kinds via EDITOR.deriveKind/EDITOR.endingTier" requirement (test_derived_kinds_used failed: `EDITOR.endingTier not found in asset`).
- **Fix:** renderNodes now calls `EDITOR.endingTier(node)` and derives the ending-level color from the is_ending VALUE (LEVEL_STYLE map) for BOTH box fill and left stripe — the anaerobic endings (is_ending without the ending:* tag) are correct by construction; KIND_STYLE stays as fallback.
- **Files modified:** tools/story_editor_assets/30_graph.js (renderNodes + LEVEL_STYLE constant)
- **Verification:** tests/test_story_editor_graph.py 7/7 green; full suite green
- **Committed in:** ea01fab (Task 1 commit — the fix landed before the commit)

---

**Total deviations:** 1 auto-fixed (1 bug — caught by the plan's own battery before any commit)
**Impact on plan:** No scope creep; the pin did exactly what the plan built it for.

## Issues Encountered
- **Parallel-agent race on the committed artifact (handled per the spawn-prompt's deferral rule, no deviation):** at Task-1 commit time the sibling 07.1-08's `20_validate.js` carried UNCOMMITTED in-flight modifications in the shared worktree (its agent was mid-work), so regenerating+committing `story_editor.html` would have baked their unlanded code into my commit. I committed only my asset and test and left the artifact unstaged (07.1-05 precedent). Within the same plan session the sibling's commits landed (`585afaa` + `a82eff2` + `bd89b01`), so I regenerated the artifact — now containing EXCLUSIVELY landed assets (00_core, 05_json, 10_load, 20_validate, 30_graph) — and committed it as `b8400b7`, with the full suite re-run green (488 OK including the siblings' tests that landed concurrently; 460 OK at my own Task-2 verification point).
- **No JS runtime in WSL** (no node): the renderer's runtime behavior — SVG correctness, zoom-at-cursor math, pan/click interaction — is NOT machine-executable here. Per the plan + RESEARCH-UI Pitfall 6, runtime verification belongs to the Firefox human-verify checkpoints (07.1-11/19). Mitigations: strict ES5 (test-pinned), hand-review, the structural battery, and the viewer-parity port (the geometry math is copied from a proven implementation, not invented).
- A first-battery failure also caught my own test's bootstrap anchor (`EDITOR.runInits` found at the core asset's DEFINITION instead of the shell bootstrap CALL) — fixed with `rfind("EDITOR.runInits();")` + a comment citing the 07.1-04 anchor-ambiguity precedent; same failure class, different string.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- **07.1-09 (40_form.js)** can code against a LIVE selection: every graph click/double-click lands in `EDITOR.state.sel` via the shared `EDITOR.select`; double-click additionally scrolls/focuses `#node-form` (its renderer should un-hide the panel and fill the four fixed mount sections).
- **The committed story_editor.html is current again** — it embeds all five landed assets in pipeline order; later plans regenerate on landing (the standing watch item from 07.1-04).
- **Human-verify checkpoints (07.1-11/19) — expected graph behaviors:** all 57 nodes in 7 columns matching the viewer (endings+bad pool share column 5 with edit.prompt at its top; fa.stub/alc.stub in the stubs column); endings gold/green/steel/red; 15 edit-allowed + 1 rng (teal wheel badge) + 2 restored (purple double border) + 1 edit-prompt + 2 stubs badged; solid choice edges with arrowheads + w=0.5/cond labels on the shuffle node; 13 amber dashed edit routes + 2 amber dashed pool edges; zoom 0.4–2.5×, drag pan, Reset view; click select (blue outline) feeding the (currently empty) form panel; dirty dots after any mutation.
- **Watch item:** the graph's `viewBox` is recomputed from the layout each render (canvas ≈ 2110×~1850 for the live data); if a later plan changes column pitch/labels, update FILE_COL/COL_LABELS here AND the viewer citations together.

---
*Phase: 07.1-story-framework-node-editing-html-interface*
*Completed: 2026-09-05*
