---
phase: 07.1-story-framework-node-editing-html-interface
plan: 11
type: execute
wave: 5
depends_on: ["07.1-06", "07.1-07", "07.1-08", "07.1-09", "07.1-10"]
files_modified: []
autonomous: false
must_haves:
  truths:
    - "In Firefox on file://, the folder pick loads the full live bundle with a found/missing checklist (fetch probe rejection handled gracefully)"
    - "The graph renders all 57 nodes with derived kind/tier badges and the console is clean"
    - "Node selection opens the form; text edits + undo work; the trace tab renders the full story"
    - "Zero network requests are made (offline/self-contained proof)"
  artifacts:
    - path: ".planning/phases/07.1-story-framework-node-editing-html-interface/07.1-11-SUMMARY.md"
      provides: "The recorded human verdict + any Firefox-behavior findings (download/filename behavior noted for 07.1-13)"
  key_links:
    - from: "human Firefox session"
      to: "story_editor.html"
      via: "file:// open → folder pick → render → edit → undo → trace"
      pattern: "story_editor\\.html"
---

<objective>
CHECKPOINT 1 (blocking, human-verify): prove the foundation in real Firefox before building the deep editing capabilities on top of it — load (probe → folder pick → checklist), graph render, node form, undo, and path trace.

Purpose: browser behavior cannot be automated from WSL (RESEARCH-UI Pitfall 6); this early gate catches load/render/undo defects before 8 more plans stack on the foundation. It also records Firefox download-name behavior as input for the persistence plan.
Output: recorded verdict in 07.1-11-SUMMARY.md.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-RESEARCH-UI.md   # Pitfall 6 human-verify checkpoints (1)+(4)
@story_editor.html
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Firefox foundation verification</name>
  <what-built>
The editor foundation: data-free shell at repo root story_editor.html; load layer (fetch probe → webkitdirectory folder pick → found/missing checklist → draft reload input); SVG graph (57 nodes, 7-column manifest layout, kind/tier badges, zoom/pan); the JS validator running as a background hook (errors surface in the status area). Plans 09 (node form) and 10 (trace tab) are declared dependencies of this checkpoint (see depends_on) — the dependency graph guarantees their assets are complete before this gate runs, so their verification steps below are in scope too.
  </what-built>
  <how-to-verify>
Prerequisite: `python3.6 tools/story_editor.py` has been run (regenerate if unsure) — story_editor.html exists at the repo root.

1. Open `story_editor.html` in **Firefox** (double-click → file://). Open DevTools (F12) → Console AND Network panels.
2. EXPECTED on boot: the boot panel explains that file:// fetch is blocked and shows the folder-pick button. Console shows NO errors (a logged fetch rejection handled by the UI is acceptable only if the panel explains it — it must not be a red unhandled error). Network panel: at most the single probe request (which may fail) — ZERO other external requests.
3. Click the folder-pick input → select the repository root folder. EXPECTED: found/missing checklist shows every expected path ✓ (manifest + 7 story files + citations + sources + edits + cast). Then the graph tab renders all 57 nodes.
4. Sanity-count the graph: intro/glycolysis/pyruvate/tca/etc columns follow the manifest order; endings + bad endings cluster right; True ending gold, Bad red; edit-allowed nodes badged; tca.shuffle shows the wheel badge.
5. Click gly.pfk → the node form opens: id-as-title header, story/science textareas filled, claim chips green (DIS-PFKM-01-cand etc.), tags listed.
6. Edit gly.pfk's story text (append " XX"); click elsewhere (commit); press Ctrl+Z → text reverts; Ctrl+Y / Ctrl+Shift+Z → re-applies; undo again → reverts. Status bar dirty indicator flips with edits.
7. Select intro.preface in the graph, open the Trace tab, click "Trace from selected graph node" (or default start). EXPECTED: a nested list tracing the path; expand/collapse works; Copy as text puts a readable indented story on the clipboard (or shows the fallback textarea).
8. Negative-load check (optional but valuable): copy story_editor.html ALONE into a temp folder outside the repo, open it there, pick the repo folder → the pick still works (location-independent picker) and the checklist fills.
9. RECORD for the persistence plan: nothing to record yet (downloads come later) — but note any console warnings and the exact Firefox version used.
Report: "approved" or the list of defects (console errors, rendering problems, undo misbehavior, trace issues).
  </how-to-verify>
  <resume-signal>Type "approved" or describe the defects found</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record verdict + fix-forward triage</name>
  <files>.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-11-SUMMARY.md</files>
  <action>
Write the SUMMARY recording: the human verdict per step (1-9), Firefox version, console findings, and any defect triage. If defects were reported: classify each as (a) fixed in this plan (small JS/CSS fixes are permitted here — the fix touches ONLY the asset file that owns the defect, never cross-plan files), (b) assigned to the owning later plan, or (c) a new gap requiring replanning. Re-verify any (a) fix by re-running the generator + full suite. Do NOT touch other plans' asset files; do NOT proceed to the subsequent capability plans (12+) until the human approves or explicitly redirects.
  </action>
  <verify>SUMMARY exists with a per-step verdict table; if fixes were made: suite green + generator re-run</verify>
  <done>Checkpoint verdict recorded; foundation approved (or defects dispatched with owners)</done>
</task>

</tasks>

<verification>
Human verdict recorded in the SUMMARY; any in-plan fixes re-verified (generator + suite); no forbidden files touched.
</verification>

<success_criteria>
The foundation is human-verified in Firefox: load, render, select, edit, undo, trace all work; the gate unblocks the subsequent capability plans (12+ follow behind it in plan order).
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-11-SUMMARY.md`
</output>
