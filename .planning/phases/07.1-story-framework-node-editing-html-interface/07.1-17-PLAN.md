---
phase: 07.1-story-framework-node-editing-html-interface
plan: 17
type: execute
wave: 5
depends_on: ["07.1-09", "07.1-14", "07.1-15", "07.1-16"]
files_modified:
  - tools/story_editor_assets/45_lifecycle.js
  - tests/test_story_editor_lifecycle.py
autonomous: true
must_haves:
  truths:
    - "Add node validates unique dot-convention ids, places the node in an existing manifest file, and seeds initial type fields"
    - "Delete node is blocked-with-explanations while any goto / edits bucket / branch_node / cast id / manifest.start / pool references it, with a re-point-N-edges offer"
    - "Type transitions are implemented as the multi-field recipes (edit-allowed = tag + offer choice + edits bucket + optional cast; ending = is_ending + tags + reachable; rng = weights with determinism acknowledgment)"
    - "Any pinned-count shift (57/21/15) forces an explicit acknowledgment modal naming the exact tests + viewer constants"
  artifacts:
    - path: "tools/story_editor_assets/45_lifecycle.js"
      provides: "Add-node wizard, delete-node guard + re-point, type-transition recipes, count-shift acknowledgment"
      min_lines: 180
    - path: "tests/test_story_editor_lifecycle.py"
      provides: "Structural battery: id validation, reference blocking, recipe composition, acknowledgment modal"
  key_links:
    - from: "45_lifecycle.js recipes"
      to: "RESEARCH-DATA §1.7 transition table"
      via: "type changes map to the underlying data changes (never a stored node_type field)"
      pattern: "edit:enzyme:|is_ending|weight"
    - from: "45_lifecycle.js acknowledgment"
      to: "20_validate.js count_shift"
      via: "the modal names test_manifest_loads_all_57_nodes / test_reachability_green_all_four_tiers / test_15_edit_allowed_nodes"
      pattern: "count_shift|test_manifest_loads_all_57_nodes"
---

<objective>
Build the node-lifecycle editor (Requirements B1 + B2): add/delete nodes with reference safety, and node-type transitions implemented as the DERIVED-KIND recipes (there is no stored node_type — "making a node X" means composing the underlying tag/choice/manifest changes, with pinned-count acknowledgment when the frozen invariants move).

Purpose: B1/B2 are the highest-risk capabilities — every path from RESEARCH-DATA §1.7's transition table + RESEARCH-SAFETY's B1/B2 breakage maps must be guarded, because the editor edits data the engine + 345-test battery consume.
Output: tools/story_editor_assets/45_lifecycle.js + structural tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-RESEARCH-DATA.md     # §1.7 type-transition recipes + §1.1 manifest bundling rules
@.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-RESEARCH-SAFETY.md   # breakage map B1 (add/delete) + B2 (type change) + Pitfall S2
@tools/story_editor_assets/00_core.js
@tools/story_editor_assets/20_validate.js
@tools/story_editor_assets/50_editscast.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 45_lifecycle.js — add, delete, type recipes</name>
  <files>tools/story_editor_assets/45_lifecycle.js</files>
  <action>
Write the lifecycle asset (owns the fixed shell sub-section `#node-form-lifecycle` + adds its toolbar buttons into the graph tab via delegated listeners on its own container only). IIFE:

1. **Add node (B1)**: "＋ node" opens a wizard: (a) target FILE select (the 7 manifest files only — a new file would need a manifest edit + lands in unmapped viewer column 6; warn and allow only with explicit manifest-edit acknowledgment; RESEARCH-DATA §1.1); (b) id input validated LIVE: unique across all files (the graph.py:76-80 ValueError class), dot-convention `<seg>.<name>` matching the existing id style (regex ^[a-z0-9_]+\.[a-z0-9_]+$ warn otherwise); (c) initial-type seed select (story / multiple-choices / mutation / ending / rng — each seeds the recipe fields below); (d) create → appends `{text_dramatic: "", text_teaching: "", claim_ids: [], tags: [], on_enter: [{op: "hide_all"}], choices: [...]}` minimal-per-type (endings: choices [] + is_ending + suggested ending:<tier> tag; the two-layer validator immediately flags empty text — intended, the human fills them next) via apply().
2. **Delete node (B1)**: computes referencing sets first — inbound choice.goto, edits.json branch_node/signature targets, cast.json id, edits.json enzyme id + graph edit:enzyme: tag, manifest.start, bad_ending_pool membership; BLOCKED with an itemized explanation list if any exist (mirror of integrity gates 5a/5b/5c), offering "re-point N incoming edges →" (select new targets per edge) and "delete bucket/cast entry first" deep links to the Data tab; manifest.start deletion additionally requires typing the replacement start id. Clean delete runs via apply() (removes the node from its file).
3. **Type transitions (B2)** — a "Node type" section listing the derived kind + the recipe buttons per RESEARCH-DATA §1.7:
   - **Make edit-allowed (mutation node)**: guided flow — add `edit:enzyme:<id>` tag → add the `edit:offer` choice (goto edit.prompt, tag edit:offer) → create the edits.json bucket `<id>` (delegates to the Data tab with the id prefilled; structural cases like citrate_synthase skip the bucket with the edit:structural note) → optional cast entry → shows the 15-count acknowledgment (below).
   - **Make ending**: set is_ending (none/true/good/normal/bad) + suggested ending:<tier> + stage:* tags + choices []; immediately runs the BFS — if unreachable from manifest.start, BLOCK with the orphan-ending explanation (reachability red class) until an inbound edge is added (deep-link to the choices editor of a candidate parent).
   - **Make starting node**: requires the manifest-edit acknowledgment flow (start is a manifest property, not a node property) — edit manifest.start via apply() with a typed confirmation.
   - **Make rng (weighted)**: allow adding weights ONLY on tca.shuffle without ceremony; on ANY other node require the determinism acknowledgment (test 16 breakage: seed-42 fate + shuffle-uniqueness pins; RESEARCH-DATA §1.7 row 4).
   - **Un-make ending**: refuse while the id is in any bad_ending_pool (pool_node_not_ending); warn on inbound-edge retarget needs.
4. **Count-shift acknowledgment (Pitfall S2)**: whenever a pending/just-applied action moves nodes/endings/edit-allowed counts, show a modal: "Pinned counts will change: nodes 57→N, endings 21→M, edit-allowed 15→K. These tests + viewer constants must be updated in the SAME commit: test_manifest_loads_all_57_nodes, test_reachability_green_all_four_tiers, test_15_edit_allowed_nodes, tools/story_graph_viewer.py EXPECTED_* constants." — requiring an explicit "I understand — allow" to proceed (the action applies only after acknowledgment). The validator's count_shift notice mirrors this.
5. All mutations via apply() with per-file dirty marking; every new node leaves text editing to the form (empty-text errors expected + surfaced). ES5 only; header: ownership 07.1-17; cite §1.7 + breakage map B1/B2 + Pitfall S2.
  </action>
  <verify>Regenerate; emitted HTML contains the add-wizard id-uniqueness check, the delete blocking reference list, the recipe buttons (edit-allowed/ending/rng/starting), and the count-shift modal naming the three tests</verify>
  <done>Lifecycle editor complete: guarded add/delete + recipe-driven type transitions + acknowledgment gate</done>
</task>

<task type="auto">
  <name>Task 2: Structural lifecycle tests</name>
  <files>tests/test_story_editor_lifecycle.py</files>
  <action>
Create `tests/test_story_editor_lifecycle.py`:
1. `test_id_validation`: uniqueness check + dot-convention regex present.
2. `test_delete_blocklist`: the six reference classes (goto / branch_node / pool / cast / edits-enzyme-tag / manifest.start) each checked before delete; re-point offer present.
3. `test_recipes_not_fields`: no `node_type` write anywhere; the edit-allowed recipe composes tag + offer choice + bucket (grep the three steps); the ending recipe sets is_ending + tags + choices [].
4. `test_count_shift_modal`: the modal text names the three pinned tests + the viewer EXPECTED constants; acknowledgment required before apply (grep the gate).
5. `test_rng_acknowledgment`: weights outside tca.shuffle require the determinism acknowledgment.
6. `test_orphan_guard`: making an ending runs a reachability check and blocks when unreachable.
7. ES5 discipline; full suite green.
  </action>
  <verify>`python3.6 -m unittest tests.test_story_editor_lifecycle -v` green; full suite green</verify>
  <done>B1/B2 structurally pinned with the full guard set; suite green</done>
</task>

</tasks>

<verification>
- Full suite green; emitted HTML contains the lifecycle asset
- No forbidden files touched
</verification>

<success_criteria>
B1/B2 delivered with every §1.7 recipe + breakage-map guard: reference-safe delete, recipe-driven type transitions, count-shift acknowledgment — the editor now edits topology without silently breaking the frozen invariants.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-17-SUMMARY.md`
</output>
