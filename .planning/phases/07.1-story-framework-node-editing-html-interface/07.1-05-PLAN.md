---
phase: 07.1-story-framework-node-editing-html-interface
plan: 05
type: execute
wave: 2
depends_on: ["07.1-01", "07.1-02"]
files_modified:
  - tools/story_editor_assets/05_json.js
  - tests/test_story_editor_serializer_js.py
autonomous: true
must_haves:
  truths:
    - "The JS serializer reproduces the canonical Python output for every fixture vector"
    - "A no-op save of any loaded data file produces byte-identical text (the diff-hygiene guarantee), verified in-browser by the embedded self-test"
    - "The edits-array multiline hand-style exception is ported (edits.json entries never inline)"
  artifacts:
    - path: "tools/story_editor_assets/05_json.js"
      provides: "EDITOR.housy.stringify(obj) — 1:1 JS port of tools/story_editor_json.py + SELFTEST_VECTORS + EDITOR.selfTestSerializer()"
      min_lines: 120
    - path: "tests/test_story_editor_serializer_js.py"
      provides: "Parity pins: every canonical expected string literal from the Python fixtures appears in the JS asset; asset inlined"
  key_links:
    - from: "tools/story_editor_assets/05_json.js"
      to: "tools/story_editor_json.py"
      via: "fixture-vector parity (expected literals embedded in both)"
      pattern: "SELFTEST_VECTORS"
    - from: "05_json.js stringify"
      to: "07.1-13 (80_save.js)"
      via: "save path calls EDITOR.housy.stringify — never JSON.stringify(x, null, 2) directly"
      pattern: "EDITOR\\.housy\\.stringify"
---

<objective>
Port the canonical house-style serializer (07.1-02) to JS inside the emitted page, with the fixture vectors embedded as a boot-time self-test — the in-browser guarantee that draft and final saves emit the house style (indent 2, inline short scalar arrays, inline small dicts, `edits`-array multiline exception) and that a no-op save is byte-identical.

Purpose: serialization is load-bearing for diff hygiene (RESEARCH-SAFETY Pitfall S1); the Python module is the spec, this asset is the runtime.
Output: tools/story_editor_assets/05_json.js + parity tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@tools/story_editor_json.py          # the canonical spec (07.1-02) — port 1:1
@tests/test_story_editor_json.py     # fixture vectors to mirror
@.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-RESEARCH-SAFETY.md   # Pitfall S1 + no-op byte-identical check
</context>

<tasks>

<task type="auto">
  <name>Task 1: 05_json.js — serializer port + embedded self-test</name>
  <files>tools/story_editor_assets/05_json.js</files>
  <action>
Write the second asset (loads after 00_core.js). One IIFE attaching `EDITOR.housy`:

1. **stringify(obj)** — exact JS port of the Python canonical renderer: 2-space indent; `JSON.stringify`-compatible string escaping EQUIVALENT to Python `json.dumps(s, ensure_ascii=False)` for the writable-file character set (strings escape `"` `\` control chars; non-ASCII stays literal — JS JSON.stringify already does this); scalar-list inline iff `indent + len(compact) <= 110`; container arrays always multiline with per-element fits check (dict fits: `indent + compact <= 175` AND no container-array child AND all descendant containers fit at their depth); `edits`-key force-multiline elements (parent-key context flows down the recursion exactly as the Python `ser_keyed` does); empty containers `{}`/`[]`.
2. **Known limitation documented in the header comment**: raw float-token preservation (2.40) is Python-only (JSON.parse cannot recover it); the writable targets (story files, edits.json, cast.json) contain no such literals — 0.5 round-trips; citations/sources are READ-ONLY in the editor so their floats are never re-serialized. Cite 07.1-02.
3. **SELFTEST_VECTORS**: embed the SAME vector table as `tools/story_editor_json.py:fixture_vectors()` — each entry `{name, input (JS literal), expected (exact string)}`. The expected strings MUST equal the Python canonical outputs (copy them from the Python test literals).
4. **EDITOR.selfTestSerializer()**: runs every vector through stringify and returns `{pass: bool, results: [{name, ok, got, expected}]}`; display helper renders a compact pass/fail table (used by the Diagnostics tab in 07.1-18; safe to call from console before that plan lands).
5. Register an init hook that (defensively, try/catch) runs the self-test once at boot and logs a one-line `EDITOR: serializer self-test N/N pass` to the status area — a silent Canary so a ported bug is visible immediately.
6. Header comment: file ownership (07.1-05), spec source (`tools/story_editor_json.py`), constants (W=175, SW=110, edits exception).
  </action>
  <verify>Regenerate; emitted HTML contains `EDITOR.housy`, `SELFTEST_VECTORS`, `selfTestSerializer`; the boot self-test init is registered (grep `selfTestSerializer()` inside an init registration)</verify>
  <done>JS serializer + vectors + self-test inlined; boot canary wired</done>
</task>

<task type="auto">
  <name>Task 2: Parity pins between Python canonical and JS port</name>
  <files>tests/test_story_editor_serializer_js.py</files>
  <action>
Create `tests/test_story_editor_serializer_js.py`:
1. Import `tools/story_editor_json.py` (sys.path.insert tools) and enumerate `fixture_vectors()`.
2. For each vector, assert its `expected` string appears VERBATIM in the asset source `tools/story_editor_assets/05_json.js` (the expected literals are embedded as JS string constants — this pins parity mechanically from WSL; runtime equivalence is proven by the in-browser self-test at the checkpoints).
3. Assert the constants survive the port: `175` and `110` and `'edits'` present in the asset; `JSON.stringify(` appears only inside escaping/compact helpers — specifically assert the asset does NOT call `JSON.stringify(x, null, 2)` anywhere (grep the asset for `null, 2` must be zero).
4. Assert the self-test registration exists (`selfTestSerializer` referenced in an init hook).
5. Full suite green.
  </action>
  <verify>`python3.6 -m unittest tests.test_story_editor_serializer_js -v` green; full suite green</verify>
  <done>Every fixture's expected literal is pinned in the JS asset; no naive-serializer call sites; suite green</done>
</task>

</tasks>

<verification>
- `python3.6 -m unittest discover -s tests` green
- Emitted HTML: housy asset inlined after core, before all view assets
- No forbidden files touched
</verification>

<success_criteria>
The JS serializer is a mechanical 1:1 port with mechanically-pinned parity to the canonical module; the boot canary makes any future drift visible at load.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-05-SUMMARY.md`
</output>
