---
phase: 07.1-story-framework-node-editing-html-interface
plan: 15
type: execute
wave: 4
depends_on: ["07.1-04", "07.1-08"]
files_modified:
  - tools/story_editor_assets/42_choices.js
  - tests/test_story_editor_choices.py
autonomous: true
must_haves:
  truths:
    - "Each choice row edits label, goto (dropdown of existing node ids grouped by file), cond, weight, effects, tags — add/delete/reorder"
    - "Connections + directions (B5) are edited via the goto dropdown; direction is inherent src→dst"
    - "Single-Continue and router-only violations surface inline as validation errors; TBD in labels flagged"
    - "weight/cond fields are exposed with their runtime semantics warnings (dict-method cond form mandatory; weight only inside tca.shuffle per the determinism pin)"
  artifacts:
    - path: "tools/story_editor_assets/42_choices.js"
      provides: "Choice list editor inside the node form: rows for label/goto/cond/weight/effects/tags with add/delete/reorder"
      min_lines: 150
    - path: "tests/test_story_editor_choices.py"
      provides: "Structural battery: goto dropdown, single-continue guard, cond warning, weight restriction, effects schema"
  key_links:
    - from: "42_choices.js goto dropdown"
      to: "all node ids"
      via: "grouped-by-file select = the B5 connection/direction editor (edge direction inherent)"
      pattern: "goto"
    - from: "42_choices.js inline errors"
      to: "EDITOR.validate rule ids"
      via: "single_continue / router_only_incoming / cond_attribute_form / weight_outside_shuffle surfaced per row"
      pattern: "single_continue|cond_attribute_form"
---

<objective>
Build the choices editor (Requirements B3 + B5): full CRUD over a node's choices — the multiple-contents editor (B3) and the connection/direction editor (B5) — with the pinned choice-schema semantics (label/goto/cond/weight/effects/tags) and inline validation.

Purpose: choices ARE the graph's edges; the goto dropdown is the unambiguous, keyboard-accessible connection editor (RESEARCH-UI "Graph editing UX": click-source-click-target wiring is explicitly NOT needed). cond/weight carry runtime landmines (attribute-form cond silently hides choices — the Phase-6 SC#3 bug class; weights break the determinism pin).
Output: tools/story_editor_assets/42_choices.js + structural tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-RESEARCH-DATA.md    # §1.6 choice schema + cond dict-method mandate + weight pin
@.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-RESEARCH-SAFETY.md  # breakage map B3/B5
@tools/story_editor_assets/00_core.js
@tools/story_editor_assets/40_form.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 42_choices.js — choice rows in the node form</name>
  <files>tools/story_editor_assets/42_choices.js</files>
  <action>
Write the choices asset — fills the fixed shell sub-section `#node-form-choices` (defined by 07.1-01's shell; this asset never edits other assets' files). IIFE:

1. **Choice rows**: per choice — index badge, `label` text input, `goto` select (ALL node ids grouped by file via manifest order; current target preselected; empty option "(none — invalid)" to make dangling visible), `cond` text input (shown with a ⚠ hint: "dict-method form only — flags.get('x') / visits.get('x', 0); attribute form silently hides the choice at runtime"), `weight` number input (visible ONLY when the node carries rng:weighted or any choice has weight — otherwise a lock note "weighted choices exist only on tca.shuffle (determinism pin)"), `effects` editor (compact JSON object: {"set": {flag: val}, "incr": {counter: delta}} — a small key/value list editor, not free JSON), `tags` chip editor (vocabulary: mc:observe, edit:offer, edit:unknown, edit:known, edit:known_critical, rng:weighted, branch:aerobic, branch:anaerobic, char:glucose/alc/fa, cycle_trap, divert:amino_acid, divert:fatty_acid, fermentation:lactic/ethanolic/crisis, etc:enter, ending:normal, stub — RESEARCH-DATA §1.5).
2. **Add/delete/reorder**: add appends `{label: "New choice", goto: <first other node>}`; delete with confirm (warn if it would leave <2 choices on a non-ending non-stub node — single_continue); move up/down buttons (order = player-facing selection order for non-weighted).
3. **Inline validation**: per-row error chips from EDITOR.validate mapped to the row (dangling goto, router_only_incoming when target is a restored node, cond_attribute_form regex check client-side, tbd_text on label); the count of choices vs the 0-single-Continue rule shown as a live hint.
4. **Edits via mutators** (choiceUpdate/choiceAdd/choiceDelete/choiceMove in core or local mutators calling EDITOR.apply with the file of the parent node); unknown choice keys preserved (mutators touch only named fields — B11).
5. Ending nodes (choices: []) show the empty-state note "endings have no choices (pinned)". ES5 only; header: ownership 07.1-15; cite §1.6 + the cond/warning sources (rpg/story/interpreter.py:101-135 via RESEARCH-DATA).
  </action>
  <verify>Regenerate; emitted HTML contains `goto`, `cond`, `weight`, `effects`, choice-tag vocabulary strings, the dict-method hint, and the reorder controls</verify>
  <done>Choices editor complete: full CRUD + goto dropdown + semantic warnings + inline validation mapping</done>
</task>

<task type="auto">
  <name>Task 2: Structural choices tests</name>
  <files>tests/test_story_editor_choices.py</files>
  <action>
Create `tests/test_story_editor_choices.py`:
1. `test_goto_dropdown`: a select over node ids grouped by file drives goto edits (B5's mechanism — grep the grouping logic).
2. `test_cond_warning`: the dict-method hint text present; the attribute-form regex check present.
3. `test_weight_restriction`: weight input gated on rng:weighted/weight presence + the tca.shuffle determinism note.
4. `test_single_continue_guard`: delete-<2 warning + inline single_continue chip mapping.
5. `test_effects_editor`: structured set/incr editor (not a raw JSON textarea).
6. `test_unknown_choice_keys`: mutators touch named fields only (no choice rebuild from whitelist — grep).
7. ES5 discipline; full suite green.
  </action>
  <verify>`python3.6 -m unittest tests.test_story_editor_choices -v` green; full suite green</verify>
  <done>B3/B5 structurally pinned; suite green</done>
</task>

</tasks>

<verification>
- Full suite green; emitted HTML contains the choices asset
- No forbidden files touched
</verification>

<success_criteria>
B3 (choice contents CRUD) and B5 (connection + direction via goto) are operable with the pinned runtime semantics warnings, structurally enforced.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-15-SUMMARY.md`
</output>
