---
phase: 07.1-story-framework-node-editing-html-interface
plan: 06
type: execute
wave: 3
depends_on: ["07.1-04"]
files_modified:
  - tools/story_editor_assets/30_graph.js
  - tests/test_story_editor_graph.py
autonomous: true
must_haves:
  truths:
    - "All nodes render as an SVG network laid out by the manifest's 7-column order (file column × insertion row), matching the committed viewer's layout derivation"
    - "Node kinds/tiers are visually distinguishable (derived badges: ending tier colors, edit-allowed, rng, restored, stubs, edit-prompt)"
    - "Click a node → selection; edges render solid (choice.goto) with dashed overlay for edit/pool relations"
    - "Zoom/pan/drag-pan work as view-state transforms that survive rerender"
  artifacts:
    - path: "tools/story_editor_assets/30_graph.js"
      provides: "EDITOR.view('graph') renderer: layout, SVG nodes+edges, kind badges, zoom/pan, click-select"
      min_lines: 150
    - path: "tests/test_story_editor_graph.py"
      provides: "Structural battery: renderer entry points, layout constants, selection wiring present"
  key_links:
    - from: "30_graph.js node click"
      to: "00_core.js EDITOR.select"
      via: "delegated click sets selection → node form + status bar update"
      pattern: "EDITOR\\.select"
    - from: "30_graph.js layout"
      to: "manifest.files order"
      via: "FILE_COL mapping (intro→0 … endings/bad_endings→5, Phase-8 stubs→6)"
      pattern: "FILE_COL"
---

<objective>
Build the SVG graph view — the editor's primary navigation surface: the 7-column layered layout derived from the manifest (no stored coordinates exist), kind/tier coloring, solid choice edges + dashed edit/pool overlay, zoom/pan, and click-to-select feeding the node form.

Purpose: B1/B2/B5 editing is driven from this view's selection; the layout mirrors the committed viewer so the human's mental model transfers (RESEARCH-UI "Graph editing UX").
Output: tools/story_editor_assets/30_graph.js + structural tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-RESEARCH-UI.md   # Graph editing UX + prior-art pointers
@.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-RESEARCH-DATA.md  # §1.7 derived kinds, §1.5 tag vocabulary
@tools/story_editor_assets/00_core.js
@data/story_glucose/manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: 30_graph.js — layout, render, interactions</name>
  <files>tools/story_editor_assets/30_graph.js</files>
  <action>
Write the graph asset (loads after core; owns `#tab-graph`). IIFE attaching to EDITOR:

1. **Layout derivation** (port the committed viewer's approach — read `git show HEAD:tools/story_graph_viewer.py` layout section for the mapping; cite in comments): column = file of the node via FILE_COL `{intro.json:0, glycolysis.json:1, pyruvate_branch.json:2, tca.json:3, etc_atp.json:4, endings.json:5, bad_endings.json:5}`, unmapped ids → column 6 (Phase-8 stubs); row = insertion order within the file. Node box ~180×64, vertical gap ~24, column gap ~220. NO stored coordinates anywhere (data has no x/y; RESEARCH-UI anti-pattern 3).
2. **Render**: build the SVG inside `#tab-graph` — `<g>` per node (rect + id text + kind badge chip + tier color stripe), edges as paths (solid for every choice.goto, arrowhead marker for direction — direction is inherent src→dst; dashed overlay edges for edits.json branch_node relations and bad_ending_pool membership, derived from the loaded edits/cast). Endings colored by tier (true=gold, good=green, normal=steel, bad=red — match the viewer's palette if visible in HEAD; else pick and document). Edit-allowed nodes get an `edit` badge; rng nodes a `wheel` badge; restored/stub/edit-prompt get their badges. Derived kinds via `EDITOR.deriveKind`/`EDITOR.endingTier` (NOT stored fields).
3. **Selection**: delegated click on node groups → `EDITOR.select(id)`; selected node gets a highlight outline; re-render keeps selection. Double-click also opens the form tab focus (select + scroll form into view if present).
4. **Zoom/pan/drag-pan**: wheel zoom (0.4–2.5×) + drag pan on the SVG background; transform matrix kept in module view-state that survives rerender (RESEARCH-UI structural choice 3); "reset view" button. Node drag is OPTIONAL view-only — if implemented, never persisted.
5. **Dirty/validity hints**: node boxes with changes in their file get a subtle dot marker (reads EDITOR.state.dirty); the renderer re-runs on every rerender hook.
6. **Empty state**: if bundle not loaded, render the "load data first" placeholder pointing at the boot panel.
7. Register via `EDITOR.view('graph', render)` + `EDITOR.init(...)`; inject its CSS (svg sizing, badges, edge styles). ES5 only, no modules. Header comment: ownership 07.1-06, cite viewer layout lines.
  </action>
  <verify>Regenerate; emitted HTML contains `EDITOR.view('graph'` (or equivalent registration), `FILE_COL`, arrowhead marker def, zoom handler; structural tests below pass</verify>
  <done>Graph view registered; layout derived from manifest order; selection wired to EDITOR.select; zoom/pan view-state survives rerender</done>
</task>

<task type="auto">
  <name>Task 2: Structural graph tests</name>
  <files>tests/test_story_editor_graph.py</files>
  <action>
Create `tests/test_story_editor_graph.py`:
1. `test_graph_registered`: `EDITOR.view` called with 'graph'; render function present.
2. `test_layout_constants`: FILE_COL mapping matches the manifest order (assert the 7 filenames + column numbers appear in the asset; column 6 stub fallback present).
3. `test_derived_kinds_used`: renderer references `EDITOR.deriveKind`/`EDITOR.endingTier` (never reads a `node_type`/`tier` field — grep asserts no `"node_type"` / `"tier"` key access).
4. `test_selection_wiring`: click delegation calls `EDITOR.select(`.
5. `test_no_persisted_coords`: no `x:`/`y:` written back into the bundle — grep the asset for `state.bundle` mutation (only reads; mutations belong to mutators from 00_core).
6. `test_es5_discipline`: no arrow functions / let/const in the asset (pins the emitted-JS convention).
7. Full suite green.
  </action>
  <verify>`python3.6 -m unittest tests.test_story_editor_graph -v` green; full suite green</verify>
  <done>Graph asset structurally pinned; suite green; no data/viewer files touched</done>
</task>

</tasks>

<verification>
- `python3.6 tools/story_editor.py` emits with graph asset inlined
- Full suite green
- No forbidden files touched
</verification>

<success_criteria>
The graph renders the full manifest-derived layout with derived kind/tier badges, selection wired into the shared state, zoom/pan as view state — all structurally pinned by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-06-SUMMARY.md`
</output>
