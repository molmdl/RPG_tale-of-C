---
phase: 07.1-story-framework-node-editing-html-interface
plan: 14
type: execute
wave: 4
depends_on: ["07.1-04", "07.1-08"]
files_modified:
  - tools/story_editor_assets/50_editscast.js
  - tests/test_story_editor_editscast.py
autonomous: true
must_haves:
  truths:
    - "The edits table editor adds/deletes/updates enzyme buckets and their signature entries with exact normalized-signature duplicate detection and branch_node/node-id validation"
    - "The cast editor adds/deletes/updates entries with the coverage + relationship warnings surfaced live (cast ⊆ edits == tags − {tca.citrate_synthase})"
    - "The bad-ending pool editor manages the global pool (never empty) and optional per-enzyme overrides (empty = legal fallback), members must be endings"
    - "Known-wrong mutations are shown where they WOULD live (edits.json entries whose branch_node is a bad ending) — none exist today by the M5 decision; adding one is supported but flagged"
  artifacts:
    - path: "tools/story_editor_assets/50_editscast.js"
      provides: "EDITOR.view('data'): edits.json bucket editor, cast.json editor, bad_ending_pool editor, relationship-pin display"
      min_lines: 180
    - path: "tests/test_story_editor_editscast.py"
      provides: "Structural battery: normalized-signature logic, pool rules, relationship display, M5 flag"
  key_links:
    - from: "50_editscast.js signature editor"
      to: "edit_router normalize semantics"
      via: "exact dict-equality preview (target strip+lowercase, args stringify) — cite rpg/edit_router.py:122-149"
      pattern: "signature"
    - from: "50_editscast.js pool editor"
      to: "validate_edits_table rules"
      via: "dangling_pool_node / pool_node_not_ending / empty_bad_ending_pool enforced via EDITOR.validate"
      pattern: "bad_ending_pool"
---

<objective>
Build the Data tab (Requirement B4 + B7 + B10): the edits.json table editor (allowed + known-wrong mutations), the cast.json editor (cast PDBs), and the bad-ending random pool editor — the three manifests whose set-relationships are pinned by tests.

Purpose: B4 ("add/delete/update the mutation allowed / known to be wrong"), B7 ("the PDB used as the cast"), B10 ("the nodes in the random pool of bad endings") all live in rpg/data/ manifests whose cross-set invariants the suite pins (RESEARCH-DATA §1.8/§1.9 + RESEARCH-SAFETY breakage map B4/B7/B10).
Output: tools/story_editor_assets/50_editscast.js + structural tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-RESEARCH-DATA.md     # §1.8 edits.json + §1.9 cast.json + pool semantics
@.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-RESEARCH-SAFETY.md   # breakage map B4/B7/B10
@tools/story_editor_assets/00_core.js
@tools/story_editor_assets/20_validate.js
@rpg/data/edits.json
@rpg/data/cast.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: 50_editscast.js — the three manifest editors</name>
  <files>tools/story_editor_assets/50_editscast.js</files>
  <action>
Write the Data-tab asset (owns `#tab-data`). IIFE registering `EDITOR.view('data', ...)` with three sections:

1. **Edits table (B4)**: per-enzyme bucket list (13 today) — bucket header (enzyme id = node id; edit:enzyme: tag carrier) + entries. Entry editor fields: `signature.op` (select: point_mutation; warn on other values), `signature.target` (text, with normalize preview: strip + lowercase displayed live), `signature.args.new_res` (text), `branch_node` (dropdown of node ids grouped by file — must exist in graph), `claim_id` (text + status chip from citations; the citation GATE does not scan edits.json — the editor enforces registry membership as an editor-only lint, LOW criticality, flagged as such). Duplicate detection WITHIN a bucket using exact normalized-dict equality (target strip+lowercase, args values stringified — cite rpg/edit_router.py:122-149; mirror the JS validator's `duplicate_signature` inline). Add/delete bucket and entry via mutators on the edits bundle section; deletion of a bucket REFUSES while cast.json still lists the id (coverage) with a clear message.
2. **Known-wrong mutations (B4 second half)**: an explanatory panel — known-wrong entries WOULD be edits.json entries whose branch_node points at a bad ending (bad.active_site_destroyed / bad.substrate_channel_blocked / bad.cofactor_lost / bad.critical_residue_break carry the edit:known/edit:known_critical tags today as the structural BFS marker). ZERO such entries exist by the M5 decision (STATE 07-01 outcome 4) — the editor SUPPORTS creating one (any entry whose branch_node resolves to an is_ending node gets a "known-wrong routing" badge) but shows a confirmation note citing M5. Both the entry editor and this panel label the difference (allowed-fix → restored/branch node vs known-wrong → bad ending).
3. **Cast editor (B7)**: entry list (12 today) with fields id/label/source(select download)/pdb_id/character/claim_id(+chip). Add requires (live warnings, hard-block only on validator errors): an edits bucket with ≥1 entry, the enzyme id carrying an edit:enzyme:<id> tag in the graph (or explicit acknowledge checkbox for the relationship-test update), claim_id ∈ registry AND approved. Delete refuses while an edits bucket exists for the id. The relationship pin rendered live: `cast(N) ⊆ edits(M) == tags(K) − {tca.citrate_synthase}` with set-diff chips naming offenders (cite tests/test_glucose_content.py:404-449).
4. **Bad-ending pool editor (B10)**: global pool list (members must be existing endings — dropdown filtered to is_ending nodes; length ≥ 1 enforced — validate_graph empty pool rule) + per-enzyme override pools (empty override = legal fallback — INFO not error, cite edit_router.py:193-199). add/remove via mutators. A note distinguishes this RUNTIME pool from edit.prompt's 13 structural choices (never player-facing — RESEARCH-DATA §5).
5. All mutations via EDITOR mutators marking `edits.json`/`cast.json` dirty; validator runs after each change (hook). ES5 only; header: ownership 07.1-14 + M5 + relationship citations.
  </action>
  <verify>Regenerate; emitted HTML contains `bad_ending_pool`, `branch_node`, `new_res`, normalize-preview comment, the M5 reference, and the relationship-pin formula string</verify>
  <done>Data tab registered with the three editors + live relationship display; validator-enforced rules surface inline</done>
</task>

<task type="auto">
  <name>Task 2: Structural edits/cast/pool tests</name>
  <files>tests/test_story_editor_editscast.py</files>
  <action>
Create `tests/test_story_editor_editscast.py`:
1. `test_signature_normalize`: the normalize preview (strip+lowercase / stringify args) implemented — grep the lowercase + trim chain near the signature editor.
2. `test_pool_rules`: global-pool ≥1 enforcement + per-enzyme-empty-is-legal distinction (both code paths present, cite comment).
3. `test_coverage_guards`: bucket-delete refusal while cast lists the id; cast-add warnings (bucket + tag + claim) present.
4. `test_relationship_pin_display`: the relationship formula rendered with set-diff chips.
5. `test_m5_flag`: the M5/known-wrong explanatory panel exists and creating a bad-ending-routing entry requires confirmation.
6. `test_only_edits_cast_dirty`: mutator calls target the edits/cast sections only (no story-file mutations from this asset).
7. ES5 discipline; full suite green.
  </action>
  <verify>`python3.6 -m unittest tests.test_story_editor_editscast -v` green; full suite green</verify>
  <done>B4/B7/B10 structurally pinned; suite green</done>
</task>

</tasks>

<verification>
- Full suite green; emitted HTML contains the Data-tab asset
- No forbidden files touched
</verification>

<success_criteria>
B4 (allowed + known-wrong mutations with the M5 posture), B7 (cast PDBs with coverage/relationship guards), and B10 (bad-ending pool with exact fallback semantics) are operable editors, structurally pinned.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-14-SUMMARY.md`
</output>
