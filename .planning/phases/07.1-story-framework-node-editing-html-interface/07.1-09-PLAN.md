---
phase: 07.1-story-framework-node-editing-html-interface
plan: 09
type: execute
wave: 4
depends_on: ["07.1-04", "07.1-06"]
files_modified:
  - tools/story_editor_assets/40_form.js
  - tests/test_story_editor_form.py
autonomous: true
must_haves:
  truths:
    - "Selecting a node opens a form showing id-as-title, both text layers, claim chips with live approval status, and the tag vocabulary"
    - "B6 mapping honored exactly: title = the node id (displayed, not an editable field — no invented schema fields), story = text_dramatic, science = text_teaching, reference/links = claim_ids resolved live"
    - "Text edits commit one undo step per field on change; unknown node keys are never rebuilt away"
    - "Ending nodes show the Phase-12 CG reminder (B9 inline obligation)"
  artifacts:
    - path: "tools/story_editor_assets/40_form.js"
      provides: "Node form: identity header, text editors, claim chips + claim_ids editor, tags editor, is_ending select, ending-CG reminder"
      min_lines: 180
    - path: "tests/test_story_editor_form.py"
      provides: "Structural battery: field names, B6 mapping markers, ending reminder, unknown-key contract"
  key_links:
    - from: "40_form.js text fields"
      to: "00_core.js mutators"
      via: "data-field bindings commit via apply() — one snapshot per field commit"
      pattern: "EDITOR\\.nodeSet"
    - from: "40_form.js claim chips"
      to: "bundle.citations"
      via: "live approval_status/review_tier resolution per claim_id (C groundwork; full panel in 07.1-12)"
      pattern: "approval_status"
---

<objective>
Build the node form — the editing surface for a node's identity and content half (B6 + B2's is_ending/tag half): id-as-title header, story/science text editors, claim_ids editor with live approval chips, the tag vocabulary editor, and the ending-type control.

Purpose: the form is the primary B6/B2 surface and the anchor for every later capability editor (choices 07.1-15, on_enter 07.1-16, lifecycle 07.1-17). The B6 mapping is a research-pinned schema fact — there is NO title field; id-as-title + live claim/source resolution, never invented fields (flagged to the human in the SUMMARY).
Output: tools/story_editor_assets/40_form.js + structural tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-RESEARCH-UI.md   # B6 field mapping + selection-driven editing
@.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-RESEARCH-DATA.md  # §1.3 node schema (6 keys + is_ending), §1.5 tag vocabulary
@tools/story_editor_assets/00_core.js
@tools/story_editor_assets/30_graph.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 40_form.js — identity, texts, claims, tags, ending control</name>
  <files>tools/story_editor_assets/40_form.js</files>
  <action>
Write the node-form asset (owns `#node-form-identity` — the fixed sub-section the shell provides; never edits other assets' files). IIFE:

1. **Identity header**: node id rendered LARGE as the title (id-as-title — B6a); derived-kind badge(s) + tier chip next to it; file placement shown (`in tca.json`); a comment documents the B6 mapping (title=id; story=text_dramatic; science=text_teaching; reference/links=claim_ids→citations→sources) and the "no invented schema fields" rule.
2. **Text editors** (B6b/c): two textareas bound `data-field="text_dramatic"` / `"data-field="text_teaching"` labeled "Story (dramatic layer)" / "Science (teaching layer)"; use `EDITOR.textFieldBindings` (commit on change = one undo step; live preview on input). Character counts displayed; two-layer validator flags surface inline if the field is emptied.
3. **Claim chips + claim_ids editor** (B6d + C groundwork): chips for each claim_id with live badge from bundle.citations (approved=green, pending=amber, rejected/red, MISSING=red) + review_tier tag; click a chip → `EDITOR.ui.claimFocus(claimId)` hook (the claims panel, 07.1-12, consumes; safe no-op before it lands). An add-input + remove ✕ per chip mutates `claim_ids` via mutators (order preserved). Sanctioned-residual context: PLACEHOLDER_PHASE8 allowed ONLY on fa.stub/alc.stub — the form shows the validator's `placeholder_misuse` error inline when violated.
4. **Tags editor** (B2 partial): chip list of current tags + add-input; the KNOWN vocabulary offered as quick-add suggestions with counts documented (stage:*, edit:enzyme:<id>, edit:structural, edit:prompt, edit:known, edit:known_critical, edit:unknown, ending:<tier>, rng:weighted, preface, char:glucose, cycle_trap — RESEARCH-DATA §1.5); removing/adding runs through mutators; the validator flags tag-rule violations inline (offer_without_tag etc.).
5. **is_ending select** (B2): select with none/true/good/normal/bad; changing it mutates is_ending (add/remove key — ABSENT when none, never null) and reminds (inline note) that ending convention wants ending:<tier> + stage:* tags + empty choices + reachability — validator surfaces the real errors. Router-only/restored/stub nodes show an informational note when their special shape is touched.
6. **B9 reminder**: when the node is an ending (is_ending set), render a pinned info banner: "Ending CG (cutscene render) is Phase-12 territory — the editor tracks this as a reminder, not a field (roadmap B9)."
7. **Unknown-key safety**: the form renders unknown extra keys (if any) in a read-only "extra keys" list with a warning icon — NEVER editable here, never deleted (B11 round-trip).
8. Re-render on `EDITOR.select` + afterChange; ES5 only; inject CSS; header comment: ownership 07.1-09 + B6 mapping + title-field flag (any literal editable title field = schema change requiring engine/gate sign-off — OUT OF SCOPE, surfaced to the human in the SUMMARY).
  </action>
  <verify>Regenerate; emitted HTML contains `text_dramatic`, `text_teaching`, `claim_ids`, `is_ending`, `data-field` bindings, the Phase-12 reminder text, and the extra-keys guard</verify>
  <done>Node form renders on selection with the exact B6 mapping, live claim chips, tag vocabulary, is_ending control, and the B9 reminder</done>
</task>

<task type="auto">
  <name>Task 2: Structural form tests</name>
  <files>tests/test_story_editor_form.py</files>
  <action>
Create `tests/test_story_editor_form.py`:
1. `test_b6_mapping_no_title_field`: asset references text_dramatic/text_teaching/claim_ids; contains the id-as-title comment; contains NO attempt to create a `"title"` key (grep asserts no `nodeSet.*title` / `"title":` write).
2. `test_text_commit_granularity`: uses `EDITOR.textFieldBindings` (change-commit, not per-keystroke snapshot).
3. `test_claim_chips_live`: reads `bundle.citations` + `approval_status` for chips; MISSING state handled.
4. `test_is_ending_absent_not_null`: the select handler removes the key (grep for `delete` usage on is_ending path or explicit absent handling — assert no `is_ending: null` write).
5. `test_b9_reminder`: the Phase-12 reminder text present, conditioned on endings.
6. `test_unknown_keys_guarded`: extra-keys read-only section exists; no code path deletes unknown keys (grep `delete ` scoped review in the test with an allowlist comment).
7. ES5 discipline; full suite green.
  </action>
  <verify>`python3.6 -m unittest tests.test_story_editor_form -v` green; full suite green</verify>
  <done>Form structurally pinned to the B6 contract; suite green</done>
</task>

</tasks>

<verification>
- Full suite green; emitted HTML contains the form asset between graph (30_) and trace (60_)
- No forbidden files touched
</verification>

<success_criteria>
The node form implements the exact B6 mapping (id-as-title, no invented fields — flagged to human), two-layer editors, live claim chips, tag vocabulary, is_ending control, and the B9 ending reminder.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-09-SUMMARY.md` — include the explicit note to the human: nodes have NO title field (id-as-title is the mapping); a literal title field would be a schema change requiring engine/gate sign-off.
</output>
