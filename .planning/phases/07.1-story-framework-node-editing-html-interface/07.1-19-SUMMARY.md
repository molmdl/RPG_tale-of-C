---
phase: 07.1-story-framework-node-editing-html-interface
plan: 19
subsystem: verification (phase-final capstone: cross-asset integration battery + STATE B9 tracking + FINAL human round-trip checkpoint)
tags: [capstone, integration-battery, capability-matrix, offline-clean, fixture-vectors, validator-rule-parity, state-b9, human-verify, firefox, round-trip, diff-hygiene, byte-identity, pending-checkpoint]

# Dependency graph
requires:
  - phase: 07.1-story-framework-node-editing-html-interface (plans 01-18)
    provides: the committed story_editor.html (14 assets inlined) + the suite (600 OK at handoff) + the lint (exit 0) this battery sits on top of
  - phase: 07.1-story-framework-node-editing-html-interface (plan 11)
    provides: the verified load contract (drag-the-folder is THE load path; pick removed per the round-4 user directive; XHR/fetch file:// reads hard-blocked in all current browsers) the checkpoint script is built on
provides:
  - "tests/test_story_editor_integration.py — the capstone cross-asset integration battery (8 tests): 14-asset inlining order + per-block capability signatures, the B1-B11/C/D/E capability matrix, offline cleanliness, committed-artifact freshness, serializer fixture-vector embedding, validator-rule parity (21 error + 3 notice + 2 JS-only), the lint/generator gate pairing"
  - "STATE.md Pending Todos +1: the B9 (ending CG) tracking entry — Phase-12 territory reminder landed exactly once, no other STATE.md section touched by Task 1"
  - "PARTIAL artifact: this SUMMARY is PENDING the Task-2 FINAL human-verify checkpoint (the full edit -> undo -> draft -> reload -> save-edit -> place-files -> gates-green round trip + the no-op-save diff-hygiene check); the verdict + OQ-4 download-filename recording + byte-identity results are written by the continuation agent only after the human response"
affects: [phase-7.1-exit (continuation agent finalizes this summary after the human verdict), 07-18-Task-2 (independent Phase-7 human gate, still pending), phase-8 content editing (the editor's first real consumer), phase-12 ending-CG work (the B9 obligation)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Capstone battery pattern: ONE setUpClass generator run to a temp path; every cross-asset assertion scoped to its owning /* asset: NAME */ block (capability removal, rename, or pipeline fallout fails the pin in one loud place)"
    - "Capability matrix as data: (requirement, owning asset, needles) rows — B1-B11/C/D/E traced row-for-row in a table comment"
    - "Committed-artifact freshness: regen == committed modulo the generated-at timestamp (a stale committed page fails loudly instead of drifting)"
    - "Rule-parity list cross-checked against a live LintIssue kind harvest from story_editor_lint.py (drift guard; extend in the same commit)"

key-files:
  created:
    - "tests/test_story_editor_integration.py (this battery)"
    - ".planning/phases/07.1-story-framework-node-editing-html-interface/07.1-19-SUMMARY.md (this partial record)"
  modified:
    - ".planning/STATE.md (exactly one bullet appended to ### Pending Todos — the B9 entry; this plan exclusively owns STATE.md within 7.1)"

key-decisions:
  - "The diff-hygiene gold standard under the universal download model: a no-op session shows 'Nothing to save — no files are dirty.' (the honest no-op — ZERO downloads on a clean session, pinned in 80_save.js); byte-identity of writable save bodies is already machine-proven (07.1-13's 48/48 headless smoke — every writable body byte-identical to the real repo file) and the human checkpoint re-proves it end-to-end via the one-line git diff on a real edit/save cycle"
  - "fetch() call-site bound: <= 1 fetch site repo-wide in the asset set, and only inside 10_load.js (currently ZERO — transport removed in v0.2.0 per the corrected MFSA 2019-21 policy record)"

# Metrics
duration: ~35 min (Task 1 only; artifact continues after the checkpoint)
completed: PENDING (Task 2 FINAL human-verify) / Task 1 complete 2026-09-17 (UTC)
---

# Phase 7.1 Plan 19: Capstone Summary — PARTIAL (Task 1 complete; Task 2 FINAL checkpoint PENDING)

**Cross-asset integration battery landed (8 tests, suite 600 -> 608 OK; lint exit 0; committed story_editor.html freshness proven), the B9 ending-CG Phase-12 reminder is tracked in STATE.md Pending Todos (exactly one entry; house format), and the FINAL blocking human-verify checkpoint is open: the complete Firefox round trip — drag-load -> text edit -> undo -> draft download -> F5 -> draft reload -> no-op save hygiene -> real save-edit -> place files -> Python gates green — with the download-filename recording (OQ 4) and the console/version record.**

## Performance

- **Duration:** ~35 min (Task 1; artifact continues after the checkpoint)
- **Started (UTC):** ~2026-09-17T18:30Z
- **Completed (Task 1):** 2026-09-17 (UTC)
- **Tasks:** 1 of 3 complete (Task 2 = checkpoint:human-verify BLOCKING; Task 3 = record the verdict + write the close-out)
- **Files modified:** 1 created battery + this record + STATE.md (single Pending-Todos bullet)

## Task 1 — the automated evidence (all green)

| # | Gate | Command / artifact | Outcome | Verdict |
|---|------|--------------------|---------|---------|
| 1 | Integration battery | `python3.6 -m unittest tests.test_story_editor_integration -v` | **8 tests OK** (asset order + freshness, capability matrix, offline clean, token leaks, serializer vectors, rule parity, gate pairing) | GREEN |
| 2 | Full suite | `python3.6 -m unittest discover -s tests` | `Ran 608 tests in 52.722s` — **OK** (600 handoff baseline + 8 net-new) | GREEN |
| 3 | Editor lint (live repo) | `python3.6 tools/story_editor_lint.py` | exit **0** — `LINT: 0 errors, 1 notices (exit 0)` (the 1 notice = sanctioned_residual fa.stub/alc.stub PLACEHOLDER_PHASE8) — also subprocess-verified INSIDE the battery | GREEN |
| 4 | Generator green | `python3.6 tools/story_editor.py --output <tmp>` | exit **0**, output written (subprocess-verified inside the battery) | GREEN |
| 5 | Committed-artifact freshness | battery test_committed_artifact_fresh | regen == committed story_editor.html modulo the generated-at stamp (14 assets, 728,006 bytes) | GREEN |
| 6 | py_compile | `python3.6 -m py_compile tests/test_story_editor_integration.py` | exit 0 | GREEN |
| 7 | STATE.md B9 entry | `grep -c "B9 (Phase 7.1 editor)" .planning/STATE.md` | **1** — exactly one Pending-Todos bullet, house format, research-prescribed wording | GREEN |
| 8 | STATE.md diff hygiene | `git diff .planning/STATE.md` at Task-1 commit | only the `### Pending Todos` section changed (1 added line); no other section touched | GREEN |

### The battery, row by row (what each test pins)

| Test | Contract |
|------|----------|
| test_asset_order_complete | ALL 14 assets inlined in sorted pipeline order (00_core -> 90_boot); each block carries its capability signature; NO unexpected 15th asset marker |
| test_committed_artifact_fresh | regenerating now reproduces the committed repo-root story_editor.html modulo the generated-at timestamp |
| test_capability_matrix | one row per requirement id — B1/B2 lifecycle (checkId/computeReferences/SEED_TYPES/COUNT_SHIFT), B3/B5 goto dropdown, B4/B7/B10 bad_ending_pool + cast/data tab, B6 form text fields, B8 parseScenePaste, B9 `CG → Phase 12` chip, C claims reverse index, D traceWalk + cycle-guard wording, E downloadText/draftDoc/draft filename + EDITOR.apply/undo/redo, B11 nodeSet/nodeAdd/nodeDelete |
| test_offline_clean | no external src/href, no `<link>`, no ES modules, no top-level imports (browser-faithful block extraction via tests/script_blocks.py); fetch() sites <= 1 and only in 10_load.js (currently 0) |
| test_no_token_leaks | named tokens + any `__UPPERCASE__` residue absent |
| test_serializer_vectors_embedded | all 11 tools/story_editor_json.py fixture_vectors() (name, expected) pairs embedded VERBATIM in the emitted 05_json block, decoded with the SHARED sibling-battery JS-string decoder |
| test_validator_rule_parity | 21 error + 3 notice + 2 JS-only rule ids quoted in the emitted 20_validate block; the live LintIssue kind harvest from story_editor_lint.py is a subset (drift guard) |
| test_lint_green_and_generator_green | subprocess lint exit 0 on the live repo + subprocess generator re-run exit 0 (the suite-level gate pairing) |

### Task 1 Part B — the STATE.md B9 entry (verbatim)

> - [ ] **B9 (Phase 7.1 editor) — ending CG edit capability deferred to Phase 12 (roadmap insertion 2026-09-04):** the 7.1 HTML editor ships B9 as a reminder (inline banner on ending nodes + this tracking entry), not a feature; when Phase 12 (Ending Cutscene Rendering) is planned, extend the editor's ending-node view to add/delete/update CG assets.

## Task commits

1. **032883f** — `test(07.1-19)`: the capstone cross-asset integration battery (tests/test_story_editor_integration.py, 412 lines, 8 tests)
2. (metadata) — STATE.md B9 entry + this partial SUMMARY, in the docs commit below

## Deviations from Plan

None in Task 1 — plan executed exactly as written. (The htmL the user round-trips is the SAME committed artifact the battery regenerates fresh-proof — no asset changes landed in this plan by design: the integration tests live in tests/.)

## Issues Encountered

- `test_story_editor_serializer_js` does not re-export `fixture_vectors` at module top-level — it imports the canonical module as `story_editor_json`. One-line attribute fix (`sej.story_editor_json.fixture_vectors()`); battery went green immediately. No asset impact.

## PENDING — Task 2 (checkpoint:human-verify, BLOCKING)

The continuation agent records: the per-step verdict table (the round-trip steps + optional topology proof + offline check), the recorded Firefox download-filename behavior (RESEARCH-UI OQ 4), the one-line git-diff hygiene result, the gates outputs, the Firefox version, and any console warnings. If the human reports defects: fixes touch ONLY the owning asset file; anything beyond small fixes becomes a documented follow-up gap.

**What is NOT automated (and why it must stay human):** the real OS drag gesture onto the real browser page, the real Firefox download dialog (suggested-filename behavior differs per Firefox download settings), and the placement of downloaded files over repo destinations. Every surrounding machine gate is already green above.

## Next Phase Readiness

- Phase 7.1 is DONE except this checkpoint; on "approved" the continuation agent finalizes this SUMMARY (verdict + recordings), marks the phase complete against the phase goal, and closes 07.1-19.
- Independent pending human gates elsewhere (not blocking 07.1): Phase 7's 07-18 Task 2 batch content review; the lossless float-token parse decision (Pending Todos); the smoke-drift re-alignment (Pending Todos).

---
*Phase: 07.1-story-framework-node-editing-html-interface*
*Plan status: PARTIAL — Task 1 complete; Task 2 FINAL checkpoint PENDING (human round trip); Task 3 = record the verdict*
