---
phase: 07.1-story-framework-node-editing-html-interface
plan: 03
subsystem: testing
tags: [lint, validation, citation-gate, reachability, edits-table, tdd, unittest, python36, story-editor, js-port-contract]

# Dependency graph
requires:
  - phase: 05.1-story-graph-design
    provides: the 57-node glucose skeleton + manifest loader semantics the lint validates
  - phase: 07-content-expansion-i
    provides: the authored real bundle (57/21/15), 07-16 content-invariant tests + gate battery this lint mirrors
  - phase: 02-story-engine-core
    provides: rpg.story.validate (check_reachability/validate_graph) imported for exact parity
  - phase: 04-editing-protonation-restore
    provides: rpg.edit_router (validate_edits_table/scan_edit_coverage) imported for exact parity
  - phase: 07.1-story-framework-node-editing-html-interface
    provides: 07.1-RESEARCH-SAFETY.md (breakage map + gate inventory) and 07.1-RESEARCH-DATA.md (invariants table) — the rule sources
provides:
  - tools/story_editor_lint.py — the Python-authoritative pre-save/post-save lint CLI (exit 0/1/2), the "editor green != battery green" antidote
  - tests/test_story_editor_lint.py — 31-test battery: green-on-real + red-on-seeded (22 seeded violation classes) + exit-code contract + loader pins
  - The FROZEN rule catalog (15 def rule_* functions with stable ids + (src:) citations) that 07.1-08's JS validator ports 1:1
affects: [07.1-08 JS validator port, 07.1 editor save path (post-save verdict), Phase 8 topology work (count_shift acknowledgment path)]

# Tech tracking
tech-stack:
  added: []   # stdlib only (argparse/json/os/re/sys/collections/io) + sanctioned rpg.* imports
  patterns:
    - "def rule_* per rule id — the greppable JS-port contract (plan key_links pattern)"
    - "(src: <path:lines>) runtime citation on every lint line (repo src-citation convention)"
    - "parity imports of rpg validators instead of reimplementation (check_citations.py precedent)"
    - "subprocess CLI battery with per-test temp-dir deep copies (real data never mutated)"

key-files:
  created:
    - tools/story_editor_lint.py
    - tests/test_story_editor_lint.py
  modified: []

key-decisions:
  - "Rule ids are the frozen JS-port contract: dangling_divert, unreachable_ending, router_only_incoming, single_continue, text_layer_empty, tbd_text, claim_missing, claim_unapproved, placeholder_misuse, offer_without_tag, empty_bad_ending_pool, pool_node_not_ending, dangling_pool_node, duplicate_signature, dangling_edit_branch, weight_outside_shuffle, cond_attribute_form, coverage_uncovered, relationship_pin, start_node_pdb_load, unknown_op + notices set_view_phase10 / count_shift / sanctioned_residual"
  - "count_shift is a NOTICE, never a hard error (RESEARCH-SAFETY key finding 1): a well-formed topology addition exits 0 but the notice names the same-commit obligations (test_manifest_loads_all_57_nodes, test_reachability_green_all_four_tiers, test_15_edit_allowed_nodes + viewer EXPECTED_NODES/EXPECTED_TIER_COUNTS/EXPECTED_EDIT_ALLOWED)"
  - "Sanctioned residual encoded EXACTLY (Pitfall S6): PLACEHOLDER_PHASE8 only on fa.stub/alc.stub — and each sanctioned stub must carry it exactly once (a lost reference breaks the gate's pinned 2-MISSING form and is a placeholder_misuse error); exact pair present -> sanctioned_residual notice"
  - "cond_attribute_form scans flags./visits./counters. attribute access (the owning test scans flags. only; visits/counters share the identical AttributeError->hidden->stuck failure mode via interpreter._cond); the dict-METHOD form never matches (negative lookahead)"
  - "Loader mirrors the viewer + StoryGraph.load: manifest file order -> OrderedDict nodes + file_of map; duplicate node id raises ValueError -> schema exit 2"
  - "Start-node set for start_node_pdb_load = manifest start + intro.* prefix (derives the owning test's 3 pinned ids intro.preface/shell_glucose/select)"
  - "relationship_pin = edits == tags - {tca.citrate_synthase} (both directions) + cast subset-of edits; coverage_uncovered carries the check_edit_coverage semantics (a cast-without-bucket mutation reports both — mirrors the repo where the coverage gate AND TestManifestRelationships both fail)"
  - "--sources is loaded for claim source-resolution context only, NOT gated (tolerant load; a malformed sources.json never changes the verdict)"
  - "set_view is a NOTICE (set_view_phase10), never an error — scene_capture Phase-10 precedent; unknown ops are errors (NotImplementedError at dispatch)"

patterns-established:
  - "Rule-id + (src:) citation format is the JS port's contract; LINT <KIND>: <where> -- <detail> (src: <path:lines>) for errors, LINT NOTICE <KIND>: ... for notices, summary 'LINT: N errors, M notices (exit X)'"
  - "Seeded-violation testing pattern: per-test shutil.copytree deep copy in tempfile.mkdtemp, one-aspect mutation, subprocess CLI run, assert rule id + exit code"

# Metrics
duration: 10min
completed: 2026-09-04
---

# Phase 7.1 Plan 03: Python Mirror Lint Summary

**Python-authoritative story-editor lint (tools/story_editor_lint.py): 15 rule functions, green on the live 57-node bundle (exit 0, exactly the sanctioned-residual notice) and red on 22 seeded violation classes, with the count-shift acknowledgment path — the frozen rule catalog 07.1-08 ports 1:1 into the browser.**

## Performance

- **Duration:** 10 min
- **Started:** 2026-09-04T03:51:01Z
- **Completed:** 2026-09-04T04:00:39Z
- **Tasks:** 2 (TDD RED + GREEN; REFACTOR not needed — implementation emerged clean, no changes made)
- **Files modified:** 2 created, 0 modified

## Accomplishments
- TDD cycle complete: RED (31 tests, 25 failures + 4 errors with the module missing) -> GREEN (31/31 pass) -> full suite 376 OK (345 + 31), zero data files modified
- The lint runs green on the untouched live bundle: `python3.6 tools/story_editor_lint.py` -> exit 0, `LINT: 0 errors, 1 notices (exit 0)`, the single notice being the sanctioned_residual (fa.stub/alc.stub -> PLACEHOLDER_PHASE8)
- Every breakage-map rule from 07.1-RESEARCH-SAFETY has a named lint rule + a seeded test proving detection (22 seeded classes over 15 rule functions); exit-code contract pinned (0 clean / 1 errors / 2 schema-config incl. duplicate registry key via object_pairs_hook)
- Parity imports in place: rpg.story.validate (check_reachability/validate_graph), rpg.edit_router (validate_edits_table/scan_edit_coverage), rpg.citations (CitationRegistry) — no algorithm reimplemented, the rpg versions are the test-verified source of truth

## Task Commits

Each TDD phase committed atomically:

1. **Task 1: RED — green-on-real + red-on-seeded battery** - `42bfecd` (test)
2. **Task 2: GREEN — lint CLI to green-on-real / red-on-seeded** - `4c7466f` (feat)

_REFACTOR: no commit (no changes needed — one `rule_*` function per contract id, cited constants, separated loaders from the start)._

## Files Created/Modified
- `tools/story_editor_lint.py` — the lint CLI: loaders (bundle/registry/edits/cast/sources), 15 `rule_*` functions, run_lint orchestration, argparse CLI with repo-relative defaults; 3 notices (sanctioned_residual, count_shift, set_view_phase10) + 20 error kinds; `(src:)` citation on every line
- `tests/test_story_editor_lint.py` — 31 tests: TestRealBundleClean, TestSeededViolations (22 seeded cases on per-test deep copies), TestExitCodeContract (5), TestLintLoader (3 in-process: ordered 57-node load + file_of map, duplicate-id raise, pure-import pin)

## Decisions Made
See frontmatter key-decisions. Highlights:
- **count_shift = NOTICE, not error** (per plan Task 2): Phase 8 topology work flows through the acknowledgment path; the notice names the exact pinned tests + viewer constants as same-commit obligations.
- **Sanctioned residual bidirectional**: exactly the stub pair is a notice; more (anywhere else) OR fewer (a stub losing its reference) is a `placeholder_misuse` error — both directions pinned by RESEARCH-SAFETY Pitfall S6 + test_glucose_content.py:213-236.
- **cond_attribute_form generalizes the broken-form regex** to visits./counters. (seeded case is flags., mirroring the owning test) — same runtime failure mode (AttributeError -> choice hidden -> stuck).
- **Loader duplicate-id = schema exit 2** (raises ValueError, mirrors StoryGraph.load:76-80); the JS editor surfaces duplicate ids pre-save.
- **Non-string claim_ids are claim_missing** (defensive; keeps the rule total — nothing silently skipped).

## Deviations from Plan

None - plan executed exactly as written. (The cond-regex extension to visits/counters and the bidirectional sanctioned-residual check are faithful readings of plan rule 13 / RESEARCH-SAFETY Pitfall S6, recorded as decisions above, not scope changes.)

## Issues Encountered
- RED-phase nuance (cosmetic): the 2 exit-2 schema tests (missing manifest / unparseable JSON) passed vacuously in RED because a missing lint script also yields subprocess exit 2 ("can't open file"). True RED evidence came from the 25 failures + 4 errors and the TestLintLoader pins (module must exist to load). GREEN gives both tests real content (the lint's own schema paths fire).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- **07.1-08 (JS validator)** can start immediately: the rule catalog is frozen — each `def rule_*` name is the port target, every issue carries its `(src: path:lines)` citation, and the output format (`LINT <KIND>: ...` / `LINT NOTICE <KIND>: ...` / `LINT: N errors, M notices (exit X)`) is pinned by tests.
- **Editor save path (07.1-04+)**: post-save verdict = one command, `python3.6 tools/story_editor_lint.py` (defaults resolve repo-relative; CWD-independent).
- **Phase 8 topology work**: the count_shift acknowledgment path is live — a well-formed add exits 0 with the notice naming the pinned trio + viewer constants.
- No blockers. Wave-1 parallel-safety honored: only `tools/story_editor_lint.py` + `tests/test_story_editor_lint.py` touched (07.1-01/07.1-02 files untouched); no data files modified.

---
*Phase: 07.1-story-framework-node-editing-html-interface*
*Completed: 2026-09-04*
