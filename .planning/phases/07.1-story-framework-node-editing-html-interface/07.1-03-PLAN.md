---
phase: 07.1-story-framework-node-editing-html-interface
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - tools/story_editor_lint.py
  - tests/test_story_editor_lint.py
autonomous: true
must_haves:
  truths:
    - "The lint runs green (exit 0, zero errors) on the untouched live bundle"
    - "Each seeded violation class (dangling goto, orphan ending, unapproved claim, single-Continue, router-only edge, empty pool, cond attribute-form, TBD text, weight outside shuffle, cast-without-bucket) is detected with a named issue"
    - "Count shifts (57/21/15) are reported as NOTICES naming the exact pinned tests + viewer constants — never as silent passes or hard errors"
    - "Post-save workflow: a human can run one command to get the Python-authoritative verdict on edited data"
  artifacts:
    - path: "tools/story_editor_lint.py"
      provides: "CLI: python3.6 tools/story_editor_lint.py [--story-dir DIR] [--registry PATH] [--edits PATH] [--cast PATH] -> issues list, exit 0/1/2"
      min_lines: 150
    - path: "tests/test_story_editor_lint.py"
      provides: "Green-on-real + red-on-seeded-fixture battery (one seeded case per rule)"
  key_links:
    - from: "tools/story_editor_lint.py"
      to: "rpg/story/validate.py + rpg/edit_router.py"
      via: "imports check_reachability/validate_graph/validate_edits_table for exact parity (check_citations.py precedent)"
      pattern: "from rpg\\.(story\\.validate|edit_router) import"
    - from: "tools/story_editor_lint.py"
      to: "07.1-08 (JS validator 20_validate.js)"
      via: "the rule catalog the JS mirror ports 1:1 with src: citations"
      pattern: "def rule_"
---

<objective>
Implement the **Python mirror lint** — the authoritative pre-save/post-save validator that the JS validator (07.1-08) ports 1:1 into the browser. This is the "editor green ≠ battery green" antidote: the same checks the 345-suite pins, runnable in one command against edited data BEFORE the suite.

Purpose: the editor cannot see the filesystem; the lint is the fast Python-authoritative gate the human runs after saving files (and the spec the JS mirrors). TDD: green on the real bundle, red on seeded violations.
Output: tools/story_editor_lint.py + tests/test_story_editor_lint.py.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-RESEARCH-SAFETY.md   # gate inventory + breakage map — the rule sources
@.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-RESEARCH-DATA.md     # invariants table §2.2 + pitfalls §4
@rpg/story/validate.py
@rpg/edit_router.py
@tests/test_glucose_reachability.py
@tests/test_glucose_content.py
@tools/check_citations.py
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED — green-on-real + red-on-seeded battery</name>
  <files>tests/test_story_editor_lint.py</files>
  <action>
Write the failing tests first. Structure: load the REAL bundle via the lint's own loader (deep-copy into per-test temp dirs for mutation); each seeded case mutates one aspect and asserts the specific issue kind appears and exit code 1. Rule catalog (each test names the rule id the lint must emit — these ids are the contract the JS port mirrors):

1. `real_bundle_clean`: lint on the live repo data → zero errors, exit 0 (notices about sanctioned residual allowed: exactly the PLACEHOLDER_PHASE8 stub pair).
2. `dangling_goto`: retarget a choice.goto to a missing node → issue kind `dangling_divert`.
3. `orphan_ending`: add an ending node with no inbound edge → `unreachable_ending` (+ reachability not_ok).
4. `router_only_edge`: add a choice.goto INTO gly.pfk_restored → `router_only_incoming` (tests/test_glucose_reachability.py:326).
5. `single_continue`: reduce a 2-choice non-ending node to one Continue → `single_continue` (:245).
6. `two_layer_blank`: blank a text_teaching → `text_layer_empty` (:126-184); `tbd_text`: insert "TBD" into a label → `tbd_text` (:154).
7. `claim_unapproved`: point a node's claim_ids at a pending/missing id → `claim_missing` / `claim_unapproved` (:238).
8. `sanctioned_residual_scope`: PLACEHOLDER_PHASE8 on any node other than fa.stub/alc.stub → `placeholder_misuse` (:213-236).
9. `edit_offer_tag_mismatch`: add an edit:offer choice to a node without an edit:enzyme: tag → `offer_without_tag` (:462).
10. `pool_empty`: set global bad_ending_pool to [] → `empty_bad_ending_pool`; `pool_not_ending`: point the pool at a non-ending → `pool_node_not_ending`; `dangling_pool` → `dangling_pool_node` (validate_edits_table :172-213).
11. `signature_duplicate`: duplicate a signature in one bucket → `duplicate_signature`; `branch_dangling`: branch_node to a missing node → `dangling_edit_branch`.
12. `weight_outside_shuffle`: add weight to a non-shuffle choice → `weight_outside_shuffle` (test_glucose_content.py:539).
13. `cond_attribute_form`: write cond `flags.host_o2_low` (attribute form) → `cond_attribute_form` (reachability :716-752 — the dict-method form is mandatory).
14. `cast_without_bucket`: add a cast entry with no edits bucket → `coverage_uncovered` (check_edit_coverage semantics); relationship pin `cast ⊆ edits == tags − {tca.citrate_synthase}` broken → `relationship_pin` (test_glucose_content.py:404-449).
15. `start_node_pdb_load`: add a `pdb:` load to intro.preface on_enter → `start_node_pdb_load` (reachability :781-798).
16. `unknown_op`: add an op outside the frozen vocabulary → `unknown_op`; `set_view` → NOTICE not error (scene_capture Phase-10 precedent).
17. `count_shift_notice`: add a well-formed extra node+edge making everything else green → exit 0 BUT a `count_shift` NOTICE reporting old→new for nodes/endings/edit-allowed and naming `test_manifest_loads_all_57_nodes`, `test_reachability_green_all_four_tiers`, `test_15_edit_allowed_nodes` + the viewer pinned constants as the same-commit update obligations (RESEARCH-SAFETY key finding 1).
18. `exit_code_contract`: errors → 1; unparseable JSON/schema error → 2; clean → 0.
Implementation notes for the lint (encoded in the tests' expectations): import `rpg.story.validate.check_reachability/validate_graph` + `rpg.edit_router.validate_edits_table/scan_edit_coverage` for exact parity (tools importing rpg is sanctioned — check_citations.py precedent); the lint is pure-Python, runs in WSL python3.6, no f-strings.
  </action>
  <verify>`python3.6 -m unittest tests.test_story_editor_lint` fails (module missing — RED); count of seeded cases ≥ 15</verify>
  <done>Full seeded-violation battery written; every rule id named; exit-code contract pinned</done>
</task>

<task type="tdd">
  <name>Task 2: GREEN — lint CLI to green-on-real / red-on-seeded</name>
  <files>tools/story_editor_lint.py</files>
  <action>
Implement the CLI (Python 3.6, stdlib + the two rpg imports, no f-strings):
- Args: `--story-dir` (default data/story_glucose), `--registry` (default data/citations.json), `--edits` (default rpg/data/edits.json), `--cast` (default rpg/data/cast.json), `--sources` (default data/sources.json; loaded for claim source resolution context, NOT gated).
- Loaders mirroring the viewer's committed loaders (manifest + files → ordered OrderedDict of nodes + file_of; duplicate-id raise). Registry load with duplicate-key rejection (json object_pairs_hook detecting dupes → schema error exit 2).
- Rules: implement every rule id from Task 1. Each issue prints `LINT <KIND>: <node/file> — <detail> (src: <path:line>)` mirroring the repo's src-citation convention; the JS port (07.1-08) carries the same rule ids + citations.
- Count-shift detector: compare live (nodes, endings by tier, edit-allowed ids) against the pinned trio **as reported values, not assertions**; emit the `count_shift` notice with the exact test/constant names. This is deliberately NOT a hard failure — Phase 8 topology work flows through the acknowledgment path.
- Sanctioned residual encoded EXACTLY (RESEARCH-SAFETY Pitfall S6): PLACEHOLDER_PHASE8 permitted only as fa.stub/alc.stub claim_ids; citation gate semantics = `approval_status == "approved"` strictly.
- Exit: 0 no errors (notices OK) / 1 errors / 2 schema-config.
- Print a final summary line: `LINT: N errors, M notices (exit 0|1|2)`.
  </action>
  <verify>`python3.6 -m unittest tests.test_story_editor_lint -v` all green; `python3.6 tools/story_editor_lint.py` on the live repo → exit 0 with the sanctioned-residual notice + zero errors; full suite green</verify>
  <done>Lint green on real data, red on every seeded class, exit codes pinned, rpg-parity imports in place</done>
</task>

</tasks>

<verification>
- `python3.6 tools/story_editor_lint.py` → exit 0, zero errors on untouched data
- `python3.6 -m unittest tests.test_story_editor_lint -v` green (RED→GREEN recorded)
- Full suite `python3.6 -m unittest discover -s tests` green
- No data files modified
</verification>

<success_criteria>
Every breakage-map rule from RESEARCH-SAFETY has a named lint rule + a seeded test proving detection; the count-shift acknowledgment path exists; the JS port contract (rule ids + src citations) is frozen by this module.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-03-SUMMARY.md`
</output>
