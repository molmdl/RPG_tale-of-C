---
phase: 07.1-story-framework-node-editing-html-interface
plan: 16
type: execute
wave: 4
depends_on: ["07.1-04", "07.1-08"]
files_modified:
  - tools/story_editor_assets/43_onenter.js
  - tests/test_story_editor_onenter.py
autonomous: true
must_haves:
  truths:
    - "The on_enter editor lists each MolAction with a human summary and per-op field forms for the frozen vocabulary"
    - "Paste-in accepts scene_capture.py's emitted jsonc (// headers, one JSON array, # NOT CAPTURED notes) and validates op-by-op"
    - "Unknown ops warn-but-accept (B11 extensibility); set_view entries are accepted with the Phase-10 flag visible"
    - "Pinned sequences (start-node hero highlight, restored-node [edit,load,align,show_as]) warn loudly before modification"
  artifacts:
    - path: "tools/story_editor_assets/43_onenter.js"
      provides: "on_enter op list editor + parseScenePaste (scene_capture contract) + pinned-sequence guards"
      min_lines: 170
    - path: "tests/test_story_editor_onenter.py"
      provides: "Structural battery: paste contract, op vocabulary, set_view flag, pinned guards, no-load/hide_all-invention rule"
  key_links:
    - from: "43_onenter.js parseScenePaste"
      to: "tools/scene_capture.py render_capture format"
      via: "jsonc paste contract: strip // and # lines, first [ .. last ], per-entry op validation"
      pattern: "NOT CAPTURED|set_view"
    - from: "43_onenter.js op forms"
      to: "molops dispatch vocabulary"
      via: "field shapes per op (target/args) from the 10-op story vocabulary + molops extras"
      pattern: "select_focus|zoom|set_color"
---

<objective>
Build the on_enter editor (Requirement B8): view/edit the node's MolAction sequence, and paste in camera/color/representation blocks captured from a real PyMOL session using `tools/scene_capture.py`'s emitted paste-ready JSON — "ask the user to copy something, keep it simple, no PyMOL call" per the spec.

Purpose: B8's paste format is pinned by the roadmap pointer to 07-19's scene_capture output (frozen molops vocabulary + exactly one Phase-10-flagged set_view; load/hide_all never emitted replay-side). The op arg-shapes come from the verified RESEARCH-DATA §1.4 table.
Output: tools/story_editor_assets/43_onenter.js + structural tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-RESEARCH-DATA.md   # §1.4 op vocabulary table (10 ops + arg shapes) + pinned sequences
@.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-RESEARCH-UI.md     # scene_capture paste contract + Code Example 5
@tools/scene_capture.py    # read-only prior art — the paste format source (FROZEN file; never modify)
@tools/story_editor_assets/00_core.js
@tools/story_editor_assets/40_form.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 43_onenter.js — op list + paste-in editor</name>
  <files>tools/story_editor_assets/43_onenter.js</files>
  <action>
Write the on_enter asset — fills the fixed shell sub-section `#node-form-onenter` (defined by 07.1-01's shell; this asset never edits other assets' files). IIFE:

1. **Op list**: numbered rows, each showing a HUMAN SUMMARY (port the committed viewer's summarize_on_enter mapping — read `git show HEAD:tools/story_graph_viewer.py` :254-312 for the exact mapping incl. the unknown-op fallback; cite in a comment) plus expandable per-op fields: `op` (select over the KNOWN vocabulary: hide_all, load, show_as, edit, align, set_color, color, show, set, label, select_focus, zoom, delete, protonate, restore, set_view — the 10 story ops + molops-supported extras + set_view), `target` (text; hidden for ops without target), `args` (per-op fields per RESEARCH-DATA §1.4: load{object}, show_as{rep,sele?}, edit{edit_type,sele,new_resn}, align{reference,method,align_sele}, set_color{name,rgb[3]}, color{color,sele}, show{rep,sele}, set{name,value,sele}, label{sele,text}, select_focus{name?,sele}, zoom{sele?}, set_view{view[18]}, delete{}, protonate{variant_id}, restore{}). Add/delete/move (order matters — reorder is a structural change and shows a confirm).
2. **Paste-in (B8 core)**: a textarea + "Attach pasted ops" button implementing `parseScenePaste` exactly (RESEARCH-UI Code Example 5): split lines, drop empty/`//`/`#` lines, take first `[` .. last `]`, JSON.parse; per-entry: object with string op required; op in KNOWN → ok; op UNKNOWN → warn-but-accept (B11: "unknown op 'X' (kept)" — never silently dropped, never rejected); `set_view` → accepted + VISIBLE "Phase-10 flag: camera rides home until the set_view dispatch lands" badge (scene_capture stdout-warning parity). Parsed ops render as a PREVIEW list (summarized) before a confirm attaches them via apply() (append after existing ops — never reorder existing ops of pinned nodes).
3. **Pinned-sequence guards**: for intro.preface/intro.shell_glucose/intro.select (start nodes) — appending a `load` with a `pdb:` target or modifying the hero-highlight 6-op ordered subsequence shows a hard-warn (start-shape tests :781-908); for gly.pfk_restored/tca.aconitase_restored — modifying/reordering the [edit, load, align, show_as] reveal warns (restoration pin :391-414); `edit`/`restore`/`protonate` ops on NON-restored nodes warn (zero-restore-ops invariant, 07-17). All warns are confirms, not blocks (the validator carries the authoritative errors).
4. **No-invention note**: load/hide_all never appear in scene_capture OUTPUT (replay-side) — the paste parser accepts them if hand-written but the preview notes their replay-side semantics.
5. Mutators touch only the on_enter array (per-op field edits, insert, delete, move); unknown op KEYS preserved on round-trip (op dicts may carry extra keys — never stripped). ES5 only; header: ownership 07.1-16; cite scene_capture.py:416-434 + molops.py vocabulary + §1.4 table.
  </action>
  <verify>Regenerate; emitted HTML contains `parseScenePaste`, `NOT CAPTURED` handling comment, `set_view` Phase-10 flag text, the per-op arg field names (new_resn, align_sele, sphere_scale via set{name:...}), and the pinned-node guards</verify>
  <done>on_enter editor + paste contract complete: per-op forms, warn-but-accept unknowns, set_view flag, pinned guards</done>
</task>

<task type="auto">
  <name>Task 2: Structural on_enter tests</name>
  <files>tests/test_story_editor_onenter.py</files>
  <action>
Create `tests/test_story_editor_onenter.py`:
1. `test_paste_contract`: strip-`//`/`#`, first-`[`-last-`]`, JSON.parse chain present (grep the sequence); per-entry op-string validation present.
2. `test_warn_but_accept`: unknown-op warn path KEEPS the entry (no reject/throw on unknown op).
3. `test_set_view_flag`: set_view accepted with the Phase-10 badge text.
4. `test_op_vocabulary`: the KNOWN list contains the 10 story ops + select_focus/zoom/delete/protonate/restore/set_view.
5. `test_pinned_guards`: start-node pdb-load warning + restored-sequence warning + zero-restore warning paths present (cite test lines in comments).
6. `test_no_reorder_silent`: reorder/attach require confirm (grep confirm dialogs near move/attach).
7. ES5 discipline; full suite green.
  </action>
  <verify>`python3.6 -m unittest tests.test_story_editor_onenter -v` green; full suite green</verify>
  <done>B8 structurally pinned to the scene_capture contract; suite green</done>
</task>

</tasks>

<verification>
- Full suite green; emitted HTML contains the on_enter asset
- No forbidden files touched
</verification>

<success_criteria>
B8 delivered: paste-in scene capture per the frozen contract, per-op editing across the molops vocabulary, extensibility posture (warn-but-accept), and pinned-sequence guards.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-16-SUMMARY.md`
</output>
