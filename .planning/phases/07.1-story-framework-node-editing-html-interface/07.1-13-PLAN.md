---
phase: 07.1-story-framework-node-editing-html-interface
plan: 13
type: execute
wave: 4
depends_on: ["07.1-05", "07.1-08"]
files_modified:
  - tools/story_editor_assets/80_save.js
  - tests/test_story_editor_save.py
autonomous: true
must_haves:
  truths:
    - "Save draft downloads ONE self-contained draft JSON (bundle + dirty + undo_meta + fingerprints) intended for git-ignored tmp/"
    - "Save edit lists ONLY dirty files, one row each with the exact repo-relative destination, downloading per-file in house style"
    - "A stale draft (fingerprints mismatch vs re-picked folder) warns loudly before clobbering"
    - "localStorage is optional guarded crash-copy only — never load-bearing; after any save the panel reminds the human to run the Python gates"
  artifacts:
    - path: "tools/story_editor_assets/80_save.js"
      provides: "downloadText primitive, draftDoc, Save-draft + Save-edit panel, sequential batch, guarded crash copy, stale detection"
      min_lines: 180
    - path: "tests/test_story_editor_save.py"
      provides: "Structural battery: no-FSA contract, Blob/download primitive, destination display, fingerprint gates, tmp/ guidance"
  key_links:
    - from: "80_save.js serialize"
      to: "EDITOR.housy.stringify"
      via: "every emitted file body uses the house-style serializer (never JSON.stringify(x, null, 2))"
      pattern: "EDITOR\\.housy\\.stringify"
    - from: "80_save.js save gate"
      to: "EDITOR.validate"
      via: "save refused while errors exist (saveBlocked); post-save reminder names the lint + suite commands"
      pattern: "saveBlocked|story_editor_lint"
---

<objective>
Build the persistence layer (Requirement E's save half, per the BINDING user directives): universal `<a download>`/Blob saves — draft as one self-contained JSON for git-ignored `tmp/`, save-edit as per-dirty-file downloads with displayed repo-relative destinations — plus the guarded localStorage crash copy and stale-draft fingerprint detection. NO File System Access API, no server.

Purpose: "saving temp/final json could be more simple and universal. no api, no server" (user directive; RESEARCH-UI Persistence section is the binding mechanism set).
Output: tools/story_editor_assets/80_save.js + structural tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-RESEARCH-UI.md   # Persistence + Code Examples 3-4 + Pitfalls 3/3b/3c
@.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-RESEARCH-SAFETY.md # draft fingerprints + save sequence + Pitfall S3
@tools/story_editor_assets/00_core.js
@tools/story_editor_assets/05_json.js
@tools/story_editor_assets/20_validate.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 80_save.js — draft + final save + crash copy</name>
  <files>tools/story_editor_assets/80_save.js</files>
  <action>
Write the save asset (owns `#tab-save`). IIFE registering `EDITOR.view('save', ...)`:

1. **downloadText(filename, text)** — the ONE save primitive (RESEARCH-UI Code Example 3): Blob URL → temp `<a download>` → click → revoke after timeout. Header comment: filename is a SUGGESTION (MDN; Pitfall 3b) — every affordance displays the exact expected name + destination; Firefox "Always ask you where to save files" guidance text shown in the panel.
2. **Save draft**: builds `draftDoc()` = `{version: 1, saved_at, bundle, dirty: [fnames], undo_meta: {undo_depth, redo_depth}, fingerprints}` → downloads `story_editor_draft.json` with instruction "save into the repo's tmp/ folder (git-ignored)". Draft reload comes from the boot panel input (07.1-07).
3. **Save edit (final)**: refuse while `EDITOR.saveBlocked()` (validator errors) — show the error list instead. Otherwise a panel with ONE ROW PER DIRTY FILE: `[Download] tca.json → save over data/story_glucose/tca.json` (dest map: citations/sources → data/, edits/cast → rpg/data/, story files → data/story_glucose/ — display-only; the editor never writes). Row body = `EDITOR.housy.stringify(fileContent)` (house style ONLY). Per-row buttons default; opt-in "Download all (N)" fires rows SEQUENTIALLY with ~400 ms gaps (Pitfall 3). After the panel opens: reminder box naming the post-save commands — `python3.6 tools/story_editor_lint.py`, `python3.6 -m unittest discover -s tests`, `python3.6 tools/check_citations.py --story data/story_glucose --registry data/citations.json`, `git diff` review. After a successful save action the user marks rows saved (button per row: "mark saved") which clears those files' dirty bits ONLY on explicit confirm (git remains the atomicity layer; RESEARCH-SAFETY save sequence).
4. **Stale-draft detection**: `EDITOR.save.staleCheck(fingerprints)` — compares a loaded draft's stored fingerprints against freshly-loaded raw texts (hook point from 07.1-07's `onDraftLoaded` + a "re-check against folder" button after a re-pick): mismatch → loud warning "Data changed since this draft was saved — loading may clobber newer changes" with Load-anyway / Discard options (Pitfall S3).
5. **localStorage crash copy** (optional, non-load-bearing): guarded `storageAvailable` try/catch pattern; debounced 800 ms after last mutation stores the draftDoc; on boot, if a crash copy exists and no draft loaded, banner "draft from earlier session found — restore? / dismiss". Any storage failure silently swallowed. Nothing in load/save DEPENDS on it (user simplicity directive).
6. **Dirty recomputation**: undo/redo must re-derive dirty state (compare against originals kept by core) — expose `EDITOR.save.recomputeDirty()` used by core hooks.
7. Register + init + CSS. ES5 only. Header: ownership 07.1-13; cite the user directives + RESEARCH-UI Persistence + RESEARCH-SAFETY save sequence; note the FILE-SYSTEM-ACCESS-API IS BANNED (grep-guarded by tests).
  </action>
  <verify>Regenerate; emitted HTML contains `downloadText`, `draftDoc`, `story_editor_draft.json`, `tmp/`, `data/story_glucose/` + `rpg/data/` destination strings, `storageAvailable`, `~400`-ms batch comment, and the post-save gate commands including `story_editor_lint`</verify>
  <done>Persistence layer complete: universal downloads for draft + final, crash copy guarded, stale detection, gate reminders</done>
</task>

<task type="auto">
  <name>Task 2: Structural persistence tests</name>
  <files>tests/test_story_editor_save.py</files>
  <action>
Create `tests/test_story_editor_save.py`:
1. `test_no_fsa_api`: the asset contains NO `showSaveFilePicker` / `showOpenFilePicker` / `showDirectoryPicker` / `FileSystemHandle` (the binding user directive bans the API — mechanically pinned).
2. `test_house_style_only`: every file-body serialization calls `EDITOR.housy.stringify` (grep); NO `null, 2` JSON.stringify calls.
3. `test_destinations_displayed`: the destination map lists all four non-story targets + the story prefix; save rows render fname + dest.
4. `test_draft_doc_shape`: version/saved_at/bundle/dirty/undo_meta/fingerprints all present in draftDoc.
5. `test_gate_reminder`: the lint command + suite command strings appear verbatim.
6. `test_stale_check`: fingerprint comparison + loud-warning path present.
7. `test_local_storage_guarded`: storageAvailable try/catch pattern present; a comment states non-load-bearing.
8. ES5 discipline; full suite green.
  </action>
  <verify>`python3.6 -m unittest tests.test_story_editor_save -v` green; full suite green</verify>
  <done>E's save half structurally pinned to the universal no-API model; suite green</done>
</task>

</tasks>

<verification>
- Full suite green; emitted HTML contains the save asset
- No forbidden files touched
</verification>

<success_criteria>
Requirement E's persistence delivered per the binding directives: no-API universal downloads, tmp/ draft guidance, per-dirty-file final saves with displayed destinations, stale-draft protection, guarded crash copy, post-save gate reminder.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-13-SUMMARY.md`
</output>
