---
phase: 07.1-story-framework-node-editing-html-interface
plan: 18
type: execute
wave: 5
depends_on: ["07.1-04", "07.1-07", "07.1-09", "07.1-10", "07.1-12", "07.1-13"]
files_modified:
  - tools/story_editor_assets/90_boot.js
  - tests/test_story_editor_boot.py
autonomous: true
must_haves:
  truths:
    - "The status bar shows load state, dirty-file count, undo depth, validation summary, and selection at all times"
    - "The Diagnostics tab runs the serializer self-test and the full validator on demand and shows the inlined asset manifest"
    - "The Help tab documents the complete workflow: Firefox save-setting recommendation, folder layout, draft/tmp flow, save-edit destinations, post-save gates, B9 Phase-12 note, and the B11 extensibility posture"
  artifacts:
    - path: "tools/story_editor_assets/90_boot.js"
      provides: "Status bar renderer + Diagnostics panel (self-test, validator report, asset manifest) + Help tab content"
      min_lines: 130
    - path: "tests/test_story_editor_boot.py"
      provides: "Structural battery: status bar fields, diagnostics entry points, help content coverage"
  key_links:
    - from: "90_boot.js diagnostics"
      to: "EDITOR.selfTestSerializer + EDITOR.validateCurrent"
      via: "on-demand panels running the embedded self-test and validator"
      pattern: "selfTestSerializer|validateCurrent"
    - from: "90_boot.js help"
      to: "the user directives"
      via: "Firefox 'Always ask you where to save files' + tmp/ draft flow + repo-relative save destinations documented"
      pattern: "Always ask you where to save"
---

<objective>
Build the editor's chrome: the always-visible status bar, the Diagnostics tab (serializer self-test + validator report + asset manifest — the in-page verification surface), and the Help tab (the human's operating manual for the whole tool).

Purpose: B11's extensibility posture and the B9 reminder live in the help/diagnostics surface; the diagnostics tab gives the human (and the final checkpoint) a one-click health report without DevTools.
Output: tools/story_editor_assets/90_boot.js + structural tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-RESEARCH-UI.md   # persistence guidance (Firefox setting) + Pitfall 6 checkpoints
@.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-RESEARCH-SAFETY.md # post-save gate checklist
@tools/story_editor_assets/00_core.js
@tools/story_editor_assets/05_json.js
@tools/story_editor_assets/20_validate.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 90_boot.js — status bar + diagnostics + help</name>
  <files>tools/story_editor_assets/90_boot.js</files>
  <action>
Write the chrome asset (loads LAST; owns `#statusbar`, `#tab-diagnostics`, `#tab-help`). IIFE:

1. **Status bar** (rerender hook): `loaded: <n nodes / n files | not loaded>` · `dirty: <n files>` (click → Save tab) · `undo: <depth>` · `validate: <n errors / n notices>` (click → Diagnostics) · `selected: <id>` · a B9 chip when an ending is selected ("CG → Phase 12").
2. **Diagnostics tab**: three panels — (a) Serializer self-test: button running `EDITOR.selfTestSerializer()`, rendering the pass/fail table; auto-runs once on first open. (b) Validator report: button running `EDITOR.validateCurrent()` (or reusing the last hook result) rendering errors/notices/count-shifts with rule ids + details; (c) Build info: the `__ASSET_MANIFEST__` comment content is parsed at boot (the generator's inline manifest) and rendered — asset list + byte sizes + editor version; plus the data-fingerprint list (per-file FNV-1a) for support/debug.
3. **Help tab** (static content, thorough):
   - What this tool is (authoring editor for data/story_glucose + rpg/data manifests; committed but never bundled — teachers may use it; not runtime game code).
   - Loading: the folder layout tree (story_editor.html at repo root; data/ + rpg/data/ below it); why file:// fetch is blocked (one honest paragraph, CVE reference) and that the folder pick is the normal path; the draft-reload flow.
   - Saving: the Firefox one-time setting recommendation — Settings → General → Files → **"Always ask you where to save files"** — then draft → tmp/story_editor_draft.json; save-edit → per-file rows showing exact destinations; the "filename is a suggestion" caveat; AFTER SAVING run: `python3.6 tools/story_editor_lint.py`, `python3.6 -m unittest discover -s tests`, `python3.6 tools/check_citations.py --story data/story_glucose --registry data/citations.json`, `python3.6 tools/check_edit_coverage.py`, then `git diff` review (the editor cannot see the filesystem — the Python gates are authoritative).
   - Editing semantics: structural vs text halves (which edits are risky); the derived-kind model (no node_type field — B2 transitions are recipes); the pinned-count acknowledgment; undo semantics (one step per logical action, resets on draft reload); unknown keys/ops round-trip (B11: new node types — the editor preserves what it does not understand; add new ops via paste with warn-but-accept).
   - B9 note: ending CG is Phase-12 territory; the editor shows a reminder only.
   - Offline statement: zero servers, zero frameworks, zero network requests.
4. Register + init + CSS. ES5 only; header: ownership 07.1-18.
  </action>
  <verify>Regenerate; emitted HTML contains the status-bar field set, `selfTestSerializer` diagnostics wiring, `validateCurrent` wiring, the "Always ask you where to save files" string, the lint/suite commands, the B9 note, and the B11/extensibility paragraph</verify>
  <done>Chrome complete: status bar, diagnostics (self-test + validator + manifest), comprehensive help</done>
</task>

<task type="auto">
  <name>Task 2: Structural chrome tests</name>
  <files>tests/test_story_editor_boot.py</files>
  <action>
Create `tests/test_story_editor_boot.py`:
1. `test_status_bar_fields`: dirty/undo/validate/selected indicators present.
2. `test_diagnostics_entry_points`: selfTestSerializer + validateCurrent invoked from the diagnostics tab; asset manifest rendered.
3. `test_help_completeness`: the help tab contains (verbatim greps): "Always ask you where to save files", "tmp/", "story_editor_lint", "unittest discover", "check_citations", "Phase-12"/"Phase 12", and an offline statement.
4. `test_b11_posture`: the unknown-keys/ops round-trip paragraph exists ("preserve"/"round-trip"/"warn" language).
5. ES5 discipline; full suite green.
  </action>
  <verify>`python3.6 -m unittest tests.test_story_editor_boot -v` green; full suite green</verify>
  <done>Chrome structurally pinned; suite green</done>
</task>

</tasks>

<verification>
- Full suite green; emitted HTML contains the chrome asset (last inlined)
- No forbidden files touched
</verification>

<success_criteria>
The editor ships with self-explaining chrome: live status, one-click diagnostics (self-test + validator), and a help tab that encodes the whole operating procedure including the Firefox save setting and post-save gates.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-18-SUMMARY.md`
</output>
