---
phase: 07.1-story-framework-node-editing-html-interface
plan: 19
type: execute
wave: 6
depends_on: ["07.1-17", "07.1-18"]
files_modified:
  - tests/test_story_editor_integration.py
  - .planning/STATE.md
autonomous: false
user_setup: []
must_haves:
  truths:
    - "The full emitted editor passes the cross-asset integration battery (all assets inlined in order, every capability entry point present, offline-clean, no token leaks)"
    - "A human completes the full edit → undo → draft → reload → save-edit → place-files → gates-green round trip in Firefox"
    - "The B9 (ending CG = Phase 12) obligation is tracked in STATE.md Pending Todos"
  artifacts:
    - path: "tests/test_story_editor_integration.py"
      provides: "Capstone structural battery: asset order, capability matrix over the emitted HTML, offline checks, suite-level gates"
    - path: ".planning/STATE.md"
      provides: "One new Pending-Todos entry tracking the B9 Phase-12 reminder (ONLY this plan touches STATE.md)"
  key_links:
    - from: "tests/test_story_editor_integration.py"
      to: "all story_editor assets"
      via: "capability matrix asserting every B1-B11/C/D/E entry point is present in the emitted HTML"
      pattern: "EDITOR\\.(apply|housy|stringify|validate|select|view)"
    - from: "07.1-19"
      to: ".planning/STATE.md ### Pending Todos"
      via: "the B9 reminder entry (research-prescribed wording)"
      pattern: "Pending Todos"
---

<objective>
Capstone: the cross-asset integration battery, the STATE.md B9 tracking entry, and the FINAL blocking human-verify — the complete Firefox round trip (edit → undo → draft download → draft reload → save-edit downloads → place files → Python gates green) including the diff-hygiene check (no-op save byte-identical).

Purpose: this is the phase's verification gate — automated evidence where WSL can automate (integration structure + gates) and the human verdict where only a browser can decide (RESEARCH-SAFETY's 8-step script, adapted to the universal download model).
Output: integration tests + STATE.md entry + recorded final verdict.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-RESEARCH-SAFETY.md   # the 8-step human-verify script + gates battery
@.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-RESEARCH-UI.md     # Pitfall 6 checkpoint list (1)-(4)
@.planning/STATE.md    # Pending Todos format (line ~324) — read-only for all plans EXCEPT this one
@story_editor.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration battery + STATE.md B9 entry</name>
  <files>tests/test_story_editor_integration.py, .planning/STATE.md</files>
  <action>
**Part A — tests/test_story_editor_integration.py** (run the generator once in setUpClass to a temp path):
1. `test_asset_order_complete`: the emitted HTML inlines ALL expected assets in sorted order (00_core, 05_json, 10_load, 20_validate, 30_graph, 40_form, 42_choices, 43_onenter, 45_lifecycle, 50_editscast, 60_trace, 70_claims, 80_save, 90_boot) — assert each marker/function signature.
2. `test_capability_matrix`: every capability entry point present — B1/B2 lifecycle (add/delete/recipe markers), B3/B5 choices (goto dropdown), B4/B7/B10 data tab (bad_ending_pool/cast editors), B6 form (text fields), B8 paste (parseScenePaste), C claims (reverse index), D trace (cycle guard), E save (downloadText/draftDoc) + undo (EDITOR.apply/undo/redo) — one assertion per B-item id in a table comment for traceability.
3. `test_offline_clean`: no external src/href; no module scripts; no fetch calls EXCEPT the documented probe (count ≤ 1 fetch site, in 10_load).
4. `test_no_token_leaks` + `test_serializer_vectors_embedded` (all fixture expected literals present) + `test_validator_rule_parity` (all lint rule ids present).
5. `test_lint_green_and_generator_green`: subprocess `python3.6 tools/story_editor_lint.py` → exit 0; generator re-run → exit 0. (The suite-level gate pairing.)

**Part B — STATE.md entry** (this plan EXCLUSIVELY owns .planning/STATE.md; no other 7.1 plan touches it): append ONE bullet to `### Pending Todos` following the exact house format (`- [ ] **Title (source/date):** details`), e.g.:
`- [ ] **B9 (Phase 7.1 editor) — ending CG edit capability deferred to Phase 12 (roadmap insertion 2026-09-04):** the 7.1 HTML editor ships B9 as a reminder (inline banner on ending nodes + this tracking entry), not a feature; when Phase 12 (Ending Cutscene Rendering) is planned, extend the editor's ending-node view to add/delete/update CG assets.`
Do NOT modify any other part of STATE.md.
  </action>
  <verify>`python3.6 -m unittest tests.test_story_editor_integration -v` green; full suite green; `grep -c "B9 (Phase 7.1 editor)" .planning/STATE.md` → 1</verify>
  <done>Integration battery green; STATE.md carries exactly one B9 tracking entry</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: FINAL round-trip verification in Firefox</name>
  <what-built>
The complete editor (all 14 assets) with every B1-B11/C/D/E capability, house-style saving, draft flow, and the Python gate pairing.
  </what-built>
  <how-to-verify>
Prerequisite: `python3.6 tools/story_editor.py` freshly run; repo worktree otherwise clean (`git status`).

1. **Load**: open story_editor.html in Firefox → folder-pick the repo → checklist all ✓ → graph renders 57 nodes. Diagnostics tab: serializer self-test N/N PASS; validator report shows the sanctioned residual only (0 errors).
2. **Safe text edit round trip**: select intro.shell_glucose; append " (edited)" to its story text; Ctrl+Z / Ctrl+Y to verify undo/redo; leave it edited.
3. **Draft**: Save tab → Save draft → Firefox saves `story_editor_draft.json` — RECORD the actual filename Firefox used (the suggested-name behavior is UNVERIFIED — this closes research OQ 4) — move it into the repo's `tmp/` folder. `git status --short` must show NOTHING from tmp/ (git-ignored).
4. **Draft reload**: reload the page (F5), re-pick the folder, then Load draft → pick tmp/story_editor_draft.json → the edit from step 2 is back; undo history is fresh (expected — documented); make a second small edit.
5. **No-op save byte-identity (the diff-hygiene gate)**: reload fresh, load the folder, make NO edits, Save edit → download intro.json → in a terminal `diff` the downloaded file against `data/story_glucose/intro.json` → MUST be byte-identical (house-style serializer proof). RECORD the result.
6. **Real save**: with the one small edit, Save edit → download ONLY the dirty file (intro.json) → place it over `data/story_glucose/intro.json` (via the save dialog directly, if "Always ask you where to save files" is enabled — enable it once if not: Settings → General → Files). Mark saved in the panel.
7. **Gates (WSL)**: run exactly — `python3.6 tools/story_editor_lint.py` (expect exit 0), `python3.6 -m unittest discover -s tests` (expect all OK), `python3.6 tools/check_citations.py --story data/story_glucose --registry data/citations.json` (expect the sanctioned exit-1 residual: 2 MISSING fa.stub/alc.stub, 0 UNAPPROVED), `python3.6 tools/check_edit_coverage.py` (exit 0), `git diff --stat` (ONLY data/story_glucose/intro.json) and `git diff` (the one intended line, NO reflow).
8. **Topology edit proof (optional but recommended)**: add a test node via ＋ node (accept the count-shift acknowledgment), connect it with one goto, save BOTH dirty files, run the lint (expect exit 0 + count_shift notice) and the suite (expect the pinned-count tests red — the DESIGNED signal), then `git checkout -- data/` to revert. This proves the acknowledgment path end-to-end.
9. **Offline check**: Network panel over the whole session: zero external requests (beyond the one probe).
Verdict: report "approved" with the recorded filename behavior + diff results, or the defect list.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the defects found</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record final verdict + close-out</name>
  <files>.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-19-SUMMARY.md</files>
  <action>
Write the SUMMARY: per-step verdict table (1-9), the recorded Firefox download-filename behavior (closes RESEARCH-UI OQ 4 — update the note in the phase dir if it contradicts the assumption), the byte-identity result, the gates outputs, and any fix-forward triage (fixes touch ONLY the owning asset file). If the human requested changes that exceed small fixes, list them as follow-up gaps for replanning. Revert any leftover test data (step 8 checkout) and confirm `git status` is clean of unintended files.
  </action>
  <verify>SUMMARY complete with verdicts + recorded findings; suite green; worktree clean of unintended changes</verify>
  <done>Phase 7.1 verified end-to-end; final verdict recorded</done>
</task>

</tasks>

<verification>
- `python3.6 -m unittest discover -s tests` green (all 7.1 batteries + the 345)
- `python3.6 tools/story_editor_lint.py` exit 0 on the live repo
- STATE.md carries exactly one B9 entry; no other STATE.md changes
- Human round-trip verdict recorded
</verification>

<success_criteria>
The emitted editor passes the full integration battery; the human completed the edit→draft→save→gates round trip with byte-identical no-op proof; the B9 Phase-12 obligation is tracked; the phase closes with a recorded verdict.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-story-framework-node-editing-html-interface/07.1-19-SUMMARY.md`
</output>
