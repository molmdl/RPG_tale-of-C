---
phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts
plan: 15
type: execute
wave: 2
depends_on: ["05.1-12", "05.1-13"]
files_modified:
  - .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md
autonomous: true

must_haves:
  truths:
    - "05.1-DESIGN.md reflects the 54-node / 20-ending (1T+3G+2N+14B) / intro.preface-start skeleton (was 43 / 11 / intro.select)"
    - "§1 Overview + node inventory lists the 11 new nodes (9 bad endings + 2 preface) + documents the bad-ending expansion (Req 1: 1a randomize pool + 1b known-consequence) + the preface sequence (Req 2: Beat A + Beat B)"
    - "§3 File split table: bad_endings.json 5 -> 14 entries (13 bad endings + edit.prompt); intro.json 3 -> 5 entries (+intro.preface +intro.shell_glucose); total 43 -> 54"
    - "§4 Choice-point contract: 2 new rows (intro.preface + intro.shell_glucose, MC-choice, 2 choices each) + the edit.prompt stub row updated (3 -> 12 structural choices)"
    - "§5 Edit-node contract: the edit.prompt stub description updated (12 structural choices to 12 bad endings: 3 existing-reachable + 9 new); the 1a edit:unknown / 1b edit:known distinction documented; the 1b deterministic-binding-is-Phase-7 boundary documented"
    - "§6 SC5 table: 11 new rows (9 bad endings + 2 preface) + the SC5 count updated (MC-choice, both, edit-allowed, neither); §7 reachability chains + count 11 -> 20; §8 no-impact note; §9 new OQs for the preface + bad-ending decisions"
  artifacts:
    - path: ".planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md"
      provides: "the authoritative companion design doc updated for the 54-node bad-ending + preface expansion skeleton"
      contains: "intro.preface"
  key_links:
    - from: "05.1-DESIGN.md §6 SC5 table"
      to: "the 11 new skeleton nodes (9 bad endings + 2 preface)"
      via: "one row per node with structural types + SC5 class + claim_ids"
      pattern: "intro\\.preface|bad\\.enzyme_collapse"
---

<objective>
Update 05.1-DESIGN.md (the authoritative companion design doc) for the post-expansion skeleton: the bad-ending pool grew (11 -> 20 endings; 1T+3G+2N+5B -> 1T+3G+2N+14B), the preface sequence shifted the start (intro.select -> intro.preface) + added 2 nodes, and the node count grew (43 -> 54). This is the human-reviewable cross-reference for the 2 new structural requirements; the skeleton JSON stays clean (no inline comments), this doc carries the commentary.

Purpose: The 05.1-06 checkpoint reviewer uses this doc to review the expanded skeleton. The SC5 table (§6) is the per-node classification; the SC3 (§4) + SC4 (§5) tables are the integration contracts; §7 is the reachability proof; §9 surfaces the new open questions for the re-review. The decisions baked into Plans 12+13 (shared bad-ending pool, 1a/1b tag distinction, per-character shell node, gly.start hide-20-AA approach, 0-single-Continue kept via Observe 2nd choice) are documented here as locked decisions + flagged OQs for Phase 7.

Output: 1 modified file (05.1-DESIGN.md) with updated §1/§3/§4/§5/§6/§7/§8/§9. The doc stays the authoritative companion (the skeleton JSON is the source of truth for structure; this doc is the human-reviewable cross-reference).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-CHECKPOINT-FEEDBACK.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-12-SUMMARY.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-13-SUMMARY.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md
@data/story_glucose/bad_endings.json
@data/story_glucose/intro.json
@data/story_glucose/manifest.json
@data/story_glucose/glycolysis.json
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update §1 Overview + §3 File split + §4 Choice-point contract + §5 Edit-node contract + §8 Backward-compat</name>
  <files>.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md</files>
  <action>
Edit 05.1-DESIGN.md. The skeleton grew from 43 -> 54 nodes + the ending distribution grew on the Bad side (1T+3G+2N+5B -> 1T+3G+2N+14B) + the start node shifted (intro.select -> intro.preface) per Plans 05.1-12 (bad-ending expansion) + 05.1-13 (preface sequence). Update the structural-contract sections; leave the SC5 table (§6) + reachability (§7) + open questions (§9) for Task 2.

1. §1 Overview — "Skeleton location + the 43-node inventory" subsection:
   - Update the heading + prose: "43-node inventory" -> "54-node inventory". Add a paragraph after the tiered-completeness expansion blockquote documenting the 05.1-12/13 expansion: "Phase 5.1 bad-ending + preface expansion (Plans 05.1-12/13): the skeleton grew from 43 -> 54 nodes per the 05.1-06 checkpoint feedback's 2 new structural requirements. Req 1 (CG-collection bad endings): +9 bad-ending nodes (6 unknown-pool 1a + 3 known-consequence 1b) in bad_endings.json; the RngEngine-weighted unknown-edit pool grew 2 -> 8 (variety via node count, not new routing logic); 1b adds 3 deterministic known-consequence bad endings (tagged edit:known, distinct from 1a's edit:unknown). Req 2 (preface sequence): +2 preface nodes (intro.preface Beat A before character select + intro.shell_glucose Beat B before the journey) in intro.json; the manifest start shifted intro.select -> intro.preface; gly.start on_enter now hides the 20 AA + re-shows glucose. Ending distribution grew on the Bad side: 1T+3G+2N+5B -> 1T+3G+2N+14B. The 14 edit-allowed count + the 0 single-Continue invariant are UNCHANGED."
   - Update the node-inventory table: add 11 new rows (9 bad endings #44-52 + 2 preface nodes; insert intro.preface as #1 before intro.select, shifting intro.select to #2, fa.stub #3, alc.stub #4, then glycolysis etc. keep their numbers OR renumber — keep it simple: append the 9 new bad endings as #44-52 after edit.prompt #43, and note intro.preface + intro.shell_glucose inserted in intro.json; the table can list them in the intro.json + bad_endings.json rows with "+N" markers like the expansion did). Use the same "+(NEW)" marker convention as the tiered-completeness expansion.
   - Update the "3 stubs" note: now "5 stubs" (fa.stub + alc.stub + edit.prompt + intro.preface placeholder-shell + intro.shell_glucose placeholder-shell — actually only edit.prompt is a structural stub; the preface nodes are real narrative beats. Clarify: "3 stubs (fa.stub + alc.stub Phase 8 placeholders + edit.prompt structural stub) + 2 preface nodes (intro.preface + intro.shell_glucose — real narrative beats with placeholder on_enter MolActions, NOT stubs)".)
   - Update the ending-distribution sentence: "1T + 3G + 2N + 5B = 11 endings" -> "1T + 3G + 2N + 14B = 20 endings (the Bad tier grew 5 -> 14 via the 05.1-12 bad-ending expansion)".

2. §3 File split table:
   - intro.json row: "3" -> "5 (intro.preface + intro.select + intro.shell_glucose + fa.stub + alc.stub)"; rationale: "Character-select screen + the preface sequence (Beat A hero's blessing + Beat B glucose shell); Phase 8 stubs"
   - bad_endings.json row: "5" -> "14 (13 bad endings + edit.prompt structural stub)"; rationale: "Bad-ending pool (4 original + 9 new from Req 1: 6 unknown-pool 1a + 3 known-consequence 1b) + edit.prompt structural stub (12 structural choices to 12 bad endings)"
   - glycolysis.json row: note gly.start on_enter updated (hide_all + show_as glucose — the 20-AA hide after the shell beat).
   - Update "Total: 43 nodes across 7 files." -> "Total: 54 nodes across 7 files (was 43 before the bad-ending + preface expansion)."
   - manifest.json conventions block: update the example "start": "intro.select" -> "start": "intro.preface" + add a note that the preface Beat A is the entry point after Plan 05.1-13.

3. §4 Choice-point contract — "Choice nodes (MC-choice)" table:
   - Add a row for intro.preface: structural types "choice (2 non-weighted: Continue + Observe)"; (a) "Continue to character selection"; "Observe the hero and the cast"; (b) both -> intro.select; (c) effects {} then intro.select.on_enter (hide_all); (d) none; (e) none — player picks by index; pathway branch point: "Story preface — the hero's blessing (Req 2 Beat A; NOT a pathway branch — a narrative beat before character select)"; SC5 class MC-choice.
   - Add a row for intro.shell_glucose: structural types "choice (2 non-weighted: Continue + Observe)"; (a) "Begin the journey — enter glycolysis"; "Observe your new form"; (b) both -> gly.start; (c) effects {} then gly.start.on_enter (hide_all + show_as glucose); (d) none; (e) none; pathway branch point: "Shell beat — the hero dons the glucose molecule (Req 2 Beat B; per-character — FA/Alc get their own shell nodes in Phase 8)"; SC5 class MC-choice.
   - Update the edit.prompt row: (a) now 12 structural choices (3 existing + 6 1a edit:unknown + 3 1b edit:known); (b) the 12 goto targets (list them or summarize: 3 existing bad endings + 9 new bad endings); note "12 STRUCTURAL placeholder choices (at runtime the EditRouter routes via apply_player_edit, BYPASSING these; the stub exists so the BFS sees all 12 bad endings reachable)".

4. §5 Edit-node contract — "The edit.prompt stub" subsection:
   - Update the stub description: the stub now has 12 choice.goto edges to 12 bad endings (3 existing: bad.critical_residue_break [edit:known_critical], bad.lost_connection [edit:unknown], bad.released_from_host [edit:unknown] + 6 new 1a [edit:unknown]: bad.enzyme_collapse, bad.wrong_substrate_trapped, bad.broken_active_site, bad.lost_in_cytosol, bad.proton_leak, bad.misfolded_aggregate + 3 new 1b [edit:known]: bad.active_site_destroyed, bad.substrate_channel_blocked, bad.cofactor_lost).
   - Add a paragraph documenting the 1a/1b distinction: "1a (unknown-pool, edit:unknown): the RngEngine-weighted pool the EditRouter picks from for unknown edits (variety, not punishment — the CG-collection feel). 1b (known-consequence, edit:known): deterministic outcomes of specific known edits, distinct from the random pool. The BINDING of a specific known edit to a specific 1b bad ending is Phase 7 edits.json content (the skeleton only carries the NODES + structural reachability via edit.prompt)."
   - Add a paragraph documenting the shared-pool decision: "Shared pool (NOT per-enzyme) for the skeleton — the variety comes from node count (8 unknown + 3 known), not new routing logic. Per-enzyme bad-ending pools are Phase 7 content (the Phase 4 EditRouter OVERRIDE semantics already support per-enzyme pools; Phase 7 may populate them for richer CG-collection feel)."
   - The edits.json location subsection: note the bad_ending_pool placeholder in c14/data/edits.json currently lists ["bad.lost_connection", "bad.released_from_host"] — Phase 7 expands this to include the 6 new 1a pool ids (the 1b ids are NOT in the random pool; they are routed deterministically via edits.json enzyme entries). NO edits.json change in this phase (the placeholder is tooling-bound; Phase 7 populates the real entries).

5. §8 Backward-compat + gate impact: add a note that the bad-ending + preface expansion is purely JSON authoring + design-doc (ZERO code changes to engine/model/gates/tools, same as the tiered-completeness expansion); the 198 prior tests pass unchanged; the 8 glucose reachability tests are updated in Plan 05.1-14 (count assertions 43->54, 11->20, start intro.select->intro.preface + 1 new bad-ending-reachability invariant). The new preface nodes use existing MolOps ops (hide_all, load, show_as) — NO new ops invented. The pdb:TBD_* placeholders follow the TBD_ACONITASE precedent (loud-fail at runtime until Phase 6/7).

Preserve all existing prose that is still accurate (the LOCKED Phase 5 decisions in §1, the SC1 taxonomy in §2, the choice-point contract facets in §4, the edit-node 5-step data-flow in §5). Do NOT re-litigate locked decisions. Keep markdown formatting consistent with the existing doc.
  </action>
  <verify>
1. grep -c "intro.preface" .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md (expect >= 6 mentions: §1 inventory + §3 file split + §4 choice table + §5 stub + §8 note + the manifest example)
2. grep -c "bad.enzyme_collapse" .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md (expect >= 3: §1 inventory + §5 stub + §6 table [Task 2 adds the §6 row])
3. grep "54 nodes" .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md (expect the updated total)
4. grep "1T + 3G + 2N + 14B" .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md (expect the updated ending distribution)
5. python3.6 -c "import markdown 2>/dev/null or None" 2>/dev/null; python3.6 -c "open('.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md').read()"  (file is readable + valid text)
  </verify>
  <done>
05.1-DESIGN.md §1/§3/§4/§5/§8 updated: 54-node inventory, 1T+3G+2N+14B distribution, intro.preface start, the 11 new nodes documented, the 1a/1b + shared-pool decisions documented, the edit.prompt 12-choice stub updated, the 2 new preface MC-choice rows added, the backward-compat no-impact note added.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update §6 SC5 table (11 new rows + counts) + §7 Reachability (chains + count) + §9 new OQs</name>
  <files>.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md</files>
  <action>
Edit 05.1-DESIGN.md (continuing from Task 1). Update the per-node classification table (§6), the reachability proof (§7), and add the new open questions (§9) for the bad-ending + preface expansion.

1. §6 SC5 table — add 11 new rows. The table has columns: # | node_id | file | structural types (derived) | MC-choice? | edit-allowed? | SC5 class | enzyme_id | pathway branch point mapped | ending tier | claim_ids. Add (renumber as needed; the existing table is 43 rows, add 11 = 54):

   intro.preface (intro.json): structural types "choice (2 choices: Continue + Observe)"; MC-choice yes (preface Beat A); edit-allowed no; SC5 class MC-choice; enzyme_id —; pathway branch "Story preface — the hero's blessing (Req 2 Beat A; NOT a pathway branch)"; ending tier —; claim_ids PLACEHOLDER_PHASE7.
   intro.shell_glucose (intro.json): structural types "choice (2 choices: Continue + Observe)"; MC-choice yes (preface Beat B); edit-allowed no; SC5 class MC-choice; enzyme_id —; pathway branch "Shell beat — hero dons glucose (Req 2 Beat B; per-character, FA/Alc Phase 8)"; ending tier —; claim_ids PLACEHOLDER_PHASE7.

   9 bad endings (bad_endings.json) — all: structural types "ending (choices=[], is_ending=bad)"; MC-choice no; edit-allowed no; SC5 class neither; enzyme_id —; ending tier Bad; claim_ids PLACEHOLDER_PHASE7_BAD. The pathway branch point column distinguishes 1a vs 1b:
   - bad.enzyme_collapse: "1a unknown-edit pool (RngEngine-weighted; edit misfolded the enzyme)"
   - bad.wrong_substrate_trapped: "1a unknown-edit pool (foreign substrate jammed in active site)"
   - bad.broken_active_site: "1a unknown-edit pool (catalytic residues scattered)"
   - bad.lost_in_cytosol: "1a unknown-edit pool (no enzyme binds; drift free)"
   - bad.proton_leak: "1a unknown-edit pool (proton gradient dissipates; ETC context)"
   - bad.misfolded_aggregate: "1a unknown-edit pool (enzyme aggregates; proteostasis collapse)"
   - bad.active_site_destroyed: "1b known-consequence (deterministic; specific known edit shreds active site — Phase 7 binding)"
   - bad.substrate_channel_blocked: "1b known-consequence (deterministic; specific known edit plugs channel — Phase 7 binding)"
   - bad.cofactor_lost: "1b known-consequence (deterministic; specific known edit loses cofactor — Phase 7 binding)"

   Update the SC5 count summary: "MC-choice = 18 (was 16; +2 preface); both = 9; edit-allowed = 5; neither = 22 (was 13; +9 bad endings). Total = 18 + 9 + 5 + 22 = 54." (the 9 new bad endings are "neither" — ending nodes with choices=[]; the 2 preface nodes are "MC-choice" — 2 choices each.) Update the "neither" list to include the 9 new bad-ending ids. Update the MC-choice list to include intro.preface + intro.shell_glucose.

   Update the ending-tier distribution verification: "1 True + 3 Good + 2 Normal + 14 Bad (was 5 Bad) = 20 endings (was 11). The Bad tier grew via the 05.1-12 expansion (6 1a unknown-pool + 3 1b known-consequence); True/Good/Normal UNCHANGED."

2. §7 Reachability validation:
   - Update the green-test code block: `assert len(rep.all_endings) == 11` -> `assert len(rep.all_endings) == 20  # 1T + 3G + 2N + 14B (Bad grew 5 -> 14 via 05.1-12)`. Update `start = "intro.select"` -> `start = "intro.preface"` (the preface is the new entry point).
   - Update the reachability chains table: add 9 rows for the new bad endings (all reachable via "... -> <any edit-allowed node> -> edit.prompt -> bad.<new>" — structural path through the stub, same pattern as the existing bad.lost_connection / bad.released_from_host / bad.critical_residue_break rows). Add a row note: "the edit.prompt stub now has 12 structural choices (3 existing + 9 new); all 12 bad endings are reachable via the stub. bad.cycle_trap_host_death is still reached via tca.shuffle (cond-gated), NOT via edit.prompt."
   - Update the True-ending reachability chain: prepend "intro.preface -> " before "intro.select -> gly.start" AND insert "intro.shell_glucose -> " between "intro.select" and "gly.start": "intro.preface -> intro.select -> intro.shell_glucose -> gly.start -> gly.g6p -> ... -> end.true". Same for the other chains (Good/Normal/Bad) that start from intro.select — prepend intro.preface + insert intro.shell_glucose for the glucose path. The anaerobic paths also start intro.preface -> intro.select -> ... (intro.shell_glucose is glucose-only; FA/Alc skip it — but FA/Alc are stubs returning to intro.select, so the anaerobic path from intro.select goes through pyr.branch, not through intro.shell_glucose; intro.shell_glucose -> gly.start is the glucose path). Clarify: the glucose path threads intro.shell_glucose; the FA/Alc paths skip it (Phase 8 will add their shell nodes).
   - Update the "CRITICAL: the aerobic-only-True-ending rule" note: the start is now intro.preface; the BFS from intro.preface still reaches end.true via the aerobic path (intro.preface -> intro.select -> intro.shell_glucose -> gly.start -> ... -> end.true). The anaerobic guard is still a runtime cond. NO structural change.
   - Update the SC2 red-test description: the red test rebuilds etc.atp_synthase + calls check_reachability(orphaned, g.start_node()) — g.start_node() is now intro.preface; the orphaned end.true is still unreachable from intro.preface. Still RED.
   - Update the "tests/test_glucose_reachability.py file" subsection: now 9 tests (was 8): the 5 original + 3 replan-invariant + 1 new bad-ending-reachability (Plan 05.1-14). Update the count + start assertions note.

3. §9 Open questions — add a new subsection "Bad-Ending + Preface Expansion Open Questions (Plans 05.1-12/13 — for the 05.1-06 re-review)" with these OQs:

   OQ-BAD-1. [LOW] Per-enzyme bad-ending pools — Phase 7 content. The skeleton uses a SHARED bad-ending pool (8 unknown + 3 known) for variety via node count. Per-enzyme pools (each enzyme's wrong edits lead to enzyme-thematically-relevant bad endings) are Phase 7 edits.json content (the Phase 4 EditRouter OVERRIDE semantics already support them). Flag: the human decides at Phase 7 whether to populate per-enzyme pools for richer CG-collection feel.

   OQ-BAD-2. [MEDIUM] 1b known-consequence binding — Phase 7 edits.json. The 3 1b bad endings (bad.active_site_destroyed, bad.substrate_channel_blocked, bad.cofactor_lost) are reachable via edit.prompt structural choices (BFS) but their DETERMINISTIC binding to specific known edits (which edit-allowed node + which reverse-mutation signature -> which 1b bad ending) is Phase 7 edits.json content. Flag: the human approves the edits.json entries that route specific known edits to specific 1b bad endings (Phase 7).

   OQ-BAD-3. [INFO] Bad-ending teaching-layer text — Phase 7. All 9 new bad endings carry PLACEHOLDER_PHASE7_BAD claim_ids + "TBD — Phase 7 teaching text." Per Phase 10 SC3 "bad-endings carry explanatory teaching-layer text (not punitive)." Flag: Phase 7 authors the non-punitive teaching text for each new bad ending.

   OQ-PREFACE-1. [LOW] Parametric vs per-character shell node — DECIDED (per-character). The skeleton uses intro.shell_glucose (per-character) for the glucose shell beat. A parametric single intro.shell node (loading via state.character) was considered but rejected for the skeleton (less duplication for glucose; the parametric MolAction logic is more complex). FA/Alc get their own shell nodes (intro.shell_fa / intro.shell_alc) in Phase 8. Flag: Phase 8 adds the FA/Alc shell nodes; the human confirms the per-character approach at the 05.1-06 re-review.

   OQ-PREFACE-2. [MEDIUM] 20-AA cartoon representation — Phase 5.2 scene-template. The preface on_enter uses show_as aa_cast cartoon (cartoon naturally hides terminal N-term/C-term groups). The exact cmd.* (selection strings, the 20-AA cast PDB, the cartoon settings) is a Phase 5.2 scene-template question. The skeleton carries the op name + target object placeholder (pdb:TBD_AA_CAST_20). Flag: Phase 5.2 designs the 20-AA cast representation convention; Phase 6 implements it.

   OQ-PREFACE-3. [LOW] gly.start hide-20-AA approach — DECIDED (hide_all + show_as glucose, NO journey_start node). gly.start on_enter is [hide_all, show_as glucose sticks] — hide_all hides the 20 AA cast (loaded in intro.preface, persisted through intro.shell_glucose), then show_as re-shows only the hero's glucose molecule. Decided NOT to add a dedicated intro.journey_start node (simpler; gly.start already does hide_all). Flag: the human confirms at the 05.1-06 re-review; Phase 6 may adjust the "glucose" object name + "sticks" rep.

   OQ-PREFACE-4. [MEDIUM] Hero-atom load placeholder — Phase 5.2/6. The preface on_enter loads pdb:TBD_HERO_CARBON (a single carbon; the hero). The Phase 5.2 hero-highlight convention (selection rule + color + label + persistence) is TBD. The skeleton uses pdb:TBD_* (loud-fail at runtime until Phase 6/7 resolves — NO fabricated PDB ID). Flag: Phase 5.2 designs the hero-highlight convention; Phase 6/7 resolves the hero-atom load.

   OQ-PREFACE-5. [INFO] 0-single-Continue invariant — DECIDED (kept UNCHANGED via Observe 2nd choice). The 2 new preface nodes each get a Continue + an Observe (mc:observe) 2nd choice (same-destination, different-experience, per the Continue-to-MC pattern from Plans 07-09). The invariant is NOT re-scoped. Flag: the human confirms at the 05.1-06 re-review.

   Update the "Still-Pending Checkpoint Items" note (if present in §9 or cross-reference the 05.1-CHECKPOINT-FEEDBACK.md): the 3 CRITICAL disease-mutant decisions + the 24 OQs are STILL pending the 05.1-06 re-review (Plans 12-13 implement ONLY the 2 new requirements; they do NOT resolve the pending items).

4. Update the footer: append "updated 2026-08-22 for the bad-ending + preface expansion (Plans 05.1-12/13: 43 -> 54 nodes, 11 -> 20 endings [1T+3G+2N+14B], start intro.select -> intro.preface, +9 bad-ending nodes [Req 1: 6 1a unknown-pool + 3 1b known-consequence], +2 preface nodes [Req 2: Beat A intro.preface + Beat B intro.shell_glucose], gly.start on_enter hide-20-AA)."

Preserve all still-accurate prose (the SC1 taxonomy §2, the choice-point contract facets §4, the edit-node 5-step data-flow §5, the existing OQ1-13 + OQ-DM1-DM11). Do NOT re-litigate locked decisions. Keep markdown formatting consistent.
  </action>
  <verify>
1. grep -c "bad.active_site_destroyed\|bad.substrate_channel_blocked\|bad.cofactor_lost" .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md (expect >= 6: §1 inventory + §5 stub + §6 table 3 rows + §7 chain + §9 OQ-BAD-2)
2. grep "OQ-PREFACE\|OQ-BAD" .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md (expect the 8 new OQs: OQ-BAD-1/2/3 + OQ-PREFACE-1/2/3/4/5)
3. grep "18 + 9 + 5 + 22 = 54\|MC-choice = 18" .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md (expect the updated SC5 count)
4. grep "intro.preface -> intro.select -> intro.shell_glucose -> gly.start" .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md (expect the updated True-ending reachability chain)
5. python3.6 -c "open('.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md').read()"  (file readable)
  </verify>
  <done>
05.1-DESIGN.md §6 (54-row SC5 table with 11 new rows + updated counts), §7 (reachability chains + 20-ending count + intro.preface start), §9 (8 new OQs: OQ-BAD-1/2/3 + OQ-PREFACE-1/2/3/4/5) + footer updated. The doc is the authoritative companion for the 54-node expanded skeleton.
  </done>
</task>

</tasks>

<verification>
- grep -c "intro.preface" .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md (>= 8 mentions across §1/§3/§4/§5/§6/§7/§9)
- grep "54 nodes" .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md (the updated total in §1 + §3)
- grep "1T + 3G + 2N + 14B\|1T+3G+2N+14B" .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md (the updated ending distribution in §1 + §6 + §7)
- grep -c "OQ-PREFACE\|OQ-BAD" .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md (8 new OQ headers in §9)
- The doc reads as the authoritative companion for the 54-node skeleton (no stale 43/11/intro.select references in the sections this plan updated — the §1/§3/§4/§5/§6/§7/§8/§9 are consistent with the 54-node / 20-ending / intro.preface-start state).
</verification>

<success_criteria>
- §1: 54-node inventory, 1T+3G+2N+14B distribution, the bad-ending + preface expansion documented.
- §3: file split table updated (intro.json 5, bad_endings.json 14, total 54, manifest start intro.preface).
- §4: 2 new preface MC-choice rows + the edit.prompt 12-choice stub row.
- §5: edit.prompt stub 12-choice description + the 1a/1b + shared-pool decisions + the 1b-binding-is-Phase-7 boundary.
- §6: 54-row SC5 table (11 new rows) + updated SC5 counts (18 MC-choice / 9 both / 5 edit-allowed / 22 neither).
- §7: reachability chains updated (intro.preface -> intro.select -> intro.shell_glucose -> gly.start) + 20-ending count + intro.preface start.
- §8: no-impact note (JSON + doc only; existing MolOps ops; pdb:TBD_* placeholders).
- §9: 8 new OQs (OQ-BAD-1/2/3 + OQ-PREFACE-1/2/3/4/5) + the still-pending-checkpoint note.
- Footer updated with the 2026-08-22 expansion entry.
</success_criteria>

<output>
After completion, create .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-15-SUMMARY.md recording: the 8 updated sections (§1/§3/§4/§5/§6/§7/§8/§9), the 11 new SC5 table rows, the 8 new OQs, the updated counts (54 nodes, 20 endings, 18/9/5/22 SC5), the reachability chain updates, and the decisions documented (shared pool, 1a/1b tags, per-character shell, gly.start hide-20-AA, Observe 2nd choice, pdb:TBD_* placeholders).
</output>
