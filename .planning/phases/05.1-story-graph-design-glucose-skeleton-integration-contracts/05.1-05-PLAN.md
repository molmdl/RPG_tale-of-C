---
phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts
plan: 05
type: execute
wave: 2
depends_on: ["05.1-01", "05.1-02", "05.1-03"]
files_modified:
  - .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md
autonomous: true

must_haves:
  truths:
    - "The choice-point contract (SC3) is documented: for each choice/rng-shuffle node, the design doc specifies the player-facing options, the edge each selects, the effects+on_enter fired on traversal, the cond gating, the weight, the real pathway branch point mapped, and the SC5 class"
    - "The edit-node contract (SC4) is documented: for each edit-allowed node, the design doc specifies the enzyme_id binding (edit:enzyme:<id> tag), the EditIntents players can generate, the known-edit branch route, the unknown-edit bad-ending fallback, and the 5-step data-flow (player edit -> EditIntent -> EditRouter.route(enzyme_id) -> node-id -> engine._enter -> on_enter MolAction(op=edit))"
    - "Every node is explicitly classified as MC-choice / edit-allowed / both / neither (SC5) in a per-node table (one row per node, 34 rows)"
    - "The node-type taxonomy (SC1) is documented as a DERIVED property (computed from existing fields: is_ending / choices[].weight / len(choices)) + a tags declaration for edit-allowed (edit:enzyme:<id>) — zero schema change"
    - "The citation-gate coexistence approach is documented: the skeleton uses PLACEHOLDER_PHASE7 claim_ids; the gate is a pre-ship manually-invoked CLI tool, NOT run on data/story_glucose/ during Phase 5.1; Phase 7 replaces PLACEHOLDER_PHASE7 with approved claims"
    - "The open questions from the research are surfaced for human review (ethanolic host tension, Normal-ending framing, cycle-trap visit-cap N, cataplerotic-exit claims, ETC PDB candidates, char-setting mechanism, on_enter_divert dormancy, edits.json location)"
  artifacts:
    - path: ".planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md"
      provides: "the reviewable design artifact holding the choice-point contract (SC3) + edit-node contract (SC4) + per-node SC5 classification table (SC5) + node-type taxonomy (SC1) + reachability validation approach (SC2) + open questions"
      contains: "05.1-DESIGN"
  key_links:
    - from: "05.1-DESIGN.md (SC5 classification table)"
      to: "data/story_glucose/*.json (the 34 skeleton nodes)"
      via: "one row per node cross-referencing the actual node ids + their derived structural types + SC5 class"
      pattern: "intro\\.select|gly\\.pfk|tca\\.shuffle|end\\.true|bad\\.cycle_trap"
    - from: "05.1-DESIGN.md (edit-node contract)"
      to: "c14/edit_router.py (EditRouter.route) + c14/data/edits.json (the lookup table)"
      via: "the 5-step data-flow documentation (player edit -> EditIntent -> EditRouter.route(enzyme_id) -> node-id -> engine._enter)"
      pattern: "EditRouter|edit:enzyme|enzyme_id"
---

<objective>
Author the 05.1-DESIGN.md companion design doc — the REVIEWABLE artifact that makes the two gameplay-integration contracts (choice-point SC3 + edit-node SC4) and the per-node classification (SC5) explicit, cross-referencing the actual node ids in the skeleton JSON (authored in Plans 01-03). This is the doc the human reviews in Plan 06 against spec.md + approved sources.

Purpose: SC3, SC4, and SC5 all say "documented"/"explicitly classified" — a single reviewable design artifact is easier to human-review than three. The skeleton JSON stays clean (no inline comments); the design doc is the cross-reference. The doc also documents the node-type taxonomy (SC1 — derived property + tags declaration, zero schema change), the citation-gate coexistence approach, the reachability validation design (SC2), and the open questions for human review.
Output: .planning/phases/05.1-.../05.1-DESIGN.md (a planning/review artifact in the phase dir, NOT shipped content).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-RESEARCH-pathway-topology.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-RESEARCH-graph-contracts.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-01-SUMMARY.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-02-SUMMARY.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-03-SUMMARY.md
@c14/story/model.py
@c14/story/interpreter.py
@c14/engine.py
@c14/edit_router.py
@c14/story/validate.py
@data/story_glucose/intro.json
@data/story_glucose/glycolysis.json
@data/story_glucose/pyruvate_branch.json
@data/story_glucose/tca.json
@data/story_glucose/etc_atp.json
@data/story_glucose/endings.json
@data/story_glucose/bad_endings.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the design doc sections 1-5 (overview + node-type taxonomy SC1 + file split + choice-point contract SC3 + edit-node contract SC4)</name>
  <files>.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md</files>
  <action>
Create `05.1-DESIGN.md` in the phase directory (NOT in data/story_glucose/ — it's a planning/review artifact, not shipped content; per research Q7 recommendation). Write sections 1-5:

## 1. Overview
- Reference the two RESEARCH.md files (topology + graph-contracts) as the basis.
- State the LOCKED Phase 5 decisions (honor, do not re-litigate): soul-jump True ending (metamorphosis framing, PROJECT.md row 104); C14-decay DROPPED (cycle-trap-until-host-death is the sole timescale bad-ending, row 105); anaerobic = option (d) 3-ending branch (lactic=Good, ethanolic=Normal, crisis=Bad, row 97); TCA RNG 0.5/0.5 (approved TCA-RNG-WEIGHT-01); edit routing = lookup table + bad-ending fallback (row 77); ending distribution 1T+3G+2N+5B (row 107).
- State the skeleton location: data/story_glucose/ (a NEW directory; the existing data/story/ toy fixture stays untouched for the 198 engine/interpreter tests). The skeleton is STRUCTURE + PLACEHOLDER TEXT, not cited content.
- State the 34-node inventory: 31 story nodes + 3 stubs (fa.stub, alc.stub for Phase 8; edit.prompt for structural reachability of the edit-related bad endings).

## 2. Node-type taxonomy (SC1)
- Document the 5 SC1 types as a DERIVED property (computed from existing Node fields) + a tags declaration for edit-allowed. ZERO schema change (research Q1 Option c RECOMMENDED).
- Include the classifier helper (pure-Python, documentation reference — the interpreter already dispatches on these fields):
```python
def node_types(node):
    types = []
    if node.is_ending is not None: types.append("ending")
    if any(c.weight is not None for c in node.choices): types.append("rng-shuffle")
    if len(node.choices) > 0: types.append("choice")
    if any(t.startswith("edit:enzyme:") for t in node.tags): types.append("edit-allowed")
    if not types: types.append("story")
    return types
```
- Explain: the 5 types are a SET of roles, NOT a single enum (a node can be rng-shuffle + choice + edit-allowed at once — the ARCHITECTURE.md TCA shuffle node is the canonical example). A single-valued node_type field is REJECTED (breaks multi-role nodes).
- Explain: the structural type (SC1, mechanism) is ORTHOGONAL to the SC5 class (gameplay role). A rng-shuffle node (structural) is MC-choice (gameplay — the RNG is the "multiple-choice" agent).
- Document the on_enter_divert DORMANCY: on_enter_divert is defined in the model but NEVER referenced by interpreter.py or engine.py (verified via grep — only in model.py + test_model.py). Therefore every non-ending node MUST have >=1 choice.goto; on_enter_divert is NOT used for forward paths. Reserve on_enter_divert for a future engine extension (research OQ4/Q9).

## 3. File split + manifest
- Document the 7-file split (intro.json, glycolysis.json, pyruvate_branch.json, tca.json, etc_atp.json, endings.json, bad_endings.json) per ARCHITECTURE.md line 147 (per-pathway-file rationale: per-claim diffability + parallel authoring + manifest-based loading).
- Document the manifest.json conventions: version=1, default_seed=0, start="intro.select", files[] (all 7).
- Document the loader: StoryGraph.load(dir) reads manifest.json, merges files' nodes dicts, raises ValueError on duplicate node ids (c14/story/graph.py:76-80).
- Document the location split: story JSON at repo-root data/story_glucose/ (dev) + c14/data/story_glucose/ (runtime bundle, created at install time). edits.json/cast.json STAY at c14/data/ (tooling-bound — tools/check_edit_coverage.py:41-42 hardcodes it; ARCHITECTURE.md says data/ but the tooling-bound path wins — do NOT move without updating the tool).

## 4. Choice-point contract (SC3)
- Document the 5-facet spec per choice node (research Q2 table): (a) player-facing options (Choice.label); (b) the edge each selects (Choice.goto); (c) MolActions fired on traversal (choice.effects applied to GameState FIRST, THEN the target node's on_enter MolActions emitted); (d) how cond gates options (interpreter._cond evaluates against {flags,char,counters,visits} with no builtins, fail-safe False); (e) how weight makes an RNG branch (pick_choice: if any eligible has weight AND total>0 -> single RNG pick via rng.random()*total + cumulative sum).
- Key distinctions the contract MUST state: (1) choice.effects are STATE mutations, NOT MolActions; (2) order of operations: pick_choice -> apply_effects -> _enter(choice.goto) -> enter_node; (3) cond vs weight are orthogonal (cond filters eligibility; weight decides WHO picks among the eligible); (4) on_enter_divert is NOT followed — edges are choice.goto ONLY.
- Per-choice-node table (one row per choice/rng-shuffle node — there are 5: intro.select, pyr.branch, tca.shuffle, tca.divert_to_good, anaer.entry — PLUS the 5 edit-allowed nodes each have an edit:offer choice + a Continue choice = 10 rows for the edit-allowed nodes' choices). For EACH: node id, structural types, (a) options, (b) edges, (c) traversal fires, (d) cond, (e) weight, pathway branch point mapped, SC5 class. Use the ACTUAL node ids + labels from the skeleton JSON (read data/story_glucose/*.json to fill the table).

## 5. Edit-node contract (SC4)
- Document the 5-step data-flow (research Q3, verified against engine.py:135-161 + edit_router.py:122-149):
  1. Player triggers an edit in the UI (pick residue, choose mutation).
  2. Controller builds an EditIntent(op, target, args) — op in {point_mutation, substrate_edit, protonation_change}.
  3. Controller reads the CURRENT story node's enzyme_id binding (the edit:enzyme:<id> tag) and calls engine.apply_player_edit(intent, enzyme_id).
  4. engine.apply_player_edit: node_id = edit_router.route(intent, enzyme_id, rng) — KNOWN (exact signature match in enzymes[enzyme_id].edits) -> branch_node; UNKNOWN (no match / unknown enzyme) -> bad-ending pool node (per-enzyme override else global fallback; uniform rng.weighted_pick).
  5. The routed node's on_enter applies the edit: branch node on_enter has MolAction(op=edit, target=..., args={edit_type, sele, new_resn}); bad-ending pool nodes are endings (is_ending=bad).
- Document the 5 facets per edit-allowed node (research Q3 table): (a) node declares edit-allowed (edit:enzyme:<id> tag); (b) enzyme_id binding (the EditRouter lookup bucket); (c) EditIntents players can generate (op/target/args, must match edits.json signature after EditIntent.signature() normalization); (d) routing outcome (known -> branch_node; unknown -> bad-ending pool); (e) routed node applies the edit (on_enter MolAction(op=edit)).
- Document the enzyme_id binding mechanism: tags entry `edit:enzyme:<id>` (research Q3 Option 1 RECOMMENDED — zero schema change, backward-compatible, greppable). The controller extracts it: `enzyme_id = next((t.split(":", 2)[2] for t in node.tags if t.startswith("edit:enzyme:")), None)`. Flag the alternative (new Node.edit_enzyme_id field, 4-line additive change) as an open question (Q9 of research) but recommend the tag.
- Per-edit-allowed-node table (5 rows: gly.pfk, pyr.pdh, tca.citrate_synthase, tca.aconitase, etc.complex_i): node id, enzyme_id (the edit:enzyme:<id> value), real enzyme + PDB, EditIntents players can generate (placeholder — Phase 7 content), known-edit branch route (Phase 7), unknown-edit fallback (bad.lost_connection / bad.released_from_host — the Phase 4 EditRouter pool), SC5 class (both).
- Document the edit.prompt stub: the skeleton has ONE shared edit.prompt stub node (in bad_endings.json) with 3 choice.goto edges to the edit-related bad endings (bad.critical_residue_break, bad.lost_connection, bad.released_from_host) — these are STRUCTURAL edges for the BFS, NOT player-facing options. At runtime, the EditRouter routes directly (bypassing the stub's choices); the stub exists so check_reachability sees the bad endings as reachable. Phase 6+ implements the real edit UI (the controller calls apply_player_edit, which routes via the EditRouter).
- Document the edits.json location: c14/data/edits.json (tooling-bound; NOT data/edits.json as ARCHITECTURE.md says — the tooling path wins). The existing Phase 4 placeholder (bad_ending_pool: ["bad.lost_connection", "bad.released_from_host"]) is COMPATIBLE with the skeleton's bad-ending node ids — zero edits.json update needed for the skeleton. The enzyme entries (known-edit signatures + branch nodes) are Phase 7 content.
  </action>
  <verify>
```bash
python3.6 -c "
with open('.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md') as f:
    content = f.read()
# sections 1-5 exist
for section in ['## 1. Overview', '## 2. Node-type taxonomy', '## 3. File split', '## 4. Choice-point contract', '## 5. Edit-node contract']:
    assert section in content, 'missing section: ' + section
# SC3 per-choice-node table references actual node ids
for nid in ['intro.select', 'pyr.branch', 'tca.shuffle', 'tca.divert_to_good']:
    assert nid in content, 'choice-point contract missing node: ' + nid
# SC4 per-edit-node table references actual node ids + enzyme_id binding
for nid in ['gly.pfk', 'pyr.pdh', 'tca.citrate_synthase', 'tca.aconitase', 'etc.complex_i']:
    assert nid in content, 'edit-node contract missing node: ' + nid
assert 'edit:enzyme:' in content, 'edit-node contract missing the edit:enzyme tag convention'
assert 'EditRouter' in content, 'edit-node contract missing the EditRouter reference'
assert 'bad_ending_pool' in content or 'bad-ending pool' in content, 'edit-node contract missing the bad-ending fallback'
# locked decisions honored (not re-litigated)
for term in ['metamorphosis', 'soul-jump', 'cycle-trap', 'PLACEHOLDER_PHASE7']:
    assert term in content, 'missing locked-decision term: ' + term
print('OK: 05.1-DESIGN.md sections 1-5 present; SC3 + SC4 tables reference actual node ids; locked decisions honored')
"
```
  </verify>
  <done>
05.1-DESIGN.md sections 1-5 exist: overview (locked decisions + skeleton location + 34-node inventory), node-type taxonomy (SC1 derived + tags for edit-allowed + on_enter_divert dormancy), file split (7 files + manifest + location split), choice-point contract (SC3 — 5-facet spec + per-choice-node table referencing actual node ids), edit-node contract (SC4 — 5-step data-flow + 5-facet spec + per-edit-node table + edit:enzyme tag + edits.json location + edit.prompt stub explanation).
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the design doc sections 6-9 (SC5 classification table + SC2 reachability + backward-compat + open questions)</name>
  <files>.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md</files>
  <action>
APPEND sections 6-9 to the 05.1-DESIGN.md (created in Task 1):

## 6. Per-node classification table (SC5)
- One row per node (34 rows — every node in the skeleton). Columns: node_id, structural types (derived per section 2), MC-choice? (yes/no), edit-allowed? (yes/no), SC5 class (MC-choice / edit-allowed / both / neither), enzyme_id (or —), pathway branch point mapped, claim_ids (approved vs PLACEHOLDER_PHASE7).
- The SC5 class is DERIVED from the structural type set (research Q4 table):
  - has choice/rng-shuffle, NO edit-allowed -> MC-choice
  - has edit-allowed, NO choice/rng-shuffle -> edit-allowed (rare — none in the skeleton)
  - has BOTH choice/rng-shuffle AND edit-allowed -> both
  - none of the above (pure story or ending with no choices + no edit) -> neither
  - rng-shuffle (structural) -> MC-choice (gameplay — the RNG is the MC agent); if also edit-allowed -> both
- Read the actual skeleton JSON (data/story_glucose/*.json) to fill the table. The 34 nodes:
  - MC-choice (3): intro.select, pyr.branch, tca.divert_to_good. PLUS anaer.entry (the planner's design decision — the 3-way fermentation-outcome choice; OVERRIDES the research's tentative story/neither classification; flag in the open questions) = 4 total.
  - both (5): gly.pfk, pyr.pdh, tca.citrate_synthase, tca.aconitase, etc.complex_i (each has a Continue choice + an edit:offer choice + the edit:enzyme tag).
  - both (1 more): tca.shuffle (rng-shuffle + choice + edit-allowed — the ARCHITECTURE.md Pattern 2 canonical multi-role node). = 6 total "both".
  - neither (24): gly.start, gly.g6p, gly.fbp_to_pyruvate, gly.pyruvate, anaer.entry's 3 ending children (anaer.lactic, anaer.ethanolic, anaer.crisis), tca.entry, tca.co2_turn1, tca.co2_turn2, etc.entry, etc.complex_ii_iii_iv, etc.atp_synthase, end.true, end.good.fatty_acid, end.good.amino_acid, end.normal.co2, bad.lost_connection, bad.released_from_host, bad.cycle_trap_host_death, bad.critical_residue_break, fa.stub, alc.stub, edit.prompt. Wait — etc.entry HAS 2 choices (follow-ETC vs let-go) so it's MC-choice, NOT neither. And edit.prompt HAS 3 choices so it's MC-choice (placeholder). Correct the count: etc.entry -> MC-choice; edit.prompt -> MC-choice (placeholder stub). Recompute: MC-choice = 6 (intro.select, pyr.branch, tca.divert_to_good, anaer.entry, etc.entry, edit.prompt); both = 6 (the 5 edit-allowed story nodes + tca.shuffle); neither = 22 (the rest). Total = 34. VERIFY against the actual JSON when filling the table.
- The table is the REVIEWABLE artifact for SC5: the human reviews one row per node and confirms the gameplay role is the intended design decision (Plan 06).

## 7. Reachability validation (SC2)
- Document the green test: StoryGraph.load('data/story_glucose/') -> check_reachability(all_nodes, 'intro.select') -> is_ok True, all 4 tiers reachable, 11 endings (1T+3G+2N+5B), no unreachable endings. The BFS follows choice.goto ONLY (not on_enter_divert); it ignores cond + weight, so both the aerobic and anaerobic subtrees are traversed.
- Document the red test: remove the choice.goto edge to end.true (change etc.atp_synthase's goto to end.normal.co2) -> end.true unreachable -> is_ok False. Mirrors tests/test_integration.py:348-362.
- Document the CRITICAL aerobic-only-True-ending rule: the True ending is biochemically unreachable anaerobically (no O2 -> no ETC -> no ATP synthase -> no soul-jump). BUT check_reachability does NOT model O2 — it follows ALL choice.goto edges regardless of cond. So the True ending MUST be statically reachable via the aerobic choice.goto chain (pyr.branch -> pyr.pdh -> ... -> etc.atp_synthase -> end.true); the BFS finds it. The anaerobic guard is a RUNTIME cond on pyr.branch (cond: "not flags.host_o2_low" for the aerobic choice); at runtime when host_o2_low is True, the aerobic choice is hidden so the player can't reach end.true — but the STRUCTURAL edge still exists, so the BFS traverses it. DO NOT model the anaerobic block as a missing structural edge (that would make end.true unreachable_endings -> FAIL SC2). The rule: cond gates RUNTIME eligibility; choice.goto gates STRUCTURAL reachability.
- Document the tests/test_glucose_reachability.py file (5 tests, authored in Plan 04).

## 8. Backward-compat + gate impact
- The RECOMMENDED approach (derived types + tags for edit-allowed) requires ZERO code changes to the engine, model, gates, or tools (research Q8 table). Summarize: Node.from_dict backward-compat (NONE — tags parsed via .get); Node.to_dict round-trip (NONE — tags already round-trips); AST import gate (NONE — scans pymol/PyQt5 only); citation gate (NONE — collect_claim_ids reads only claim_ids, ignores tags); edit-coverage gate (NONE — reads c14/data/cast.json + c14/data/edits.json, not story nodes); interpreter/engine runtime (NONE — the classifier is documentation; tags is carried but not read by the interpreter).
- The 198 existing tests pass unchanged (zero regression — the toy fixture at data/story/ is untouched; the glucose skeleton is at data/story_glucose/).

## 9. Open questions for human review (surfaced to Plan 06)
List the open questions from the research (with the research's recommendation + the planner's resolution where applicable):
1. **Ethanolic anaerobic ending vs mammalian host** (research OQ1, HIGH priority): the skeleton INCLUDES anaer.ethanolic (per the user's 3-ending confirmation) but its text_teaching is FLAGGED — mammalian hosts do not do ethanolic fermentation (it's a yeast pathway). Human decides at Phase 7: (a) drop it, (b) frame as yeast counterfactual, (c) allow host variation. The skeleton provides the node; the human reviews the branch-point mapping.
2. **Normal-ending framing is a NARRATIVE tier** (research OQ2, MEDIUM): the skeleton places end.normal.co2 as a branch off etc.entry (the player "lets go" instead of following the soul into the ETC). Human reviews: is this the right narrative branch point, or should the Normal ending be a retrospective framing?
3. **Cycle-trap visit-cap N** (research OQ3, LOW): the skeleton uses N=5 (placeholder). Human confirms at Phase 6 playtest. NOT a science claim (no citation needed).
4. **Cataplerotic-exit claims (Good endings) not in first-batch** (research OQ4, MEDIUM): end.good.fatty_acid + end.good.amino_acid + tca.divert_to_good use PLACEHOLDER_PHASE7. Human approves a source at Phase 7.
5. **ETC PDB structures CANDIDATE** (research OQ5, MEDIUM — Phase 7): 5LDW/1ZOY/1BGY/1OCC/1E79 are web-verified CANDIDATE; the skeleton's ETC nodes use NO PDB loads (placeholder on_enter). Human approves the PDB structures before Phase 7 wires the on_enter loads.
6. **PDH cast PDB not identified** (research OQ6, LOW — Phase 9): pyr.pdh uses placeholder on_enter. Phase 9 cast item.
7. **char-setting mechanism for character-select** (research OQ7, COORDINATION): the skeleton's intro.select choices use placeholder effects: {} (no state mutation); the controller (Phase 6) reads the chosen index and calls engine.start(character="glucose", ...). The choice-point contract should specify how character-select sets state.character.
8. **on_enter_divert dormancy** (research OQ8 / Q1 flag, COORDINATION): on_enter_divert is defined but never referenced by interpreter/engine. The skeleton uses choice.goto for all forward paths (single-option "Continue" choices). IF the designer wants on_enter_divert to be BFS-traversable, that's a reachability-checker EXTENSION (not proposed in Phase 5.1).
9. **edits.json / cast.json location** (research Q9.2, low-stakes): c14/data/ (tooling-bound, current) vs data/ (ARCHITECTURE.md). Recommendation: keep c14/data/ (zero-work); treat ARCHITECTURE.md as outdated.
10. **edit-allowed + enzyme_id binding: tags vs new field** (research Q9.1): the skeleton uses the tags approach (edit:enzyme:<id>, zero schema change). The alternative (new Node.edit_enzyme_id field, 4-line additive) is flagged but not adopted.
11. **SC5 classification: design-doc table vs JSON field** (research Q9.3): the skeleton uses a design-doc table (this section 6). A JSON classification field is REJECTED (derivable, would drift).
12. **Node-level vs choice-level edit:offer tag** (research Q9.5): the skeleton uses a NODE-level edit:enzyme:<id> tag (the enzyme_id binding is a node property) AND a CHOICE-level edit:offer tag (marks which choice launches the edit UI). They coexist.
13. **anaer.entry classification** (planner decision): the research tentatively classified anaer.entry as story/neither (left the 3-ending branching mechanism unspecified). The planner classifies it as choice/MC-choice (it offers the 3-way fermentation-outcome choice). Human reviews this classification.

Citation-gate coexistence (documented in section 1 + here): the skeleton uses PLACEHOLDER_PHASE7 claim_ids for pending nodes. The citation gate (tools/check_citations.py --story <path> --registry <path>) is a pre-ship, manually-invoked CLI tool — it is NOT auto-run on data/story_glucose/ during Phase 5.1 (there is no CI/Makefile auto-runner; the gate tests use their own fixtures under tests/fixtures/). The existing toy fixture (data/story/intro.json) uses placeholder-intro etc. (NOT in the registry) and the gate is not broken because it's never run on data/story/. Phase 7 replaces PLACEHOLDER_PHASE7 with real approved claims and runs the gate as a release gate. The 4-5 APPROVED claim_ids (GLY-PFK-01, CAST-PFK-PDB-01, CAST-CSC-PDB-01, TCA-RNG-CITRATE-PROCHIRALITY-01, TCA-RNG-WEIGHT-01) ARE in the registry and would PASS the gate if it were run; only the PLACEHOLDER_PHASE7 nodes would fail (expected — the skeleton is a design artifact, not shippable content).
  </action>
  <verify>
```bash
python3.6 -c "
with open('.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md') as f:
    content = f.read()
# sections 6-9 exist
for section in ['## 6. Per-node classification table', '## 7. Reachability validation', '## 8. Backward-compat', '## 9. Open questions']:
    assert section in content, 'missing section: ' + section
# SC5 table has ~34 rows (one per node). Check a sample of node ids across all 7 files
for nid in ['intro.select', 'gly.start', 'gly.pfk', 'pyr.branch', 'anaer.lactic', 'tca.shuffle', 'etc.entry', 'end.true', 'end.normal.co2', 'bad.cycle_trap_host_death', 'edit.prompt', 'fa.stub']:
    assert nid in content, 'SC5 table missing node: ' + nid
# SC2 reachability section documents the aerobic-only-True rule
assert 'cond gates' in content and 'choice.goto gates' in content, 'SC2 section missing the cond-vs-structural rule'
# open questions surface the key items
for oq in ['ethanolic', 'Normal-ending', 'cycle-trap', 'cataplerotic', 'ETC PDB', 'PLACEHOLDER_PHASE7']:
    assert oq in content, 'open questions missing: ' + oq
# citation-gate coexistence documented
assert 'pre-ship' in content or 'manually-invoked' in content, 'citation-gate coexistence not documented'
# the doc is substantial (not a stub)
assert len(content) > 3000, 'design doc too short (' + str(len(content)) + ' chars); expected a substantial reviewable artifact'
print('OK: 05.1-DESIGN.md sections 6-9 present; SC5 table covers all node ids; SC2 rule documented; ' + str(len(content)) + ' chars')
"
```
  </verify>
  <done>
05.1-DESIGN.md is complete (sections 1-9): overview + node-type taxonomy (SC1) + file split + choice-point contract (SC3, per-choice-node table) + edit-node contract (SC4, per-edit-node table + 5-step data-flow) + per-node classification table (SC5, ~34 rows) + reachability validation (SC2, aerobic-only-True rule) + backward-compat + open questions (13 items, including the citation-gate coexistence documentation). The doc cross-references the actual node ids in data/story_glucose/*.json. It is a substantial reviewable artifact (>3000 chars).
  </done>
</task>

</tasks>

<verification>
- 05.1-DESIGN.md exists in the phase directory (NOT in data/story_glucose/ — it's a planning artifact)
- All 9 sections present (1-9)
- SC3 (choice-point contract) has a per-choice-node table referencing actual node ids (intro.select, pyr.branch, tca.shuffle, tca.divert_to_good, anaer.entry, etc.entry + the 5 edit-allowed nodes' choices)
- SC4 (edit-node contract) has a per-edit-node table (5 rows: gly.pfk, pyr.pdh, tca.citrate_synthase, tca.aconitase, etc.complex_i) + the 5-step data-flow + the edit:enzyme:<id> tag convention + the edit.prompt stub explanation + the edits.json location
- SC5 (classification table) has ~34 rows (one per node), each with the derived SC5 class
- SC2 (reachability) documents the aerobic-only-True rule (cond gates runtime; choice.goto gates structural)
- The citation-gate coexistence approach is documented (PLACEHOLDER_PHASE7 + pre-ship gate + not run on the skeleton until Phase 7)
- 13 open questions surfaced for human review (Plan 06)
- Locked Phase 5 decisions honored (not re-litigated): soul-jump metamorphosis, C14-decay DROP, anaerobic option (d), TCA RNG 0.5/0.5, 1T+3G+2N+5B
</verification>

<success_criteria>
- 05.1-DESIGN.md is a complete, reviewable design artifact in the phase directory
- SC3 choice-point contract documented with a per-choice-node table (actual node ids + the 5 facets each)
- SC4 edit-node contract documented with a per-edit-node table + the 5-step data-flow + the edit:enzyme:<id> binding + the edit.prompt stub explanation
- SC5 per-node classification table covers all 34 nodes (one row each, derived SC5 class)
- SC1 node-type taxonomy documented (derived property + tags declaration, zero schema change)
- SC2 reachability approach documented (aerobic-only-True rule: cond gates runtime, choice.goto gates structural)
- Citation-gate coexistence resolved + documented (PLACEHOLDER_PHASE7 + pre-ship gate)
- 13 open questions surfaced for the human review checkpoint (Plan 06)
- Locked Phase 5 decisions honored (not re-litigated)
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-05-SUMMARY.md`
</output>
