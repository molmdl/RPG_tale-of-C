---
phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts
plan: 16
type: execute
wave: 2
depends_on: ["05.1-12", "05.1-13"]
files_modified:
  - .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-GRAPH-DIAGRAM.md
  - .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.svg
  - .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.txt
autonomous: true

must_haves:
  truths:
    - "05.1-graph.svg + 05.1-graph.txt are regenerated via tools/render_story_graph.py from the 54-node skeleton (the renderer auto-detects the 11 new nodes: bad.* -> bad stage, intro.* -> intro stage)"
    - "the regenerated SVG is well-formed XML + shows the 2 new preface nodes (intro.preface + intro.shell_glucose) + the 9 new bad-ending nodes + the new edges (intro.preface -> intro.select -> intro.shell_glucose -> gly.start; edit.prompt -> 9 new bad endings)"
    - "05.1-GRAPH-DIAGRAM.md is updated: header counts 43 -> 54 nodes / 11 -> 20 endings; the mermaid diagram adds the 11 new nodes; the Ending-Tier Distribution table shows Bad 5 -> 14 (total 20); a new 'Bad-Ending + Preface Expansion (Plans 05.1-12/13)' section documents the 11 new nodes + the decisions"
    - "the 14 edit-allowed node markers are still present in the SVG (the expansion added NO edit:enzyme tags); the 0 single-Continue invariant is visually evident (preface nodes have Continue + Observe)"
  artifacts:
    - path: ".planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.svg"
      provides: "regenerated swim-lane SVG of the 54-node skeleton (11 new nodes + new edges)"
      contains: "intro.preface"
    - path: ".planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.txt"
      provides: "regenerated ASCII diagram of the 54-node skeleton"
      contains: "-->"
    - path: ".planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-GRAPH-DIAGRAM.md"
      provides: "updated diagram narrative + mermaid + tables for the 54-node / 20-ending / intro.preface-start skeleton"
      contains: "intro.preface"
  key_links:
    - from: "tools/render_story_graph.py"
      to: "data/story_glucose/*.json (the 54-node skeleton)"
      via: "reads the manifest + node fragments; auto-detects stages (bad.* -> bad, intro.* -> intro) + edit-allowed (edit:enzyme tags) + endings (is_ending)"
      pattern: "StoryGraph|manifest"
---

<objective>
Regenerate the story-graph diagram (SVG + ASCII) from the 54-node post-expansion skeleton + update 05.1-GRAPH-DIAGRAM.md (the visual-narrative artifact for the 05.1-06 re-review). The 11 new nodes (9 bad endings + 2 preface) + the new edges (intro.preface -> intro.select -> intro.shell_glucose -> gly.start; edit.prompt -> 9 new bad endings) must be reflected.

Purpose: The 05.1-06 checkpoint reviewer uses this diagram as the visual overview of the skeleton. The renderer (tools/render_story_graph.py) is CWD-independent + auto-detects stages (bad.* -> "bad" stage at line 80; intro.* -> "intro" stage at line 94) + edit-allowed nodes (edit:enzyme tags at line 121) + endings (is_ending) — so regenerating after Plans 12+13 automatically reflects the 11 new nodes + the 14 unchanged edit-allowed markers. The .md is hand-authored narrative (mermaid + tables + decision log) that must be updated to match.

Output: 3 modified files (regenerated svg + txt + updated .md). The diagram is the accurate visual artifact for the 54-node skeleton.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-CHECKPOINT-FEEDBACK.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-12-SUMMARY.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-13-SUMMARY.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-GRAPH-DIAGRAM.md
@tools/render_story_graph.py
@data/story_glucose/manifest.json
@data/story_glucose/bad_endings.json
@data/story_glucose/intro.json
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate the SVG + ASCII diagram from the 54-node skeleton + verify well-formedness + new-node presence</name>
  <files>.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.svg, .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.txt</files>
  <action>
Regenerate the diagram by running the renderer against the post-expansion skeleton. The renderer is CWD-independent (defaults resolved relative to the script path: --story-dir = data/story_glucose, --output-dir = the phase 5.1 planning dir). It auto-detects the 11 new nodes (bad.* -> "bad" stage swim-lane at line 80; intro.* -> "intro" stage at line 94) + the 14 unchanged edit-allowed nodes (edit:enzyme tags at line 121) + endings (is_ending) + the new edges (intro.preface -> intro.select; intro.select -> intro.shell_glucose; intro.shell_glucose -> gly.start; edit.prompt -> 9 new bad endings, dashed structural). No renderer code change needed — the new nodes are just nodes.

1. Run: python3.6 tools/render_story_graph.py
   (This overwrites 05.1-graph.svg + 05.1-graph.txt in place. Confirm exit 0.)
2. Verify the SVG is well-formed XML: python3.6 -c "import xml.etree.ElementTree as ET; ET.parse('.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.svg'); print('SVG well-formed')"
3. Verify the SVG + TXT contain the 11 new node ids + the new edges:
   - grep -c "intro.preface" the svg + txt (expect >= 1 each)
   - grep -c "intro.shell_glucose" the svg + txt (expect >= 1 each)
   - grep -c "bad.enzyme_collapse\|bad.wrong_substrate_trapped\|bad.broken_active_site\|bad.lost_in_cytosol\|bad.proton_leak\|bad.misfolded_aggregate\|bad.active_site_destroyed\|bad.substrate_channel_blocked\|bad.cofactor_lost" the txt (expect >= 9 — the 9 new bad endings appear in the ASCII)
4. Verify the 14 edit-allowed markers are still present (the expansion added NO edit:enzyme tags): grep -c "FFF8DC" the svg (the renderer uses #FFF8DC fill for edit-allowed nodes per the classDef at line 203 of the .md / line 487 of the renderer — expect 14 occurrences, one per edit-allowed node).
5. Verify the start node change is reflected: the renderer labels the start node (intro.preface is now the entry point — confirm the SVG/TXT marks intro.preface as the start, not intro.select).

Do NOT modify the renderer (tools/render_story_graph.py) — it auto-detects the new nodes. If the renderer crashes or mis-renders a new node, investigate the renderer's stage_key/node_style logic (lines 71-130) — the bad.* + intro.* prefixes are already handled; the new nodes should render correctly without code change. Only if a new node has an unexpected shape that the renderer can't classify would a renderer tweak be needed (unlikely — all 11 new nodes are standard ending/choice nodes).
  </action>
  <verify>
1. python3.6 tools/render_story_graph.py && echo "renderer exit $?"  (exit 0)
2. python3.6 -c "import xml.etree.ElementTree as ET; ET.parse('.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.svg'); print('SVG well-formed')"
3. grep -c "intro.preface" .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.svg (>= 1)
4. grep -c "intro.preface" .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.txt (>= 1)
5. grep -c "bad.enzyme_collapse\|bad.wrong_substrate_trapped\|bad.broken_active_site\|bad.lost_in_cytosol\|bad.proton_leak\|bad.misfolded_aggregate\|bad.active_site_destroyed\|bad.substrate_channel_blocked\|bad.cofactor_lost" .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.txt (>= 9)
6. grep -c "FFF8DC" .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.svg (expect ~14 — one per edit-allowed node; the exact count depends on the renderer's marker style — confirm it's in the 12-16 range, NOT zero)
7. head -50 .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.txt  (confirm the ASCII shows the new preface + bad-ending nodes)
  </verify>
  <done>
05.1-graph.svg + 05.1-graph.txt regenerated from the 54-node skeleton; SVG well-formed; both contain intro.preface + intro.shell_glucose + the 9 new bad-ending ids + the new edges; 14 edit-allowed markers present; intro.preface marked as the start.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update 05.1-GRAPH-DIAGRAM.md — header counts + mermaid + Ending-Tier table + new "Bad-Ending + Preface Expansion" section</name>
  <files>.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-GRAPH-DIAGRAM.md</files>
  <action>
Edit 05.1-GRAPH-DIAGRAM.md (the hand-authored narrative; the svg/txt are regenerated by Task 1). Update the doc to reflect the 54-node / 20-ending / intro.preface-start skeleton.

1. Header (lines 1-20): update "43-node glucose skeleton" -> "54-node glucose skeleton"; update the post-replan state paragraph to add the 05.1-12/13 expansion: "Post-expansion state (Plans 05.1-12/13): 54 nodes (was 43); 20 endings (was 11; 1T+3G+2N+14B — the Bad tier grew 5 -> 14); start node intro.preface (was intro.select); +9 bad-ending nodes (Req 1: 6 1a unknown-pool + 3 1b known-consequence) + 2 preface nodes (Req 2: intro.preface Beat A + intro.shell_glucose Beat B). The 14 edit-allowed count + the 0 single-Continue invariant are UNCHANGED." Keep the legend table; add a row for the new preface-node style if the renderer introduces one (or note preface nodes are plain boxes like other choice nodes).

2. Full Graph Topology mermaid (lines 39-200ish): update the mermaid diagram:
   - In the "intro" subgraph: add IP["intro.preface<br/>Beat A — the hero's blessing (start)"] BEFORE IS["intro.select..."], and ISG["intro.shell_glucose<br/>Beat B — the hero dons glucose"] AFTER IS. Update IS's label to note it's now the character-select (no longer the start).
   - In the "bad" subgraph: add the 9 new bad-ending nodes (BEC["bad.enzyme_collapse<br/>BAD: 1a unknown pool"], BST["bad.wrong_substrate_trapped"], BBA["bad.broken_active_site"], BLIC["bad.lost_in_cytosol"], BPL["bad.proton_leak"], BMA["bad.misfolded_aggregate"], BAS["bad.active_site_destroyed<br/>BAD: 1b known consequence"], BSC["bad.substrate_channel_blocked"], BCL["bad.cofactor_lost"]). Use the red rounded box style (same as the existing 4 bad endings).
   - Update the MAIN FLOW edges: change `IS -->|"Glucose"| GS` to `IP -->|"Continue"| IS` (intro.preface -> intro.select) + `IS -->|"Glucose"| ISG` (intro.select -> intro.shell_glucose) + `ISG -->|"Begin journey"| GS` (intro.shell_glucose -> gly.start). The FA/Alc edges from IS stay (`IS -.->|"Phase 8"| FA` + `IS -.->|"Phase 8"| AL`).
   - Update the edit.prompt edges: the EP (edit.prompt) node now has 12 dashed structural edges to 12 bad endings (3 existing + 9 new). Add the 9 new dashed edges: `EP -.->|"structural"| BEC`, `EP -.->|"structural"| BST`, etc. (one per new bad ending). Keep the existing 3 (EP -> BL, EP -> BR, EP -> BK). Also keep the cycle-trap edge (TS -> BC, cond-gated).

3. Ending-Tier Distribution table (lines 245-256): update the header "11 endings — UNCHANGED by the expansion" -> "20 endings (was 11; the Bad tier grew 5 -> 14 via Plans 05.1-12/13)". Update the table:
   - True: 1 (unchanged)
   - Good: 3 (unchanged)
   - Normal: 2 (unchanged)
   - Bad: 14 (was 5) — list: `anaer.crisis`, `bad.lost_connection`, `bad.released_from_host`, `bad.cycle_trap_host_death`, `bad.critical_residue_break` (existing 5) + `bad.enzyme_collapse`, `bad.wrong_substrate_trapped`, `bad.broken_active_site`, `bad.lost_in_cytosol`, `bad.proton_leak`, `bad.misfolded_aggregate` (1a, 6) + `bad.active_site_destroyed`, `bad.substrate_channel_blocked`, `bad.cofactor_lost` (1b, 3).
   - Update the note: "Matches PROJECT.md row 107 (user-confirmed asymmetric distribution) + the 05.1-06 checkpoint feedback Req 1 (CG-collection bad endings). The Bad tier grew 5 -> 14 via Plans 05.1-12/13 (9 new bad endings: 6 1a unknown-pool + 3 1b known-consequence); True/Good/Normal UNCHANGED."

4. Edit-Allowed Nodes section (lines 260-297): NO CHANGE — the 14 edit-allowed nodes are UNCHANGED by Plans 12+13. Add a one-line note at the top: "UNCHANGED by the 05.1-12/13 bad-ending + preface expansion (no edit:enzyme tags added)."

5. Add a NEW section "Bad-Ending + Preface Expansion (Plans 05.1-12/13)" after the "Disease-Mutant Replan Changes" section (or after the "New Nodes Added" section). Document:
   - A table of the 11 new nodes: | Node | File | Type | Tier/Role | Req | Decision |
     - intro.preface | intro.json | choice (Continue+Observe) | MC-choice (preface Beat A) | Req 2 Beat A | hero's blessing before character select; on_enter placeholders (load hero atom spheres + 20 AA cartoon)
     - intro.shell_glucose | intro.json | choice (Continue+Observe) | MC-choice (preface Beat B) | Req 2 Beat B | hero dons glucose; per-character (FA/Alc Phase 8)
     - 9 bad endings (bad_endings.json) | ending (is_ending=bad) | neither | Req 1 | 1a (6, edit:unknown, RngEngine-weighted pool) + 1b (3, edit:known, deterministic)
   - A "Decisions Locked" subsection:
     - Req 1a (randomize pool): shared pool (NOT per-enzyme) — variety via node count (unknown pool 2 -> 8); per-enzyme pools are Phase 7.
     - Req 1b (known-consequence): 3 deterministic bad endings tagged edit:known (distinct from 1a's edit:unknown); the binding to specific known edits is Phase 7 edits.json content; the skeleton makes them reachable via edit.prompt structural choices.
     - Req 2 topology: intro.preface -> intro.select -> intro.shell_glucose -> gly.start; manifest start intro.select -> intro.preface.
     - Req 2 shell node: per-character (intro.shell_glucose), NOT parametric; FA/Alc stubbed (Phase 8 adds intro.shell_fa / intro.shell_alc).
     - Req 2 hide-20-AA: gly.start on_enter [hide_all, show_as glucose] (NO journey_start node — simpler; hide_all hides the 20 AA + show_as re-shows only glucose).
     - 0-single-Continue: preface nodes get Observe 2nd choice (invariant UNCHANGED, NOT re-scoped).
     - 20-AA cartoon: show_as rep "cartoon" (terminals naturally hidden; exact cmd is Phase 5.2/6).
     - Placeholders: pdb:TBD_HERO_CARBON + pdb:TBD_AA_CAST_20 + pdb:TBD_GLUCOSE (loud-fail at runtime until Phase 6/7; NO fabricated PDB IDs; follow the TBD_ACONITASE precedent).
   - A "Pending 05.1-06 Re-review" note: the 3 CRITICAL disease-mutant decisions + 24 OQs are STILL pending (Plans 12-13 implement ONLY the 2 new requirements). Cross-reference 05.1-CHECKPOINT-FEEDBACK.md + the new OQ-BAD-1/2/3 + OQ-PREFACE-1/2/3/4/5 in 05.1-DESIGN.md §9 (Plan 05.1-15).

6. Key Design Decisions Locked section (lines 405+): add the new locked decisions from Plans 12+13 (the shared-pool, 1a/1b tags, per-character shell, gly.start hide-20-AA, Observe 2nd choice, pdb:TBD_* placeholders) — or cross-reference the new "Bad-Ending + Preface Expansion" section.

Preserve the existing stage-grouping + legend + branch-point mapping sections where still accurate. Keep mermaid syntax valid. Keep markdown formatting consistent.
  </action>
  <verify>
1. grep -c "intro.preface" .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-GRAPH-DIAGRAM.md (>= 5: header + mermaid + new-section + ending-table + decisions)
2. grep "54-node\|54 nodes" .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-GRAPH-DIAGRAM.md (the updated count in the header)
3. grep "Bad | 14" .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-GRAPH-DIAGRAM.md (the updated Ending-Tier table row)
4. grep "Bad-Ending + Preface Expansion" .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-GRAPH-DIAGRAM.md (the new section header)
5. python3.6 -c "open('.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-GRAPH-DIAGRAM.md').read()"  (file readable)
6. python3.6 -m unittest tests.test_glucose_reachability -v  (still GREEN — the diagram is doc-only, does not affect tests; confirms no accidental skeleton JSON edit)
  </verify>
  <done>
05.1-GRAPH-DIAGRAM.md updated: header 54 nodes / 20 endings; mermaid diagram with 11 new nodes + new edges; Ending-Tier table Bad 5 -> 14 (total 20); new "Bad-Ending + Preface Expansion (Plans 05.1-12/13)" section with the 11-node table + locked decisions + pending-review note; Key Design Decisions updated. Reachability tests still GREEN (doc-only change).
  </done>
</task>

</tasks>

<verification>
- python3.6 tools/render_story_graph.py && echo "renderer exit $?"  (exit 0; regenerates svg + txt from the 54-node skeleton)
- python3.6 -c "import xml.etree.ElementTree as ET; ET.parse('.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.svg'); print('SVG well-formed')"
- grep -c "intro.preface\|intro.shell_glucose\|bad.enzyme_collapse" .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.txt (the new nodes appear in the ASCII)
- grep -c "FFF8DC" .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.svg (14 edit-allowed markers present — UNCHANGED)
- python3.6 -m unittest tests.test_glucose_reachability -v  (9 tests PASS after Plan 14; the diagram regen + .md update are doc-only, zero test impact)
- python3.6 -m unittest discover -s tests  (full suite passes — zero regression)
</verification>

<success_criteria>
- 05.1-graph.svg + 05.1-graph.txt regenerated from the 54-node skeleton; SVG well-formed; both show intro.preface + intro.shell_glucose + the 9 new bad endings + the new edges; 14 edit-allowed markers present; intro.preface is the start.
- 05.1-GRAPH-DIAGRAM.md: header counts 54 nodes / 20 endings; mermaid diagram updated (11 new nodes + new edges); Ending-Tier table Bad 5 -> 14 (total 20); new "Bad-Ending + Preface Expansion (Plans 05.1-12/13)" section with the 11-node table + locked decisions + pending-rereview note; Key Design Decisions updated.
- Reachability tests still GREEN (doc-only change, zero regression); full suite passes.
</success_criteria>

<output>
After completion, create .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-16-SUMMARY.md recording: the renderer invocation + exit 0, the SVG well-formedness, the 11 new nodes in the svg/txt, the 14 edit-allowed markers, the .md updates (header counts, mermaid, Ending-Tier table, new section, decisions), and the reachability-tests-still-GREEN confirmation.
</output>
