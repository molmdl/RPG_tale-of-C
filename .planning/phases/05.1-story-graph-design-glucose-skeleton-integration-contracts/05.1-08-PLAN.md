---
phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - data/story_glucose/tca.json
autonomous: true

must_haves:
  truths:
    - "tca.isocitrate_dh, tca.succinyl_coa_synthetase, tca.fumarase each have edit:enzyme tag + edit:offer choice (goto edit.prompt) + DIS-*-cand claim_id + 3 choices (Continue + Observe + edit:offer)"
    - "tca.malate_dh has edit:enzyme tag + edit:offer choice + DIS-MDH2-01-cand + 3 choices (loop + forward + edit:offer) - it was already 2-choice so NO Observe added"
    - "tca.aconitase claim_ids include DIS-ACO2-01-cand (added alongside the existing APPROVED TCA-RNG-CITRATE-PROCHIRALITY-01)"
    - "tca.entry and tca.akg_dh each have >=2 choices (Continue + Observe, same destination) - NOT promoted (akg_dh has no confirmed pathogenic missense; entry is a narrative node)"
    - "tca.citrate_synthase carries an edit:structural tag (reframed as structural-exploration, NOT disease-restoration) - keeps its edit:enzyme tag + 2 choices unchanged"
    - "tca.isocitrate_dh uses DIS-IDH3A-01-cand as DEFAULT (IDH3A M204I is the biochemically-correct TCA enzyme); the IDH1 R132H alternative is FLAGGED for human review in the design doc, NOT decided in the skeleton"
    - "reachability stays GREEN (no node added/removed; node count still 43; new choices goto existing nodes)"
  artifacts:
    - path: "data/story_glucose/tca.json"
      provides: "7 modified TCA nodes (tca.entry, tca.citrate_synthase, tca.aconitase, tca.isocitrate_dh, tca.akg_dh, tca.succinyl_coa_synthetase, tca.fumarase, tca.malate_dh)"
      contains: "DIS-IDH3A-01-cand"
  key_links:
    - from: "tca.isocitrate_dh / tca.succinyl_coa_synthetase / tca.fumarase / tca.malate_dh edit:offer choices"
      to: "edit.prompt"
      via: "choice.goto"
      pattern: "edit\\.prompt"
    - from: "tca.citrate_synthase"
      to: "edit:structural tag"
      via: "node tag (reframe marker)"
      pattern: "edit:structural"
---

<objective>
Apply the disease-mutant research + Continue-to-MC directive to the TCA skeleton region: promote 4 TCA enzymes to edit-allowed (IDH3A, SUCLG1, FH, MDH2), add DIS-ACO2-01-cand to the existing tca.aconitase node, add Observe choices to 2 single-Continue nodes (tca.entry, tca.akg_dh), and reframe tca.citrate_synthase as structural-exploration (edit:structural tag; NO disease mutant exists for CS).

Purpose: Driver 1 (disease-mutant research) + Driver 2 (Continue-to-MC). STRUCTURE ONLY. The IDH3A-vs-IDH1 decision is FLAGGED for the 05.1-06 human-review checkpoint (skeleton uses IDH3A as the biochemically-correct DEFAULT; the IDH1 cancer-mutant alternative is documented but NOT chosen unilaterally). tca.akg_dh is NOT promoted (no confirmed pathogenic missense in first 14/163 ClinVar records - a LOW-confidence negative flagged for deeper scan). tca.citrate_synthase is reframed (no disease point mutant; the existing edit:enzyme tag predates the restoration narrative and was for structural exploration).
Output: 1 modified skeleton JSON file (tca.json) with 8 node modifications; node count unchanged (43).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-REPLAN-CONTEXT.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-RESEARCH-disease-mutants-tca.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md
@data/story_glucose/tca.json
@data/story_glucose/manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote 4 TCA enzymes to edit-allowed (isocitrate_dh, succinyl_coa_synthetase, fumarase, malate_dh) + add DIS-ACO2-01-cand to aconitase</name>
  <files>data/story_glucose/tca.json</files>
  <action>
Edit data/story_glucose/tca.json. All changes are additive (no node added/removed). Each promoted node gains: an edit:enzyme:<id> tag, an edit:offer choice (goto edit.prompt), and its DIS-*-cand claim_id (CANDIDATE pending Phase 7). The 3 linear promoted nodes also gain an Observe choice (Continue-to-MC). tca.malate_dh is already 2-choice (loop + forward) so it gets ONLY the edit:offer (no Observe).

1. tca.aconitase - claim_id add only (already edit-allowed, already 2 choices; NO MC change):
   - claim_ids: change ["TCA-RNG-CITRATE-PROCHIRALITY-01"] to ["TCA-RNG-CITRATE-PROCHIRALITY-01", "DIS-ACO2-01-cand"]
   - Rationale: ACO2 S112R-to-R112S is a confirmed ICRD point mutant (ClinVar 29584 Pathogenic, OMIM 614559). DIS-ACO2-01-cand is CANDIDATE pending Phase 7. (The bovine-1ACO homology + residue-numbering caveat is Phase 7 text_teaching content - do NOT add it to the skeleton here.)

2. tca.isocitrate_dh - PROMOTE + Continue-to-MC (becomes 3-choice); IDH3A DEFAULT:
   - tags: change ["stage:tca"] to ["stage:tca", "edit:enzyme:tca.isocitrate_dh"]
   - claim_ids: change ["PLACEHOLDER_PHASE7_TCA"] to ["PLACEHOLDER_PHASE7_TCA", "DIS-IDH3A-01-cand"]
     - Rationale: IDH3A M204I-to-I204M is the biochemically-correct TCA enzyme disease mutant (ClinVar Pathogenic, OMIM 619007 Retinitis pigmentosa 90). DIS-IDH3A-01-cand is CANDIDATE pending Phase 7. IDH3A is the DEFAULT; the IDH1 R132H alternative (cancer-mutant, real mutant PDB 3INM, narratively powerful but NOT the TCA enzyme) is FLAGGED for human review in the 05.1-06 checkpoint - the skeleton does NOT decide this unilaterally.
   - choices: change the single-element array to a 3-element array:
     [{"label": "Continue", "goto": "tca.akg_dh"}, {"label": "Examine the isocitrate dehydrogenase active site", "goto": "tca.akg_dh", "tags": ["mc:observe"]}, {"label": "Try to edit isocitrate dehydrogenase", "goto": "edit.prompt", "tags": ["edit:offer"]}]

3. tca.succinyl_coa_synthetase - PROMOTE + Continue-to-MC (becomes 3-choice):
   - tags: change ["stage:tca"] to ["stage:tca", "edit:enzyme:tca.succinyl_coa_synthetase"]
   - claim_ids: change ["PLACEHOLDER_PHASE7_TCA"] to ["PLACEHOLDER_PHASE7_TCA", "DIS-SUCLG1-01-cand"]
     - Rationale: SUCLG1 G85A-to-A85G is a confirmed point mutant (OMIM 245400 MTDPS9). DIS-SUCLG1-01-cand is CANDIDATE pending Phase 7. (The skeleton's existing 6WCV PDB is SUCLG1 - no PDB swap needed.)
   - choices: change the single-element array to a 3-element array:
     [{"label": "Continue", "goto": "tca.fumarase"}, {"label": "Examine the succinyl-CoA synthetase active site", "goto": "tca.fumarase", "tags": ["mc:observe"]}, {"label": "Try to edit succinyl-CoA synthetase", "goto": "edit.prompt", "tags": ["edit:offer"]}]

4. tca.fumarase - PROMOTE + Continue-to-MC (becomes 3-choice); HIGHEST-confidence candidate:
   - tags: change ["stage:tca"] to ["stage:tca", "edit:enzyme:tca.fumarase"]
   - claim_ids: change ["PLACEHOLDER_PHASE7_TCA"] to ["PLACEHOLDER_PHASE7_TCA", "DIS-FH-01-cand"]
     - Rationale: FH R233H-to-H233R is the STRONGEST candidate (multi-submitter ClinVar Pathogenic, UniProt "catalytically inactive", OMIM 150800 HLRCC/FMRD). DIS-FH-01-cand is CANDIDATE pending Phase 7.
   - choices: change the single-element array to a 3-element array:
     [{"label": "Continue", "goto": "tca.malate_dh"}, {"label": "Examine the fumarase active site", "goto": "tca.malate_dh", "tags": ["mc:observe"]}, {"label": "Try to edit fumarase", "goto": "edit.prompt", "tags": ["edit:offer"]}]

5. tca.malate_dh - PROMOTE only (already 2-choice: loop + forward; NO Observe added):
   - tags: change ["stage:tca"] to ["stage:tca", "edit:enzyme:tca.malate_dh"]
   - claim_ids: change ["PLACEHOLDER_PHASE7_TCA"] to ["PLACEHOLDER_PHASE7_TCA", "DIS-MDH2-01-cand"]
     - Rationale: MDH2 G37R-to-R37G is a confirmed point mutant (ClinVar Pathogenic, OMIM 617339 DEE51). DIS-MDH2-01-cand is CANDIDATE pending Phase 7.
   - choices: ADD an edit:offer choice to the EXISTING 2-element array (do NOT remove the loop/forward choices). Result is a 3-element array:
     [{"label": "The cycle turns again", "goto": "tca.shuffle"}, {"label": "Continue onward - the soul calls", "goto": "tca.divert_to_good"}, {"label": "Try to edit malate dehydrogenase", "goto": "edit.prompt", "tags": ["edit:offer"]}]

Do NOT touch text fields. Preserve the existing on_enter (no PDB loads for these narrative nodes). Preserve JSON formatting.
  </action>
  <verify>
1. python3.6 -c "import json; d=json.load(open('data/story_glucose/tca.json')); n=d['nodes']; assert 'DIS-ACO2-01-cand' in n['tca.aconitase']['claim_ids']; assert 'edit:enzyme:tca.isocitrate_dh' in n['tca.isocitrate_dh']['tags']; assert 'DIS-IDH3A-01-cand' in n['tca.isocitrate_dh']['claim_ids']; assert len(n['tca.isocitrate_dh']['choices'])==3; assert any(c.get('goto')=='edit.prompt' for c in n['tca.isocitrate_dh']['choices']); assert 'DIS-SUCLG1-01-cand' in n['tca.succinyl_coa_synthetase']['claim_ids']; assert 'DIS-FH-01-cand' in n['tca.fumarase']['claim_ids']; assert 'DIS-MDH2-01-cand' in n['tca.malate_dh']['claim_ids']; assert len(n['tca.malate_dh']['choices'])==3; assert any(c.get('goto')=='tca.shuffle' for c in n['tca.malate_dh']['choices']); print('TCA promotions OK')"
2. python3.6 -m unittest tests.test_glucose_reachability -v  (all 5 tests PASS)
  </verify>
  <done>
4 TCA enzymes promoted to edit-allowed (isocitrate_dh with IDH3A default, succinyl_coa_synthetase, fumarase, malate_dh); tca.aconitase carries DIS-ACO2-01-cand; the 3 linear promoted nodes have 3 choices (Continue + Observe + edit:offer); tca.malate_dh has 3 choices (loop + forward + edit:offer); reachability GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: Continue-to-MC for tca.entry + tca.akg_dh; reframe tca.citrate_synthase as structural-exploration</name>
  <files>data/story_glucose/tca.json</files>
  <action>
Edit data/story_glucose/tca.json (same file as Task 1; sequential).

1. tca.entry - Continue-to-MC (NOT promoted - narrative entry node):
   - choices: change the single-element array to a 2-element array:
     [{"label": "Continue", "goto": "tca.citrate_synthase"}, {"label": "Examine the TCA cycle entry", "goto": "tca.citrate_synthase", "tags": ["mc:observe"]}]

2. tca.akg_dh - Continue-to-MC (NOT promoted - no confirmed pathogenic missense in first 14/163 ClinVar records; LOW-confidence negative flagged for deeper scan in the checkpoint):
   - choices: change the single-element array to a 2-element array:
     [{"label": "Continue", "goto": "tca.succinyl_coa_synthetase"}, {"label": "Examine the alpha-ketoglutarate dehydrogenase step", "goto": "tca.succinyl_coa_synthetase", "tags": ["mc:observe"]}]
   - NOTE: do NOT add an edit:enzyme tag or edit:offer or DIS-*-cand to tca.akg_dh (confirmed NOT promoted per research).

3. tca.citrate_synthase - REFRAME as structural-exploration (NO disease point mutant; keep the existing edit:enzyme tag + 2 choices unchanged):
   - tags: change ["stage:tca", "edit:enzyme:tca.citrate_synthase"] to ["stage:tca", "edit:enzyme:tca.citrate_synthase", "edit:structural"]
   - Rationale (from 05.1-RESEARCH-disease-mutants-tca.md section 1): CS has NO confirmed disease-causing point mutation (UniProt O75390 disease field empty; the 7 ClinVar Pathogenic records are ALL structural variants - inversions/CN-gains, not point mutations implementable via cmd.alter). The existing edit:enzyme tag predates the restoration narrative (it was for structural exploration). The edit:structural tag marks this exception so the design doc + Phase 7 know CS is structural-exploration, NOT disease-restoration. Do NOT add a DIS-*-cand claim_id (no disease claim). Do NOT remove the edit:enzyme tag (keep it; the human may later decide to remove it - flagged in the checkpoint).

Do NOT touch text fields, on_enter, or the existing 2 choices of tca.citrate_synthase. Do NOT touch tca.shuffle, tca.co2_turn1/2, tca.divert_to_good (already multi-choice). Preserve JSON formatting.
  </action>
  <verify>
1. python3.6 -c "import json; d=json.load(open('data/story_glucose/tca.json')); n=d['nodes']; assert len(n['tca.entry']['choices'])==2; assert len(n['tca.akg_dh']['choices'])==2; assert 'edit:enzyme' not in str(n['tca.akg_dh'].get('tags',[])); assert 'edit:structural' in n['tca.citrate_synthase']['tags']; assert 'edit:enzyme:tca.citrate_synthase' in n['tca.citrate_synthase']['tags']; assert len(n['tca.citrate_synthase']['choices'])==2; assert not any('DIS-' in c for c in n['tca.citrate_synthase']['claim_ids']); print('TCA MC+reframe OK')"
2. python3.6 -m unittest tests.test_glucose_reachability -v  (all 5 tests PASS)
  </verify>
  <done>
tca.entry and tca.akg_dh each have 2 choices (Continue + Observe); tca.akg_dh is NOT edit-allowed; tca.citrate_synthase carries the edit:structural tag (reframed) and keeps its edit:enzyme tag + 2 choices; reachability GREEN.
  </done>
</task>

</tasks>

<verification>
- python3.6 -m unittest tests.test_glucose_reachability -v  (5 tests PASS; node count 43; 11 endings; 4 tiers reachable)
- python3.6 -c "from c14.story.graph import StoryGraph; g=StoryGraph.load('data/story_glucose'); n=g.all_nodes(); assert len(n)==43; ea=[k for k,v in n.items() if any(t.startswith('edit:enzyme:') for t in v.tags)]; print('edit-allowed nodes in TCA file:', [k for k in ea if k.startswith('tca.')])"
- tca.json has 14 nodes (unchanged count). The 4 new edit:offer choices all goto edit.prompt (exists in bad_endings.json) - no orphaned endings.
- IDH3A is the DEFAULT (DIS-IDH3A-01-cand); the IDH1 alternative is NOT in the skeleton (documented in the design doc + checkpoint only).
</verification>

<success_criteria>
- 4 TCA enzymes promoted to edit-allowed (isocitrate_dh [IDH3A default], succinyl_coa_synthetase, fumarase, malate_dh) with edit:enzyme tag + edit:offer choice + DIS-*-cand claim.
- tca.aconitase carries DIS-ACO2-01-cand alongside its APPROVED prochirality claim.
- tca.entry + tca.akg_dh each have 2 choices; tca.akg_dh is NOT edit-allowed (no confirmed missense).
- tca.citrate_synthase carries edit:structural (reframed; keeps edit:enzyme + 2 choices; NO DIS-*-cand).
- tca.malate_dh has 3 choices (loop + forward + edit:offer; NO Observe - it was already 2-choice).
- IDH3A is the DEFAULT; IDH1 alternative is flagged for human review, NOT decided in the skeleton.
- Reachability GREEN; node count 43; zero regression.
</success_criteria>

<output>
After completion, create .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-08-SUMMARY.md recording: the 8 node modifications (aconitase claim add, 4 promotions, 2 MC conversions, CS reframe), the IDH3A-default/IDH1-alternative flag, the akg_dh NOT-promoted rationale, the CS edit:structural reframe, and the GREEN reachability confirmation.
</output>
