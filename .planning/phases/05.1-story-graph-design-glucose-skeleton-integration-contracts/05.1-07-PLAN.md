---
phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - data/story_glucose/glycolysis.json
  - data/story_glucose/pyruvate_branch.json
autonomous: true

must_haves:
  truths:
    - "gly.pyruvate_kinase has the edit:enzyme:gly.pyruvate_kinase tag + an edit:offer choice (goto edit.prompt) + DIS-PKLR-01-cand in claim_ids + 3 choices (Continue + Observe + edit:offer)"
    - "gly.start, gly.g6p, gly.fbp_to_pyruvate, gly.pyruvate each have >=2 choices (the existing Continue + a new Observe choice going to the SAME next node)"
    - "anaer.ldh has >=2 choices (its existing choice + a new Observe choice going to anaer.lactic) and is NOT edit-allowed"
    - "pyr.pdh on_enter loads pdb:6CFO (NOT 2OZL) + claim_ids include DIS-PDHA1-01-cand and CAST-PDH-WT-PDB-01-cand"
    - "gly.pfk claim_ids include DIS-PFKM-01-cand (added alongside the existing APPROVED GLY-PFK-01 + CAST-PFK-PDB-01)"
    - "the skeleton still loads + reachability stays GREEN (no node added/removed; node count still 43; new choices goto existing nodes)"
  artifacts:
    - path: "data/story_glucose/glycolysis.json"
      provides: "5 modified glycolysis nodes (gly.start, gly.g6p, gly.fbp_to_pyruvate, gly.pyruvate, gly.pyruvate_kinase) + gly.pfk claim_id add"
      contains: "DIS-PKLR-01-cand"
    - path: "data/story_glucose/pyruvate_branch.json"
      provides: "pyr.pdh cast-PDB fix + claim_id add; anaer.ldh MC conversion"
      contains: "pdb:6CFO"
  key_links:
    - from: "gly.pyruvate_kinase edit:offer choice"
      to: "edit.prompt"
      via: "choice.goto"
      pattern: "edit\\.prompt"
    - from: "pyr.pdh on_enter"
      to: "pdb:6CFO"
      via: "MolAction load target"
      pattern: "pdb:6CFO"
---

<objective>
Apply the disease-mutant research + Continue-to-MC directive to the GLYCOLYSIS + PYRUVATE BRANCH skeleton regions: promote gly.pyruvate_kinase to edit-allowed (PKLR R479H), add a second "Observe" choice to the 5 single-Continue glycolysis nodes + anaer.ldh, fix the pyr.pdh cast PDB (2OZL phospho-mimic to 6CFO wild-type), and add the DIS-*-cand disease-claim references to gly.pfk, gly.pyruvate_kinase, and pyr.pdh.

Purpose: Driver 1 (disease-mutant research) + Driver 2 (Continue-to-MC) of the Phase 5.1 replan. This is STRUCTURE ONLY: no text_dramatic/text_teaching authoring, no edits.json entries, no on_enter PDB-load content beyond the cast-PDB id swap. The 2OZL-to-6CFO fix is critical: 2OZL is the S264E phospho-mimic mutant (verified via RCSB title), NOT a wild-type structure; the PDH cast must be a real WT structure.
Output: 2 modified skeleton JSON files (glycolysis.json, pyruvate_branch.json) with 8 net node modifications; node count unchanged (43).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-REPLAN-CONTEXT.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-RESEARCH-disease-mutants-glycolysis.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md
@data/story_glucose/glycolysis.json
@data/story_glucose/pyruvate_branch.json
@data/story_glucose/manifest.json
@c14/story/model.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Glycolysis - promote gly.pyruvate_kinase + Continue-to-MC for 4 linear nodes + gly.pfk claim_id</name>
  <files>data/story_glucose/glycolysis.json</files>
  <action>
Edit data/story_glucose/glycolysis.json. All changes are additive (no node added/removed; node count stays 6 in this file). Use the existing skeleton's conventions: choices carry a tags array; edit:offer choices goto edit.prompt; mc:observe is the NEW tag marking the Observe-pattern second choice (same destination as Continue; different EXPERIENCE not different DESTINATION; the biochemistry is fixed). Choice labels are structural placeholders (Phase 7 finalizes text).

1. gly.pfk - claim_id add only (already edit-allowed, already 2 choices; NO MC change):
   - claim_ids: change ["GLY-PFK-01", "CAST-PFK-PDB-01"] to ["GLY-PFK-01", "CAST-PFK-PDB-01", "DIS-PFKM-01-cand"]
   - Rationale: PFKM G209D-to-D209G is the confirmed disease point mutant (UniProt P08237, OMIM 232800 GSD VII/Tarui). DIS-PFKM-01-cand is a CANDIDATE claim pending Phase 7 approval (do NOT mark approved).

2. gly.start - Continue-to-MC (add Observe choice, same destination):
   - choices: change the single-element array to a 2-element array:
     [{"label": "Continue", "goto": "gly.g6p"}, {"label": "Examine the glucose substrate", "goto": "gly.g6p", "tags": ["mc:observe"]}]

3. gly.g6p - Continue-to-MC:
   - choices: [{"label": "Continue", "goto": "gly.pfk"}, {"label": "Examine the G6P structure", "goto": "gly.pfk", "tags": ["mc:observe"]}]

4. gly.fbp_to_pyruvate - Continue-to-MC:
   - choices: [{"label": "Continue", "goto": "gly.pyruvate_kinase"}, {"label": "Examine the triose intermediates", "goto": "gly.pyruvate_kinase", "tags": ["mc:observe"]}]

5. gly.pyruvate - Continue-to-MC:
   - choices: [{"label": "Continue", "goto": "pyr.branch"}, {"label": "Examine the pyruvate branch point", "goto": "pyr.branch", "tags": ["mc:observe"]}]

6. gly.pyruvate_kinase - PROMOTE to edit-allowed + Continue-to-MC (becomes a 3-choice node):
   - tags: change ["stage:glycolysis"] to ["stage:glycolysis", "edit:enzyme:gly.pyruvate_kinase"]
   - claim_ids: change ["PLACEHOLDER_PHASE7_GLYCOLYSIS"] to ["PLACEHOLDER_PHASE7_GLYCOLYSIS", "DIS-PKLR-01-cand"]
     - Rationale: PKLR R479H-to-H479R (dbSNP rs118204085, OMIM 266200 CNSHA2) is the confirmed disease point mutant. The REPLAN-CONTEXT's "R479W" was CORRECTED by the research to R479H (the verified Amish mutation, Valentini 2002). DIS-PKLR-01-cand is CANDIDATE pending Phase 7.
   - choices: change the single-element array to a 3-element array:
     [{"label": "Continue", "goto": "gly.pyruvate"}, {"label": "Examine the pyruvate kinase active site", "goto": "gly.pyruvate", "tags": ["mc:observe"]}, {"label": "Try to edit pyruvate kinase", "goto": "edit.prompt", "tags": ["edit:offer"]}]
   - NOTE: the existing on_enter load pdb:7FS3 stays UNCHANGED (7FS3 is a CANDIDATE WT human PKLR structure pending Phase 7 approval; the research confirmed it is WT, not a mutant).

Do NOT touch text_dramatic/text_teaching (Phase 7 content). Do NOT modify on_enter MolActions (the cast PDB for gly.pyruvate_kinase stays 7FS3). Preserve JSON formatting (2-space indent, matching the existing file).
  </action>
  <verify>
Run from repo root:
1. python3.6 -c "import json; d=json.load(open('data/story_glucose/glycolysis.json')); n=d['nodes']; assert len(n)==6; assert 'edit:enzyme:gly.pyruvate_kinase' in n['gly.pyruvate_kinase']['tags']; assert 'DIS-PKLR-01-cand' in n['gly.pyruvate_kinase']['claim_ids']; assert 'DIS-PFKM-01-cand' in n['gly.pfk']['claim_ids']; assert len(n['gly.pyruvate_kinase']['choices'])==3; assert any(c.get('goto')=='edit.prompt' for c in n['gly.pyruvate_kinase']['choices']); assert all(len(n[k]['choices'])>=2 for k in ['gly.start','gly.g6p','gly.fbp_to_pyruvate','gly.pyruvate']); assert n['gly.pfk']['on_enter'][1]['target']=='pdb:4PFK'; print('glycolysis.json OK')"
2. python3.6 -m unittest tests.test_glucose_reachability -v  (all 5 tests PASS; node count still 43; the new edit:offer choice on gly.pyruvate_kinase goto's edit.prompt which exists, so no orphaned endings)
  </verify>
  <done>
glycolysis.json has 6 nodes (unchanged count); gly.pyruvate_kinase is edit-allowed (tag + edit:offer + DIS-PKLR-01-cand) with 3 choices; gly.start/g6p/fbp_to_pyruvate/pyruvate each have 2 choices; gly.pfk carries DIS-PFKM-01-cand; reachability tests GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: Pyruvate branch - pyr.pdh cast-PDB fix + claim_ids; anaer.ldh Continue-to-MC</name>
  <files>data/story_glucose/pyruvate_branch.json</files>
  <action>
Edit data/story_glucose/pyruvate_branch.json. Node count stays 7 in this file (no add/remove).

1. pyr.pdh - cast-PDB fix + claim_id add (already edit-allowed, already 2 choices; NO MC change):
   - on_enter: change the load target from {"op": "load", "target": "pdb:2OZL", "args": {"object": "pdh"}} to {"op": "load", "target": "pdb:6CFO", "args": {"object": "pdh"}}.
     - Rationale (from 05.1-RESEARCH-disease-mutants-glycolysis.md sections 2.3 + 4.3): RCSB confirms 2OZL's title is "Human pyruvate dehydrogenase S264E variant" - a phospho-mimic mutant engineered to mimic regulatory phosphorylation, NOT a wild-type structure. 6CFO (2.70 A, human PDHA1 + acetyl phosphinate substrate analog) is the verified WILD-TYPE structure from the same paper (Whitley 2018) as the mutant 6CER. 6CFO is a CANDIDATE pending Phase 7 approval.
   - claim_ids: change ["PLACEHOLDER_PHASE7"] to ["PLACEHOLDER_PHASE7", "DIS-PDHA1-01-cand", "CAST-PDH-WT-PDB-01-cand"]
     - DIS-PDHA1-01-cand = the disease claim (PDHA1 V138M = UniProt V167M, dbSNP rs2063174067, PDHAD OMIM 312170; reverse-mutation M138V-to-V138M is the "correct fix" the player performs - but the edits.json ENTRY is Phase 7 content, NOT this replan).
     - CAST-PDH-WT-PDB-01-cand = the WT-cast-PDB claim (6CFO). The mutant-PDB reference 6CER (CAST-PDH-MUTANT-PDB-01-cand) is Phase 7 content - do NOT add it to the skeleton in this replan (the skeleton loads ONE cast PDB per node; the mutant-reveal approach is a Phase 7 on_enter-content decision).
     - Both are CANDIDATE pending Phase 7 approval.

2. anaer.ldh - Continue-to-MC (add Observe choice, same destination; NOT promoted - GSD11 is a frameshift not a point mutation per research, so NO edit:enzyme tag, NO edit:offer, NO DIS-*-cand claim_id):
   - choices: change the single-element array to a 2-element array:
     [{"label": "Continue to the lactate ending", "goto": "anaer.lactic"}, {"label": "Examine the LDH active site", "goto": "anaer.lactic", "tags": ["mc:observe"]}]

Do NOT touch pyr.branch (already 2 cond-gated choices), anaer.entry (already 3 choices), or the 3 anaer endings (anaer.lactic/ethanolic/crisis - they are is_ending nodes with empty choices). Do NOT modify text fields. Preserve JSON formatting.
  </action>
  <verify>
1. python3.6 -c "import json; d=json.load(open('data/story_glucose/pyruvate_branch.json')); n=d['nodes']; assert len(n)==7; assert n['pyr.pdh']['on_enter'][1]['target']=='pdb:6CFO'; assert 'DIS-PDHA1-01-cand' in n['pyr.pdh']['claim_ids']; assert 'CAST-PDH-WT-PDB-01-cand' in n['pyr.pdh']['claim_ids']; assert n['pyr.pdh']['on_enter'][1]['target']!='pdb:2OZL'; assert len(n['anaer.ldh']['choices'])==2; assert 'edit:enzyme' not in str(n['anaer.ldh'].get('tags',[])); print('pyruvate_branch.json OK')"
2. python3.6 -m unittest tests.test_glucose_reachability -v  (all 5 tests PASS)
  </verify>
  <done>
pyr.pdh on_enter loads pdb:6CFO (not 2OZL) + carries DIS-PDHA1-01-cand + CAST-PDH-WT-PDB-01-cand; anaer.ldh has 2 choices (Continue + Observe) and is NOT edit-allowed; reachability GREEN.
  </done>
</task>

</tasks>

<verification>
- python3.6 -m unittest tests.test_glucose_reachability -v  (5 tests PASS: node count 43, 11 endings, 4 tiers reachable, red variants still red, no pymol import)
- python3.6 -c "from c14.story.graph import StoryGraph; g=StoryGraph.load('data/story_glucose'); assert len(g.all_nodes())==43; print('loads OK, 43 nodes')"
- No node was added or removed (43 total across 7 files). New choices goto EXISTING nodes (gly.g6p, gly.pfk, gly.pyruvate_kinase, gly.pyruvate, pyr.branch, anaer.lactic, edit.prompt) - all exist, so reachability is preserved.
- The 2OZL-to-6CFO swap is the only on_enter change; 6CFO is a CANDIDATE PDB id (not yet in data/citations.json - Phase 7 approves it; the citation gate is NOT run during Phase 5.1).
</verification>

<success_criteria>
- gly.pyruvate_kinase is edit-allowed (edit:enzyme tag + edit:offer choice to edit.prompt + DIS-PKLR-01-cand claim) with 3 choices (Continue + Observe + edit:offer).
- 4 glycolysis linear nodes (gly.start, gly.g6p, gly.fbp_to_pyruvate, gly.pyruvate) each have at least 2 choices.
- anaer.ldh has at least 2 choices and is NOT edit-allowed (GSD11 is frameshift, not point mutation).
- pyr.pdh on_enter loads pdb:6CFO (wild-type), not pdb:2OZL (phospho-mimic mutant); claim_ids carry DIS-PDHA1-01-cand + CAST-PDH-WT-PDB-01-cand.
- gly.pfk carries DIS-PFKM-01-cand alongside its APPROVED claims.
- Reachability tests GREEN; node count 43; zero regression.
</success_criteria>

<output>
After completion, create .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-07-SUMMARY.md recording: the 8 node modifications (gly.pfk claim add, gly.start/g6p/fbp_to_pyruvate/pyruvate MC, gly.pyruvate_kinase promote+MC, pyr.pdh cast fix+claim, anaer.ldh MC), the 2OZL-to-6CFO rationale, the R479H (not R479W) correction, the 0-nodes-added fact, and the GREEN reachability confirmation.
</output>
