---
phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts
verified: 2026-08-22T07:55:32Z
status: passed
score: 5/5 must-haves verified
re_verification: No — initial verification
---

# Phase 5.1: Story Graph Design — Glucose Skeleton + Integration Contracts Verification Report

**Phase Goal:** The real glucose story graph is designed as a reviewable JSON skeleton — every node, edge, choice point (mapped to a real pathway branch point), RNG-weighted node, edit-allowed node (with routing target), and ending attachment is specified and validated — AND the two integration contracts that connect the story to gameplay are defined: (a) the choice-point contract (how a text multiple-choice selection maps to a graph edge) and (b) the edit-node contract (which story nodes allow edits, what EditIntent each player-edit-action generates, and how the EditRouter's known/unknown outcome routes back into the graph). This de-risks Phases 6–7 by making the story structure + gameplay integration explicit design artifacts before any UI or cited content is built on top.

**Verified:** 2026-08-22T07:55:32Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

The Phase 5.1 success criteria map to 5 observable truths. All verified by structural inspection + test execution against the ACTUAL codebase (not SUMMARY claims).

| #   | Truth (SC) | Status | Evidence |
| --- | ---------- | ------ | -------- |
| 1   | SC1 — A glucose story graph skeleton JSON exists with every node typed, every choice point annotated with the real pathway branch point it maps to, and all four ending tiers attached — no invented branches (human-review the mapping against spec.md + approved sources) | ✓ VERIFIED | 7 JSON files + manifest.json in `data/story_glucose/`; `StoryGraph.load` returns 54 nodes; endings = 1 True + 3 Good + 2 Normal + 14 Bad = 20 (verified via `g.endings()`). Node types derived from fields per DESIGN.md §2 SC1 taxonomy (story/choice/rng-shuffle/edit-allowed/ending as a SET of roles — multi-role `tca.shuffle` is rng-shuffle + choice + edit-allowed). SC5 table "pathway branch point mapped" column annotates every choice node; real branches confirmed (pyruvate fate O2-dependent → acetyl-CoA vs lactate/ethanol; TCA citrate prochirality RNG shuffle [Ogston]; cataplerotic exit citrate→FA/OAA→Asp/α-KG→Glu; anaerobic 3-ending branch); narrative/meta branches explicitly flagged "NOT a pathway branch" (intro.preface, intro.shell_glucose) or "NARRATIVE-FRAMING FLAG §9 OQ2" (etc.entry, end.normal.co2). DESIGN.md line 1322: "The branch-point MAPPING is real (not invented); the CLAIMS are pending." Human review APPROVED (SC1 human-review COMPLETE — see 05.1-06-SUMMARY.md `provides:` + line 27: "branch-point mapping confirmed against spec.md + approved sources; no invented branches"). |
| 2   | SC2 — The Phase 2 reachability checker runs GREEN on the glucose skeleton (all four ending tiers reachable from the start) and RED on a deliberately-orphaned variant — the checker now guards real content, not just the toy graph | ✓ VERIFIED | `tests/test_glucose_reachability.py` (9 tests, all pass via `python3.6 -m unittest tests.test_glucose_reachability -v`): `test_reachability_green_all_four_tiers` (GREEN — all 4 tiers reachable, 20 endings, `rep.is_ok` True, `unreachable_endings == []`), `test_reachability_red_orphaned_true_ending` (RED variant 1 — orphaning end.true → `is_ok` False), `test_reachability_red_extra_orphaned_ending` (RED variant 2 — extra orphaned ending → unreachable). Tests import `check_reachability` from `c14.story.validate` — the SAME Phase 2 checker (provenance confirmed: Phase 2 Plan 02-03 SUMMARY line 14: "c14.story.validate.check_reachability: pure BFS reachability checker ... Phase 2 success criterion #4"). Full suite `python3.6 -m unittest discover -s tests` = 207 tests OK. The checker now guards the real 54-node glucose skeleton (was the 3-node toy graph in Phase 2). |
| 3   | SC3 — The choice-point contract is documented: for each choice node, the skeleton specifies the player-facing options, the edge each selects, and the MolActions fired on traversal | ✓ VERIFIED | `05.1-DESIGN.md §4 Choice-point contract (SC3)` (lines 390-505). 5-facet spec per choice node: (a) player-facing options [Choice.label], (b) edge each selects [Choice.goto], (c) MolActions fired on traversal [choice.effects then target.on_enter], (d) cond gating, (e) weight/RNG. Per-choice-node table covers: 7 MC-choice nodes (intro.preface, intro.shell_glucose, intro.select, pyr.branch, anaer.entry, tca.divert_to_good, etc.entry, edit.prompt) + 1 RNG-shuffle node (tca.shuffle) + 5 edit-allowed-class representative rows. JSON content VERIFIED to match the contract table exactly: `pyr.branch` has 2 cond-gated choices ("Proceed aerobically..." → pyr.pdh with cond `not flags.host_o2_low`; "The air runs out — ferment" → anaer.entry with cond `flags.host_o2_low` + effects {set: {anaerobic: true}}); `tca.shuffle` has 4 choices (turn1 weight 0.5 → tca.co2_turn1, turn2 weight 0.5 → tca.co2_turn2, edit:offer → edit.prompt, cycle-trap cond `visits.get('tca.shuffle', 0) > 5` → bad.cycle_trap_host_death). The contract is an explicit reviewable spec, not implicit in interpreter code. |
| 4   | SC4 — The edit-node contract is documented: the skeleton marks which story nodes allow edits, the EditIntent each player-edit-action generates, and the branch/bad-ending node the EditRouter routes to (known → defined branch node, unknown → bad-ending pool node) | ✓ VERIFIED | `05.1-DESIGN.md §5 Edit-node contract (SC4)` (lines 507-786). 14-row per-edit-allowed-node table (lines 643-658): columns = node_id, enzyme_id, real enzyme + PDB, DIS-*-cand CANDIDATE claim, reverse-mutation signature (NAME ONLY), known-edit branch route (`edit.prompt → EditRouter → branch node (Phase 7)`), unknown-edit fallback (`bad.lost_connection / bad.released_from_host`), SC5 class. 14 edit-allowed nodes = 12 disease-mutant + 1 structural (tca.citrate_synthase edit:structural, NO disease mutant) + 1 shared (tca.shuffle, edit:enzyme:tca.aconitase). The `edit.prompt` structural stub has exactly 12 choice.goto edges (verified in JSON): 1 edit:known_critical → bad.critical_residue_break + 8 edit:unknown → bad.lost_connection/released_from_host/enzyme_collapse/wrong_substrate_trapped/broken_active_site/lost_in_cytosol/proton_leak/misfolded_aggregate + 3 edit:known → bad.active_site_destroyed/substrate_channel_blocked/cofactor_lost. Runtime routing mechanism WIRED: `c14/edit_router.py:105 class EditRouter` (Phase 4) + `c14/engine.py:135 def apply_player_edit` — the EditRouter routes known → branch node, unknown → bad-ending pool, BYPASSING the stub's structural choices (stub exists so BFS sees all 12 bad endings reachable). |
| 5   | SC5 — Every node is explicitly classified as MC-choice, edit-allowed, both, or neither | ✓ VERIFIED | `05.1-DESIGN.md §6 Per-node classification table (54 rows — one per node in the skeleton)` (lines 887-950). Exactly 54 data rows (1 header + 54 = 55 `^\| ` lines). Each row has the SC5 class column with one of the 4 classes. SC5 class distribution: MC-choice=18, edit-allowed=5, both=9, neither=22, total=54 (matches the node count). All 4 classes used. SC5 derivation rule documented (line 789): "gameplay design decision per node" — orthogonal to the structural type SET (SC1). The 14 edit-allowed nodes = 5 edit-allowed-class (1 non-edit choice) + 9 both-class (≥2 non-edit choices + edit:offer) — matches the SC4 table. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `data/story_glucose/manifest.json` | bundle index (version, default_seed, start, files[]) | ✓ VERIFIED | 234 bytes; `{"version":1, "default_seed":0, "start":"intro.preface", "files":[7 files]}` — verified by reading |
| `data/story_glucose/intro.json` | intro stage nodes (preface, select, shell_glucose, fa.stub, alc.stub) | ✓ VERIFIED | 5 nodes; intro.preface (2 choices), intro.select (3 choices), intro.shell_glucose (2 choices), fa.stub (1), alc.stub (1) |
| `data/story_glucose/glycolysis.json` | glycolysis stage nodes | ✓ VERIFIED | 6 nodes (gly.start, gly.g6p, gly.pfk [edit-allowed], gly.fbp_to_pyruvate, gly.pyruvate_kinase [both], gly.pyruvate) |
| `data/story_glucose/pyruvate_branch.json` | pyruvate fate + anaerobic branch | ✓ VERIFIED | 7 nodes (pyr.branch, pyr.pdh [edit-allowed], anaer.entry, anaer.ldh, anaer.lactic [good], anaer.ethanolic [normal], anaer.crisis [bad]) |
| `data/story_glucose/tca.json` | TCA cycle + RNG shuffle + cataplerotic exit | ✓ VERIFIED | 14 nodes (tca.entry, tca.citrate_synthase [edit-allowed structural], tca.aconitase, tca.shuffle [both], tca.co2_turn1/2, tca.isocitrate_dh [both], tca.akg_dh, tca.succinyl_coa_synthetase [both], tca.fumarase [both], tca.malate_dh [both], tca.divert_to_good) |
| `data/story_glucose/etc_atp.json` | ETC + ATP synthase + True ending | ✓ VERIFIED | 7 nodes (etc.entry, etc.complex_i/ii/iii/iv, etc.atp_synthase, end.true [true]) |
| `data/story_glucose/endings.json` | Good + Normal endings | ✓ VERIFIED | 3 nodes (end.good.fatty_acid, end.good.amino_acid, end.normal.co2) |
| `data/story_glucose/bad_endings.json` | Bad endings + edit.prompt stub | ✓ VERIFIED | 14 nodes (13 bad endings + edit.prompt 12-choice stub) |
| `tests/test_glucose_reachability.py` | SC2 green/red + 3 invariants + bad-ending-reachability (9 tests) | ✓ VERIFIED | 352 lines, 9 test methods; imports `StoryGraph`, `check_reachability`, `Node`; all 9 pass |
| `.planning/.../05.1-DESIGN.md` | choice-point contract §4 + edit-node contract §5 + SC5 classification §6 | ✓ VERIFIED | 1708 lines; §1 Overview, §2 SC1 taxonomy, §3 file split + manifest, §4 SC3 contract (5-facet + per-choice-node table), §5 SC4 contract (14-row table + edit.prompt stub), §6 SC5 table (54 rows), §7 SC2 reachability, §8 backward-compat, §9 open questions |
| `.planning/.../05.1-GRAPH-DIAGRAM.md` | graph topology diagram doc | ✓ VERIFIED | 528 lines; Full Graph Topology, Branch-Point Mapping (SC1 human-review), Ending-Tier Distribution, Edit-Allowed Nodes (SC4), expansions, Disease-Mutant Replan Changes, Bad-Ending + Preface Expansion, Key Design Decisions Locked |
| `.planning/.../05.1-graph.svg` | visual graph diagram | ✓ VERIFIED | 49403 bytes; valid minified SVG (starts `<?xml ... ?><svg ...>`, ends `</svg>`); viewBox 3132×1450 |
| `.planning/.../05.1-graph.txt` | ASCII graph diagram | ✓ VERIFIED | 368 lines |
| `.planning/.../05.1-06-SUMMARY.md` | human review APPROVAL record | ✓ VERIFIED | 204 lines; frontmatter `provides:` explicitly lists "Human APPROVAL of the FINAL post-expansion 54-node glucose skeleton (no fixes required)", "SC1 human-review COMPLETE (branch-point mapping confirmed against spec.md + approved sources; no invented branches)", "SC4 design-review COMPLETE", "SC2 + invariants CONFIRMED (9 reachability tests PASS; 207 full suite PASS)"; 3 CRITICAL disease-mutant decisions confirmed as defaults (IDH3A, 6CFO cast, CS edit:structural); 32 OQs disposed; skeleton FROZEN |
| 16 PLAN.md + 16 SUMMARY.md files | all plans complete | ✓ VERIFIED | `ls *-PLAN.md | wc -l` = 16; `ls *-SUMMARY.md | wc -l` = 17 (16 numbered + 1 EXPANSION-SUMMARY); every plan 01-16 has a matching SUMMARY |
| `c14/story/graph.py:StoryGraph` | the loader (Phase 2) | ✓ VERIFIED | 126 lines; `StoryGraph.load(story_dir)` reads manifest + merges files via `Node.from_dict`; raises ValueError on duplicate node id; `start_node()`, `endings()`, `all_nodes()` |
| `c14/story/validate.py:check_reachability` | the Phase 2 reachability checker (reused for glucose) | ✓ VERIFIED | Phase 2 Plan 02-03 provenance confirmed; BFS over choice.goto edges; `ReachabilityReport.is_ok` + `unreachable_endings`; reused unchanged on the real 54-node skeleton |
| `c14/edit_router.py:EditRouter` | the EditRouter (Phase 4) — SC4 routing mechanism | ✓ VERIFIED | `class EditRouter` at line 105; referenced by SC4 contract §5 as the runtime routing mechanism |
| `c14/engine.py:apply_player_edit` | the engine edit-routing entry point | ✓ VERIFIED | `def apply_player_edit(self, edit_intent, enzyme_id)` at line 135; the SC4 contract's "player edit → routed node → applied edit" 5-step data-flow entry |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| `data/story_glucose/manifest.json` | `data/story_glucose/{intro,glycolysis,pyruvate_branch,tca,etc_atp,endings,bad_endings}.json` | `StoryGraph.load` reads `manifest.files` + merges via `Node.from_dict` | ✓ WIRED | `StoryGraph.load('data/story_glucose/')` returns 54 nodes with no ValueError (duplicate-id guard); `g.start_node()` = "intro.preface"; `g.version`=1, `g.default_seed`=0 — all from manifest |
| `tests/test_glucose_reachability.py` | `c14/story/validate.py:check_reachability` | `from c14.story.validate import check_reachability` + `check_reachability(g.all_nodes(), g.start_node())` | ✓ WIRED | 4 of 9 tests call `check_reachability` (green, 2 reds, bad-ending-reachability); import confirmed at test file line 46; all pass |
| `tests/test_glucose_reachability.py` | `c14/story/graph.py:StoryGraph` | `from c14.story.graph import StoryGraph` + `StoryGraph.load(GLUCOSE_STORY_DIR)` | ✓ WIRED | Every test loads the real skeleton via `StoryGraph.load`; import at test file line 45 |
| `edit.prompt` stub (bad_endings.json) | 12 edit-related bad endings | 12 `choice.goto` edges (structural BFS paths) | ✓ WIRED | `test_all_new_bad_endings_reachable_from_edit_prompt` verifies each of the 9 new bad endings has an incoming `edit.prompt` choice.goto; JSON confirmed: 12 choices (1 edit:known_critical + 8 edit:unknown + 3 edit:known); stub exists so BFS sees all 12 bad endings reachable (SC2) |
| 14 edit-allowed nodes | `edit.prompt` via `edit:offer` choice | each edit-allowed node has a choice with `edit:offer` tag OR `goto == "edit.prompt"` | ✓ WIRED | `test_14_edit_allowed_nodes` asserts each of the 14 edit-allowed nodes has an edit:offer choice; verified in JSON (gly.pfk, pyr.pdh, tca.citrate_synthase, tca.aconitase, etc.complex_i have 2 choices [continue + edit:offer]; the 9 both-class nodes have 3-4 choices including edit:offer) |
| `c14/edit_router.py:EditRouter` | `c14/engine.py:apply_player_edit` | runtime edit routing (known → branch node, unknown → bad-ending pool) | ✓ WIRED | `class EditRouter` at edit_router.py:105; `def apply_player_edit` at engine.py:135 (referenced in engine.py:76 docstring: "An optional edit_router (Phase 4) enables apply_player_edit"); SC4 contract §5 documents this as the runtime path that BYPASSES the edit.prompt stub's structural choices |
| Real pathway branch points (spec.md + approved sources) | `pyr.branch`, `tca.shuffle`, `tca.divert_to_good`, `anaer.entry`, `etc.entry` choice nodes | SC5 table "pathway branch point mapped" column + human review | ✓ WIRED | Human review APPROVED the mapping (05.1-06-SUMMARY.md: "branch-point mapping confirmed against spec.md + approved sources; no invented branches"); real branches: pyruvate fate O2-dependent, TCA citrate prochirality (Ogston, TCA-RNG-WEIGHT-01 + TCA-RNG-CITRATE-PROCHIRALITY-01 APPROVED), cataplerotic exit, anaerobic 3-ending; narrative/meta branches explicitly flagged |

### Requirements Coverage

Phase 5.1 owns no requirements directly (design phase; enables STORY-01, STORY-03, STORY-06, STORY-07, EDIT-04 delivery in Phases 6–7 per ROADMAP). The phase's success is measured by its 5 SCs (the truths above), all of which are verified.

| Requirement (enabled) | Status | Evidence |
| --------------------- | ------ | -------- |
| STORY-01 (glucose pathway graph includes all 4 ending tiers + reachability checker confirms all 4 reachable) — enabled for Phase 6 | ✓ SATISFIED (design artifact ready) | SC1 + SC2 verified: 4 tiers attached (1T+3G+2N+14B=20), reachability GREEN confirms all 4 reachable from intro.preface |
| STORY-03 (choice-point contract) — enabled for Phase 6 | ✓ SATISFIED (design artifact ready) | SC3 verified: §4 contract with 5-facet spec + per-choice-node table |
| STORY-06 (edit-node contract) — enabled for Phase 6 | ✓ SATISFIED (design artifact ready) | SC4 verified: §5 contract with 14-row table + edit.prompt stub + EditRouter wiring |
| STORY-07 (per-node MC/edit classification) — enabled for Phase 6 | ✓ SATISFIED (design artifact ready) | SC5 verified: §6 54-row table with 4 classes (MC-choice/edit-allowed/both/neither) |
| EDIT-04 (edit routing) — enabled for Phase 6 | ✓ SATISFIED (design artifact ready) | SC4 verified: EditRouter + apply_player_edit wiring + 5-step data-flow documented |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `data/story_glucose/*.json` | (many) | 89 × `"TBD — ..."` text_dramatic/text_teaching placeholders | ℹ️ Info | INTENTIONAL — the skeleton is a topology-only design artifact; text content is explicitly Phase 7 (documented in DESIGN.md §5 "Phase 7 CONTENT boundary" + §8). Not a stub/anti-pattern; the SC requirements are about topology + branch-point mapping + contracts, NOT cited content. Human review APPROVED the skeleton with these placeholders. |
| `data/story_glucose/*.json` | (many) | `PLACEHOLDER_PHASE7*` + `DIS-*-cand` claim_ids | ℹ️ Info | INTENTIONAL — the skeleton REFERENCES candidates, does NOT assert disease as approved fact (AGENTS.md no-fabricated-science rule). 12 `DIS-*-cand` are CANDIDATE pending Phase 7 per-claim approval; 5 APPROVED claim_ids (GLY-PFK-01, CAST-PFK-PDB-01, CAST-CSC-PDB-01, TCA-RNG-CITRATE-PROCHIRALITY-01, TCA-RNG-WEIGHT-01) are in `data/citations.json`. Documented + approved boundary. |
| `data/story_glucose/etc_atp.json`, `tca.json` | `pyr.pdh`, `tca.aconitase` | `pdb:TBD_*` + `pdb:TBD_ACONITASE` placeholders | ℹ️ Info | INTENTIONAL — loud-fail-at-runtime until Phase 6/7 resolves them; NO fabricated PDB IDs (DESIGN.md line 1246-1248: "follow the TBD_ACONITASE precedent — NO fabricated PDB IDs"). |

**Blocker anti-patterns: NONE.** All flagged patterns are intentional, documented design decisions at the topology/content boundary (the skeleton carries STRUCTURE; Phase 7 carries CONTENT). No TODO/FIXME/XXX/HACK markers, no lorem/placeholder-content, no empty returns, no console.log-only implementations in the skeleton or test files.

### Human Verification Required

**The one human-verify item for this phase — SC1's "human-review the mapping against spec.md + approved sources" — was ALREADY COMPLETED in Plan 05.1-06 (2026-08-22) and recorded in `05.1-06-SUMMARY.md`.** The human APPROVED the final 54-node skeleton as-is (no fixes required), confirmed the 3 CRITICAL disease-mutant decisions as defaults (IDH3A M204I, 6CFO cast + mutant-reveal approach deferred to Phase 7, CS edit:structural reframe), and disposed of 32 open questions (13 original + 11 disease-mutant + 8 bad-endings/preface — deferred to Phase 7 / accepted as skeleton placeholders / confirmed as skeleton decisions). The skeleton is FROZEN at the approved state.

No further human verification is required for the Phase 5.1 goal. The following items are explicitly scoped to Phase 7 (NOT Phase 5.1 gaps):
- Per-enzyme bad-ending pool content (OQ-BAD-1/2/3)
- Cited text content (text_dramatic/text_teaching) for all 54 nodes
- Per-claim approval of the 12 `DIS-*-cand` disease-mutant candidates
- The mutant-reveal approach for pyr.pdh (apply-edit vs load-mutant-PDB)
- WT-aligned structure load convention (deferred to Phase 5.2/5.3 — separate INSERTED phases)

### Gaps Summary

**No gaps found.** All 5 success criteria (observable truths) are verified against the actual codebase:

- **SC1 (skeleton + branch-point mapping + 4 ending tiers + no invented branches):** 54 nodes across 7 JSON files + manifest; 20 endings (1T+3G+2N+14B); node types derived from fields (multi-role `tca.shuffle` = rng-shuffle + choice + edit-allowed); real pathway branch points mapped in SC5 table; narrative/meta branches explicitly flagged; human review APPROVED with "no invented branches" confirmation.
- **SC2 (Phase 2 reachability checker GREEN + RED on real content):** 9 reachability tests pass (GREEN: all 4 tiers reachable, 20 endings; RED variant 1: orphaned end.true; RED variant 2: extra orphaned ending); reuses the SAME Phase 2 `check_reachability` from `c14/story/validate.py` (Plan 02-03 provenance confirmed); full suite 207 tests pass.
- **SC3 (choice-point contract):** DESIGN.md §4 with 5-facet spec (player options / edge / MolActions / cond / weight) + per-choice-node table; JSON content verified to match the contract table exactly (pyr.branch 2 cond-gated, tca.shuffle 4 weighted/non-weighted).
- **SC4 (edit-node contract):** DESIGN.md §5 with 14-row per-edit-allowed-node table (enzyme_id, real enzyme + PDB, DIS-*-cand, reverse-mutation NAME ONLY, known-edit branch route, unknown-edit fallback, SC5 class); edit.prompt stub has exactly 12 structural choices (verified in JSON); runtime routing wired via `c14/edit_router.py:EditRouter` + `c14/engine.py:apply_player_edit`.
- **SC5 (per-node classification):** DESIGN.md §6 with exactly 54 rows (one per node); all 4 classes used (MC-choice=18, edit-allowed=5, both=9, neither=22, total=54); SC5 derivation rule documented; SC5 class orthogonal to SC1 structural type SET.

The skeleton is FROZEN at human approval (2026-08-22). The phase goal — designing the real glucose story graph topology + the two integration contracts as reviewable, validated design artifacts that de-risk Phases 6–7 — is fully achieved. Phase 5.2 + Phase 6 are UNBLOCKED.

---

_Verified: 2026-08-22T07:55:32Z_
_Verifier: OpenCode (gsd-verifier)_
