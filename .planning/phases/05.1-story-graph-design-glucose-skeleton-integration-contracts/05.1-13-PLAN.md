---
phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - data/story_glucose/intro.json
  - data/story_glucose/manifest.json
  - data/story_glucose/glycolysis.json
autonomous: true

must_haves:
  truths:
    - "the player sees a PREFACE beat (intro.preface) BEFORE character selection — the hero carbon atom is blessed with a superpower, shown alongside the 20 amino acids (the cast) in cartoon representation"
    - "the player sees a SHELL beat (intro.shell_glucose) AFTER selecting glucose but BEFORE the journey — the hero dons the glucose structure as its shell, shown with the 20 AA cast"
    - "on entering glycolysis (gly.start), the 20 AA are hidden and only the hero's glucose molecule remains (hide_all + show_as glucose — the 'shell' persists, the 'cast' steps back)"
    - "the manifest start is intro.preface (the new entry point); all 4 ending tiers remain reachable from intro.preface through the new chain intro.preface -> intro.select -> intro.shell_glucose -> gly.start -> ..."
    - "the 0 single-Continue invariant holds UNCHANGED — the 2 new preface nodes each get a Continue + an Observe (mc:observe) 2nd choice (same-destination, different-experience); the invariant test is NOT re-scoped"
    - "the 14 edit-allowed count UNCHANGED; the 4 ending tiers stay; ending count UNCHANGED (preface nodes are NOT endings); FA/Alc stubs unchanged (their shell nodes are Phase 8)"
  artifacts:
    - path: "data/story_glucose/intro.json"
      provides: "2 new preface nodes (intro.preface Beat A + intro.shell_glucose Beat B) + intro.select glucose goto rerouted to intro.shell_glucose"
      contains: "intro.preface"
    - path: "data/story_glucose/manifest.json"
      provides: "start node changed from intro.select to intro.preface (the preface is the new entry point)"
      contains: "intro.preface"
    - path: "data/story_glucose/glycolysis.json"
      provides: "gly.start on_enter expanded to hide_all + show_as glucose (re-show the hero's molecule after hiding the 20 AA cast)"
      contains: "show_as"
  key_links:
    - from: "intro.preface"
      to: "intro.select"
      via: "choice.goto (Continue + Observe, same destination)"
      pattern: "intro\\.select"
    - from: "intro.select (glucose choice)"
      to: "intro.shell_glucose"
      via: "choice.goto (rerouted from gly.start)"
      pattern: "intro\\.shell_glucose"
    - from: "intro.shell_glucose"
      to: "gly.start"
      via: "choice.goto (Continue + Observe)"
      pattern: "gly\\.start"
    - from: "gly.start on_enter"
      to: "the glucose object (loaded in intro.shell_glucose)"
      via: "show_as MolAction (re-show after hide_all hides the 20 AA)"
      pattern: "show_as"
---

<objective>
Add a story PREFACE sequence before the intro.select character-selection node, implementing Requirement 2 from the 05.1-06 checkpoint feedback. Two new visual story beats:

- Beat A (intro.preface, BEFORE character select): the hero carbon atom is blessed with a superpower, shown alongside the 20 amino acids (the cast) in cartoon representation (without terminal groups — cartoon naturally hides them).
- Beat B (intro.shell_glucose, AFTER selecting glucose, BEFORE the journey): the hero dons the glucose structure as its "shell"; then on entering glycolysis the 20 AA are hidden + only the hero's glucose remains.

Purpose: The user wants a narrative preface that establishes the hero + the cast before character selection, and a "shell" beat that visually bonds the hero to the chosen molecule before the journey begins. This de-risks Phase 6 by making the intro topology an explicit, reviewable skeleton artifact.

BOUNDARY (per AGENTS.md): the skeleton carries on_enter MolAction PLACEHOLDERS using the EXISTING MolOps op vocabulary (hide_all, load, show_as — see c14/pymol_layer/molops.py). The exact cmd.* args (representation, selection strings, the hero-atom load convention) are Phase 5.2/6. The TBD_* PDB targets follow the TBD_ACONITASE precedent (loud-fail at runtime until Phase 6/7 resolves them — NO fabricated PDB IDs). text_dramatic/text_teaching are Phase 7 (PLACEHOLDER_PHASE7 claim_ids). No new PDB IDs or DIS-*-cand claims are added to data/citations.json.

DECISIONS BAKED IN (documented in the actions + later in 05.1-DESIGN.md via Plan 15):
- Topology: intro.preface -> intro.select -> intro.shell_glucose -> gly.start (the preface is the new entry point; manifest start changes intro.select -> intro.preface).
- Per-character shell node (intro.shell_glucose), NOT parametric — less duplication for glucose; FA/Alc get their own shell nodes in Phase 8 (stubbed for now via the existing fa.stub/alc.stub unchanged).
- gly.start on_enter hides the 20 AA + re-shows glucose (hide_all + show_as glucose) — achieves "only the hero's molecule remains" WITHOUT adding a dedicated journey_start node (simpler; gly.start already does hide_all). The "show_as glucose" re-shows the glucose object loaded in intro.shell_glucose (objects persist across nodes — hide_all hides but does not delete).
- 0-single-Continue: the 2 new preface nodes get a Continue + an Observe (mc:observe) 2nd choice (same-destination, different-experience, per the Continue-to-MC pattern from Plans 07-09) — the invariant is UNCHANGED, NOT re-scoped.
- 20-AA cartoon: show_as with rep "cartoon" (cartoon naturally hides terminal groups — N-term/C-term; document in DESIGN; exact cmd is Phase 5.2/6).

Output: 3 modified files (intro.json +2 nodes + intro.select reroute; manifest.json start; glycolysis.json gly.start on_enter). Node count 52 -> 54 (after Plan 12) or 43 -> 45 (standalone); ending count unchanged (preface nodes are NOT endings).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-CHECKPOINT-FEEDBACK.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-EXPANSION-SUMMARY.md
@data/story_glucose/intro.json
@data/story_glucose/manifest.json
@data/story_glucose/glycolysis.json
@c14/pymol_layer/molops.py
@c14/story/model.py
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Beat A — add intro.preface node (pre-character-select) + change manifest start</name>
  <files>data/story_glucose/intro.json, data/story_glucose/manifest.json</files>
  <action>
Edit data/story_glucose/intro.json. Add a NEW node "intro.preface" to the "nodes" object (insert BEFORE the existing "intro.select" key so the JSON reads in narrative order). This node is the story preface — the hero's blessing — shown BEFORE character selection.

The intro.preface node (exact shape):
{
  "text_dramatic": "TBD — the hero, a single carbon atom, is blessed with a superpower: the destiny to travel the respiratory pathway and become ATP.",
  "text_teaching": "TBD — Phase 7 preface text (the hero's blessing + the 20-AA cast introduction).",
  "claim_ids": ["PLACEHOLDER_PHASE7"],
  "tags": ["stage:intro", "preface"],
  "on_enter": [
    {"op": "hide_all"},
    {"op": "load", "target": "pdb:TBD_HERO_CARBON", "args": {"object": "hero_atom"}},
    {"op": "show_as", "target": "hero_atom", "args": {"rep": "spheres"}},
    {"op": "load", "target": "pdb:TBD_AA_CAST_20", "args": {"object": "aa_cast"}},
    {"op": "show_as", "target": "aa_cast", "args": {"rep": "cartoon"}}
  ],
  "choices": [
    {"label": "Continue to character selection", "goto": "intro.select"},
    {"label": "Observe the hero and the cast", "goto": "intro.select", "tags": ["mc:observe"]}
  ]
}

Rationale for the on_enter placeholders (the skeleton carries op names + target objects, NOT full cmd args — Phase 5.2/6 resolves the exact cmd.*):
- hide_all: clear the scene (molops hide_all -> cmd.hide("everything","all")).
- load pdb:TBD_HERO_CARBON: the hero atom (a single carbon; the Phase 5.2 hero-highlight convention is TBD — pdb:TBD_* loud-fails at runtime until Phase 6/7, per the TBD_ACONITASE precedent; NO fabricated PDB ID).
- show_as hero_atom spheres: show the hero atom as spheres (the simplest "see the single atom" representation; Phase 5.2 may change this).
- load pdb:TBD_AA_CAST_20: the 20 amino-acid backbones (the cast the hero will meet; pdb:TBD_* placeholder).
- show_as aa_cast cartoon: the 20 AA in CARTOON representation (per the user requirement — cartoon naturally hides the terminal N-term/C-term groups; the exact selection/hide is Phase 5.2/6).

The 2 choices (Continue + Observe, both -> intro.select) keep the 0-single-Continue invariant (the node is NOT single-Continue; the Observe is the same-destination-different-experience pattern from Plans 07-09).

THEN edit data/story_glucose/manifest.json: change "start": "intro.select" to "start": "intro.preface" (the preface is the new entry point). Leave version, default_seed, and the files array UNCHANGED.

Do NOT modify intro.select, fa.stub, or alc.stub in this task (intro.select's glucose goto is rerouted in Task 2). Preserve 2-space indentation.
  </action>
  <verify>
1. python3.6 -c "import json; d=json.load(open('data/story_glucose/intro.json')); n=d['nodes']; assert len(n)==4, len(n); ip=n['intro.preface']; assert ip['tags']==['stage:intro','preface']; assert ip['claim_ids']==['PLACEHOLDER_PHASE7']; assert len(ip['choices'])==2; assert all(c['goto']=='intro.select' for c in ip['choices']); assert any('mc:observe' in c.get('tags',[]) for c in ip['choices']); ops=[m['op'] for m in ip['on_enter']]; assert ops==['hide_all','load','show_as','load','show_as']; assert ip['on_enter'][1]['target']=='pdb:TBD_HERO_CARBON'; assert ip['on_enter'][4]['args']['rep']=='cartoon'; print('intro.preface OK')"
2. python3.6 -c "import json; m=json.load(open('data/story_glucose/manifest.json')); assert m['start']=='intro.preface', m['start']; assert m['files']==['intro.json','glycolysis.json','pyruvate_branch.json','tca.json','etc_atp.json','endings.json','bad_endings.json']; print('manifest start -> intro.preface OK')"
3. python3.6 -c "from c14.story.graph import StoryGraph; g=StoryGraph.load('data/story_glucose'); assert g.start_node()=='intro.preface'; print('start node = intro.preface')"
  </verify>
  <done>
intro.json has 4 nodes (intro.preface + intro.select + fa.stub + alc.stub); intro.preface has 2 choices (Continue + Observe, both -> intro.select) + on_enter placeholders (hide_all + load hero atom spheres + load 20 AA cartoon); manifest start is intro.preface; StoryGraph.start_node() returns intro.preface.
  </done>
</task>

<task type="auto">
  <name>Task 2: Beat B — add intro.shell_glucose node (post-character-select) + reroute intro.select glucose goto + gly.start on_enter hide-20-AA</name>
  <files>data/story_glucose/intro.json, data/story_glucose/glycolysis.json</files>
  <action>
Edit data/story_glucose/intro.json (continuing from Task 1). Add a NEW node "intro.shell_glucose" to the "nodes" object (insert AFTER "intro.select" so the JSON reads in narrative order). This node is Beat B — the atom gets its shell after the player selects glucose.

The intro.shell_glucose node (exact shape):
{
  "text_dramatic": "TBD — you select glucose; the atom gains its shell — the glucose structure becomes your form.",
  "text_teaching": "TBD — Phase 7 shell text (the hero dons the glucose molecule; the 20 AA remain as the cast).",
  "claim_ids": ["PLACEHOLDER_PHASE7"],
  "tags": ["stage:intro", "preface", "char:glucose"],
  "on_enter": [
    {"op": "hide_all"},
    {"op": "load", "target": "pdb:TBD_GLUCOSE", "args": {"object": "glucose"}},
    {"op": "show_as", "target": "glucose", "args": {"rep": "sticks"}},
    {"op": "show_as", "target": "aa_cast", "args": {"rep": "cartoon"}}
  ],
  "choices": [
    {"label": "Begin the journey — enter glycolysis", "goto": "gly.start"},
    {"label": "Observe your new form", "goto": "gly.start", "tags": ["mc:observe"]}
  ]
}

Rationale: hide_all clears the scene (hides the hero atom from Beat A + the 20 AA), then load+show_as glucose (the hero's new shell) + show_as aa_cast cartoon (re-show the 20 AA cast alongside). The aa_cast object persists from intro.preface (hide_all hides but does not delete — molops hide_all -> cmd.hide, NOT cmd.delete). pdb:TBD_GLUCOSE is a placeholder (loud-fail until Phase 6/7; NO fabricated PDB ID).

THEN reroute intro.select's glucose choice: change the FIRST choice's goto from "gly.start" to "intro.shell_glucose" (so glucose selection -> the shell beat -> gly.start). The choice label "Glucose — enter the glycolysis path" + effects {} + tags ["char:glucose"] stay UNCHANGED (only the goto changes). The FA + Alc choices (goto fa.stub / alc.stub) stay UNCHANGED — their shell nodes are Phase 8 (the stubs return to intro.select for now; Phase 8 adds intro.shell_fa / intro.shell_alc between intro.select and the stubs, OR replaces the stubs).

THEN edit data/story_glucose/glycolysis.json: modify the "gly.start" node's on_enter from [{"op": "hide_all"}] to:
[
  {"op": "hide_all"},
  {"op": "show_as", "target": "glucose", "args": {"rep": "sticks"}}
]
Rationale (the "hide 20 AA after continue" decision): gly.start's hide_all hides EVERYTHING (the 20 AA cast + the glucose shell loaded in intro.shell_glucose); then show_as glucose re-shows ONLY the hero's glucose molecule. This achieves "only the hero's molecule remains" (the 20 AA step back, the hero's shell persists). Decided NOT to add a dedicated intro.journey_start node (simpler; gly.start already does hide_all, and the glucose object persists from intro.shell_glucose). The "glucose" object name + "sticks" rep are placeholders (Phase 5.2/6 may adjust; the op vocabulary is correct). Do NOT change gly.start's choices (Continue + Observe to gly.g6p — already 2 choices, 0-single-Continue holds).

DECISIONS DOCUMENTED (in this action for the executor + later in 05.1-DESIGN.md via Plan 15):
- Per-character shell node (intro.shell_glucose), NOT parametric. FA/Alc stubbed (Phase 8 adds their shell nodes).
- gly.start on_enter hide-20-AA approach: hide_all + show_as glucose (NO new journey_start node — simpler, achieves the intent).
- intro.select glucose goto reroute: gly.start -> intro.shell_glucose (the shell beat is between character select and the journey).
- FA/Alc paths skip the shell beat for now (Phase 8 adds intro.shell_fa / intro.shell_alc).

Do NOT modify fa.stub, alc.stub, gly.g6p, gly.pfk, or any other node. Preserve 2-space indentation.
  </action>
  <verify>
1. python3.6 -c "import json; d=json.load(open('data/story_glucose/intro.json')); n=d['nodes']; assert len(n)==5, len(n); sg=n['intro.shell_glucose']; assert sg['tags']==['stage:intro','preface','char:glucose']; assert sg['claim_ids']==['PLACEHOLDER_PHASE7']; assert len(sg['choices'])==2; assert all(c['goto']=='gly.start' for c in sg['choices']); ops=[m['op'] for m in sg['on_enter']]; assert ops==['hide_all','load','show_as','show_as']; assert sg['on_enter'][1]['target']=='pdb:TBD_GLUCOSE'; gc=[c for c in n['intro.select']['choices'] if 'char:glucose' in c.get('tags',[])]; assert len(gc)==1 and gc[0]['goto']=='intro.shell_glucose', gc; fa=[c for c in n['intro.select']['choices'] if 'char:fa' in c.get('tags',[])]; assert fa[0]['goto']=='fa.stub'; print('intro.shell_glucose + intro.select reroute OK')"
2. python3.6 -c "import json; d=json.load(open('data/story_glucose/glycolysis.json')); gs=d['nodes']['gly.start']; ops=[m['op'] for m in gs['on_enter']]; assert ops==['hide_all','show_as'], ops; assert gs['on_enter'][1]['target']=='glucose'; assert gs['on_enter'][1]['args']['rep']=='sticks'; assert len(gs['choices'])==2; print('gly.start on_enter hide_all + show_as glucose OK')"
3. python3.6 -c "from c14.story.graph import StoryGraph; from c14.story.validate import check_reachability; g=StoryGraph.load('data/story_glucose'); rep=check_reachability(g.all_nodes(), g.start_node()); assert rep.is_ok, rep.unreachable_endings; teirs={g.get_node(e).is_ending for e in rep.reachable_endings}; assert teirs=={'true','good','normal','bad'}, teirs; print('direct BFS GREEN from intro.preface; 4 tiers reachable')"
4. python3.6 -c "from c14.story.graph import StoryGraph; g=StoryGraph.load('data/story_glucose'); sc=[nid for nid,nd in g.all_nodes().items() if nd.choices and len(nd.choices)==1 and nd.choices[0].label=='Continue']; assert sc==[], sc; print('0 single-Continue UNCHANGED')"
  </verify>
  <done>
intro.json has 5 nodes (intro.preface + intro.select + intro.shell_glucose + fa.stub + alc.stub); intro.shell_glucose has 2 choices (-> gly.start) + on_enter placeholders (hide_all + load glucose sticks + show 20 AA cartoon); intro.select glucose goto is intro.shell_glucose; gly.start on_enter is [hide_all, show_as glucose sticks]; direct BFS GREEN from intro.preface; 0 single-Continue UNCHANGED.
  </done>
</task>

</tasks>

<verification>
- python3.6 -c "from c14.story.graph import StoryGraph; from c14.story.validate import check_reachability; g=StoryGraph.load('data/story_glucose'); rep=check_reachability(g.all_nodes(), g.start_node()); assert rep.is_ok and not rep.unreachable_endings; assert {g.get_node(e).is_ending for e in rep.reachable_endings}=={'true','good','normal','bad'}; print('SC2 GREEN from intro.preface')"
- python3.6 -c "from c14.story.graph import StoryGraph; g=StoryGraph.load('data/story_glucose'); assert g.start_node()=='intro.preface'; assert 'intro.preface' in g.all_nodes() and 'intro.shell_glucose' in g.all_nodes(); sc=[nid for nid,nd in g.all_nodes().items() if nd.choices and len(nd.choices)==1 and nd.choices[0].label=='Continue']; assert sc==[]; ea=[nid for nid,nd in g.all_nodes().items() if any(str(t).startswith('edit:enzyme:') for t in (nd.tags or []))]; assert len(ea)==14; print('start=intro.preface; 0 single-Continue; 14 edit-allowed UNCHANGED')"
- INVARIANTS PRESERVED (direct checks, NOT the unittest which has stale count/start assertions until Plan 14): 0 single-Continue (new preface nodes have 2 choices each); 14 edit-allowed (no edit:enzyme tags added); 4 ending tiers; ending count UNCHANGED (preface nodes are NOT endings); True/Good/Normal counts unchanged (1/3/2).
- NOTE: python3.6 -m unittest tests.test_glucose_reachability will show STALE-COUNT/START failures (test_manifest_loads_all_43_nodes expects 43 + start intro.select; the node count is now 45 [standalone] / 54 [after Plan 12], start is intro.preface). These are EXPECTED — Plan 14 updates those assertions. The other 6 tests (red variants use g.start_node() which is now intro.preface — still RED; no pymol; 0 single-Continue; 14 edit-allowed; pdh cast fix) still PASS. Do NOT run the unittest as a PASS gate in this plan.
</verification>

<success_criteria>
- intro.preface (Beat A) exists with 2 choices (Continue + Observe -> intro.select) + on_enter placeholders (hide_all + load hero atom spheres + load 20 AA cartoon).
- intro.shell_glucose (Beat B) exists with 2 choices (Continue + Observe -> gly.start) + on_enter placeholders (hide_all + load glucose sticks + show 20 AA cartoon).
- intro.select's glucose choice goto is intro.shell_glucose (rerouted from gly.start); FA/Alc choices unchanged.
- manifest start is intro.preface; StoryGraph.start_node() returns intro.preface.
- gly.start on_enter is [hide_all, show_as glucose sticks] (the 20 AA hidden + only glucose re-shown).
- Direct BFS reachability GREEN from intro.preface: all 4 tiers reachable, no unreachable endings.
- 0 single-Continue UNCHANGED; 14 edit-allowed UNCHANGED; ending count UNCHANGED; no fabricated science (PLACEHOLDER_PHASE7 only; pdb:TBD_* placeholders, no real PDB IDs claimed).
</success_criteria>

<output>
After completion, create .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-13-SUMMARY.md recording: the 2 new preface node ids (intro.preface Beat A + intro.shell_glucose Beat B), the manifest start change (intro.select -> intro.preface), the intro.select glucose goto reroute (gly.start -> intro.shell_glucose), the gly.start on_enter change (hide_all -> hide_all + show_as glucose), the per-character-shell decision (NOT parametric; FA/Alc Phase 8), the hide-20-AA decision (gly.start on_enter hide_all + show_as glucose; NO journey_start node), the 0-single-Continue decision (Observe 2nd choice; invariant UNCHANGED), the 20-AA cartoon decision (show_as rep cartoon; terminals naturally hidden), the direct-BFS GREEN confirmation, and the note that the test count + start assertions are stale (fixed in Plan 14).
</output>
