---
phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - data/story_glucose/etc_atp.json
autonomous: true

must_haves:
  truths:
    - "etc.complex_ii, etc.complex_iii, etc.complex_iv each have edit:enzyme tag + edit:offer choice (goto edit.prompt) + DIS-*-cand claim_id + 3 choices (Continue + Observe + edit:offer)"
    - "etc.complex_i claim_ids == [DIS-NDUFS8-01-cand] (PLACEHOLDER_PHASE7_ETC REPLACED, not appended - per the explicit replan fix)"
    - "etc.complex_i keeps its edit:enzyme tag + 2 choices unchanged (already edit-allowed; claim_id fix only)"
    - "etc.atp_synthase has >=2 choices (Become ATP + Observe, both goto end.true) and is NOT edit-allowed (no disease mutant in scope)"
    - "reachability stays GREEN (no node added/removed; node count still 43)"
  artifacts:
    - path: "data/story_glucose/etc_atp.json"
      provides: "5 modified ETC nodes (etc.complex_i claim_id fix, etc.complex_ii/iii/iv promote+MC, etc.atp_synthase MC)"
      contains: "DIS-NDUFS8-01-cand"
  key_links:
    - from: "etc.complex_ii / etc.complex_iii / etc.complex_iv edit:offer choices"
      to: "edit.prompt"
      via: "choice.goto"
      pattern: "edit\\.prompt"
    - from: "etc.complex_i claim_ids"
      to: "DIS-NDUFS8-01-cand"
      via: "claim_id replacement (PLACEHOLDER_PHASE7_ETC removed)"
      pattern: "DIS-NDUFS8-01-cand"
---

<objective>
Apply the disease-mutant research + Continue-to-MC directive to the ETC + ATP skeleton region: promote 3 ETC complexes to edit-allowed (SDHA/Complex II, CYC1/Complex III, COX4I1/Complex IV), fix the etc.complex_i claim_id (PLACEHOLDER_PHASE7_ETC replaced by the real CANDIDATE DIS-NDUFS8-01-cand), and add an Observe choice to etc.atp_synthase.

Purpose: Driver 1 (disease-mutant research) + Driver 2 (Continue-to-MC). STRUCTURE ONLY. The etc.complex_i claim_id fix is critical: PLACEHOLDER_PHASE7_ETC was a generic placeholder; the research confirmed a real CANDIDATE disease claim (NDUFS8 H68R, PMID 9837812 Loeffen 1998 - the first nuclear Complex I Leigh mutation). The 3 promotions close the ETC chain (Complex II = first nuclear resp-chain mutation ever found, PMID 7550341; Complex III = CYC1 ClinVar Pathogenic; Complex IV = COX4I1 structural subunit, NOT SURF1 assembly factor which is absent from 1OCC).
Output: 1 modified skeleton JSON file (etc_atp.json) with 5 node modifications; node count unchanged (43).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-REPLAN-CONTEXT.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-RESEARCH-disease-mutants-etc.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md
@data/story_glucose/etc_atp.json
@data/story_glucose/manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote etc.complex_ii/iii/iv to edit-allowed + Continue-to-MC (3 nodes become 3-choice)</name>
  <files>data/story_glucose/etc_atp.json</files>
  <action>
Edit data/story_glucose/etc_atp.json. All changes are additive (no node added/removed). Each promoted complex gains: an edit:enzyme:<id> tag, an edit:offer choice (goto edit.prompt), its DIS-*-cand claim_id, and an Observe choice (Continue-to-MC).

1. etc.complex_ii - PROMOTE + Continue-to-MC (becomes 3-choice):
   - tags: change ["stage:etc"] to ["stage:etc", "edit:enzyme:etc.complex_ii"]
   - claim_ids: change ["PLACEHOLDER_PHASE7_ETC"] to ["PLACEHOLDER_PHASE7_ETC", "DIS-SDHA-01-cand"]
     - Rationale: SDHA W512R-to-R512W is a confirmed point mutant (ClinVar Likely pathogenic, PMID 7550341 Bourgeron 1995 - the first nuclear respiratory-chain mutation ever found). DIS-SDHA-01-cand is CANDIDATE pending Phase 7.
   - choices: change the single-element array to a 3-element array:
     [{"label": "Continue", "goto": "etc.complex_iii"}, {"label": "Examine the Complex II active site", "goto": "etc.complex_iii", "tags": ["mc:observe"]}, {"label": "Try to edit Complex II", "goto": "edit.prompt", "tags": ["edit:offer"]}]

2. etc.complex_iii - PROMOTE + Continue-to-MC (becomes 3-choice):
   - tags: change ["stage:etc"] to ["stage:etc", "edit:enzyme:etc.complex_iii"]
   - claim_ids: change ["PLACEHOLDER_PHASE7_ETC"] to ["PLACEHOLDER_PHASE7_ETC", "DIS-CYC1-01-cand"]
     - Rationale: CYC1 C12W-to-W12C (or the alternative L215F) is a confirmed Complex III point mutant (ClinVar Pathogenic, PMID 23910460 Gaignard 2013, MC3DN6). DIS-CYC1-01-cand is CANDIDATE pending Phase 7. The CYC1 W96C-vs-L215F residue choice is flagged for human review in the checkpoint (the skeleton uses C12W as the PDB-chain-D resi-12 candidate; the alternative is documented in the research).
   - choices: change the single-element array to a 3-element array:
     [{"label": "Continue", "goto": "etc.complex_iv"}, {"label": "Examine the Complex III Q-cycle site", "goto": "etc.complex_iv", "tags": ["mc:observe"]}, {"label": "Try to edit Complex III", "goto": "edit.prompt", "tags": ["edit:offer"]}]

3. etc.complex_iv - PROMOTE + Continue-to-MC (becomes 3-choice):
   - tags: change ["stage:etc"] to ["stage:etc", "edit:enzyme:etc.complex_iv"]
   - claim_ids: change ["PLACEHOLDER_PHASE7_ETC"] to ["PLACEHOLDER_PHASE7_ETC", "DIS-COX4I1-01-cand"]
     - Rationale: COX4I1 P152T (human UniProt precursor) / T130P (PDB chain D resi 130, bovine 1OCC) is a confirmed Complex IV point mutant (ClinVar Pathogenic, PMID 31290619 Pillai 2019, MC4DN16 Leigh-spectrum). COX4I1 is the regulatory subunit present in 1OCC chain D (SURF1 is an assembly factor NOT in 1OCC - hence COX4I1 not SURF1). DIS-COX4I1-01-cand is CANDIDATE pending Phase 7. The residue-numbering caveat (human precursor P152 vs PDB T130) is Phase 7 edits.json content.
   - choices: change the single-element array to a 3-element array (note: the existing label "Continue to ATP synthase" is preserved as the Continue choice):
     [{"label": "Continue to ATP synthase", "goto": "etc.atp_synthase"}, {"label": "Examine the Complex IV O2-reduction site", "goto": "etc.atp_synthase", "tags": ["mc:observe"]}, {"label": "Try to edit Complex IV", "goto": "edit.prompt", "tags": ["edit:offer"]}]

Do NOT touch text fields or on_enter (the existing 1OCC load on etc.complex_iv stays; complex_ii/iii have no PDB load - narrative-only). Preserve JSON formatting.
  </action>
  <verify>
1. python3.6 -c "import json; d=json.load(open('data/story_glucose/etc_atp.json')); n=d['nodes']; for nid,claim in [('etc.complex_ii','DIS-SDHA-01-cand'),('etc.complex_iii','DIS-CYC1-01-cand'),('etc.complex_iv','DIS-COX4I1-01-cand')]: assert 'edit:enzyme:'+nid in n[nid]['tags'], nid; assert claim in n[nid]['claim_ids'], nid; assert len(n[nid]['choices'])==3, nid; assert any(c.get('goto')=='edit.prompt' for c in n[nid]['choices']), nid; print('ETC promotions OK')"
2. python3.6 -m unittest tests.test_glucose_reachability -v  (all 5 tests PASS)
  </verify>
  <done>
3 ETC complexes (II, III, IV) promoted to edit-allowed with edit:enzyme tag + edit:offer + DIS-*-cand + 3 choices each (Continue + Observe + edit:offer); reachability GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix etc.complex_i claim_id (PLACEHOLDER to DIS-NDUFS8-01-cand) + etc.atp_synthase Continue-to-MC</name>
  <files>data/story_glucose/etc_atp.json</files>
  <action>
Edit data/story_glucose/etc_atp.json (same file as Task 1; sequential).

1. etc.complex_i - claim_id FIX (already edit-allowed, already 2 choices; NO MC change, NO tag change):
   - claim_ids: change ["PLACEHOLDER_PHASE7_ETC"] to ["DIS-NDUFS8-01-cand"]  (REPLACE, not append - per the explicit replan instruction + the must_have truth that claim_id != PLACEHOLDER_PHASE7_ETC)
   - Rationale: NDUFS8 H68R-to-R68H (PDB chain I resi 68, bovine 5LDW) is a confirmed Complex I point mutant (PMID 9837812 Loeffen 1998 - the first nuclear Complex I Leigh mutation). DIS-NDUFS8-01-cand is a CANDIDATE pending Phase 7 approval (do NOT mark approved). The generic PLACEHOLDER_PHASE7_ETC is REPLACED by this specific candidate; the ETC pathway-fact claim for Complex I will be added as a separate claim in Phase 7 (all ETC pathway claims are pending Phase 7).
   - Do NOT change the edit:enzyme:etc.complex_i tag or the 2 choices (Continue down the chain + Try to edit Complex I).

2. etc.atp_synthase - Continue-to-MC (NOT promoted - no disease mutant in scope for ATP synthase in this replan):
   - choices: change the single-element array to a 2-element array (both goto end.true - same destination, different experience):
      [{"label": "Become ATP — the soul-jump", "goto": "end.true"}, {"label": "Examine ATP synthase before the soul-jump", "goto": "end.true", "tags": ["mc:observe"]}]
    - IMPORTANT: preserve the existing first-choice label VERBATIM (it uses an em-dash "—" in the current file, not a hyphen "-"); only APPEND the Observe choice as the second array element. Do NOT rewrite or re-punctuate the existing label.
   - Do NOT add an edit:enzyme tag or edit:offer (ATP synthase is the climax node, not a disease-mutant edit node in this replan).

Do NOT touch etc.entry (already 2 choices), end.true (is_ending, empty choices), or text fields. Preserve JSON formatting.
  </action>
  <verify>
1. python3.6 -c "import json; d=json.load(open('data/story_glucose/etc_atp.json')); n=d['nodes']; assert n['etc.complex_i']['claim_ids']==['DIS-NDUFS8-01-cand'], n['etc.complex_i']['claim_ids']; assert 'PLACEHOLDER_PHASE7_ETC' not in n['etc.complex_i']['claim_ids']; assert 'edit:enzyme:etc.complex_i' in n['etc.complex_i']['tags']; assert len(n['etc.complex_i']['choices'])==2; assert len(n['etc.atp_synthase']['choices'])==2; assert all(c.get('goto')=='end.true' for c in n['etc.atp_synthase']['choices']); assert 'edit:enzyme' not in str(n['etc.atp_synthase'].get('tags',[])); print('complex_i fix + atp_synthase MC OK')"
2. python3.6 -m unittest tests.test_glucose_reachability -v  (all 5 tests PASS)
  </verify>
  <done>
etc.complex_i claim_ids == [DIS-NDUFS8-01-cand] (PLACEHOLDER removed); etc.atp_synthase has 2 choices (Become ATP + Observe, both to end.true) and is NOT edit-allowed; reachability GREEN.
  </done>
</task>

</tasks>

<verification>
- python3.6 -m unittest tests.test_glucose_reachability -v  (5 tests PASS; node count 43; 11 endings; 4 tiers reachable)
- python3.6 -c "from c14.story.graph import StoryGraph; g=StoryGraph.load('data/story_glucose'); n=g.all_nodes(); assert len(n)==43; assert n['etc.complex_i'].claim_ids==['DIS-NDUFS8-01-cand']; print('ETC skeleton loads OK')"
- etc_atp.json has 7 nodes (unchanged). The 3 new edit:offer choices goto edit.prompt (exists) - no orphaned endings. etc.atp_synthase's 2 choices both goto end.true (exists) - reachability preserved.
- etc.complex_i claim_id is a CANDIDATE (DIS-NDUFS8-01-cand), NOT approved (Phase 7 approves it; the citation gate is NOT run during Phase 5.1).
</verification>

<success_criteria>
- 3 ETC complexes (II, III, IV) promoted to edit-allowed (edit:enzyme + edit:offer + DIS-*-cand) with 3 choices each.
- etc.complex_i claim_ids == [DIS-NDUFS8-01-cand] (PLACEHOLDER_PHASE7_ETC replaced); keeps its edit:enzyme tag + 2 choices.
- etc.atp_synthase has 2 choices (Become ATP + Observe, both to end.true); NOT edit-allowed.
- Reachability GREEN; node count 43; zero regression.
- All DIS-*-cand claim_ids are CANDIDATE pending Phase 7 (no fabricated science; the skeleton references candidates, does not assert disease as approved fact).
</success_criteria>

<output>
After completion, create .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-09-SUMMARY.md recording: the 5 node modifications (3 promotions, complex_i claim_id fix, atp_synthase MC), the DIS-*-cand claim_ids added (SDHA, CYC1, COX4I1, NDUFS8), the COX4I1-not-SURF1 rationale, the CYC1 C12W-vs-L215F + COX4I1 numbering caveats flagged for the checkpoint, and the GREEN reachability confirmation.
</output>
