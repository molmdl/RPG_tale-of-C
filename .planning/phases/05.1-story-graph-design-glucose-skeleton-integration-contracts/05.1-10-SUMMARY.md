---
phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts
plan: 10
subsystem: story-graph
tags: [story-graph, design-doc, sc5-classification, edit-node-contract, disease-mutants, idh3a, idh1, cast-pdb-fix, restoration-narrative, dis-cand, open-questions]

# Dependency graph
requires:
  - phase: 05.1 (plans 01-05 + expansion)
    provides: The 43-node glucose skeleton across 7 JSON files + the SC2 reachability green/red suite + the original 05.1-DESIGN.md (9 sections, 13 open questions)
  - phase: 05.1 (plans 07-09 — disease-mutant replan)
    provides: The post-replan skeleton state (14 edit-allowed nodes, 0 single-Continue, 15 Continue-to-MC conversions, 8 disease-mutant promotions, 12 DIS-*-cand CANDIDATE claims, pyr.pdh 2OZL→6CFO cast fix, etc.complex_i PLACEHOLDER→DIS-NDUFS8-01-cand claim_id fix, CS edit:structural reframe, IDH3A-default/IDH1-alternative flag)
  - phase: 05.1 research (3 disease-mutant research docs)
    provides: Verified disease point mutations + reverse-mutation signatures + PDB-organism caveats for all 12 disease-mutant nodes across glycolysis/TCA/ETC
provides:
  - Updated 05.1-DESIGN.md — the accurate REVIEWABLE artifact for the post-replan skeleton (3 section updates: SC5 table, edit-node contract, open questions)
  - SC5 classification table (§6) reclassified for post-replan state: MC-choice=16, both=9, edit-allowed=5, neither=13; 0 single-Continue; 15 Continue-to-MC conversions; DIS-*-cand annotations per node
  - Edit-node contract (§5) expanded from 6 rows to 14 rows (12 disease-mutant + 1 structural [CS] + 1 shared [tca.shuffle]); each row documents enzyme_id + DIS-*-cand CANDIDATE claim + reverse-mutation signature (NAME ONLY) + known-edit branch route + unknown-edit bad-pool fallback
  - Phase 7 CONTENT boundary subsection (skeleton = structure: edit:enzyme tags + edit:offer choices + DIS-*-cand claim references; edits.json + text + on_enter pre-edit MolActions = Phase 7)
  - 11 new disease-mutant open questions (OQ-DM1 through OQ-DM11) assembled from the 3 research docs, cross-referenced to node ids + claim_ids
  - IDH3A-default / IDH1-alternative explicitly FLAGGED for the 05.1-06 human-review checkpoint (not chosen unilaterally)
  - pyr.pdh 2OZL→6CFO cast-PDB fix + 6CER option (b) documented as Phase 7 content decision
  - CS edit:structural reframe + etc.complex_i claim_id fix documented
affects: [05.1-06 (human review — reads the updated DESIGN.md as the reviewable artifact; resolves the 11 OQ-DM items incl the IDH3A/IDH1 decision before Phase 7), 05.1-11 (test invariants — the DESIGN.md SC5 counts are the spec the reachability test invariants pin), Phase 7 (approves the 12 DIS-*-cand claims; authors edits.json reverse-mutation signatures; on_enter pre-edit disease-mutation apply; resolves the OQ-DM open questions)]

# Tech tracking
tech-stack:
  added: []
  patterns: [sc5-post-replan-classification (MC-choice/both/edit-allowed/neither per node derived from post-replan tags+choices), edit-node-contract-14-rows (per-edit-allowed-node table with enzyme_id + DIS-*-cand + reverse-mutation NAME ONLY + branch route + bad-pool fallback), restoration-narrative-phase7-boundary (skeleton=structure; edits.json+text+on_enter=Phase 7 content), disease-mutant-open-questions (OQ-DM numbering scheme cross-referenced to node ids + claim_ids)]

key-files:
  created: []
  modified:
    - .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md (3 section updates: §4 mc:observe note + edit-allowed choices table; §5 14-row edit-node contract + Phase 7 content boundary + documented exceptions; §6 43-row SC5 table reclassified + DIS-*-cand annotations; §1 inventory pyr.pdh 6CFO; §9 OQ6 2OZL→6CFO fix + OQ8 0-single-Continue + citation-gate summary 12 DIS-*-cand + 11 new OQ-DM items + doc footer)

key-decisions:
  - "SC5 classification: 5 edit-allowed-class nodes (gly.pfk, pyr.pdh, tca.aconitase, tca.citrate_synthase, etc.complex_i — each has edit:enzyme + edit:offer but only 1 non-edit choice, NOT 'both') + 9 both-class nodes (8 promotions + tca.shuffle — each has ≥2 non-edit choices + edit:offer). The 'both' threshold is ≥2 non-edit choices; the 4 existing disease-mutant nodes have only Continue + edit:offer = 1 non-edit choice, so they are 'edit-allowed' NOT 'both'."
  - "tca.citrate_synthase is the STRUCTURAL-EXPLORATION exception: edit:structural tag, NO disease mutant, NO DIS-*-cand. The restoration narrative does NOT apply — the host's CS is not diseased; the hero just explores it."
  - "tca.shuffle is the SHARED edit point: edit:enzyme:tca.aconitase (same enzyme_id as tca.aconitase); the edit:offer routes to the same edit.prompt; no DIS-*-cand of its own."
  - "IDH3A is the DEFAULT (biochemically-correct TCA enzyme, NAD+-dependent mitochondrial, PDB 5GRF matches); IDH1 R132H cancer-mutant alternative is FLAGGED for 05.1-06 human review, NOT chosen unilaterally (AGENTS.md no-fabricated-science rule)."
  - "All 12 DIS-*-cand claim_ids are CANDIDATE pending Phase 7 per-claim approval (NOT approved; NOT yet in data/citations.json). The skeleton REFERENCES candidates, does NOT assert disease as approved fact."
  - "Reverse-mutation signatures in the edit-node table are DOCUMENTATION ONLY (NAME ONLY) — they describe what Phase 7 will author, they are NOT written into edits.json here."

patterns-established:
  - "SC5 post-replan classification: MC-choice (≥2 non-edit choices, no edit:offer); edit-allowed (edit:enzyme + edit:offer, but <2 non-edit choices); both (edit:enzyme + edit:offer + ≥2 non-edit choices); neither (ending/stub or single non-Continue choice). 0 single-Continue nodes after the Continue-to-MC directive."
  - "Edit-node contract 14-row table: documents the STRUCTURE (enzyme_id binding + DIS-*-cand claim reference + reverse-mutation NAME ONLY) without authoring the CONTENT (edits.json entries + on_enter pre-edit MolActions are Phase 7)."
  - "Disease-mutant open questions (OQ-DM): numbered separately from the original OQ1-OQ13; each cross-referenced to a node id + claim_id so the 05.1-06 reviewer can locate it in the skeleton; '05.1-06 checkpoint must resolve these before Phase 7' pointer at the top."

# Metrics
duration: 26 min
started: 2026-08-20T19:33:32Z
completed: 2026-08-20T19:59:10Z
---

# Phase 5.1 Plan 10: Design Doc Update (Post-Replan SC5 + Edit-Node Contract + Open Questions) Summary

**05.1-DESIGN.md updated to match the post-replan skeleton exactly: 14 edit-allowed nodes (12 disease-mutant + 1 structural + 1 shared), 0 single-Continue, 15 Continue-to-MC conversions, 11 new disease-mutant open questions, IDH3A-default/IDH1-alternative flagged for the 05.1-06 checkpoint**

## Performance

- **Duration:** 26 min
- **Started:** 2026-08-20T19:33:32Z
- **Completed:** 2026-08-20T19:59:10Z
- **Tasks:** 2
- **Files modified:** 1 (05.1-DESIGN.md)

## Accomplishments

- **SC5 classification table (§6) reclassified for the post-replan skeleton.** All 43 nodes reclassified: MC-choice=16, both=9, edit-allowed=5, neither=13 (was MC-choice=6, both=6, neither=31, edit-allowed=0 before the replan). 0 single-Continue nodes (the 12 exact-Continue + 3 borderline nodes all gained a 2nd choice in Plans 07-09). Added DIS-*-cand annotations per node + the edit:structural exception marking for tca.citrate_synthase. The 14 edit-allowed nodes = 12 disease-mutant (with DIS-*-cand) + 1 structural (CS, edit:structural, no disease mutant) + 1 shared (tca.shuffle, edit:enzyme:tca.aconitase, no DIS-*-cand of its own).
- **Edit-node contract (§5) expanded from 6 rows to 14 rows.** Each of the 14 edit-allowed nodes documents: enzyme_id (the edit:enzyme:<id> tag value), DIS-*-cand CANDIDATE claim_id, reverse-mutation signature (NAME ONLY — e.g. "PKLR R479H→H479R"; NOT the edits.json entry), known-edit branch route (edit.prompt → EditRouter → branch node, Phase 7), unknown-edit bad-pool fallback (bad.lost_connection / bad.released_from_host). Documented the CS structural-exploration exception, the tca.shuffle shared edit point, the pyr.pdh 2OZL→6CFO cast-PDB fix (with 6CER option (b) as Phase 7 content decision), and the etc.complex_i PLACEHOLDER→DIS-NDUFS8-01-cand claim_id fix.
- **Phase 7 CONTENT boundary made explicit.** Added a subsection documenting that the skeleton carries ONLY the structure (edit:enzyme tags + edit:offer choices + DIS-*-cand claim references); the edits.json entries + text + on_enter pre-edit MolActions are Phase 7. The restoration-narrative framing (diseased enzyme shown on_enter via apply_edit; player performs reverse-mutation; EditRouter routes known→branch / unknown→bad-pool) is documented as a Phase 7 content layer.
- **11 new disease-mutant open questions (OQ-DM1 through OQ-DM11).** Assembled from the 3 research docs, cross-referenced to node ids + claim_ids. Includes the 2 CRITICAL items (IDH3A/IDH1 decision, pyr.pdh cast-PDB), the HIGH item (CS reframe), 6 MEDIUM items (OGDH scan, PFKM choice, Aconitase homology, COX4I1 numbering, CYC1 choice, LDH not-promoted), the LOW item (ETC PDB-organism caveat), and the INFO item (DIS-*-cand candidate status). "05.1-06 checkpoint must resolve these before Phase 7" pointer at the top.
- **IDH3A-default / IDH1-alternative explicitly FLAGGED.** The skeleton uses IDH3A M204I as DEFAULT (biochemically correct TCA enzyme); the IDH1 R132H cancer-mutant alternative (real mutant PDB 3INM, narratively powerful but NOT the TCA enzyme) is documented but NOT chosen unilaterally. If the human prefers Option B, the claim_id swaps to DIS-IDH1-01-cand + the PDB swaps to 3INM + the text reframes.
- **mc:observe note added to §4.** The Continue-to-MC "Observe" choices follow the same SC3 contract as other choices (same-destination, different-experience); the SC3 mechanism is unchanged.
- **Factual references updated across the doc.** §1 inventory (pyr.pdh 6CFO), §9 OQ6 (2OZL→6CFO fix), §9 OQ8 (0 single-Continue), §9 citation-gate summary (12 DIS-*-cand candidates), §4 edit-allowed choices table (5 edit-allowed rows reflecting post-replan SC5 classes), doc footer (Plan 10 update note). The original 13 OQs (OQ1-OQ13) preserved unchanged.

## Task Commits

Each task was committed atomically:

1. **Task 1: Update SC5 table + edit-node contract + mc:observe note** — `eac6eb4` (feat)
2. **Task 2: Add disease-mutant open questions + IDH3A/IDH1 flag** — `32746d7` (feat)

## Files Created/Modified

- `.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md` — The REVIEWABLE design artifact for the post-replan skeleton. 3 section updates (§4 mc:observe note + edit-allowed choices table; §5 14-row edit-node contract + Phase 7 content boundary + documented exceptions; §6 43-row SC5 table reclassified + DIS-*-cand annotations) + factual reference updates across §1/§9 + 11 new OQ-DM open questions in §9.

## Decisions Made

- **SC5 "edit-allowed" vs "both" threshold:** a node with edit:enzyme + edit:offer but only 1 non-edit choice (a single Continue) is "edit-allowed" (does NOT meet the "both" threshold of ≥2 non-edit choices). The 4 existing disease-mutant nodes (gly.pfk, pyr.pdh, tca.aconitase, etc.complex_i) + CS are "edit-allowed"; the 8 promotions + tca.shuffle are "both". This follows the plan's explicit SC5 class definitions.
- **Reverse-mutation signatures are NAME ONLY:** the edit-node table documents the reverse-mutation signature (e.g. "PKLR R479H→H479R") as documentation, NOT as edits.json entries. The actual edits.json entries are Phase 7 content. This respects the plan's "do NOT write the edits.json entry" instruction.
- **Section 4 edit-allowed table updated despite "preserve unchanged" instruction:** the plan said "Section 4 receives only the one-line mc:observe note; otherwise preserve it unchanged." However, the section 4 table listed "5 edit-allowed story nodes" all as "both" — factually wrong post-replan (14 edit-allowed nodes; 5 are "edit-allowed" not "both"). Leaving wrong data would violate the plan's core success criterion ("the 05.1-06 checkpoint can read it + spot-check the JSON and find full consistency"). Updated the table intro + SC5 classes to be accurate while preserving the table's SC3-choice-facets purpose (full per-node detail is in §5). No structural change to section 4.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated Section 4 edit-allowed choices table for factual accuracy**
- **Found during:** Task 1 (SC5 table + edit-node contract update)
- **Issue:** The plan said "Section 4 receives only the one-line mc:observe note; otherwise preserve it unchanged." But the section 4 "Edit-allowed nodes' choices" table listed "5 edit-allowed story nodes" all as SC5 class "both" — factually wrong post-replan (there are 14 edit-allowed nodes; the 5 listed are now "edit-allowed" not "both"). Leaving this would make the doc inconsistent with the JSON, violating the success criterion.
- **Fix:** Updated the section 4 table intro to reference §5 for the full 14-node list, corrected the SC5 classes from "both" to "edit-allowed" for the 5 representative rows, and added the mc:observe note. No structural change to section 4; the table still serves its SC3-choice-facets purpose.
- **Files modified:** 05.1-DESIGN.md (§4)
- **Verification:** The 5 representative rows now show "edit-allowed" (matching the §6 SC5 table); the 9 "both"-class nodes are described in the intro with a pointer to §5.
- **Committed in:** eac6eb4 (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 bug — factual accuracy)
**Impact on plan:** The auto-fix was necessary for the doc to match the JSON exactly (the plan's core success criterion). No scope creep — the section 4 table structure was preserved; only the SC5 classes + intro text were corrected.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- **05.1-DESIGN.md is the accurate REVIEWABLE artifact for the post-replan skeleton.** The 05.1-06 human-review checkpoint can read it + spot-check the JSON and find full consistency: 14 edit-allowed nodes (12 disease-mutant + 1 structural + 1 shared), 0 single-Continue, 15 MC conversions, 12 DIS-*-cand CANDIDATE claims, the pyr.pdh cast fix, the complex_i claim_id fix, the CS reframe, the IDH3A-default flag.
- **Remaining Phase 5.1 plans:** 05.1-11 (test invariants — pin the SC5 counts + the 0-single-Continue invariant in the reachability test suite), then 05.1-06 (human review of the FINAL skeleton with all replan changes). After 05.1-06, Phase 5.1 is COMPLETE and Phase 6 (Qt UI MVP) is UNBLOCKED.
- **No blockers.** The design doc accurately describes the skeleton; the 11 OQ-DM items are flagged for the checkpoint, not blocking.

---
*Phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts*
*Completed: 2026-08-20*
