---
phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts
plan: 16
subsystem: story-graph
tags: [story-graph, diagram, svg, ascii, mermaid, bad-endings, preface, render-story-graph, doc-only]

# Dependency graph
requires:
  - phase: 05.1-11
    provides: the original 43-node diagram (svg/txt) + the renderer (tools/render_story_graph.py) + the GRAPH-DIAGRAM.md narrative this plan regenerates/updates
  - phase: 05.1-12
    provides: the post-bad-ending-expansion skeleton (52 nodes, 20 endings, 9 new bad endings + edit.prompt 3->12 structural choices)
  - phase: 05.1-13
    provides: the post-preface-expansion skeleton (54 nodes, manifest start intro.preface, 2 new preface nodes, gly.start hide-20-AA)
  - phase: 05.1-14
    provides: the fixed reachability test assertions (43->54, 11->20, intro.select->intro.preface) so the test-verification gate is GREEN
provides:
  - regenerated 05.1-graph.svg (swim-lane SVG of the 54-node skeleton, well-formed XML, 14 edit-allowed #FFF8DC markers, 11 new nodes + new edges visible)
  - regenerated 05.1-graph.txt (ASCII diagram; intro.preface as BFS root/start; edit.prompt -> 12 bad endings structural edges)
  - updated 05.1-GRAPH-DIAGRAM.md (header 54 nodes / 20 endings; mermaid with 11 new nodes + new edges; Ending-Tier table Bad 5->14; new "Bad-Ending + Preface Expansion (Plans 05.1-12/13)" section with 11-node table + decisions locked + pending-rereview note; Key Design Decisions updated)
affects: [05.1-06 (human review uses this diagram as the visual overview of the final 54-node skeleton), phase-5.2 (preface hero-highlight convention referenced in the diagram), phase-7 (preface + shell + bad-ending text + TBD_* PDB targets)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Renderer auto-detection: tools/render_story_graph.py auto-detects new nodes via id-prefix stage grouping (bad.* -> bad stage, intro.* -> intro stage) + edit:enzyme tags + is_ending — NO renderer code change needed when the skeleton expands; the new nodes are just nodes"
    - "Doc-only diagram regen has zero test impact: regenerating svg/txt + editing GRAPH-DIAGRAM.md does not touch skeleton JSON or tests; verified by identical test results before/after"

key-files:
  created: []
  modified:
    - .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.svg — regenerated (54 nodes, 89 edges, well-formed XML, 14 #FFF8DC edit-allowed markers)
    - .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.txt — regenerated (intro.preface BFS root; 12 edit.prompt structural edges to bad endings)
    - .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-GRAPH-DIAGRAM.md — header 54/20, mermaid +11 nodes + new edges, Ending-Tier Bad 5->14, new expansion section, Key Decisions

key-decisions:
  - "Renderer NOT modified — it auto-detects the 11 new nodes via id-prefix stage grouping (bad.* / intro.*) + edit:enzyme tags + is_ending; the expansion needed zero renderer code change"
  - "Diagram regen + .md update are doc-only (zero test impact); the reachability tests are GREEN because the concurrent Plan 05.1-14 fixed the test assertions"

patterns-established:
  - "Diagram regen is a doc-only follow-up to skeleton expansion: run the renderer (auto-detects new nodes) + hand-update the .md narrative (mermaid + tables + decisions) to match; no code/test impact"

# Metrics
duration: 9 min
completed: 2026-08-22
---

# Phase 5.1 Plan 16: Regenerate Story-Graph Diagram + Update GRAPH-DIAGRAM.md Summary

**Regenerated the SVG + ASCII story-graph diagram from the 54-node post-expansion skeleton (11 new nodes + new edges auto-detected by the renderer with zero code change) and updated 05.1-GRAPH-DIAGRAM.md (header 54/20, mermaid, Ending-Tier Bad 5->14, new expansion section) — the visual-narrative artifact for the 05.1-06 re-review**

## Performance

- **Duration:** 9 min
- **Started:** 2026-08-22T05:04:43Z
- **Completed:** 2026-08-22T05:14:08Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Regenerated 05.1-graph.svg + 05.1-graph.txt via `python3.6 tools/render_story_graph.py` (exit 0; 54 nodes, 89 edges). The renderer auto-detected the 11 new nodes (9 bad endings -> "bad" stage swim-lane via `bad.*` prefix; 2 preface nodes -> "intro" stage via `intro.*` prefix) + the 14 unchanged edit-allowed nodes (`edit:enzyme` tags -> `#FFF8DC` fill) + the new edges — with ZERO renderer code change.
- Verified the SVG is well-formed XML; both svg + txt contain intro.preface + intro.shell_glucose + the 9 new bad-ending ids + the new edges (intro.preface -> intro.select -> intro.shell_glucose -> gly.start; edit.prompt -> 9 new bad endings, 12 structural total). The 14 edit-allowed #FFF8DC markers are present (UNCHANGED by the expansion). intro.preface is the BFS root / start (top of ASCII, first intro-stage node in SVG).
- Updated 05.1-GRAPH-DIAGRAM.md: header 43 -> 54 nodes + post-expansion paragraph; mermaid diagram with +intro.preface (IP) + intro.shell_glucose (ISG) in the intro subgraph + 9 new bad-ending nodes in the bad subgraph + main-flow edges IP->IS->ISG->GS + 9 new edit.prompt structural edges; Ending-Tier table Bad 5 -> 14 (total 20; all 14 bad endings listed); Edit-Allowed section UNCHANGED note; new "Bad-Ending + Preface Expansion (Plans 05.1-12/13)" section (11-node table + Decisions Locked + Pending 05.1-06 Re-review note); Key Design Decisions +2 locked decisions (CG-collection + preface sequence).
- Reachability tests GREEN (8 tests OK after the concurrent Plan 05.1-14 fixed the assertions); full suite 206 tests OK (zero regression — the diagram regen + .md update are doc-only, zero test impact).

## Task Commits

Each task was committed atomically:

1. **Task 1: Regenerate SVG + ASCII diagram** — committed in `3a268c0` (docs/feat; see Deviations — a parallelization race swept the staged svg/txt into the concurrent Plan 05.1-14's feat commit alongside its test-file changes). The regenerated svg/txt ARE committed + verified (svg has intro.preface=1, 14 #FFF8DC markers; files clean).
2. **Task 2: Update 05.1-GRAPH-DIAGRAM.md** - `32c4dd5` (docs)

**Plan metadata:** (this commit) (docs: complete plan)

## Files Created/Modified
- `.planning/phases/05.1-.../05.1-graph.svg` — regenerated swim-lane SVG (54 nodes, 89 edges; well-formed XML; 14 #FFF8DC edit-allowed markers; 11 new nodes + new edges visible; intro.preface as first intro-stage node)
- `.planning/phases/05.1-.../05.1-graph.txt` — regenerated ASCII diagram (intro.preface BFS root/start at top; intro.select -> intro.shell_glucose -> gly.start; edit.prompt -> 12 bad endings structural edges in the unreached/loose section; summary line "54 nodes | 20 endings (1T/3G/2N/14B) | 14 edit-allowed | 1 RNG")
- `.planning/phases/05.1-.../05.1-GRAPH-DIAGRAM.md` — header 54-node/20-ending; post-expansion paragraph; preface-node legend note; mermaid +11 nodes + new edges (IP/ISG + 9 bad endings + IP->IS->ISG->GS + 9 EP structural edges + badEnd class); Ending-Tier table Bad 5->14 (20 total); Edit-Allowed UNCHANGED note; new "Bad-Ending + Preface Expansion (Plans 05.1-12/13)" section (11-node table + Decisions Locked + Pending Re-review); Key Design Decisions +2

## Decisions Made
- **Renderer NOT modified:** the plan said "Do NOT modify the renderer — it auto-detects the new nodes." Confirmed: the bad.* + intro.* id-prefix stage grouping (lines 80/94) + edit:enzyme tag detection (line 121) + is_ending already handle the 11 new nodes. Zero renderer code change; the new nodes render correctly as standard ending/choice nodes.
- **SVG grep -c vs grep -o:** the SVG is single-line compact XML (ET.write default), so `grep -c` (line count) returns 1 for any present pattern. Used `grep -o | wc -l` for the actual occurrence count (FFF8DC = 14, exactly one per edit-allowed node — in the 12-16 range, NOT zero).
- **Doc-only change, zero test impact:** the diagram regen (renderer reads skeleton JSON read-only, writes svg/txt) + the .md edit do NOT touch skeleton JSON or tests. Verified by the full suite (206 OK) + the reachability tests (8 OK after the concurrent Plan 14 fixed the assertions).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Parallelization race: Task 1 svg/txt swept into the concurrent Plan 05.1-14 commit**
- **Found during:** Task 1 commit (staged svg/txt, then `git commit` returned no hash)
- **Issue:** With `parallelization: true`, a concurrent Plan 05.1-14 agent was executing in the same repo. I staged my regenerated 05.1-graph.svg + 05.1-graph.txt. Before my `git commit` ran, the concurrent Plan 14 agent ran `git commit` (commit `3a268c0 feat(05.1-14): ...`) which swept ALL staged files — including my svg/txt — into ITS commit alongside its own tests/test_glucose_reachability.py changes. My `git commit` then found nothing staged (the index was drained) and returned "no changes added to commit."
- **Fix:** Verified the svg/txt ARE committed in 3a268c0 (`git show --stat 3a268c0` lists both files; svg has intro.preface=1; files are clean/tracked). Accepted the race — the Task 1 deliverable is committed + verified, just attributed to the 05.1-14 commit rather than a dedicated docs(05.1-16) commit. Did NOT rewrite history (no force-push/rebase — unsafe with a concurrent agent active). For Task 2, staged + committed in quick succession to minimize the race window; Task 2 committed cleanly as `32c4dd5`.
- **Files modified:** (none beyond the plan — the svg/txt were already regenerated; the attribution landed in 3a268c0)
- **Verification:** `git show --stat 3a268c0` includes 05.1-graph.svg + 05.1-graph.txt; `grep -o FFF8DC` svg = 14; `grep intro.preface` svg/txt = present; files clean post-commit
- **Committed in:** 3a268c0 (shared with Plan 14's test work — not a separate Plan 16 task commit)

---

**Total deviations:** 1 auto-fixed (1 blocking — parallelization race)
**Impact on plan:** The race was administrative (commit attribution only) — the Task 1 deliverable (regenerated svg/txt) is committed + fully verified; only the commit message says "feat(05.1-14)" instead of "docs(05.1-16)". No content, verification, or scope impact. Task 2 committed cleanly.

## Issues Encountered
- **Concurrent Plan 14 + 15 agents:** Plans 05.1-14 (test assertions) + 05.1-15 (DESIGN.md) were executing in parallel (parallelization=true). Plan 14 (3a268c0) completed (fixed the 43->54 / 11->20 / intro.select->intro.preface test assertions — the 2 stale failures documented in the 05.1-12/13 summaries are now resolved; all 8 reachability tests pass). Plan 15 (ea1763e) committed §1/§3/§4/§5/§8 of 05.1-DESIGN.md but has §6/§7/§9 still uncommitted (in progress at the time of this summary). These concurrent agents are NOT my concern — I staged only my own files (svg/txt/GRAPH-DIAGRAM.md/SUMMARY/STATE/ROADMAP) and did not touch 05.1-DESIGN.md or the test file.
- **Plan's "9 tests PASS after Plan 14" vs actual 8:** the plan's verification text anticipated 9 reachability tests after Plan 14, but the concurrent Plan 14 commit (3a268c0) kept the count at 8 (renamed test_manifest_loads_all_43_nodes -> test_manifest_loads_all_54_nodes; the "new bad-ending-reachability invariant test" mentioned in the 05.1-13 SUMMARY may have been folded into the existing assertions or is still pending). All 8 pass — the verification intent ("reachability tests PASS after Plan 14") is satisfied.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Plan 16 complete; the diagram is the accurate visual artifact for the 54-node / 20-ending / intro.preface-start skeleton. The 05.1-06 human-review checkpoint reviewer uses this diagram as the visual overview.
- Remaining in Phase 5.1: Plan 05.1-15 (DESIGN.md §6/§7/§9 — in progress at time of writing; §1/§3/§4/§5/§8 committed in ea1763e) + Plan 05.1-06 (human review of the FINAL post-expansion skeleton: the 2 new requirements + the 3 CRITICAL disease-mutant decisions + the 24 OQs). After 15 completes + 06 human review, Phase 5.1 is COMPLETE and Phase 6 (Qt UI MVP) is UNBLOCKED.
- Blocker/concern: none for Plan 16 (doc-only, fully verified). The concurrent Plan 15 §6/§7/§9 work is separate; the 05.1-06 re-review is the next human gate.

---
*Phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts*
*Completed: 2026-08-22*
