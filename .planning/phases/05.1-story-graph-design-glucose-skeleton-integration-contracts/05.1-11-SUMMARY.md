---
phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts
plan: 11
subsystem: story-graph
tags: [story-graph, reachability-tests, replan-invariants, diagram, svg, ascii, edit-allowed, continue-to-mc, disease-mutants, cast-pdb, 6cfo, ndufs8]

# Dependency graph
requires:
  - phase: 05.1 (plans 01-05 + expansion + 07/08/09)
    provides: The 43-node post-replan glucose skeleton (14 edit-allowed, 0 single-Continue, 8 disease-mutant promotions, pyr.pdh 6CFO cast, etc.complex_i DIS-NDUFS8-01-cand, tca.citrate_synthase edit:structural) + the 5-test SC2 reachability suite + the renderer tool
  - phase: 05.1-07 (glycolysis+pyruvate replan)
    provides: gly.pyruvate_kinase promotion, pyr.pdh 2OZL->6CFO cast fix, 5 Continue-to-MC conversions in glycolysis+anaerobic
  - phase: 05.1-08 (TCA replan)
    provides: 4 TCA enzyme promotions (IDH3A/SUCLG1/FH/MDH2), tca.citrate_synthase edit:structural reframe, 5 Continue-to-MC conversions in TCA
  - phase: 05.1-09 (ETC+ATP replan)
    provides: 3 ETC complex promotions (SDHA/CYC1/COX4I1), etc.complex_i PLACEHOLDER->DIS-NDUFS8-01-cand fix, 4 Continue-to-MC conversions in ETC
provides:
  - 3 new SC2 reachability test methods that machine-check the 2 replan invariants (0 single-Continue nodes; 14 edit-allowed nodes) + the cast-PDB/claim_id metadata fixes
  - 8-test reachability suite (5 original + 3 new), all GREEN, zero regression across 206 full-suite tests
  - Regenerated story-graph diagram (SVG + ASCII) reflecting the 14 edit-allowed nodes + the new MC/Observe edges
  - Updated 05.1-GRAPH-DIAGRAM.md with the post-replan narrative (8 promotions table, 2OZL->6CFO fix, PLACEHOLDER->DIS-NDUFS8-01-cand fix, edit:structural reframe, 15 Continue-to-MC conversions, Disease-Mutant Replan Changes subsection)
affects: [05.1-06 (human review checkpoint — now has the machine-checked invariants + the visual diagram artifact for the FINAL post-replan skeleton), Phase 6 (Qt UI MVP — the 14 edit-allowed nodes + 0 single-Continue invariant is locked), Phase 7 (the DIS-*-cand + CAST-*-cand claims to approve)]

# Tech tracking
tech-stack:
  added: []
  patterns: [replan-invariant tests (machine-check structural truths so a future regression fails loudly), renderer auto-detection of edit-allowed nodes from edit:enzyme tags]

key-files:
  created: []
  modified:
    - tests/test_glucose_reachability.py (updated docstrings + 3 new test methods)
    - .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.svg (regenerated)
    - .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.txt (regenerated)
    - .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-GRAPH-DIAGRAM.md (updated with post-replan narrative)

key-decisions:
  - "The 2 replan invariants (0 single-Continue; 14 edit-allowed) are now machine-checked by dedicated test methods — a future regression that re-introduces a single-Continue node or drops an edit:enzyme tag fails loudly."
  - "The pyr.pdh 2OZL->6CFO cast fix + the etc.complex_i PLACEHOLDER->DIS-NDUFS8-01-cand claim_id fix are machine-checked by a dedicated test method."
  - "The tca.citrate_synthase edit:structural reframe is machine-checked: the node carries the edit:structural tag AND has NO DIS-* claim_id (it is structural-exploration, not disease-mutant)."
  - "The renderer auto-detects edit-allowed nodes from edit:enzyme tags (line 121) + edit:offer choices (line 154), so regenerating after plans 07-09 automatically reflects the 14 edit-allowed nodes + the new MC edges — no renderer code changes needed."

patterns-established:
  - "Replan-invariant test pattern: after a structural replan, add dedicated test methods that assert the new invariants (not just that the old tests still pass) — so a regression is caught at the specific invariant, not as a vague failure."
  - "Diagram regeneration as a verification step: running the renderer after a skeleton replan confirms the visual artifact matches the JSON state (well-formed SVG, correct node counts, correct edit-allowed markers)."

# Metrics
duration: 20 min
completed: 2026-08-20
---

# Phase 5.1 Plan 11: Reachability Tests + Diagram Regen for Post-Replan Skeleton Summary

**3 new SC2 reachability test methods machine-checking the 2 replan invariants (0 single-Continue, 14 edit-allowed) + the cast-PDB/claim_id fixes (8 tests GREEN, 206 suite zero regression), + regenerated SVG/ASCII diagram showing 14 edit-allowed nodes + new MC edges, + updated GRAPH-DIAGRAM.md with the Disease-Mutant Replan Changes subsection**

## Performance

- **Duration:** 20 min
- **Started:** 2026-08-20T19:34:11Z
- **Completed:** 2026-08-20T19:54:28Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Added 3 new SC2 reachability test methods that machine-check the Phase 5.1 replan invariants:
  - `test_no_single_continue_choice` — asserts 0 single-Continue nodes (Driver 2: the Continue-to-MC conversion added an `mc:observe` Observe second choice to every formerly single-Continue node; a future regression re-introducing a single-Continue node fails loudly)
  - `test_14_edit_allowed_nodes` — asserts exactly 14 `edit:enzyme:*` nodes with `edit:offer` choices + the exact 14-id set + `tca.citrate_synthase` carries the `edit:structural` tag with NO `DIS-*` claim (Driver 1: the 8 disease-mutant promotions grew the set from 5+shuffle to 14)
  - `test_pdh_cast_pdb_fix_and_complex_i_claim_id` — asserts `pyr.pdh` loads `pdb:6CFO` (NOT `pdb:2OZL`) + carries `DIS-PDHA1-01-cand` + `CAST-PDH-WT-PDB-01-cand`; `etc.complex_i` claim_ids == `['DIS-NDUFS8-01-cand']` (PLACEHOLDER absent)
- Updated module + class docstrings to reflect the post-replan state (43 nodes unchanged; 14 edit-allowed; 0 single-Continue; 8 disease-mutant promotions; pyr.pdh 2OZL→6CFO cast fix; etc.complex_i PLACEHOLDER→DIS-NDUFS8-01-cand; tca.citrate_synthase edit:structural reframe; Continue-to-MC conversion)
- All 8 reachability tests PASS (5 original + 3 new); full suite 206 tests, zero regression; AST import gate clean (no pymol/PyQt5)
- Regenerated the story-graph diagram (SVG + ASCII) via `tools/render_story_graph.py` (43 nodes, 76 edges, exit 0; SVG well-formed XML)
- SVG shows 14 edit-allowed node markers (13 cream `#FFF8DC` fills + 1 gold-bordered purple RNG diamond for `tca.shuffle`) + legend swatches (15 `DAA520` gold-stroke occurrences = 14 nodes + 1 legend)
- ASCII summary line: `43 nodes | 11 endings (1T/3G/2N/5B) | 14 edit-allowed | 1 RNG`
- Updated `05.1-GRAPH-DIAGRAM.md` with the post-replan narrative: updated mermaid diagram (8 newly promoted nodes marked with gear icon, pyr.pdh PDB 2OZL→6CFO, etc.complex_i DIS-NDUFS8-01-cand, tca.citrate_synthase edit:structural, 15 mc:observe Observe edges + 8 new edit:offer edges, updated class assignments); rewritten Edit-Allowed Nodes section (14-row table with [PROMOTED] markers + DIS-*-cand claim_ids); new "Disease-Mutant Replan Changes" subsection (8 promotions table, 2OZL→6CFO fix, PLACEHOLDER→DIS-NDUFS8-01-cand fix, edit:structural reframe, 15 Continue-to-MC conversions, SC5 cross-reference)

## Task Commits

Each task was committed atomically:

1. **Task 1: Update reachability tests - docstrings + 3 replan-invariant assertions** - `84663bd` (feat)
2. **Task 2: Regenerate story-graph diagram + update GRAPH-DIAGRAM.md** - `04b3858` (feat)

## Files Created/Modified

- `tests/test_glucose_reachability.py` — updated docstrings + 3 new test methods (test_no_single_continue_choice, test_14_edit_allowed_nodes, test_pdh_cast_pdb_fix_and_complex_i_claim_id); 8 tests total, all GREEN
- `.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.svg` — regenerated from the post-replan skeleton (43 nodes, 76 edges, 14 edit-allowed markers)
- `.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.txt` — regenerated ASCII diagram (summary: 43 nodes | 11 endings | 14 edit-allowed | 1 RNG)
- `.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-GRAPH-DIAGRAM.md` — updated with post-replan narrative + Disease-Mutant Replan Changes subsection

## Decisions Made

- **Replan invariants are machine-checked by dedicated tests** (not just "old tests still pass"): the 3 new test methods assert the specific replan truths (0 single-Continue, 14 edit-allowed, 6CFO cast, DIS-NDUFS8-01-cand claim) so a regression is caught at the exact invariant that broke, not as a vague downstream failure.
- **Renderer requires no code changes**: the renderer auto-detects edit-allowed nodes from `edit:enzyme:` tags (line 121) + edit:offer choices (line 154), so regenerating after plans 07-09 automatically reflects the 14 edit-allowed nodes + the new MC edges. The only code change was to the test file + the doc files.
- **Pre-existing uncommitted `05.1-DESIGN.md` changes (plan 10's SC5 table update) were NOT committed under plan 11**: these changes belong to plan 10, not plan 11. They were left unstaged. The GRAPH-DIAGRAM.md cross-references the SC5 table in DESIGN.md (updated in plan 10), acknowledging the dependency.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Pre-existing uncommitted 05.1-DESIGN.md changes found**
- **Found during:** Task 2 (diagram regen + GRAPH-DIAGRAM.md update)
- **Issue:** `git status` showed `05.1-DESIGN.md` was already modified (247 insertions, 116 deletions) before I started Task 2 — these are plan 10's SC5 table update (pyr.pdh 2OZL→6CFO, 14 edit-allowed nodes, mc:observe contract note, etc.), executed but never committed. If committed under plan 11, the attribution would be incorrect.
- **Fix:** Left `05.1-DESIGN.md` unstaged — committed only the 3 files I modified (05.1-graph.svg, 05.1-graph.txt, 05.1-GRAPH-DIAGRAM.md). The DESIGN.md changes remain uncommitted for plan 10 to formally complete (or for the user to decide how to attribute them).
- **Files modified:** (none — the fix was to NOT modify/commit DESIGN.md)
- **Verification:** `git status` after commit shows only `05.1-DESIGN.md` as unstaged; the 3 plan-11 files are committed
- **Committed in:** `04b3858` (Task 2 commit — only 3 files staged)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** No scope creep. The pre-existing DESIGN.md changes are plan 10's work, correctly left for plan 10 to commit. Plan 11's scope (tests + diagram + GRAPH-DIAGRAM.md) was completed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The SC2 reachability tests now machine-check the 2 replan invariants (0 single-Continue; 14 edit-allowed) + the cast-PDB/claim_id fixes. All 8 tests GREEN, 206 full suite zero regression.
- The diagram (SVG + ASCII) is regenerated from the post-replan skeleton and shows the 14 edit-allowed nodes + the new MC/Observe edges. The SVG is well-formed XML.
- The GRAPH-DIAGRAM.md is the accurate visual-narrative artifact for the post-replan skeleton, with the Disease-Mutant Replan Changes subsection documenting all replan changes.
- **Remaining Phase 5.1 plans:** 05.1-10 (DESIGN.md SC5 table — has uncommitted changes, needs formal completion) + 05.1-06 (human review checkpoint — reviews the FINAL post-replan skeleton with all replan changes). After both, Phase 5.1 is COMPLETE and Phase 6 (Qt UI MVP) is UNBLOCKED.
- **Pre-existing uncommitted `05.1-DESIGN.md` changes** (plan 10's work) should be committed separately as plan 10's work before or alongside the 05.1-06 human review checkpoint.

---
*Phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts*
*Completed: 2026-08-20*
