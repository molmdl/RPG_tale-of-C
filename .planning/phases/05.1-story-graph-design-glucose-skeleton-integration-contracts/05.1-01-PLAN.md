---
phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/story_glucose/intro.json
  - data/story_glucose/glycolysis.json
  - data/story_glucose/pyruvate_branch.json
autonomous: true

must_haves:
  truths:
    - "The intro.select node offers a character-select choice with a choice.goto edge to gly.start (glucose), plus fa.stub and alc.stub stubs for Phase 8"
    - "The glycolysis path (gly.start -> gly.g6p -> gly.pfk -> gly.fbp_to_pyruvate -> gly.pyruvate) is a chain of single-option 'Continue' choices using choice.goto (NOT on_enter_divert) — the BFS can traverse every edge"
    - "The gly.pfk node cites the approved claim_ids GLY-PFK-01 and CAST-PFK-PDB-01, carries tags ['stage:glycolysis', 'edit:enzyme:gly.pfk'], and has an edit:offer choice -> edit.prompt (binding the node to its enzyme for the EditRouter)"
    - "The pyruvate branch node (pyr.branch) offers two cond-gated choices: aerobic (cond 'not flags.host_o2_low' -> pyr.pdh) and anaerobic (cond 'flags.host_o2_low' -> anaer.entry, effects set anaerobic=true)"
    - "The anaerobic 3-ending branch is reachable via choice.goto: anaer.entry offers 3 choices to anaer.lactic (Good), anaer.ethanolic (Normal), anaer.crisis (Bad)"
    - "Every non-ending node has at least one choice.goto edge (no on_enter_divert forward paths — the BFS does not traverse on_enter_divert)"
  artifacts:
    - path: "data/story_glucose/intro.json"
      provides: "intro.select character-select + fa.stub/alc.stub Phase 8 stubs"
      contains: "intro.select"
    - path: "data/story_glucose/glycolysis.json"
      provides: "glycolysis path nodes (gly.start through gly.pyruvate)"
      contains: "gly.pfk"
    - path: "data/story_glucose/pyruvate_branch.json"
      provides: "pyruvate aerobic/anaerobic branch + PDH + anaerobic 3-ending branch"
      contains: "pyr.branch"
  key_links:
    - from: "data/story_glucose/intro.json (intro.select)"
      to: "data/story_glucose/glycolysis.json (gly.start)"
      via: "choice.goto edge"
      pattern: "gly\\.start"
    - from: "data/story_glucose/glycolysis.json (gly.pfk)"
      to: "data/story_glucose/glycolysis.json (gly.fbp_to_pyruvate)"
      via: "choice.goto (Continue)"
      pattern: "gly\\.fbp_to_pyruvate"
    - from: "data/story_glucose/pyruvate_branch.json (pyr.branch)"
      to: "data/story_glucose/pyruvate_branch.json (pyr.pdh)"
      via: "choice.goto with cond 'not flags.host_o2_low'"
      pattern: "pyr\\.pdh"
    - from: "data/story_glucose/pyruvate_branch.json (pyr.branch)"
      to: "data/story_glucose/pyruvate_branch.json (anaer.entry)"
      via: "choice.goto with cond 'flags.host_o2_low'"
      pattern: "anaer\\.entry"
    - from: "gly.pfk (edit-allowed node)"
      to: "edit.prompt (in bad_endings.json, Plan 03)"
      via: "edit:offer choice.goto"
      pattern: "edit\\.prompt"
---

<objective>
Author the EARLY-PATH half of the glucose story graph skeleton JSON: the character-select intro, the glycolysis chain (glucose -> pyruvate, anchored on the approved-claim PFK control point), and the pyruvate aerobic/anaerobic branch point + its 3-ending anaerobic subtree.

Purpose: This is the front half of the real glucose story graph topology (SC1). The skeleton is STRUCTURE + PLACEHOLDER TEXT, not cited content — text_dramatic/text_teaching are placeholder/TBD except where an approved claim already covers the node (gly.pfk). Every forward path uses choice.goto (NOT on_enter_divert — the BFS does not traverse on_enter_divert; this is the single most important structural constraint).
Output: 3 JSON files under data/story_glucose/ (14 nodes total).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-RESEARCH-pathway-topology.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-RESEARCH-graph-contracts.md
@c14/story/model.py
@c14/story/validate.py
@c14/story/graph.py
@data/story/intro.json
@data/citations.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author intro.json + glycolysis.json + pyruvate_branch.json</name>
  <files>data/story_glucose/intro.json, data/story_glucose/glycolysis.json, data/story_glucose/pyruvate_branch.json</files>
  <action>
Create the directory `data/story_glucose/` then author 3 JSON files. Each file is a fragment `{"nodes": {id: {...}}}` — the node id is the JSON key (the loader injects it as `Node.id`; do NOT duplicate the id inside the node body). Use the EXISTING Node/Choice/MolAction schema from `c14/story/model.py` — ZERO schema change.

CRITICAL STRUCTURAL RULE: every non-ending node MUST have at least one `choice` with a `goto` edge. The BFS reachability checker (`c14/story/validate.py:check_reachability`) ONLY follows `choice.goto` — it does NOT traverse `on_enter_divert` (verified dormant: never referenced by interpreter/engine). Do NOT use `on_enter_divert` for any forward path. Use single-option "Continue" choices for deterministic advances: `{"label": "Continue", "goto": "<next.node>"}`.

CITATION-GATE COEXISTENCE: the skeleton uses `PLACEHOLDER_PHASE7` claim_ids for nodes whose science is pending Phase 7 approval (matching the toy fixture's `placeholder-intro` precedent — the gate is a pre-ship, manually-invoked CLI tool, never auto-run on `data/story/`; it is NOT run on the skeleton during Phase 5.1). The 4-5 APPROVED first-batch claim_ids (in `data/citations.json`) ARE used for the nodes they cover. If someone DID run the gate on the skeleton today, only the `PLACEHOLDER_PHASE7` nodes would fail (expected — the skeleton is a design artifact, not shippable content).

=== data/story_glucose/intro.json (3 nodes) ===
- `intro.select` — character-select. type: choice. SC5: MC-choice. claim_ids: ["PLACEHOLDER_PHASE7"]. text_dramatic: "Choose your form — which carbon are you?". text_teaching: "TBD — Phase 7 content (character-select screen)". on_enter: [{"op": "hide_all"}]. choices: [
    {"label": "Glucose — enter the glycolysis path", "goto": "gly.start", "tags": ["char:glucose"]},
    {"label": "Fatty acid (coming in Phase 8)", "goto": "fa.stub", "tags": ["char:fa", "stub"]},
    {"label": "Alcohol (coming in Phase 8)", "goto": "alc.stub", "tags": ["char:alc", "stub"]}
  ]. NOTE: the controller (Phase 6) reads the chosen index and calls engine.start(character="glucose", ...) — the choice effects do NOT set state.character directly (that's the controller's job, per research OQ7). Use placeholder effects: {} (no state mutation in the skeleton).
- `fa.stub` — Phase 8 placeholder. claim_ids: ["PLACEHOLDER_PHASE8"]. text_dramatic: "This character is not yet available. Returning to character select." on_enter: [{"op": "hide_all"}]. choices: [{"label": "Back to character select", "goto": "intro.select"}]. NOT is_ending (a dead-end stub that returns; not counted in the ending distribution).
- `alc.stub` — Phase 8 placeholder. Same shape as fa.stub. choices: [{"label": "Back to character select", "goto": "intro.select"}].

=== data/story_glucose/glycolysis.json (5 nodes) ===
All are `story` type (narrative forward-only) EXCEPT `gly.pfk` which is `story + edit-allowed` (both). Every node has a single "Continue" choice.goto to the next node. on_enter: [{"op": "hide_all"}] for all (no PDB loads except gly.pfk which has an approved cast).
- `gly.start` — glucose entry (cytosol). claim_ids: ["PLACEHOLDER_PHASE7_GLYCOLYSIS"]. text_dramatic: "TBD — glucose arrives in the cytosol." choices: [{"label": "Continue", "goto": "gly.g6p"}].
- `gly.g6p` — hexokinase step (glucose-6-phosphate). claim_ids: ["PLACEHOLDER_PHASE7_GLYCOLYSIS"]. text_dramatic: "TBD — hexokinase traps glucose as G6P." choices: [{"label": "Continue", "goto": "gly.pfk"}].
- `gly.pfk` — **PFK-1 committed rate-limiting step** (F6P -> FBP). THIS IS AN APPROVED-CLAIM NODE. claim_ids: ["GLY-PFK-01", "CAST-PFK-PDB-01"] (both APPROVED in data/citations.json). tags: ["stage:glycolysis", "edit:enzyme:gly.pfk"] (the edit:enzyme tag DECLARES edit-allowed + binds enzyme_id="gly.pfk" for the EditRouter — zero schema change, per research Q3 Option 1 RECOMMENDED). text_dramatic: "TBD — PFK-1, the committed step of glycolysis." text_teaching: "TBD — Phase 7 teaching text citing GLY-PFK-01." on_enter: [{"op": "hide_all"}, {"op": "load", "target": "pdb:4PFK", "args": {"object": "pfk"}}] (PDB 4PFK is APPROVED via CAST-PFK-PDB-01). choices: [
    {"label": "Continue glycolysis", "goto": "gly.fbp_to_pyruvate"},
    {"label": "Try to edit PFK's active site", "goto": "edit.prompt", "tags": ["edit:offer"]}
  ] (the edit:offer choice launches the edit UI at runtime; the edit.prompt stub node is authored in Plan 03's bad_endings.json).
- `gly.fbp_to_pyruvate` — post-PFK glycolysis (FBP -> GAP -> ... -> PEP -> pyruvate; net 2 ATP, 2 NADH). claim_ids: ["PLACEHOLDER_PHASE7_GLYCOLYSIS"]. text_dramatic: "TBD — the rest of glycolysis unfolds; net 2 ATP, 2 NADH." choices: [{"label": "Continue", "goto": "gly.pyruvate"}].
- `gly.pyruvate` — pyruvate product. claim_ids: ["PLACEHOLDER_PHASE7_GLYCOLYSIS"]. text_dramatic: "TBD — you arrive at pyruvate, the branch point." choices: [{"label": "Continue", "goto": "pyr.branch"}].

=== data/story_glucose/pyruvate_branch.json (6 nodes) ===
- `pyr.branch` — THE central aerobic/anaerobic branch point. type: choice. SC5: MC-choice. claim_ids: ["PLACEHOLDER_PHASE7_ANAEROBIC"] (the branch-point fact traces to 05-RESEARCH-anaerobic CANDIDATE sources — NOT first-batch approved). text_dramatic: "TBD — the air is thin. What fate, little carbon?" on_enter: [{"op": "hide_all"}]. choices: [
    {"label": "Proceed aerobically (O2 available) -> acetyl-CoA -> TCA", "goto": "pyr.pdh", "cond": "not flags.host_o2_low", "tags": ["branch:aerobic"]},
    {"label": "The air runs out — ferment (anaerobic)", "goto": "anaer.entry", "cond": "flags.host_o2_low", "effects": {"set": {"anaerobic": true}}, "tags": ["branch:anaerobic"]}
  ]. CRITICAL: the BFS IGNORES cond — it traverses BOTH goto edges, so both the aerobic and anaerobic subtrees are structurally reachable. The "True unreachable anaerobically" is a RUNTIME cond concern, NOT a structural one (per research Q3 / Q5).
- `pyr.pdh` — pyruvate dehydrogenase complex (pyruvate -> acetyl-CoA + CO2 + NADH; the FIRST carbon exits as CO2 here). type: story + edit-allowed. SC5: both. claim_ids: ["PLACEHOLDER_PHASE7"] (ATP-SOUL-01 is CANDIDATE; PDH PDB not in first batch). tags: ["stage:pyruvate", "edit:enzyme:pyr.pdh"]. text_dramatic: "TBD — PDH strips your first carbon as CO2." on_enter: [{"op": "hide_all"}] (NO PDB load — PDH PDB is TBD Phase 9). choices: [
    {"label": "Continue into the TCA cycle", "goto": "tca.entry"},
    {"label": "Try to edit the PDH complex", "goto": "edit.prompt", "tags": ["edit:offer"]}
  ].
- `anaer.entry` — anaerobic entry (O2 low, fermentation begins). type: choice. SC5: MC-choice (the planner's design decision: anaer.entry offers the 3-way fermentation-outcome choice — this OVERRIDES the research's tentative `story/neither` classification, which left the branching mechanism unspecified; the skeleton needs a choice here for the 3 endings to be reachable). claim_ids: ["PLACEHOLDER_PHASE7_ANAEROBIC"] (AN-G-01 CANDIDATE). text_dramatic: "TBD — the air runs out; your host switches to fermentation." on_enter: [{"op": "hide_all"}]. choices: [
    {"label": "Lactic fermentation (stay as lactate)", "goto": "anaer.lactic", "tags": ["fermentation:lactic"]},
    {"label": "Ethanolic fermentation (CO2 + ethanol)", "goto": "anaer.ethanolic", "tags": ["fermentation:ethanolic"]},
    {"label": "The energy runs out — crisis", "goto": "anaer.crisis", "tags": ["fermentation:crisis"]}
  ]. NOTE: this is a placeholder 3-way choice; Phase 7 may refine (e.g. cond-gate by host type, or RNG). Flag in the design doc (Plan 05).
- `anaer.lactic` — **lactic fermentation** (pyruvate -> lactate via LDH; NAD+ regenerated; NO CO2; carbon RETAINED). type: ending. is_ending: "good". claim_ids: ["PLACEHOLDER_PHASE7_ANAEROBIC"] (AN-G-02 CANDIDATE; LDH PDB not in first batch). text_dramatic: "TBD — you are retained as lactate." text_teaching: "TBD — Phase 7 teaching text." on_enter: [{"op": "hide_all"}]. choices: [].
- `anaer.ethanolic` — **ethanolic fermentation** (pyruvate -> acetaldehyde + CO2 -> ethanol; CO2 RELEASED). type: ending. is_ending: "normal". claim_ids: ["PLACEHOLDER_PHASE7_ANAEROBIC"] (AN-G-03 CANDIDATE). text_dramatic: "TBD — you drift off as CO2 and ethanol." text_teaching: "TBD — Phase 7 teaching text. [HOST-TENSION FLAG: mammalian host does not do ethanolic fermentation — this is a yeast pathway; human review needed before Phase 7]." on_enter: [{"op": "hide_all"}]. choices: []. THIS NODE IS FLAGGED FOR HUMAN REVIEW (research OQ1: mammal-vs-yeast host tension).
- `anaer.crisis` — anaerobic energy crisis (fermentation can't keep up -> host death). type: ending. is_ending: "bad". claim_ids: ["PLACEHOLDER_PHASE7_ANAEROBIC"] (AN-ETC-02 CANDIDATE). text_dramatic: "TBD — fermentation cannot sustain the host; the host passes." on_enter: [{"op": "hide_all"}]. choices: [].

JSON conventions: 2-space indent, trailing newline. No inline JSON comments (JSON doesn't support them). The `edit:offer` tag goes on the CHOICE; the `edit:enzyme:<id>` tag goes on the NODE (per research Q3 Option 1: the enzyme_id binding is a node property; the edit:offer marks which choice launches the edit UI).
  </action>
  <verify>
Run this Python 3.6 one-liner to confirm all 3 files parse + every node loads via Node.from_dict (injecting the JSON key as id, as the loader does):
```bash
python3.6 -c "
import json, sys
sys.path.insert(0, '.')
from c14.story.model import Node
for fn in ['intro.json', 'glycolysis.json', 'pyruvate_branch.json']:
    with open('data/story_glucose/' + fn) as f:
        data = json.load(f)
    for nid, nd in data['nodes'].items():
        nd['id'] = nid
        n = Node.from_dict(nd)
        assert n.id == nid, 'id mismatch: ' + nid
        if not n.is_ending:
            assert len(n.choices) > 0, 'non-ending node has no choices (dead-end, BFS unreachable): ' + nid
print('OK: 14 nodes parsed across 3 files, all non-ending nodes have >=1 choice.goto')
"
```
Then verify the gly.pfk node carries the approved claim_ids + the edit:enzyme tag:
```bash
python3.6 -c "
import json
d = json.load(open('data/story_glucose/glycolysis.json'))['nodes']['gly.pfk']
assert 'GLY-PFK-01' in d['claim_ids'] and 'CAST-PFK-PDB-01' in d['claim_ids'], 'gly.pfk missing approved claim_ids'
assert 'edit:enzyme:gly.pfk' in d['tags'], 'gly.pfk missing edit:enzyme tag'
assert any(c.get('goto') == 'edit.prompt' for c in d['choices']), 'gly.pfk missing edit:offer choice'
print('OK: gly.pfk has approved claim_ids + edit:enzyme tag + edit:offer choice')
"
```
  </verify>
  <done>
3 JSON files exist under data/story_glucose/ with 14 nodes total (intro.select, fa.stub, alc.stub, gly.start, gly.g6p, gly.pfk, gly.fbp_to_pyruvate, gly.pyruvate, pyr.branch, pyr.pdh, anaer.entry, anaer.lactic, anaer.ethanolic, anaer.crisis). Every non-ending node has >=1 choice.goto edge. gly.pfk cites the 2 approved claim_ids + carries the edit:enzyme:gly.pfk tag + has an edit:offer choice. pyr.branch has 2 cond-gated choices (aerobic -> pyr.pdh, anaerobic -> anaer.entry). anaer.entry has 3 choices to the anaerobic endings. No on_enter_divert used for any forward path.
  </done>
</task>

</tasks>

<verification>
- `python3.6 -c "import json; ..."` confirms all 14 nodes parse via Node.from_dict
- gly.pfk carries the approved claim_ids + edit:enzyme tag (greppable: `grep -l "edit:enzyme:gly.pfk" data/story_glucose/glycolysis.json`)
- No on_enter_divert in any file (greppable: `grep -r "on_enter_divert" data/story_glucose/` returns nothing)
- pyr.branch has both cond-gated choices (greppable: `grep "host_o2_low" data/story_glucose/pyruvate_branch.json`)
</verification>

<success_criteria>
- 3 skeleton JSON files exist at data/story_glucose/ with the 14 early-path nodes
- Every forward path uses choice.goto (BFS-traversable); no on_enter_divert forward paths
- gly.pfk (the approved-claim anchor) cites GLY-PFK-01 + CAST-PFK-PDB-01 + carries the edit:enzyme:gly.pfk tag
- The pyruvate branch point (pyr.branch) has 2 cond-gated choices (aerobic/anaerobic); the BFS traverses both
- The 3 anaerobic endings (lactic=Good, ethanolic=Normal, crisis=Bad) are reachable via choice.goto from anaer.entry
- Placeholder claim_ids use PLACEHOLDER_PHASE7 (not fabricated / not in the registry — the gate is not run on the skeleton)
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-01-SUMMARY.md`
</output>
