---
phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts
plan: 11
type: execute
wave: 2
depends_on: ["05.1-04", "05.1-07", "05.1-08", "05.1-09"]
files_modified:
  - tests/test_glucose_reachability.py
  - .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.svg
  - .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.txt
  - .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-GRAPH-DIAGRAM.md
autonomous: true

must_haves:
  truths:
    - "tests/test_glucose_reachability.py passes GREEN on the post-replan 43-node skeleton (all 4 tiers reachable) + RED on both orphaned variants"
    - "the tests assert the 2 new replan invariants: (a) NO node has a single 'Continue' choice (the Continue-to-MC invariant); (b) exactly 14 nodes have an edit:enzyme tag + an edit:offer choice (the promotion invariant)"
    - "the tests assert the pyr.pdh cast-PDB fix (on_enter load target == pdb:6CFO, not 2OZL) + the etc.complex_i claim_id fix (DIS-NDUFS8-01-cand present, PLACEHOLDER_PHASE7_ETC absent)"
    - "05.1-graph.svg + 05.1-graph.txt are regenerated via tools/render_story_graph.py and show the 14 edit-allowed nodes (golden) + the new Observe/MC edges"
    - "05.1-GRAPH-DIAGRAM.md is updated for the new structure (14 edit-allowed, 0 single-Continue, the 15 MC conversions, the DIS-*-cand claim references)"
  artifacts:
    - path: "tests/test_glucose_reachability.py"
      provides: "SC2 reachability tests + the 2 new replan-invariant assertions"
      contains: "single"
    - path: ".planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.svg"
      provides: "regenerated swim-lane SVG of the post-replan skeleton"
      contains: "edit"
    - path: ".planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.txt"
      provides: "regenerated ASCII diagram of the post-replan skeleton"
      contains: "-->"
    - path: ".planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-GRAPH-DIAGRAM.md"
      provides: "updated diagram narrative + mermaid for the post-replan skeleton"
      contains: "edit:enzyme"
  key_links:
    - from: "tools/render_story_graph.py"
      to: "data/story_glucose/*.json"
      via: "reads the manifest + node fragments; auto-detects edit-allowed from edit:enzyme tags (line 121) + edit:offer choices (line 154)"
      pattern: "edit:enzyme:"
    - from: "tests/test_glucose_reachability.py"
      to: "data/story_glucose/*.json"
      via: "StoryGraph.load + check_reachability BFS"
      pattern: "StoryGraph\\.load"
---

<objective>
Update the SC2 reachability tests to enforce the 2 new replan invariants (0 single-Continue nodes; 14 edit-allowed nodes) + the cast-PDB/claim_id fixes, and regenerate the story-graph diagram (SVG + ASCII) + the GRAPH-DIAGRAM.md narrative for the post-replan skeleton.

Purpose: Driver 3 (doc + test updates). The tests machine-check the replan's structural truths (so a future regression that re-introduces a single-Continue node or drops an edit:enzyme tag fails loudly). The diagram is the visual artifact the 05.1-06 checkpoint reviewer uses. The renderer auto-detects edit-allowed nodes from edit:enzyme tags, so regenerating after plans 07-09 automatically reflects the 14 edit-allowed + the new MC edges.
Output: 4 updated files (tests + 3 diagram artifacts).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-REPLAN-CONTEXT.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-04-SUMMARY.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-07-SUMMARY.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-08-SUMMARY.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-09-SUMMARY.md
@tests/test_glucose_reachability.py
@tools/render_story_graph.py
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-GRAPH-DIAGRAM.md
@data/story_glucose/manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update reachability tests - docstrings + 2 new replan-invariant assertions + cast-PDB/claim_id fix assertions</name>
  <files>tests/test_glucose_reachability.py</files>
  <action>
Edit tests/test_glucose_reachability.py. The existing 5 tests still pass (node count 43, 11 endings, 4 tiers, red variants, no pymol import). Update the module + class docstrings to reflect the post-replan state (43 nodes UNCHANGED count; 14 edit-allowed nodes; 0 single-Continue nodes; 8 disease-mutant promotions; the pyr.pdh 2OZL->6CFO cast fix; the etc.complex_i PLACEHOLDER->DIS-NDUFS8-01-cand claim_id fix; the tca.citrate_synthase edit:structural reframe; the Continue-to-MC conversion). Then ADD 3 new test methods that machine-check the replan invariants:

1. test_no_single_continue_choice (the Continue-to-MC invariant):
   - Load the skeleton; for every node, assert NOT (len(choices)==1 and choices[0].get('label')=='Continue'). I.e. zero nodes have a single 'Continue' choice. (This enforces Driver 2 - a future regression that re-introduces a single-Continue node fails loudly.)
   - Assert the count of single-Continue nodes == 0.

2. test_14_edit_allowed_nodes (the promotion invariant):
   - Load the skeleton; collect nodes where any tag startswith 'edit:enzyme:'. Assert len == 14.
   - For each of the 14, assert it has an edit:offer choice (a choice whose tags include 'edit:offer' OR whose goto == 'edit.prompt').
   - Assert the 14 node ids are exactly: gly.pfk, pyr.pdh, tca.citrate_synthase, tca.aconitase, tca.shuffle, etc.complex_i, gly.pyruvate_kinase, tca.isocitrate_dh, tca.succinyl_coa_synthetase, tca.fumarase, tca.malate_dh, etc.complex_ii, etc.complex_iii, etc.complex_iv.
   - Assert tca.citrate_synthase has the 'edit:structural' tag (the reframe marker) AND has NO 'DIS-' claim_id (no disease mutant).

3. test_pdh_cast_pdb_fix_and_complex_i_claim_id (the metadata fixes):
   - Load the skeleton; assert pyr.pdh on_enter load target == 'pdb:6CFO' (NOT 'pdb:2OZL').
   - Assert pyr.pdh claim_ids include 'DIS-PDHA1-01-cand' + 'CAST-PDH-WT-PDB-01-cand'.
   - Assert etc.complex_i claim_ids == ['DIS-NDUFS8-01-cand'] (PLACEHOLDER_PHASE7_ETC absent).

Do NOT remove or weaken the existing 5 tests. Do NOT import pymol/PyQt5 (the AST import gate scans tests/). Use the same StoryGraph.load + Node access patterns as the existing tests. Run with python3.6 -m unittest.
  </action>
  <verify>
python3.6 -m unittest tests.test_glucose_reachability -v  (8 tests PASS: the original 5 + the 3 new invariant tests). Confirm zero failures.
  </verify>
  <done>
tests/test_glucose_reachability.py has 8 tests, all PASS; the 2 replan invariants (0 single-Continue, 14 edit-allowed) + the cast-PDB/claim_id fixes are machine-checked.
  </done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the story-graph diagram (SVG + ASCII) + update GRAPH-DIAGRAM.md</name>
  <files>.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.svg, .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.txt, .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-GRAPH-DIAGRAM.md</files>
  <action>
1. Regenerate the SVG + ASCII diagram by running the renderer against the post-replan skeleton (the renderer is CWD-independent; it auto-detects edit-allowed nodes from edit:enzyme tags at line 121 + edit:offer choices at line 154, so the 14 edit-allowed nodes + the new MC/Observe edges are reflected automatically):
   python3.6 tools/render_story_graph.py
   (Default --story-dir = data/story_glucose; default --output-dir = the phase 5.1 planning dir. This overwrites 05.1-graph.svg + 05.1-graph.txt in place.)
   - Confirm exit 0 + the regenerated SVG opens (xml well-formed: python3.6 -c "import xml.etree.ElementTree as ET; ET.parse('.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.svg')").
   - Confirm the SVG contains 14 edit-allowed node markers (grep -c 'edit:enzyme\|fill="#FFF8DC"' the SVG - the renderer uses #FFF8DC fill for edit-allowed nodes per line 487).

2. Update 05.1-GRAPH-DIAGRAM.md to reflect the post-replan structure:
   - Update the node inventory + counts (43 nodes; 14 edit-allowed; 0 single-Continue; 15 Continue-to-MC conversions; the 8 disease-mutant promotions).
   - Update the mermaid diagram (or the embedded diagram reference) to show the new edit-allowed nodes (golden) + the new Observe/MC edges (the second choice on each converted node).
   - Add a "Disease-Mutant Replan Changes" subsection documenting: the 8 promotions (+ their DIS-*-cand claim_ids), the pyr.pdh 2OZL->6CFO cast fix, the etc.complex_i PLACEHOLDER->DIS-NDUFS8-01-cand claim_id fix, the tca.citrate_synthase edit:structural reframe, and the Continue-to-MC conversion (the mc:observe tag). Cross-reference the 05.1-DESIGN.md SC5 table (updated in plan 10) for the full classification.
   - Preserve the existing stage-grouping + legend explanations; update only where the structure changed.
  </action>
  <verify>
1. python3.6 tools/render_story_graph.py && echo "renderer exit $?"
2. python3.6 -c "import xml.etree.ElementTree as ET; ET.parse('.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.svg'); print('SVG well-formed')"
3. grep -c "edit:enzyme\|FFF8DC" .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.svg (expect 14 edit-allowed markers)
4. head -40 .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-graph.txt (confirm the ASCII diagram shows the new nodes + edges)
5. python3.6 -m unittest tests.test_glucose_reachability -v  (still 8 tests PASS after the diagram regen - the diagram is doc-only, does not affect tests)
  </verify>
  <done>
05.1-graph.svg + 05.1-graph.txt regenerated from the post-replan skeleton (14 edit-allowed nodes marked, new MC edges shown); 05.1-GRAPH-DIAGRAM.md updated with the replan changes subsection; SVG well-formed; reachability tests still GREEN.
  </done>
</task>

</tasks>

<verification>
- python3.6 -m unittest tests.test_glucose_reachability -v  (8 tests PASS: 5 original + 3 new invariant tests)
- python3.6 -m unittest discover -s tests  (full suite passes - zero regression; the diagram regen + test additions do not break other tests)
- The regenerated SVG is well-formed XML + shows 14 edit-allowed node markers.
- The GRAPH-DIAGRAM.md documents the replan changes + cross-references the SC5 table.
</verification>

<success_criteria>
- The SC2 reachability tests machine-check the 2 replan invariants (0 single-Continue; 14 edit-allowed) + the cast-PDB/claim_id fixes; all 8 tests GREEN.
- The diagram (SVG + ASCII) is regenerated from the post-replan skeleton + shows the 14 edit-allowed nodes + the new MC edges.
- 05.1-GRAPH-DIAGRAM.md is the accurate visual-narrative artifact for the post-replan skeleton.
- Zero regression across the full test suite.
</success_criteria>

<output>
After completion, create .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-11-SUMMARY.md recording: the 3 new test methods, the 8-test PASS, the renderer invocation + SVG well-formedness, the 14 edit-allowed markers in the SVG, and the GRAPH-DIAGRAM.md updates.
</output>
