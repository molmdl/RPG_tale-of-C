---
phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts
plan: 10
type: execute
wave: 2
depends_on: ["05.1-05", "05.1-07", "05.1-08", "05.1-09"]
files_modified:
  - .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md
autonomous: true

must_haves:
  truths:
    - "the SC5 classification table in 05.1-DESIGN.md reflects the post-replan skeleton: 14 edit-allowed nodes (12 disease-mutant + 1 structural [CS] + 1 shared [tca.shuffle]), 0 single-Continue nodes, the 15 Continue-to-MC conversions, and the updated MC-choice/both/neither counts"
    - "the edit-node contract section lists all 14 edit-allowed nodes with their enzyme_id + DIS-*-cand claim_id (CANDIDATE, not approved) + the known-edit-branch/unknown-edit-bad-pool routing; the CS structural-exploration exception is explicitly documented"
    - "the IDH3A-default / IDH1-alternative decision is FLAGGED in the design doc for the 05.1-06 human-review checkpoint (skeleton uses IDH3A as DEFAULT; IDH1 R132H alternative is documented but NOT chosen unilaterally)"
    - "the restoration-narrative framing (diseased enzyme shown on_enter; player performs reverse-mutation; EditRouter routes known-edit to branch / unknown-edit to bad-pool) is documented as a Phase 7 CONTENT boundary (the skeleton carries the STRUCTURE: edit:enzyme tags + edit:offer choices + DIS-*-cand claim references; the edits.json entries + text + on_enter pre-edit MolActions are Phase 7)"
    - "the 2OZL-to-6CFO cast-PDB fix + the DIS-PDHA1-01-cand disease claim are documented for pyr.pdh; the 6CER mutant-reference option (b) is noted as a Phase 7 content decision"
  artifacts:
    - path: ".planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md"
      provides: "updated SC5 classification table (section 6) + expanded edit-node contract (section 5) + new disease-mutant open questions (section 9)"
      contains: "DIS-IDH3A-01-cand"
  key_links:
    - from: "the SC5 table"
      to: "data/story_glucose/*.json node ids"
      via: "the table cross-references the actual node ids + their post-replan tags/choices"
      pattern: "edit:enzyme|edit:structural|mc:observe"
---

<objective>
Update 05.1-DESIGN.md to reflect the post-replan skeleton (14 edit-allowed nodes, 0 single-Continue nodes, 15 Continue-to-MC conversions, 8 disease-mutant promotions, the pyr.pdh cast-PDB fix, the etc.complex_i claim_id fix, the CS structural-exploration reframe, and the IDH3A-default/IDH1-alternative flag). The design doc is the REVIEWABLE artifact the 05.1-06 human-review checkpoint reads; it must match the actual post-replan skeleton JSON exactly.

Purpose: Driver 3 (doc + test updates) of the replan. The design doc's SC5 table + edit-node contract + open-questions section are the human-review surface; they must be accurate before the checkpoint. This is STRUCTURE/documentation ONLY - no content (text/edits/PDB-loads) is authored here; the doc DESCRIBES the structure + flags Phase 7 content boundaries + assembles the disease-mutant open questions for the checkpoint.
Output: 1 updated design doc (05.1-DESIGN.md) with 3 section updates (SC5 table, edit-node contract, open questions).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-REPLAN-CONTEXT.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-RESEARCH-disease-mutants-glycolysis.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-RESEARCH-disease-mutants-tca.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-RESEARCH-disease-mutants-etc.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-05-SUMMARY.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-07-SUMMARY.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-08-SUMMARY.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-09-SUMMARY.md
@data/story_glucose/glycolysis.json
@data/story_glucose/pyruvate_branch.json
@data/story_glucose/tca.json
@data/story_glucose/etc_atp.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the SC5 classification table (section 6) + the edit-node contract (section 5) for the 14 edit-allowed + 0 single-Continue post-replan state</name>
  <files>.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md</files>
  <action>
Read the post-replan skeleton JSON (data/story_glucose/*.json - now finalized by plans 07-09) to derive the EXACT current tags/choices per node. Then update 05.1-DESIGN.md:

A. SC5 classification table (section 6) - reclassify every node against the post-replan skeleton. The 4 SC5 classes: MC-choice (>=2 non-edit choices, no edit:offer), edit-allowed (edit:enzyme tag + edit:offer, but if the only non-edit choice is a single Continue it is NOT MC), both (edit:enzyme + edit:offer + >=2 non-edit choices), neither (single non-Continue choice OR ending/stub). Post-replan expected counts (VERIFY against the JSON; the doc must match the JSON exactly, not this plan's expectation):
   - 0 nodes have a single "Continue" choice (the 12 exact-Continue + 3 borderline single-choice nodes all gained a 2nd choice in plans 07-09).
   - 14 edit-allowed nodes (12 disease-mutant with DIS-*-cand + 1 structural [tca.citrate_synthase, edit:structural, no DIS-*-cand] + 1 shared [tca.shuffle, edit:enzyme:tca.aconitase, secondary aconitase edit point]).
   - The 8 promoted nodes (gly.pyruvate_kinase, tca.isocitrate_dh, tca.succinyl_coa_synthetase, tca.fumarase, tca.malate_dh, etc.complex_ii, etc.complex_iii, etc.complex_iv) are now class "both".
   - The 4 existing edit-allowed disease-mutant nodes (gly.pfk, pyr.pdh, tca.aconitase, etc.complex_i) are class "edit-allowed" (each has exactly 2 choices: 1 Continue + 1 edit:offer = only 1 non-edit choice, which does NOT meet the SC5 "both" threshold of >=2 non-edit choices; they are NOT "both"). The 8 new promotions have Continue + Observe + edit:offer = "both". tca.shuffle has 2 weighted + edit:offer + cycle-trap = "both". tca.citrate_synthase has Continue + edit:offer = "edit-allowed" (structural).
   - The 15 Continue-to-MC nodes (12 exact-Continue + etc.complex_iv + etc.atp_synthase + anaer.ldh): the 7 that also got edit:offer are "both" (gly.pyruvate_kinase, tca.isocitrate_dh, tca.succinyl_coa_synthetase, tca.fumarase, etc.complex_ii, etc.complex_iii, etc.complex_iv); the remaining 8 are "MC-choice".
   - Add a "DIS-*-cand" column or annotation to the table marking each disease-mutant node's candidate claim; mark tca.citrate_synthase as "edit:structural (no disease mutant)".

B. Edit-node contract (section 5) - expand the per-edit-allowed-node table from 6 rows to 14 rows. For each of the 14 edit-allowed nodes, document: node id, enzyme_id (the edit:enzyme:<id> tag value), the disease-mutant DIS-*-cand claim_id (CANDIDATE pending Phase 7), the reverse-mutation signature (the "correct fix" - NAME ONLY, e.g. "PKLR R479H-to-H479R"; do NOT write the edits.json entry), the known-edit branch route (edit.prompt -> EditRouter -> branch node, Phase 7), the unknown-edit bad-pool fallback (edit.prompt -> bad.lost_connection / bad.released_from_host). Explicitly document:
   - tca.citrate_synthase as the STRUCTURAL-EXPLORATION exception (edit:structural tag; NO disease mutant; NO DIS-*-cand; the restoration narrative does NOT apply - the host's CS is not diseased; the hero just explores it).
   - tca.shuffle as the secondary aconitase edit point (edit:enzyme:tca.aconitase shared with tca.aconitase; the edit:offer routes to the same edit.prompt).
   - pyr.pdh cast-PDB fix (2OZL phospho-mimic -> 6CFO WT; the 6CER mutant-reference option (b) as a Phase 7 content decision).
   - etc.complex_i claim_id fix (PLACEHOLDER_PHASE7_ETC -> DIS-NDUFS8-01-cand).
   Add a subsection documenting the restoration-narrative framing (diseased enzyme shown on_enter via apply_edit disease mutation; player performs reverse-mutation; EditRouter routes known->branch / unknown->bad-pool) as a Phase 7 CONTENT boundary - the skeleton carries ONLY the structure (tags + edit:offer + DIS-*-cand claim references); the edits.json entries + text + on_enter pre-edit MolActions are Phase 7.

C. Choice-point contract (section 4) - add a one-line note that mc:observe choices (the Observe option added to converted nodes) follow the same contract as other choices (same-destination, different-experience); the SC3 mechanism is unchanged.

Preserve all other sections of 05.1-DESIGN.md (sections 1-3, 7-8) unchanged unless they reference the old 6-edit-allowed / single-Continue state (update those references to the new counts). Section 4 receives only the one-line mc:observe note from point C above; otherwise preserve it unchanged. Do NOT delete the original 13 open questions (section 9) - they are extended in Task 2.
  </action>
  <verify>
1. grep -c "edit:enzyme" 05.1-DESIGN.md (should reference 14 edit-allowed nodes)
2. python3.6 -c "import json,glob; n={}; [n.update(json.load(open(f))['nodes']) for f in glob.glob('data/story_glucose/*.json') if 'manifest' not in f]; ea=[k for k,v in n.items() if any(t.startswith('edit:enzyme:') for t in v.tags)]; sc=[k for k,v in n.items() if len(v.get('choices',[]))==1 and v['choices'][0].get('label')=='Continue']; assert len(ea)==14, ('edit-allowed='+str(len(ea))); assert len(sc)==0, ('single-Continue='+str(sc)); print('skeleton has 14 edit-allowed, 0 single-Continue - DESIGN.md must match')"
3. Read the updated SC5 table + edit-node contract in 05.1-DESIGN.md and confirm the node ids + DIS-*-cand claim_ids match the skeleton JSON exactly.
  </verify>
  <done>
05.1-DESIGN.md SC5 table + edit-node contract reflect the 14 edit-allowed + 0 single-Continue post-replan skeleton exactly; the CS structural exception + tca.shuffle shared edit point + pyr.pdh cast fix + etc.complex_i claim_id fix are documented; the restoration-narrative Phase 7 content boundary is explicit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the disease-mutant open questions to section 9 + flag the IDH3A/IDH1 decision for the 05.1-06 checkpoint</name>
  <files>.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md</files>
  <action>
Extend 05.1-DESIGN.md section 9 (Open Questions) with the NEW disease-mutant research open questions (do NOT remove the original 13 - append the new ones as a "Disease-Mutant Replan Open Questions" subsection). These feed the 05.1-06 human-review checkpoint. Assemble from the 3 research docs:

1. [CRITICAL] IDH3A vs IDH1 (tca.isocitrate_dh): Option A = IDH3A M204I-to-I204M (biochemically correct TCA enzyme, ClinVar Pathogenic, OMIM 619007 RP90) - the skeleton DEFAULT. Option B = IDH1 R132H-to-H132R (cancer-mutant, real mutant PDB 3INM, narratively powerful "the hero encounters a cancer-mutant version" but NOT the TCA enzyme - IDH1 is NADP+-dependent cytosolic, not the NAD+-dependent mitochondrial TCA enzyme). Flag: the human decides; the skeleton uses Option A as DEFAULT; if the human prefers Option B, the claim_id swaps to DIS-IDH1-01-cand + the PDB swaps to 3INM + the text reframes.

2. [CRITICAL] pyr.pdh cast PDB: 2OZL (phospho-mimic S264E mutant, WRONG) -> 6CFO (WT, 2.70 A) [skeleton DEFAULT] or 1NI4 (WT, 1.95 A, partial residues 30-309). Mutant reference: 6CER (V138M, 2.69 A). Phase 7 content decision: option (a) apply-edit (load 6CFO WT -> cmd.alter resi 138 -> diseased state, approximate geometry) vs option (b) load-mutant-PDB (fetch 6CER directly, real geometry, scientifically stronger per spec.md item 4 "reveal correct 3D model"). Flag: human decides the approach; the skeleton loads 6CFO as the WT cast.

3. [HIGH] tca.citrate_synthase reframe: keep edit:enzyme + edit:structural (structural-exploration, NO disease mutant) [skeleton DEFAULT] vs remove the edit:enzyme tag entirely. Flag: the human confirms the reframe; if removed, the SC5 class + edit-node contract row are updated.

4. [MEDIUM] tca.akg_dh deeper scan: LOW-confidence negative (no pathogenic missense in first 14/163 ClinVar Oxoglutaricaciduria records - NCBI rate-limit truncated the scan). A complete scan of all 163 records OR a focused PubMed search for "OGDH pathogenic missense" may surface a confirmed point mutation. Flag: if the human wants an OGDH edit-allowed node, commission a follow-up research pass; until then OGDH stays NOT edit-allowed.

5. [MEDIUM] PFKM mutant choice (gly.pfk): G209D-to-D209G [skeleton DEFAULT] vs W686C-to-C686W vs R39L-to-L39R. Flag: the human picks the PFKM mutant for the edits.json entry (Phase 7); the skeleton references DIS-PFKM-01-cand without choosing the residue.

6. [MEDIUM] Aconitase bovine-1ACO homology + residue numbering (tca.aconitase): human mito ACO2 has NO PDB (verified UniProt REST); bovine 1ACO (2.05 A) is the teaching proxy. The S112R reverse-mutation must be cross-checked for bovine-vs-human numbering (may differ slightly). Flag: the human approves the 1ACO teaching-proxy claim + the residue mapping (Phase 7 text_teaching must note the homology caveat + approximate geometry from cmd.alter).

7. [MEDIUM] COX4I1 residue numbering (etc.complex_iv): human UniProt precursor P152T vs PDB chain D resi 130 (T130P, mature numbering). Flag: the human confirms the numbering for the edits.json entry (Phase 7); the skeleton references DIS-COX4I1-01-cand.

8. [MEDIUM] CYC1 residue choice (etc.complex_iii): C12W-to-W12C (PDB chain D resi 12) [skeleton DEFAULT] vs L215F (PDB resi 131, same paper, ClinVar Pathogenic alternative). Flag: the human picks the CYC1 residue for the edits.json entry (Phase 7).

9. [LOW] anaer.ldh NOT promoted: GSD11 (LDHA deficiency) canonical mutation is a 20-bp FRAMESHIFT (not a point mutation implementable via cmd.alter); ClinVar point variants are VUS. Flag: anaer.ldh stays a non-edit-allowed MC node; if the human wants an LDH edit, a different disease/variant would need research.

10. [LOW] SDHA / NDUFS8 / CYC1 / COX4I1 PDB-organism caveat: the ETC PDBs (5LDW bovine, 1ZOY pig, 1BGY bovine, 1OCC bovine) are non-human; the disease mutations are human. The cmd.alter apply-edit on a non-human PDB needs residue-number mapping (bovine/pig vs human). Flag: Phase 7 text_teaching must note the organism + numbering caveat; the human approves each PDB-cast claim.

11. [INFO] All 12 DIS-*-cand claim_ids are CANDIDATE pending Phase 7 approval (NOT approved; NOT yet in data/citations.json). The citation gate is NOT run during Phase 5.1. Phase 7 adds + approves each DIS-*-cand claim + the cast-PDB claims before the on_enter loads + edits.json entries ship.

Cross-reference: each open question maps to a specific node id + DIS-*-cand claim_id so the 05.1-06 checkpoint reviewer can locate it in the skeleton. Add a one-line "05.1-06 checkpoint must resolve these before Phase 7" pointer at the top of the subsection.
  </action>
  <verify>
1. grep -c "DIS-" 05.1-DESIGN.md (should reference the 12 DIS-*-cand claim_ids)
2. grep "IDH3A\|IDH1" 05.1-DESIGN.md (the IDH3A-default/IDH1-alternative flag is present)
3. grep "2OZL\|6CFO\|6CER" 05.1-DESIGN.md (the pyr.pdh cast-PDB fix + 6CER option is documented)
4. Read the new "Disease-Mutant Replan Open Questions" subsection and confirm it has 11 items + the 05.1-06 checkpoint pointer.
  </verify>
  <done>
05.1-DESIGN.md section 9 has the original 13 open questions + the new 11 disease-mutant open questions (IDH3A/IDH1, cast-PDB, CS reframe, OGDH scan, PFKM choice, Aconitase homology, COX4I1 numbering, CYC1 choice, LDH not-promoted, ETC organism caveat, DIS-*-cand candidate status); each maps to a node id + claim_id; the IDH3A-default/IDH1-alternative is explicitly flagged for the 05.1-06 checkpoint.
  </done>
</task>

</tasks>

<verification>
- The SC5 table + edit-node contract in 05.1-DESIGN.md match the post-replan skeleton JSON exactly (14 edit-allowed, 0 single-Continue, the DIS-*-cand claim_ids, the edit:structural exception).
- The 11 new disease-mutant open questions are assembled from the 3 research docs and cross-referenced to node ids + claim_ids.
- The IDH3A-default/IDH1-alternative + pyr.pdh cast-PDB fix + CS reframe are explicitly flagged for the 05.1-06 human-review checkpoint.
- The restoration-narrative Phase 7 content boundary is documented (skeleton = structure; edits.json + text + on_enter pre-edit = Phase 7).
</verification>

<success_criteria>
- 05.1-DESIGN.md is the accurate REVIEWABLE artifact for the post-replan skeleton; the 05.1-06 checkpoint can read it + spot-check the JSON and find full consistency.
- 14 edit-allowed nodes are documented (12 disease-mutant + 1 structural + 1 shared); 0 single-Continue; 15 MC conversions.
- The IDH decision is FLAGGED (not decided unilaterally); the skeleton uses IDH3A as DEFAULT.
- All DIS-*-cand claim_ids are marked CANDIDATE pending Phase 7 (no fabricated science).
</success_criteria>

<output>
After completion, create .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-10-SUMMARY.md recording: the 3 section updates (SC5 table, edit-node contract, open questions), the 14-edit-allowed/0-single-Continue classification, the 11 new open questions, and the IDH3A-default flag.
</output>
