---
phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts
plan: 12
subsystem: story-graph
tags: [story-graph, bad-endings, cg-collection, edit-router, bfs-reachability, skeleton, json]

# Dependency graph
requires:
  - phase: 05.1-03
    provides: the original bad_endings.json (4 bad endings + edit.prompt stub with 3 structural choices) this plan expands
  - phase: 05.1-04
    provides: the manifest.json + StoryGraph.load + check_reachability BFS used to verify the expanded pool
  - phase: 05.1-06-checkpoint-feedback
    provides: Requirement 1 (CG-collection bad-ending variety: 1a randomize/expand the unknown-edit pool + 1b known-consequence bad endings)
provides:
  - 9 new bad-ending nodes in data/story_glucose/bad_endings.json (6 1a unknown-pool [edit:unknown] + 3 1b known-consequence [edit:known])
  - edit.prompt stub expanded from 3 to 12 structural choices so the BFS sees all 9 new bad endings reachable
  - the RngEngine-weighted unknown-edit bad-ending pool grows 2 -> 8 (CG-collection variety via node count, not new routing logic)
  - 3 known-consequence bad-ending NODES (deterministic, distinct from the random pool; binding to specific edits is Phase 7)
affects: [05.1-14 (test count assertions update), 05.1-15 (DESIGN.md expansion update), 05.1-16 (diagram regen), 05.1-06 (human review), phase-7 (edits.json content binds 1b deterministic routing + text)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Bad-ending CG-collection: the SAME wrong edit can produce DIFFERENT bad endings (variety via node count in the shared RngEngine pool, not punishment)"
    - "edit:unknown (1a random pool) vs edit:known (1b deterministic) vs edit:known_critical (existing critical-residue break) tag distinction so Phase 7 routes deterministically"
    - "Skeleton carries NODES + is_ending + PLACEHOLDER claim_ids + structural reachability; edits.json binding + text + on_enter pre-edit MolActions are ALL Phase 7 (no-fabricated-science boundary)"

key-files:
  created: []
  modified:
    - data/story_glucose/bad_endings.json — +9 bad-ending nodes (6 1a + 3 1b) + edit.prompt stub choices 3->12

key-decisions:
  - "Shared pool (NOT per-enzyme) for the skeleton: variety comes from node count (8 unknown + 3 known), not new routing logic; per-enzyme bad-ending pools are Phase 7 content (the Phase 4 EditRouter OVERRIDE semantics already support them)"
  - "1b nodes reachable via edit.prompt structural choices (same as 1a) for the BFS; the DETERMINISTIC routing (specific known edit -> specific 1b bad ending) is Phase 7 edits.json content; the skeleton only guarantees structural reachability"
  - "edit:known tag distinguishes 1b from 1a's edit:unknown so Phase 7 can route deterministically; existing bad.critical_residue_break uses edit:known_critical (a third category)"

patterns-established:
  - "Bad-ending node shape: is_ending='bad', claim_ids=['PLACEHOLDER_PHASE7_BAD'], on_enter=[{hide_all}], choices=[] (ending nodes carry no choices)"
  - "edit.prompt stub structural-choice pattern: one choice per bad ending, tagged with the edit category (edit:unknown/edit:known/edit:known_critical) so the BFS sees reachability while runtime routing is owned by the Phase 4 EditRouter"

# Metrics
duration: 3 min
completed: 2026-08-22
---

# Phase 5.1 Plan 12: Bad-Ending Expansion (CG-Collection) Summary

**Expanded the bad-ending pool from 5 to 14 nodes (6 1a unknown-pool + 3 1b known-consequence + 1 cycle-trap + 4 pre-existing) so the same wrong edit can yield different endings (CG-collection variety via node count), with edit.prompt stub grown 3->12 structural choices for BFS reachability**

## Performance

- **Duration:** 3 min
- **Started:** 2026-08-22T04:44:16Z
- **Completed:** 2026-08-22T04:47:54Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- Added 6 new 1a unknown-pool bad endings (RngEngine-weighted pool grows 2 -> 8): bad.enzyme_collapse, bad.wrong_substrate_trapped, bad.broken_active_site, bad.lost_in_cytosol, bad.proton_leak, bad.misfolded_aggregate — all tagged `edit:unknown`, is_ending="bad", PLACEHOLDER_PHASE7_BAD, on_enter hide_all, choices=[]
- Added 3 new 1b known-consequence bad endings (deterministic, distinct from the random pool): bad.active_site_destroyed, bad.substrate_channel_blocked, bad.cofactor_lost — all tagged `edit:known`, is_ending="bad", PLACEHOLDER_PHASE7_BAD, on_enter hide_all, choices=[]
- Expanded the edit.prompt stub's structural choices from 3 to 12 (one per new bad ending) so the direct BFS sees all 9 new endings reachable; 8 tagged `edit:unknown` + 3 tagged `edit:known` + 1 existing `edit:known_critical`
- Direct BFS reachability GREEN: 43 -> 52 nodes, 11 -> 20 endings (1T+3G+2N+14B), all 4 tiers reachable, no unreachable endings; 14 edit-allowed UNCHANGED; 0 new single-Continue nodes; True/Good/Normal counts unchanged (1/3/2); no fabricated science (PLACEHOLDER_PHASE7_BAD only, no new PDB IDs or DIS-*-cand claims)

## Task Commits

Each task was committed atomically:

1. **Task 1: 1a — add 6 unknown-pool bad endings + expand edit.prompt to 9 choices** - `eab886b` (feat)
2. **Task 2: 1b — add 3 known-consequence bad endings + expand edit.prompt to 12 choices** - `68e2d68` (feat)

## Files Created/Modified
- `data/story_glucose/bad_endings.json` — the bad-ending pool + edit.prompt stub; +9 bad-ending nodes (6 1a unknown-pool + 3 1b known-consequence) and edit.prompt choices grown 3->12 (structural BFS paths)

## Decisions Made
- **Shared pool (NOT per-enzyme) for the skeleton:** the variety comes from node count (8 unknown + 3 known), not new routing logic. Per-enzyme bad-ending pools are Phase 7 content — the Phase 4 EditRouter OVERRIDE semantics (per-enzyme else global fallback) already support them, so this plan only EXPANDS the POOL the router picks from, it does NOT change routing logic.
- **1b reachable via edit.prompt structural choices (same as 1a) for the BFS:** the DETERMINISTIC routing (specific known edit -> specific 1b bad ending) is Phase 7 edits.json content; the skeleton only guarantees structural reachability via edit.prompt choices.
- **`edit:known` tag distinguishes 1b from 1a's `edit:unknown`** so Phase 7 can route deterministically. The existing bad.critical_residue_break uses `edit:known_critical` (a third category — known CRITICAL residue break).

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- The `tests.test_glucose_reachability` unittest shows 2 STALE-COUNT failures (EXPECTED, not a pass gate for this plan): `test_manifest_loads_all_43_nodes` expects 43 nodes but now 52; `test_reachability_green_all_four_tiers` expects 11 endings but now 20. These are EXPECTED — Plan 14 (05.1-14) updates those count assertions. The other 6 tests still PASS (SC2 RED variants x2, no-pymol testability, 0 single-Continue, 14 edit-allowed, pyr.pdh cast-PDB fix). The direct BFS checks (the plan's verification) are GREEN.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Plan 12 complete; the bad-ending pool is expanded (43 -> 52 nodes, 11 -> 20 endings). 
- Remaining in Phase 5.1 expansion Wave: Plan 13 (preface sequence +2 nodes in intro.json -> 54 total; manifest start shift), Plan 14 (update reachability test count assertions 43->54 / 11->20 + start fix + new bad-ending-reachability invariant), Plan 15 (DESIGN.md expansion update), Plan 16 (diagram regen). After 13-16, Plan 06 (human review of the FINAL post-expansion skeleton) re-runs.
- Blocker/concern: 2 test count assertions are stale (fixed in Plan 14). The unittest must NOT be run as a pass gate until Plan 14 executes; the direct BFS checks (this plan's verification) are the authoritative green.

---
*Phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts*
*Completed: 2026-08-22*
