---
phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts
plan: 02
subsystem: story-graph
tags: [json, story-graph, tca-cycle, etc, atp-synthase, rng-shuffle, soul-jump, metamorphosis, reachability, citation-gate]

# Dependency graph
requires:
  - phase: 02-story-engine-core
    provides: Node/Choice/MolAction data model (c14/story/model.py), StoryGraph.load + manifest-merge loader (c14/story/graph.py), check_reachability BFS over choice.goto (c14/story/validate.py)
  - phase: 05-pre-content-key-decisions-source-approval
    provides: 5 approved first-batch claims (GLY-PFK-01, TCA-RNG-CITRATE-PROCHIRALITY-01, TCA-RNG-WEIGHT-01, CAST-PFK-PDB-01, CAST-CSC-PDB-01) + 4 approved sources (LIBRETEXTS-METAB-GLYCOLYSIS, LIBRETEXTS-METAB-TCA, PDB-4PFK, PDB-1CSC); soul-jump metamorphosis framing (PROJECT.md row 104); C14-decay DROP (row 105); TCA-RNG-WEIGHT-01 = 0.5/0.5 per user acceptance
  - phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts (research)
    provides: 05.1-RESEARCH-pathway-topology.md (the node inventory + branch-point mapping + ending-tier distribution + claim_id status per node + cycle-trap visit-cap recommendation N=5) + 05.1-RESEARCH-graph-contracts.md (the choice-point + edit-node contract mechanics)
provides:
  - data/story_glucose/tca.json — 7 TCA cycle nodes including the RNG-weighted shuffle (tca.shuffle, the spec-mandated stochastic node anchored on 2 approved claims) + the TCA loop + the Good-ending diversion branch point (tca.divert_to_good) + the cycle-trap Bad-ending trigger edge (tca.shuffle -> bad.cycle_trap_host_death cond-gated on visits > 5)
  - data/story_glucose/etc_atp.json — 5 ETC/ATP-synthase nodes including the soul-jump arc (etc.entry -> etc.complex_i -> etc.complex_ii_iii_iv -> etc.atp_synthase -> end.true) + the True ending (end.true, metamorphosis framing) + the Normal-ending "let go" branch off etc.entry (-> end.normal.co2)
  - The 12 TCA+ETC nodes of the glucose story graph skeleton (the middle + climax of the glucose path; SC1 partial delivery)
  - The tca.shuffle node as the SOLE spec-mandated RNG-weighted story node (spec.md line 13), with 0.5/0.5 weights citing the 2 approved high-stakes TCA claims (TCA-RNG-WEIGHT-01 + TCA-RNG-CITRATE-PROCHIRALITY-01)
  - Cross-file choice.goto edges declaring the skeleton's structural reachability (etc.entry, end.good.fatty_acid, end.good.amino_acid, end.normal.co2, bad.cycle_trap_host_death, edit.prompt live in OTHER files authored in Plans 01/03/05)
affects:
  - 05.1-01 (early-path skeleton — glycolysis + pyruvate branch + anaerobic; the cross-file edges from tca.json to etc_atp.json's etc.entry + to endings.json's end.good.* are resolved here)
  - 05.1-03 (endings skeleton — end.good.fatty_acid, end.good.amino_acid, end.normal.co2, bad.cycle_trap_host_death, edit.prompt are authored there; this plan's cross-file choice.goto edges resolve once 03 lands)
  - 05.1-04 (manifest + SC2 reachability validation — runs the BFS on the full merged graph; this plan's tca.shuffle -> bad.cycle_trap_host_death edge makes the cycle-trap structurally reachable; this plan's tca.divert_to_good -> end.good.* edges make the Good tier reachable; this plan's etc.entry -> end.normal.co2 edge makes the Normal tier reachable)
  - 05.1-05 (DESIGN.md choice-point + edit-node contracts — this plan's edit:offer choices + edit:enzyme tags are the topology inputs the contracts formalize)
  - 05.1-06 (human review checkpoint — the cycle-trap visit-cap N=5, the Normal-ending "let go" branch point, and the Good-ending cataplerotic-exit claims are flagged for review)
  - 07-03 (TCA content — fleshed out with cited two-layer text + real PDB loads; tca.aconitase PDB approved here)
  - 07-04 (ETC content — etc.* PDBs 5LDW/1ZOY/1BGY/1OCC/1E79 approved here; ATP-SOUL-07/08 claims approved here)

# Tech tracking
tech-stack:
  added: []  # pure JSON skeleton; no new libraries
  patterns:
    - "JSON skeleton = STRUCTURE artifact, not cited content: text is PLACEHOLDER/TBD except where approved claims cover a node (the 3 approved-claim TCA nodes carry real claim_ids; all others use PLACEHOLDER_PHASE7_<stage>)"
    - "id is the JSON key under {\"nodes\": {id: {...}}}, NOT duplicated in the node body (the loader injects it via Node.from_dict; matches the existing data/story/intro.json fixture shape + the c14/story/graph.py:85-87 loader contract)"
    - "Every forward path uses choice.goto (NOT on_enter_divert) — the reachability BFS (c14/story/validate.py:198-200) ONLY follows choice.goto edges; on_enter_divert would orphan downstream nodes"
    - "Cross-file choice.goto is fine — the loader merges all manifest files into one {id: Node} dict before any traversal (graph.py:69-88); a choice.goto can target a node id that lives in a sibling file"
    - "RNG-weighted node (tca.shuffle) carries BOTH weighted choices (weight 0.5 each, the RNG branch) AND a cond-gated choice (the cycle-trap) AND an edit:offer choice (the edit branch) — a node holds several structural types at once (ARCHITECTURE.md Pattern 2 canonical example)"
    - "The cycle-trap is a cond-gated choice.goto (visits.get('tca.shuffle', 0) > N), NOT an RNG node — the BFS IGNORES cond, so it traverses the goto edge, making bad.cycle_trap_host_death structurally reachable (the 'RNG stays in a cycle long enough' of spec.md line 18 is modeled by the 0.5/0.5 RNG re-entering tca.shuffle, not by a separate RNG draw for the trap)"
    - "Soul-jump framing (PROJECT.md row 104): end.true text reconciles BOTH halves — electrons -> ATP (the soul lives on) AND carbon -> CO2 (the body was shed). NOT 'carbon becomes ATP' (Pitfall 4 RESOLVED). The metamorphosis metaphor (caterpillar -> butterfly; carbon -> CO2 = chrysalis; electrons -> ATP = the soul lives on) is the True-ending framing."
    - "No C14-decay ending (DROPPED per PROJECT.md row 105) — the cycle-trap-until-host-death (bad.cycle_trap_host_death) is the SOLE timescale-based bad-ending"

key-files:
  created:
    - data/story_glucose/tca.json
    - data/story_glucose/etc_atp.json
  modified: []

key-decisions:
  - "Cycle-trap visit-cap N=5 chosen as a placeholder game-design parameter (research OQ3 — NOT a science claim; mid-value; human confirms at Phase 6 playtest). The cond expression is visits.get('tca.shuffle', 0) > 5 (matches the _cond evaluator's restricted-namespace {visits: {...}} env in c14/story/interpreter.py:101-135)."
  - "The Normal-ending 'let go' branch is placed off etc.entry (the player chooses to drift away as CO2 instead of entering the ETC) — the research OQ2 recommendation. This makes the Normal tier STRUCTURALLY REACHABLE via the BFS. Flagged for human review at Plan 05.1-06 (whether this is the right narrative branch point vs a retrospective framing)."
  - "tca.shuffle's edit:enzyme tag is 'tca.aconitase' (the shuffle's edit context is the aconitase active site — same enzyme as tca.aconitase; the EditRouter routes by enzyme_id, so both tca.aconitase and tca.shuffle bind to the same enzyme_id 'tca.aconitase')."
  - "tca.citrate_synthase cites BOTH CAST-CSC-PDB-01 (the cast PDB 1CSC) AND TCA-RNG-CITRATE-PROCHIRALITY-01 (the CS stereospecificity is the START of the prochirality mechanism the approved claim describes). The on_enter loads pdb:1CSC (the only PDB load in this plan; all ETC PDBs are CANDIDATE pending Phase 7)."
  - "ETC nodes (etc.entry, etc.complex_i, etc.complex_ii_iii_iv, etc.atp_synthase, end.true) carry NO PDB loads in on_enter — all 5 ETC PDBs (5LDW, 1ZOY, 1BGY, 1OCC, 1E79) are CANDIDATE, pending Phase 7 human approval (research OQ5). The skeleton's on_enter is {hide_all} only for ETC nodes."
  - "Placeholder claim_ids use stage-suffixed PLACEHOLDER_PHASE7_<stage> (PLACEHOLDER_PHASE7_TCA, PLACEHOLDER_PHASE7_ETC) for traceability — the citation gate will fail on these until Phase 7 replaces them with approved claims (intended gate behavior; the skeleton is a design artifact, not shippable content). The gate is NOT run on the skeleton during Phase 5.1 (the plan explicitly notes this)."

patterns-established:
  - "Glucose story graph skeleton JSON convention: one file per pathway segment (tca.json, etc_atp.json), top-level {\"nodes\": {id: {...}}}, 2-space indent, trailing newline, no inline comments, id is the JSON key (NOT in the body). Matches the existing data/story/intro.json fixture + the graph.py loader contract."
  - "edit:offer tag goes on the CHOICE; edit:enzyme:<id> tag goes on the NODE. The <id> is the EditRouter lookup key (the enzyme_id). A node can have multiple edit:offer choices but they all share the node's edit:enzyme context."
  - "RNG-weighted choice: {weight: 0.5, goto: <target>, tags: [\"rng:weighted\"]}. The interpreter's pick_choice (c14/story/interpreter.py:43-73) picks one deterministically via rng.random()*total + cumulative sum (total=1.0 for 0.5/0.5)."
  - "cond-gated choice (cycle-trap): {goto: <bad_ending>, cond: \"visits.get('<node_id>', 0) > N\", tags: [\"cycle_trap\"]}. The BFS ignores cond, so the goto edge IS traversed (structurally reachable). At playtime, _cond hides the choice until the visit count exceeds N."

# Metrics
duration: 61min
completed: 2026-08-16
---

# Phase 5.1 Plan 02: TCA + ETC Skeleton Summary

**12 TCA+ETC glucose story graph nodes authored as a reviewable JSON skeleton — the spec-mandated RNG-weighted citrate-prochirality shuffle (0.5/0.5, anchored on 2 approved high-stakes claims) + the TCA loop + Good-ending diversion + cond-gated cycle-trap Bad-ending trigger + the ETC soul-jump arc culminating in the True ending (metamorphosis framing) with a Normal-ending "let go" branch off the ETC entry**

## Performance

- **Duration:** 61 min (wall-clock; the plan was read-heavy — verified the exact JSON schema against data/story/intro.json + c14/story/model.py + c14/story/graph.py + c14/story/validate.py + the 5 approved claims in data/citations.json + the 2 Phase 5.1 research files + spec.md lines 13/15/18 + PROJECT.md rows 104/105 before authoring)
- **Started:** 2026-08-16T10:08:33Z
- **Completed:** 2026-08-16T11:09:40Z
- **Tasks:** 2 (both type=auto; fully autonomous — Pattern A, no checkpoints)
- **Files created:** 2

## Accomplishments

- **tca.json (7 nodes):** the TCA cycle skeleton including the spec-mandated RNG-weighted citrate-prochirality shuffle (tca.shuffle, weights 0.5/0.5, citing the 2 approved high-stakes claims TCA-RNG-WEIGHT-01 + TCA-RNG-CITRATE-PROCHIRALITY-01), the citrate synthase + aconitase edit-allowed nodes (citing CAST-CSC-PDB-01 + TCA-RNG-CITRATE-PROCHIRALITY-01; PDB 1CSC loaded for citrate synthase), the TCA loop (tca.co2_turn1/turn2 -> tca.shuffle via choice.goto), the forward path (-> tca.divert_to_good -> etc.entry / end.good.fatty_acid / end.good.amino_acid via choice.goto), and the cond-gated cycle-trap Bad-ending trigger (tca.shuffle -> bad.cycle_trap_host_death on visits > 5).
- **etc_atp.json (5 nodes):** the ETC soul-jump arc (etc.entry -> etc.complex_i [edit-allowed, edit:enzyme:etc.complex_i] -> etc.complex_ii_iii_iv -> etc.atp_synthase -> end.true) culminating in the True ending (is_ending="true", metamorphosis framing), PLUS the Normal-ending "let go" branch off etc.entry (-> end.normal.co2) making the Normal tier structurally reachable via the BFS. No ETC PDB loads (all 5 ETC PDBs CANDIDATE, pending Phase 7).
- **Soul-jump framing honored:** end.true text reconciles BOTH halves — electrons -> ATP (the soul lives on) AND carbon -> CO2 (the body was shed as chrysalis). NO "carbon becomes ATP" (Pitfall 4 RESOLVED). The metamorphosis metaphor (caterpillar -> butterfly) is the True-ending framing.
- **C14-decay DROPPED honored:** no decay/radioactive/half-life text anywhere; the cycle-trap-until-host-death (bad.cycle_trap_host_death) is the SOLE timescale-based bad-ending (PROJECT.md row 105).
- **All forward paths use choice.goto** (NOT on_enter_divert) — the reachability BFS (c14/story/validate.py:198-200) traverses choice.goto only. Zero on_enter_divert occurrences across both files (greppable verification: 0).
- **Citation-gate coexistence:** the 3 approved-claim TCA nodes (tca.citrate_synthase, tca.aconitase, tca.shuffle) carry real approved claim_ids; all other nodes use PLACEHOLDER_PHASE7_<stage> (TCA or ETC) — the gate is not run on the skeleton during Phase 5.1.

## Task Commits

Each task was committed atomically:

1. **Task 1: Author tca.json (7 nodes — the TCA cycle + RNG shuffle + Good-ending diversion + cycle-trap)** — `50cbc13` (feat)
2. **Task 2: Author etc_atp.json (5 nodes — ETC soul-jump arc + True ending + Normal-ending branch)** — `2f00207` (feat)

**Plan metadata:** `pending` (docs: complete plan — committed in the final step)

## Files Created/Modified

- `data/story_glucose/tca.json` — 7 TCA cycle nodes (tca.entry, tca.citrate_synthase, tca.aconitase, tca.shuffle, tca.co2_turn1, tca.co2_turn2, tca.divert_to_good). The spec-mandated RNG shuffle anchor + the Good-ending diversion branch point + the cycle-trap Bad-ending trigger.
- `data/story_glucose/etc_atp.json` — 5 ETC/ATP-synthase nodes (etc.entry, etc.complex_i, etc.complex_ii_iii_iv, etc.atp_synthase, end.true). The soul-jump arc + the True ending + the Normal-ending "let go" branch.

## Decisions Made

1. **Cycle-trap visit-cap N=5** (research OQ3 — game-design parameter, NOT a science claim). Chose N=5 as a reasonable mid-value placeholder (research recommendation). The cond expression is `visits.get('tca.shuffle', 0) > 5` — matches the `_cond` evaluator's restricted-namespace `{visits: {...}}` env in c14/story/interpreter.py:101-135. Flagged for human confirmation at Phase 6 playtest (too few = player feels cheated; too many = trap never triggers). Documented in the tca.shuffle node's 4th choice.

2. **Normal-ending "let go" branch placed off etc.entry** (research OQ2 recommendation). The player chooses "Let go — drift away as CO2 (the unremarkable exit)" instead of entering the ETC. This makes the Normal tier STRUCTURALLY REACHABLE via the BFS (the 2nd choice on etc.entry -> end.normal.co2). Flagged for human review at Plan 05.1-06 (whether this is the right narrative branch point vs a retrospective "the story ends at CO2" framing).

3. **tca.shuffle's edit:enzyme tag = "tca.aconitase"** (per the plan). The shuffle's edit context is the aconitase active site — same enzyme as tca.aconitase. The EditRouter routes by enzyme_id, so both tca.aconitase and tca.shuffle bind to the same enzyme_id "tca.aconitase". This avoids duplicating the enzyme_id namespace.

4. **tca.citrate_synthase cites BOTH CAST-CSC-PDB-01 AND TCA-RNG-CITRATE-PROCHIRALITY-01** (per the plan). CAST-CSC-PDB-01 for the cast PDB 1CSC (the on_enter loads pdb:1CSC — the only PDB load in this plan); TCA-RNG-CITRATE-PROCHIRALITY-01 for the mechanism context (the CS stereospecificity is the START of the prochirality mechanism the approved claim describes). The CS-step-specific teaching text is a Phase 7 pending claim.

5. **ETC nodes carry NO PDB loads** (per the plan + research OQ5). All 5 ETC PDBs (5LDW Complex I, 1ZOY Complex II, 1BGY Complex III, 1OCC Complex IV, 1E79 ATP synthase F1) are CANDIDATE, pending Phase 7 human approval. The skeleton's on_enter is `{hide_all}` only for ETC nodes. The actual PDB loads are Phase 7 content (after the human approves the 5 PDB structures).

6. **Placeholder claim_ids use stage-suffixed PLACEHOLDER_PHASE7_<stage>** (PLACEHOLDER_PHASE7_TCA, PLACEHOLDER_PHASE7_ETC) for traceability. The citation gate (tools/check_citations.py) will FAIL on these until Phase 7 replaces them with approved claims — intended gate behavior (the skeleton is a design artifact, not shippable content). The gate is NOT run on the skeleton during Phase 5.1 (the plan explicitly notes this).

## Deviations from Plan

None — plan executed exactly as written. The 2 JSON files match the plan's node specifications verbatim (node ids, claim_ids, tags, choices, on_enter, text, cond expression, weights). The visit-cap N=5 was chosen per the plan's explicit recommendation ("Pick a reasonable N (e.g. 5) and document it as an open question for human review"). The Normal-ending "let go" branch placement (off etc.entry) follows the plan's explicit specification. The soul-jump metamorphosis framing in end.true follows the plan's explicit text. The C14-decay DROP is honored (no decay-based ending). All forward paths use choice.goto (no on_enter_divert).

### Note on a self-corrected verification assertion (NOT a content deviation)

During the extra (non-plan) verification of etc_atp.json, I wrote an over-strict assertion that required `etc.atp_synthase` to mention carbon+CO2. This was a verification-script bug, NOT a content bug: the soul-jump framing correctly does NOT re-mention the carbon at the ATP synthase stage (the carbon body was already shed as CO2 during PDH + TCA decarboxylations; by the ATP synthase stage only the electrons — the "soul" — remain). Only `end.true` (the climax that reconciles both halves of the soul-jump) needs all three terms (electrons, ATP, carbon, CO2), and it has them. I re-ran the corrected assertion (checking etc.atp_synthase for electrons+ATP only, and end.true for all four terms) — passed. No file content was changed; the JSON matches the plan verbatim.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required. This is a design phase producing JSON skeleton files; no environment variables, no dashboard configuration, no running services.

## Next Phase Readiness

- **Plan 05.1-01 (early-path skeleton):** UNBLOCKED — runs in parallel (Wave 1). The cross-file choice.goto edges from this plan's tca.divert_to_good (-> end.good.fatty_acid, end.good.amino_acid) and tca.co2_turn1/turn2 (implicit via tca.divert_to_good -> etc.entry) will resolve once 05.1-01's glycolysis nodes + 05.1-03's endings nodes land. No coordination needed beyond the agreed node id namespace (the dotted `knot.stitch` convention).
- **Plan 05.1-03 (endings skeleton):** UNBLOCKED — runs in parallel (Wave 1). This plan references (via cross-file choice.goto) the endings nodes that 05.1-03 will author: `end.good.fatty_acid`, `end.good.amino_acid` (from tca.divert_to_good), `end.normal.co2` (from etc.entry), `bad.cycle_trap_host_death` (from tca.shuffle), `edit.prompt` (from the 3 edit-allowed nodes). These node ids are now pinned by this plan's choice.goto edges; 05.1-03 must use these exact ids.
- **Plan 05.1-04 (manifest + SC2 reachability validation):** PENDING this plan + 01 + 03 completing (the manifest merges all 7 files; the BFS runs on the full merged graph). This plan's tca.shuffle -> bad.cycle_trap_host_death edge makes the cycle-trap structurally reachable; tca.divert_to_good -> end.good.* makes the Good tier reachable; etc.entry -> end.normal.co2 makes the Normal tier reachable; etc.atp_synthase -> end.true makes the True tier reachable. The 4 ending tiers are structurally reachable from this plan's nodes (contingent on 05.1-03 authoring the ending nodes with the pinned ids).
- **Plan 05.1-05 (DESIGN.md choice-point + edit-node contracts):** PENDING this plan + 01 + 03. This plan's edit:offer choices + edit:enzyme tags (on tca.citrate_synthase, tca.aconitase, tca.shuffle, etc.complex_i) are the topology inputs the contracts formalize.
- **Plan 05.1-06 (human review checkpoint):** PENDING 04 + 05. The 3 open questions flagged for human review: (a) the cycle-trap visit-cap N=5 (game-design parameter); (b) the Normal-ending "let go" branch point off etc.entry (narrative framing); (c) the Good-ending cataplerotic-exit claims (PLACEHOLDER_PHASE7 — pending Phase 7 source approval). Plus the ethanolic anaerobic ending (05.1-01's scope, mammal-vs-yeast host tension).

**No blockers.** The skeleton's structural reachability is fully declared via choice.goto edges; the cross-file targets resolve once the sibling plans (01, 03) land. The 3 approved TCA claims are correctly anchored on the 3 approved-claim nodes. The soul-jump + C14-decay-DROP + cycle-trap-sole-timescale-bad-ending constraints are all honored.

---
*Phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts*
*Plan: 02*
*Completed: 2026-08-16*
