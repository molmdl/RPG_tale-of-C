---
phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts
plan: 06
type: execute
wave: 3
depends_on: ["05.1-04", "05.1-05"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "A human has reviewed the branch-point mapping in 05.1-DESIGN.md against spec.md + the approved sources (SC1 human-review)"
    - "A human has design-reviewed the edit-node contract in 05.1-DESIGN.md (SC4 design-review): the enzyme_id binding, the EditIntents, the known-edit branch route, the unknown-edit bad-ending fallback, and the 5-step data-flow"
    - "A human has acknowledged the 13 open questions in 05.1-DESIGN.md section 9 (ethanolic host tension, Normal-ending framing, cycle-trap visit-cap N, cataplerotic-exit claims, ETC PDB candidates, PDH PDB, char-setting, on_enter_divert dormancy, edits.json location, tags-vs-field, SC5 table-vs-field, node-vs-choice edit:offer, anaer.entry classification)"
    - "A human has confirmed the SC2 reachability validation passes (green on the full skeleton, red on the orphaned variant) — the checker guards real content"
  artifacts:
    - path: ".planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-06-SUMMARY.md"
      provides: "the human-review outcome (approved / issues-found) + the open-question dispositions"
      contains: "human-review"
  key_links:
    - from: "the human review"
      to: "spec.md + data/citations.json + data/sources.json"
      via: "the human confirms the branch-point mapping matches spec.md lines 11-34 + the 5 approved first-batch claims"
      pattern: "spec\\.md|approved"
    - from: "the human review"
      to: ".planning/phases/05.1-.../05.1-DESIGN.md"
      via: "the human reads the design doc (SC3 choice-point contract + SC4 edit-node contract + SC5 classification table) + spot-checks the skeleton JSON"
      pattern: "05\\.1-DESIGN"
---

<objective>
Human review checkpoint: the human reviews the glucose skeleton's branch-point mapping against spec.md + approved sources (SC1), design-reviews the edit-node contract (SC4), acknowledges the open questions, and confirms the SC2 reachability validation passes. This is the REVIEW gate for Phase 5.1 — the branch-point mapping + contracts + classification need human eyes before Phase 7 builds cited content on top.

Purpose: SC1 explicitly requires "human-review the mapping against spec.md + approved sources" and SC4 requires "design-review." The research surfaced 13 open questions (ethanolic host tension, Normal-ending framing, cycle-trap N, cataplerotic-exit claims, ETC PDBs, etc.) that need human acknowledgement. This checkpoint is checkpoint:human-verify (the human confirms OpenCode's automated work) — NOT checkpoint:human-action (there is no manual work the agent can't do; the agent authored the skeleton + design doc + tests; the human REVIEWS them).
Output: the human's review outcome (approved or issues-found) recorded in the SUMMARY, with dispositions for the open questions.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-RESEARCH-pathway-topology.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-RESEARCH-graph-contracts.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-04-SUMMARY.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-05-SUMMARY.md
@spec.md
@data/citations.json
@data/sources.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human review of the glucose skeleton + contracts + classification</name>
  <what-built>
OpenCode authored the complete glucose story graph skeleton (34 nodes across 7 JSON files at data/story_glucose/), the 05.1-DESIGN.md companion design doc (choice-point contract SC3 + edit-node contract SC4 + per-node SC5 classification table + node-type taxonomy SC1 + reachability validation SC2 + 13 open questions), and the SC2 reachability validation test suite (tests/test_glucose_reachability.py — 5 tests, all PASS: green on the full skeleton with all 4 ending tiers reachable, red on a deliberately-orphaned variant). The skeleton uses PLACEHOLDER_PHASE7 claim_ids for pending nodes + the 5 approved first-batch claim_ids for the nodes they cover (gly.pfk, tca.citrate_synthase, tca.aconitase, tca.shuffle). The citation gate is NOT run on the skeleton during Phase 5.1 (it's a pre-ship, manually-invoked CLI tool; Phase 7 replaces PLACEHOLDER_PHASE7 with approved claims).
  </what-built>
  <how-to-verify>
**Step 1 — Review the branch-point mapping (SC1) against spec.md + approved sources:**
Read `05.1-DESIGN.md` sections 4 (choice-point contract) + 6 (SC5 classification table). For each choice node, confirm the "pathway branch point mapped" column matches spec.md lines 11-34 + the approved sources in data/sources.json (LIBRETEXTS-METAB-GLYCOLYSIS, LIBRETEXTS-METAB-TCA, PDB-4PFK, PDB-1CSC). Specifically confirm:
  - `pyr.branch` maps to "pyruvate fate depends on O2" (aerobic -> PDH -> TCA; anaerobic -> lactate/ethanol) — spec.md line 14 NOTE (anaerobic option d).
  - `tca.shuffle` maps to "citrate prochirality / aconitase discrimination (0.5/0.5)" — spec.md line 13 (the TCA RNG shuffle example); approved via TCA-RNG-WEIGHT-01 + TCA-RNG-CITRATE-PROCHIRALITY-01.
  - `gly.pfk` maps to "PFK-1 committed rate-limiting step" — approved via GLY-PFK-01 + CAST-PFK-PDB-01.
  - `tca.divert_to_good` maps to "acetyl-CoA/TCA cataplerotic exits" (citrate -> FA, OAA -> aspartate, alpha-KG -> glutamate) — standard biochem, NOT first-batch approved (PLACEHOLDER_PHASE7).
  - `anaer.entry` maps to "anaerobic fermentation 3-ending branch" (lactic=Good, ethanolic=Normal, crisis=Bad) — spec.md line 14 NOTE (option d).
  - `etc.entry` "let go" -> `end.normal.co2` maps to the Normal-ending narrative tier (PROJECT.md row 74).
  - The 4 ending tiers (1 True + 3 Good + 2 Normal + 5 Bad = 11 endings) match PROJECT.md row 107.
  Confirm: NO invented branches (every branch point traces to spec.md, an approved source, or a flagged PLACEHOLDER_PHASE7 node).

**Step 2 — Design-review the edit-node contract (SC4):**
Read `05.1-DESIGN.md` section 5 (edit-node contract). Confirm:
  - The enzyme_id binding mechanism (edit:enzyme:<id> tag) is sound — zero schema change, backward-compatible, greppable.
  - The 5-step data-flow (player edit -> EditIntent -> EditRouter.route(enzyme_id) -> node-id -> engine._enter -> on_enter MolAction(op=edit)) is correct against c14/engine.py:135-161 + c14/edit_router.py:122-149.
  - The per-edit-allowed-node table (5 rows: gly.pfk, pyr.pdh, tca.citrate_synthase, tca.aconitase, etc.complex_i) correctly maps each node to its enzyme_id + the known-edit branch route + the unknown-edit bad-ending fallback.
  - The edit.prompt stub explanation is clear (structural BFS path, not a playtime node; the EditRouter routes at runtime).

**Step 3 — Acknowledge the 13 open questions (05.1-DESIGN.md section 9):**
Review each open question + provide a disposition (defer to Phase 7 / accept the skeleton's placeholder / adjust). The highest-priority items:
  1. Ethanolic anaerobic ending vs mammalian host (HIGH) — the skeleton includes anaer.ethanolic (Normal) but it's a yeast pathway, not mammalian. Decide: keep flagged / drop / reframe as counterfactual.
  2. Normal-ending framing (MEDIUM) — end.normal.co2 is a branch off etc.entry (the player "lets go"). Decide: accept this branch point / reframe.
  3. Cycle-trap visit-cap N=5 (LOW) — a game-design parameter; confirm at Phase 6 playtest.
  4. Cataplerotic-exit claims (MEDIUM) — PLACEHOLDER_PHASE7; approve a source at Phase 7.
  5. ETC PDB structures (MEDIUM) — 5LDW/1ZOY/1BGY/1OCC/1E79 CANDIDATE; approve before Phase 7 on_enter loads.
  6-13. Lower-priority items (PDH PDB, char-setting, on_enter_divert, edits.json location, tags-vs-field, SC5 table-vs-field, node-vs-choice edit:offer, anaer.entry classification) — acknowledge the skeleton's chosen approach.

**Step 4 — Confirm SC2 reachability validation passes:**
Run `python3.6 -m unittest tests.test_glucose_reachability -v` — confirm all 5 tests PASS (green on the full skeleton with all 4 tiers reachable + 11 endings; red on the orphaned variant). The checker now guards real content (34-node skeleton), not just the 3-node toy graph.

**Step 5 — Spot-check the skeleton JSON:**
Open 1-2 skeleton files (e.g. data/story_glucose/tca.json for the RNG shuffle, data/story_glucose/pyruvate_branch.json for the aerobic/anaerobic branch) and confirm the node fields match the design doc's description (claim_ids, tags, choices, cond, weight, is_ending).
  </how-to-verify>
  <resume-signal>
Type "approved" if the branch-point mapping + contracts + classification + SC2 validation are correct. OR describe specific issues to fix (e.g. "OQ1: drop anaer.ethanolic — mammalian host should not have ethanolic fermentation" or "OQ2: reframe the Normal ending as a retrospective, not a branch off etc.entry"). The agent will record the dispositions in the SUMMARY + apply any required skeleton/design-doc fixes.
  </resume-signal>
</task>

</tasks>

<verification>
- The human has read 05.1-DESIGN.md (sections 4, 5, 6, 9) + spot-checked the skeleton JSON
- The human has confirmed the branch-point mapping matches spec.md + approved sources (SC1 human-review)
- The human has design-reviewed the edit-node contract (SC4 design-review)
- The human has acknowledged the 13 open questions + provided dispositions
- The human has confirmed SC2 passes (python3.6 -m unittest tests.test_glucose_reachability -v — 5 tests PASS)
- The human has typed "approved" OR described specific issues
</verification>

<success_criteria>
- The human's review outcome is recorded in the SUMMARY (approved / issues-found + dispositions)
- SC1 human-review complete: the branch-point mapping is confirmed against spec.md + approved sources (no invented branches)
- SC4 design-review complete: the edit-node contract is confirmed sound
- The 13 open questions have dispositions (defer to Phase 7 / accept placeholder / adjust)
- SC2 confirmed: the reachability checker passes green on the full skeleton + red on the orphaned variant
- If issues were found, the agent applies the required fixes to the skeleton JSON + design doc before the phase is marked complete
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-06-SUMMARY.md` recording the human-review outcome (approved / issues-found) + the open-question dispositions + any fixes applied.
</output>
