---
phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts
plan: 06
type: execute
wave: 3
depends_on: ["05.1-10", "05.1-11"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "A human has reviewed the branch-point mapping in 05.1-DESIGN.md against spec.md + the approved sources (SC1 human-review)"
    - "A human has design-reviewed the edit-node contract in 05.1-DESIGN.md (SC4 design-review): the enzyme_id binding, the EditIntents, the known-edit branch route, the unknown-edit bad-ending fallback, the 5-step data-flow, AND the 14 edit-allowed nodes (12 disease-mutant + 1 structural [CS] + 1 shared [tca.shuffle])"
    - "A human has acknowledged the open questions in 05.1-DESIGN.md section 9 (the original 13 + the 11 new disease-mutant replan open questions) and provided dispositions"
    - "A human has resolved the 3 CRITICAL/HIGH disease-mutant decisions: (1) IDH3A-default vs IDH1-alternative for tca.isocitrate_dh; (2) pyr.pdh cast-PDB 6CFO + the 6CER mutant-reveal approach (a apply-edit vs b load-mutant-PDB); (3) tca.citrate_synthase edit:structural reframe (keep vs remove the edit:enzyme tag)"
    - "A human has confirmed the SC2 reachability validation passes (green on the full 43-node post-replan skeleton with 14 edit-allowed + 0 single-Continue, red on the orphaned variants) - the checker + the 2 new replan-invariant assertions guard real content"
  artifacts:
    - path: ".planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-06-SUMMARY.md"
      provides: "the human-review outcome (approved / issues-found) + the open-question dispositions + the 3 disease-mutant decisions"
      contains: "human-review"
  key_links:
    - from: "the human review"
      to: "spec.md + data/citations.json + data/sources.json + the 3 disease-mutant research docs"
      via: "the human confirms the branch-point mapping matches spec.md + the approved sources + the DIS-*-cand CANDIDATE claim_ids are scientifically justified (not fabricated)"
      pattern: "spec\\.md|DIS-.*-cand|approved"
    - from: "the human review"
      to: ".planning/phases/05.1-.../05.1-DESIGN.md + data/story_glucose/*.json"
      via: "the human reads the design doc (SC3 + SC4 + SC5 + the 11 new OQs) + spot-checks the skeleton JSON (14 edit-allowed, 0 single-Continue, the DIS-*-cand references, the 6CFO cast, the edit:structural tag)"
      pattern: "05\\.1-DESIGN|edit:enzyme|DIS-"
---

<objective>
Human review checkpoint for the FINAL post-replan glucose skeleton: the human reviews the branch-point mapping against spec.md + approved sources (SC1), design-reviews the edit-node contract covering the 14 edit-allowed nodes (SC4), resolves the 3 CRITICAL/HIGH disease-mutant decisions (IDH3A/IDH1, pyr.pdh cast-PDB, CS reframe), acknowledges the 24 open questions (13 original + 11 new), and confirms the SC2 reachability validation + the 2 new replan invariants pass.

Purpose: SC1 requires "human-review the mapping against spec.md + approved sources"; SC4 requires "design-review." The disease-mutant replan (plans 07-11) added 8 edit-allowed promotions, the Continue-to-MC conversion (0 single-Continue), the pyr.pdh 2OZL->6CFO cast fix, the etc.complex_i PLACEHOLDER->DIS-NDUFS8-01-cand claim_id fix, the tca.citrate_synthase edit:structural reframe, and 11 new open questions from the 3 research docs. The 3 CRITICAL/HIGH decisions (IDH3A/IDH1, cast-PDB approach, CS reframe) were FLAGGED in the skeleton (IDH3A default, 6CFO cast, edit:structural kept) but NOT decided unilaterally - the human confirms or overrides. This is checkpoint:human-verify (the human confirms OpenCode's automated work), NOT checkpoint:human-action.
Output: the human's review outcome (approved or issues-found) recorded in the SUMMARY, with dispositions for the 24 open questions + the 3 disease-mutant decisions.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-GRAPH-DIAGRAM.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-RESEARCH-disease-mutants-glycolysis.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-RESEARCH-disease-mutants-tca.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-RESEARCH-disease-mutants-etc.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-RESEARCH-pathway-topology.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-10-SUMMARY.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-11-SUMMARY.md
@spec.md
@data/citations.json
@data/sources.json
@data/story_glucose/tca.json
@data/story_glucose/pyruvate_branch.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human review of the FINAL post-replan glucose skeleton + contracts + classification + disease-mutant decisions</name>
  <what-built>
OpenCode authored the glucose story graph skeleton (43 nodes across 7 JSON files at data/story_glucose/), then applied the disease-mutant replan (plans 07-11): 8 enzyme nodes promoted to edit-allowed (gly.pyruvate_kinase [PKLR], tca.isocitrate_dh [IDH3A default], tca.succinyl_coa_synthetase [SUCLG1], tca.fumarase [FH], tca.malate_dh [MDH2], etc.complex_ii [SDHA], etc.complex_iii [CYC1], etc.complex_iv [COX4I1]) for a total of 14 edit-allowed nodes (12 disease-mutant + 1 structural [tca.citrate_synthase, edit:structural, no disease mutant] + 1 shared [tca.shuffle]); every single-"Continue" node converted to MC (0 single-Continue; 15 Continue-to-MC conversions with mc:observe second choices); the pyr.pdh cast PDB fixed (2OZL phospho-mimic mutant -> 6CFO wild-type); the etc.complex_i claim_id fixed (PLACEHOLDER_PHASE7_ETC -> DIS-NDUFS8-01-cand); the tca.citrate_synthase reframed (edit:structural tag); DIS-*-cand CANDIDATE claim_ids added to all 12 disease-mutant nodes (CANDIDATE pending Phase 7, NOT approved, NOT yet in data/citations.json). 05.1-DESIGN.md updated (SC5 table + 14-row edit-node contract + 11 new disease-mutant open questions). 05.1-GRAPH-DIAGRAM.md + SVG + ASCII regenerated. tests/test_glucose_reachability.py updated with 3 new invariant assertions (0 single-Continue, 14 edit-allowed, cast-PDB + claim_id fixes); 8 tests PASS. Node count UNCHANGED at 43 (the replan added choices/tags/claim_ids, not nodes). The citation gate is NOT run during Phase 5.1 (Phase 7 approves the DIS-*-cand claims + cast-PDB claims).
  </what-built>
  <how-to-verify>
**Step 1 - Review the branch-point mapping (SC1) against spec.md + approved sources:**
Read 05.1-DESIGN.md sections 4 (choice-point contract) + 6 (SC5 classification table). Confirm the branch-point mapping matches spec.md lines 11-34 + the approved sources (LIBRETEXTS-METAB-GLYCOLYSIS, LIBRETEXTS-METAB-TCA, PDB-4PFK, PDB-1CSC). Confirm NO invented branches (every branch point traces to spec.md, an approved source, or a flagged PLACEHOLDER_PHASE7/DIS-*-cand node). The replan did NOT change any branch-point mapping (it added edit-allowed tags + MC choices to EXISTING nodes; the goto destinations are unchanged).

**Step 2 - Design-review the edit-node contract (SC4) for the 14 edit-allowed nodes:**
Read 05.1-DESIGN.md section 5. Confirm:
  - The 14-row edit-allowed table correctly maps each node to its enzyme_id + DIS-*-cand claim_id (CANDIDATE) + the known-edit branch route + the unknown-edit bad-ending fallback.
  - The tca.citrate_synthase structural-exploration exception is documented (edit:structural tag; NO disease mutant; the restoration narrative does NOT apply at CS).
  - The tca.shuffle shared edit point is documented (edit:enzyme:tca.aconitase; secondary aconitase edit).
  - The 5-step data-flow (player edit -> EditIntent -> EditRouter.route(enzyme_id) -> node-id -> engine._enter -> on_enter MolAction(op=edit)) is correct against c14/engine.py + c14/edit_router.py.
  - The restoration-narrative Phase 7 content boundary is explicit (skeleton = structure; edits.json + text + on_enter pre-edit MolActions = Phase 7).

**Step 3 - Resolve the 3 CRITICAL/HIGH disease-mutant decisions (FLAGGED in the skeleton; the human confirms or overrides):**
  1. IDH3A vs IDH1 (tca.isocitrate_dh): the skeleton uses IDH3A M204I (DIS-IDH3A-01-cand) as DEFAULT (biochemically correct TCA enzyme). Alternative: IDH1 R132H (DIS-IDH1-01-cand, cancer-mutant, real mutant PDB 3INM, narratively powerful but NOT the TCA enzyme - cytosolic NADP+-dependent). Decide: keep IDH3A default / switch to IDH1 / defer.
  2. pyr.pdh cast PDB: the skeleton loads 6CFO (WT, 2.70 A) [DEFAULT]. Alternative: 1NI4 (WT, 1.95 A, partial). Phase 7 approach: (a) apply-edit (load 6CFO -> cmd.alter resi 138 -> diseased state, approximate geometry) vs (b) load-mutant-PDB (fetch 6CER V138M directly, real geometry, scientifically stronger per spec.md item 4). Decide: confirm 6CFO cast / prefer 1NI4 / prefer approach (a) or (b) for the mutant reveal.
  3. tca.citrate_synthase reframe: the skeleton keeps edit:enzyme + adds edit:structural (structural-exploration, NO disease mutant) [DEFAULT]. Alternative: remove the edit:enzyme tag entirely. Decide: keep the reframe / remove the tag.

**Step 4 - Acknowledge the 24 open questions (05.1-DESIGN.md section 9: 13 original + 11 new disease-mutant):**
Provide a disposition for each (defer to Phase 7 / accept the skeleton's placeholder / adjust). The 11 new disease-mutant OQs: IDH3A/IDH1, cast-PDB 6CFO/1NI4 + 6CER approach, CS reframe, OGDH deeper scan, PFKM mutant choice (G209D/W686C/R39L), Aconitase bovine-1ACO homology + numbering, COX4I1 P152T/T130P numbering, CYC1 C12W/L215F choice, LDH not-promoted (frameshift), ETC non-human-PDB organism caveat, DIS-*-cand candidate status.

**Step 5 - Confirm SC2 reachability + the 2 new replan invariants pass:**
Run `python3.6 -m unittest tests.test_glucose_reachability -v` - confirm all 8 tests PASS: green on the full 43-node skeleton (all 4 tiers reachable, 11 endings), red on both orphaned variants, NO single-Continue node (Continue-to-MC invariant), exactly 14 edit-allowed nodes (promotion invariant), pyr.pdh casts 6CFO (not 2OZL), etc.complex_i carries DIS-NDUFS8-01-cand (not PLACEHOLDER).

**Step 6 - Spot-check the skeleton JSON + the diagram:**
Open 1-2 skeleton files (e.g. data/story_glucose/tca.json for the 4 promotions + CS reframe; data/story_glucose/pyruvate_branch.json for the pyr.pdh cast fix + anaer.ldh MC) and confirm the tags/choices/claim_ids match the design doc. Open 05.1-graph.svg (or 05.1-graph.txt) and confirm the 14 edit-allowed nodes are marked (golden) + the new MC edges are shown.
  </how-to-verify>
  <resume-signal>
Type "approved" if the branch-point mapping + 14-row edit-node contract + SC5 classification + SC2/invariant validation + the 3 disease-mutant decisions are correct. OR describe specific issues to fix (e.g. "IDH: switch to IDH1 R132H" or "CS: remove the edit:enzyme tag" or "OQ: drop anaer.ethanolic - mammalian host"). For the 3 CRITICAL/HIGH decisions, state your choice explicitly (e.g. "IDH3A default confirmed", "6CFO cast + approach (b) load-mutant-PDB", "CS reframe kept"). The agent records the dispositions + decisions in the SUMMARY + applies any required skeleton/design-doc fixes.
  </resume-signal>
</task>

</tasks>

<verification>
- The human has read 05.1-DESIGN.md (sections 4, 5, 6, 9) + the 3 research docs + spot-checked the skeleton JSON + the diagram.
- The human has confirmed the branch-point mapping matches spec.md + approved sources (SC1 human-review).
- The human has design-reviewed the 14-row edit-node contract (SC4 design-review) including the CS structural exception + the tca.shuffle shared edit point.
- The human has resolved the 3 CRITICAL/HIGH disease-mutant decisions (IDH3A/IDH1, pyr.pdh cast-PDB + approach, CS reframe).
- The human has acknowledged the 24 open questions + provided dispositions.
- The human has confirmed SC2 + the 2 replan invariants pass (python3.6 -m unittest tests.test_glucose_reachability -v - 8 tests PASS).
- The human has typed "approved" OR described specific issues.
</verification>

<success_criteria>
- The human's review outcome is recorded in the SUMMARY (approved / issues-found + the 24 OQ dispositions + the 3 disease-mutant decisions).
- SC1 human-review complete: the branch-point mapping is confirmed (no invented branches; the replan did not change mappings).
- SC4 design-review complete: the 14-row edit-node contract is confirmed sound.
- The 3 CRITICAL/HIGH disease-mutant decisions are resolved (IDH3A/IDH1, cast-PDB, CS reframe).
- The 24 open questions have dispositions.
- SC2 + the 2 replan invariants confirmed: 8 reachability tests PASS (green + red + 0 single-Continue + 14 edit-allowed + cast-PDB + claim_id fixes).
- If issues were found, the agent applies the required fixes to the skeleton JSON + design doc before the phase is marked complete.
</success_criteria>

<output>
After completion, create .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-06-SUMMARY.md recording the human-review outcome (approved / issues-found) + the 24 open-question dispositions + the 3 disease-mutant decisions + any fixes applied.
</output>
