---
phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts
plan: 14
subsystem: testing
tags: [reachability, bfs, sc2, bad-endings, preface, invariant, edit-prompt, python36, unittest]

# Dependency graph
requires:
  - phase: 05.1-12
    provides: the 9 new bad-ending nodes (6 1a + 3 1b) + edit.prompt 12-choice stub this plan's new invariant test machine-checks
  - phase: 05.1-13
    provides: the 2 preface nodes + manifest start shift (intro.select -> intro.preface) this plan's count/start assertions track
  - phase: 05.1-04
    provides: the manifest.json + StoryGraph.load + check_reachability BFS + the original 5-test SC2 green/red suite this plan updates
  - phase: 05.1-11
    provides: the 3 replan-invariant test methods (0 single-Continue, 14 edit-allowed, pdh cast-PDB fix) this plan preserves UNCHANGED
provides:
  - the SC2 reachability suite updated for the post-expansion skeleton (54 nodes / 20 endings / intro.preface start)
  - 3 stale assertions corrected (node 43->54, ending 11->20, start intro.select->intro.preface)
  - 1 new invariant test (test_all_new_bad_endings_reachable_from_edit_prompt) locking in the CG-collection bad-ending reachability
  - 9-test reachability suite (8 prior + 1 new); full suite 207 tests (zero regression)
affects: [05.1-06 (human review -- the 9-test suite is the machine-checked guard the reviewer relies on), phase-7 (the bad-ending-reachability invariant guards the expanded pool so a future regression fails loudly)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Bad-ending-reachability invariant: assert every new bad ending has an incoming edit.prompt choice.goto (structural BFS path) + the 1a edit:unknown / 1b edit:known tag distinction, so a future regression that adds a bad ending without an incoming edge fails the test loudly"
    - "Stale-count-assertion update protocol: when a skeleton expansion changes node/ending counts or the start node, the reachability tests are the single place to update the 3 pinned assertions (node count, ending count, start node) -- the BFS + tier-set + invariant logic stay verbatim"

key-files:
  created: []
  modified:
    - tests/test_glucose_reachability.py — 3 stale assertions updated (54 nodes / 20 endings / intro.preface start) + docstrings rewritten for the 05.1-12/13 expansion + 1 new test method (test_all_new_bad_endings_reachable_from_edit_prompt)

key-decisions:
  - "Updated (not weakened) the 3 stale assertions to the new final values: 43->54 nodes, 11->20 endings (1T+3G+2N+5B -> 1T+3G+2N+14B), start intro.select->intro.preface. The assertions stay EXACT (equality), not range-based, so a future regression off the final count fails loudly."
  - "Added the bad-ending-reachability invariant as a NEW test (not a re-scope of an existing one): it checks all 9 Plan-12 bad endings are reachable from edit.prompt via choice.goto + the 1a edit:unknown / 1b edit:known tag distinction. This locks in Requirement 1 (CG-collection) at the structural level."
  - "Preserved all 8 prior tests verbatim in logic: the 2 RED variants still RED from intro.preface (orphaning is unreachable from any start); the 3 replan invariants (0 single-Continue, 14 edit-allowed, pdh cast-PDB fix) UNCHANGED; the no-pymol testability test UNCHANGED. Only docstrings + the 3 count/start assertions + 1 new method changed."

patterns-established:
  - "Expansion follow-up test update: a skeleton expansion plan (e.g. 05.1-12/13) intentionally leaves count assertions stale; the IMMEDIATELY following test plan (e.g. 05.1-14) updates them + adds any new invariant. This decoupling lets the expansion plan verify via direct BFS (not the unittest) and concentrates the test maintenance in one plan."

# Metrics
duration: 29 min
completed: 2026-08-22
---

# Phase 5.1 Plan 14: Reachability Test Update (54-Node / 20-Ending / Preface Start) Summary

**Updated the 3 stale SC2 reachability assertions to the post-expansion final values (54 nodes, 20 endings 1T+3G+2N+14B, start intro.preface) + added a new bad-ending-reachability invariant test that all 9 Plan-12 bad endings are reachable from edit.prompt with the 1a edit:unknown / 1b edit:known tag distinction -- 9 tests PASS, full suite 207 PASS**

## Performance

- **Duration:** 29 min
- **Started:** 2026-08-22T05:03:56Z
- **Completed:** 2026-08-22T05:33:34Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- Updated the 3 stale count/start assertions to the post-expansion skeleton: node count 43 -> 54 (43 base + 9 bad endings from Plan 05.1-12 + 2 preface nodes from Plan 05.1-13); ending count 11 -> 20 (1T+3G+2N+5B -> 1T+3G+2N+14B; the Bad tier grew 5 -> 14 via 9 new bad-ending nodes); manifest start intro.select -> intro.preface (the preface Beat A is the new entry point after Plan 05.1-13).
- Renamed test_manifest_loads_all_43_nodes -> test_manifest_loads_all_54_nodes (method name + docstring + 2 assertions) so the test name reflects the current final count.
- Rewrote the module + class docstrings to document the 05.1-12/13 expansion (Req 1 bad-ending CG-collection + Req 2 preface sequence) + the manifest start shift; updated the 2 red-variant docstrings to note the BFS now starts from intro.preface (still RED -- orphaning is unreachable from any start).
- Added test_all_new_bad_endings_reachable_from_edit_prompt (the 9th test): asserts all 9 Plan-12 bad endings (6 1a unknown-pool + 3 1b known-consequence) exist + are is_ending="bad" + have an incoming edit.prompt choice.goto (structural BFS reachability) + the 1a choices carry edit:unknown and the 1b choices carry edit:known (the tag distinction so Phase 7 routes deterministically) + the full check_reachability is still GREEN.
- Full suite 207 tests PASS (was 206; +1 new test); the 8 prior tests UNCHANGED in logic (only docstrings + the 3 count/start assertions changed); zero regression.

## Task Commits

Each task was committed atomically:

1. **Task 1: Update the 3 stale count/start assertions + docstrings for the 54-node / 20-ending / intro.preface-start skeleton** - `3a268c0` (feat)
2. **Task 2: Add test_all_new_bad_endings_reachable_from_edit_prompt (the CG-collection reachability invariant)** - `37fa3bb` (feat)

**Plan metadata:** (this commit) (docs: complete plan)

## Files Created/Modified
- `tests/test_glucose_reachability.py` - the SC2 reachability suite. Task 1: 3 stale assertions updated (54 nodes / 20 endings / intro.preface start) + module/class/red-variant docstrings rewritten for the 05.1-12/13 expansion + test_manifest_loads_all_43_nodes renamed to _54_nodes. Task 2: +1 new test method (test_all_new_bad_endings_reachable_from_edit_prompt) asserting all 9 Plan-12 bad endings are reachable from edit.prompt with the 1a/1b tag distinction. 9 tests total (8 prior + 1 new); all PASS.

## Decisions Made
- **Updated (not weakened) the 3 stale assertions to exact new final values** (54, 20, intro.preface) rather than loosening to ranges -- keeps the machine-checked guard strict so a future regression off the final count fails loudly.
- **Added the bad-ending-reachability invariant as a NEW test** rather than re-scoping an existing one -- preserves the 8 prior tests verbatim in logic and concentrates the Plan-12 expansion's structural guarantee in one explicitly-named test.
- **Preserved the 2 RED variants verbatim in assertions** -- orphaning end.true / adding an extra orphaned ending is unreachable from intro.preface too (the preface is upstream of everything), so the RED tests still pass with g.start_node() (now intro.preface); only the docstrings changed to note the new start.
- **Did NOT rewrite git history** after the parallel-race deviation (see Deviations) -- the Task 1 commit 3a268c0 is in the published local branch with parallel agents active; rewriting risked further races. The test content is correct; the accidentally-bundled diagram content is also correct (54-node version), just redundantly attributed.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Task 1 commit accidentally bundled 2 pre-staged diagram files (parallel-execution race)**
- **Found during:** Task 1 commit (staging tests/test_glucose_reachability.py)
- **Issue:** The working tree held 2 pre-staged diagram files (`.planning/.../05.1-graph.svg` + `05.1-graph.txt`) left in the git index by an interrupted prior Plan 16 ("diagram regen") attempt. When I ran `git add tests/test_glucose_reachability.py` (staging ONLY the test file) and then `git commit`, the commit included all 3 files (test + 2 diagrams) because the diagrams were already staged. The commit message said "test assertion update" but the commit also contained diagram changes, violating the atomic-commit principle.
- **Fix:** Attempted to undo + recommit cleanly. The undo itself hit a second race (see deviation 2). Ultimately left commit 3a268c0 as-is: the test content is correct + verified (8 tests PASS); the diagram content is the correct 54-node version (just redundantly attributed to Plan 14 as well as Plan 16's own commit). Did NOT rewrite history (parallel agents were actively committing; rewriting the published local branch risked further races). Documented in this Summary + STATE.md.
- **Files modified:** (none beyond the planned test file; the diagram files were already modified by the interrupted Plan 16 attempt)
- **Verification:** `git show --stat 3a268c0` shows 3 files (test + 2 diagrams); `git show HEAD:tests/test_glucose_reachability.py` confirms the test changes are present + correct; 8 tests PASS at that commit.
- **Committed in:** 3a268c0 (Task 1 commit; the diagram files rode along)

**2. [Rule 3 - Blocking] `git reset --soft HEAD~1` undid a parallel agent's commit instead of my own (parallel-execution race)**
- **Found during:** The undo attempt for deviation 1
- **Issue:** To cleanly recommit only the test file, I ran `git reset --soft HEAD~1` intending to undo my commit (3a268c0). But between my commit and my reset, parallel agents (Plan 15 + Plan 16, running concurrently per parallelization=true) had added 4 commits ON TOP of mine (ea1763e, 32c4dd5, fb826ea, d5a70a0). So HEAD was d5a70a0 (a Plan 15 agent's commit), not 3a268c0. `HEAD~1` therefore moved HEAD from d5a70a0 to fb826ea, orphaning the Plan 15 agent's d5a70a0 commit and leaving its DESIGN.md changes staged in my index.
- **Fix:** Recognized the race via `git reflog` (showed the parallel commits + my botched reset). Restored the correct tip with `git reset --soft d5a70a0` -- HEAD returned to d5a70a0 (the correct tip with Plans 14/15/16 all complete), the index matched HEAD (nothing staged), the orphaned commit was re-incorporated. No data loss; my Task 1 commit (3a268c0) was safely in history the whole time. Then proceeded to commit Task 2 cleanly (37fa3bb) with a pre-commit staging check (`git diff --cached --name-only` empty before `git add`).
- **Files modified:** (none -- this was a git-state restoration; no file content changed)
- **Verification:** `git log --oneline` confirmed d5a70a0 restored as tip; `git diff --cached --name-only` empty (clean index); `git status --short` only the unrelated oc_stats.txt. Task 2 then committed cleanly as 37fa3bb (1 file, only the test file).
- **Committed in:** (no commit -- state restoration; the fix is visible in the reflog)

---

**Total deviations:** 2 auto-fixed (2 blocking, both from the same parallel-execution race)
**Impact on plan:** Both deviations were git-state side effects of parallel execution (parallelization=true), NOT code/data issues. The deliverable (the test file) is correct + verified (9 tests PASS, 207 full suite PASS, zero regression). No scope creep; no test logic changed beyond the plan. The atomic-commit principle was slightly compromised (Task 1's commit redundantly includes diagram files) but the content is correct and the redundancy is harmless (Plan 16's own commit also has the diagrams).

## Issues Encountered
- Parallel-execution race (parallelization=true): Plans 14, 15, 16 executed concurrently, interleaving commits on the same branch. This caused 2 git-state deviations (documented above). The key mitigations: (a) use `git reflog` to diagnose race-induced state; (b) `git reset --soft <correct-tip>` to restore; (c) pre-commit staging check (`git diff --cached --name-only`) before every commit to ensure only intended files are staged. The Plan 16 agent's STATE.md entry incorrectly attributed my Task 2 to commit 32c4dd5 (actually Plan 16's own GRAPH-DIAGRAM commit); this Summary + the [Execution 05.1-14] STATE.md entry correct the record (my Task 2 = 37fa3bb).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Plan 14 complete; the SC2 reachability suite is GREEN on the final post-expansion skeleton (54 nodes / 20 endings / intro.preface start) with 9 tests + the new bad-ending-reachability invariant.
- Phase 5.1 status: 15 of 16 plans complete (01-05 + 07-16 all DONE; the expansion Wave 2 Plans 12/13/14/15/16 all complete). ONLY 05.1-06 (human review of the FINAL post-expansion skeleton: 2 new requirements + 3 CRITICAL disease-mutant decisions + 32 OQs) remains. After 06, Phase 5.1 is COMPLETE and Phase 6 (Qt UI MVP) is UNBLOCKED.
- Blocker/concern: none from this plan. The 9-test suite is the machine-checked guard the 05.1-06 reviewer relies on. The parallel-execution race is a process issue (parallelization=true on plans with shared git state), not a deliverable issue -- all 3 plans (14/15/16) completed correctly.

---
*Phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts*
*Completed: 2026-08-22*
