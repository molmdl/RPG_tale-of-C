---
phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - data/story_glucose/bad_endings.json
autonomous: true

must_haves:
  truths:
    - "the RngEngine-weighted unknown-edit bad-ending pool has 8 nodes (bad.lost_connection + bad.released_from_host [existing] + 6 new) so the SAME wrong edit can produce DIFFERENT bad endings (CG-collection variety, not punishment)"
    - "3 known-consequence bad-ending nodes exist (deterministic outcomes of specific known edits, tagged edit:known — distinct from the 1a random pool tagged edit:unknown)"
    - "all 9 new bad-ending nodes are reachable from edit.prompt via choice.goto (no orphaned endings; the BFS sees them)"
    - "all new bad endings carry is_ending='bad' + claim_ids=['PLACEHOLDER_PHASE7_BAD'] + on_enter=[{hide_all}] + choices=[] (ending nodes; text is Phase 7)"
    - "the 4 ending tiers stay (True/Good/Normal/Bad); the Bad tier GROWS from 5 to 14; True/Good/Normal counts UNCHANGED (1/3/2); the 14 edit-allowed count UNCHANGED; 0 single-Continue UNCHANGED (bad endings have choices=[], not subject to the single-Continue rule)"
  artifacts:
    - path: "data/story_glucose/bad_endings.json"
      provides: "9 new bad-ending nodes (6 unknown-pool 1a + 3 known-consequence 1b) + the edit.prompt stub expanded from 3 to 12 structural choices"
      contains: "bad.enzyme_collapse"
  key_links:
    - from: "edit.prompt stub choices"
      to: "the 9 new bad-ending nodes"
      via: "choice.goto (structural BFS paths; runtime routing is owned by the Phase 4 EditRouter)"
      pattern: "bad\\.(enzyme_collapse|wrong_substrate_trapped|broken_active_site|lost_in_cytosol|proton_leak|misfolded_aggregate|active_site_destroyed|substrate_channel_blocked|cofactor_lost)"
---

<objective>
Expand the bad-ending pool for the "CG-collection" feel: the SAME wrong edit operation can lead to DIFFERENT bad endings (variety, not punishment). This implements Requirement 1 from the 05.1-06 checkpoint feedback, split into 1a (randomize/expand the unknown-edit pool) + 1b (known-consequence bad endings).

Purpose: The user wants failure to feel like a "CG collection" — a repeated mistake shouldn't always produce the identical ending. The RngEngine-weighted bad-ending pool + the EditRouter routing ALREADY EXIST (Phase 4, c14/edit_router.py: per-enzyme OVERRIDE else global fallback, RngEngine-weighted, reproducible). This plan EXPANDS the POOL the router picks from (more ending nodes), it does NOT change routing logic. 1a grows the unknown-edit pool from 2 → 8 (variety via node count). 1b adds 3 known-consequence bad-ending NODES (deterministic, distinct from the random pool; the BINDING to specific edits is Phase 7 edits.json content — the skeleton carries only the NODES + reachability + PLACEHOLDER claim_ids).

BOUNDARY (per AGENTS.md no-fabricated-science): the skeleton carries NODES + is_ending="bad" + PLACEHOLDER_PHASE7_BAD claim_ids + structural reachability (edit.prompt choices). The edits.json entries (reverse-mutation signatures + routing), the text_dramatic/text_teaching content, and the on_enter pre-edit MolActions are ALL Phase 7. No new PDB IDs or DIS-*-cand claims are added to data/citations.json (Phase 7 batch). The naming is creative but scientifically grounded in what could go wrong from a bad edit (not punitive).

Output: 1 modified file (data/story_glucose/bad_endings.json) with +9 bad-ending nodes + the edit.prompt stub expanded. Node count 43 → 52; ending count 11 → 20 (1T+3G+2N+14B). No other skeleton file touched.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-CHECKPOINT-FEEDBACK.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-EXPANSION-SUMMARY.md
@data/story_glucose/bad_endings.json
@data/story_glucose/manifest.json
@c14/story/model.py
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 1a — add 6 unknown-pool bad endings + expand edit.prompt structural choices</name>
  <files>data/story_glucose/bad_endings.json</files>
  <action>
Edit data/story_glucose/bad_endings.json. Add 6 NEW bad-ending nodes to the "nodes" object (the unknown-edit RngEngine pool grows from 2 → 8). Each new node follows the EXACT shape of the existing bad.lost_connection / bad.released_from_host nodes: text_dramatic placeholder (creative, scientifically grounded in what could go wrong from a bad edit, NOT punitive), text_teaching "TBD — Phase 7 teaching text.", claim_ids ["PLACEHOLDER_PHASE7_BAD"], tags ["ending:bad", "edit:unknown"], on_enter [{"op": "hide_all"}], is_ending "bad", choices []. Use 2-space indentation matching the existing file.

The 6 new 1a nodes (naming convention bad.<descriptive_name>, per the feedback examples):
1. "bad.enzyme_collapse" — text_dramatic: "TBD — your edit misfolded the enzyme; it collapses into a useless tangle."
2. "bad.wrong_substrate_trapped" — text_dramatic: "TBD — a foreign substrate is jammed in the active site; the reaction stalls."
3. "bad.broken_active_site" — text_dramatic: "TBD — the catalytic residues are scattered; the active site is broken."
4. "bad.lost_in_cytosol" — text_dramatic: "TBD — with no enzyme to bind you, you drift free in the cytosol, adrift and purposeless."
5. "bad.proton_leak" — text_dramatic: "TBD — the proton gradient bleeds away; your electrons leak to nowhere."
6. "bad.misfolded_aggregate" — text_dramatic: "TBD — the enzyme aggregates into a proteostasis tangle; the cell flags it for destruction."

THEN expand the edit.prompt stub's "choices" array from 3 → 9 entries (add 6 new structural choices, one per new 1a bad ending). Keep the existing 3 choices UNCHANGED (bad.critical_residue_break / bad.lost_connection / bad.released_from_host). Append the 6 new choices after the existing 3, each with the SAME structural-stub label pattern + the "edit:unknown" tag:
- {"label": "Make an unrecognized edit (structural path C)", "goto": "bad.enzyme_collapse", "tags": ["edit:unknown"]}
- {"label": "Make an unrecognized edit (structural path D)", "goto": "bad.wrong_substrate_trapped", "tags": ["edit:unknown"]}
- {"label": "Make an unrecognized edit (structural path E)", "goto": "bad.broken_active_site", "tags": ["edit:unknown"]}
- {"label": "Make an unrecognized edit (structural path F)", "goto": "bad.lost_in_cytosol", "tags": ["edit:unknown"]}
- {"label": "Make an unrecognized edit (structural path G)", "goto": "bad.proton_leak", "tags": ["edit:unknown"]}
- {"label": "Make an unrecognized edit (structural path H)", "goto": "bad.misfolded_aggregate", "tags": ["edit:unknown"]}

Do NOT modify the existing 4 bad-ending nodes (bad.lost_connection, bad.released_from_host, bad.cycle_trap_host_death, bad.critical_residue_break) — they stay verbatim. Do NOT touch edit.prompt's text_dramatic/text_teaching/claim_ids/tags/on_enter (only its choices array grows). Do NOT add edit:enzyme tags to any new node (the 14-edit-allowed invariant must stay). Preserve JSON validity + 2-space indent.
  </action>
  <verify>
Run from repo root (these do NOT depend on the stale test count assertions — the full unittest will fail on counts until Plan 14; that is EXPECTED, do NOT run the unittest as a PASS gate here):
1. python3.6 -c "import json; d=json.load(open('data/story_glucose/bad_endings.json')); n=d['nodes']; assert len(n)==11, len(n); assert n['bad.enzyme_collapse']['is_ending']=='bad'; assert n['bad.enzyme_collapse']['claim_ids']==['PLACEHOLDER_PHASE7_BAD']; assert n['bad.enzyme_collapse']['tags']==['ending:bad','edit:unknown']; assert n['bad.enzyme_collapse']['choices']==[]; assert n['bad.enzyme_collapse']['on_enter']==[{'op':'hide_all'}]; assert 'bad.wrong_substrate_trapped' in n and 'bad.broken_active_site' in n and 'bad.lost_in_cytosol' in n and 'bad.proton_leak' in n and 'bad.misfolded_aggregate' in n; assert len(n['edit.prompt']['choices'])==9; assert sum(1 for c in n['edit.prompt']['choices'] if 'edit:unknown' in c.get('tags',[]))==8; print('bad_endings.json 1a OK')"
2. python3.6 -c "from c14.story.graph import StoryGraph; from c14.story.validate import check_reachability; g=StoryGraph.load('data/story_glucose'); rep=check_reachability(g.all_nodes(), g.start_node()); print('is_ok', rep.is_ok, 'unreachable', rep.unreachable_endings); assert rep.is_ok, rep.unreachable_endings; assert not rep.unreachable_endings; print('direct BFS GREEN (all 17 endings reachable incl. 6 new 1a bad endings)')"
  </verify>
  <done>
bad_endings.json has 11 entries (4 existing bad endings + 6 new 1a bad endings + edit.prompt stub); edit.prompt has 9 structural choices (3 existing + 6 new 1a); all 6 new 1a bad endings are is_ending="bad" + PLACEHOLDER_PHASE7_BAD + edit:unknown tag + reachable from edit.prompt; direct BFS is GREEN (no unreachable endings).
  </done>
</task>

<task type="auto">
  <name>Task 2: 1b — add 3 known-consequence bad endings + expand edit.prompt structural choices</name>
  <files>data/story_glucose/bad_endings.json</files>
  <action>
Edit data/story_glucose/bad_endings.json (continuing from Task 1). Add 3 MORE bad-ending nodes to the "nodes" object. These are KNOWN-CONSEQUENCE bad endings — deterministic outcomes of specific known edits (distinct from the 1a random pool). Same node shape as Task 1 BUT with the tag "edit:known" (NOT "edit:unknown") to mark them as the deterministic pool. The BINDING to specific edit-allowed nodes (which known edit → which 1b bad ending) is Phase 7 edits.json content — the skeleton only adds the NODES + makes them reachable via edit.prompt structural choices.

The 3 new 1b nodes (naming convention bad.<descriptive_name>):
1. "bad.active_site_destroyed" — text_dramatic: "TBD — this specific edit shredded the active site; the enzyme is definitively dead." — tags: ["ending:bad", "edit:known"]
2. "bad.substrate_channel_blocked" — text_dramatic: "TBD — the substrate channel is plugged; nothing enters, nothing leaves." — tags: ["ending:bad", "edit:known"]
3. "bad.cofactor_lost" — text_dramatic: "TBD — the cofactor (NAD+/FAD) can no longer bind; the reaction is starved." — tags: ["ending:bad", "edit:known"]

(All with text_teaching "TBD — Phase 7 teaching text.", claim_ids ["PLACEHOLDER_PHASE7_BAD"], on_enter [{"op": "hide_all"}], is_ending "bad", choices [].)

THEN expand the edit.prompt stub's "choices" array from 9 → 12 entries (append 3 new structural choices, one per new 1b bad ending, with the "edit:known" tag to distinguish from 1a's "edit:unknown"):
- {"label": "Make a known-wrong edit (structural path I)", "goto": "bad.active_site_destroyed", "tags": ["edit:known"]}
- {"label": "Make a known-wrong edit (structural path J)", "goto": "bad.substrate_channel_blocked", "tags": ["edit:known"]}
- {"label": "Make a known-wrong edit (structural path K)", "goto": "bad.cofactor_lost", "tags": ["edit:known"]}

DECISIONS DOCUMENTED (in this action for the executor + later in 05.1-DESIGN.md via Plan 15):
- Shared pool (NOT per-enzyme) for the skeleton — the variety comes from node count (8 unknown + 3 known), not new routing logic. Per-enzyme bad-ending pools are Phase 7 content (the Phase 4 EditRouter OVERRIDE semantics already support them).
- 1b nodes are reachable via edit.prompt structural choices (same as 1a) for the BFS. The DETERMINISTIC routing (specific known edit → specific 1b bad ending) is Phase 7 edits.json content; the skeleton only guarantees structural reachability.
- The "edit:known" tag distinguishes 1b from 1a's "edit:unknown" so Phase 7 can route deterministically. The existing bad.critical_residue_break uses "edit:known_critical" (a third category — known CRITICAL residue break).

Do NOT modify any node from Task 1 or the existing 4. Preserve JSON validity + 2-space indent.
  </action>
  <verify>
1. python3.6 -c "import json; d=json.load(open('data/story_glucose/bad_endings.json')); n=d['nodes']; assert len(n)==14, len(n); assert n['bad.active_site_destroyed']['tags']==['ending:bad','edit:known']; assert n['bad.active_site_destroyed']['is_ending']=='bad'; assert n['bad.active_site_destroyed']['claim_ids']==['PLACEHOLDER_PHASE7_BAD']; assert n['bad.active_site_destroyed']['choices']==[]; assert 'bad.substrate_channel_blocked' in n and 'bad.cofactor_lost' in n; assert len(n['edit.prompt']['choices'])==12; assert sum(1 for c in n['edit.prompt']['choices'] if 'edit:known' in c.get('tags',[]))==3; assert sum(1 for c in n['edit.prompt']['choices'] if 'edit:unknown' in c.get('tags',[]))==8; assert sum(1 for c in n['edit.prompt']['choices'] if 'edit:known_critical' in c.get('tags',[]))==1; print('bad_endings.json 1b OK')"
2. python3.6 -c "from c14.story.graph import StoryGraph; from c14.story.validate import check_reachability; g=StoryGraph.load('data/story_glucose'); rep=check_reachability(g.all_nodes(), g.start_node()); assert rep.is_ok, rep.unreachable_endings; teirs={g.get_node(e).is_ending for e in rep.reachable_endings}; assert teirs=={'true','good','normal','bad'}, teirs; assert sum(1 for nd in g.all_nodes().values() if nd.is_ending)==20, 'ending count'; assert len(g.all_nodes())==52, 'node count'; print('direct BFS GREEN; 52 nodes; 20 endings (1T+3G+2N+14B); all 4 tiers reachable')"
3. python3.6 -c "from c14.story.graph import StoryGraph; g=StoryGraph.load('data/story_glucose'); ea=[nid for nid,nd in g.all_nodes().items() if any(str(t).startswith('edit:enzyme:') for t in (nd.tags or []))]; assert len(ea)==14, ea; print('14 edit-allowed UNCHANGED')"
  </verify>
  <done>
bad_endings.json has 14 entries (4 existing bad endings + 6 1a + 3 1b + edit.prompt stub); edit.prompt has 12 structural choices (3 existing + 6 1a edit:unknown + 3 1b edit:known); direct BFS GREEN; 52 nodes; 20 endings (1T+3G+2N+14B); 14 edit-allowed UNCHANGED.
  </done>
</task>

</tasks>

<verification>
- python3.6 -c "import json; d=json.load(open('data/story_glucose/bad_endings.json')); assert len(d['nodes'])==14; assert len(d['nodes']['edit.prompt']['choices'])==12; print('bad_endings.json valid: 14 entries, edit.prompt 12 choices')"
- python3.6 -c "from c14.story.graph import StoryGraph; from c14.story.validate import check_reachability; g=StoryGraph.load('data/story_glucose'); rep=check_reachability(g.all_nodes(), g.start_node()); assert rep.is_ok and not rep.unreachable_endings; assert {g.get_node(e).is_ending for e in rep.reachable_endings}=={'true','good','normal','bad'}; assert sum(1 for n in g.all_nodes().values() if n.is_ending)==20; assert len(g.all_nodes())==52; print('SC2 GREEN: 52 nodes, 20 endings, 4 tiers')"
- INVARIANTS PRESERVED (direct checks, NOT the unittest which has stale count assertions until Plan 14): 14 edit-allowed nodes (no edit:enzyme tags added to new bad endings); 0 single-Continue (new bad endings have choices=[]; edit.prompt has 12 choices — not single-Continue); 4 ending tiers; True/Good/Normal counts unchanged (1/3/2); Bad tier grew 5 → 14.
- NOTE: python3.6 -m unittest tests.test_glucose_reachability will show 2 STALE-COUNT failures (test_manifest_loads_all_43_nodes expects 43 nodes but now 52; test_reachability_green_all_four_tiers expects 11 endings but now 20). These are EXPECTED — Plan 14 updates those count assertions. The other 6 tests (red variants, no pymol, 0 single-Continue, 14 edit-allowed, pdh cast fix) still PASS. Do NOT run the unittest as a PASS gate in this plan.
</verification>

<success_criteria>
- 6 new 1a unknown-pool bad endings + 3 new 1b known-consequence bad endings exist in bad_endings.json (is_ending="bad", PLACEHOLDER_PHASE7_BAD, edit:unknown/edit:known tags, on_enter hide_all, choices=[]).
- The edit.prompt stub has 12 structural choices (3 existing + 6 1a edit:unknown + 3 1b edit:known) so the BFS sees all 9 new bad endings reachable.
- Direct BFS reachability is GREEN: 52 nodes, 20 endings (1T+3G+2N+14B), all 4 tiers reachable, no unreachable endings.
- 14 edit-allowed nodes UNCHANGED (no edit:enzyme tags added); 0 single-Continue UNCHANGED; True/Good/Normal counts unchanged.
- No fabricated science (PLACEHOLDER_PHASE7_BAD only; no new PDB IDs or DIS-*-cand claims in data/citations.json).
</success_criteria>

<output>
After completion, create .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-12-SUMMARY.md recording: the 9 new bad-ending node ids (6 1a + 3 1b), the edit:unknown vs edit:known tag distinction, the edit.prompt stub expansion (3→12 choices), the shared-pool decision (variety via node count, not new routing), the 1b-Reachable-via-edit.prompt decision (deterministic binding is Phase 7), the 43→52 node count, the 11→20 ending count (1T+3G+2N+14B), the direct-BFS GREEN confirmation, and the note that 2 test count assertions are stale (fixed in Plan 14).
</output>
