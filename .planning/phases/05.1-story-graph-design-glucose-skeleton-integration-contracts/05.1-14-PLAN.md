---
phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts
plan: 14
type: execute
wave: 2
depends_on: ["05.1-12", "05.1-13"]
files_modified:
  - tests/test_glucose_reachability.py
autonomous: true

must_haves:
  truths:
    - "tests/test_glucose_reachability.py passes GREEN (8 tests) on the post-expansion skeleton: 54 nodes (was 43), 20 endings (was 11; 1T+3G+2N+14B), start node intro.preface (was intro.select)"
    - "the 2 stale count assertions are updated: test_manifest_loads_all_43_nodes renamed to _54_nodes with count 54 + start intro.preface; test_reachability_green_all_four_tiers ending-count assertion 11 -> 20"
    - "a NEW invariant test asserts all 9 bad endings added in Plan 12 (1a + 1b) are reachable from edit.prompt via choice.goto chains (the CG-collection reachability invariant)"
    - "all other tests UNCHANGED + still PASS: both red variants (orphaned end.true; extra orphaned ending), no pymol import, 0 single-Continue, 14 edit-allowed, pdh cast-PDB fix"
    - "the test docstrings are updated to explain the bad-ending expansion (Req 1) + the preface sequence (Req 2) + the new count/start"
  artifacts:
    - path: "tests/test_glucose_reachability.py"
      provides: "SC2 reachability tests updated for the 54-node / 20-ending / intro.preface-start skeleton + a new bad-ending-reachability invariant test"
      contains: "54"
  key_links:
    - from: "tests/test_glucose_reachability.py"
      to: "data/story_glucose/bad_endings.json (the 9 new bad endings)"
      via: "StoryGraph.load + check_reachability BFS from intro.preface"
      pattern: "edit\\.prompt"
---

<objective>
Update the SC2 reachability tests for the post-expansion skeleton (Plans 12 + 13): the bad-ending pool grew (11 -> 20 endings; 1T+3G+2N+14B), the preface sequence shifted the start node (intro.select -> intro.preface), and the node count grew (43 -> 54). Add a new invariant test that all 9 Plan-12 bad endings are reachable from edit.prompt (the CG-collection reachability requirement).

Purpose: The 2 stale count assertions (node count 43, ending count 11) + the stale start assertion (intro.select) are the ONLY tests that fail after Plans 12+13. This plan updates those 3 assertions to the new final values + adds the new bad-ending-reachability invariant. After this plan, the full 8-test suite is GREEN again (9 tests total after the new one). The tests machine-check the expansion's structural truths so a future regression (orphaned bad ending, wrong start, lost preface node) fails loudly.

Output: 1 modified file (tests/test_glucose_reachability.py) with updated docstrings + 3 updated assertions + 1 new test method. The full suite passes.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-CHECKPOINT-FEEDBACK.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-12-SUMMARY.md
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-13-SUMMARY.md
@tests/test_glucose_reachability.py
@data/story_glucose/manifest.json
@data/story_glucose/bad_endings.json
@data/story_glucose/intro.json
@c14/story/validate.py
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the 3 stale count/start assertions + docstrings for the 54-node / 20-ending / intro.preface-start skeleton</name>
  <files>tests/test_glucose_reachability.py</files>
  <action>
Edit tests/test_glucose_reachability.py. The 2 skeleton-expansion plans (05.1-12 bad-endings + 05.1-13 preface) grew the skeleton from 43 -> 54 nodes (added 9 bad-ending nodes + 2 preface nodes), grew the ending count from 11 -> 20 (the 9 new bad endings; 1T+3G+2N+14B), and changed the manifest start from intro.select -> intro.preface (the new preface Beat A is the entry point). Update the 3 stale assertions + the docstrings; keep all other tests verbatim.

1. Module docstring (lines 1-27): update the count + the expansion narrative. Change "43 nodes" references to "54 nodes"; add a paragraph documenting the 05.1-12/13 expansion: "+9 bad-ending nodes (Req 1: 6 unknown-pool 1a + 3 known-consequence 1b for the CG-collection feel; the RngEngine-weighted bad-ending pool grew 2 -> 8 unknown + 3 known) + 2 preface nodes (Req 2: intro.preface Beat A before character select + intro.shell_glucose Beat B before the journey; manifest start intro.select -> intro.preface). Ending distribution grew on the Bad side: 1T+3G+2N+5B -> 1T+3G+2N+14B. The 14 edit-allowed count + the 0 single-Continue invariant are UNCHANGED."

2. Class docstring (TestGlucoseReachability, ~lines 39-57): same updates — 54 nodes; the bad-ending + preface expansion; start intro.preface; ending count 20 (1T+3G+2N+14B); 14 edit-allowed + 0 single-Continue UNCHANGED.

3. test_manifest_loads_all_43_nodes: RENAME to test_manifest_loads_all_54_nodes. Update the docstring to explain the 43 -> 54 growth (+9 bad endings + 2 preface). Change `self.assertEqual(len(nodes), 43, ...)` to `self.assertEqual(len(nodes), 54, "glucose skeleton has 54 nodes after the bad-ending + preface expansion (43 base + 9 bad endings + 2 preface)")`. Change `self.assertEqual(g.start_node(), "intro.select", ...)` to `self.assertEqual(g.start_node(), "intro.preface", "manifest start is intro.preface (the preface Beat A is the entry point after Plan 05.1-13)")`.

4. test_reachability_green_all_four_tiers: update the ending-count assertion. Change `self.assertEqual(len(all_endings), 11, "exactly 11 ending nodes (1T+3G+2N+5B)")` to `self.assertEqual(len(all_endings), 20, "exactly 20 ending nodes after the bad-ending expansion (1T+3G+2N+14B; the Bad tier grew 5 -> 14 via 9 new bad-ending nodes)")`. The `{"true","good","normal","bad"}` tier-set assertion STAYS (the new bad endings are all "bad" tier). The `rep.is_ok` + `rep.unreachable_endings == []` assertions STAY (the BFS still reaches all endings). Update the docstring to note the BFS now starts from intro.preface (the preface is upstream of everything; the endings are unchanged in tier, only the Bad count grew).

5. The 2 red-variant tests (test_reachability_red_orphaned_true_ending + test_reachability_red_extra_orphaned_ending): they call `check_reachability(orphaned, g.start_node())` which uses the NEW start (intro.preface) — still RED (orphaning end.true / adding an extra orphaned ending is unreachable from intro.preface too). NO assertion changes needed; just update the docstrings to note "start is now intro.preface (Plan 05.1-13)".

6. The 3 invariant tests (test_no_single_continue_choice, test_14_edit_allowed_nodes, test_pdh_cast_pdb_fix_and_complex_i_claim_id): UNCHANGED — the new preface nodes have 2 choices each (0 single-Continue holds), no new edit:enzyme tags (14 edit-allowed holds), pyr.pdh cast PDB + etc.complex_i claim_id UNCHANGED. Just update the class docstring context (already done in step 2).

Do NOT remove or weaken any test. Do NOT import pymol/PyQt5. Use the same StoryGraph.load + Node access patterns.
  </action>
  <verify>
python3.6 -m unittest tests.test_glucose_reachability -v  (8 tests PASS — the original 5 + the 3 replan-invariant tests, all with updated counts. The 2 formerly-stale count assertions now pass with 54 nodes + 20 endings + start intro.preface.)
  </verify>
  <done>
tests/test_glucose_reachability.py: test_manifest_loads_all_54_nodes (54 nodes, start intro.preface); test_reachability_green_all_four_tiers (20 endings, 4 tiers); 2 red variants still RED from intro.preface; 3 invariant tests unchanged. All 8 tests PASS.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add test_all_new_bad_endings_reachable_from_edit_prompt (the CG-collection reachability invariant)</name>
  <files>tests/test_glucose_reachability.py</files>
  <action>
Edit tests/test_glucose_reachability.py. ADD a new test method to the TestGlucoseReachability class that machine-checks Requirement 1 (the CG-collection bad-ending pool): all 9 bad endings added in Plan 05.1-12 (6 1a unknown-pool + 3 1b known-consequence) are reachable from the edit.prompt stub via choice.goto chains (so the BFS sees them — none are orphaned).

The new test method (insert after test_pdh_cast_pdb_fix_and_complex_i_claim_id, as the 9th test):

    def test_all_new_bad_endings_reachable_from_edit_prompt(self):
        """Bad-ending expansion invariant (Plan 05.1-12, Req 1 CG-collection):
        the 9 new bad endings (6 1a unknown-pool + 3 1b known-consequence)
        are all reachable from the edit.prompt structural stub via choice.goto
        chains, so the BFS sees them (no orphaned bad endings). The edit.prompt
        stub's 12 structural choices (3 existing + 6 1a edit:unknown + 3 1b
        edit:known) are the structural BFS paths; at runtime the Phase 4
        EditRouter (c14/edit_router.py) routes known edits to a branch node +
        unknown edits to the RngEngine-weighted bad-ending pool, BYPASSING the
        stub's choices. This test locks in the structural reachability of the
        expanded pool (a future regression that adds a bad ending without an
        incoming edit.prompt choice fails this test loudly)."""
        g = StoryGraph.load(self._story_dir)
        # The 9 new bad-ending node ids added in Plan 05.1-12.
        new_bad = [
            # 1a unknown-pool (6): tags ["ending:bad", "edit:unknown"]
            "bad.enzyme_collapse", "bad.wrong_substrate_trapped",
            "bad.broken_active_site", "bad.lost_in_cytosol",
            "bad.proton_leak", "bad.misfolded_aggregate",
            # 1b known-consequence (3): tags ["ending:bad", "edit:known"]
            "bad.active_site_destroyed", "bad.substrate_channel_blocked",
            "bad.cofactor_lost",
        ]
        # Each exists + is a bad ending.
        for nid in new_bad:
            self.assertIn(nid, g.all_nodes(),
                          "new bad ending %r must exist in the skeleton" % nid)
            self.assertEqual(g.get_node(nid).is_ending, "bad",
                             "new bad ending %r must be is_ending=bad" % nid)
        # The edit.prompt stub has a choice.goto to each of the 9 new bad endings.
        edit_prompt = g.get_node("edit.prompt")
        gotos = {c.goto for c in edit_prompt.choices}
        for nid in new_bad:
            self.assertIn(nid, gotos,
                          "edit.prompt must have a structural choice.goto to "
                          "new bad ending %r (else it is orphaned; the BFS "
                          "cannot reach it)" % nid)
        # The 1a/1b tag distinction: 1a edit:unknown, 1b edit:known.
        for c in edit_prompt.choices:
            if c.goto in new_bad[:6]:  # 1a pool
                self.assertIn("edit:unknown", c.tags or [],
                              "1a bad-ending choice must carry edit:unknown")
            elif c.goto in new_bad[6:]:  # 1b pool
                self.assertIn("edit:known", c.tags or [],
                              "1b bad-ending choice must carry edit:known")
        # The full check_reachability is still GREEN (no orphaned endings).
        rep = check_reachability(g.all_nodes(), g.start_node())
        self.assertTrue(rep.is_ok, "all endings reachable (no orphans)")
        self.assertNotIn(set(), [rep.unreachable_endings])

Use the same import + access patterns as the existing tests (StoryGraph, check_reachability are already imported at module top). Do NOT add new imports. Do NOT weaken the existing 8 tests.
  </action>
  <verify>
python3.6 -m unittest tests.test_glucose_reachability -v  (9 tests PASS — the 8 from Task 1 + the new test_all_new_bad_endings_reachable_from_edit_prompt. Confirm the new test runs + passes.)
  </verify>
  <done>
tests/test_glucose_reachability.py has 9 tests, all PASS; the new test_all_new_bad_endings_reachable_from_edit_prompt asserts all 9 Plan-12 bad endings are reachable from edit.prompt (structural BFS) + the 1a edit:unknown / 1b edit:known tag distinction.
  </done>
</task>

</tasks>

<verification>
- python3.6 -m unittest tests.test_glucose_reachability -v  (9 tests PASS: 5 original SC2 + 3 replan-invariant + 1 new bad-ending-reachability)
- python3.6 -m unittest discover -s tests  (full suite passes — zero regression; the test updates are count-assertion + docstring + 1 new method, no other test affected)
</verification>

<success_criteria>
- test_manifest_loads_all_54_nodes: 54 nodes, start intro.preface (PASS).
- test_reachability_green_all_four_tiers: 20 endings (1T+3G+2N+14B), 4 tiers reachable, is_ok, no unreachable endings (PASS).
- test_all_new_bad_endings_reachable_from_edit_prompt (NEW): all 9 Plan-12 bad endings reachable from edit.prompt + 1a edit:unknown / 1b edit:known tags (PASS).
- Both red variants still RED from intro.preface (PASS — they assert is_ok == False).
- 0 single-Continue, 14 edit-allowed, pdh cast fix: UNCHANGED + PASS.
- Full suite passes (zero regression).
</success_criteria>

<output>
After completion, create .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-14-SUMMARY.md recording: the 3 updated assertions (node 43->54, ending 11->20, start intro.select->intro.preface), the renamed test (test_manifest_loads_all_43_nodes -> _54_nodes), the new test method (test_all_new_bad_endings_reachable_from_edit_prompt), the 9-test PASS, and the full-suite green confirmation.
</output>
