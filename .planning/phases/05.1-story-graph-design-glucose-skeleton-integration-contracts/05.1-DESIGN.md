# Phase 5.1 Design: Glucose Story Graph Skeleton + Integration Contracts

**Authoritative companion design doc for the glucose skeleton at `data/story_glucose/`.**
This is a PLANNING / REVIEW artifact (in the phase directory), NOT shipped
content. The skeleton JSON stays clean (no inline JSON comments — JSON does not
support them); this doc is the human-reviewable cross-reference for the two
gameplay-integration contracts (SC3 choice-point + SC4 edit-node), the per-node
SC5 classification, the SC1 node-type taxonomy, the SC2 reachability approach,
and the open questions surfaced for Plan 06.

**Basis:** the two Phase 5.1 research tracks —
[`05.1-RESEARCH-pathway-topology.md`](./05.1-RESEARCH-pathway-topology.md) (the
story CONTENT/topology: nodes, edges, branch points, endings, RNG shuffles) and
[`05.1-RESEARCH-graph-contracts.md`](./05.1-RESEARCH-graph-contracts.md) (the
ENGINEERING EXPRESSION + CONTRACTS: node-type taxonomy, choice-point contract,
edit-node contract, per-node classification, reachability validation). This
doc reconciles both against the ACTUAL 43-node skeleton (was 34 before the
tiered-completeness expansion; see §1) authored in Plans
01-03 (`data/story_glucose/*.json`).

---

## 1. Overview

### Locked Phase 5 decisions (HONORED — do not re-litigate)

The following Phase 5 Key Decisions (PROJECT.md rows 104-108) are LOCKED and
this design doc honors them without re-opening:

- **Soul-jump True ending (metamorphosis framing)** — PROJECT.md row 104. The
  hero is the CARBON; C14 is its tracking isotope label, not its fate. The True
  ending = the hero's ELECTRONS (the narrative "soul") are harvested into ATP via
  the ETC → ATP synthase after the RNG-weighted TCA path; the carbon BODY is shed
  as CO2 (the chrysalis stage of the metamorphosis). The narrative POV follows
  the electrons onward ("my electrons power the cell"). The carbon transforms,
  not dies. **Do NOT reintroduce** claims that the C14 carbon itself becomes
  ATP or enters oxidative phosphorylation — those are scientifically wrong.
- **C14-decay DROPPED** — PROJECT.md row 105. Radioactive decay is REMOVED as a
  bad-ending trigger. The cycle-trap-until-host-death trigger (spec.md line 18:
  "RNG stay in a cycle long enough for the host organism passed") is the SOLE
  timescale-based bad-ending. No NUBASE2020 / C14-TIME-01 citation needed.
- **Anaerobic = option (d) CHOICE-FOR-GLUCOSE + BAD-FOR-FA/ALC** — PROJECT.md
  row 97. Glucose gets a 3-ending anaerobic branch (lactic=Good/retained,
  ethanolic=Normal/CO2, crisis=Bad); FA + alcohol get the Bad-ending trigger
  (O2-depletion energy crisis). Host = mammal/human (lactic fermentation via
  LDH; LDH enters the cast in Phase 9).
- **TCA RNG 0.5/0.5** — approved TCA-RNG-WEIGHT-01. The single spec-mandated
  RNG-weighted story node (`tca.shuffle`) uses weight 0.5/0.5 for first-turn vs
  second-turn CO2 exit, grounded in the approved citrate-prochirality mechanism
  (TCA-RNG-CITRATE-PROCHIRALITY-01). Source: LIBRETEXTS-METAB-TCA.
- **Edit routing = lookup table + bad-ending fallback** — PROJECT.md row 77.
  Known edits (exact signature match in `edits.json`) route to a defined branch
  node; unknown edits route to the bad-ending pool (uniform RNG pick). No
  chemistry-correctness engine.
- **Ending distribution 1T + 3G + 2N + 5B = 11 endings** — PROJECT.md row 107.
  The user-confirmed asymmetric distribution: 1 True + 3 Good (fatty-acid
  storage, amino-acid synthesis, anaerobic lactic) + 2 Normal (aerobic CO2,
  anaerobic ethanolic) + 5 Bad (lost-connection, released-from-host,
  cycle-trap-host-death, critical-residue-break, anaerobic crisis).

### Skeleton location + the 43-node inventory

**Skeleton location:** `data/story_glucose/` (a NEW directory; the existing
`data/story/` toy fixture stays UNTOUCHED so the 198 engine/interpreter tests
pass unchanged with zero regression). The skeleton is STRUCTURE + PLACEHOLDER
TEXT, NOT cited content: `text_dramatic` / `text_teaching` are placeholder/TBD;
`claim_ids` use `PLACEHOLDER_PHASE7*` except where the first-batch approved
claims already cover the node (the 5 approved: GLY-PFK-01,
TCA-RNG-CITRATE-PROCHIRALITY-01, TCA-RNG-WEIGHT-01, CAST-PFK-PDB-01,
CAST-CSC-PDB-01).

> **Phase 5.1 tiered-completeness expansion (Plans 05.1-01..05 → EXPANSION):**
> the skeleton grew from 34 → 43 nodes per the user-approved tiered expansion
> (Decisions 1-5: FULL nodes with PDB loads for high-priority teaching points +
> NARRATIVE-only nodes to complete the cycle visibly). Net +9 nodes: +1
> `gly.pyruvate_kinase`, +1 `anaer.ldh`, +5 TCA enzymes (`tca.isocitrate_dh`,
> `tca.akg_dh`, `tca.succinyl_coa_synthetase`, `tca.fumarase`, `tca.malate_dh`),
> +3 ETC complexes (`etc.complex_ii`, `etc.complex_iii`, `etc.complex_iv`),
> -1 removed `etc.complex_ii_iii_iv`. The ending distribution is UNCHANGED
> (1T+3G+2N+5B = 11 endings); only path nodes were added. The RNG shuffle
> stays the SOLE stochastic node. See `05.1-EXPANSION-SUMMARY.md`.

**The 43-node inventory** = 40 story nodes + 3 stubs:

| # | node id | file | role |
|---|---------|------|------|
| 1 | `intro.select` | intro.json | character-select (manifest `start`) |
| 2 | `fa.stub` | intro.json | Phase 8 fatty-acid stub (returns to select) |
| 3 | `alc.stub` | intro.json | Phase 8 alcohol stub (returns to select) |
| 4-9 | `gly.start`, `gly.g6p`, `gly.pfk`, `gly.fbp_to_pyruvate`, `gly.pyruvate_kinase` *(NEW)*, `gly.pyruvate` | glycolysis.json | glycolysis path (pyruvate_kinase split out per Decision 1+3) |
| 10 | `pyr.branch` | pyruvate_branch.json | aerobic/anaerobic branch point |
| 11 | `pyr.pdh` | pyruvate_branch.json | PDH (aerobic entry, edit-allowed; PDB 6CFO WT candidate loaded — was 2OZL phospho-mimic, FIXED Plan 07) |
| 12 | `anaer.entry` | pyruvate_branch.json | anaerobic 3-ending branch |
| 13 | `anaer.ldh` *(NEW)* | pyruvate_branch.json | LDH step (FULL, PDB 5W8J candidate; per Decision 4) |
| 14-16 | `anaer.lactic`, `anaer.ethanolic`, `anaer.crisis` | pyruvate_branch.json | 3 anaerobic endings |
| 17 | `tca.entry` | tca.json | acetyl-CoA + OAA condense |
| 18 | `tca.citrate_synthase` | tca.json | citrate synthase (edit-allowed) |
| 19 | `tca.aconitase` | tca.json | aconitase (edit-allowed; PDB TBD_ACONITASE — human ACO2 has no PDB) |
| 20 | `tca.shuffle` | tca.json | RNG 0.5/0.5 shuffle (SOLE stochastic node) |
| 21-22 | `tca.co2_turn1`, `tca.co2_turn2` | tca.json | 1st/2nd-turn CO2 exit (forward → isocitrate_dh) |
| 23 | `tca.isocitrate_dh` *(NEW)* | tca.json | isocitrate DH (FULL, PDB 5GRF candidate; rate-limiting, 1st CO2) |
| 24 | `tca.akg_dh` *(NEW)* | tca.json | α-KG DH (NARRATIVE, NO PDB per Decision 2; 2nd CO2) |
| 25 | `tca.succinyl_coa_synthetase` *(NEW)* | tca.json | succinyl-CoA synthetase (NARRATIVE; GTP) |
| 26 | `tca.fumarase` *(NEW)* | tca.json | fumarase (NARRATIVE) |
| 27 | `tca.malate_dh` *(NEW)* | tca.json | malate DH (NARRATIVE; regenerates OAA, closes cycle) |
| 28 | `tca.divert_to_good` | tca.json | cataplerotic-exit branch point |
| 29-33 | `etc.entry`, `etc.complex_i`, `etc.complex_ii` *(NEW)*, `etc.complex_iii` *(NEW)*, `etc.complex_iv` *(NEW)* | etc_atp.json | ETC chain (Complexes II/III/IV split per Decision 3) |
| 34 | `etc.atp_synthase` | etc_atp.json | ATP synthase (soul-jump climax) |
| 35 | `end.true` | etc_atp.json | True ending (soul-jump climax) |
| 36-38 | `end.good.fatty_acid`, `end.good.amino_acid`, `end.normal.co2` | endings.json | 3 aerobic endings |
| 39-42 | `bad.lost_connection`, `bad.released_from_host`, `bad.cycle_trap_host_death`, `bad.critical_residue_break` | bad_endings.json | 4 bad-ending pool nodes |
| 43 | `edit.prompt` | bad_endings.json | shared structural stub (BFS reachability for edit-related bad endings) |

**3 stubs:** `fa.stub` + `alc.stub` (Phase 8 character placeholders) +
`edit.prompt` (structural stub so the BFS sees the 3 edit-related bad endings
reachable — see §5 for the runtime-vs-structural distinction).

**9 NEW nodes (tiered-completeness expansion):** 4 FULL nodes with PDB loads
(`gly.pyruvate_kinase` 7FS3, `anaer.ldh` 5W8J, `tca.isocitrate_dh` 5GRF,
`etc.complex_iv` 1OCC — all CANDIDATE/PENDING Phase 7 approval) + 5 NARRATIVE
nodes without PDB (`tca.akg_dh`, `tca.succinyl_coa_synthetase`, `tca.fumarase`,
`tca.malate_dh`, `etc.complex_ii`, `etc.complex_iii`). All use
`PLACEHOLDER_PHASE7*` claim_ids; the candidate PDB IDs are NOT in
`data/citations.json` / `data/sources.json` (separate Phase 7 approval batch).
The aconitase node uses `pdb:TBD_ACONITASE` because human mitochondrial ACO2
(UniProt Q99798) has NO PDB structure (verified via UniProt REST: 0 cross-refs);
bovine mito aconitase 1ACO (2.05 Å, classic Ogston-hypothesis teaching
structure) is a candidate for Phase 7 human review.

### Citation-gate coexistence (PLACEHOLDER_PHASE7 + pre-ship gate)

The skeleton uses `PLACEHOLDER_PHASE7*` claim_ids for all nodes whose science
is NOT in the first-batch approved claims. The citation gate
(`tools/check_citations.py --story <path> --registry <path>`) is a **pre-ship,
manually-invoked CLI tool** — it is NOT auto-run on `data/story_glucose/`
during Phase 5.1 (there is no CI / Makefile auto-runner; the gate tests use
their own fixtures under `tests/fixtures/`). The existing toy fixture
(`data/story/intro.json`) uses `placeholder-intro` etc. (NOT in the registry)
and the gate is not broken because it is never run on `data/story/`.

- The 5 APPROVED first-batch claim_ids (GLY-PFK-01, CAST-PFK-PDB-01,
  CAST-CSC-PDB-01, TCA-RNG-CITRATE-PROCHIRALITY-01, TCA-RNG-WEIGHT-01) ARE in
  the registry (`data/citations.json`, all `approval_status: "approved"`) and
  WOULD PASS the gate if it were run on the skeleton.
- Only the `PLACEHOLDER_PHASE7*` nodes would fail (expected — the skeleton is a
  design artifact, NOT shippable content). **Phase 7** replaces
  `PLACEHOLDER_PHASE7*` with real approved claims and runs the gate as a
  release gate.

This coexistence is the intended design: the skeleton is a STRUCTURE artifact
that the gate correctly flags as not-yet-shippable; Phase 7 fills in the cited
content and the gate goes green.

---

## 2. Node-type taxonomy (SC1)

### The 5 SC1 types as a DERIVED property + a tags declaration (ZERO schema change)

SC1 requires every node TYPED as one of `story` / `choice` / `rng-shuffle` /
`edit-allowed` / `ending`. Reading the actual `Node` class
(`c14/story/model.py:239-329`), the model fields are: `id`, `text_dramatic`,
`text_teaching`, `claim_ids[]`, `choices[]`, `on_enter[]`, `is_ending`
(None|"true"|"good"|"normal"|"bad"), `tags[]`, `on_enter_divert`. There is
**no** `node_type` field.

The 5 SC1 types map to existing fields as follows (research Q1 Option c
RECOMMENDED):

| SC1 type | Existing field(s) that express it | Source (file:line) | Derivable? |
|----------|-----------------------------------|--------------------|------------|
| `ending` | `Node.is_ending is not None` | `model.py:269`, `:311-314` | YES — derived |
| `rng-shuffle` | any `Choice.weight is not None` among `Node.choices` | `model.py:189`, `interpreter.py:61` | YES — derived |
| `choice` | `len(Node.choices) > 0` | `model.py:267`, `interpreter.py:60` | YES — derived |
| `edit-allowed` | NONE — declared via `tags` entry `edit:enzyme:<id>` | (see §5) | NO — must be declared |
| `story` | default: not ending, no choices, no edit declaration | `model.py:259-271` | YES — derived (the "none of the above" case) |

### The 5 types are a SET of roles, NOT a single enum

A node can hold several of these at once. The canonical example is the
ARCHITECTURE.md TCA shuffle node (`tca.shuffle` in the skeleton) which is
SIMULTANEOUSLY `rng-shuffle` (it has 2 weighted choices) AND `choice` (it has
non-weighted choices — an edit:offer + a cond-gated cycle-trap) AND
`edit-allowed` (it carries `edit:enzyme:tca.aconitase` in its tags). A
single-valued `node_type` field CANNOT represent this reality — it would
force a false classification. **A single-valued `node_type` field is REJECTED**
(it breaks the engine's mixed weighted/non-weighted choices on one node,
`interpreter.py:60-73`).

### The classifier helper (documentation reference — NOT new runtime code)

The interpreter already dispatches on these exact fields; the following
pure-Python helper is a DESIGN-DOC reference that documents the derivation:

```python
def node_types(node):
    """Return the SET of structural roles a node plays (SC1 taxonomy).

    Documentation reference only -- the interpreter already dispatches on
    these exact fields. A node can hold several roles at once (e.g. the TCA
    shuffle node is rng-shuffle + choice + edit-allowed).
    """
    types = []
    if node.is_ending is not None:                          # model.py:269
        types.append("ending")
    if any(c.weight is not None for c in node.choices):     # model.py:189
        types.append("rng-shuffle")
    if len(node.choices) > 0:                               # model.py:267
        types.append("choice")
    if any(t.startswith("edit:enzyme:") for t in node.tags):  # Q3 declaration
        types.append("edit-allowed")
    if not types:                                           # none of the above
        types.append("story")
    return types
```

**Why this is correct and backward-compatible:**
- Uses ONLY existing fields. `Node.from_dict` (`model.py:273-293`) already
  parses `is_ending`, `choices` (as `Choice` objects), and `tags` via `.get`.
  No `from_dict`/`to_dict` change needed.
- Matches the interpreter's actual dispatch: `pick_choice` splits weighted vs
  non-weighted on the SAME node (`interpreter.py:60-73`); `enter_node` checks
  `is_ending` (`interpreter.py:97`); the engine presents `node.choices` as
  `eligible_choices` (`engine.py:193`).
- Multi-role nodes are first-class (the TCA shuffle node →
  `["rng-shuffle", "choice", "edit-allowed"]`).

### Structural type (SC1) vs SC5 class — ORTHOGONAL axes

- **Structural type (SC1, this section):** the MECHANISM a node uses —
  `ending` / `rng-shuffle` / `choice` / `edit-allowed` / `story`. A node has a
  SET. Derived from fields + the edit tag.
- **SC5 classification (§6):** the GAMEPLAY role — `MC-choice` / `edit-allowed`
  / `both` / `neither`. A node has ONE. A per-node DESIGN decision.

They correlate but are NOT identical: an `rng-shuffle` node (structural) is
`MC-choice` (gameplay — the RNG is the "multiple-choice" agent). A `story`
node with a single forced "Continue" choice (structural type = `choice`
because `len(choices)>0`) is `neither` (gameplay role = narrative progression,
NOT a branching choice). The SC5 class is a gameplay design decision per node,
informed by but NOT mechanically equal to the structural type set (see §6 for
the full derivation rule + the per-node table).

### on_enter_divert DORMANCY (affects the `story` type)

`Node.on_enter_divert` ("auto-advance target node id", `model.py:255`) is
defined, round-tripped by `from_dict`/`to_dict` (`model.py:292`, `:307`), and
tested for default-None + round-trip (`tests/test_model.py:64`, `:129`) — but
it is **NEVER referenced by `interpreter.py` or `engine.py`** (verified via
grep: `on_enter_divert` appears only in `model.py` + `test_model.py`).
Consequence: a non-ending node with NO `choices` is a **dead end** —
`engine.choose` calls `pick_choice` which returns `[]` (empty eligible list,
`interpreter.py:60`), then `index >= len(pick)` raises `IndexError`
(`engine.py:124-127`).

**Skeleton resolution:** every non-ending node has ≥1 `choice` (a single-option
"Continue" choice whose `goto` is the next node — `choices: [{label: "Continue",
goto: "next.node"}]`). This requires ZERO engine changes and keeps
`on_enter_divert` reserved for a future engine extension. The BFS
(`check_reachability`) follows `choice.goto` edges ONLY (not `on_enter_divert`),
so using choices for forward paths is also the reachability-critical design
(see §7). `on_enter_divert` dormancy is flagged as an open question (§9 OQ8).

---

## 3. File split + manifest

### The 7-file split (per ARCHITECTURE.md line 147 per-pathway-file rationale)

ARCHITECTURE.md line 147 rationale: "One JSON file per pathway/scene. Reasons:
(a) per-claim citation review is line-by-line against a source — small
diffable files make approval tractable; (b) parallel authoring of pathways
without merge conflicts; (c) the validator can load the bundle via
`manifest.json`."

| file | nodes | rationale |
|------|-------|-----------|
| `intro.json` | `intro.select`, `fa.stub`, `alc.stub` (3) | Character-select screen; Phase 8 stubs |
| `glycolysis.json` | `gly.start`, `gly.g6p`, `gly.pfk`, `gly.fbp_to_pyruvate`, `gly.pyruvate_kinase` *(NEW)*, `gly.pyruvate` (6) | Glycolysis path; PFK = approved-claim control point; pyruvate_kinase split out (Decision 1+3) |
| `pyruvate_branch.json` | `pyr.branch`, `pyr.pdh`, `anaer.entry`, `anaer.ldh` *(NEW)*, `anaer.lactic`, `anaer.ethanolic`, `anaer.crisis` (7) | Aerobic/anaerobic branch + PDH + LDH step (Decision 4) + 3-ending anaerobic branch |
| `tca.json` | `tca.entry`, `tca.citrate_synthase`, `tca.aconitase`, `tca.shuffle`, `tca.co2_turn1`, `tca.co2_turn2`, `tca.isocitrate_dh` *(NEW)*, `tca.akg_dh` *(NEW)*, `tca.succinyl_coa_synthetase` *(NEW)*, `tca.fumarase` *(NEW)*, `tca.malate_dh` *(NEW)*, `tca.divert_to_good` (12) | TCA cycle incl. RNG shuffle + 5 enzyme nodes (Decisions 1+2+5) |
| `etc_atp.json` | `etc.entry`, `etc.complex_i`, `etc.complex_ii` *(NEW)*, `etc.complex_iii` *(NEW)*, `etc.complex_iv` *(NEW)*, `etc.atp_synthase`, `end.true` (7) | ETC chain (Complexes II/III/IV split, Decision 3) + ATP synthase soul-jump arc + True ending |
| `endings.json` | `end.good.fatty_acid`, `end.good.amino_acid`, `end.normal.co2` (3) | Aerobic Good + Normal endings (diversions off TCA/ETC) |
| `bad_endings.json` | `bad.lost_connection`, `bad.released_from_host`, `bad.cycle_trap_host_death`, `bad.critical_residue_break`, `edit.prompt` (5) | Bad-ending pool + edit.prompt structural stub |

**Total: 43 nodes across 7 files.** (Was 34 before the tiered-completeness
expansion added 9 net nodes: +1 `gly.pyruvate_kinase`, +1 `anaer.ldh`, +5 TCA
enzymes, +3 ETC complexes, -1 removed `etc.complex_ii_iii_iv`.)

### manifest.json conventions

```json
{
  "version": 1,
  "default_seed": 0,
  "start": "intro.select",
  "files": [
    "intro.json", "glycolysis.json", "pyruvate_branch.json",
    "tca.json", "etc_atp.json", "endings.json", "bad_endings.json"
  ]
}
```

- `version` (int) — bundle format version (`graph.py:64`).
- `default_seed` (int) — bundle-level default RNG seed for demo mode
  (`graph.py:65`); `engine.start(seed=None)` records the auto-picked seed,
  `engine.start(seed=int)` uses it (`engine.py:92-104`).
- `start` (string) — the start node id; `StoryGraph.start_node()` returns it
  (`graph.py:108-111`); `engine.start` enters it (`engine.py:102-104`). The
  manifest MUST set `start` explicitly (`graph.py:67`); if absent,
  `start_node()` returns None and `engine.start` fails with `KeyError` on
  `get_node(None)`.
- `files` (list of strings) — JSON files in the same dir; each has
  `{"nodes": {id: {...}}}`; merged in order; **duplicate node id across files
  raises `ValueError`** (`graph.py:76-80`).

> **Note:** `manifest.json` is authored by Plan 05.1-04 (Wave 2, parallel with
> this Plan 05.1-05). It is NOT part of this design doc's deliverable; the
> conventions above are the cross-reference for the human reviewer.

### The loader

`StoryGraph.load(dir)` (`c14/story/graph.py:50-88`) reads `manifest.json`,
merges the listed files' `nodes` dicts, injects the JSON object key as the
node's `id` field before `Node.from_dict` (`graph.py:85-87`), and raises
`ValueError` on duplicate node ids (`graph.py:76-80`). The loader is
CWD-independent (explicit `story_dir` arg); `all_nodes()` returns the
`{id: Node}` dict (NOT a list) (`graph.py:99-106`).

### Location split (story JSON vs edits/cast JSON)

There is a real split between two location conventions:

- **Story JSON:** repo-root `data/story_glucose/` (dev-time, per
  ARCHITECTURE.md + the skeleton authored in Plans 01-03) + a bundled
  `c14/data/story_glucose/` copy at runtime (created at install/bundle time,
  Phase 6+). `StoryGraph.load(story_dir)` takes an explicit dir so both work.
  The existing toy fixture stays at `data/story/` (untouched).
- **edits.json / cast.json:** `c14/data/` (tooling-bound). The existing
  placeholder is at `c14/data/edits.json` (verified); the SC5 coverage tool
  `tools/check_edit_coverage.py:41-42` hardcodes
  `EDITS_PATH = os.path.join(REPO_ROOT, "c14", "data", "edits.json")` and
  `CAST_PATH = os.path.join(REPO_ROOT, "c14", "data", "cast.json")`.
  ARCHITECTURE.md says `data/edits.json` (repo-root), but **the tooling-bound
  path wins** — do NOT move `edits.json`/`cast.json` to `data/` without
  updating `tools/check_edit_coverage.py:41-42`. Treat ARCHITECTURE.md's
  `data/edits.json` as aspirational/outdated (see §9 OQ9).

---

## 4. Choice-point contract (SC3)

### The 5-facet spec per choice node

The "text MC → graph edge" mechanism is fully implemented in `interpreter.py`
+ `engine.py`. The contract makes it an explicit, reviewable spec. For each
choice/rng-shuffle node, the design doc captures these 5 facets (research Q2
table):

| Facet | What it is | Existing mechanism (file:line) |
|-------|------------|-------------------------------|
| **(a) Player-facing options** | The list of labels the UI shows the player | `Choice.label` (`model.py:171`); engine returns `node.choices` as `TurnResult.eligible_choices` (`engine.py:193`) |
| **(b) The edge each selects** | The target node id advancing to | `Choice.goto` (`model.py:172`); `engine.choose` advances via `self._enter(choice.goto)` (`engine.py:133`) |
| **(c) MolActions fired on traversal** | What fires when the player picks — in order | `choice.effects` applied to GameState FIRST (`engine.py:132` → `interpreter.apply_effects`, `interpreter.py:137-154`), THEN the target node's `on_enter` MolActions emitted (`engine.py:133` → `_enter` → `interpreter.enter_node`, `interpreter.py:75-99`; dispatched per-action to sink at `engine.py:187-189`) |
| **(d) How `cond` gates options** | A choice with a falsy `cond` is hidden (not eligible) | `interpreter._cond` (`interpreter.py:101-135`): `None`→always eligible; else `eval(cond, {"__builtins__":{}}, {flags, char, counters, visits})`; ANY exception → `False` (fail-safe hide) |
| **(e) How `weight` makes an RNG branch** | A weighted choice is RNG-picked (not player-picked) | `interpreter.pick_choice` (`interpreter.py:43-73`): eligible = cond-passing; if ANY eligible has `weight` AND total>0 → single RNG pick (`rng.random()*total` + cumulative sum); else → full eligible list for the player to pick by index |

### Key distinctions the contract MUST state

1. **`choice.effects` are STATE mutations, NOT MolActions.** `effects` is a dict
   with optional `"set"` (flags) and `"incr"` (counters) sub-dicts
   (`interpreter.py:137-154`). It mutates `GameState`; it does NOT emit
   `MolAction`s. The MolActions that change the molecular scene come from the
   **target node's `on_enter`**. Do not conflate them.
2. **Order of operations on a choice traversal:** (i) `pick_choice` resolves
   (RNG or player-index) → one `Choice`; (ii) `apply_effects(choice)` mutates
   state (`engine.py:132`); (iii) `_enter(choice.goto)` emits the target's
   `on_enter` MolActions + records the visit + detects ending
   (`engine.py:133` → `interpreter.enter_node`). Authors put state changes in
   `choice.effects` and scene changes in `target.on_enter`.
3. **`cond` vs `weight` are orthogonal.** `cond` filters ELIGIBILITY (a
   falsy-cond choice is hidden from BOTH the player and the RNG). `weight`
   decides WHO picks among the eligible (RNG if any weighted, else player). A
   choice can have both (a weighted, conditionally-gated branch).
4. **`on_enter_divert` is NOT followed** (see §2 dormancy). Edges are
   `choice.goto` ONLY. Do not author auto-advance nodes expecting
   `on_enter_divert` to work.
5. **`mc:observe` choices follow the SAME contract as other choices** (the
   SC3 mechanism is unchanged). The `mc:observe` "Observe" option added to
   converted nodes (Plans 07-09 Continue-to-MC directive) has the SAME
   `choice.goto` destination as the corresponding "Continue" choice —
   same-destination, different-experience (the player examines the structure
   before advancing). It is a regular non-weighted, non-cond-gated choice;
   no new mechanism.

### Per-choice-node table (SC3 — one row per choice/rng-shuffle node)

The skeleton has the following choice/rng-shuffle nodes. For each, the table
specifies the 5 facets (a-e), the real pathway branch point mapped, and the
SC5 class. The node ids + labels are the ACTUAL values from
`data/story_glucose/*.json`.

#### Choice nodes (MC-choice — real branching, no edit:enzyme tag)

| Node id | Structural types | (a) Player options | (b) Edge per option | (c) Traversal fires | (d) cond gating | (e) weight | Pathway branch point mapped | SC5 class |
|---------|------------------|--------------------|---------------------|----------------------|----------------|-------------|-----------------------------|-----------|
| `intro.select` | `choice` (3 non-weighted) | "Glucose — enter the glycolysis path"; "Fatty acid (coming in Phase 8)"; "Alcohol (coming in Phase 8)" | Glucose → `gly.start`; FA → `fa.stub`; Alc → `alc.stub` | effects `{}` (none — controller sets `state.character` from the chosen index, see §9 OQ7); THEN target `on_enter` (each: `hide_all`) | none (all 3 always eligible) | none — player picks by index | Character selection (spec.md line 19: "chose the character to start with") — a meta-game branch, not a pathway branch point | MC-choice |
| `pyr.branch` | `choice` (2 non-weighted, both cond-gated) | "Proceed aerobically (O2 available) -> acetyl-CoA -> TCA"; "The air runs out — ferment (anaerobic)" | aerobic → `pyr.pdh`; anaerobic → `anaer.entry` | aerobic: effects `{}` then `pyr.pdh.on_enter` (`hide_all`); anaerobic: effects `{set: {anaerobic: true}}` then `anaer.entry.on_enter` (`hide_all`) | aerobic: `not flags.host_o2_low`; anaerobic: `flags.host_o2_low` | none — player picks by index (BUT only the eligible option is shown — the cond makes it a forced pick at runtime) | **Pyruvate fate depends on O2 presence** (real — O2 determines pyruvate → acetyl-CoA [aerobic, via PDH] vs → lactate/ethanol [anaerobic, via LDH / pyruvate decarboxylase + ADH]) | MC-choice |
| `anaer.entry` | `choice` (3 non-weighted) | "Lactic fermentation (stay as lactate)"; "Ethanolic fermentation (CO2 + ethanol)"; "The energy runs out — crisis" | lactic → `anaer.ldh` (NEW LDH step per Decision 4); ethanolic → `anaer.ethanolic`; crisis → `anaer.crisis` | lactic: effects `{}` then `anaer.ldh.on_enter` (`hide_all` + `load pdb:5W8J`); ethanolic/crisis: effects `{}` then target `on_enter` (`hide_all`); ethanolic + crisis are endings; lactic's target `anaer.ldh` is a FULL non-ending node whose Continue choice → `anaer.lactic` (Good ending) | none (all 3 always eligible) | none — player picks by index | **3-ending anaerobic branch** (option (d): lactic=Good [C retained via LDH — now a playable LDH step before the ending], ethanolic=Normal [CO2 via pyruvate decarboxylase — HOST-TENSION FLAG §9 OQ1], crisis=Bad [energy crisis → host death]) | MC-choice |
| `tca.divert_to_good` | `choice` (3 non-weighted) | "Follow the soul to the ETC"; "Divert to fatty-acid storage (your carbon body lives on)"; "Divert to amino-acid synthesis (become a building block)" | ETC → `etc.entry`; FA → `end.good.fatty_acid`; AA → `end.good.amino_acid` | each: effects `{}` then target `on_enter` (`hide_all`); FA + AA are endings | none (all 3 always eligible) | none — player picks by index | **Acetyl-CoA / TCA-intermediate cataplerotic exit** (citrate → cytosol → fatty-acid synthesis; OAA → aspartate; α-KG → glutamate — carbon diverted pre-oxidation, matches Good semantics) | MC-choice |
| `etc.entry` | `choice` (2 non-weighted) | "Follow the soul into the electron transport chain"; "Let go — drift away as CO2 (the unremarkable exit)" | ETC → `etc.complex_i`; let-go → `end.normal.co2` | each: effects `{}` then target `on_enter` (`hide_all`); let-go target is an ending | none (both always eligible) | none — player picks by index | **ETC entry vs CO2 exit** (the narrative "let go" branch point — the Normal-ending narrative tier; NARRATIVE-FRAMING FLAG §9 OQ2) | MC-choice |
| `edit.prompt` | `choice` (3 non-weighted, structural) | "Break a critical residue (structural path)"; "Make an unrecognized edit (structural path A)"; "Make an unrecognized edit (structural path B)" | critical → `bad.critical_residue_break`; unknown-A → `bad.lost_connection`; unknown-B → `bad.released_from_host` | each: effects `{}` then target `on_enter` (`hide_all`); all 3 targets are bad endings | none (all 3 always eligible) | none — STRUCTURAL placeholder (at runtime the EditRouter routes via `apply_player_edit`, BYPASSING these choices; the stub exists so the BFS sees the bad endings reachable — see §5) | (structural stub — NOT a real pathway branch point; the edit-routing contract owns the real branching) | MC-choice (placeholder stub) |

#### RNG-shuffle node (rng-shuffle + choice + edit-allowed — multi-role)

| Node id | Structural types | (a) Player options | (b) Edge per option | (c) Traversal fires | (d) cond gating | (e) weight | Pathway branch point mapped | SC5 class |
|---------|------------------|--------------------|---------------------|----------------------|----------------|-------------|-----------------------------|-----------|
| `tca.shuffle` | `rng-shuffle`, `choice`, `edit-allowed` (multi-role — the ARCHITECTURE.md Pattern 2 canonical node) | "The wheel spins — first-turn CO2 exit"; "The wheel spins — second-turn CO2 exit"; "Try to edit aconitase"; "The cycle has spun too long — the host fades" | turn1 → `tca.co2_turn1` (weight 0.5); turn2 → `tca.co2_turn2` (weight 0.5); edit → `edit.prompt` (tags `edit:offer`); cycle-trap → `bad.cycle_trap_host_death` | turn1/turn2: no effects then target `on_enter` (`hide_all`); edit: no effects then `edit.prompt.on_enter`; cycle-trap: no effects then bad-ending `on_enter` | turn1/turn2/edit: none (always eligible); cycle-trap: `visits.get('tca.shuffle', 0) > 5` (visit-count cond gate — Pitfall 8 cycle-cap, N=5 placeholder §9 OQ3) | turn1=0.5, turn2=0.5 (TCA-RNG-WEIGHT-01 approved 0.5/0.5); edit + cycle-trap = none (non-weighted — but when ANY eligible has weight AND total>0, the RNG picks among ALL eligible via `rng.random()*total`; the non-weighted edit/cycle-trap choices are NOT separately player-pickable while a weighted choice is eligible) | **Citrate prochirality RNG shuffle** (the spec-mandated stochastic step — the Ogston hypothesis: the two acetyl-CoA carbons are distinguishable on the first TCA turn; 0.5/0.5 first-turn vs second-turn CO2 exit, APPROVED TCA-RNG-WEIGHT-01 + TCA-RNG-CITRATE-PROCHIRALITY-01) | both (rng-shuffle + choice + edit-allowed) |

> **Note on `tca.shuffle` RNG mechanics:** when the player triggers the choice
> resolution, `pick_choice` (`interpreter.py:43-73`) sees 4 eligible choices:
> 2 weighted (turn1=0.5, turn2=0.5, total=1.0) + 2 non-weighted (edit:offer +
> cycle-trap, if their conds pass). Because ANY eligible has weight AND total>0,
> the RNG picks ONE among ALL eligible via `rng.random()*total` + cumulative sum.
> The non-weighted choices (edit:offer, cycle-trap) are effectively
> player-triggered but RNG-decided WHEN a weighted choice is present. The
> cycle-trap choice has `cond: "visits.get('tca.shuffle', 0) > 5"` — it only
> becomes eligible after 5+ visits; before that, the RNG picks turn1/turn2. The
> edit:offer choice is always eligible (no cond) but the RNG may not pick it
> (it has no weight, so it's not in the weighted cumulative sum — it's only
> pickable if the player explicitly chooses it by index when NO weighted
> choice is eligible, which never happens here since turn1+turn2 are always
> eligible). **Phase 6 UI design must clarify how the player triggers the
> edit:offer vs the RNG shuffle** — this is a UI/UX open question for Plan 06.

#### Edit-allowed nodes' choices (edit-allowed + both — Continue/Observe + edit:offer)

The skeleton has **14 edit-allowed nodes** (see §5 for the full per-node
table with enzyme_id + DIS-*-cand + reverse-mutation signatures). Their
choice-point facets follow two patterns:

- **5 `edit-allowed`-class nodes** (`gly.pfk`, `pyr.pdh`, `tca.aconitase`,
  `tca.citrate_synthase`, `etc.complex_i`) — 2 choices each: 1 non-edit
  (Continue) + 1 `edit:offer`. SC5 class = `edit-allowed` (only 1 non-edit
  choice — does NOT meet the "both" threshold of ≥2 non-edit choices).
- **9 `both`-class nodes** (`gly.pyruvate_kinase`, `tca.isocitrate_dh`,
  `tca.succinyl_coa_synthetase`, `tca.fumarase`, `tca.malate_dh`,
  `tca.shuffle`, `etc.complex_ii`, `etc.complex_iii`, `etc.complex_iv`) —
  3-4 choices each: ≥2 non-edit (Continue + Observe, or 2 weighted + loop)
  + 1 `edit:offer`. SC5 class = `both`.

Representative facets (the 5 `edit-allowed`-class nodes — the 9 `both`-class
nodes follow the same pattern with an added `mc:observe` Observe choice; full
detail in §5):

| Node id | (a) Player options | (b) Edge per option | (c) Traversal fires | (d) cond | (e) weight | SC5 class |
|---------|--------------------|---------------------|----------------------|----------|-------------|-----------|
| `gly.pfk` | "Continue glycolysis"; "Try to edit PFK's active site" | continue → `gly.fbp_to_pyruvate`; edit → `edit.prompt` (tags `edit:offer`) | continue: effects `{}` then `gly.fbp_to_pyruvate.on_enter` (`hide_all`); edit: effects `{}` then `edit.prompt.on_enter` | none (both always eligible) | none | edit-allowed |
| `pyr.pdh` | "Continue into the TCA cycle"; "Try to edit the PDH complex" | continue → `tca.entry`; edit → `edit.prompt` (tags `edit:offer`) | continue: effects `{}` then `tca.entry.on_enter`; edit: effects `{}` then `edit.prompt.on_enter` | none | none | edit-allowed |
| `tca.citrate_synthase` | "Continue to aconitase"; "Try to edit citrate synthase" | continue → `tca.aconitase`; edit → `edit.prompt` (tags `edit:offer`) | continue: effects `{}` then `tca.aconitase.on_enter`; edit: effects `{}` then `edit.prompt.on_enter` | none | none | edit-allowed |
| `tca.aconitase` | "Continue to the shuffle"; "Try to edit aconitase" | continue → `tca.shuffle`; edit → `edit.prompt` (tags `edit:offer`) | continue: effects `{}` then `tca.shuffle.on_enter`; edit: effects `{}` then `edit.prompt.on_enter` | none | none | edit-allowed |
| `etc.complex_i` | "Continue down the chain"; "Try to edit Complex I" | continue → `etc.complex_ii`; edit → `edit.prompt` (tags `edit:offer`) | continue: effects `{}` then `etc.complex_ii.on_enter` (`hide_all`); edit: effects `{}` then `edit.prompt.on_enter` | none | none | edit-allowed |

---

## 5. Edit-node contract (SC4)

### Narrative frame: restoration (decided 2026-08-19, PROJECT.md row 109)

The edit mechanic has TWO uses under the "hero as restorer" narrative frame
(decided 2026-08-19; see PROJECT.md Key Decisions row 109 + spec.md Description
NOTE). The skeleton structure is UNCHANGED — this is a content-layer frame
implemented in Phase 7:

1. **Diseased-state display (on_enter pre-edit):** when the player enters an
   edit-allowed node, the enzyme is shown in its DISEASED (mutated) state. The
   `on_enter` MolActions chain: `load pdb:X` → `edit(point_mutation, WT→disease)`
   via the Phase 4 `apply_edit` helper. The player sees the broken enzyme.
   (Geometry caveat: `cmd.alter` changes residue identity but does not repack
   side chains — the structure has correct sequence but approximate coordinates;
   `text_teaching` notes this is a simplified representation.)
2. **Restoration attempt (player edit via EditRouter):** the player's edit is the
   hero's attempt to fix the enzyme. The EditRouter routing maps to the narrative:
   - **Known edit (correct reverse-mutation, matches `edits.json` signature)**
     → known-edit branch node → "you restored the enzyme!"
   - **Unknown edit (wrong fix, no signature match)** → bad-ending pool
     → "you made it worse" (bad.lost_connection / bad.released_from_host)
   - **Break a critical residue** → `bad.critical_residue_break`

Not all 14 edit-allowed nodes need a full restoration subplot — 1-2 key enzymes
(e.g. PFK, aconitase — both already have approved claims) can carry the arc while
the rest stay "exploration" editing. Each disease mutation requires a real source
(what mutation? what disease?) — additional per-claim approval in Phase 7.

> **Phase 7 CONTENT boundary (the skeleton carries ONLY structure):** the
> skeleton carries the STRUCTURE that makes the restoration narrative
> possible — the `edit:enzyme:<id>` tags (binding), the `edit:offer` choices
> (UI trigger), and the `DIS-*-cand` claim_id REFERENCES (CANDIDATE, not
> approved). The skeleton does NOT carry the CONTENT: the `edits.json`
> entries (the reverse-mutation signatures + known-edit branch nodes), the
> `text_dramatic` / `text_teaching` text, and the `on_enter` pre-edit
> MolActions (the diseased-state `apply_edit` calls) are ALL Phase 7
> content. The reverse-mutation signatures in the table below are
> DOCUMENTATION ONLY (NAME ONLY — they describe what Phase 7 will author,
> they are NOT written into `edits.json` here). The 12 `DIS-*-cand`
> claim_ids are CANDIDATE pending Phase 7 per-claim approval.

### The 5-step data-flow (player edit → routed node → applied edit)

The contract documents this 5-step data-flow (verified against
`engine.py:135-161` + `edit_router.py:122-149` + the fixture
`tests/fixtures/edit_routing/`):

```
1. Player triggers an edit in the UI (pick residue, choose mutation).
2. Controller builds an EditIntent(op, target, args) — op ∈ {point_mutation,
   substrate_edit, protonation_change} (model.py:91-93).
3. Controller reads the CURRENT story node's enzyme_id binding (the
   edit:enzyme:<id> tag — see "enzyme_id binding mechanism" below) and calls
   engine.apply_player_edit(intent, enzyme_id)  (engine.py:135).
4. engine.apply_player_edit  (engine.py:135-161):
   a. node_id = edit_router.route(intent, enzyme_id, rng)  (edit_router.py:122-149)
      - KNOWN edit (exact signature match in enzymes[enzyme_id].edits)
        → branch_node  (the defined story node for this known edit)
      - UNKNOWN edit (no match, or enzyme_id not in table)
        → bad-ending pool node  (per-enzyme override else global fallback;
          uniform rng.weighted_pick(pool, [1.0]*len(pool)))
   b. state.add_edit({enzyme: enzyme_id, intent: edit_intent.to_dict(),
      route: node_id})  (records in edits_history)
   c. return self._enter(node_id)  (reuses existing _enter: on_enter MolActions
      flow to the existing molaction_sink; engine.py:163-194)
5. The routed node's on_enter applies the edit:
   - branch node on_enter has MolAction(op="edit", target=...,
     args={edit_type, sele, new_resn})
     (see fixture: tests/fixtures/edit_routing/story.json:12)
   - molops dispatches op="edit" → edit_ops (04-05) → cmd.alter + cmd.sort +
     cmd.rebuild
   - bad-ending pool nodes are ENDINGS (is_ending="bad") — on_enter is just
     scene reset (hide_all)
```

### The 5 facets per edit-allowed node

| Facet | What it is | Existing mechanism (file:line) |
|-------|------------|-------------------------------|
| **(a) Node declares edit-allowed** | The node says "edits are offered here" | `tags` entry `edit:enzyme:<id>` (see binding mechanism below) |
| **(b) enzyme_id binding** | The EditRouter lookup bucket the node binds to | `tags` entry `edit:enzyme:<id>` — the `<id>` IS the enzyme_id |
| **(c) EditIntent per player action** | The `op`/`target`/`args` each player-edit-action produces; must match `edits.json` `signature` after `EditIntent.signature()` normalization | `EditIntent.signature()` (`model.py:112-126`): `{op, target.strip().lower(), {k: _norm_val(v)}}`; compared with `==` against `edits.json` `signature` (`edit_router.py:142`) |
| **(d) Routing outcome** | known → `branch_node`; unknown → bad-ending pool node | `EditRouter.route` (`edit_router.py:122-149`): exact dict-equality match → `entry["branch_node"]`; else `rng.weighted_pick(pool, [1.0]*len(pool))` |
| **(e) Routed node applies the edit** | The branch node's `on_enter` MolAction(op="edit") executes via molops→edit_ops | `engine._enter` (`engine.py:163-194`); fixture branch node (`tests/fixtures/edit_routing/story.json:9-15`): `on_enter: [{"op":"edit", "target":"...", "args":{"edit_type":"point_mutation","sele":"...","new_resn":"ALA"}}]` |

### enzyme_id binding mechanism (RECOMMENDED: tags entry `edit:enzyme:<id>`)

**Option 1 (RECOMMENDED — ADOPTED in the skeleton): a `tags` entry
`edit:enzyme:<enzyme_id>`.** Zero schema change. The existing `tags` list
(`model.py:270`) is the documented home for "game-side hook tags"
(`model.py:253`). One tag carries BOTH the edit-allowed declaration AND the
enzyme_id binding:

```json
"tags": ["stage:glycolysis", "edit:enzyme:gly.pfk"]
```

The controller extracts it:
```python
enzyme_id = next((t.split(":", 2)[2] for t in node.tags
                  if t.startswith("edit:enzyme:")), None)
```
If `None`, the node is not edit-allowed. Backward-compatible: `Node.from_dict`
already parses `tags` via `.get` (`model.py:291`); old nodes without the tag
load fine. No `to_dict` change (tags already round-trips, `model.py:306`).
No gate impact (see §8).

**Option 2 (ALTERNATIVE — flagged, NOT adopted): a new optional
`Node.edit_enzyme_id` field** (None when not edit-allowed). Cleaner/more
explicit; the binding is a first-class field. Backward-compatible with
`from_dict` if added as `d.get("edit_enzyme_id")`. REQUIRES a `to_dict` update
(`model.py:295-308`) for round-trip. A 4-line additive change. Flagged as an
open question (§9 OQ10) but NOT adopted (the tags approach needs zero
changes).

**Node-level vs choice-level tags:** the skeleton uses a NODE-level
`edit:enzyme:<id>` tag (the enzyme_id binding is a node property — the whole
node binds to one enzyme context for the EditRouter) AND a CHOICE-level
`edit:offer` tag (marks which choice launches the edit UI). They coexist:
the node tag declares "this node's enzyme context"; the choice tag declares
"this choice opens the edit UI". (ARCHITECTURE.md puts `edit:offer` on a
choice — the skeleton follows this AND adds the node-level binding.)

### Per-edit-allowed-node table (SC4 — 14 rows)

The skeleton has **14 edit-allowed nodes** (nodes carrying an
`edit:enzyme:<id>` tag): 12 disease-mutant (each with a `DIS-*-cand`
CANDIDATE claim) + 1 structural (`tca.citrate_synthase`, `edit:structural`,
NO disease mutant) + 1 shared (`tca.shuffle`, `edit:enzyme:tca.aconitase`,
secondary aconitase edit point, no DIS-*-cand of its own). For each, the
table documents the enzyme_id binding, the DIS-*-cand candidate claim, the
reverse-mutation signature (the "correct fix" — NAME ONLY, NOT the
`edits.json` entry; the actual entry is Phase 7 content), the known-edit
branch route, and the unknown-edit bad-pool fallback.

| Node id | enzyme_id | Real enzyme + PDB | DIS-*-cand (CANDIDATE) | Reverse-mutation (the "correct fix" — NAME ONLY) | Known-edit branch route | Unknown-edit fallback | SC5 class |
|---------|-----------|-------------------|------------------------|--------------------------------------------------|-------------------------|------------------------|-----------|
| `gly.pfk` | `gly.pfk` | PFK-1; PDB **4PFK** (APPROVED: CAST-PFK-PDB-01, 2.40 Å — bacterial cast, residue-mapping caveat) | **DIS-PFKM-01-cand** (PFKM G209D, GSD VII/Tarui) | PFKM G209D→D209G (default; alt W686C→C686W, R39L→L39R — §9 OQ-DM5) | `edit.prompt` → EditRouter → branch node (Phase 7) | `bad.lost_connection` / `bad.released_from_host` | edit-allowed |
| `gly.pyruvate_kinase` | `gly.pyruvate_kinase` | pyruvate kinase (PKLR); PDB **7FS3** (CANDIDATE, human WT 1.66 Å) | **DIS-PKLR-01-cand** (PKLR R479H, CNSHA2 — the Amish founder; NOT R479W) | PKLR R479H→H479R | `edit.prompt` → EditRouter → branch node (Phase 7) | `bad.lost_connection` / `bad.released_from_host` | both |
| `pyr.pdh` | `pyr.pdh` | PDH E1α; PDB **6CFO** WT (CANDIDATE, 2.70 Å — was **2OZL** phospho-mimic S264E, FIXED Plan 07; 6CER V138M mutant-reference is a Phase 7 option) | **DIS-PDHA1-01-cand** (PDHA1 V138M, PDHAD) + CAST-PDH-WT-PDB-01-cand (6CFO) | PDHA1 V138M→M138V (mature/PDB; =V167M UniProt precursor) | `edit.prompt` → EditRouter → branch node (Phase 7) | `bad.lost_connection` / `bad.released_from_host` | edit-allowed |
| `tca.citrate_synthase` | `tca.citrate_synthase` | citrate synthase; PDB **1CSC** (APPROVED: CAST-CSC-PDB-01, 1.70 Å) | **NONE — `edit:structural`** (NO disease mutant; 7 ClinVar Pathogenic records are ALL structural variants) | N/A — STRUCTURAL-EXPLORATION exception (the host's CS is NOT diseased; the hero just explores it; the restoration narrative does NOT apply) | `edit.prompt` → EditRouter → branch node (Phase 7) | `bad.lost_connection` / `bad.released_from_host` | edit-allowed |
| `tca.aconitase` | `tca.aconitase` | aconitase (ACO2); PDB **TBD_ACONITASE** (human ACO2 has NO PDB — bovine **1ACO** 2.05 Å teaching proxy; homology caveat §9 OQ-DM6) | **DIS-ACO2-01-cand** (ACO2 S112R, ICRD) | ACO2 S112R→R112S (bovine-vs-human numbering caveat — §9 OQ-DM6) | `edit.prompt` → EditRouter → branch node (Phase 7) | `bad.lost_connection` / `bad.released_from_host` | edit-allowed |
| `tca.shuffle` | `tca.aconitase` (**SHARED** — secondary aconitase edit point) | aconitase (SAME as `tca.aconitase`); NO PDB load | **NONE** — shared edit point (no DIS-*-cand of its own; routes to the SAME `tca.aconitase` EditRouter bucket) | same as `tca.aconitase` (S112R→R112S — the `edit:offer` routes to the same `edit.prompt`) | `edit.prompt` → EditRouter → same `tca.aconitase` bucket | `bad.lost_connection` / `bad.released_from_host` | both |
| `tca.isocitrate_dh` | `tca.isocitrate_dh` | isocitrate DH (IDH3A, NAD+-dependent mito — the REAL TCA enzyme); PDB **5GRF** (CANDIDATE, IDH3A) | **DIS-IDH3A-01-cand** (IDH3A M204I, RP90) — **IDH3A is DEFAULT**; IDH1 R132H cancer-mutant alternative FLAGGED §9 OQ-DM1 | IDH3A M204I→I204M (default; if IDH1 chosen: R132H→H132R + PDB swaps to 3INM) | `edit.prompt` → EditRouter → branch node (Phase 7) | `bad.lost_connection` / `bad.released_from_host` | both |
| `tca.succinyl_coa_synthetase` | `tca.succinyl_coa_synthetase` | succinyl-CoA synthetase α (SUCLG1); NO PDB (narrative-only) | **DIS-SUCLG1-01-cand** (SUCLG1 G85A, MTDPS9) | SUCLG1 G85A→A85G | `edit.prompt` → EditRouter → branch node (Phase 7) | `bad.lost_connection` / `bad.released_from_host` | both |
| `tca.fumarase` | `tca.fumarase` | fumarase (FH); NO PDB (narrative-only) | **DIS-FH-01-cand** (FH R233H, HLRCC — catalytically inactive, strongest candidate) | FH R233H→H233R | `edit.prompt` → EditRouter → branch node (Phase 7) | `bad.lost_connection` / `bad.released_from_host` | both |
| `tca.malate_dh` | `tca.malate_dh` | malate DH (MDH2); NO PDB (narrative-only) | **DIS-MDH2-01-cand** (MDH2 G37R, DEE51) | MDH2 G37R→R37G | `edit.prompt` → EditRouter → branch node (Phase 7) | `bad.lost_connection` / `bad.released_from_host` | both |
| `etc.complex_i` | `etc.complex_i` | Complex I (NDUFS8 subunit, 5LDW chain I); PDB **5LDW** CANDIDATE (not loaded in skeleton) | **DIS-NDUFS8-01-cand** (NDUFS8 R102H, MC1DN2) — claim_id FIXED Plan 09 (was `PLACEHOLDER_PHASE7_ETC`) | NDUFS8 R68H→H68R (PDB chain I resi 68 in 5LDW; =R102H UniProt) | `edit.prompt` → EditRouter → branch node (Phase 7) | `bad.lost_connection` / `bad.released_from_host` | edit-allowed |
| `etc.complex_ii` | `etc.complex_ii` | Complex II (SDHA, 1ZOY chain A); PDB **1ZOY** CANDIDATE (not loaded — narrative-only) | **DIS-SDHA-01-cand** (SDHA R554W, Leigh syndrome) | SDHA R512W→W512R (PDB chain A resi 512 in 1ZOY; =R554W UniProt) | `edit.prompt` → EditRouter → branch node (Phase 7) | `bad.lost_connection` / `bad.released_from_host` | both |
| `etc.complex_iii` | `etc.complex_iii` | Complex III (CYC1, 1BGY chain D); PDB **1BGY** CANDIDATE (not loaded — narrative-only) | **DIS-CYC1-01-cand** (CYC1 W96C, MC3DN6) | CYC1 W12C→C12W (PDB chain D resi 12 in 1BGY; =W96C UniProt; L215F alternative §9 OQ-DM8) | `edit.prompt` → EditRouter → branch node (Phase 7) | `bad.lost_connection` / `bad.released_from_host` | both |
| `etc.complex_iv` | `etc.complex_iv` | Complex IV (COX4I1, 1OCC chain D); PDB **1OCC** CANDIDATE (loaded in skeleton) | **DIS-COX4I1-01-cand** (COX4I1 P152T, MC4DN16) | COX4I1 P130T→T130P (PDB chain D resi 130 in 1OCC; =P152T UniProt; numbering caveat §9 OQ-DM7) | `edit.prompt` → EditRouter → branch node (Phase 7) | `bad.lost_connection` / `bad.released_from_host` | both |

> **Documented exceptions + fixes (Plans 07-09):**
>
> - **`tca.citrate_synthase` — STRUCTURAL-EXPLORATION exception:** this node
>   carries `edit:structural` (NOT a disease mutant). The 7 ClinVar Pathogenic
>   records for CS are ALL structural variants (inversions / CN-gains), not
>   implementable via `cmd.alter`. The restoration narrative does NOT apply
>   here — the host's CS is not diseased; the hero just explores the
>   structure. (Human may later remove the `edit:enzyme` tag entirely — flagged
>   §9 OQ-DM3.)
> - **`tca.shuffle` — SHARED edit point:** the shuffle node binds
>   `edit:enzyme:tca.aconitase` (the SAME enzyme_id as `tca.aconitase`). The
>   `edit:offer` on the shuffle node lets the player edit aconitase — the
>   enzyme whose prochirality-discrimination mechanism GROUNDS the RNG
>   shuffle. The EditRouter uses the SAME `tca.aconitase` lookup bucket for
>   edits triggered from either node. This is the ARCHITECTURE.md Pattern 2
>   canonical multi-role node (`rng-shuffle` + `choice` + `edit-allowed`).
> - **`pyr.pdh` cast-PDB fix (Plan 07):** the cast PDB was **2OZL** (an
>   S264E **phospho-mimic mutant**, NOT wild-type — verified via RCSB title).
>   FIXED to **6CFO** (WT, 2.70 Å, Whitley 2018 PubMed:29970614). The
>   **6CER** (V138M mutant, 2.69 Å, same paper) is a Phase 7 content option:
>   (a) apply-edit (load 6CFO WT → `cmd.alter` resi 138 → diseased state,
>   approximate geometry) vs (b) load-mutant-PDB (fetch 6CER directly, real
>   geometry, scientifically stronger per spec.md item 4 "reveal correct 3D
>   model"). The skeleton loads 6CFO as the WT cast; the human decides the
>   approach at Phase 7 (§9 OQ-DM2).
> - **`etc.complex_i` claim_id fix (Plan 09):** the claim_id was
>   `PLACEHOLDER_PHASE7_ETC` (a domain placeholder). REPLACED by
>   **DIS-NDUFS8-01-cand** (NDUFS8 R102H, PMID 9837812 Loeffen 1998 — the
>   first nuclear-encoded Complex I Leigh mutation). The `edit:enzyme:etc.complex_i`
>   tag + 2 choices are preserved unchanged.

> **ETC PDB-organism caveat (§9 OQ-DM10):** the 4 ETC PDBs (5LDW bovine,
> 1ZOY pig, 1BGY bovine, 1OCC bovine) are NON-HUMAN; the disease mutations
> are human. The `cmd.alter` apply-edit on a non-human PDB needs
> residue-number mapping (bovine/pig vs human — performed via 1:1 ungapped
> UniProt↔PDB alignment in the research docs). Phase 7 `text_teaching` must
> note the organism + numbering caveat; the human approves each PDB-cast
> claim.

### The `edit.prompt` stub (structural vs runtime distinction)

The skeleton has ONE shared `edit.prompt` stub node (in `bad_endings.json`)
with 3 `choice.goto` edges to the edit-related bad endings:
- `bad.critical_residue_break` (the known-critical-residue edit route)
- `bad.lost_connection` (unknown edit, structural path A)
- `bad.released_from_host` (unknown edit, structural path B)

These are **STRUCTURAL edges for the BFS, NOT player-facing options.** At
runtime, the EditRouter (`c14/edit_router.py`) routes directly via
`engine.apply_player_edit(intent, enzyme_id)` — known → branch_node, unknown →
bad-ending pool — **BYPASSING the stub's choices entirely.** The stub exists
so `check_reachability` (the BFS) sees the 3 edit-related bad endings as
reachable (SC2); without the stub, those bad endings would have no incoming
`choice.goto` edge and would be flagged as `unreachable_endings` → RED.

**The stub has NO cancel/return choice** — at runtime, the player does not
visit `edit.prompt` as a story node; the controller calls
`apply_player_edit` which routes via the EditRouter. Phase 6+ implements the
real edit UI (the controller builds the `EditIntent`, reads the current
node's `edit:enzyme:<id>` tag, and calls `apply_player_edit`). The stub's 3
choices are structural BFS paths, documented in the SC3 table above as a
placeholder MC-choice node.

### edits.json location + the existing placeholder's compatibility

**Location:** `c14/data/edits.json` (tooling-bound; NOT `data/edits.json` as
ARCHITECTURE.md says — the tooling path wins, see §3 + §9 OQ9).

The existing Phase 4 placeholder `c14/data/edits.json` has:
```json
{
  "version": 1,
  "bad_ending_pool": ["bad.lost_connection", "bad.released_from_host"],
  "enzymes": {
    "fixture_enzyme_1": { "edits": [...] }
  }
}
```

The `bad_ending_pool` ids (`bad.lost_connection`, `bad.released_from_host`)
**MATCH the skeleton's bad-ending node ids EXACTLY** — zero `edits.json`
update needed for the skeleton's bad-ending fallback. The enzyme entries
(the 14 edit-allowed nodes' known-edit signatures + branch nodes — see §5
table) are **Phase 7 content** — they are NOT in the placeholder yet (the
placeholder has only `fixture_enzyme_1`). Phase 7 populates the real enzyme
entries with approved claim_ids + known-edit signatures + branch nodes. The
skeleton is structurally compatible with the existing placeholder (the
fallback pool ids match); Phase 7 adds the enzyme-specific known-edit
entries.

---

## 6. Per-node classification table (SC5)

### SC5 derivation rule (gameplay design decision per node)

SC5 requires every node explicitly classified as `MC-choice` / `edit-allowed` /
`both` / `neither` — a per-node DESIGN decision, NOT an emergent property of
the implementation. The SC5 class is DERIVED from the structural type set
(§2) but is NOT mechanically equal to it: the structural type is the MECHANISM
(`ending` / `rng-shuffle` / `choice` / `edit-allowed` / `story`); the SC5 class
is the GAMEPLAY ROLE. The derivation rule:

| Structural type SET (§2) | SC5 class | How derived |
|---------------------------|-----------|-------------|
| has `choice` or `rng-shuffle` offering a REAL branching choice (≥2 choices with different ending-tier consequences, OR an RNG-shuffle node, OR a placeholder stub classified as MC-choice by design decision), NO `edit-allowed` | **MC-choice** | real branch + no `edit:enzyme:` tag |
| has `edit-allowed`, NO `choice`/`rng-shuffle` | **edit-allowed** | `edit:enzyme:` tag AND `len(choices)==0` (rare — none in the skeleton) |
| has BOTH `choice`/`rng-shuffle` AND `edit-allowed` | **both** | `len(choices)>0` AND `edit:enzyme:` tag |
| none of the above (ending node with `choices=[]`, OR non-ending node with only a single forced-advance "Continue"/"Back" choice that is NOT a tier-determining branch, AND no `edit:enzyme:` tag) | **neither** | `len(choices)==0` (ending) OR single forced-advance choice + no edit tag |

**Key distinction:** a node with a single "Continue" choice has structural
type `choice` (because `len(choices)>0`) but SC5 class `neither` (gameplay
role = narrative progression, NOT a branching choice — the player has no real
alternative). A node with ≥2 choices offering different ending-tier
consequences is `MC-choice` (a real branch). The structural types column in
the table below shows the raw derived types; the SC5 class column shows the
gameplay role.

> **Post-replan (Plans 07-09) — the single-"Continue" → MC conversion:**
> BEFORE the disease-mutant replan, 12 nodes had a single "Continue" choice
> (forced-advance `neither`) + 3 borderline single-choice nodes. Plans 07-09
> gave every one of these a 2nd `mc:observe` "Observe" choice (same
> destination, different experience — see §4 mc:observe note) OR an
> `edit:offer` choice. **There are now 0 single-"Continue" nodes** — every
> former forced-advance node is now `MC-choice` (≥2 non-edit choices) or
> `both`/`edit-allowed` (gained an `edit:offer`). The `mc:observe` choice
> does NOT change the ending-tier outcome (same `goto` as the Continue
> choice); it adds an "examine the structure" experience. So these converted
> nodes are `MC-choice` by the SC5 rule (≥2 non-edit choices) even though
> they are not tier-determining branches — the SC3 mechanism is unchanged
> (§4).

**Skeleton classification count (post-replan):** MC-choice = 16; both = 9;
edit-allowed = 5; neither = 13. Total = 16 + 9 + 5 + 13 = 43. ✓

- **both (9):** `gly.pyruvate_kinase`, `tca.isocitrate_dh`,
  `tca.succinyl_coa_synthetase`, `tca.fumarase`, `tca.malate_dh`,
  `tca.shuffle`, `etc.complex_ii`, `etc.complex_iii`, `etc.complex_iv`.
  (8 of these are the disease-mutant promotions of Plans 07-09: each gained
  `edit:enzyme:<id>` + `edit:offer` + a DIS-*-cand claim, and already had ≥2
  non-edit choices [Continue + Observe, or loop + forward]. `tca.shuffle`
  was already "both" — it has 2 weighted + `edit:offer` + cycle-trap.)
- **edit-allowed (5):** `gly.pfk`, `pyr.pdh`, `tca.aconitase`,
  `tca.citrate_synthase`, `etc.complex_i`. (Each has `edit:enzyme:<id>` +
  `edit:offer` but only 1 non-edit choice [a single Continue] — does NOT
  meet the "both" threshold of ≥2 non-edit choices. `tca.citrate_synthase`
  is the STRUCTURAL-EXPLORATION exception: `edit:structural` tag, NO disease
  mutant, NO DIS-*-cand — see §5.)
- **MC-choice (16):** the 6 original branch points (`intro.select`,
  `pyr.branch`, `anaer.entry`, `tca.divert_to_good`, `etc.entry`,
  `edit.prompt`) + the 10 Continue-to-MC conversions (`gly.start`, `gly.g6p`,
  `gly.fbp_to_pyruvate`, `gly.pyruvate`, `anaer.ldh`, `tca.entry`,
  `tca.co2_turn1`, `tca.co2_turn2`, `tca.akg_dh`, `etc.atp_synthase` — each
  gained an `mc:observe` 2nd choice).
- **neither (13):** the 11 endings (`end.true`, `end.good.fatty_acid`,
  `end.good.amino_acid`, `end.normal.co2`, `anaer.lactic`,
  `anaer.ethanolic`, `anaer.crisis`, `bad.lost_connection`,
  `bad.released_from_host`, `bad.cycle_trap_host_death`,
  `bad.critical_residue_break`) + 2 Phase-8 stubs (`fa.stub`, `alc.stub` —
  single "Back" return choice, NOT a tier-determining branch).

**14 edit-allowed nodes (the `edit:enzyme:`-tagged set):** 12 disease-mutant
(each carrying a `DIS-*-cand` CANDIDATE claim — see §5 edit-node contract) +
1 structural (`tca.citrate_synthase`, `edit:structural`, no disease mutant) +
1 shared (`tca.shuffle`, `edit:enzyme:tca.aconitase` — secondary aconitase
edit point, no DIS-*-cand of its own). All 12 `DIS-*-cand` claim_ids are
CANDIDATE pending Phase 7 per-claim approval (NOT approved; NOT yet in
`data/citations.json` — the citation gate is NOT run during Phase 5.1).

> **History:** BEFORE the tiered-completeness expansion: 6 + 6 + 22 = 34
> (MC-choice=6, both=6, neither=22). The expansion (34 → 43) added 9
> "neither" nodes. The disease-mutant replan (Plans 07-09) then converted
> the 15 single-"Continue"/borderline nodes to MC-choice/both and promoted
> 8 disease-mutant nodes to edit-allowed — arriving at the current 16 + 9 +
> 5 + 13 = 43.

### Per-node classification table (43 rows — one per node in the skeleton)

> **DIS-*-cand annotation:** the `claim_ids` column marks each disease-mutant
> node's CANDIDATE claim. `tca.citrate_synthase` is marked
> `edit:structural (no disease mutant)`. All `DIS-*-cand` claim_ids are
> CANDIDATE pending Phase 7 per-claim approval (NOT approved; NOT yet in
> `data/citations.json`).

| # | node_id | file | structural types (derived) | MC-choice? | edit-allowed? | SC5 class | enzyme_id | pathway branch point mapped | ending tier | claim_ids |
|---|---------|------|----------------------------|------------|---------------|-----------|-----------|-----------------------------|-------------|-----------|
| 1 | `intro.select` | intro.json | `choice` (3 choices, no weight) | yes | no | **MC-choice** | — | character-select (spec.md L19) | — | PLACEHOLDER_PHASE7 |
| 2 | `fa.stub` | intro.json | `choice` (1 "Back" choice) | no (forced return) | no | **neither** | — | Phase 8 stub (returns to select) | — | PLACEHOLDER_PHASE8 |
| 3 | `alc.stub` | intro.json | `choice` (1 "Back" choice) | no (forced return) | no | **neither** | — | Phase 8 stub (returns to select) | — | PLACEHOLDER_PHASE8 |
| 4 | `gly.start` | glycolysis.json | `choice` (2 choices: Continue + Observe) | yes (Continue-to-MC, Plan 07) | no | **MC-choice** | — | glucose entry (cytosol) | — | PLACEHOLDER_PHASE7_GLYCOLYSIS |
| 5 | `gly.g6p` | glycolysis.json | `choice` (2 choices: Continue + Observe) | yes (Continue-to-MC, Plan 07) | no | **MC-choice** | — | hexokinase step (G6P) | — | PLACEHOLDER_PHASE7_GLYCOLYSIS |
| 6 | `gly.pfk` | glycolysis.json | `choice`, `edit-allowed` (2 choices, `edit:enzyme:gly.pfk`) | no (1 non-edit choice) | yes | **edit-allowed** | `gly.pfk` | PFK-1 committed rate-limiting step | — | APPROVED: GLY-PFK-01, CAST-PFK-PDB-01; **DIS-PFKM-01-cand** (PFKM G209D, CANDIDATE) |
| 7 | `gly.fbp_to_pyruvate` | glycolysis.json | `choice` (2 choices: Continue + Observe) | yes (Continue-to-MC, Plan 07) | no | **MC-choice** | — | post-PFK glycolysis steps 4-9 (aldolase→enolase/PEP; net 2 ATP, 2 NADH) | — | PLACEHOLDER_PHASE7_GLYCOLYSIS |
| 8 | `gly.pyruvate_kinase` | glycolysis.json | `choice`, `edit-allowed` (3 choices: Continue + Observe + edit:offer, `edit:enzyme:gly.pyruvate_kinase`) | yes | yes | **both** | `gly.pyruvate_kinase` | pyruvate kinase (PEP→pyruvate + 2nd substrate-level ATP; PDB 7FS3 CANDIDATE) | — | PLACEHOLDER_PHASE7_GLYCOLYSIS; **DIS-PKLR-01-cand** (PKLR R479H, CANDIDATE) |
| 9 | `gly.pyruvate` | glycolysis.json | `choice` (2 choices: Continue + Observe) | yes (Continue-to-MC, Plan 07) | no | **MC-choice** | — | pyruvate product (branch point is next node) | — | PLACEHOLDER_PHASE7_GLYCOLYSIS |
| 10 | `pyr.branch` | pyruvate_branch.json | `choice` (2 cond-gated choices) | yes | no | **MC-choice** | — | pyruvate fate (aerobic vs anaerobic — O2-dependent) | — | PLACEHOLDER_PHASE7_ANAEROBIC |
| 11 | `pyr.pdh` | pyruvate_branch.json | `choice`, `edit-allowed` (2 choices, `edit:enzyme:pyr.pdh`) | no (1 non-edit choice) | yes | **edit-allowed** | `pyr.pdh` | PDH complex (pyruvate → acetyl-CoA + CO2; PDB 6CFO WT CANDIDATE loaded — was 2OZL phospho-mimic, FIXED Plan 07) | — | PLACEHOLDER_PHASE7; **DIS-PDHA1-01-cand** (PDHA1 V138M, CANDIDATE); CAST-PDH-WT-PDB-01-cand (6CFO, CANDIDATE) |
| 12 | `anaer.entry` | pyruvate_branch.json | `choice` (3 choices) | yes | no | **MC-choice** | — | 3-ending anaerobic branch (lactic→LDH step, ethanolic, crisis) | — | PLACEHOLDER_PHASE7_ANAEROBIC |
| 13 | `anaer.ldh` | pyruvate_branch.json | `choice` (2 choices: Continue + Observe) | yes (Continue-to-MC, Plan 07) | no | **MC-choice** | — | LDH step (pyruvate→lactate + NAD+; PDB 5W8J CANDIDATE; Decision 4; NOT promoted — GSD11 is frameshift, see §9 OQ-DM9) | — | PLACEHOLDER_PHASE7_ANAEROBIC |
| 14 | `anaer.lactic` | pyruvate_branch.json | `ending` (choices=[], is_ending=good) | no | no | **neither** | — | lactic fermentation (C retained via LDH) | **Good** | PLACEHOLDER_PHASE7_ANAEROBIC |
| 15 | `anaer.ethanolic` | pyruvate_branch.json | `ending` (choices=[], is_ending=normal) | no | no | **neither** | — | ethanolic fermentation (CO2 via pyruvate decarboxylase) [HOST-TENSION FLAG §9 OQ1] | **Normal** | PLACEHOLDER_PHASE7_ANAEROBIC |
| 16 | `anaer.crisis` | pyruvate_branch.json | `ending` (choices=[], is_ending=bad) | no | no | **neither** | — | anaerobic energy crisis → host death | **Bad** | PLACEHOLDER_PHASE7_ANAEROBIC |
| 17 | `tca.entry` | tca.json | `choice` (2 choices: Continue + Observe) | yes (Continue-to-MC, Plan 08) | no | **MC-choice** | — | acetyl-CoA enters TCA (condensation with OAA) | — | PLACEHOLDER_PHASE7_TCA |
| 18 | `tca.citrate_synthase` | tca.json | `choice`, `edit-allowed` (2 choices, `edit:enzyme:tca.citrate_synthase` + `edit:structural`) | no (1 non-edit choice) | yes | **edit-allowed** | `tca.citrate_synthase` | citrate synthase (stereospecific (S)-citryl-CoA → citrate) — STRUCTURAL-EXPLORATION exception (no disease mutant, see §5) | — | APPROVED: CAST-CSC-PDB-01, TCA-RNG-CITRATE-PROCHIRALITY-01; **edit:structural (no disease mutant)** |
| 19 | `tca.aconitase` | tca.json | `choice`, `edit-allowed` (2 choices, `edit:enzyme:tca.aconitase`) | no (1 non-edit choice) | yes | **edit-allowed** | `tca.aconitase` | aconitase (prochirality 180° flip — the Ogston hypothesis; PDB TBD_ACONITASE — human ACO2 has no PDB, bovine 1ACO teaching proxy) | — | APPROVED: TCA-RNG-CITRATE-PROCHIRALITY-01; **DIS-ACO2-01-cand** (ACO2 S112R, CANDIDATE) |
| 20 | `tca.shuffle` | tca.json | `rng-shuffle`, `choice`, `edit-allowed` (4 choices: 2 weighted + edit:offer + cond-gated; `edit:enzyme:tca.aconitase`) | yes (RNG) | yes | **both** | `tca.aconitase` (SHARED — secondary aconitase edit point) | citrate prochirality RNG shuffle (0.5/0.5 first-turn vs second-turn CO2 exit) — SOLE stochastic node | — | APPROVED: TCA-RNG-WEIGHT-01, TCA-RNG-CITRATE-PROCHIRALITY-01 |
| 21 | `tca.co2_turn1` | tca.json | `choice` (2 choices: loop vs forward) | yes (Continue-to-MC, Plan 08 — "Continue onward" + loop) | no | **MC-choice** | — | first-turn CO2 exit (forward → isocitrate_dh; loop → shuffle) | — | PLACEHOLDER_PHASE7_TCA |
| 22 | `tca.co2_turn2` | tca.json | `choice` (2 choices: loop vs forward) | yes (Continue-to-MC, Plan 08 — "Continue onward" + loop) | no | **MC-choice** | — | second-turn CO2 exit (forward → isocitrate_dh; loop → shuffle) | — | PLACEHOLDER_PHASE7_TCA |
| 23 | `tca.isocitrate_dh` | tca.json | `choice`, `edit-allowed` (3 choices: Continue + Observe + edit:offer, `edit:enzyme:tca.isocitrate_dh`) | yes | yes | **both** | `tca.isocitrate_dh` | isocitrate DH (isocitrate→α-KG + CO2 + NADH; rate-limiting, 1st CO2; PDB 5GRF CANDIDATE) | — | PLACEHOLDER_PHASE7_TCA; **DIS-IDH3A-01-cand** (IDH3A M204I, CANDIDATE — IDH3A is DEFAULT; IDH1 R132H alternative FLAGGED §9 OQ-DM1) |
| 24 | `tca.akg_dh` | tca.json | `choice` (2 choices: Continue + Observe) | yes (Continue-to-MC, Plan 08) | no | **MC-choice** | — | α-KG DH (α-KG→succinyl-CoA + CO2 + NADH; 2nd CO2; NARRATIVE-only, NO PDB per Decision 2; NOT promoted — low-confidence negative, see §9 OQ-DM4) | — | PLACEHOLDER_PHASE7_TCA |
| 25 | `tca.succinyl_coa_synthetase` | tca.json | `choice`, `edit-allowed` (3 choices: Continue + Observe + edit:offer, `edit:enzyme:tca.succinyl_coa_synthetase`) | yes | yes | **both** | `tca.succinyl_coa_synthetase` | succinyl-CoA synthetase (succinyl-CoA→succinate + GTP; only substrate-level phosphorylation in TCA; NARRATIVE) | — | PLACEHOLDER_PHASE7_TCA; **DIS-SUCLG1-01-cand** (SUCLG1 G85A, CANDIDATE) |
| 26 | `tca.fumarase` | tca.json | `choice`, `edit-allowed` (3 choices: Continue + Observe + edit:offer, `edit:enzyme:tca.fumarase`) | yes | yes | **both** | `tca.fumarase` | fumarase (fumarate→malate; NARRATIVE) | — | PLACEHOLDER_PHASE7_TCA; **DIS-FH-01-cand** (FH R233H, CANDIDATE — catalytically inactive, strongest candidate) |
| 27 | `tca.malate_dh` | tca.json | `choice`, `edit-allowed` (3 choices: loop + forward + edit:offer, `edit:enzyme:tca.malate_dh`) | yes | yes | **both** | `tca.malate_dh` | malate DH (malate→OAA + NADH; regenerates OAA, closes the cycle; loop → shuffle, forward → divert_to_good; NARRATIVE) | — | PLACEHOLDER_PHASE7_TCA; **DIS-MDH2-01-cand** (MDH2 G37R, CANDIDATE) |
| 28 | `tca.divert_to_good` | tca.json | `choice` (3 choices) | yes | no | **MC-choice** | — | acetyl-CoA/TCA-intermediate cataplerotic exit (citrate→FA, OAA→Asp, α-KG→Glu) | — | PLACEHOLDER_PHASE7_TCA |
| 29 | `etc.entry` | etc_atp.json | `choice` (2 choices) | yes | no | **MC-choice** | — | ETC entry vs CO2 exit (the narrative "let go" branch — NARRATIVE-FRAMING FLAG §9 OQ2) | — | PLACEHOLDER_PHASE7_ETC |
| 30 | `etc.complex_i` | etc_atp.json | `choice`, `edit-allowed` (2 choices, `edit:enzyme:etc.complex_i`) | no (1 non-edit choice) | yes | **edit-allowed** | `etc.complex_i` | Complex I (NADH:ubiquinone oxidoreductase; proton pumping; claim_id FIXED Plan 09: PLACEHOLDER_PHASE7_ETC → DIS-NDUFS8-01-cand) | — | **DIS-NDUFS8-01-cand** (NDUFS8 R102H, CANDIDATE) |
| 31 | `etc.complex_ii` | etc_atp.json | `choice`, `edit-allowed` (3 choices: Continue + Observe + edit:offer, `edit:enzyme:etc.complex_ii`) | yes | yes | **both** | `etc.complex_ii` | Complex II (succinate DH; succinate→fumarate + FADH2; TCA↔ETC bridge; NARRATIVE, PDB 1ZOY candidate) | — | PLACEHOLDER_PHASE7_ETC; **DIS-SDHA-01-cand** (SDHA R554W, CANDIDATE) |
| 32 | `etc.complex_iii` | etc_atp.json | `choice`, `edit-allowed` (3 choices: Continue + Observe + edit:offer, `edit:enzyme:etc.complex_iii`) | yes | yes | **both** | `etc.complex_iii` | Complex III (cytochrome bc1; Q-cycle, proton pump; NARRATIVE, PDB 1BGY candidate) | — | PLACEHOLDER_PHASE7_ETC; **DIS-CYC1-01-cand** (CYC1 W96C, CANDIDATE — C12W default, L215F alternative §9 OQ-DM8) |
| 33 | `etc.complex_iv` | etc_atp.json | `choice`, `edit-allowed` (3 choices: Continue + Observe + edit:offer, `edit:enzyme:etc.complex_iv`) | yes | yes | **both** | `etc.complex_iv` | Complex IV (cytochrome c oxidase; cyt c→O2→H2O; O2 terminal acceptor; PDB 1OCC CANDIDATE) | — | PLACEHOLDER_PHASE7_ETC; **DIS-COX4I1-01-cand** (COX4I1 P152T, CANDIDATE — T130P numbering caveat §9 OQ-DM7) |
| 34 | `etc.atp_synthase` | etc_atp.json | `choice` (2 choices: "Become ATP" + Observe, both → end.true) | yes (Continue-to-MC, Plan 09) | no | **MC-choice** | — | ATP synthase (Complex V; the soul-jump climax; NOT edit-allowed — no disease mutant in scope) | — | PLACEHOLDER_PHASE7_ETC |
| 35 | `end.true` | etc_atp.json | `ending` (choices=[], is_ending=true) | no | no | **neither** | — | ETC → ATP synthase electron harvest (soul-jump; metamorphosis framing) | **True** | PLACEHOLDER_PHASE7_ETC |
| 36 | `end.good.fatty_acid` | endings.json | `ending` (choices=[], is_ending=good) | no | no | **neither** | — | acetyl-CoA → fatty-acid synthesis (C retained) | **Good** | PLACEHOLDER_PHASE7_ENDINGS |
| 37 | `end.good.amino_acid` | endings.json | `ending` (choices=[], is_ending=good) | no | no | **neither** | — | TCA intermediate → amino-acid synthesis (C retained) | **Good** | PLACEHOLDER_PHASE7_ENDINGS |
| 38 | `end.normal.co2` | endings.json | `ending` (choices=[], is_ending=normal) | no | no | **neither** | — | aerobic "unremarkable CO2 exit" (narrative-only branch) [NARRATIVE-FRAMING FLAG §9 OQ2] | **Normal** | PLACEHOLDER_PHASE7_ENDINGS |
| 39 | `bad.lost_connection` | bad_endings.json | `ending` (choices=[], is_ending=bad) | no | no | **neither** | — | edit-failure fallback (unknown edit → "lost connection") | **Bad** | PLACEHOLDER_PHASE7_BAD |
| 40 | `bad.released_from_host` | bad_endings.json | `ending` (choices=[], is_ending=bad) | no | no | **neither** | — | edit-failure fallback (unknown edit → "released from host") | **Bad** | PLACEHOLDER_PHASE7_BAD |
| 41 | `bad.cycle_trap_host_death` | bad_endings.json | `ending` (choices=[], is_ending=bad) | no | no | **neither** | — | cycle-trap-until-host-death (SOLE timescale bad-ending; C14-decay DROPPED) [VISIT-CAP FLAG §9 OQ3] | **Bad** | PLACEHOLDER_PHASE7_BAD |
| 42 | `bad.critical_residue_break` | bad_endings.json | `ending` (choices=[], is_ending=bad) | no | no | **neither** | — | critical-residue-break → host cannot survive (known edit → bad branch) | **Bad** | PLACEHOLDER_PHASE7_BAD |
| 43 | `edit.prompt` | bad_endings.json | `choice` (3 structural choices) | yes (placeholder stub — design decision) | no (has `edit:prompt` tag, NOT `edit:enzyme:`) | **MC-choice** | — | structural stub (NOT a real pathway branch — edit-routing contract owns the real branching) | — | PLACEHOLDER_PHASE7_EDIT |

**Ending-tier distribution verified:** 1 True (#35) + 3 Good (#14, #36, #37) +
2 Normal (#15, #38) + 5 Bad (#16, #39, #40, #41, #42) = 11 endings. ✓ Matches
the user-confirmed asymmetric distribution (PROJECT.md row 107). The
tiered-completeness expansion added 9 path nodes but ZERO new endings; the
disease-mutant replan (Plans 07-09) added ZERO new nodes — the distribution
is unchanged.

**Approved-claim nodes (5, unchanged):** #6 `gly.pfk` (GLY-PFK-01 +
CAST-PFK-PDB-01), #18 `tca.citrate_synthase` (CAST-CSC-PDB-01 +
TCA-RNG-CITRATE-PROCHIRALITY-01), #19 `tca.aconitase`
(TCA-RNG-CITRATE-PROCHIRALITY-01), #20 `tca.shuffle` (TCA-RNG-WEIGHT-01 +
TCA-RNG-CITRATE-PROCHIRALITY-01). All other nodes use PLACEHOLDER_PHASE7* or
DIS-*-cand. The 5 APPROVED claim_ids are in `data/citations.json` (all
`approval_status: "approved"`); the gate would pass on these 5 and fail on
the PLACEHOLDER_PHASE7* + DIS-*-cand nodes (expected — the skeleton is a
design artifact, not shippable content; Phase 7 replaces the placeholders +
approves the DIS-*-cand candidates).

**DIS-*-cand disease-mutant nodes (12, all CANDIDATE — added Plans 07-09):**
#6 `gly.pfk` (DIS-PFKM-01-cand), #8 `gly.pyruvate_kinase` (DIS-PKLR-01-cand),
#11 `pyr.pdh` (DIS-PDHA1-01-cand), #19 `tca.aconitase` (DIS-ACO2-01-cand),
#23 `tca.isocitrate_dh` (DIS-IDH3A-01-cand), #25 `tca.succinyl_coa_synthetase`
(DIS-SUCLG1-01-cand), #26 `tca.fumarase` (DIS-FH-01-cand), #27 `tca.malate_dh`
(DIS-MDH2-01-cand), #30 `etc.complex_i` (DIS-NDUFS8-01-cand), #31
`etc.complex_ii` (DIS-SDHA-01-cand), #32 `etc.complex_iii` (DIS-CYC1-01-cand),
#33 `etc.complex_iv` (DIS-COX4I1-01-cand). NONE are in `data/citations.json`
yet — they are CANDIDATE pending Phase 7 per-claim approval (the skeleton
REFERENCES the candidates, it does NOT assert disease as approved fact —
AGENTS.md no-fabricated-science rule). The citation gate is NOT run during
Phase 5.1.

**PDB candidate nodes (CANDIDATE/PENDING — Phase 7 approval batch):**
#8 `gly.pyruvate_kinase` (7FS3), #11 `pyr.pdh` (6CFO WT — was 2OZL
phospho-mimic, FIXED Plan 07; 6CER V138M mutant-reference is a Phase 7
content option), #13 `anaer.ldh` (5W8J), #19 `tca.aconitase`
(TBD_ACONITASE — human ACO2 has no PDB; bovine 1ACO 2.05 Å teaching proxy),
#23 `tca.isocitrate_dh` (5GRF), #33 `etc.complex_iv` (1OCC). The 4 ETC
structures (5LDW Complex I, 1ZOY Complex II, 1BGY Complex III, 1OCC Complex
IV) are non-human (bovine/pig) — organism + residue-numbering caveats are
flagged in §9 OQ-DM10. NONE of these PDB IDs are in `data/citations.json` /
`data/sources.json` — they are a separate Phase 7 approval batch.

---

## 7. Reachability validation (SC2)

### The existing checker (verified)

`check_reachability(nodes, start_id)` (`c14/story/validate.py:167-207`) runs a
BFS from `start_id` over `choice.goto` edges, returning a `ReachabilityReport`
(`validate.py:86-117`) with `all_endings`, `reachable_endings`,
`unreachable_endings`, and an `is_ok` property (True iff
`unreachable_endings` is empty). Critical behavior, verified at
`validate.py:191-203`:

- **The BFS does NOT evaluate `cond` or `weight`.** It follows EVERY
  `choice.goto` that points to an EXISTING node (`validate.py:198-202`). So
  conditional edges and RNG-weighted edges are ALL traversed for reachability —
  the skeleton must route every ending via a REAL `choice.goto` chain, not
  rely on a `cond` to gate reachability.
- A `goto` to a nonexistent node is simply not traversed (flagging it is
  `validate_graph`'s job, `validate.py:214-235` — a separate check).
- Missing `start_id` → graceful: `is_ok=False`, all endings unreachable
  (`validate.py:187-189`).
- Duck-types on Node objects OR raw dicts via `_choices`/`_goto`/`_is_ending`
  (`validate.py:124-160`).

### SC2 green test (all 4 ending tiers reachable)

The green test follows the proven pattern in `tests/test_integration.py:336-346`.
Plan 05.1-04 authors `tests/test_glucose_reachability.py` (5 tests):

```python
g = StoryGraph.load("data/story_glucose/")              # repo-root data/story_glucose
rep = check_reachability(g.all_nodes(), g.start_node())  # start = "intro.select"
assert rep.is_ok, "glucose skeleton has no orphaned endings"
reachable_tiers = {n.is_ending for n in (g.get_node(e) for e in rep.reachable_endings)}
assert {"true", "good", "normal", "bad"} <= reachable_tiers, "all 4 tiers reachable"
assert rep.unreachable_endings == []
assert len(rep.all_endings) == 11  # 1T + 3G + 2N + 5B
```

The BFS follows `choice.goto` ONLY (not `on_enter_divert`); it ignores `cond`
+ `weight`, so BOTH the aerobic and anaerobic subtrees are traversed. All 11
endings are structurally reachable via the `choice.goto` chains documented in
the research (05.1-RESEARCH-pathway-topology.md §Q7 GREEN table, updated for
the tiered-completeness expansion):

| tier | ending node | reachable via this `choice.goto` chain |
|------|-----------|----------------------------------------|
| True | `end.true` | `intro.select → gly.start → gly.g6p → gly.pfk → gly.fbp_to_pyruvate → gly.pyruvate_kinase → gly.pyruvate → pyr.branch → pyr.pdh → tca.entry → tca.citrate_synthase → tca.aconitase → tca.shuffle → tca.co2_turn1/turn2 → tca.isocitrate_dh → tca.akg_dh → tca.succinyl_coa_synthetase → tca.fumarase → tca.malate_dh → tca.divert_to_good → etc.entry → etc.complex_i → etc.complex_ii → etc.complex_iii → etc.complex_iv → etc.atp_synthase → end.true` |
| Good | `end.good.fatty_acid` | `... → tca.malate_dh → tca.divert_to_good → end.good.fatty_acid` |
| Good | `end.good.amino_acid` | `... → tca.malate_dh → tca.divert_to_good → end.good.amino_acid` |
| Good | `anaer.lactic` | `intro.select → ... → pyr.branch → anaer.entry → anaer.ldh → anaer.lactic` |
| Normal | `end.normal.co2` | `... → etc.entry → end.normal.co2` (the "let go" choice) |
| Normal | `anaer.ethanolic` | `... → pyr.branch → anaer.entry → anaer.ethanolic` |
| Bad | `bad.lost_connection` | `... → <any edit-allowed node> → edit.prompt → bad.lost_connection` (structural path through the stub) |
| Bad | `bad.released_from_host` | `... → edit.prompt → bad.released_from_host` |
| Bad | `bad.cycle_trap_host_death` | `... → tca.shuffle → bad.cycle_trap_host_death` (cond-gated; BFS ignores cond, traverses goto) |
| Bad | `bad.critical_residue_break` | `... → edit.prompt → bad.critical_residue_break` |
| Bad | `anaer.crisis` | `... → pyr.branch → anaer.entry → anaer.crisis` |

> **Note (tiered-completeness expansion):** the forward path from
> `tca.co2_turn1`/`tca.co2_turn2` now goes through the 5 new TCA enzyme
> nodes (`tca.isocitrate_dh → tca.akg_dh → tca.succinyl_coa_synthetase →
> tca.fumarase → tca.malate_dh`) before reaching `tca.divert_to_good` —
> the previous direct `co2_turn* → divert_to_good` edge was replaced.
> `tca.malate_dh` offers a 2nd loop-back to `tca.shuffle` (regenerating
> OAA = the cycle turning again), mirroring the `co2_turn*` loop. The
> aerobic True-ending path now also threads through `gly.pyruvate_kinase`
> and the 3 split ETC complexes (`complex_ii → complex_iii →
> complex_iv`). The anaerobic lactic path now threads through
> `anaer.ldh`. All 11 endings remain reachable; the BFS is unchanged
> (it follows `choice.goto` edges only — the new nodes are all
> `choice.goto`-reachable).

### SC2 red test (deliberately orphaned variant)

The red test follows `tests/test_integration.py:348-362` — remove the
`choice.goto` edge to `end.true` (change `etc.atp_synthase`'s choice from
`goto: "end.true"` to `goto: "end.normal.co2"`) → `end.true` has no incoming
`choice.goto` edge → `check_reachability` reports
`unreachable_endings = ["end.true"]` → `is_ok == False` → RED.

```python
g = StoryGraph.load("data/story_glucose/")
orphaned = dict(g.all_nodes())                     # shallow copy the {id: Node} dict
orphaned["etc.atp_synthase"] = Node.from_dict({
    "id": "etc.atp_synthase",
    "choices": [{"label": "Become ATP", "goto": "end.normal.co2"}],  # re-routed
    "claim_ids": list(orphaned["etc.atp_synthase"].claim_ids),
    "is_ending": None,
    "tags": list(orphaned["etc.atp_synthase"].tags),
})
rep = check_reachability(orphaned, g.start_node())
assert not rep.is_ok, "orphaned variant is red"
assert "end.true" in rep.unreachable_endings
```

### CRITICAL: the aerobic-only-True-ending rule (SC2 satisfiability)

The True ending is biochemically unreachable anaerobically (no O2 → no ETC →
no ATP synthase → no soul-jump — verified 05-RESEARCH-anaerobic.md, settled
chemistry, PROJECT.md row 108). BUT `check_reachability` does NOT model O2 —
it follows ALL `choice.goto` edges regardless of `cond`. Therefore:

- **The True ending MUST be statically reachable via a real `choice.goto`
  chain** (the aerobic path) — the BFS finds it via
  `pyr.branch → pyr.pdh → ... → etc.atp_synthase → end.true`. The `pyr.branch`
  aerobic choice has `cond: "not flags.host_o2_low"` — the BFS IGNORES this
  cond, so it traverses the aerobic `goto: "pyr.pdh"` edge. ✓ True is
  structurally reachable.
- **The anaerobic guard is a RUNTIME `cond` on a choice, NOT a structural
  edge.** At playtime, if `flags.host_o2_low == true`, the `pyr.branch`
  aerobic choice is hidden (`_cond` returns False, `interpreter.py:134-135`)
  so the player cannot take it — but the STRUCTURAL edge still exists in the
  JSON, so the BFS traverses it for reachability. ✓
- **DO NOT model the anaerobic block as a missing structural edge** (e.g. a
  separate anaerobic sub-graph with the True ending physically absent) — that
  would make the True ending `unreachable_endings` and FAIL SC2. The skeleton
  keeps the True ending structurally reachable (aerobic path) and gates it at
  runtime via `cond`.

**The key design rule for the skeleton topology: cond gates RUNTIME
eligibility; choice.goto gates STRUCTURAL reachability.** (The `cond` field
gates runtime eligibility — a falsy `cond` hides the choice from the player
at playtime; the `choice.goto` field gates structural reachability — the BFS
follows every `choice.goto` edge regardless of `cond`.) SC2 (structural
reachability of all 4 tiers) is satisfiable ONLY if every ending — including
the True ending — has at least one real `choice.goto` path from `start`. The
skeleton honors this: all 11 endings are structurally reachable via
`choice.goto` chains; the aerobic/anaerobic split is a runtime `cond`
concern the BFS does not model.

The same logic applies to the anaerobic 3-ending branch (lactic=Good /
ethanolic=Normal / crisis=Bad): those endings are structurally reachable via
`pyr.branch → anaer.entry → anaer.*` (the BFS ignores the
`cond: "flags.host_o2_low"` on the anaerobic choice); the `host_o2_low` cond
gates which branch is OFFERED at runtime, not whether it's structurally
reachable.

### The tests/test_glucose_reachability.py file (5 tests, authored in Plan 04)

Plan 05.1-04 authors the reachability test file with 5 tests:
1. Green: all 11 endings reachable, all 4 tiers reachable, `is_ok == True`.
2. Red: orphaned `end.true` (re-routed `etc.atp_synthase`) → `is_ok == False`.
3. Tier count: exactly 11 endings (1T + 3G + 2N + 5B).
4. Start node: `g.start_node() == "intro.select"`.
5. No dangling diverts: `validate_graph(g.all_nodes()) == []` (all `goto`
   targets exist).

---

## 8. Backward-compat + gate impact

### The RECOMMENDED approach (derived types + `tags` for edit-allowed) — ZERO impact

The recommended approach (derived structural types + `tags` entry
`edit:enzyme:<id>` for edit-allowed) requires ZERO code changes to the engine,
model, gates, or tools (research Q8 table):

| Concern | Impact | Evidence |
|---------|--------|----------|
| `Node.from_dict` backward-compat | NONE — uses existing fields only (`is_ending`, `choices`, `tags` all parsed via `.get`, `model.py:281-292`) | `model.py:273-293` |
| `Node.to_dict` round-trip | NONE — `tags` already round-trips (`model.py:306`); no new field | `model.py:295-308` |
| AST import gate (`tools/check_imports.py`) | NONE — scans for `pymol`/`PyQt5` imports only (`check_imports.py:34`); a `tags` convention adds no imports | `check_imports.py:33-74` |
| Citation gate (`tools/check_citations.py` + `collect_claim_ids`) | NONE — `collect_claim_ids` reads ONLY `claim_ids` (`validate.py:261`: `node.get("claim_ids", [])`); ignores `tags`/`choices`/`is_ending` | `validate.py:242-262`, `check_citations.py:55` |
| Edit-coverage gate (`tools/check_edit_coverage.py`) | NONE — reads `c14/data/cast.json` + `c14/data/edits.json` only (`check_edit_coverage.py:41-42`); does NOT read story nodes | `check_edit_coverage.py:41-42` |
| `validate_edits_table` / `scan_edit_coverage` | NONE — operate on the edits table + cast, not node tags | `edit_router.py:172-255` |
| Interpreter / engine runtime | NONE — the classifier (§2) is documentation; the interpreter already dispatches on `is_ending`/`choices`/`weight`; `tags` is carried but not read by the interpreter (only the controller would read `edit:enzyme:`) | `interpreter.py:43-99`, `engine.py:106-194` |
| Existing tests | NONE — `tags` conventions don't change any parsed field; the 198 existing tests pass unchanged | (no field change) |

**The recommended approach requires ZERO code changes to the engine, model,
gates, or tools.** It is purely a JSON authoring convention + a design doc.
This is the lowest-risk path and is strongly recommended.

### IF Option 2 (new `edit_enzyme_id` field) were adopted — additive impact

| Concern | Impact | What must change |
|---------|--------|------------------|
| `Node.from_dict` | Backward-compatible IF added as `d.get("edit_enzyme_id")` (mirrors `on_enter_divert=d.get(...)`, `model.py:292`) | Add 1 line to `from_dict` (`model.py:283-293`) |
| `Node.to_dict` round-trip | REQUIRES update — add `"edit_enzyme_id": self.edit_enzyme_id` | Add 1 line to `to_dict` (`model.py:295-308`) |
| `Node.__init__` | Add `edit_enzyme_id=None` param + `self.edit_enzyme_id = ...` | 2 lines (`model.py:259-271`) |
| AST gate / citation gate / coverage gate | NONE — unaffected (no new imports; `collect_claim_ids` ignores it) | nothing |
| Existing tests | `test_model.py` round-trip test would still pass (new field defaults None); no regression | nothing (verify) |
| `test_model.py:129` | The `to_dict`-keys assertion may need the new key added if it asserts exact key set | check + maybe update |

**Net: Option 2 is a 4-line additive change to `model.py`,
backward-compatible, no gate impact.** Still, Option 1 (`tags`) is adopted
because it needs zero changes. Flagged as an open question (§9 OQ10).

### No OTHER proposed field is needed

- `node_type` field: REJECTED (§2 — wrong for multi-role nodes).
- `classification` field: NOT recommended (derivable, would drift; use a
  design-doc table — §6).
- `edit_allowed` boolean: redundant with the `edit:enzyme:<id>` tag (the
  tag's presence IS the boolean).

### The 198 existing tests pass unchanged (zero regression)

The toy fixture at `data/story/` is UNTOUCHED (the glucose skeleton is at
`data/story_glucose/`, a NEW directory). The 198 existing tests
(engine + interpreter + graph + validate + model + edit_router + integration +
molops + edit_ops + protonation + citations) all point at `data/story/` or
their own fixtures; none reference `data/story_glucose/`. The skeleton's
`tags` conventions (`edit:enzyme:<id>`, `edit:offer`, `rng:weighted`,
`stage:*`) don't change any parsed field — `Node.from_dict` already parses
`tags` via `.get`. Zero regression.

---

## 9. Open questions for human review (surfaced to Plan 06)

Per the no-fabricated-science rule, every branch point + ending whose science
is NOT in an approved source must be human-reviewed before Phase 7 content.
The roadmap (ROADMAP.md line 128, SC1) explicitly requires "human-review the
mapping against spec.md + approved sources." The following open questions are
surfaced from the two research tracks + the planner's design decisions for
the human review checkpoint (Plan 05.1-06):

### OQ1. Ethanolic anaerobic ending vs mammalian host [HIGH priority — research OQ1]

The skeleton INCLUDES `anaer.ethanolic` (per the user's 3-ending confirmation:
lactic=Good, ethanolic=Normal, crisis=Bad) but its `text_teaching` is FLAGGED
— mammalian hosts do NOT do ethanolic fermentation (it's a yeast pathway;
mammals lack pyruvate decarboxylase). The user confirmed BOTH the 3-ending
anaerobic branch (including ethanolic) AND the mammal/human host. The tension
was flagged in 05-RESEARCH-anaerobic.md Open Question 3.

**Human decides at Phase 7:** (a) drop `anaer.ethanolic` (mammal host → only
lactic + crisis, 2-ending anaerobic branch — the Normal tier is still
reachable via `end.normal.co2` aerobically, so all 4 tiers remain reachable);
(b) frame as a yeast counterfactual ("in yeast, this would happen; in your
mammalian host, this path doesn't exist — but here's what it would look
like"); (c) allow host variation (mammal for lactic, yeast for ethanolic —
contradicts the mammal/human decision). The skeleton provides the node; the
human reviews the branch-point mapping + the teaching text framing.

### OQ2. Normal-ending framing is a NARRATIVE tier [MEDIUM — research OQ2]

The skeleton places `end.normal.co2` as a branch off `etc.entry` (the player
"lets go" instead of following the soul into the ETC). Biochemically, if the
carbon goes through PDH + TCA, it exits as CO2 AND its electrons (NADH/FADH2)
go to the ETC. So "CO2 without the harvest arc" is a NARRATIVE distinction
(the story ends at CO2, the dramatic soul-jump climax doesn't happen), NOT a
separate biochemical path.

**Human reviews:** is `etc.entry`'s "let go" choice the right narrative
branch point for the Normal ending, or should the Normal ending be a
retrospective framing (the story ends at CO2 regardless of the ETC — the
narrative just doesn't follow the electrons to ATP)? This is a Phase 7
narrative decision; the skeleton just provides the node + the branch.

### OQ3. Cycle-trap visit-cap N [LOW — research OQ3]

The skeleton uses N=5 (placeholder) for the cycle-trap cond gate:
`cond: "visits.get('tca.shuffle', 0) > 5"` → `bad.cycle_trap_host_death`. The
value of N is a game-design parameter (how many TCA turns before the host
dies), NOT a science claim (no citation needed). Too few → the player feels
cheated; too many → the trap never triggers.

**Human confirms N at Phase 6 playtest.** NOT a science-approval item — it's
a game-balance parameter. The skeleton uses N=5 as a reasonable mid-value;
the planner can adjust.

### OQ4. Cataplerotic-exit claims (Good endings) NOT in first-batch [MEDIUM — research OQ4]

`end.good.fatty_acid` (citrate → cytosol → fatty-acid synthesis via
ATP-citrate lyase + ACC + FAS) + `end.good.amino_acid` (OAA → aspartate; α-KG
→ glutamate via transamination) + `tca.divert_to_good` use
`PLACEHOLDER_PHASE7`. These are STANDARD biochemistry (the TCA cataplerotic
exits — the carbon is diverted pre-oxidation, matches Good semantics) but
they are NOT in the first-batch approved sources (`data/sources.json` has only
LIBRETEXTS-METAB-GLYCOLYSIS, LIBRETEXTS-METAB-TCA, PDB-4PFK, PDB-1CSC).

**Human approves a source at Phase 7** (e.g. a LibreTexts amino-acid-metabolism
+ fatty-acid-synthesis chapter). The branch-point MAPPING is real (not
invented); the CLAIMS are pending.

### OQ5. ETC PDB structures CANDIDATE [MEDIUM — research OQ5, Phase 7]

5 ETC/ATP-synthase PDB structures (5LDW Complex I, 1ZOY Complex II, 1BGY
Complex III, 1OCC Complex IV, 1E79 ATP synthase F1) are web-verified at
rcsb.org but CANDIDATE (NOT yet human-approved). Per the tiered-completeness
expansion (Decision 3), `etc.complex_iv` NOW LOADS PDB 1OCC (candidate) in its
`on_enter` — pending Phase 7 human approval. `etc.complex_ii` and
`etc.complex_iii` are NARRATIVE nodes with NO PDB load (candidates 1ZOY / 1BGY
deferred for Phase 7). `etc.complex_i` and `etc.atp_synthase` still use
placeholder `on_enter` (`hide_all` only).

**Human approves the 5 PDB structures before Phase 7 wires the remaining
`on_enter` loads.** This is a Phase 7 approval item, NOT a Phase 5.1 blocker —
the skeleton is structure, not content. The skeleton's CLAIM is the topology +
the cast composition; the actual PDB loads are Phase 7.

### OQ6. PDH cast PDB — 2OZL→6CFO fix applied (Plan 07) [LOW — Phase 7 cast]

`pyr.pdh` is edit-allowed (the player can break a critical PDH residue →
`bad.critical_residue_break`). Per the tiered-completeness expansion,
`pyr.pdh` loaded PDB 2OZL (candidate) in its `on_enter` — but Plan 07
DISCOVERED that 2OZL is an S264E **phospho-mimic mutant** (NOT wild-type).
**FIXED to 6CFO** (WT, 2.70 Å) — pending Phase 7 human approval. The
6CER (V138M mutant, 2.69 Å) is a Phase 7 content option (apply-edit vs
load-mutant-PDB — see §5 + §9 OQ-DM2). `claim_ids` now include
`DIS-PDHA1-01-cand` + `CAST-PDH-WT-PDB-01-cand` (both CANDIDATE).

**The PDH PDB ID approval is a Phase 7 item** (the ~20+ cast). NOT a Phase 5.1
blocker.

### OQ7. char-setting mechanism for character-select [COORDINATION — research OQ7]

The skeleton's `intro.select` choices use placeholder `effects: {}` (no state
mutation). The `GameState.character` field (`c14/state.py:34`) is set at
`GameState.new_game(character, seed, start_node_id)` (`state.py:117-132`).
The choice `effects.set` writes to `state.flags` (not `state.character`
directly — `state.py:141-144` `set_flag` writes to `self.flags`).

**The controller (Phase 6) reads the chosen index and calls
`engine.start(character="glucose", ...)` which sets `state.character`.** The
choice-point contract (§4) should specify how character-select sets
`state.character` — the skeleton's `intro.select` choices use placeholder
`effects: {}` and the controller handles `state.character` setting from the
choice index. This is a Phase 6 UI/controller design decision.

### OQ8. on_enter_divert dormancy [COORDINATION — research OQ8 / Q1 flag]

`on_enter_divert` is defined in the model (`model.py:255`) but NEVER
referenced by `interpreter.py` or `engine.py` (verified via grep — only in
`model.py` + `test_model.py`). The skeleton uses `choice.goto` for all
forward paths (the post-replan skeleton has 0 single-"Continue" nodes —
former forced-advance nodes now have a 2nd `mc:observe` Observe choice;
zero engine change, recommended).

**IF the designer wants `on_enter_divert` to be BFS-traversable, that's a
reachability-checker EXTENSION** (the checker would need to follow
`on_enter_divert` as well as `choice.goto`) — NOT proposed in Phase 5.1.
Reserve `on_enter_divert` for a future engine extension. The skeleton's
"Continue" choices work today with zero engine change.

### OQ9. edits.json / cast.json location [low-stakes — research Q9.2]

`c14/data/edits.json` + `c14/data/cast.json` (tooling-bound, current —
`tools/check_edit_coverage.py:41-42` hardcodes this path) vs `data/edits.json`
+ `data/cast.json` (ARCHITECTURE.md says repo-root `data/`).

**Recommendation: keep `c14/data/` (zero-work); treat ARCHITECTURE.md as
outdated.** Moving to `data/` would require updating
`tools/check_edit_coverage.py:41-42` and break the Phase 4 placeholder + its
VERIFICATION. The tooling-bound path wins.

### OQ10. edit-allowed + enzyme_id binding: tags vs new field [research Q9.1]

The skeleton uses the `tags` approach (`edit:enzyme:<id>`, zero schema change
— Option 1, RECOMMENDED, ADOPTED). The alternative (new `Node.edit_enzyme_id`
field, 4-line additive change — Option 2) is flagged but NOT adopted.

**Decision: tags (Option 1) adopted** — zero schema change, backward-
compatible, greppable, uses `tags` for its designed purpose. Option 2 remains
available if the planner prefers first-class explicitness in a future phase.

### OQ11. SC5 classification: design-doc table vs JSON field [research Q9.3]

The skeleton uses a design-doc table (§6 — this section). A JSON
`classification` field is REJECTED (derivable, would drift out of sync — the
same problem as a `node_type` field).

**Decision: design-doc table (§6)** — reviewable, derivable, no drift. A small
pure-Python check (a future `tools/check_classification.py` gate) could
ASSERT the table matches the derived classification (catches drift) —
nice-to-have, NOT required for SC5 (a table suffices).

### OQ12. Node-level vs choice-level edit:offer tag [research Q9.5]

The skeleton uses a NODE-level `edit:enzyme:<id>` tag (the enzyme_id binding
is a node property — the whole node binds to one enzyme context for the
EditRouter) AND a CHOICE-level `edit:offer` tag (marks which choice launches
the edit UI). They coexist: the node tag declares "this node's enzyme
context"; the choice tag declares "this choice opens the edit UI".
ARCHITECTURE.md puts `edit:offer` on a choice (`:250`); the skeleton follows
this AND adds the node-level binding.

**Decision: both coexist** — node-level `edit:enzyme:<id>` for the binding +
choice-level `edit:offer` for the UI trigger.

### OQ13. anaer.entry classification [planner decision]

The research tentatively classified `anaer.entry` as `story` / `neither`
(left the 3-ending branching mechanism unspecified). The planner classifies
it as `choice` / **MC-choice** — it offers the 3-way fermentation-outcome
choice (lactic / ethanolic / crisis), which IS a real tier-determining branch
(each choice leads to a different ending tier: Good / Normal / Bad).

**Human reviews this classification** at Plan 06. The planner's override (per
the plan's instruction: "anaer.entry (the planner's design decision — the
3-way fermentation-outcome choice; OVERRIDES the research's tentative
story/neither classification") is documented in §6 row 11.

### Disease-Mutant Replan Open Questions (Plans 07-09 — for the 05.1-06 checkpoint)

> **The 05.1-06 human-review checkpoint MUST resolve these before Phase 7.**
> Each maps to a specific node id + `DIS-*-cand` claim_id so the reviewer can
> locate it in the skeleton. All 12 `DIS-*-cand` claim_ids are CANDIDATE
> pending Phase 7 per-claim approval (§9 OQ-DM11).

#### OQ-DM1. [CRITICAL] IDH3A vs IDH1 — `tca.isocitrate_dh` (`DIS-IDH3A-01-cand`)

The skeleton uses **IDH3A M204I→I204M** as the DEFAULT (Option A):
biochemically correct TCA enzyme (NAD+-dependent mitochondrial), ClinVar
Pathogenic, OMIM 619007 (RP90), PDB 5GRF matches IDH3A.

**Alternative (Option B):** IDH1 R132H→H132R — cancer-mutant (glioma, OMIM
137800), real mutant PDB 3INM, narratively powerful ("the hero encounters a
cancer-mutant version") but NOT the TCA enzyme (IDH1 is NADP+-dependent
cytosolic, not the NAD+-dependent mitochondrial TCA enzyme).

**Flag:** the human decides. If the human prefers Option B, the claim_id
swaps to `DIS-IDH1-01-cand` + the PDB swaps to 3INM + the text reframes.
The skeleton does NOT choose unilaterally (researcher's presentation, not a
unilateral decision per AGENTS.md no-fabricated-science rule).

#### OQ-DM2. [CRITICAL] pyr.pdh cast PDB — `pyr.pdh` (`DIS-PDHA1-01-cand`, `CAST-PDH-WT-PDB-01-cand`)

**2OZL (S264E phospho-mimic mutant, WRONG) → 6CFO (WT, 2.70 Å) [skeleton
DEFAULT]** or 1NI4 (WT, 1.95 Å, partial residues 30-309). Mutant reference:
**6CER (V138M, 2.69 Å)**.

**Phase 7 content decision:** option (a) apply-edit (load 6CFO WT →
`cmd.alter` resi 138 → diseased state, approximate geometry) vs option (b)
load-mutant-PDB (fetch 6CER directly, real geometry, scientifically stronger
per spec.md item 4 "reveal correct 3D model").

**Flag:** human decides the approach; the skeleton loads 6CFO as the WT cast.

#### OQ-DM3. [HIGH] tca.citrate_synthase reframe — `tca.citrate_synthase` (NO DIS-*-cand, `edit:structural`)

**Keep `edit:enzyme` + `edit:structural` (structural-exploration, NO disease
mutant) [skeleton DEFAULT]** vs remove the `edit:enzyme` tag entirely.

CS has NO disease point mutant (7 ClinVar Pathogenic records are ALL
structural variants — inversions/CN-gains, not implementable via
`cmd.alter`; UniProt O75390 disease field empty).

**Flag:** the human confirms the reframe. If removed, the SC5 class + the
edit-node contract row are updated.

#### OQ-DM4. [MEDIUM] tca.akg_dh deeper scan — `tca.akg_dh` (NOT promoted, no DIS-*-cand)

LOW-confidence negative: no pathogenic missense in first 14/163 ClinVar
Oxoglutaricaciduria records (NCBI rate-limit truncated the scan). A complete
scan of all 163 records OR a focused PubMed search for "OGDH pathogenic
missense" may surface a confirmed point mutation.

**Flag:** if the human wants an OGDH edit-allowed node, commission a
follow-up research pass. Until then, OGDH stays NOT edit-allowed (a 2-choice
MC node with Continue + Observe).

#### OQ-DM5. [MEDIUM] PFKM mutant choice — `gly.pfk` (`DIS-PFKM-01-cand`)

**G209D→D209G [skeleton DEFAULT]** vs W686C→C686W vs R39L→L39R.

The skeleton references `DIS-PFKM-01-cand` WITHOUT choosing the residue (the
claim is CANDIDATE; the specific residue is a Phase 7 edits.json content
decision).

**Flag:** the human picks the PFKM mutant for the edits.json entry (Phase 7).

#### OQ-DM6. [MEDIUM] Aconitase bovine-1ACO homology + residue numbering — `tca.aconitase` (`DIS-ACO2-01-cand`)

Human mito ACO2 (UniProt Q99798) has NO PDB (verified UniProt REST, 0
cross-refs). Bovine 1ACO (2.05 Å) is the teaching proxy. The S112R
reverse-mutation must be cross-checked for bovine-vs-human numbering (may
differ slightly).

**Flag:** the human approves the 1ACO teaching-proxy claim + the residue
mapping. Phase 7 `text_teaching` must note the homology caveat + approximate
geometry from `cmd.alter`.

#### OQ-DM7. [MEDIUM] COX4I1 residue numbering — `etc.complex_iv` (`DIS-COX4I1-01-cand`)

Human UniProt precursor P152T vs PDB chain D resi 130 (T130P, mature
numbering). The disease mutation is P152T (UniProt) which maps to PDB resi
130 (PRO→THR).

**Flag:** the human confirms the numbering for the edits.json entry (Phase 7).
The skeleton references `DIS-COX4I1-01-cand`.

#### OQ-DM8. [MEDIUM] CYC1 residue choice — `etc.complex_iii` (`DIS-CYC1-01-cand`)

**C12W→W12C (PDB chain D resi 12) [skeleton DEFAULT]** vs L215F (PDB resi
131, same paper, ClinVar Pathogenic alternative).

**Flag:** the human picks the CYC1 residue for the edits.json entry (Phase 7).

#### OQ-DM9. [LOW] anaer.ldh NOT promoted — `anaer.ldh` (NOT edit-allowed, no DIS-*-cand)

GSD11 (LDHA deficiency, MIM:612933) canonical mutation is a 20-bp FRAMESHIFT
(not a point mutation implementable via `cmd.alter`); ClinVar point variants
are VUS.

**Flag:** `anaer.ldh` stays a non-edit-allowed MC node (Continue + Observe). If
the human wants an LDH edit, a different disease/variant would need research.

#### OQ-DM10. [LOW] SDHA / NDUFS8 / CYC1 / COX4I1 PDB-organism caveat — `etc.complex_i/ii/iii/iv`

The ETC PDBs (5LDW bovine, 1ZOY pig, 1BGY bovine, 1OCC bovine) are non-human;
the disease mutations are human. The `cmd.alter` apply-edit on a non-human
PDB needs residue-number mapping (bovine/pig vs human — performed via 1:1
ungapped UniProt↔PDB alignment in the research docs).

**Flag:** Phase 7 `text_teaching` must note the organism + numbering caveat;
the human approves each PDB-cast claim.

#### OQ-DM11. [INFO] All 12 `DIS-*-cand` claim_ids are CANDIDATE pending Phase 7

All 12 `DIS-*-cand` claim_ids (`DIS-PFKM-01-cand`, `DIS-PKLR-01-cand`,
`DIS-PDHA1-01-cand`, `DIS-ACO2-01-cand`, `DIS-IDH3A-01-cand`,
`DIS-SUCLG1-01-cand`, `DIS-FH-01-cand`, `DIS-MDH2-01-cand`,
`DIS-NDUFS8-01-cand`, `DIS-SDHA-01-cand`, `DIS-CYC1-01-cand`,
`DIS-COX4I1-01-cand`) are CANDIDATE pending Phase 7 approval (NOT approved;
NOT yet in `data/citations.json`). The citation gate is NOT run during Phase
5.1. Phase 7 adds + approves each `DIS-*-cand` claim + the cast-PDB claims
before the `on_enter` loads + `edits.json` entries ship.

### Citation-gate coexistence summary (cross-ref §1)

The skeleton uses `PLACEHOLDER_PHASE7*` claim_ids for 38 nodes (some of which
ALSO carry a `DIS-*-cand` CANDIDATE claim or an APPROVED claim alongside the
placeholder). Additionally, 12 nodes carry `DIS-*-cand` CANDIDATE disease-mutant
claim_ids (added Plans 07-09 — `etc.complex_i` carries ONLY `DIS-NDUFS8-01-cand`,
no placeholder; the other 11 carry DIS-*-cand alongside their placeholder). The
4 nodes with APPROVED claims (`gly.pfk`, `tca.citrate_synthase`,
`tca.aconitase`, `tca.shuffle`) are unchanged. The citation gate
(`tools/check_citations.py --story <path> --registry <path>`)
is a **pre-ship, manually-invoked CLI tool** — it is NOT auto-run on
`data/story_glucose/` during Phase 5.1 (there is no CI/Makefile auto-runner;
the gate tests use their own fixtures under `tests/fixtures/`). The existing
toy fixture (`data/story/intro.json`) uses `placeholder-intro` etc. (NOT in
the registry) and the gate is not broken because it is never run on
`data/story/`. **Phase 7 replaces `PLACEHOLDER_PHASE7*` with real approved
claims AND approves each `DIS-*-cand` candidate, then runs the gate as a
release gate.** The 5 APPROVED claim_ids
(GLY-PFK-01, CAST-PFK-PDB-01, CAST-CSC-PDB-01, TCA-RNG-CITRATE-PROCHIRALITY-01,
TCA-RNG-WEIGHT-01) ARE in the registry and would PASS the gate if it were
run; the 38 `PLACEHOLDER_PHASE7*` nodes + the 12 `DIS-*-cand` candidates
would fail (expected — the skeleton is a design artifact, not shippable
content; the `DIS-*-cand` claims are CANDIDATE, NOT yet in
`data/citations.json`).

---

*Design doc authored: 2026-08-16; updated 2026-08-17 for the tiered-completeness
expansion (34 → 43 nodes, Decisions 1-5); updated 2026-08-20 for the
disease-mutant replan (Plans 07-09: 14 edit-allowed nodes, 0 single-Continue,
15 Continue-to-MC conversions, 8 disease-mutant promotions, 12 DIS-*-cand
CANDIDATE claims, the pyr.pdh 2OZL→6CFO cast fix, the etc.complex_i
PLACEHOLDER→DIS-NDUFS8-01-cand claim_id fix, the CS edit:structural reframe,
the IDH3A-default/IDH1-alternative flag). Cross-references the 43-node skeleton
at `data/story_glucose/*.json` (Plans 01-03 + EXPANSION + 07-09 replan), the
Phase 5.1 research tracks (graph-contracts + pathway-topology + 3
disease-mutant research docs), the Phase 2/4 engine/model/validator/edit_router
source, and the Phase 5 Key Decisions (PROJECT.md rows 104-109). For human
review at Plan 05.1-06.*
