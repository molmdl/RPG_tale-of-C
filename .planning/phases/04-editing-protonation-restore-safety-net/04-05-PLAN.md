---
phase: 04-editing-protonation-restore-safety-net
plan: 05
type: execute
wave: 3
depends_on: ["04-01", "04-03", "04-04"]
files_modified:
  - c14/pymol_layer/molops.py
  - tests/test_molops.py
  - tools/edit_smoke.py
  - tools/protonation_smoke.py
autonomous: true

must_haves:
  truths:
    - "molops.apply(MolAction('edit',...)) delegates to editops (point_mutation/substrate_remove_group/substrate_add_group/protonation_change) — no more NotImplementedError"
    - "molops.apply(MolAction('protonate',...)) delegates to ProtonationManager.apply_variant (no more NotImplementedError)"
    - "molops.apply(MolAction('restore',...)) delegates to editops.restore (no more NotImplementedError)"
    - "A headless smoke confirms a post-edit byres selection returns the expected atoms (SC1 — no silent corruption from the alter->sort trap)"
    - "A headless smoke confirms backup/restore round-trip: post-restore atom count + residue signature == pre-edit (SC2)"
    - "A headless smoke confirms ProtonationManager applies HIS_HID, switches to HIS_HIE, and restores back to HIS (SC4 + EDIT-05 for protonation)"
  artifacts:
    - path: "c14/pymol_layer/molops.py"
      provides: "edit/restore/protonate branches implemented (delegate to EditOps/ProtonationManager); __init__ gains editops + protonation optional params"
      contains: "elif op == \"edit\":"
    - path: "tests/test_molops.py"
      provides: "Updated: edit/restore/protonate dispatch assertions REPLACE the NotImplementedError assertions (backward-compatible: no editops/protonation injected -> still raises RuntimeError, not NotImplementedError)"
      contains: "test_molops_edit_delegates"
    - path: "tools/edit_smoke.py"
      provides: "Headless SC1+SC2 smoke: load _edit_smoke.pdb -> backup -> point_mutation -> byres post-edit -> restore -> verify round-trip; SMOKE_RESULT sentinel"
      contains: "SMOKE_RESULT"
    - path: "tools/protonation_smoke.py"
      provides: "Headless SC4 smoke: load _his_smoke.pdb -> backup -> apply HIS_HID -> switch HIS_HIE -> restore -> verify; SMOKE_RESULT sentinel"
      contains: "SMOKE_RESULT"
  key_links:
    - from: "c14/pymol_layer/molops.py edit branch"
      to: "c14/pymol_layer/edit_ops.py EditOps.point_mutation/substrate_remove_group/substrate_add_group/protonation_change"
      via: "dispatch on args[\"edit_type\"]"
      pattern: "self\\._editops\\.(point_mutation|substrate_remove_group|substrate_add_group|protonation_change)"
    - from: "c14/pymol_layer/molops.py protonate branch"
      to: "c14/pymol_layer/protonation.py ProtonationManager.apply_variant"
      via: "action.args[\"variant_id\"]"
      pattern: "self\\._protonation\\.apply_variant"
    - from: "c14/pymol_layer/molops.py restore branch"
      to: "c14/pymol_layer/edit_ops.py EditOps.restore"
      via: "object_name lookup (handle registry)"
      pattern: "self\\._editops\\.restore"
    - from: "tools/edit_smoke.py"
      to: "count_atoms(\"byres ...\") post-edit (SC1) + restore round-trip (SC2)"
      via: "SMOKE_RESULT: PASS sentinel"
      pattern: "byres"
---

<objective>
Wire the **integration**: replace the `NotImplementedError` branches in `molops.py` (edit/restore/protonate) with delegation to the EditOps (04-01) + ProtonationManager (04-03) built in earlier waves, and prove SC1 + SC2 + SC4 headlessly via two new smoke scripts that exercise the REAL PyMOL cmd.* contract against the committed fixtures (04-04). This is the Wave 3 plan that depends on the full Phase 4 stack: 04-01 (EditOps), 04-03 (ProtonationManager), 04-04 (fixtures + gates).

Purpose: Phase 3 left `molops.py:117-119` raising `NotImplementedError` for `edit`/`protonate`/`restore` as an explicit Phase 4 boundary. Phase 4 now implements these by delegating to the sanctioned helpers. The molops `edit` branch dispatches on `args["edit_type"]` to the right EditOps convenience method; the `protonate` branch delegates to ProtonationManager; the `restore` branch delegates to EditOps.restore. The two headless smokes prove the REAL cmd.* contract end-to-end (the unit tests in 04-01/04-03 proved the dispatch logic with MockCmd; the smokes prove the actual PyMOL API behavior — the alter→sort trap mitigation, the backup/restore round-trip, the protonation switch).

Output: `c14/pymol_layer/molops.py` (edit/restore/protonate branches implemented — THIS PLAN IS THE SOLE OWNER of molops.py per the file-conflict constraint) + `tests/test_molops.py` (updated: NotImplementedError assertions → delegation assertions) + `tools/edit_smoke.py` (SC1+SC2 headless) + `tools/protonation_smoke.py` (SC4 headless).

**This plan owns molops.py** — no other Phase 4 plan touches it (avoids the file-conflict the cross-research coordination flagged).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-editing-protonation-restore-safety-net/04-RESEARCH-editing-safety.md
@.planning/phases/04-editing-protonation-restore-safety-net/04-RESEARCH-protonation.md

# The 04-01 + 04-03 + 04-04 contracts this plan DEPENDS ON (read their SUMMARYs first):
@c14/pymol_layer/edit_ops.py
@c14/pymol_layer/protonation.py
@c14/protonation_catalog.py
@.planning/phases/04-editing-protonation-restore-safety-net/04-01-PLAN.md
@.planning/phases/04-editing-protonation-restore-safety-net/04-03-PLAN.md
@.planning/phases/04-editing-protonation-restore-safety-net/04-04-PLAN.md

# Existing contracts this plan MODIFIES (molops.py) or BUILDS ON:
@c14/pymol_layer/molops.py
@c14/pymol_layer/asset_manager.py
@c14/story/model.py
@tools/run_headless.sh
@tools/molops_smoke.py
@tests/test_molops.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire molops.py edit/restore/protonate branches to delegate to EditOps + ProtonationManager; update tests/test_molops.py (NotImplementedError -> delegation assertions)</name>
  <files>c14/pymol_layer/molops.py, tests/test_molops.py</files>
  <action>
**Modify `c14/pymol_layer/molops.py`** (read it first — it has `MolOps(cmd, asset_manager=None)`, `apply(action)` with 8 implemented ops + `edit`/`protonate`/`restore`/unknown raising `NotImplementedError` at line 117-119).

**Constructor widening (backward-compatible — mirror the `asset_manager=None` pattern at line 80):** add two optional params `editops=None` and `protonation=None` to `MolOps.__init__`:
```python
def __init__(self, cmd, asset_manager=None, editops=None, protonation=None):
    self._cmd = cmd
    self._assets = asset_manager
    self._editops = editops
    self._protonation = protonation
```
This is backward-compatible: existing `MolOps(cmd)` / `MolOps(cmd, am)` calls still work (the new params default to None).

**Replace the `NotImplementedError` else-branch with explicit edit/restore/protonate delegation** (keep `unknown` raising NotImplementedError — a stray op still fails loudly):
```python
elif op == "edit":
    if self._editops is None:
        raise RuntimeError("molops.edit requires an EditOps (editops=None)")
    et = action.args.get("edit_type")
    if et == "point_mutation":
        self._editops.point_mutation(action.target, action.args["sele"], action.args["new_resn"])
    elif et == "substrate_remove_group":
        self._editops.substrate_remove_group(action.target, action.args["group_sele"])
    elif et == "substrate_add_group":
        self._editops.substrate_add_group(action.target, action.args["frag_atom_sele"], action.args["target_atom_sele"])
    elif et == "protonation_change":
        self._editops.protonation_change(action.target, action.args["sele"], action.args["new_resn"], action.args.get("h_ops"))
    else:
        raise ValueError("molops.edit: unknown edit_type {!r}".format(et))
elif op == "protonate":
    if self._protonation is None:
        raise RuntimeError("molops.protonate requires a ProtonationManager (protonation=None)")
    self._protonation.apply_variant(action.target, action.args["variant_id"])
elif op == "restore":
    if self._editops is None:
        raise RuntimeError("molops.restore requires an EditOps (editops=None)")
    self._editops.restore(action.target)
else:
    raise NotImplementedError("molops: unknown op {!r}".format(op))
```

Update the module docstring + the `__init__` docstring to reflect the Phase 4 implementation (edit/restore/protonate now delegate; the NotImplementedError is now ONLY for genuinely unknown ops). Keep the `# src:` citation convention (no new direct cmd.* calls here — the delegation is to editops/protonation, which have their own citations).

**Modify `tests/test_molops.py`** (read it first — it has tests asserting `edit`/`protonate`/`restore` raise `NotImplementedError`). REPLACE those assertions:
- `test_edit_raises_not_implemented` → `test_edit_raises_runtime_error_without_editops`: `MolOps(cmd)` (no editops) + `apply(MolAction("edit", "pep", {...}))` raises `RuntimeError` ("requires an EditOps"), NOT NotImplementedError.
- `test_protonate_raises_not_implemented` → `test_protonate_raises_runtime_error_without_protonation`: same pattern, raises RuntimeError.
- `test_restore_raises_not_implemented` → `test_restore_raises_runtime_error_without_editops`: same pattern.
- ADD `test_edit_delegates_to_editops_point_mutation`: `MolOps(cmd, editops=mock_editops)` + `apply(MolAction("edit", "pep", {"edit_type":"point_mutation","sele":"pep and resi 1","new_resn":"GLY"}))` calls `mock_editops.point_mutation("pep", "pep and resi 1", "GLY")` (assert the MockEditOps recorded the call with the right args).
- ADD `test_edit_delegates_to_editops_protonation_change`: same for edit_type="protonation_change" with h_ops.
- ADD `test_edit_unknown_edit_type_raises_value_error`: `apply(MolAction("edit","pep",{"edit_type":"bogus"}))` raises ValueError.
- ADD `test_protonate_delegates_to_protonation_manager`: `MolOps(cmd, protonation=mock_pm)` + `apply(MolAction("protonate","scene",{"variant_id":"HIS_HID"}))` calls `mock_pm.apply_variant("scene","HIS_HID")`.
- ADD `test_restore_delegates_to_editops`: `MolOps(cmd, editops=mock_editops)` + `apply(MolAction("restore","pep"))` calls `mock_editops.restore("pep")`.

The MockEditOps + MockProtonationManager record calls (name, args, kwargs) like MockCmd. The existing 8-op dispatch tests (hide_all/show/show_as/select_focus/zoom/color/load/delete) MUST stay green (unchanged — the new branches are additive). Keep the `test_citations_present_in_source` test (the existing direct cmd.* calls in molops.py still carry # src: comments; the new delegation branches have no direct cmd.* calls so no new citations needed).

**Run:** `python3.6 -m unittest tests.test_molops -v` + `python3.6 -m unittest discover -s tests` (full suite green — the constructor widening is backward-compatible; the existing tests that called `MolOps(cmd)` still work; only the 3 NotImplementedError assertions changed to RuntimeError + the new delegation tests added).
  </action>
  <verify>
cd /mnt/c/Users/nglok/Desktop/WORKDIR/molmdl/RPG_tale-of-C && python3.6 -m py_compile c14/pymol_layer/molops.py && python3.6 tools/check_imports.py && python3.6 tools/check_alter_gate.py && python3.6 -m unittest tests.test_molops -v && python3.6 -m unittest discover -s tests 2>&1 | tail -5
# py_compile passes; check_imports clean (molops.py in pymol_layer/ = SKIP_DIRS); check_alter_gate exits 0 (molops.py has NO cmd.alter — it delegates); test_molops passes (8 existing ops + 3 updated RuntimeError + 4 new delegation tests); full suite green.
  </verify>
  <done>
molops.py edit/restore/protonate branches delegate to EditOps/ProtonationManager (no NotImplementedError for these 3 ops; unknown ops still raise NotImplementedError). __init__ has editops=None + protonation=None optional params (backward-compatible). tests/test_molops.py updated: 3 NotImplementedError tests -> RuntimeError tests; 4 new delegation tests added. The 8 existing op dispatch tests stay green. check_alter_gate exits 0 (molops.py has no cmd.alter — SC1 gate holds). Full suite green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create tools/edit_smoke.py — headless SC1 (byres post-edit) + SC2 (backup/restore round-trip) smoke exercising EditOps directly</name>
  <files>tools/edit_smoke.py</files>
  <action>
Create `tools/edit_smoke.py` (NEW headless smoke). Follows the `tools/molops_smoke.py:53-180` shape VERBATIM (read it first): the `check(name, ok, detail)` helper, the `FAILS` list, the final `SMOKE_RESULT: {PASS|FAIL}` sentinel (the bat always returns 0 — Gotcha #1; verdict from the sentinel, NOT `$?`). Reuse the critical harness rules: `sys.path.insert(0, os.getcwd())` (Gotcha #2/#3/#4 — cwd=repo root when run via the harness); `import pymol` + `from pymol import cmd`; `pymol.finish_launching()` before any cmd.* call (Gotcha #6); use `import c14.paths` to locate bundled fixtures (its `__file__` is correct).

**Inject the REAL `pymol.cmd` into `EditOps`** (the 3-tier testability pattern — the unit tests in 04-01 inject a MockCmd; THIS smoke injects the real cmd to prove the real API contract). Build: `editops = EditOps(cmd)`.

**Stages (each prints `SMOKE: PASS|FAIL <name> <detail>`; final `SMOKE_RESULT: PASS` iff no fails):**

1. `load`: `cmd.load(<path to _edit_smoke.pdb>, "pep")` — assert `count_atoms("pep") > 0`. Use `c14.paths` to resolve the bundled fixture path (cwd-independent). Wrap the path: `str(c14.paths.data_path("assets/bundled/_edit_smoke.pdb"))` (Phase 1 `c14.paths.data_path` returns a pathlib.Path; str() coerces for PyMOL's string-path APIs — STATE.md:63).
2. `pre_signature`: collect the pre-edit residue signature via `editops._collect_residue_signature("pep")` — assert it's a sorted list like `[("A", 1, "ALA"), ("A", 2, "GLY")]` (or whatever the fixture has). Record `pre_count = cmd.count_atoms("pep")` + `pre_sig`.
3. `apply_point_mutation` (SC1 + SC2 backup): `handle = editops.point_mutation("pep", "pep and resi 1", "GLY")` (ALA at resi 1 → GLY). This calls apply_edit → backup → alter → sort → rebuild. Assert no exception.
4. `sc1_byres_post_edit` (SC1 — no silent corruption): `count_atoms("byres (pep and resi 1)")` returns the expected atom count for the residue (NOT a stale/wrong count from the pre-sort ordering — the alter→sort trap mitigation). Assert `count_atoms("pep and resi 1 and resn GLY") > 0` (the swap took — resi 1 is now GLY). Assert `count_atoms("pep and resi 1 and resn ALA") == 0` (the old resn is gone). This is the SC1 headless proof: a post-edit byres selection returns the expected atoms (no silent corruption).
5. `sc2_restore_round_trip` (SC2 — backup/restore): `editops.restore_from_handle(handle)` (or `editops.restore("pep")` which looks up the handle). Assert `count_atoms("pep") == pre_count` (atom count restored). Assert `editops._collect_residue_signature("pep") == pre_sig` (residue identity restored — the ALA came back). This is the SC2 headless proof: restore returns the object to its pre-edit atom count + residue signature.
6. `backup_independence` (BONUS — Pitfall 9): after a fresh `take_backup("pep")`, mutate the LIVE object (`editops.point_mutation("pep", "pep and resi 1", "GLY")`), then assert `count_atoms("_bak_pep") == pre_count` (the backup is UNCHANGED — `create` made an independent copy; mutating the live object didn't corrupt the backup). Restore the live object after.
7. `substrate_remove_group` (SC2 for a second edit type): `handle = editops.substrate_remove_group("pep", "pep and resi 2 and name CA")` — assert `count_atoms("pep") < pre_count` (an atom was removed). `editops.restore_from_handle(handle)` — assert `count_atoms("pep") == pre_count` (restored). This proves SC2 covers substrate edits, not just point mutations.
8. `protonation_change_via_editops` (SC2 for the third edit type): `handle = editops.protonation_change("pep", "pep and resi 1", "HID")` (a resn rename — the protonation_change edit type, exercised through EditOps directly; the full ProtonationManager path is the protonation_smoke). Assert `count_atoms("pep and resi 1 and resn HID") > 0`. `editops.restore_from_handle(handle)` — assert restored. This proves SC2 covers protonation changes.
9. `final`: print `SMOKE_RESULT: {PASS|FAIL}` (+ `SMOKE_FAILED_STAGES: ...` if any fails).

**Every `cmd.*` call in THIS smoke carries a `# src:` citation** (Phase 3 convention — `cmd.count_atoms`, `cmd.load`). The EditOps internal cmd.* calls are cited in `c14/pymol_layer/edit_ops.py` (verified by 04-01's `test_citations_present_in_source`).

**Do NOT** reproduce the bare-alter trap (don't write a `cmd.alter` without `cmd.sort` anywhere in this smoke — it would violate the alter gate). The smoke exercises `editops.point_mutation` (the sanctioned path), NOT a bare `cmd.alter`.

**Run from repo root:** `bash tools/run_headless.sh tools/edit_smoke.py` (timeout 150s via the harness). Verdict: `grep "^SMOKE_RESULT: PASS"` in the captured stdout (the harness does this).
  </action>
  <verify>
cd /mnt/c/Users/nglok/Desktop/WORKDIR/molmdl/RPG_tale-of-C && bash tools/run_headless.sh tools/edit_smoke.py
# The harness prints "PASSED" + exits 0 if SMOKE_RESULT: PASS is in the output; "FAILED" + exits 1 otherwise. All 9 stages must pass (load, pre_signature, apply_point_mutation, sc1_byres_post_edit, sc2_restore_round_trip, backup_independence, substrate_remove_group, protonation_change_via_editops, final). Inspect /tmp/opencode/edit_smoke.txt on failure.
  </verify>
  <done>
tools/edit_smoke.py exists; `bash tools/run_headless.sh tools/edit_smoke.py` prints "PASSED" (SMOKE_RESULT: PASS). SC1 proven headlessly (post-edit byres returns expected atoms; resn swap took). SC2 proven headlessly for all 3 edit types (point_mutation, substrate_remove_group, protonation_change): backup before edit + restore returns pre-edit atom count + residue signature. Backup independence proven (mutating live object doesn't corrupt backup). No bare cmd.alter in the smoke (alter gate holds).
  </done>
</task>

<task type="auto">
  <name>Task 3: Create tools/protonation_smoke.py — headless SC4 (apply HIS_HID, switch HIS_HIE, restore) smoke exercising ProtonationManager + edit_ops</name>
  <files>tools/protonation_smoke.py</files>
  <action>
Create `tools/protonation_smoke.py` (NEW headless smoke). Follows the `tools/molops_smoke.py:53-180` shape VERBATIM (same `check`/`FAILS`/`SMOKE_RESULT` sentinel pattern, same harness rules).

**Build the real stack** (inject the REAL `pymol.cmd`):
```python
import pymol
from pymol import cmd
import c14.paths
from c14.pymol_layer.edit_ops import EditOps
from c14.pymol_layer.asset_manager import AssetManager
from c14.pymol_layer.protonation import ProtonationManager
import c14.protonation_catalog as catalog

pymol.finish_launching()
editops = EditOps(cmd)
assets = AssetManager(cmd)
pm = ProtonationManager(cmd, editops, catalog, assets)
```

**Stages (each prints `SMOKE: PASS|FAIL <name> <detail>`; final `SMOKE_RESULT: PASS` iff no fails):**

1. `load`: `assets.load_bundled("_his_smoke.pdb", "scene")` (AssetManager resolves the bundled fixture cwd-independently — Phase 3). Assert `count_atoms("scene") > 0` + `count_atoms("scene and resn HIS") >= 1` (the fixture has a HIS residue).
2. `pre_state`: record `pre_count = count_atoms("scene")` + `pre_h_count = count_atoms("scene and elem H")` + confirm `count_atoms("scene and resn HIS and name HD1") >= 1` + `count_atoms("scene and resn HIS and name HE2") >= 1` (both H atoms present in the fixture — so remove has something to operate on).
3. `apply_hid` (SC4 — apply a curated variant): `pm.apply_variant("scene", "HIS_HID")`. This routes through `_apply_alter` → `editops.apply_edit("scene", [remove("resn HIS and name HE2"), alter("scene","resn='HID'"), h_add("resn HID and name ND1")])` (removes BEFORE alter, adds AFTER — Pitfall 2). Assert `count_atoms("scene and resn HID") >= 1` (resn changed to HID) + `count_atoms("scene and resn HID and name HD1") >= 1` (the Ndelta-H present) + `count_atoms("scene and resn HID and name HE2") == 0` (the Nepsilon-H absent — removed). Assert `pm.current_variant("scene") == "HIS_HID"`.
4. `switch_hie` (SC4 — user-adjustable switch): `pm.switch_variant("scene", "HIS_HIE")`. This takes a FRESH backup (EDIT-05 covers the switch) then applies HIE. Assert `count_atoms("scene and resn HIE") >= 1` + `count_atoms("scene and resn HIE and name HE2") >= 1` (Nepsilon-H present) + `count_atoms("scene and resn HIE and name HD1") == 0` (Ndelta-H absent). Assert `pm.current_variant("scene") == "HIS_HIE"`.
5. `restore` (EDIT-05 — restore safety net covers protonation): `pm.restore("scene")` (delegates to `editops.restore("scene")` — the handle from the last apply_variant). Assert `count_atoms("scene") == pre_count` (atom count restored) + `count_atoms("scene and resn HIS") >= 1` (resn back to HIS) + `count_atoms("scene and elem H") == pre_h_count` (H count restored — round-trip equality). Assert `pm.current_variant("scene") is None` (switch state cleared).
6. `backup_taken_before_variant` (EDIT-05 proof — backup/restore covers protonation): Apply `HIS_HID` again; assert `editops._handles.get("scene") is not None` (a backup handle was registered by apply_edit). `pm.restore("scene")` — assert restored. This proves every protonation change takes a backup via edit_ops (EDIT-05 covers protonation automatically).
7. `list_variants` (SC4 support): `pm.list_variants("HIS")` returns a list including `("HIS_HID", ...)`, `("HIS_HIE", ...)`, `("HIS_HIP", ...)` (the catalog has all 3 HIS tautomers). Assert the list has >= 3 entries.
8. `final`: print `SMOKE_RESULT: {PASS|FAIL}` (+ `SMOKE_FAILED_STAGES: ...` if any fails).

**Every `cmd.*` call in THIS smoke carries a `# src:` citation** (Phase 3 convention). The ProtonationManager + EditOps internal cmd.* calls are cited in their respective modules (verified by 04-01 + 04-03 citation tests).

**Do NOT** call `cmd.alter` or `cmd.h_add` directly in this smoke (it would violate the alter gate — ProtonationManager delegates to edit_ops). The smoke exercises `pm.apply_variant` (the sanctioned delegation path).

**Tolerance for fixture reality:** the exact H counts depend on the fixture geometry + PyMOL's h_add behavior. If the smoke reveals that `h_add` on the HIS fixture doesn't produce the exact expected H (e.g. h_add adds an H to a different atom than expected), ADJUST the assertions to match the observed-but-correct behavior, OR adjust the catalog h_ops selections to target the right atoms. The goal is to prove the MECHANICS (alter + remove + h_add + sort + rebuild + backup + restore) work endlessly on a real HIS, not to prove a specific tautomer geometry (that's Phase 5+ cited content). Document any adjustments in the smoke's comments. If `h_add` proves flaky on the fixture, fall back to asserting the resn change + the remove took (skip the h_add assertion) + the restore round-trip — the core SC4 mechanic is the alter+restore, not the exact H placement.

**Run from repo root:** `bash tools/run_headless.sh tools/protonation_smoke.py`. Verdict: `grep "^SMOKE_RESULT: PASS"`.
  </action>
  <verify>
cd /mnt/c/Users/nglok/Desktop/WORKDIR/molmdl/RPG_tale-of-C && bash tools/run_headless.sh tools/protonation_smoke.py
# The harness prints "PASSED" + exits 0 if SMOKE_RESULT: PASS is in the output. All 8 stages must pass (load, pre_state, apply_hid, switch_hie, restore, backup_taken_before_variant, list_variants, final). Inspect /tmp/opencode/protonation_smoke.txt on failure. If h_add behavior differs from the assertion, adjust the assertions OR the catalog h_ops + document the adjustment.
  </verify>
  <done>
tools/protonation_smoke.py exists; `bash tools/run_headless.sh tools/protonation_smoke.py` prints "PASSED" (SMOKE_RESULT: PASS). SC4 proven headlessly: ProtonationManager applies HIS_HID (alter + remove HE2 + h_add ND1 + sort + rebuild), switches to HIS_HIE (fresh backup + alter + remove HD1 + h_add NE2), and restores back to HIS (EDIT-05 covers protonation — restore returns pre-protonation atom count + resn + H count). The switch is exercisable (current_variant tracks). list_variants returns the 3 HIS tautomers. No direct cmd.alter/cmd.h_add in the smoke (alter gate holds).
  </done>
</task>

</tasks>

<verification>
1. `python3.6 -m py_compile c14/pymol_layer/molops.py tools/edit_smoke.py tools/protonation_smoke.py` — syntax clean.
2. `python3.6 tools/check_imports.py` — gate clean (molops.py in pymol_layer/ = SKIP_DIRS; the smokes are in tools/ = not scanned by check_imports which scans c14/ only).
3. `python3.6 tools/check_alter_gate.py` — exits 0 (molops.py + the smokes have NO cmd.alter — they delegate to edit_ops; SC1 gate holds repo-wide).
4. `python3.6 -m unittest tests.test_molops -v` — the 3 updated RuntimeError tests + 4 new delegation tests pass; the 8 existing op dispatch tests stay green.
5. `python3.6 -m unittest discover -s tests` — full suite green (123 prior + 04-01 + 04-02 + 04-03 + 04-05 new tests, zero regressions).
6. `bash tools/run_headless.sh tools/edit_smoke.py` — prints "PASSED" (SC1 byres + SC2 round-trip for 3 edit types proven headlessly).
7. `bash tools/run_headless.sh tools/protonation_smoke.py` — prints "PASSED" (SC4 apply HID + switch HIE + restore proven headlessly; EDIT-05 covers protonation).
</verification>

<success_criteria>
- molops.apply(MolAction("edit",...)) delegates to editops (point_mutation/substrate_remove_group/substrate_add_group/protonation_change by edit_type) — no NotImplementedError for edit (test_edit_delegates_to_editops_*).
- molops.apply(MolAction("protonate",...)) delegates to ProtonationManager.apply_variant — no NotImplementedError for protonate (test_protonate_delegates_to_protonation_manager).
- molops.apply(MolAction("restore",...)) delegates to editops.restore — no NotImplementedError for restore (test_restore_delegates_to_editops).
- Unknown ops still raise NotImplementedError (a stray op fails loudly — the boundary is preserved for genuinely unknown ops).
- The constructor widening (editops=None + protonation=None) is backward-compatible (existing MolOps(cmd) / MolOps(cmd, am) calls still work; the 8 existing op tests stay green).
- Headless edit_smoke proves SC1 (post-edit byres returns expected atoms — no silent corruption) + SC2 (backup/restore round-trip for point_mutation + substrate_remove_group + protonation_change — atom count + residue signature restored).
- Headless protonation_smoke proves SC4 (apply HIS_HID + switch HIS_HIE + restore to HIS) + EDIT-05 (every protonation change takes a backup via edit_ops; restore returns pre-protonation state).
- The alter gate (check_alter_gate.py) still exits 0 — molops.py + the smokes have no cmd.alter (they delegate); SC1 holds repo-wide.
- Full test suite green with zero regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-editing-protonation-restore-safety-net/04-05-SUMMARY.md`. Record: the molops delegation contract (edit/restore/protonate branches + editops/protonation __init__ params + the args schema for each edit_type), the backward-compatible constructor widening, the two headless smoke stage lists + their SMOKE_RESULT outcomes, any h_add fixture-reality adjustments, and the test count delta. This SUMMARY is the Phase 4 integration record — downstream phases (5.1 edit-node contract, 6 MVP controller) read it to understand the molops edit/protonate/restore dispatch.
</output>
