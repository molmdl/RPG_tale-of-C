---
phase: 04-editing-protonation-restore-safety-net
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - tools/check_alter_gate.py
  - tools/check_edit_coverage.py
  - c14/data/cast.json
  - c14/data/edits.json
  - c14/data/assets/bundled/_edit_smoke.pdb
  - c14/data/assets/bundled/_his_smoke.pdb
autonomous: true

must_haves:
  truths:
    - "The alter gate (check_alter_gate.py) passes when cmd.alter appears ONLY in c14/pymol_layer/edit_ops.py (the allowlist)"
    - "The alter gate FAILS (exit 1) when a cmd.alter call appears anywhere outside edit_ops.py (e.g. a stray cmd.alter in molops.py or protonation.py)"
    - "The alter gate treats .alter(...) Attribute calls uniformly (catches cmd.alter, self._cmd.alter, pymol.cmd.alter — AST-precise, no false positives on comments/strings)"
    - "The coverage scan (check_edit_coverage.py) is GREEN (exit 0) on the placeholder cast: every enzyme in cast.json has >=1 entry in edits.json"
    - "The coverage scan FAILS (exit 1) when an enzyme in cast.json has no edits.json entry"
    - "The _edit_smoke.pdb fixture loads headlessly with 2 residues (ALA-GLY) so a resn swap + byres + restore are meaningful"
    - "The _his_smoke.pdb fixture loads headlessly with a HIS residue + explicit H atoms (HD1, HE2) so Mode (b) remove/add has something to operate on"
  artifacts:
    - path: "tools/check_alter_gate.py"
      provides: "AST gate enforcing cmd.alter only in c14/pymol_layer/edit_ops.py (exit 0/1/2)"
      contains: "ALLOWLIST"
      min_lines: 60
    - path: "tools/check_edit_coverage.py"
      provides: "Per-enzyme min-coverage scan (every cast enzyme has >=1 edits.json entry; exit 0/1/2)"
      contains: "def main"
      min_lines: 50
    - path: "c14/data/cast.json"
      provides: "PLACEHOLDER cast manifest (one demo enzyme: fixture_enzyme_1 with its fixture)"
    - path: "c14/data/edits.json"
      provides: "PLACEHOLDER edits manifest (one demo known-edit entry for fixture_enzyme_1)"
    - path: "c14/data/assets/bundled/_edit_smoke.pdb"
      provides: "2-residue peptide fixture (ALA-GLY) for the SC1+SC2 headless smoke (04-05)"
    - path: "c14/data/assets/bundled/_his_smoke.pdb"
      provides: "Histidine fixture with explicit H atoms (HD1, HE2) for the SC4 protonation headless smoke (04-05)"
  key_links:
    - from: "tools/check_alter_gate.py"
      to: "AST scan of c14/ + tools/ for .alter(...) Attribute calls"
      via: "allowlist = {c14/pymol_layer/edit_ops.py}"
      pattern: "ALLOWLIST"
    - from: "tools/check_edit_coverage.py"
      to: "c14/data/cast.json + c14/data/edits.json cross-reference"
      via: "every cast enzyme has >=1 edits.json entry"
      pattern: "cast\\.json.*edits\\.json"
---

<objective>
Build the **two enforcement gates** (SC1 machine-checkable + SC5 per-enzyme coverage scan) + the **4 placeholder data/fixture files** that 04-05's headless smokes need. This plan makes SC1 ("apply_edit is the only sanctioned alter path — grep finds no bare cmd.alter outside it") a machine-checked gate via AST, and SC5 ("per-enzyme minimum-coverage scan is green on the current enzyme set") a CI-style data-driven scan. It also creates the two small committed PDB fixtures (no network) that the 04-05 headless smokes load.

Purpose: The alter→sort trap (SC1) is the highest-risk item in Phase 4. A unit test proves apply_edit sorts; but a stray `cmd.alter` elsewhere in the repo would bypass the safety net. The AST gate makes "no bare cmd.alter outside edit_ops.py" a hard, machine-checkable invariant (not a grep that false-positives on comments). The coverage scan (SC5) makes "every cast enzyme has >=1 known-edit entry" a data-driven check that stays green as the cast grows (add an enzyme to cast.json → scan FAILS until edits.json has an entry — forces coverage discipline).

Output: `tools/check_alter_gate.py` (AST gate) + `tools/check_edit_coverage.py` (coverage scan) + `c14/data/cast.json` + `c14/data/edits.json` (PLACEHOLDER manifests) + `c14/data/assets/bundled/_edit_smoke.pdb` + `c14/data/assets/bundled/_his_smoke.pdb` (committed fixtures).

**Phase 4 boundary:** the manifests use PLACEHOLDER enzyme ids + PLACEHOLDER claim_ids. Real cast (CAST-01, ~20+ enzymes with real PDB IDs + citations) is Phase 9; real edits.json (per-enzyme known-edit entries with real claim_ids) is Phase 5+. The fixtures are NOT real catalytic-residue extracts — they are minimal structures for mechanics testing (no claim_id).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-editing-protonation-restore-safety-net/04-RESEARCH-editing-safety.md

# The 04-01 contract this plan DEPENDS ON (the gate allowlists edit_ops.py — it must exist):
@c14/pymol_layer/edit_ops.py
@.planning/phases/04-editing-protonation-restore-safety-net/04-01-PLAN.md

# Existing contracts this plan BUILDS ON:
@tools/check_imports.py
@tools/check_citations.py
@tools/run_headless.sh
@tools/molops_smoke.py
@c14/paths.py
@c14/data/selfcheck.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tools/check_alter_gate.py (AST gate: cmd.alter only in edit_ops.py) + tools/check_edit_coverage.py (per-enzyme min-coverage scan)</name>
  <files>tools/check_alter_gate.py, tools/check_edit_coverage.py</files>
  <action>
**Create `tools/check_alter_gate.py`** (NEW AST gate). Extends the `tools/check_imports.py:44-74` AST pattern (read it first). The rule: any `*.alter(...)` Attribute call (i.e. `ast.Call` whose `func` is an `ast.Attribute` with `attr == "alter"`) may appear ONLY in `c14/pymol_layer/edit_ops.py`. Nowhere else in `c14/` (INCLUDING `c14/pymol_layer/` — unlike check_imports which skips it) or `tools/`.

**Mechanism:** Walk ALL `.py` files under `c14/` (do NOT skip `pymol_layer/` or `ui/` — the alter gate scans them too, unlike check_imports) AND `tools/`. For each file, `ast.parse` the source; for each `ast.Call` node, check if `node.func` is an `ast.Attribute` with `node.func.attr == "alter"`. If so, record the file path + line UNLESS the file path (relative to repo root) == `c14/pymol_layer/edit_ops.py` (the allowlist). AST on `Attribute(attr='alter')` is precise — it catches `cmd.alter(...)`, `self._cmd.alter(...)`, `pymol.cmd.alter(...)` uniformly, and will NOT false-positive on the word "alter" in comments/strings/docstrings (AST doesn't see those as Call nodes).

**ALLOWLIST** = exactly one file: `c14/pymol_layer/edit_ops.py`. (ProtonationManager at `c14/pymol_layer/protonation.py` is NOT in the allowlist — it delegates to edit_ops.apply_edit, so it has no `cmd.alter` call. The 04-03 plan's `test_protonation_manager_no_direct_alter` asserts this at the unit-test level; THIS gate is the repo-wide enforcement.)

**Exit codes** (Phase 1 convention, `check_citations.py:6-11`): `0` = clean (no alter calls outside the allowlist), `1` = violation (bare alter outside edit_ops.py), `2` = ERROR (AST parse failure / config error — e.g. a .py file with a SyntaxError). Print a clear report to stderr on violation: the file:line of each offending alter call + a message "cmd.alter is only allowed in c14/pymol_layer/edit_ops.py (the sanctioned apply_edit helper); route other alter needs through EditOps.apply_edit."

**Self-test:** the gate MUST exit 0 on the current repo (04-01 has edit_ops.py with the sanctioned alter; 04-03 protonation.py has none; no other file has one — confirmed by the baseline `rg "cmd\.alter|\.alter\("` returning NONE). To prove the gate FAILS on a violation, include a `--selftest` mode (or a separate test) that injects a fake `cmd.alter` into a temp file + asserts exit 1. Alternatively, add a unit test `tests/test_check_alter_gate.py` with two cases: (a) a temp dir with only edit_ops.py + an alter call → exit 0; (b) a temp dir with a stray alter in molops.py → exit 1. (Pick the approach that fits the existing test conventions — a standalone test file is cleaner. If adding a test file, include it in files_modified for this task.)

**Create `tools/check_edit_coverage.py`** (NEW pure-Python scan, mirrors `check_citations.py` structure). Loads `c14/data/cast.json` (the cast manifest — enzymes represented so far) + `c14/data/edits.json` (the known-edit entries per enzyme). For each enzyme id in cast.json, assert it has >=1 entry in edits.json's `enzymes` dict (i.e. `enzyme_id in edits["enzymes"]` and `len(edits["enzymes"][enzyme_id].get("edits", [])) >= 1`). Re-implement the scan INLINE (do NOT import c14.edit_router — to keep this tool independent of c14.edit_router, matching the `check_citations.py` does-its-own-loading precedent; this makes 04-04 cleanly independent of 04-02 so its `depends_on: ["04-01"]` is honest).

**Exit codes** (Phase 1 convention): `0` = PASS (all cast enzymes have >=1 known-edit entry), `1` = FAIL (some enzyme missing), `2` = ERROR (malformed JSON / missing file). Print a clear report: which enzymes are covered + which are missing.

**Data-driven:** the scan reads cast.json + edits.json at runtime — add an enzyme to cast.json → scan picks it up → FAILS until edits.json has an entry (forces coverage discipline). Phase 4 placeholder: both manifests have one demo enzyme (fixture_enzyme_1) with one demo edit entry → scan is GREEN.

Python 3.6 only (no @dataclass; .format() strings; subprocess uses stdout/stderr=PIPE not capture_output — Phase 1 Pitfall 3, though these tools don't subprocess). Both tools are importable in pure WSL python3.6 (no pymol).

Run: `python3.6 tools/check_alter_gate.py` (exit 0) + `python3.6 tools/check_edit_coverage.py` (exit 0 — but this requires cast.json + edits.json from Task 2; if Task 2 isn't done yet, the scan will exit 2 with "file not found" — that's expected; the verify step runs AFTER both tasks).
  </action>
  <verify>
cd /mnt/c/Users/nglok/Desktop/WORKDIR/molmdl/RPG_tale-of-C && python3.6 -m py_compile tools/check_alter_gate.py tools/check_edit_coverage.py && python3.6 tools/check_alter_gate.py; echo "alter_gate exit=$?"
# py_compile passes; check_alter_gate.py exits 0 (the repo has cmd.alter ONLY in edit_ops.py — confirmed by the baseline grep). check_edit_coverage.py is verified in Task 2's verify (needs cast.json + edits.json).
  </verify>
  <done>
tools/check_alter_gate.py exists (AST gate, ALLOWLIST={c14/pymol_layer/edit_ops.py}, exit 0/1/2); exits 0 on the current repo. tools/check_edit_coverage.py exists (per-enzyme min-coverage scan, exit 0/1/2); py_compile passes. If a test file was added for the alter gate self-test, it passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create c14/data/cast.json + c14/data/edits.json (PLACEHOLDER manifests) + c14/data/assets/bundled/_edit_smoke.pdb (ALA-GLY peptide) + c14/data/assets/bundled/_his_smoke.pdb (histidine with explicit H)</name>
  <files>c14/data/cast.json, c14/data/edits.json, c14/data/assets/bundled/_edit_smoke.pdb, c14/data/assets/bundled/_his_smoke.pdb</files>
  <action>
**Create `c14/data/cast.json`** (PLACEHOLDER cast manifest — real cast is Phase 9):
```json
{
  "version": 1,
  "enzymes": [
    {"id": "fixture_enzyme_1", "label": "Fixture Enzyme 1 (placeholder)", "fixture": "_edit_smoke.pdb", "claim_id": "PLACEHOLDER_PHASE5"}
  ]
}
```
Note: the schema is a list of enzyme objects with id/label/fixture/claim_id. Phase 9 will add real PDB IDs + resolutions + real claim_ids (CAST-01). Phase 4 has ONE placeholder enzyme so the coverage scan is green.

**Create `c14/data/edits.json`** (PLACEHOLDER edits manifest — real edits are Phase 5+):
```json
{
  "version": 1,
  "bad_ending_pool": ["bad.lost_connection", "bad.released_from_host"],
  "enzymes": {
    "fixture_enzyme_1": {
      "edits": [
        {
          "signature": {"op": "point_mutation", "target": "resi 1", "args": {"new_res": "GLY"}},
          "branch_node": "fixture.branch_1",
          "claim_id": "PLACEHOLDER_PHASE5"
        }
      ]
    }
  }
}
```
Note: this is the REAL-DATA manifest at `c14/data/edits.json` (distinct from the SC3 TEST FIXTURE at `tests/fixtures/edit_routing/edits.json` created by 04-02 — different files, different roles). The signature here is a placeholder (resi 1 → GLY) matching the _edit_smoke.pdb fixture (ALA at resi 1 → GLY). The branch_node "fixture.branch_1" is a placeholder (the real story graph is Phase 5.1). The bad_ending_pool references the spec's bad endings (PROJECT.md Key Decisions).

**Create `c14/data/assets/bundled/_edit_smoke.pdb`** (a minimal 2-residue ALA-GLY peptide — committed, no network). This is the SC1+SC2 headless smoke fixture (04-05). It MUST:
- Have 2 residues: ALA at resi 1, GLY at resi 2 (so a resn swap ALA→GLY at resi 1 is meaningful + a byres selection + restore round-trip are demonstrable).
- Have enough atoms (N, CA, C, O per residue + a few sidechain H) that count_atoms + iterate are meaningful (~10-15 atoms).
- Be a valid PDB (loads headlessly via `cmd.load`).

**Recommended generation approach** (run headlessly once, then commit the output): write a small one-time generator script `tools/_gen_edit_smoke_fixture.py` (NOT committed — delete after use; OR commit it under tools/ if it's reusable) that runs via `bash tools/run_headless.sh` and uses `cmd.fab("AG", "pep")` (PyMOL's fragment-build for an ALA-GLY dipeptide) + `cmd.h_add("pep")` + `cmd.save("c14/data/assets/bundled/_edit_smoke.pdb", "pep")`. Verify the saved file loads + has 2 residues (ALA, GLY) + count_atoms > 0. If `cmd.fab` is unavailable or flaky, FALL BACK to hand-writing a minimal PDB with ATOM records for ALA (N, CA, C, O, CB, H) + GLY (N, CA, C, O, H2) — the geometry need not be perfect, just valid enough for PyMOL to load + alter resn + byres + count. Read `tmp/pymol-src/modules/pymol/` for the `fab`/`fragment` API if needed.

**Create `c14/data/assets/bundled/_his_smoke.pdb`** (a minimal histidine residue with explicit H atoms — committed, no network). This is the SC4 protonation headless smoke fixture (04-05). It MUST:
- Have ONE HIS residue (resn="HIS", recognized by PyMOL's residue templates) so `cmd.alter(sele, "resn='HID'")` works.
- Have explicit H atoms on the ring INCLUDING HD1 (on ND1) AND HE2 (on NE2) so Mode (b) `remove("resn HIS and name HE2")` + `h_add("resn HID and name ND1")` have something to operate on.
- Be small (one residue is enough) for fast headless runs.
- Be a PLACEHOLDER fixture (NOT a real catalytic-residue extract — no claim_id; just "a histidine for mechanics testing"). Note this in a header comment if generating via script.

**Recommended generation approach:** write `tools/_gen_his_smoke_fixture.py` (NOT committed — delete after use) that runs headlessly and uses `cmd.fab("HIS", "his")` + `cmd.h_add("his")` + `cmd.save("c14/data/assets/bundled/_his_smoke.pdb", "his")`. Verify the saved file loads + has resn=="HIS" + count_atoms("name HD1") >= 1 + count_atoms("name HE2") >= 1 (both H atoms present). If `cmd.fab` is unavailable, FALL BACK to extracting a single HIS residue from a small bundled real structure OR hand-writing a minimal HIS PDB with the ring atoms (ND1, CE1, NE2, CD2, CG, CB, CA, N, C, O) + HD1 + HE2. The HIS ring geometry is standard (available in any biochemistry reference); the exact coordinates need not be perfect — just valid enough for PyMOL to load + alter + h_add.

**Verify both fixtures load headlessly** (this proves they're valid PDBs that 04-05 can use):
```bash
bash tools/run_headless.sh tools/_verify_smoke_fixtures.py
```
where `tools/_verify_smoke_fixtures.py` is a tiny one-time script (delete after use) that loads each fixture + asserts count_atoms > 0 + the expected residues. OR inline the verification into the generator scripts. The committed fixtures must be valid.

**Do NOT commit** the generator scripts (`_gen_*.py`, `_verify_*.py`) if they're one-time-use — delete them after generating the fixtures (or commit them under tools/ only if they're reusable for future fixture regen; the repo convention is to keep tools/ lean). The COMMITTED artifacts are the 2 .pdb files + cast.json + edits.json.
  </action>
  <verify>
cd /mnt/c/Users/nglok/Desktop/WORKDIR/molmdl/RPG_tale-of-C && python3.6 -c "import json; c=json.load(open('c14/data/cast.json')); e=json.load(open('c14/data/edits.json')); print('cast enzymes:', [x['id'] for x in c['enzymes']]); print('edits enzymes:', list(e['enzymes'].keys()))" && python3.6 tools/check_edit_coverage.py; echo "coverage exit=$?"
# Both JSON files valid; check_edit_coverage.py exits 0 (fixture_enzyme_1 has >=1 edits entry). Then verify the PDB fixtures load headlessly (one-time verification script via run_headless.sh — confirms count_atoms > 0 + expected residues). The committed _edit_smoke.pdb + _his_smoke.pdb must exist and be valid.
  </verify>
  <done>
c14/data/cast.json (1 placeholder enzyme) + c14/data/edits.json (1 placeholder edit entry + global bad_ending_pool) exist as valid JSON. check_edit_coverage.py exits 0 (green on the placeholder cast — SC5). c14/data/assets/bundled/_edit_smoke.pdb (2-residue ALA-GLY) + _his_smoke.pdb (HIS with HD1 + HE2) exist as valid PDBs (verified headlessly: load + count_atoms > 0 + expected residues/H atoms). One-time generator/verifier scripts deleted (not committed).
  </done>
</task>

</tasks>

<verification>
1. `python3.6 -m py_compile tools/check_alter_gate.py tools/check_edit_coverage.py` — syntax clean.
2. `python3.6 tools/check_alter_gate.py` — exits 0 (repo has cmd.alter ONLY in edit_ops.py; SC1 machine-checkable).
3. `python3.6 tools/check_edit_coverage.py` — exits 0 (fixture_enzyme_1 has >=1 edits entry; SC5 green on placeholder cast).
4. `python3.6 tools/check_imports.py` — still clean (the new tools/ files import only stdlib; no pymol).
5. `python3.6 -m unittest discover -s tests` — full suite green (the new tools don't break existing tests; if a test_check_alter_gate.py was added, it passes).
6. `c14/data/cast.json` + `c14/data/edits.json` are valid JSON with the placeholder schema.
7. `c14/data/assets/bundled/_edit_smoke.pdb` + `_his_smoke.pdb` load headlessly (verified via run_headless.sh one-time script) with the expected residues + H atoms.
</verification>

<success_criteria>
- The alter gate (check_alter_gate.py) passes (exit 0) when cmd.alter is only in edit_ops.py, and FAILS (exit 1) when a stray cmd.alter appears elsewhere — SC1 is now machine-checkable, not just unit-tested.
- The alter gate uses AST (Attribute attr=="alter") — no false positives on comments/strings; catches cmd.alter/self._cmd.alter/pymol.cmd.alter uniformly.
- The coverage scan (check_edit_coverage.py) is GREEN (exit 0) on the placeholder cast (fixture_enzyme_1 has >=1 edits entry) — SC5.
- The coverage scan is data-driven (reads cast.json + edits.json at runtime) — stays green as the cast grows, FAILS when an enzyme lacks coverage.
- The _edit_smoke.pdb fixture (2-residue ALA-GLY) + _his_smoke.pdb fixture (HIS with HD1 + HE2) are committed, valid, network-independent, and load headlessly — ready for 04-05's smokes.
- The placeholder manifests (cast.json + edits.json) use PLACEHOLDER enzyme ids + claim_ids (Phase 4 boundary; real cast/edits are Phase 5+/9).
</success_criteria>

<output>
After completion, create `.planning/phases/04-editing-protonation-restore-safety-net/04-04-SUMMARY.md`. Record: the alter gate ALLOWLIST + AST mechanism + exit codes, the coverage scan data-driven design + exit codes, the placeholder manifest schemas (cast.json + edits.json), the fixture generation approach (fab/hand-write fallback) + the committed fixture details (atom/residue counts), and the test count delta.
</output>
