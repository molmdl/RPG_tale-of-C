---
phase: 05-pre-content-key-decisions-source-approval
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - data/sources.json
  - data/citations.json
  - data/citations.README.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "data/sources.json exists with the first-batch candidate source records for the glucose critical-path + TCA RNG weights (all marked approval_status=pending — NOT auto-approved)"
    - "data/citations.json contains the first-batch seed claim entries (all marked approval_status=pending) with the extended schema fields (source_id, review_tier, claim_text, inherits_source_approval, review_notes)"
    - "The Phase 1 gate (tools/check_citations.py) still loads the extended data/citations.json without error — backward-compatible (loader ignores unknown fields; only validates approval_status)"
    - "data/citations.README.md documents the new fields (source_id, review_tier, inherits_source_approval, review_notes, claim_text) + the new data/sources.json file"
    - "NO source or claim is auto-approved — every entry is pending; the human approves them in Plan 05"
  artifacts:
    - path: "data/sources.json"
      provides: "Source registry for the batch-by-source / hybrid / per-source-record workflow (candidate sources for the glucose critical-path + TCA RNG weights, pending)"
      contains: "LIBRETEXTS-METAB-GLYCOLYSIS"
    - path: "data/citations.json"
      provides: "Extended claims registry with seed first-batch claim entries (pending) demonstrating the source_id + review_tier schema"
      contains: "TCA-RNG-CITRATE-PROCHIRALITY-01"
    - path: "data/citations.README.md"
      provides: "Updated schema docs covering the new fields + the new sources.json file"
      contains: "source_id"
  key_links:
    - from: "data/citations.json (seed claim entries)"
      to: "data/sources.json (source records)"
      via: "source_id field references a source_id key in sources.json"
      pattern: "source_id"
    - from: "tools/check_citations.py"
      to: "data/citations.json"
      via: "CitationRegistry.load — must still load cleanly with the extended fields (ignores unknown fields)"
      pattern: "approval_status"
---

<objective>
Implement the source-approval workflow schema chosen in Plan 03 and populate the first batch of PENDING candidate sources + seed claims for the glucose critical-path enzymes + TCA RNG weights — the front-loaded source approval that SC#4 requires. Every entry is marked `pending`; NO source or claim is auto-approved (the human approves them in Plan 05's checkpoint).

Purpose: SC#4 requires "the claims registry holds the first batch of approved sources for the glucose critical-path enzymes + TCA RNG weights (front-loaded source approval)." This plan creates the registry STRUCTURE and populates the PENDING entries from the research; Plan 05's human checkpoint flips them to `approved`. The schema is backward-compatible with the Phase 1 gate: `c14/citations.py:87-97` only validates that each entry is a dict with `approval_status ∈ {pending, approved, rejected}` and ignores unknown fields — so adding `source_id`, `review_tier`, `claim_text`, `inherits_source_approval`, `review_notes` is transparent. A new `data/sources.json` file holds the source records (the Phase 1 gate never reads it — verified in `05-RESEARCH-source-approval.md` §2).

Output: `data/sources.json` (new — candidate source records, pending), `data/citations.json` (extended — seed claim entries, pending), `data/citations.README.md` (updated schema docs). This is the FIRST-BATCH scope per SC#4: glucose critical-path (glycolysis 10 enzymes + PDH 3 enzymes + TCA 8 enzymes) + TCA RNG weights. The anaerobic/ATP-soul-jump/C14-decay sources are NOT in this batch (they're candidate sources for later phases — Phase 7/9; this keeps Plan 04 within context budget and matches SC#4's stated scope).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-pre-content-key-decisions-source-approval/05-RESEARCH-source-approval.md
@.planning/phases/05-pre-content-key-decisions-source-approval/05-03-SUMMARY.md
@data/citations.README.md
@c14/citations.py
@tools/check_citations.py
@data/citations.json
@tests/fixtures/citations_pass.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create data/sources.json + extend data/citations.json schema docs in data/citations.README.md (branch on Plan 03's workflow decision)</name>
  <files>data/sources.json, data/citations.README.md</files>
  <action>
Read `.planning/phases/05-pre-content-key-decisions-source-approval/05-03-SUMMARY.md` to get the human's chosen workflow from Plan 03. Branch on it:

IF the workflow is (a) STRICT PER-CLAIM: SKIP creating data/sources.json (strict per-claim doesn't need a source registry). Still update data/citations.README.md to document that the workflow is strict-per-claim (no sources.json). Skip the rest of this task's sources.json work and proceed to Task 2 (which will populate citations.json seed claims WITHOUT source_id fields, since strict per-claim doesn't use source inheritance).

IF the workflow is (b) BATCH-BY-SOURCE / (c) HYBRID / (d) PER-SOURCE-RECORD: create `data/sources.json` as a bare JSON object keyed by `source_id` (no top-level wrapper — mirrors the citations.json shape). Use the EXACT schema from `05-RESEARCH-source-approval.md` §2 "Recommended: two files, gate unchanged". Each source record has fields: `reference` (string), `url` (string, optional), `license` (string, optional), `source_type` (enum: "educational_open_textbook" | "print_textbook" | "structural_database_entry" | "primary_literature" | "nuclear_database"), `approval_status` (enum: "pending" | "approved" | "rejected" — ALL set to "pending" here), `approved_by` (omit until approved), `approved_date` (omit until approved), `notes` (string). Add a duplicate-key hook convention note in the README (the sources.json loader, if built later, should use the same object_pairs_hook as citations.py — but DO NOT build the sources.json loader or check_sources.py in this plan; they are deferred optional work per the research's "advisory, not required" note; the Phase 1 gate does not read sources.json).

Update `data/citations.README.md`:
- Add a new section "## `data/sources.json` — Source Registry (Phase 5+)" documenting: the file is a bare JSON object keyed by `source_id`; the Phase 1 gate NEVER reads it (it is a separate file); the schema fields (reference, url, license, source_type, approval_status, approved_by, approved_date, notes); that it exists only if the workflow is (b)/(c)/(d) (not for strict per-claim (a)).
- Extend the "Per-claim record schema" table with the NEW fields (all optional to the loader, used by the batch/hybrid/per-source workflows): `source_id` (string, optional — references a source_id in sources.json), `review_tier` (enum: "high-stakes" | "routine", optional — used by hybrid workflow (c) to flag individually-reviewed vs source-inherited claims), `inherits_source_approval` (bool, optional — true if the claim fast-tracks via an approved source), `review_notes` (string, optional — free-text review notes, mandatory for high-stakes claims under hybrid), `claim_text` (string, optional — the full claim text to be approved, more detailed than the existing `claim` one-liner).
- Add a note: "The loader (`c14/citations.py`) only validates `approval_status ∈ {pending, approved, rejected}` and ignores all other fields — so the extended fields are backward-compatible with the Phase 1 gate (verified c14/citations.py:87-97)."

Do NOT modify c14/citations.py (the loader already handles the extended fields — it ignores unknown fields) or tools/check_citations.py (the gate is unchanged). Do NOT create tools/check_sources.py (deferred optional work).
  </action>
  <verify>
If workflow is (a): data/sources.json does NOT exist; data/citations.README.md notes strict-per-claim (no sources.json). If workflow is (b)/(c)/(d): `python3.6 -c "import json; d=json.load(open('data/sources.json')); assert isinstance(d, dict); print('sources.json OK', len(d), 'records')"` succeeds and every record has `approval_status == "pending"`. `python3.6 -c "import json; json.load(open('data/citations.README.md'))"` is NOT required (it's markdown) — instead `grep -c "source_id" data/citations.README.md` returns ≥1 and `grep -c "data/sources.json" data/citations.README.md` returns ≥1 (if b/c/d).
  </verify>
  <done>data/sources.json exists (if b/c/d) with the schema structure documented in the research, all records pending. data/citations.README.md documents the new fields + the sources.json file. No gate code changed.</done>
</task>

<task type="auto">
  <name>Task 2: Populate the PENDING first-batch candidate sources + seed claims (glucose critical-path + TCA RNG weights) from the research</name>
  <files>data/sources.json, data/citations.json</files>
  <action>
Populate the FIRST BATCH per SC#4: glucose critical-path (glycolysis + PDH + TCA) + TCA RNG weights. Pull the candidate sources + seed claims from `05-RESEARCH-source-approval.md` §3 (glucose critical-path sources) + §4 (TCA RNG weights sources). ALL entries are `approval_status: "pending"` — the human approves them in Plan 05. Do NOT mark anything approved.

A. Populate `data/sources.json` (IF the workflow is b/c/d — skip this sub-step if (a) strict per-claim; the seed claims in citations.json then carry no source_id). Create source records (all `pending`) for the glucose+TCA first batch — these are the candidate sources from the research (use the exact source_id names + reference strings from §2/§3 of the research):
- `LIBRETEXTS-METAB-GLYCOLYSIS` — LibreTexts Biological Chemistry / Metabolism / Glycolysis (CC BY-NC-SA 4.0; hub URL verified live 2026-08-15; exact glycolysis subpage URL flagged for approval-time confirmation). source_type: "educational_open_textbook". notes: "Hub verified live 2026-08-15; exact subpage URL to confirm at approval time."
- `LIBRETEXTS-METAB-TCA` — LibreTexts Biological Chemistry / Metabolism / Pyruvate Dehydrogenase + Citric Acid Cycle (CC BY-NC-SA 4.0; hub verified; subpage URLs to confirm). source_type: "educational_open_textbook".
- `LEHNINGER-CH-CITRIC_ACID_CYCLE` — Lehninger Principles of Biochemistry (Nelson & Cox), Citric Acid Cycle chapter. source_type: "print_textbook". notes: "CANDIDATE — human to confirm edition + exact chapter number before approving. No ISBN asserted."
- `PDB-4PFK` — RCSB PDB 4PFK (phosphofructokinase, G. stearothermophilus, 2.40 Å). source_type: "structural_database_entry". url: "https://www.rcsb.org/structure/4PFK". pdb_doi: "10.2210/pdb4PFK/pdb". notes: "VERIFIED at rcsb.org 2026-08-15 — structure exists, resolution + citation read from the RCSB page. Evans/Farrants/Hudson 1981, Philos Trans R Soc London ser B 293:53-62, DOI 10.1098/rstb.1981.0059, PubMed 6115424."
- `PDB-1CSC` — RCSB PDB 1CSC (citrate synthase, Gallus gallus, 1.70 Å). source_type: "structural_database_entry". url: "https://www.rcsb.org/structure/1CSC". pdb_doi: "10.2210/pdb1CSC/pdb". notes: "VERIFIED at rcsb.org 2026-08-15. Karpusas/Holland/Remington 1991, Biochemistry 30:6024-6031, DOI 10.1021/bi00238a028, PubMed 2043640."
- `NUBASE2020-C14` — (CROSS-REF from C14-decay research, for the half-life claim if Plan 01 kept decay) NUBASE2020 evaluation (Kondev et al., 2021, Chinese Physics C 45(3) 030001, DOI 10.1088/1674-1137/abddae — VERIFIED to resolve). source_type: "primary_literature". notes: "CROSS-REF from C14-decay research; only needed if decay framing kept the half-life claim. DOI verified to resolve 2026-08-15. Half-life 5700±30 yr."

B. Populate `data/citations.json` with the SEED first-batch claims (all `pending`). Use the claim_id names + claim text from `05-RESEARCH-source-approval.md` §2 (the example entries). These are SEED entries demonstrating the schema — the full ~60-105 glucose claims get authored in Phase 7 when the content is written; this batch front-loads the SOURCE approval + a few representative/seed claims. Create these entries (set `source_id` only if the workflow is b/c/d; set `review_tier` only if the workflow is c hybrid; all `approval_status: "pending"`):
- `GLY-PFK-01` — claim_text: "Phosphofructokinase-1 catalyzes the committed, rate-limiting step of glycolysis: fructose-6-phosphate + ATP -> fructose-1,6-bisphosphate + ADP." source_id: "LIBRETEXTS-METAB-GLYCOLYSIS". review_tier: "routine" (if hybrid). inherits_source_approval: true (if b/c/d).
- `TCA-RNG-CITRATE-PROCHIRALITY-01` — claim_text: "Citrate is a prochiral molecule; citrate synthase produces a specific stereoisomer, and aconitase discriminates between citrate's two carboxymethyl groups, so the two carbons derived from acetyl-CoA do not become equivalent until after the first turn of the cycle." source_id: "LEHNINGER-CH-CITRIC_ACID_CYCLE". review_tier: "high-stakes" (if hybrid). review_notes: "Grounds the TCA RNG shuffle mechanism. Source backs the MECHANISM; the RNG weight VALUES are a separate game-design claim (TCA-RNG-WEIGHT-01) approved individually." inherits_source_approval: false.
- `TCA-RNG-WEIGHT-01` — claim_text: "In this game, the probability that a given acetyl-CoA carbon exits as CO2 on the first turn of TCA vs the second turn is set to 0.5 / 0.5 (game-design value grounded in the citrate-prochirality mechanism)." source_id: "LEHNINGER-CH-CITRIC_ACID_CYCLE". review_tier: "high-stakes" (if hybrid). review_notes: "VALUE is a game-design decision; the 0.5/0.5 is grounded in the symmetric-fate mechanism but the human chooses the exact weights for pedagogy. Reviewed individually." inherits_source_approval: false.
- `CAST-PFK-PDB-01` — claim_text: "The game's PFK cast structure is RCSB PDB entry 4PFK (2.40 Å resolution; Evans, Farrants, Hudson 1981)." source_id: "PDB-4PFK". review_tier: "routine" (if hybrid). inherits_source_approval: true (if b/c/d).
- `CAST-CSC-PDB-01` — claim_text: "The game's citrate synthase cast structure is RCSB PDB entry 1CSC (1.70 Å resolution; Karpusas, Holland, Remington 1991)." source_id: "PDB-1CSC". review_tier: "routine" (if hybrid). inherits_source_approval: true (if b/c/d).
- `C14-TIME-01` — (CROSS-REF deferred from C14-decay research, for the respiration-cycle timescale if Plan 01 chose option c/d-iii) claim_text: "A single respiration-cycle step in this game represents a timescale of seconds to minutes." source_id: "LIBRETEXTS-METAB-GLYCOLYSIS" (the biochem source family; NOT the nuclear family). review_tier: "routine" (if hybrid). notes: "CROSS-REF from C14-decay research; approves the biochem timescale that the C14-PROB-01 ~10^-12 probability leans on. Approve once here; both researches inherit."

Also preserve the existing `data/citations.json` content (it is currently `{}` — empty — so there is nothing to preserve; just write the new object). Keep the existing fixture `tests/fixtures/citations_pass.json` UNCHANGED (it has `placeholder-claim-1` / `placeholder-claim-2` for gate testing — do not touch fixtures).

Do NOT add the anaerobic/ATP-soul-jump sources or claims to this batch (they are candidate sources for Phase 7/9, not the glucose first-batch — keeps this plan within context budget and matches SC#4's scope). Do NOT mark any entry approved.
  </action>
  <verify>
`python3.6 -c "import json; d=json.load(open('data/citations.json')); assert isinstance(d, dict); assert all(v.get('approval_status')=='pending' for v in d.values()); print('citations.json OK', len(d), 'seed claims, all pending')"` succeeds. If b/c/d: same check on data/sources.json (all pending). `python3.6 -c "import sys; sys.path.insert(0,'.'); from c14.citations import CitationRegistry; r=CitationRegistry.load('data/citations.json'); print('loader OK', len(r), 'claims')"` succeeds — proves the extended fields load cleanly (backward-compatible). `python3.6 tools/check_citations.py --story tests/fixtures/story_pass.json --registry data/citations.json` exits 1 (the seed claims are pending, not approved — the gate correctly fails on them; this PROVES the gate works on the real extended registry, NOT that the claims pass — they should NOT pass until Plan 05). `grep -c "TCA-RNG-CITRATE-PROCHIRALITY-01" data/citations.json` returns ≥1.
  </verify>
  <done>data/sources.json (if b/c/d) + data/citations.json are populated with the pending first-batch candidate sources + seed claims. The Phase 1 loader loads the extended citations.json cleanly (backward-compatible). The gate correctly fails on the pending claims (exit 1) — proving the gate works on real extended data. No entry is approved; the human approves them in Plan 05.</done>
</task>

</tasks>

<verification>
- data/sources.json (if b/c/d) + data/citations.json exist with the first-batch candidate sources + seed claims, ALL pending.
- The Phase 1 loader (c14/citations.py) loads the extended citations.json without error (backward-compatible — ignores unknown fields).
- The gate (tools/check_citations.py) correctly fails (exit 1) on the pending seed claims — proving SC#5 enforcement works on real extended data (NOT that the claims pass — they must NOT pass until Plan 05).
- No source or claim was auto-approved.
- data/citations.README.md documents the new schema.
</verification>

<success_criteria>
- SC#4 registry structure is in place: sources.json (if b/c/d) + extended citations.json with pending first-batch entries for glucose critical-path + TCA RNG weights.
- SC#5 enforcement is proven on real extended data (gate exits 1 on pending claims).
- The human's Plan 05 approval checkpoint is unblocked (the pending entries are ready for review).
</success_criteria>

<output>
After completion, create `.planning/phases/05-pre-content-key-decisions-source-approval/05-04-SUMMARY.md` recording: (1) the workflow branch taken (a vs b/c/d — whether sources.json was created), (2) the list of source_ids + claim_ids populated (all pending), (3) the loader + gate verification results (loader OK; gate exits 1 on pending — correct), (4) a flag for Plan 05: the exact list of entries the human needs to review + approve (the first batch).
</output>
