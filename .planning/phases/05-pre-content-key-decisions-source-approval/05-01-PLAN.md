---
phase: 05-pre-content-key-decisions-source-approval
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - spec.md
autonomous: false
user_setup: []

must_haves:
  truths:
    - "SC#1 is confirmed complete: the ATP/True-Ending soul-jump reframing is documented as RESOLVED in PROJECT.md Key Decisions (row 104), PITFALLS.md Pitfall 4 banner, and spec.md line 15 in-place annotation"
    - "The C14-decay timescale framing decision is recorded in PROJECT.md Key Decisions (row 105) — one of 6 framing options chosen by the human"
    - "If decay is kept (options b/c/d-ii/d-iii), the half-life VALUE + source pair is recorded (5700±30 NUBASE2020 OR 5730 Cambridge)"
    - "If the chosen option deviates from spec.md line 18 (drops/moves decay as a trigger), spec.md line 18 is annotated in-place"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions row 105 updated from 'Pending' to the chosen C14-decay framing + half-life value/source"
      contains: "C14-decay timescale framing"
    - path: "spec.md"
      provides: "In-place annotation on line 18 IF the chosen option drops/moves decay as a bad-ending trigger"
      contains: "Pitfall 9 NOTE"
  key_links:
    - from: ".planning/PROJECT.md Key Decisions row 105"
      to: "05-RESEARCH-c14-decay.md Recommendation"
      via: "decision outcome references the chosen option's rationale"
      pattern: "C14-decay"
---

<objective>
Confirm SC#1 (ATP/True-Ending soul-jump reframing — already documented DONE) and resolve SC#2 (C14 radioactive-decay bad-ending timescale framing — Pitfall 9) by surfacing the 6 research options to the human for a decision, then documenting the chosen framing in PROJECT.md Key Decisions.

Purpose: SC#1 is already fully documented (verified by `05-RESEARCH-atp-soul-jump.md`); this plan confirms it with a trivial verification so the phase can mark it complete. SC#2 is a pending human decision — the C14 half-life (~5,700 yr) makes decay during one respiration cycle ~10⁻¹² probability, so presenting decay as a live in-game risk is scientifically misleading. The human chooses how to frame it (drop / time-compress / teaching-moment / side-note / epilogue / educator-toggle). This unblocks the bad-ending pool composition for Phase 5.1 (Story Graph Design).

Output: A confirmation note that SC#1 is done (recorded in this plan's SUMMARY), and PROJECT.md Key Decisions row 105 updated with the chosen C14-decay framing + half-life value/source. Conditional spec.md line 18 annotation if the choice deviates from the spec.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/PITFALLS.md
@spec.md
@.planning/phases/05-pre-content-key-decisions-source-approval/05-RESEARCH-c14-decay.md
@.planning/phases/05-pre-content-key-decisions-source-approval/05-RESEARCH-atp-soul-jump.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify SC#1 documentation is complete (ATP/True-Ending soul-jump reframing)</name>
  <files>.planning/PROJECT.md, .planning/research/PITFALLS.md, spec.md</files>
  <action>
SC#1 is ALREADY RESOLVED — this task only VERIFIES the documentation exists, it does NOT re-decide anything. Read all three artifacts in full and confirm each carries the soul-jump resolution:

1. `.planning/PROJECT.md` — confirm the "ATP/True-Ending carbon-fate reframing" row (row 104) of the Key Decisions table reads "RESOLVED 2026-08-13 via the soul-jump reframing" with outcome "— Resolved (soul-jump)" and states: the hero's ELECTRONS (the "soul") are harvested into ATP via the ETC → ATP synthase after the RNG-weighted TCA path; the carbon body is released as CO2; C14 is a tracking label not a fate determinant.

2. `.planning/research/PITFALLS.md` — confirm Pitfall 4 carries the "✅ RESOLVED (2026-08-13) via the soul-jump reframing" banner and explicitly separates Pitfall 9 (C14-decay) as still Pending.

3. `spec.md` — confirm line 15 (the "the true end is end up as ATP" bullet) carries the in-place HTML comment annotation linking to the soul-jump reframing + PROJECT.md, and flagging Pitfall 9 as separate/pending.

Do NOT modify any of these files — SC#1 is done. Do NOT reopen Pitfall 4 (per AGENTS.md the soul-jump is settled). If all three artifacts are present and consistent, SC#1 is confirmed complete; record "SC#1 CONFIRMED COMPLETE — all 3 documentation artifacts verified present + consistent" in this plan's SUMMARY (created by the execute-plan workflow). If ANY artifact is missing or inconsistent, STOP and report the gap in the SUMMARY instead of proceeding to Task 2.
  </action>
  <verify>
`grep -c "Resolved (soul-jump)" .planning/PROJECT.md` returns ≥1; `grep -c "RESOLVED (2026-08-13) via the soul-jump reframing" .planning/research/PITFALLS.md` returns ≥1; `grep -c "Pitfall 4 NOTE (2026-08-13)" spec.md` returns ≥1. All three checks pass = SC#1 confirmed.
  </verify>
  <done>SC#1's framing-decision component is confirmed complete; the SUMMARY records the verification. (The separate, later gate — per-claim citation approval of the ETC/ATP-synthase chemistry in Phase 7+ — is explicitly carved out by SC#1 and is NOT part of this confirmation.)</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: SC#2 — C14-decay timescale framing decision (human picks from 6 options + half-life value/source)</name>
  <decision>
How should the C14 radioactive-decay bad-ending timescale (Pitfall 9) be framed? C14's half-life is ~5,700 years, so the probability of a single C14 atom decaying during one respiration cycle (seconds–minutes, the actual gameplay timescale) is ~10⁻¹². Presenting decay as a live in-game risk is scientifically misleading. Pick ONE framing option below. Full pros/cons/reachability-impact/per-claim-load for each are in `05-RESEARCH-c14-decay.md` §"Framing Options" — READ that section before deciding.

ALSO decide the half-life VALUE + source (sub-decision, required for all options that mention the half-life; for option (a) DROP it is only needed if hero-intro text still calls C14 radioactive):
- Value A: 5700 ± 30 years (NUBASE2020, DOI 10.1088/1674-1137/abddae — VERIFIED to resolve). Recommended.
- Value B: ~5730 years (older Cambridge half-life, Godwin 1962 — what the project docs currently say). If chosen, note it is the superseded Cambridge value.
- Source: NUBASE2020 (primary, DOI verified) OR NuDat 3.0 (NNDC/BNL, queryable) OR IAEA LiveChart (intergovernmental). Research recommends NUBASE2020 as primary + NuDat 3.0 as cross-check.

ALSO (for option c/d-iii only): what is the "respiration cycle" timescale for the ~10⁻¹² probability claim? This biochem source (C14-TIME-01) is cross-deferred to the glucose critical-path source batch (Plan 04/05) — just note the chosen comparison timescale (seconds or minutes) here.
  </decision>
  <context>
This is a PARALLEL Phase 5 track that gates the bad-ending pool composition for Phase 5.1 (Story Graph Design). The decision is INDEPENDENT of Pitfall 4 (soul-jump, already resolved) — do NOT conflate them (decay changes the hero's ELEMENT C→N; the soul-jump is about carbon-vs-electron FATE; they are categorically different events). Whatever option is chosen, Phase 5.1's reachability checker MUST later confirm ≥1 Bad-ending trigger remains reachable per character — flag this dependency. Advisory lean from research: option (c) REFRAME AS TEACHING MOMENT (best educator fit; highest claim load ~5-6 but small vs the ~400 total). If minimizing approval throughput dominates: (a) DROP or (d-i) SIDE-NOTE.
  </context>
  <options>
    <option id="a-drop">
      <name>(a) DROP decay as a bad-ending trigger entirely</name>
      <pros>Scientifically cleanest; lowest claim load (~0 decay-narrative claims); simplifies bad-ending pool.</pros>
      <cons>SPEC DEVIATION (spec.md line 18 lists "radioactive decay" as a trigger — dropping it is a documented change); loses a dramatic beat. Neutral for Bad-tier reachability IF other triggers (cycle-trap, critical-residue-break, lost-connection) remain reachable per character.</cons>
    </option>
    <option id="b-timecompress">
      <name>(b) TIME-COMPRESS — keep decay with explicit "we fast-forward time" framing</name>
      <pros>Preserves spec trigger (no deviation); keeps the dramatic decay beat; honest framing (time-skip is explicit).</pros>
      <cons>Tonal mismatch (respiration=seconds, decay=millennia — jarring jump); time-skip must be unmistakably explicit or it re-creates the misleading-timescale pitfall. ~3 claims.</cons>
    </option>
    <option id="c-teaching">
      <name>(c) REFRAME AS TEACHING MOMENT — keep decay, teaching layer states it's a dramatic device (RECOMMENDED)</name>
      <pros>Best educator fit (applauds rather than flags); preserves spec trigger; aligns with the two-layer text design; reinforces C14-as-tracking-label identity; matches PITFALLS.md Pitfall 9's own recommended mitigation.</pros>
      <cons>Highest per-claim load (~5-6 claims: F1 half-life, F2 decay mode, F3 decay law, F4 ~10⁻¹² probability, F5 long-half-life=good-tracker); F4 requires fixing a defensible comparison timescale. Most two-layer authoring work.</cons>
    </option>
    <option id="d-i-sidenote">
      <name>(d-i) SIDE-NOTE — drop decay as a trigger, but hero-intro/glossary still teaches C14 is radioactive (~5,700-yr half-life) as flavor</name>
      <pros>Clean separation (gameplay at biological timescale only; radioactivity = factual flavor, not a threat); still teaches the real fact; lower claim load (~3: F1, F2, F5).</pros>
      <cons>SPEC DEVIATION (drops decay as a trigger, keeps it as a fact); less dramatic (no "decay" bad-ending).</cons>
    </option>
    <option id="d-ii-epilogue">
      <name>(d-ii) DEEP-TIME EPILOGUE — decay as a post-credits/special ending OUTSIDE the 4 tiers</name>
      <pros>Preserves the decay beat in a tonally-appropriate place (geological time, not gameplay); does NOT touch the 4-ending-tier reachability invariant (epilogue is orthogonal); pedagogically clean (biological vs nuclear timescale contrast).</pros>
      <cons>Adds a 5th ending-type (scope; needs design care to keep separate from the 4 tiers); SPEC DEVIATION; needs an access mechanism (seed/unlock). ~3-4 claims.</cons>
    </option>
    <option id="d-iii-toggle">
      <name>(d-iii) EDUCATOR-DEMO TOGGLE — opt-in settings toggle runs a "decay demo" (not a live gameplay trigger)</name>
      <pros>Strongest teaching fit (opt-in, unambiguous, seedable RNG makes it reproducible for classrooms); default gameplay is biologically-timescale-clean.</pros>
      <cons>Adds a feature (educator toggle + demo — scope beyond a content decision); SPEC DEVIATION (decay not a default trigger). ~4-5 claims.</cons>
    </option>
  </options>
  <resume-signal>
Type your choice on ONE line, e.g.:
`option=c-teaching, halflife=5700, source=NUBASE2020, timescale=seconds`
or
`option=a-drop, halflife=5700, source=NUBASE2020`  (half-life still needed if hero-intro calls C14 radioactive)
or
`option=d-ii-epilogue, halflife=5700, source=NUBASE2020, access=seed`

Required: `option=<a-drop|b-timecompress|c-teaching|d-i-sidenote|d-ii-epilogue|d-iii-toggle>`. Required for all except pure-drop with no radioactivity text: `halflife=<5700|5730>, source=<NUBASE2020|NuDat|IAEA>`. Required for c/d-iii: `timescale=<seconds|minutes>`. For d-ii: `access=<seed|unlock|all-endings-seen>`.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Document the SC#2 C14-decay decision in PROJECT.md Key Decisions + conditional spec.md annotation</name>
  <files>.planning/PROJECT.md, spec.md</files>
  <action>
Read the human's choice from Task 2's resume-signal. Update PROJECT.md Key Decisions and (conditionally) annotate spec.md. Do NOT approve any source — source/claim approval is a SEPARATE checkpoint in Plan 05 (this plan only records the FRAMING decision; the half-life source becomes a `pending` registry entry in Plan 04, approved by the human in Plan 05).

1. Update `.planning/PROJECT.md` Key Decisions table — the "C14-decay timescale framing" row (row 105). Change the Outcome cell from "— Pending" to a concise record of the decision:
   - State the chosen option (e.g. "RESOLVED via option (c) REFRAME AS TEACHING MOMENT").
   - Record the half-life value + source (e.g. "Half-life 5700±30 yr (NUBASE2020, DOI 10.1088/1674-1137/abddae — verified to resolve); the ~5730 figure in older docs is the superseded Cambridge value, not used").
   - Record the respiration-cycle timescale if option c/d-iii (e.g. "Comparison timescale = seconds (one enzymatic step); C14-TIME-01 biochem source to be approved in the glucose first-batch").
   - One-line rationale (why this option, referencing the educator-audience / two-layer-text / spec-deviation tradeoff).
   - Flag the Phase 5.1 dependency: "Phase 5.1 reachability checker must confirm ≥1 Bad-ending trigger remains reachable per character after this decision."
   - Update the last-updated footer date to today (2026-08-15) with a note: "Pitfall 9 (C14-decay) resolved via [chosen option]; see Key Decisions".

2. CONDITIONALLY annotate `spec.md` line 18 (the bad-ending bullet listing "radioactive decay"). Add an in-place HTML comment annotation ONLY IF the chosen option deviates from the spec (i.e. drops or moves decay as a trigger — options a-drop, d-i-sidenote, d-ii-epilogue, d-iii-toggle). Use the same annotation style as the Pitfall 4 annotation on line 15:
   `<!-- Pitfall 9 NOTE (2026-08-15): Resolved via option (X) — [one-line description of what changed, e.g. "decay removed as a bad-ending trigger; kept as glossary fact" or "decay moved to a deep-time epilogue outside the 4 tiers"]. See .planning/PROJECT.md Key Decisions. -->`
   If the chosen option is (b) TIME-COMPRESS or (c) TEACHING MOMENT (keeps decay as a trigger, no spec deviation), do NOT annotate spec.md — instead add a one-line note to the PROJECT.md decision row noting "no spec deviation (decay retained as trigger)".

Do NOT modify any other file. Do NOT touch the registry (data/citations.json) — that is Plan 04's job.
  </action>
  <verify>
`grep -c "C14-decay timescale framing" .planning/PROJECT.md` returns ≥1 and the row's Outcome no longer says "— Pending" (it says RESOLVED or the chosen option). If spec-deviating option chosen: `grep -c "Pitfall 9 NOTE (2026-08-15)" spec.md` returns ≥1. If non-deviating: the PROJECT.md row explicitly notes "no spec deviation".
  </verify>
  <done>PROJECT.md Key Decisions row 105 records the chosen C14-decay framing + half-life value/source + Phase 5.1 reachability dependency. spec.md line 18 annotated IF the choice deviates from the spec. SC#2's framing-decision component is complete (the half-life source still needs per-claim approval in Plan 05 — that is the SC#4/SC#5 gate, separate from this framing decision).</done>
</task>

</tasks>

<verification>
- SC#1: all 3 documentation artifacts verified present + consistent (Task 1 grep checks pass).
- SC#2: PROJECT.md row 105 updated from "Pending" to the chosen framing; half-life value+source recorded; spec.md annotated if spec-deviating.
- No source was auto-approved (source approval is deferred to Plan 05's human checkpoint — this plan only records the framing decision).
- Pitfall 4 (soul-jump) was NOT reopened (AGENTS.md constraint honored).
</verification>

<success_criteria>
- SC#1 confirmed complete (3 artifacts verified).
- SC#2 framing decision documented in PROJECT.md row 105 with chosen option + half-life value/source + Phase 5.1 dependency flag.
- spec.md line 18 annotated IF the choice deviates from the spec.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pre-content-key-decisions-source-approval/05-01-SUMMARY.md` recording: (1) SC#1 confirmation result, (2) the human's SC#2 choice (option + half-life + source + timescale), (3) the PROJECT.md + spec.md edits made, (4) any flags for downstream (Phase 5.1 reachability, Plan 04 first-batch sourcing of C14-TIME-01).
</output>
