---
phase: 05-pre-content-key-decisions-source-approval
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - .planning/PROJECT.md
autonomous: false
user_setup: []

must_haves:
  truths:
    - "The source-approval WORKFLOW is agreed + documented in PROJECT.md Key Decisions — one of 4 options chosen by the human"
    - "If option (c) HYBRID is chosen, the high-stakes vs routine claim taxonomy is defined + documented (which claim categories get individual review vs source-inherited fast-track)"
    - "The workflow decision is backward-compatible with the Phase 1 gate (tools/check_citations.py predicate unchanged: approval_status == 'approved')"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions 'Batch-by-source vs strict per-claim approval' row updated from 'Pending' to the chosen workflow + taxonomy"
      contains: "Batch-by-source vs strict per-claim approval"
  key_links:
    - from: ".planning/PROJECT.md Key Decisions (workflow row)"
      to: "05-RESEARCH-source-approval.md §1 Workflow Options"
      via: "decision outcome references the chosen workflow option + high-stakes taxonomy"
      pattern: "Batch-by-source"
---

<objective>
Resolve SC#4's first sub-decision (the source-approval WORKFLOW) by surfacing the 4 research options to the human, then documenting the chosen workflow + (if hybrid) the high-stakes taxonomy in PROJECT.md Key Decisions.

Purpose: The user's currently-recorded choice (PROJECT.md row 102) is "per-claim checkpoint" (closest to option (a) STRICT PER-CLAIM) — the safest but the timeline-dominating risk (Pitfall 7: ~400 claims for full content × per-claim review ≈ ~33 hours). The research surfaces option (c) HYBRID as the option that preserves the per-claim safety guarantee on load-bearing claims (RNG weight values, protonation defaults, carbon-fate claims) while amortizing the ~60-100 routine glucose claims via source inheritance. This decision unblocks Plan 04 (the schema implementation + first-batch population) and Plan 05 (the human's first-batch source approval). The Phase 1 gate stays UNCHANGED for all options — the workflow is a PROCESS convention, not a gate-enforcement change (verified in `05-RESEARCH-source-approval.md` §1: the gate predicate `approval_status == "approved"` is preserved; a separate `data/sources.json` file is never read by the Phase 1 gate).

Output: PROJECT.md Key Decisions row 106 updated with the chosen workflow + (if hybrid) the high-stakes taxonomy. No schema implementation in this plan (that is Plan 04) — this plan only records the decision.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-pre-content-key-decisions-source-approval/05-RESEARCH-source-approval.md
@.planning/phases/05-pre-content-key-decisions-source-approval/05-02-SUMMARY.md
@c14/citations.py
@tools/check_citations.py
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: SC#4 — Source-approval workflow decision + (if hybrid) high-stakes taxonomy (human picks)</name>
  <decision>
Pick the source-approval WORKFLOW for the project. This determines how the ~400 full-content claims (Pitfall 7) get human-reviewed before landing in code/content. Read `05-RESEARCH-source-approval.md` §1 "Workflow Options" + the comparison matrix BEFORE deciding. ALL FOUR OPTIONS keep the Phase 1 gate (`tools/check_citations.py`) UNCHANGED — the gate predicate `approval_status == "approved"` is preserved; the workflow is a process convention + (for b/c/d) a separate `data/sources.json` file the gate never reads.

IF you pick option (c) HYBRID, ALSO define the high-stakes vs routine taxonomy (which claim categories get individual per-claim review vs source-inherited fast-track). The research's advisory high-stakes list (§1c): RNG weight VALUES, protonation defaults, carbon-fate claims tied to the soul-jump/ending tiers, anything flagged contested. The advisory routine list: enzyme-catalyzes-reaction-X facts, enzyme name/EC/cofactor identity, PDB existence+resolution+citation, pathway ordering.
  </decision>
  <context>
The user's currently-recorded choice (PROJECT.md row 102) is "Source approval = per-claim checkpoint" (closest to option a). This research surfaces (c) HYBRID as the option that honors BOTH the per-claim safety guarantee on load-bearing claims AND Pitfall 7's timeline risk. The schema extension (Plan 04) supports all four options without rework — the separate `data/sources.json` + extended `data/citations.json` (with `source_id`, `review_tier`, `inherits_source_approval`, `review_notes` fields) is backward-compatible with the Phase 1 loader (which only validates `approval_status ∈ {pending, approved, rejected}` and ignores unknown fields — verified in `c14/citations.py:87-97`). Throughput estimates (glucose first batch ~60-105 claims): (a) ~5-9h, (b) ~1-2h, (c) ~1.5h, (d) ~3-4h. This decision gates Plan 04 (the schema implementation branches: option (a) skips `data/sources.json`; b/c/d create it).
  </context>
  <options>
    <option id="a-strict">
      <name>(a) STRICT PER-CLAIM — every individual claim reviewed + approved one-by-one (user's current choice)</name>
      <pros>Lowest fabricated-science risk (every claim individually scrutinized); zero gate change; zero schema change (no sources.json needed).</pros>
      <cons>SLOWEST — timeline-dominating risk (Pitfall 7: ~33h for full content, ~5-9h for the glucose batch alone). The user's recorded choice; this research surfaces the throughput cost.</cons>
    </option>
    <option id="b-batch">
      <name>(b) BATCH-BY-SOURCE — a source is approved up front; claims traceable to it inherit approval (fast-track)</name>
      <pros>Much faster (~10-20× amortization for routine claims); ~60-105 glucose claims collapse to ~3-6 source approvals + a fast per-claim confirmation pass.</pros>
      <cons>Medium fabricated-science risk (a wrong claim citing an approved source can inherit approval without re-reading the source for that specific claim); needs `data/sources.json` + per-claim `source_id` field.</cons>
    </option>
    <option id="c-hybrid">
      <name>(c) HYBRID — sources approved up front (batch), high-stakes claims still get individual review (RECOMMENDED)</name>
      <pros>Safety where it matters (high-stakes: RNG weights, protonation, carbon-fate, contested claims = individual review) + throughput elsewhere (routine pathway facts = source-inherited fast-track). ~1.5h for the glucose batch. Honors BOTH the per-claim safety guarantee AND Pitfall 7's timeline risk. Requires a one-time taxonomy decision (high-stakes vs routine categories).</pros>
      <cons>Requires defining the high-stakes taxonomy upfront (a one-time taxonomy decision); needs `data/sources.json` + `source_id` + `review_tier` fields; optional `tools/check_sources.py` drift-detection gate.</cons>
    </option>
    <option id="d-per-source">
      <name>(d) PER-SOURCE-RECORD — decouple "source approved to cite" from "claim approved" (lock the source family upfront, still review each claim)</name>
      <pros>Low risk (same per-claim scrutiny as (a) + a structural barrier against citing sketchy sources); locks the source family upfront (LibreTexts + Lehninger + RCSB only; no Wikipedia/blogs).</pros>
      <cons>Slower than (b)/(c) (~3-4h glucose batch); needs `data/sources.json` + a NEW `tools/check_sources.py` gate (two layered gates).</cons>
    </option>
  </options>
  <resume-signal>
Type your choice on ONE line, e.g.:
`workflow=c-hybrid, high-stakes=[RNG-weights,protonation-defaults,carbon-fate,contested]`
or
`workflow=a-strict`
or
`workflow=d-per-source, source-family=[LibreTexts,Lehninger,RCSB-PDB]`

Required: `workflow=<a-strict|b-batch|c-hybrid|d-per-source>`. Required only if c-hybrid: `high-stakes=[comma-separated categories]` (confirm/edit the advisory list: RNG-weights, protonation-defaults, carbon-fate, contested). Optional if d-per-source: `source-family=[allowed sources]`.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Document the SC#4 workflow decision + (if hybrid) high-stakes taxonomy in PROJECT.md Key Decisions</name>
  <files>.planning/PROJECT.md</files>
  <action>
Read the human's choice from Task 1's resume-signal. Update PROJECT.md Key Decisions. Do NOT implement the schema (that is Plan 04) and do NOT approve sources (that is Plan 05) — this plan only records the decision.

1. Update `.planning/PROJECT.md` Key Decisions table — the "Batch-by-source vs strict per-claim approval" row (row 106). Change the Outcome cell from "— Pending" to a concise record:
   - State the chosen workflow (e.g. "RESOLVED via option (c) HYBRID — sources approved up front (batch), high-stakes claims individually reviewed").
   - If (c) hybrid: record the high-stakes taxonomy (the categories that get individual per-claim review vs source-inherited fast-track) — copy the human's `high-stakes=[...]` list verbatim, and record the routine categories (enzyme-catalyzes-reaction-X, enzyme name/EC/cofactor, PDB existence/resolution/citation, pathway ordering).
   - If (a) strict: state "STRICT PER-CLAIM retained (user's original choice); no sources.json needed; Pitfall 7 timeline risk accepted".
   - If (b) batch: state "BATCH-BY-SOURCE; sources.json + per-claim source_id; all claims source-inherited (no high-stakes tier)".
   - If (d) per-source: state "PER-SOURCE-RECORD; sources.json + new check_sources.py gate; source family locked to [human's list]".
   - Record the backward-compatibility note: "Phase 1 gate (tools/check_citations.py) UNCHANGED — predicate approval_status == 'approved' preserved; data/sources.json is a separate file the gate never reads (verified c14/citations.py:87-97 ignores unknown fields)".
   - Record the throughput estimate for the glucose first batch (a: ~5-9h / b: ~1-2h / c: ~1.5h / d: ~3-4h) and the full-content estimate (a: ~33h / b/c: ~6-8h).
   - Update the last-updated footer date to 2026-08-15 with a note: "Source-approval workflow resolved via option (X); see Key Decisions".

2. If the human chose (c) hybrid, ALSO add a brief note to the "Source approval = per-claim checkpoint" row (row 102) cross-referencing the hybrid decision: "Refined in row 106: per-claim checkpoint retained for HIGH-STAKES claims; routine claims source-inherited (hybrid option c)". Do NOT delete the original row-102 rationale (provenance).

Do NOT modify the registry (data/citations.json), data/sources.json (does not exist yet — Plan 04 creates it), c14/citations.py, tools/check_citations.py, or any research file.
  </action>
  <verify>
`grep -c "Batch-by-source vs strict per-claim approval" .planning/PROJECT.md` ≥1 and the row Outcome no longer says "— Pending". If (c) hybrid: `grep -c "high-stakes" .planning/PROJECT.md` ≥1 and the taxonomy list is present. The backward-compatibility note ("Phase 1 gate UNCHANGED") is present in the row.
  </verify>
  <done>SC#4's workflow sub-decision is documented in PROJECT.md Key Decisions row 106 (chosen workflow + taxonomy if hybrid + backward-compatibility note + throughput estimate). The schema implementation + first-batch population (Plan 04) and the human source-approval checkpoint (Plan 05) are unblocked.</done>
</task>

</tasks>

<verification>
- SC#4 workflow: PROJECT.md row 106 updated from "Pending" to the chosen workflow + taxonomy (if hybrid).
- Backward-compatibility with the Phase 1 gate is explicitly recorded (gate predicate unchanged).
- No schema code was written (Plan 04's job) and no source was approved (Plan 05's job).
</verification>

<success_criteria>
- SC#4 workflow decision documented in PROJECT.md Key Decisions (chosen option + high-stakes taxonomy if hybrid + backward-compatibility note).
- Plan 04 (schema implementation) is unblocked — it reads this decision to know whether to create data/sources.json.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pre-content-key-decisions-source-approval/05-03-SUMMARY.md` recording: (1) the human's SC#4 workflow choice (+ taxonomy if hybrid), (2) the PROJECT.md edit, (3) a flag for Plan 04: whether to create data/sources.json (b/c/d) or skip it (a), and whether to build the optional tools/check_sources.py drift-detection gate (advisory for b/c/d).
</output>
