---
phase: 05-pre-content-key-decisions-source-approval
plan: 05
type: execute
wave: 5
depends_on: ["05-04"]
files_modified:
  - data/citations.json
  - data/sources.json
autonomous: false
user_setup: []

must_haves:
  truths:
    - "The human has reviewed and approved the first batch of candidate sources + seed claims (the human is the per-claim/per-source approver — OpenCode never auto-approves)"
    - "Approved entries in data/citations.json have approval_status='approved' with approved_by + approved_date populated"
    - "Approved source records in data/sources.json (if b/c/d) have approval_status='approved'"
    - "tools/check_citations.py passes (exit 0) on a story referencing the approved first-batch claims — SC#5 is operational on real data"
    - "Any claim/source the human REFUSED is marked approval_status='rejected' with review_notes (provenance retained, not deleted)"
  artifacts:
    - path: "data/citations.json"
      provides: "First-batch claim entries flipped from pending to approved (or rejected) per the human's review"
      contains: "approved"
    - path: "data/sources.json"
      provides: "First-batch source records flipped from pending to approved (or rejected) per the human's review (if workflow b/c/d)"
      contains: "approved"
  key_links:
    - from: "tools/check_citations.py"
      to: "data/citations.json (approved first-batch claims)"
      via: "CitationRegistry.is_approved — gate exits 0 when a story references only approved claims"
      pattern: "CITATION GATE PASSED"
---

<objective>
The human reviews and approves (or rejects) the first batch of candidate sources + seed claims populated as pending in Plan 04, then OpenCode records the approval (flips `pending` → `approved`/`rejected` in the registry) and runs the Phase 1 gate to prove SC#5 is operational on real data. This satisfies SC#4 (the first batch of approved sources lands in the registry) and SC#5 (the per-claim gate is operational on real approved data).

Purpose: Source/claim approval is ALWAYS a human checkpoint — OpenCode never auto-approves (the no-fabricated-science rule). Plan 04 populated the first batch as `pending`; this plan's checkpoint has the human review each candidate source + seed claim against its research-proposed reference (re-verifying any web-verified PDB IDs / DOIs at the source pages if the human wants), then OpenCode mechanically records the decision + runs the gate. The first batch = glucose critical-path + TCA RNG weights (SC#4's stated scope). This is the SC#4↔SC#5 link: SC#4 (agree workflow + approve first batch) is what makes SC#5 meaningful on real content (the gate is operational but had no real approved claims until now).

Output: `data/citations.json` + `data/sources.json` (if b/c/d) with the first-batch entries flipped to `approved` (or `rejected` with provenance notes); `tools/check_citations.py` passing (exit 0) on a fixture story referencing the approved claims. Phase 5 is COMPLETE: SC#1 (confirmed Plan 01), SC#2 (framing decision Plan 01), SC#3 (framing decision Plan 02), SC#4 (workflow Plan 03 + registry Plan 04 + first-batch approval here), SC#5 (gate operational on real data here).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-pre-content-key-decisions-source-approval/05-RESEARCH-source-approval.md
@.planning/phases/05-pre-content-key-decisions-source-approval/05-04-SUMMARY.md
@data/citations.json
@data/sources.json
@c14/citations.py
@tools/check_citations.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human reviews + approves (or rejects) the first batch of candidate sources + seed claims</name>
  <what-built>
Plan 04 created `data/sources.json` (if the workflow is b/c/d) + `data/citations.json` with the first-batch candidate sources + seed claims, ALL marked `pending`. The candidate sources come from `05-RESEARCH-source-approval.md` §3 (glucose critical-path) + §4 (TCA RNG weights), with 2 PDB IDs (4PFK, 1CSC) web-verified at rcsb.org and 1 DOI (NUBASE2020) verified to resolve. The seed claims are: GLY-PFK-01, TCA-RNG-CITRATE-PROCHIRALITY-01 (high-stakes — grounds the TCA shuffle mechanism), TCA-RNG-WEIGHT-01 (high-stakes — game-design weight value), CAST-PFK-PDB-01, CAST-CSC-PDB-01, C14-TIME-01 (cross-ref from C14-decay research). All are PENDING your approval.
  </what-built>
  <how-to-verify>
You are the per-claim/per-source human approver (CITE-01). Review each candidate source + seed claim and decide APPROVE or REJECT (with a reason). For each:

1. SOURCES (data/sources.json, if workflow is b/c/d) — for each source_id, confirm the source is real, authoritative, and acceptable for this project:
   - `LIBRETEXTS-METAB-GLYCOLYSIS` + `LIBRETEXTS-METAB-TCA`: fetch the LibreTexts Biological Chemistry / Metabolism hub (URL in the source record) + confirm the glycolysis / PDH / TCA subpages exist and state the pathway facts. Confirm CC BY-NC-SA 4.0 license is acceptable.
   - `LEHNINGER-CH-CITRIC_ACID_CYCLE`: confirm the edition + exact chapter number (no ISBN was asserted — record it at approval). If you prefer a different edition or want to skip the print cross-ref (LibreTexts alone sufficient), say so.
   - `PDB-4PFK` + `PDB-1CSC`: re-verify at https://www.rcsb.org/structure/4PFK and https://www.rcsb.org/structure/1CSC (resolution + deposited citation match the source record). The research verified these 2026-08-15; re-confirm if you want.
   - `NUBASE2020-C14` (if Plan 01 kept the decay half-life claim): re-verify the DOI https://doi.org/10.1088/1674-1137/abddae resolves; confirm the 5700±30 value (or pick 5730 Cambridge if you prefer — record which).

2. CLAIMS (data/citations.json) — for each claim_id, confirm the claim_text is scientifically correct AND correctly drawn from its source:
   - The two HIGH-STAKES claims (if hybrid workflow): `TCA-RNG-CITRATE-PROCHIRALITY-01` (the citrate prochirality mechanism — is the claim text faithful to the source?) and `TCA-RNG-WEIGHT-01` (the 0.5/0.5 RNG weight VALUE — is this the pedagogical value you want? you may change the weight values here, e.g. 0.5/0.5 vs a skewed split — record your choice in review_notes).
   - The routine claims: GLY-PFK-01 (PFK catalyzes the committed step), CAST-PFK-PDB-01 + CAST-CSC-PDB-01 (the cast PDB structures), C14-TIME-01 (the respiration-cycle timescale — only if Plan 01 chose option c/d-iii).

3. For each entry, tell OpenCode your decision: `approve <source_id/claim_id>` or `reject <source_id/claim_id> because <reason>` or `approve <claim_id> with weight=<X>/<Y>` (for TCA-RNG-WEIGHT-01 if you want different values). Rejected entries keep their provenance (review_notes records why) — they are NOT deleted.

You may approve all, approve some + reject some, or ask OpenCode to revise a claim_text before re-reviewing. OpenCode will NOT auto-approve anything — it only records your decisions.
  </how-to-verify>
  <resume-signal>
List your decisions, one per line, e.g.:
`approve LIBRETEXTS-METAB-GLYCOLYSIS`
`approve LIBRETEXTS-METAB-TCA`
`approve LEHNINGER-CH-CITRIC_ACID_CYCLE edition=7th chapter=16`
`approve PDB-4PFK`
`approve PDB-1CSC`
`approve NUBASE2020-C14`
`approve GLY-PFK-01`
`approve TCA-RNG-CITRATE-PROCHIRALITY-01`
`approve TCA-RNG-WEIGHT-01 with weight=0.5/0.5`
`approve CAST-PFK-PDB-01`
`approve CAST-CSC-PDB-01`
`approve C14-TIME-01`
or `reject <id> because <reason>` for any you refuse.
Type "approved all" if you approve every entry as-is, or list the specific approve/reject decisions.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record the human's approval decisions in the registry + run the Phase 1 gate to prove SC#5 on real data</name>
  <files>data/citations.json, data/sources.json</files>
  <action>
Read the human's decisions from Task 1's resume-signal. Mechanically record each decision in the registry (do NOT re-evaluate the science — the human is the approver; OpenCode only records):

1. For each `approve <id>` decision:
   - If `<id>` is a source_id in data/sources.json: set its `approval_status` to `"approved"`, add `approved_by: "human"` and `approved_date: "2026-08-15"`. If the human specified edition/chapter (e.g. Lehninger edition=7th chapter=16), record it in the `notes` field. If the human specified a weight value for TCA-RNG-WEIGHT-01, update that CLAIM's `claim_text` + `review_notes` (NOT the source) with the chosen value.
   - If `<id>` is a claim_id in data/citations.json: set its `approval_status` to `"approved"`, add `approved_by: "human"` and `approved_date: "2026-08-15"`. If the human gave a weight value, update the claim_text + review_notes.

2. For each `reject <id> because <reason>` decision:
   - Set the entry's `approval_status` to `"rejected"`, add `approved_by: "human"` and `approved_date: "2026-08-15"`, and set `review_notes` to the human's reason. Do NOT delete the entry (provenance retained per the c14/citations.py design — a rejected claim fails the gate identically to pending, no Pitfall 6 regression).

3. If the human said "approved all": flip every pending entry in both files to `approved` with `approved_by: "human"` + `approved_date: "2026-08-15"`.

4. Preserve every other field in each entry (source_id, review_tier, claim_text, inherits_source_approval, etc.) — only `approval_status` / `approved_by` / `approved_date` / `review_notes` change. Keep the JSON human-readable (indent=2, trailing newline) and diff-friendly. Do NOT touch tests/fixtures/.

5. VERIFY SC#5 ON REAL DATA: create a temporary fixture story JSON that references the approved first-batch claim_ids (mirror tests/fixtures/story_pass.json structure — a minimal 2-node story with `claim_ids` arrays pointing to the approved claims), then run:
   `python3.6 tools/check_citations.py --story <temp_fixture> --registry data/citations.json`
   - If the human approved all referenced claims: expect exit 0 + "CITATION GATE PASSED: N claim reference(s) ... all approved." — this PROVES SC#5 is operational on real approved data.
   - If the human rejected any referenced claim: expect exit 1 + "[UNAPPROVED] ... status is 'rejected'" — the gate correctly blocks the rejected claim (this is the gate WORKING, not failing — record it as SC#5 enforcement proven).
   Delete the temp fixture after (or move it to tests/fixtures/ as a real regression test if useful — optional).
   Also run the gate against the existing `tests/fixtures/story_pass.json --registry data/citations.json` to confirm the gate still behaves correctly (the placeholder fixture claims are absent from the real registry → exit 1 missing — this is the pre-Plan-04 behavior, unchanged; confirms no regression).

6. Count the approved vs rejected vs still-pending entries and record the totals in the SUMMARY.
  </action>
  <verify>
`python3.6 -c "import json; d=json.load(open('data/citations.json')); print('approved:', sum(1 for v in d.values() if v.get('approval_status')=='approved'), 'rejected:', sum(1 for v in d.values() if v.get('approval_status')=='rejected'), 'pending:', sum(1 for v in d.values() if v.get('approval_status')=='pending'))"` shows the breakdown. `python3.6 -c "import sys; sys.path.insert(0,'.'); from c14.citations import CitationRegistry; r=CitationRegistry.load('data/citations.json'); print('loader OK', len(r))"` succeeds (the approved/rejected entries still load cleanly). The gate run on a fixture referencing only approved claims exits 0 ("CITATION GATE PASSED"). No entry has `approval_status: "pending"` that the human intended to approve (all reviewed entries are approved or rejected).
  </verify>
  <done>The first batch is approved (or rejected with provenance) per the human's decisions. tools/check_citations.py passes (exit 0) on a story referencing the approved claims — SC#5 is operational on real data. SC#4 (first batch of approved sources in the registry) + SC#5 (gate operational on real approved data) are both satisfied. Phase 5 is COMPLETE: all 5 success criteria delivered.</done>
</task>

</tasks>

<verification>
- The human reviewed + decided on every first-batch source + seed claim (no auto-approval).
- Approved entries: `approval_status='approved'` + `approved_by` + `approved_date`.
- Rejected entries: `approval_status='rejected'` + `review_notes` (provenance, not deleted).
- tools/check_citations.py exits 0 on a story referencing only approved claims (SC#5 operational on real data).
- The gate still exits 1 on pending/missing/rejected (no Pitfall 6 regression — verified).
- Phase 5 complete: SC#1 (confirmed), SC#2 (framing decided), SC#3 (framing decided), SC#4 (workflow + first batch approved), SC#5 (gate operational on real approved data).
</verification>

<success_criteria>
- SC#4: the claims registry holds the first batch of approved sources for the glucose critical-path + TCA RNG weights.
- SC#5: the per-claim gate is operational on real data — a story referencing approved claims passes (exit 0); pending/missing/rejected claims are blocked.
- All 5 Phase 5 success criteria are delivered (SC#1 confirmed in Plan 01; SC#2 decided in Plan 01; SC#3 decided in Plan 02; SC#4 workflow in Plan 03 + registry in Plan 04 + approval here; SC#5 operational here).
</success_criteria>

<output>
After completion, create `.planning/phases/05-pre-content-key-decisions-source-approval/05-05-SUMMARY.md` recording: (1) the human's approval decisions (approved count, rejected count + reasons, any weight-value choices for TCA-RNG-WEIGHT-01), (2) the gate verification result (exit 0 on approved-claim fixture = SC#5 operational), (3) PHASE 5 COMPLETE confirmation — all 5 success criteria delivered, (4) flags for downstream: Phase 5.1 (Story Graph Design — needs the ATP decision [done] + anaerobic decision [done] + aerobic-scoped reachability checker config), Phase 6 (Qt UI MVP — needs the workflow operational [done]), Phase 7 (Glucose content — needs the first-batch sources [approved here] + the ATP-soul-jump claim inventory [front-loaded in research, per-claim approval in Phase 7]).
</output>
