---
phase: 05.3-wt-aligned-structure-load-convention
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05.3-wt-aligned-structure-load-convention/05.3-CONVENTION.md
autonomous: true

must_haves:
  truths:
    - "A WT-aligned structure load convention is specified and documented for review: WHEN it fires (branch-node on_enter AFTER the reverse-mutation edit, NOT the restore safety-net path), WHAT it loads (the aligned WT experimental PDB as a separate <enzyme>_wt object), HOW it aligns (cmd.super on align_sele, default 'name CA', transform=1 moves the whole _wt object), HOW it is presented (overlay for major conformational-difference mutants, replace for minor/none or the approach-a restore shortcut; side-by-side multi-viewport deferred to Phase 6)"
    - "The convention is compatible with BOTH mutant-reveal approaches (a apply-edit: backup IS WT so the restore shortcut suffices; b load-mutant-PDB: backup is mutant so the WT-aligned load is the distinct, scientifically meaningful operation) and does NOT pre-decide the Phase 7 OQ-DM2 choice"
    - "The convention's text_teaching template is scientifically honest (a visualization aid, NOT a proof of restoration; cmd.alter changes residue identity but does not repack side chains so the restored model has approximate coordinates) and does NOT imply the C14 carbon becomes ATP (hero-identity anti-confusion: at pyr.pdh the carbon IS correctly shed as CO2; the reveal is about the ENZYME's health, NOT the hero carbon's fate)"
    - "The convention applies ONLY to the 12 DIS-*-cand disease-mutant edit-allowed nodes; tca.citrate_synthase (the edit:structural exception with NO disease mutant) is explicitly excluded"
    - "The op=\"align\" MolAction shape is specified as {op:'align', target:<mobile-moved>, args:{reference:<fixed-stays>, method:'super'|'align'|'cealign', align_sele:'name CA'}} with target=mobile and args['reference']=fixed (named reference, NOT target, to avoid the PyMOL target=fixed collision) — and the molops.py dispatch branch is SPECIFIED but explicitly DEFERRED to Phase 6 (open question Q3 for the checkpoint)"
  artifacts:
    - path: ".planning/phases/05.3-wt-aligned-structure-load-convention/05.3-CONVENTION.md"
      provides: "The WT-aligned structure load convention spec — the reviewable design artifact (SC1) consumed by Phases 5.4/6/7"
      contains: "op=\"align\""
      min_lines: 200
  key_links:
    - from: ".planning/phases/05.3-wt-aligned-structure-load-convention/05.3-CONVENTION.md"
      to: "Phase 5.4 scene-template / cast-reveal convention"
      via: "the §F downstream contract: object set after reveal + <enzyme>_wt naming + alignment state + present directive (5.4 layers show_as/color/zoom/viewport on top; does NOT re-decide object set or re-align)"
      pattern: "<enzyme>_wt"
    - from: ".planning/phases/05.3-wt-aligned-structure-load-convention/05.3-CONVENTION.md"
      to: "Phase 6 molops op=\"align\" dispatch"
      via: "the §E MolAction shape + dispatch pseudocode (Phase 6 implements the elif branch + unit test; 5.3 specifies only)"
      pattern: "op == \"align\""
    - from: ".planning/phases/05.3-wt-aligned-structure-load-convention/05.3-CONVENTION.md"
      to: "Phase 7 per-enzyme content authoring"
      via: "the §F per-enzyme metadata fields table (wt_pdb, mutant_pdb, mutant_reveal_approach, conformational_difference, align_method, align_sele, present) + branch-node on_enter templates (overlay/replace/restore-shortcut)"
      pattern: "conformational_difference"
---

<objective>
Write the WT-aligned experimental structure load CONVENTION as a reviewable design artifact (Phase 5.3 Success Criterion 1).

Purpose: A correct (disease-reversing) edit should reveal what the healthy enzyme looks like — by loading the aligned wild-type experimental structure after the reverse-mutation, especially for mutants with major conformational differences (domain shift) where the edited-mutant model alone is not scientifically illustrative. This phase designs the convention (when/what/how-align/present) as a spec that Phases 5.4 (scene-template), 6 (molops dispatch + UI), and 7 (per-enzyme content) consume WITHOUT re-designing it. This is a 0-requirement design phase — it produces a reviewable artifact, not shipped code.

Output: `.planning/phases/05.3-wt-aligned-structure-load-convention/05.3-CONVENTION.md` — the convention spec document. Pure writing; NO code changes in this plan.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.3-wt-aligned-structure-load-convention/05.3-RESEARCH-pymol-align-api.md
@.planning/phases/05.3-wt-aligned-structure-load-convention/05.3-RESEARCH-integration-design.md
@c14/story/model.py
@c14/pymol_layer/molops.py

# Origin of this phase (the Note 2 imprecision this convention corrects)
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the core convention — when it fires, conditionality, substructure align + honest narrative, presentation default</name>
  <files>.planning/phases/05.3-wt-aligned-structure-load-convention/05.3-CONVENTION.md</files>
  <action>
Create `05.3-CONVENTION.md` in the phase directory. This is the WT-aligned structure load convention spec — the SC1 design artifact. Write it as a structured markdown document with a header (phase, date, status=DRAFT-for-review, the 3 SCs as a checklist) and the following sections. Synthesize from the two research files (already in the phase dir); do NOT re-research — the research is HIGH-confidence and empirically verified. Cite the research files by section number (e.g. "per RESEARCH-integration-design §A").

**§1. Purpose & scope.** One-paragraph statement: the convention specifies the WT-aligned experimental structure load that fires after a correct (disease-reversing) edit to show what the healthy enzyme looks like — especially for mutants with major conformational differences (domain shift). State it is a 0-requirement design/convention phase enabling EDIT-02 restoration-reveal delivery in Phase 6/7. State the scope: applies ONLY to the 12 `DIS-*-cand` disease-mutant edit-allowed nodes; `tca.citrate_synthase` (the `edit:structural` exception — NO disease mutant, per 05.1-DESIGN.md:662,676-682) is EXPLICITLY EXCLUDED. Note the real in-game human-verify is Phase 6; this phase produces a reviewed SPEC + headless mechanism proof (Plan 02).

**§2. When it fires (the placement decision — RESEARCH-integration-design §A).** Document with a numbered trace:
  - The correct edit routes via `EditRouter.route` (known signature → branch node) → `engine._enter(branch_node)` → the branch node's `on_enter` MolActions fire (`c14/engine.py:155-194`).
  - The WT-aligned load fires in the BRANCH NODE's `on_enter` MolAction sequence, AFTER the reverse-mutation `MolAction(op="edit", edit_type="point_mutation", ...)`. This is **option (iii)** of the three placement options.
  - Explicitly state the three options analyzed (i BEFORE the reverse edit = narratively backwards, REJECT; ii restore-safety-net IS the WT reveal = converges only under approach (a), pre-decides OQ-DM2, REJECT as sole mechanism; iii AFTER the reverse edit = RECOMMENDED) with a one-line tradeoff each.
  - **Correct the 05.1-06 Note 2 imprecision:** the Note 2 phrasing ("the restore after a correct edit is where the WT-aligned load would fire") is imprecise. Two distinct mechanisms exist: `MolAction(op="restore",...)` (the EditOps safety net for WRONG edits / smooth gameplay — spec.md:28 item 4) vs the correct-edit branch node's `on_enter` (which applies the reverse-mutation `edit`). A correct edit does NOT trigger the restore safety net — it routes to a branch node. The WT-aligned load is a **branch-node `on_enter` concern, NOT a restore-safety-net concern**. State this correction explicitly.
  - **Exception under approach (a):** when the edit-allowed node's pre-edit `on_enter` loaded the WT base (e.g. 6CFO) and altered it to disease, the backup IS the WT, so the WT-aligned reveal can be replaced by `MolAction(op="restore",...)` as an equivalent shortcut. The convention supports BOTH; Phase 7 picks per-enzyme.
  - State the canonical `on_enter` order for a restoration-reveal branch node (numbered list):
    1. `MolAction(op="edit", edit_type="point_mutation", target=<enzyme>, sele=<disease_resi>, new_resn=<WT_resn>)` — the player's restoration
    2. `MolAction(op="load", source="pdb", code=<WT_PDB>, object="<enzyme>_wt")` — load the real WT experimental structure as a SEPARATE object
    3. `MolAction(op="align", target="<enzyme>_wt", args={reference:<enzyme>, method:..., align_sele:...})` — align WT onto the restored/edited enzyme
    4. presentation MolActions (show_as / color / zoom) — Phase 5.4 layers these
  - State that the WT-aligned load is the spec's "reveal correct 3D model" (`spec.md:28`, item 4) re-cast as the restoration narrative's "this is what healthy looks like" payoff.

**§3. Conditionality (RESEARCH-integration-design §B).** The convention is CONDITIONAL, not unconditional:
  - The MECHANISM is always specified (the load + align/super + present MolAction sequence exists and is headless-prototyped in Plan 02). The convention is a CAPABILITY, always available.
  - Whether it FIRES is a per-enzyme Phase 7 decision, gated on: (a) the mutant-reveal approach chosen for that enzyme (`mutant_reveal_approach: "apply_edit"` vs `"load_mutant_pdb"` — Phase 7 decides per OQ-DM2) AND (b) whether the mutant has a major conformational difference (`conformational_difference: "major"|"minor"|"none"` per-enzyme metadata field, Phase 7-authored + source-approved).
  - **Default firing rule (recommendation, human may override at the checkpoint):** approach (b) + `conformational_difference:"major"` → FIRE the WT-aligned overlay (the scientifically strong reveal; the phase goal's target case). (b) + `"minor"`/`"none"` → optional. (a) + any → the `restore` shortcut suffices (backup IS WT); explicit `_wt` overlay redundant (Phase 7 MAY still use the explicit form for uniformity — Phase 7 discretion).
  - State OQ-DM2 is honored: the convention specifies the mechanism that COMPLEMENTS whichever approach Phase 7 chooses; it does NOT pick (a) or (b).

**§4. How it aligns — substructure superposition for domain-shift mutants (RESEARCH-integration-design §C + RESEARCH-pymol-align-api §B/§C).** Document:
  - **Default alignment method: `cmd.super`, NOT `cmd.align`** (per `fitting.py:316-320`: align is for >30% seq identity; super is sequence-INDEPENDENT, right for divergent/domain-shift). `cmd.cealign` reserved for very-low-homology Phase 7 cases (NOT the headless prototype — needs ≥16 CA, fails on tiny fixtures; reversed arg order `(target, mobile)` + dict return normalized by the dispatch).
  - **Scope the alignment to a stable substructure** via atom selections: `cmd.super("<enzyme>_wt and <align_sele>", "<enzyme> and <align_sele>")`. `transform=1` (default) transforms the WHOLE `<enzyme>_wt` object using the transform derived from the substructure — active site lines up, shifted domain shows the conformational difference (the pedagogical payload). Document that `align_sele` scopes MATCHING, not TRANSFORMATION.
  - A GLOBAL all-atom align is POOR for a domain-shift mutant (high post-outlier RMSD; outlier rejection silently discards the shifted domain → misleading). Substructure/local superposition on a stable reference (active-site core / conserved domain / conserved catalytic residues) is scientifically sound as a VISUALIZATION AID.
  - **`align_sele` is a per-enzyme metadata field** (Phase 7-authored, source-approved — which residues define the stable core). The convention specifies the FIELD; Phase 7 fills it. Default when unspecified: `"name CA"`. `None` = whole object.
  - **Argument-order gotcha (RESEARCH-pymol-align-api §G pitfall 1):** PyMOL's `target` arg = the FIXED reference, NOT "the thing aimed at". `cmd.super(mobile, target)` MOVES mobile, leaves target fixed. For the restoration reveal: mobile = `<enzyme>_wt` (WT slides into the edited enzyme's frame so the camera doesn't jump), target/fixed = `<enzyme>` (the edited enzyme the player is looking at). RMSD is symmetric (optimal superposition minimizes the same quantity regardless of which moves), so this is a VISUAL choice, not a scientific one.
  - Do NOT reimplement alignment — `align`/`super`/`cealign` are BUILT-IN (AGENTS.md). The only custom code is the ~10-line `op="align"` dispatch in molops.py (specified in §6, deferred to Phase 6).

**§5. How it is presented + the honest narrative (RESEARCH-integration-design §C/§D).**
  - **Presentation table** (3 rows: Replacement / Side-by-side / Overlay) with columns: what it shows, pedagogy, scientific clarity, Phase 6 cost. Use the research §D table verbatim-in-content.
  - **Default = OVERLAY for `conformational_difference:"major"` mutants** (the phase goal's target case): the edited/restored `<enzyme>` in one color, `<enzyme>_wt` aligned WT in another color, both visible after active-site-core `super`. The domain shift is the visible pedagogical payload. **Fallback = REPLACE for `"minor"`/`"none"`** (or under approach (a) where `restore` shortcut yields a single WT object). **Side-by-side (multi-viewport) is a Phase 6 UI concern, NOT a 5.3 data-level concern** — 5.3 only ensures the two objects exist + are aligned so 5.4/6 can present overlaid OR split.
  - A `present` directive field (`"overlay"|"replace"`, default per `conformational_difference`) records which case for 5.4.
  - **Honest narrative framing — `text_teaching` template.** State the reveal is a VISUALIZATION AID, NOT a proof of restoration. Provide this suggested template (Phase 7 finalizes with citations; use PLACEHOLDERS, not real PDB IDs):
    > "Your reverse-mutation restored the residue's identity. Because the editing tool changes residue identity without re-packing the side chains, your restored model has approximate coordinates. Here we show the **experimentally determined wild-type structure** (PDB `<WT_PDB>`, `<resolution>` Å), aligned to your enzyme on the active-site core, as a reference for what the healthy enzyme looks like. The regions that don't overlap show the conformational difference of the disease mutant."
  - **Hero-identity anti-confusion constraint (MANDATORY, from AGENTS.md):** add a boxed NOTE: the restoration reveal is about the ENZYME's health, NOT the hero carbon's fate. At `pyr.pdh` specifically, the carbon IS correctly shed as CO2 (decarboxylation); the hero's ELECTRONS continue via NADH → ETC → ATP (soul-jump reframing). The WT-aligned reveal must NOT imply the C14 carbon becomes ATP or enters oxidative phosphorylation — it shows the PDH ENZYME restored, while the carbon substrate is processed (released as CO2). Keep the enzyme-health framing and the carbon-fate framing SEPARATE.
  - **No fabricated science (MANDATORY, from AGENTS.md):** use PLACEHOLDERS `<WT_PDB>`, `<mutant_pdb>`, `<WT_resn>`, `<disease_resi>`, `<resolution>`, `<align_sele>` throughout — Phase 7 fills with source-approved values. 6CFO (WT, 2.70 Å) + 6CER (V138M mutant, 2.69 Å) are the EXAMPLE pair for `pyr.pdh`, already Phase 5.1 CANDIDATE references, but clearly mark them "pending Phase 7 per-claim approval — NOT yet approved". Do NOT invent PDB IDs, DOIs, disease associations, or `align_sele` residue ranges for the other 11 disease-mutant enzymes — those are Phase 7 to source + approve.

Write clearly and concisely; this is a design spec for human review. Use tables where the research uses tables. Each section should be self-contained enough that a reviewer can assess one decision at a time.
  </action>
  <verify>
`test -f .planning/phases/05.3-wt-aligned-structure-load-convention/05.3-CONVENTION.md` succeeds; the file is >= 200 lines; `grep -c "^##" 05.3-CONVENTION.md` shows >= 5 level-2 section headers (§1-§5); `grep -q "op=\"align\"" 05.3-CONVENTION.md` matches (the MolAction shape is referenced); `grep -qi "hero-identity\|enzyme's health\|shed as CO2" 05.3-CONVENTION.md` matches (the anti-confusion note is present); `grep -qi "tca.citrate_synthase" 05.3-CONVENTION.md` matches (the exclusion is documented); `grep -qi "OQ-DM2" 05.3-CONVENTION.md` matches (the deferred decision is honored); and `grep -c "<WT_PDB>\|<enzyme>_wt\|<align_sele>" 05.3-CONVENTION.md` shows the PLACEHOLDER convention is used (no fabricated PDB IDs). Confirm NO real PDB IDs other than 6CFO/6CER appear: `grep -oE "\b[1-9][0-9][A-Z0-9]{3}\b" 05.3-CONVENTION.md | sort -u` should list only 6CFO and 6CER (if any), all marked pending Phase 7.
  </verify>
  <done>
`05.3-CONVENTION.md` exists with §1-§5: purpose/scope (12 DIS-*-cand nodes, citrate_synthase excluded), when-it-fires (branch-node on_enter AFTER reverse-mutation edit, option iii, the 05.1-06 Note 2 correction, the approach-(a) restore-shortcut exception, the canonical on_enter order), conditionality (approach x conformational_difference, OQ-DM2 honored), substructure align (cmd.super default, align_sele per-enzyme Phase 7 field default name CA, the target=fixed gotcha), presentation (overlay/replace/side-by-side-deferred, present directive) + honest text_teaching template + hero-identity anti-confusion NOTE + no-fabricated-science PLACEHOLDERS (6CFO/6CER marked pending Phase 7).
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the integration contracts (MolAction shape + downstream 5.4/6/7 contracts + metadata table + the 8 open questions for the checkpoint)</name>
  <files>.planning/phases/05.3-wt-aligned-structure-load-convention/05.3-CONVENTION.md</files>
  <action>
APPEND the remaining sections (§6, §7, §8) to `05.3-CONVENTION.md` (Task 1 created §1-§5; this task extends the same file). Read the file first to find the insertion point (end of §5), then append.

**§6. The `op="align"` MolAction shape + the molops dispatch (RESEARCH-pymol-align-api §E + RESEARCH-integration-design §F.2).** This is the resolution of the naming disagreement between the two research files: ADOPT the API researcher's recommendation (`target`=mobile, `args["reference"]`=fixed), NOT the design researcher's `args={"mobile":..., "target":...}` (which is redundant with `MolAction.target` AND collides with PyMOL's `target`=fixed semantics).
  - State the MolAction shape (this is pure data, `c14/story/model.py`-compatible — `MolAction(op, target, args)` already supports arbitrary `args` dict keys; NO model change needed):
    ```
    {"op": "align",
     "target": "<enzyme>_wt",                       # the MOBILE object (MOVED)
     "args": {
       "reference": "<enzyme>",                      # the FIXED object (STAYS) — named "reference" NOT "target" to avoid the PyMOL target=fixed collision
       "method": "super",                           # "super"(default) | "align" | "cealign"
       "align_sele": "name CA"                      # optional substructure scope; default "name CA"; None => whole object
     }}
    ```
  - Explain the naming rationale: `MolAction.target` is reused as the mobile object (consistent with how `load`/`edit`/`restore` use `target` as the operated-on object); `args["reference"]` names the fixed object "reference" (NOT "target") because PyMOL's `cmd.super(mobile, target)` arg `target` means FIXED — naming the MolAction field "target" would collide + mislead. This avoids both the redundancy (design researcher's `args.mobile` duplicates `MolAction.target`) and the collision (PyMOL `target`=fixed).
  - Provide the **molops.py dispatch branch pseudocode** (this is a SPEC of what Phase 6 implements — do NOT add it to molops.py in 5.3):
    ```python
    elif op == "align":
        method = action.args.get("method", "super")
        reference = action.args["reference"]
        sele = action.args.get("align_sele")  # None => whole object
        mobile = action.target if sele is None else "{0} and {1}".format(action.target, sele)
        ref = reference if sele is None else "{0} and {1}".format(reference, sele)
        if method == "super":
            # src: tmp/pymol-src/modules/pymol/fitting.py:242 cmd.super  (mobile MOVED, target FIXED)
            self._cmd.super(mobile, ref)
        elif method == "align":
            # src: tmp/pymol-src/modules/pymol/fitting.py:306 cmd.align  (mobile MOVED, target FIXED)
            self._cmd.align(mobile, ref)
        elif method == "cealign":
            # src: tmp/pymol-src/modules/pymol/fitting.py:27 cmd.cealign  NOTE: arg order is (target, mobile) -- REVERSED
            self._cmd.cealign(ref, mobile)  # dispatch normalizes the reversal: ref=target(fixed) first, mobile second
        else:
            raise ValueError("molops.align: unknown method {!r}".format(method))
    ```
  - Note this uses `self._cmd` (already injected — no new dependency), `apply` returns `None` (consistent with all other ops), and the dispatch encapsulates the two API footguns (cealign reversed arg order + the `target`=fixed naming collision) so story authors never hit them. The MolAction layer stays pure data (no pymol import; AST gate holds).
  - **`<enzyme>_wt` naming convention:** a separately-loaded WT experimental object is named `<enzyme>_wt` (e.g. `pdh_wt`). Suffix is stable so 5.4/6/7 can reference deterministically. Do NOT suffix `_mutant` (the mutant IS the live `<enzyme>` object).

**§7. Downstream contracts (RESEARCH-integration-design §F).** Three subsections — the MINIMUM surface each downstream phase needs:
  - **§7.1 For Phase 5.4 (scene-template / cast-reveal):** the object set after reveal (overlay case = `{<enzyme>, <enzyme>_wt}`; replace/restore case = `{<enzyme>}`); the `<enzyme>_wt` naming; the alignment state (`<enzyme>_wt` is ALIGNED (transformed) to `<enzyme>`'s frame via `cmd.super` on `align_sele`; 5.4 does NOT re-align); the `present` directive (`"overlay"|"replace"`, default per `conformational_difference`) tells 5.4 which scene-template branch to apply (5.4 does NOT re-derive `present`). State explicitly: 5.3 owns the DATA-LEVEL mechanism (which objects exist, names, alignment state); 5.4 owns the PRESENTATION directive (representations, colors, zoom, viewport) layered on top. 5.4 does NOT re-decide the object set or alignment.
  - **§7.2 For Phase 6 (Qt UI MVP):** the controller injects `MolOps`; the engine emits the branch node's `on_enter` MolActions per-action to `molops.apply`. Add ONE new MolAction op `op="align"` to `MolOps.apply` (the §6 dispatch). NO new "reveal" super-op, NO new controller branch — composability from existing ops + one new `align` op keeps the dispatch surface flat (mirrors how `load`/`edit`/`restore` were added). The `op="align"` dispatch + its unit test (MockCmd verifying `cmd.super`/`cmd.align`/`cmd.cealign` arg order + `align_sele` scoping composition) LAND IN PHASE 6, NOT 5.3. Flag this as **Open Question Q3** (see §8) — the human may override at the checkpoint to pull it into 5.3 (the 04-05 precedent of add-op+smoke-together).
  - **§7.3 For Phase 7 (content):** the per-enzyme metadata fields table (reproduce the research §F.3 table with columns: Field | Type | Default | Meaning | Source/approval). The fields: `wt_pdb` (string, required when reveal fires, Phase 7 per-claim), `mutant_pdb` (string, approach b only, Phase 7), `mutant_reveal_approach` ("apply_edit"|"load_mutant_pdb", Phase 7 decides OQ-DM2), `conformational_difference` ("major"|"minor"|"none", default "minor", Phase 7 source-approved structural comparison), `align_method` ("super"|"align", default "super", Phase 7 per enzyme), `align_sele` (string selection, default "name CA", Phase 7 source-approved stable core), `present` ("overlay"|"replace", default "overlay" if major else "replace", Phase 7 per enzyme). Then provide the THREE branch-node `on_enter` templates Phase 7 authors: (1) the overlay case (approach b) with the full MolAction sequence including `op="align"`; (2) the replace case (approach b, minor/none) — simpler, single object; (3) the restore-shortcut case (approach a) — `op="restore"` replaces the load+align. Use the `target`=mobile / `args["reference"]`=fixed naming consistently in all templates. Mark all PDB codes as `<WT_PDB>`/`<mutant_pdb>` PLACEHOLDERS (6CFO/6CER as the pyr.pdh example, pending Phase 7).

**§8. Open questions for the human design-review checkpoint (RESEARCH-integration-design §H).** List the 8 open questions, each with: the question, the research RECOMMENDATION, the TRADEOFF/alternative, and a one-line "why it matters". These are the decisions the human makes at the Plan 02 checkpoint. Number them Q1-Q8:
  - **Q1. Always-fire vs conditional-fire.** Rec: conditional. Alt: always-fire for uniformity (avoids redundant reveals under approach a). Why: uniformity vs avoiding redundant reveals.
  - **Q2. Default presentation (overlay vs replace).** Rec: overlay for major, replace otherwise. Alt: replace as sole default (simplest UI). Why: overlay is the scientifically richer choice for the domain-shift pedagogical case the phase goal explicitly targets.
  - **Q3. Where the `op="align"` molops dispatch lands.** Rec: 5.3 specifies the op shape + prototypes `cmd.super` headlessly (direct `cmd.*` in the smoke); the `molops` `op="align"` dispatch + unit test land in Phase 6 (keeps 5.3 a pure design+prototype phase that doesn't touch molops.py in a 0-requirement design phase). Alt: the 04-05 precedent — add `op="align"` to `molops.py` in 5.3 alongside the smoke. Why: both mechanically identical; 5.3-pure keeps the design phase clean; 5.3-adds-op delivers the runnable dispatch sooner. **FLAG: if the human overrides to "add in 5.3", Plan 02 gains a task to edit `c14/pymol_layer/molops.py` + add a unit test in `tests/test_molops.py`.**
  - **Q4. `align_sele` authoring boundary.** Rec: per-enzyme Phase 7 metadata field (source-approved), default `name CA`. Alt: convention default `name CA` for all, Phase 7 overriding only for domain-shift cases. Why: per-enzyme `align_sele` requires a structural-rationale citation in Phase 7; the default covers the common case.
  - **Q5. Side-by-side (multi-viewport) scope.** Rec: OUT of 5.3's data-level scope (Phase 6 UI concern); 5.3 only guarantees two objects exist + are aligned. Why: multi-viewport is non-trivial headless verification + a UI layout concern, not a data concern.
  - **Q6. Approach (a) reveal redundancy.** Rec: `restore` suffices under (a); explicit `<enzyme>_wt` overlay redundant. Alt: Phase 7 may still use the explicit form for content-authoring uniformity. Why: avoids redundant reveals but allows per-enzyme author discretion.
  - **Q7. PDB sourcing boundary.** 6CFO + 6CER are already 5.1 CANDIDATE (pending Phase 7 per-claim approval, NOT yet approved). Any OTHER WT/mutant PDB pair, disease association, or `align_sele` residue range for the other 11 disease-mutant enzymes is **Phase 7 to source + approve**. The 5.3 convention + prototype must NOT invent PDB IDs, DOIs, or disease links. Why: AGENTS.md no-fabricated-science constraint.
  - **Q8. `conformational_difference` source.** Rec: Phase 7 evaluates per enzyme (structural comparison of WT vs mutant PDB; a citation). 5.3 defines the FIELD; Phase 7 fills + approves. "major" is a structural-science claim needing a source. Why: the firing + presentation depend on it, so it must be source-backed, not asserted.

End the document with a "## Review checklist for the human" section listing the 7 review criteria from RESEARCH-integration-design §G (where-it-fires soundness; conditionality correctness/OQ-DM2; substructure-align soundness/align_sele boundary; presentation default; narrative honesty + hero-identity; downstream contract sufficiency + minimality; API verification cross-check against the Plan 02 smoke output). This tells the human exactly what to assess at the Plan 02 checkpoint.
  </action>
  <verify>
`grep -c "^##" 05.3-CONVENTION.md` shows >= 8 level-2 headers (§1-§8 + review checklist); `grep -q "\"reference\"" 05.3-CONVENTION.md` matches (the fixed-object naming); `grep -q "args\[.reference.\]" 05.3-CONVENTION.md` matches; `grep -qi "deferred to Phase 6\|lands in Phase 6\|land in Phase 6" 05.3-CONVENTION.md` matches (the op="align" dispatch deferral is flagged); `grep -qi "Q3" 05.3-CONVENTION.md` matches (open question Q3 is surfaced); `grep -c "Q1\|Q2\|Q3\|Q4\|Q5\|Q6\|Q7\|Q8" 05.3-CONVENTION.md` shows all 8 open questions present; `grep -qi "mutant_reveal_approach\|conformational_difference\|align_sele\|align_method" 05.3-CONVENTION.md` matches (the §7.3 metadata fields are documented); `grep -qi "overlay\|replace\|restore.shortcut\|restore-shortcut" 05.3-CONVENTION.md` matches (the 3 on_enter templates are documented). Confirm `c14/pymol_layer/molops.py` was NOT modified: `git diff --name-only c14/pymol_layer/molops.py` shows no changes (the dispatch is specified-only, deferred to Phase 6).
  </verify>
  <done>
`05.3-CONVENTION.md` complete with §6 (the op="align" MolAction shape: target=mobile, args["reference"]=fixed, method, align_sele; the molops dispatch pseudocode SPEC; the <enzyme>_wt naming; the Phase-6 deferral flag), §7 (downstream contracts: 5.4 object-set/naming/alignment/present; 6 op="align" dispatch deferred; 7 the 7-field metadata table + 3 on_enter templates), §8 (the 8 open questions Q1-Q8 with recommendations + tradeoffs), and the review checklist. `molops.py` is NOT touched (dispatch deferred to Phase 6).
  </done>
</task>

</tasks>

<verification>
- `05.3-CONVENTION.md` exists in the phase directory and is a coherent, reviewable design artifact (>= 200 lines, >= 8 sections).
- The convention specifies when (branch-node on_enter after reverse-mutation edit, option iii), what (aligned WT experimental PDB as `<enzyme>_wt`), how (cmd.super on align_sele default name CA, transform=1 whole object), how-presented (overlay/replace, side-by-side deferred) — SC1.
- The naming resolution is baked in: `target`=mobile, `args["reference"]`=fixed (NOT the design researcher's `args.mobile`/`args.target`).
- The `op="align"` molops dispatch deferral to Phase 6 is explicitly flagged (open question Q3) so the human can override at the checkpoint.
- All 8 open questions (§H Q1-Q8) are surfaced with recommendations + tradeoffs.
- No fabricated science: only 6CFO/6CER appear (marked pending Phase 7); all other PDB IDs/disease/align_sele are PLACEHOLDERS.
- Hero-identity anti-confusion: the text_teaching template + a NOTE separate enzyme-health from carbon-fate; no "carbon becomes ATP" implication.
- `c14/pymol_layer/molops.py` is NOT modified (this plan is pure design writing).
- `python3.6 -m py_compile` is N/A (no .py files in this plan); no tests added (design phase).
</verification>

<success_criteria>
- SC1 delivered: a WT-aligned structure load convention is specified (when/what/how-align/how-presented) and documented for review in `05.3-CONVENTION.md`.
- The convention is compatible with both mutant-reveal approaches (OQ-DM2 honored) and conditionally fires per-enzyme.
- The convention is scientifically honest (visualization aid not proof; substructure align for domain shifts) and respects hero-identity (enzyme health, not carbon fate).
- The downstream contracts (5.4/6/7) + the `op="align"` MolAction shape + the 8 open questions are documented so Plan 02's checkpoint + Phases 5.4/6/7 can consume without re-design.
- No code modified; no fabricated science; planning doc committed.
</success_criteria>

<output>
After completion, create `.planning/phases/05.3-wt-aligned-structure-load-convention/05.3-01-SUMMARY.md`
</output>
