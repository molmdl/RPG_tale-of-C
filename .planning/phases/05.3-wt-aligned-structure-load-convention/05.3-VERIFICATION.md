---
phase: 05.3-wt-aligned-structure-load-convention
verified: 2026-08-28T00:00:00Z
status: passed
score: 8/8 must-haves verified
re_verification: No — initial verification
---

# Phase 5.3: WT-Aligned Experimental Structure Load Convention — Verification Report

**Phase Goal:** The convention for loading the aligned wild-type experimental structure after a correct (disease-reversing) edit is designed as a reviewable artifact — especially for mutants with major conformational differences (e.g. a large domain shift), where showing the aligned WT experimental structure (vs just the edited-mutant model) gives a scientifically stronger + visually clearer "this is what healthy looks like" restoration reveal.
**Verified:** 2026-08-28
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

The must_haves are split across the two PLAN frontmatters (Plan 01 = SC1 design spec; Plan 02 = SC2 headless prototype + SC3 human checkpoint). All 8 truths verified against the ACTUAL codebase — not the SUMMARY claims. The headless smoke was re-run by the verifier (not trusted from the SUMMARY).

| #  | Truth | Status | Evidence |
| -- | ----- | ------ | -------- |
| 1  | A WT-aligned structure load convention is specified + documented: WHEN (branch-node on_enter AFTER reverse-mutation edit, NOT restore safety-net), WHAT (aligned WT experimental PDB as separate `<enzyme>_wt` object), HOW it aligns (cmd.super on align_sele, default `name CA`, transform=1 moves whole _wt object), HOW presented (overlay for major, replace for minor/none or approach-a restore-shortcut; side-by-side deferred to Phase 6) | ✓ VERIFIED | `05.3-CONVENTION.md` 415 lines: §1 scope, §2 when-it-fires (option iii + 05.1-06 Note 2 correction + approach-a restore-shortcut + canonical on_enter order edit→load→align→present), §4 cmd.super default + substructure align + target=fixed gotcha, §5 presentation table (overlay/replace/side-by-side-deferred) + `present` directive |
| 2  | The convention is compatible with BOTH mutant-reveal approaches (a apply-edit / b load-mutant-PDB) and does NOT pre-decide Phase 7 OQ-DM2 | ✓ VERIFIED | §3 conditionality table (approach × conformational_difference); §2 "OQ-DM2 is honored: the convention specifies the mechanism that COMPLEMENTS whichever approach Phase 7 chooses; it does NOT pick (a) or (b)"; §2 approach-(a) restore-shortcut exception; OQ-DM2 referenced 6× |
| 3  | The text_teaching template is scientifically honest (visualization aid NOT proof; approximate-coords caveat) and does NOT imply the C14 carbon becomes ATP (hero-identity anti-confusion: at pyr.pdh carbon IS shed as CO2; reveal is about ENZYME's health) | ✓ VERIFIED | §5 `text_teaching` template (line 183: "visualization aid, NOT a proof"; "approximate coordinates"); boxed MANDATORY Hero-identity anti-confusion NOTE (lines 185-189: "ENZYME's health, NOT the hero carbon's fate"; "shed as CO2"; "must NOT imply the C14 carbon becomes ATP or enters oxidative phosphorylation"; "Keep the enzyme-health framing and the carbon-fate framing SEPARATE") |
| 4  | The convention applies ONLY to the 12 DIS-*-cand disease-mutant edit-allowed nodes; tca.citrate_synthase (edit:structural exception, NO disease mutant) is EXPLICITLY excluded | ✓ VERIFIED | §1 scope (line 27: lists the 12 `DIS-*-cand` nodes); §1 EXPLICIT EXCLUSION (line 29: "tca.citrate_synthase ... has NO disease mutant ... The WT-aligned load does NOT fire for tca.citrate_synthase"); `tca.citrate_synthase` mentioned 2× |
| 5  | The op="align" MolAction shape is specified as `{op:'align', target:<mobile-moved>, args:{reference:<fixed-stays>, method, align_sele}}` (target=mobile, args["reference"]=fixed) — and the molops.py dispatch branch is SPECIFIED but explicitly DEFERRED to Phase 6 (Q3) | ✓ VERIFIED | §6 (lines 220-229): MolAction shape jsonc with `target`=<enzyme>_wt (MOBILE) + `args["reference"]`=<enzyme> (FIXED); naming resolution (lines 207-214: adopts API researcher's rec over design researcher's); molops dispatch pseudocode (lines 240-258: `elif op == "align"`); DEFERRAL FLAG (line 260: "land in Phase 6, NOT 5.3"); `op == "align"` present 1× |
| 6  | The convention is headless-prototyped: load mutant → apply correct edit (EditOps.point_mutation) → load WT → align WT(mobile) onto edited enzyme(fixed) via cmd.super → verify finite RMSD + matched-atoms>0 + mobile coords CHANGED → substructure-scoped align (cmd.super on 'name CA' moves whole WT object) | ✓ VERIFIED | Verifier RAN the smoke headlessly via `bash tools/run_headless.sh tools/wt_align_smoke.py` (exit 0). Sentinel `/tmp/opencode/wt_align_smoke.txt` shows all 8 stage checks PASS: `load_mutant` atoms=17; `apply_correct_edit` RestoreHandle; `edit_took` resi1 GLY 10 atoms; `load_wt` atoms=14; `super_returns_finite_tuple` RMSD=0.5497 5 matched atoms; `super_moved_mobile` x:8.85→1.11; `scoped_super_matched_ca` 1 CA; `scoped_super_moved_whole_wt` x:6.11→1.46. Final `SMOKE_RESULT: PASS` |
| 7  | The headless prototype prints SMOKE_RESULT: PASS (the STDOUT sentinel; the bat always returns 0) — the 3-tier testability pattern (proves the cmd.super MECHANISM, NOT real science) | ✓ VERIFIED | Verifier RAN the smoke; stdout sentinel file contains `SMOKE_RESULT: PASS`. The smoke docstring (lines 6-10) explicitly states it proves the MECHANISM on placeholder fixtures, NOT real science (placeholder coords arbitrary; real RMSD-quality gate is Phase 7). The 4 "placeholder" mentions in the smoke are this 3-tier documentation (legit, not stubs) |
| 8  | The convention is human-reviewed for scientific accuracy + narrative clarity via a design-review checkpoint (the real in-game human-verify is Phase 6) | ✓ VERIFIED | `05.3-02-PLAN.md` Task 3 (lines 208-234) is `type="checkpoint:human-verify" gate="blocking"`. `05.3-02-SUMMARY.md` (lines 70, 93-95) documents RESOLUTION: "human typed 'approved'"; all 8 open questions (§8 Q1-Q8) accepted at research recommendations; Q3 = defer op="align" molops dispatch to Phase 6. `.planning/STATE.md` independently records "SC3 human design-review checkpoint APPROVED". Git log shows the final `cf97dde` commit. No `05.3-CONVENTION.md` modifications were needed (approved as-is) |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `.planning/phases/05.3-wt-aligned-structure-load-convention/05.3-CONVENTION.md` | SC1 design spec; contains `op="align"`; min 200 lines | ✓ VERIFIED | EXISTS, 415 lines (≥200 ✓), 10 H2 sections (SC checklist + §1-§8 + review checklist), `op="align"` ×12, substantive (no stubs), WIRED to 5.4/6/7 via downstream contracts §7 |
| `c14/data/assets/bundled/_wt_align_mut.pdb` | mutant placeholder (ALA-GLY); contains ATOM | ✓ VERIFIED | EXISTS, 17 ATOM records (ALA-GLY), ALA×10 + GLY×7, `END` terminator, valid PDB column format, NOT gitignored (ships in zip) |
| `c14/data/assets/bundled/_wt_align_wt.pdb` | WT placeholder (GLY-GLY, offset coords); contains ATOM | ✓ VERIFIED | EXISTS, 14 ATOM records (GLY-GLY, 0 ALA ✓), GLY×14, `END` terminator, coords offset +5 Å in x (verified by smoke: super does real work → finite non-zero RMSD 0.5497), NOT gitignored |
| `tools/wt_align_smoke.py` | SC2 headless prototype; contains SMOKE_RESULT; min 120 lines | ✓ VERIFIED | EXISTS, 229 lines (≥120 ✓), `SMOKE_RESULT` ×3, `cmd.super` ×8, 13 `# src:` citations, `cmd.iterate_state` ×14 (the auto-fixed coordinate-capture API), passes `python3.6 -m py_compile`, RUNS headlessly with `SMOKE_RESULT: PASS` |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `05.3-CONVENTION.md` | Phase 5.4 scene-template / cast-reveal | §7.1 downstream contract (object set + `<enzyme>_wt` naming + alignment state + `present` directive) | ✓ WIRED | `<enzyme>_wt` pattern ×29 in CONVENTION; §7.1 (lines 272-281) explicit "5.4 does NOT re-decide the object set or re-align" |
| `05.3-CONVENTION.md` | Phase 6 molops op="align" dispatch | §6 MolAction shape + dispatch pseudocode (Phase 6 implements the elif branch + unit test) | ✓ WIRED | `op == "align"` pattern ×1 (dispatch pseudocode lines 240-258); §7.2 (lines 283-288) "Add ONE new MolAction op `op="align"`"; DEFERRAL FLAG explicit (line 260) |
| `05.3-CONVENTION.md` | Phase 7 per-enzyme content authoring | §7.3 7-field per-enzyme metadata table + 3 branch-node on_enter templates | ✓ WIRED | `conformational_difference` pattern ×11; §7.3 (lines 290-350) 7-field table (wt_pdb, mutant_pdb, mutant_reveal_approach, conformational_difference, align_method, align_sele, present) + 3 templates (overlay/replace/restore-shortcut) |
| `tools/wt_align_smoke.py` | fitting.py (cmd.super / cmd.align) | direct cmd.super call with `# src:` citation (mechanism proof; NOT via molops — dispatch deferred to Phase 6) | ✓ WIRED | `cmd\.super` ×8 with `# src: tmp/pymol-src/modules/pymol/fitting.py:242` citations; smoke calls cmd.super DIRECTLY (confirmed: 0 "align" refs in molops.py) |
| `tools/wt_align_smoke.py` | edit_ops.py (EditOps.point_mutation) | Stage 2 applies the correct reverse-mutation edit (reuses Phase 4 proven edit path) | ✓ WIRED | `point_mutation` ×2 (Stage 2: `editops.point_mutation("mut", "mut and resi 1", "GLY")`); injects REAL pymol.cmd into EditOps (3-tier pattern) |
| `tools/wt_align_smoke.py` | `_wt_align_*.pdb` fixtures | `c14.paths.data_path(...)` cwd-independent resolution (NOT relative paths) | ✓ WIRED | `c14.paths.data_path` ×2 (mut_path + wt_path); confirmed headless load works (smoke passed) |

### Requirements Coverage

Phase 5.3 owns 0 requirements (convention/design phase per ROADMAP.md line 171; enables EDIT-02 restoration-reveal delivery in Phase 6/7). No REQUIREMENTS.md rows to check. The phase's "requirements" are its 3 success criteria, all verified above (SC1 = truths 1-5 + CONVENTION artifact; SC2 = truths 6-7 + smoke + fixtures artifacts; SC3 = truth 8).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `tools/wt_align_smoke.py` | 6, 10, 75, 116 | "placeholder" (×4) | ℹ️ Info | NOT a stub — these are the 3-tier testability pattern documentation (the smoke explicitly proves the MECHANISM on placeholder fixtures, NOT real science; "placeholder coords are arbitrary; a real RMSD-quality gate is Phase 7 content"). By design, legit. |

No blockers. No TODO/FIXME/HACK. No empty returns. No console.log-only implementations. The 4 "placeholder" matches are all docstring/comments describing the legitimate placeholder-fixture testing pattern (the phase's explicit methodology).

### Human Verification Required

None pending. SC3 (the human design-review checkpoint) was a `checkpoint:human-verify gate="blocking"` task (Plan 02 Task 3) that has ALREADY been performed and resolved — the human typed "approved", all 8 open questions accepted at research recommendations, Q3 = defer op="align" dispatch to Phase 6. This is documented in `05.3-02-SUMMARY.md` (lines 70, 93-95) and independently recorded in `.planning/STATE.md` ("SC3 human design-review checkpoint APPROVED"). The "real in-game human-verify" is explicitly Phase 6, NOT this phase (per ROADMAP.md SC3 + CONVENTION §1 line 31).

### Gaps Summary

No gaps. All 8 must-have truths verified. All 7 artifacts exist, are substantive, and are wired. All 6 key links are connected. All 4 constraints met:
- `c14/pymol_layer/molops.py` NOT modified — `git diff --name-only c14/pymol_layer/molops.py` is empty; `git log` since 2026-08-20 touching molops.py is empty; 0 `align`/`op == "align"` references in molops.py (dispatch correctly deferred to Phase 6 per Q3).
- No fabricated science — the only real PDB IDs anywhere in the convention are 6CFO (×9) and 6CER (×8), both the pyr.pdh worked example, both already Phase 5.1 CANDIDATE references, both explicitly marked "pending Phase 7 per-claim approval — NOT yet approved". The smoke + fixtures use only placeholder peptides (ALA-GLY / GLY-GLY); the line-number-like sequences (1412, 1578, 1610) in the smoke are `# src:` citation line numbers, NOT PDB IDs. ("2026" is a date false-positive.)
- Hero-identity anti-confusion — the boxed MANDATORY NOTE in §5 (lines 185-189) cleanly separates enzyme-health (the reveal's subject) from carbon-fate (pyr.pdh carbon shed as CO2; electrons via NADH→ETC→ATP); explicitly forbids implying the C14 carbon becomes ATP or enters oxidative phosphorylation.
- AST gate clean — `python3.6 tools/check_imports.py` exits 0 ("no pymol/PyQt5 imports in c14/ domain tier"; the smoke lives in `tools/` and uses `pymol.cmd` directly, which is allowed for headless smokes).

The one auto-fixed deviation (`cmd.iterate` → `cmd.iterate_state`, editing.py:1578, for coordinate capture) changes the smoke's coordinate-capture mechanics ONLY — it does NOT touch the convention decisions, post-conditions, or the §6 molops dispatch pseudocode (which does not capture coordinates). No scope creep. Documented in `05.3-02-SUMMARY.md` Deviations section (lines 100-109).

---

_Verified: 2026-08-28_
_Verifier: OpenCode (gsd-verifier)_
