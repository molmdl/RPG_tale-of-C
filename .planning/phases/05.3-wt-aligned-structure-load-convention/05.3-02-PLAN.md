---
phase: 05.3-wt-aligned-structure-load-convention
plan: 02
type: execute
wave: 2
depends_on:
  - 05.3-01
files_modified:
  - c14/data/assets/bundled/_wt_align_mut.pdb
  - c14/data/assets/bundled/_wt_align_wt.pdb
  - tools/wt_align_smoke.py
autonomous: false

must_haves:
  truths:
    - "The WT-aligned structure load convention is headless-prototyped on a placeholder: load a mutant -> apply the correct reverse-mutation edit (EditOps.point_mutation) -> load the WT placeholder -> align WT(mobile) onto the edited enzyme(fixed) via cmd.super -> verify alignment via finite RMSD + matched-atoms>0 + mobile coords CHANGED -> verify substructure-scoped align (cmd.super on 'name CA' moves the whole WT object)"
    - "The headless prototype prints SMOKE_RESULT: PASS (the STDOUT sentinel; the bat always returns 0 so the sentinel is the only reliable verdict) — the 3-tier testability pattern: the smoke proves the cmd.super/cmd.align MECHANISM (load -> align -> finite RMSD -> mobile transformed), NOT real science (placeholder coords are arbitrary)"
    - "The convention is human-reviewed for scientific accuracy + narrative clarity via a design-review checkpoint (the real in-game human-verify is Phase 6) — the human reviews 05.3-CONVENTION.md + the smoke output + the 8 open questions and approves or specifies adjustments"
  artifacts:
    - path: "c14/data/assets/bundled/_wt_align_mut.pdb"
      provides: "mutant placeholder fixture (2-residue ALA-GLY peptide, the 'disease' state; correct edit = point_mutation resi 1 ALA->GLY)"
      contains: "ATOM"
    - path: "c14/data/assets/bundled/_wt_align_wt.pdb"
      provides: "WT placeholder fixture (2-residue GLY-GLY peptide, coords OFFSET a few Angstrom from the mutant so cmd.super does real work + gives a non-zero-but-finite RMSD)"
      contains: "ATOM"
    - path: "tools/wt_align_smoke.py"
      provides: "headless WT-aligned-load prototype smoke (SC2) — loads mutant, applies correct edit via EditOps.point_mutation, loads WT, aligns via cmd.super directly (NOT via molops; op=\"align\" dispatch deferred to Phase 6), verifies finite RMSD + matched atoms + coords changed + substructure-scoped align"
      contains: "SMOKE_RESULT"
      min_lines: 120
  key_links:
    - from: "tools/wt_align_smoke.py"
      to: "tmp/pymol-src/modules/pymol/fitting.py (cmd.super / cmd.align)"
      via: "direct cmd.super call with # src citation on the line above (the mechanism proof; NOT via molops.apply — the op=\"align\" dispatch is deferred to Phase 6 per convention §6/§8 Q3)"
      pattern: "cmd\\.super"
    - from: "tools/wt_align_smoke.py"
      to: "c14/pymol_layer/edit_ops.py (EditOps.point_mutation)"
      via: "Stage 2 applies the correct reverse-mutation edit (reuses the Phase 4 proven edit path; EditOps internal citations live in edit_ops.py)"
      pattern: "point_mutation"
    - from: "tools/wt_align_smoke.py"
      to: "c14/data/assets/bundled/_wt_align_*.pdb"
      via: "c14.paths.data_path(...) cwd-independent resolution (NOT relative paths — PyMOL resolves against cmd.exe cwd, breaks headlessly)"
      pattern: "c14\\.paths\\.data_path"
---

<objective>
Headless-prototype the WT-aligned structure load convention (SC2) on placeholder/bundled structures, then run the human design-review checkpoint (SC3).

Purpose: Prove the `cmd.super`/`cmd.align` MECHANISM works end-to-end headlessly (load mutant → apply correct edit → load + align WT → verify alignment) so the convention spec from Plan 01 is backed by a runnable proof, then have the human review the convention + the prototype output + the 8 open questions. This is the 3-tier testability pattern: the smoke proves the real PyMOL API contract on a placeholder, NOT real science (Phase 7 sources real structures).

Output: two PDB fixtures (`_wt_align_mut.pdb`, `_wt_align_wt.pdb`), the headless smoke (`tools/wt_align_smoke.py`) printing `SMOKE_RESULT: PASS`, and a human-approved (or adjusted) convention.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.3-wt-aligned-structure-load-convention/05.3-CONVENTION.md
@.planning/phases/05.3-wt-aligned-structure-load-convention/05.3-RESEARCH-pymol-align-api.md
@.planning/phases/05.3-wt-aligned-structure-load-convention/05.3-RESEARCH-integration-design.md

# Established patterns to follow (read these before writing the smoke)
@tools/edit_smoke.py
@tools/run_headless.sh
@c14/paths.py
@c14/pymol_layer/edit_ops.py
@c14/data/assets/bundled/_edit_smoke.pdb
@c14/pymol_layer/molops.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the two PDB placeholder fixtures (mutant + WT, offset coords)</name>
  <files>c14/data/assets/bundled/_wt_align_mut.pdb, c14/data/assets/bundled/_wt_align_wt.pdb</files>
  <action>
Create two hand-written tiny PDB fixtures in `c14/data/assets/bundled/`, following the `_edit_smoke.pdb` / `_his_smoke.pdb` precedent (hand-written ATOM records, no network/fragment-library dependency, ~1 KB each). Read `c14/data/assets/bundled/_edit_smoke.pdb` first as the format template (it is a 2-residue ALA-GLY peptide, 17 atoms, valid PDB column format).

**`_wt_align_mut.pdb` — the MUTANT placeholder (the "disease" state).** A 2-residue ALA-GLY peptide. Resi 1 = ALA (the "disease" residue — alanine at a position that should be glycine in the WT); resi 2 = GLY. This is the structure the player sees pre-edit. The "correct edit" (applied in the smoke's Stage 2) is `EditOps.point_mutation("mut", "mut and resi 1", "GLY")` — reversing the disease ALA back to the WT GLY identity. Use the SAME atom set + coordinates as `_edit_smoke.pdb` (you may copy its content verbatim — same ALA-GLY, 17 atoms: N/CA/C/O/CB/H/HA/HB1/HB2/HB3 for ALA resi 1; N/CA/C/O/H/HA2/HA3 for GLY resi 2). A separate file (not reusing `_edit_smoke.pdb` by name) keeps the 5.3 prototype self-documenting + decoupled from Phase 4's fixture.

**`_wt_align_wt.pdb` — the WT placeholder (the "healthy" experimental structure).** A 2-residue GLY-GLY peptide (14 atoms: N/CA/C/O/H/HA2/HA3 per residue — NO CB, since GLY has no side chain). This represents the experimentally-determined WT structure loaded AFTER the correct edit. **The coordinates MUST be OFFSET from the mutant's GLY-GLY region by a few Angstrom** (e.g. +5.0 Å in x for every atom) so `cmd.super` does REAL work + returns a NON-zero-but-finite RMSD (if the WT coords were identical to the mutant's, super would trivially return RMSD=0 and the prototype wouldn't prove the mechanism does anything). Concretely: take the GLY resi-2 coordinates from `_edit_smoke.pdb` (atoms 11-17) for BOTH resi 1 and resi 2 of the WT fixture (so the WT is a clean GLY-GLY), then add +5.0 to every x-coordinate. Use valid PDB column format (ATOM number, atom name, resn=GLY, chain A, resi 1 then 2, x/y/z, occ=1.00, bfac=0.00, element). End each file with `END`.

Both files are network-free, deterministic, ~1 KB. They ship in the plugin zip (committed to `c14/data/assets/bundled/`, NOT gitignored — confirm with `git check-ignore c14/data/assets/bundled/_wt_align_mut.pdb` returns nothing).
  </action>
  <verify>
`test -f c14/data/assets/bundled/_wt_align_mut.pdb && test -f c14/data/assets/bundled/_wt_align_wt.pdb` succeeds. `grep -c "^ATOM" c14/data/assets/bundled/_wt_align_mut.pdb` == 17 (ALA-GLY). `grep -c "^ATOM" c14/data/assets/bundled/_wt_align_wt.pdb` == 14 (GLY-GLY, no CB). `grep -q "ALA" c14/data/assets/bundled/_wt_align_mut.pdb` matches (disease residue present). `grep -c "ALA" c14/data/assets/bundled/_wt_align_wt.pdb` == 0 (WT is pure GLY-GLY, no ALA). `grep -q "END" c14/data/assets/bundled/_wt_align_mut.pdb && grep -q "END" c14/data/assets/bundled/_wt_align_wt.pdb` (valid PDB terminator). `git check-ignore c14/data/assets/bundled/_wt_align_mut.pdb` returns nothing (NOT gitignored — ships in plugin zip). Both fixtures load headlessly (verified in Task 2's smoke).
  </verify>
  <done>
Two valid hand-written PDB fixtures exist in `c14/data/assets/bundled/`: `_wt_align_mut.pdb` (ALA-GLY mutant, 17 atoms) and `_wt_align_wt.pdb` (GLY-GLY WT, 14 atoms, coords offset ~5 Å in x from the mutant's GLY region). Both are committed-asset candidates (not gitignored), network-free, and ready for the smoke to load via `c14.paths.data_path(...)`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write tools/wt_align_smoke.py + run it headlessly; assert SMOKE_RESULT: PASS</name>
  <files>tools/wt_align_smoke.py</files>
  <action>
Create `tools/wt_align_smoke.py` following the `tools/edit_smoke.py` pattern EXACTLY (read `tools/edit_smoke.py` first — it is the established template). The smoke is a pure `pymol.cmd.*` script (NO Qt) that injects the REAL `pymol.cmd` into `EditOps` and calls `cmd.super`/`cmd.align` DIRECTLY (NOT via `molops.apply` — the `op="align"` molops dispatch is deferred to Phase 6 per convention §6/§8 Q3; this smoke proves the mechanism the dispatch will eventually wrap).

**Header + boilerplate (mirror edit_smoke.py):**
- Module docstring: "Phase 5.3 Plan 02 WT-aligned structure load headless prototype (SC2). Pure pymol.cmd.* script (NO Qt) that exercises the WT-aligned-load MECHANISM on placeholder fixtures: load mutant -> apply correct reverse-mutation edit -> load WT -> align WT(mobile) onto edited enzyme(fixed) via cmd.super -> verify finite RMSD + matched atoms + mobile coords changed + substructure-scoped align. Proves the MECHANISM, NOT real science (placeholder coords are arbitrary; a real RMSD-quality gate is Phase 7 content)."
- Reuse the CRITICAL CONTRACT RULES comment block from edit_smoke.py verbatim (Gotcha #1: process always exits 0, SMOKE_RESULT sentinel is the only verdict; Gotcha #2: `__file__` in a PyMOL-run script is wrong, use `os.getcwd()` + `c14.paths`; Gotcha #6: `pymol.finish_launching()` before any cmd.* call).
- `sys.path.insert(0, os.getcwd())` then `import pymol`, `from pymol import cmd`, `import c14.paths`, `import math`, `from c14.pymol_layer.edit_ops import EditOps`. Then `pymol.finish_launching()`.
- `FAILS = []` + `def check(name, ok, detail=""):` printing `SMOKE: {PASS|FAIL} {name} {detail}` and appending to FAILS on failure (copy from edit_smoke.py).

**Stage 1 — load the MUTANT placeholder fixture:**
- `mut_path = str(c14.paths.data_path("data", "assets", "bundled", "_wt_align_mut.pdb"))`
- `# src: tmp/pymol-src/modules/pymol/importing.py:635 cmd.load` then `cmd.load(mut_path, "mut")`
- `# src: tmp/pymol-src/modules/pymol/querying.py:1412 cmd.count_atoms` then `n_mut = cmd.count_atoms("mut")`
- `check("load_mutant", n_mut > 0, "atoms=%d" % n_mut)`

**Stage 2 — apply the CORRECT reverse-mutation edit (the player's restoration):**
- `editops = EditOps(cmd)` (inject the REAL pymol.cmd — the 3-tier testability pattern; unit tests use MockCmd, this smoke uses real cmd).
- `# The correct edit reverses the disease: ALA(disease) -> GLY(WT identity) at resi 1` then `handle = editops.point_mutation("mut", "mut and resi 1", "GLY")`
- `check("apply_correct_edit", handle is not None, "handle=%r" % handle)`
- `# src: .../querying.py:1412 cmd.count_atoms` then `n_gly = cmd.count_atoms("mut and resi 1 and resn GLY")`; `check("edit_took", n_gly > 0, "resi1 GLY atoms=%d" % n_gly)`
- Add a NOTE comment: the live `mut` object still has 17 atoms after the alter (cmd.alter changes resn, NOT atom count — the spurious CB/HB atoms from ALA remain; this is the "approximate coordinates" caveat the convention §5 text_teaching template documents — the restored model has WT residue IDENTITY but approximate GEOMETRY).

**Stage 3 — load the WT placeholder fixture:**
- `wt_path = str(c14.paths.data_path("data", "assets", "bundled", "_wt_align_wt.pdb"))`
- `# src: .../importing.py:635 cmd.load` then `cmd.load(wt_path, "wt")`
- `# src: .../querying.py:1412 cmd.count_atoms` then `n_wt = cmd.count_atoms("wt")`
- `check("load_wt", n_wt > 0, "atoms=%d" % n_wt)`

**Stage 4 — align WT (MOBILE, moved) onto the edited enzyme (FIXED, stays) — the core restoration-reveal mechanism:**
- Capture the WT CA coordinate BEFORE align. Use `cmd.iterate` with a `space` dict to collect coords into a list (verify the exact signature against `tmp/pymol-src/modules/pymol/editing.py` — `iterate(selection, expression, quiet=1, space=None, ...)` at `editing.py:1490`; the expression runs in the `space` namespace with `x,y,z` available per atom):
  ```python
  before = []
  # src: tmp/pymol-src/modules/pymol/editing.py:1490 cmd.iterate
  cmd.iterate("wt and name CA", "before.append((x, y, z))", space={"before": before})
  before_x = before[0][0] if before else None
  ```
- Run the alignment. **`cmd.super(mobile, target)` — mobile MOVED, target FIXED** (the convention §4 argument-order gotcha). mobile=`wt` (the WT slides into the edited enzyme's frame so the camera doesn't jump), target=`mut` (the edited enzyme the player is looking at, stays fixed):
  ```python
  # src: tmp/pymol-src/modules/pymol/fitting.py:242 cmd.super  (mobile MOVED, target FIXED)
  ret = cmd.super("wt", "mut")
  ```
- Verify the return is a finite tuple with matched atoms > 0 (RESEARCH-pymol-align-api §A/§D post-condition; empirically confirmed `ret` is a tuple `(RMSD, natoms, ...)`):
  ```python
  ok_ret = (isinstance(ret, tuple) and len(ret) >= 2
            and math.isfinite(float(ret[0])) and int(ret[1]) > 0)
  check("super_returns_finite_tuple", ok_ret,
        "type=%s ret[0]=%r ret[1]=%r" % (type(ret).__name__,
        ret[0] if isinstance(ret, tuple) and len(ret) > 0 else None,
        ret[1] if isinstance(ret, tuple) and len(ret) > 1 else None))
  ```
- Capture the WT CA coordinate AFTER align + assert it CHANGED (proves `transform=1` moved the mobile object):
  ```python
  after = []
  # src: tmp/pymol-src/modules/pymol/editing.py:1490 cmd.iterate
  cmd.iterate("wt and name CA", "after.append((x, y, z))", space={"after": after})
  after_x = after[0][0] if after else None
  check("super_moved_mobile", before_x is not None and after_x is not None
        and abs(after_x - before_x) > 1e-6,
        "before_x=%r after_x=%r" % (before_x, after_x))
  ```

**Stage 5 — substructure-scoped align (the domain-shift mechanism; convention §4):**
- Re-offset the WT so the scoped align does real work (otherwise stage 4 already aligned it + stage 5 would be a no-op):
  ```python
  # src: tmp/pymol-src/modules/pymol/editing.py cmd.translate  (verify the exact line in tmp/pymol-src)
  cmd.translate([5.0, 0.0, 0.0], "wt")
  ```
- Capture before-scoped coord (reuse the iterate pattern).
- Run the scoped alignment — align on `name CA` only; `transform=1` (default) moves the WHOLE `wt` object (RESEARCH-pymol-align-api §C/§A.5 empirically confirmed `cmd.align("wt and name CA", "mut and name CA")` moved the whole wt object). Use `cmd.super` for consistency with the default method:
  ```python
  # src: tmp/pymol-src/modules/pymol/fitting.py:242 cmd.super  (scoped: mobile MOVED, target FIXED; transform=1 moves whole object)
  ret2 = cmd.super("wt and name CA", "mut and name CA")
  ```
- Verify `ret2` is a tuple with matched CA atoms > 0:
  ```python
  ok_ret2 = (isinstance(ret2, tuple) and len(ret2) >= 2 and int(ret2[1]) > 0)
  check("scoped_super_matched_ca", ok_ret2, "ret2[1]=%r" % (ret2[1] if isinstance(ret2, tuple) and len(ret2) > 1 else None))
  ```
- Capture after-scoped coord + assert CHANGED (proves the scoped superposition transformed the whole WT object, not just the matched CA selection — the domain-shift pedagogical mechanism):
  ```python
  # (reuse the iterate pattern for after_scoped)
  check("scoped_super_moved_whole_wt", abs(after_scoped_x - before_scoped_x) > 1e-6, ...)
  ```

**Final verdict (copy from edit_smoke.py):**
```python
print("SMOKE_RESULT: {0}".format("FAIL" if FAILS else "PASS"))
if FAILS:
    print("SMOKE_FAILED_STAGES: " + ", ".join(FAILS))
```

**Source-citation convention (MANDATORY, established Phase 3):** every DIRECT `cmd.*` call in this smoke (`cmd.load`, `cmd.count_atoms`, `cmd.super`, `cmd.iterate`, `cmd.translate`) carries a `# src: tmp/pymol-src/modules/pymol/<file>.py:<line> cmd.<name>` comment on the line directly above. Verify each line number against `tmp/pymol-src/modules/pymol/` (the read-only symlinked source) before finalizing — `fitting.py:242` for super, `fitting.py:306` for align (if used), `importing.py:635` for load, `querying.py:1412` for count_atoms, `editing.py:1490` for iterate, `editing.py` for translate (find the line). The EditOps internal cmd.* calls are cited in `edit_ops.py` (do NOT re-cite them here).

**Run + verify:**
1. Syntax check (pure-Python, WSL): `python3.6 -m py_compile tools/wt_align_smoke.py` (must exit 0 — the smoke is PyMOL-run but syntax must be valid Python 3.6).
2. Run headlessly via the established harness: `bash tools/run_headless.sh tools/wt_align_smoke.py`. The harness greps `^SMOKE_RESULT: PASS` from the captured stdout (the bat always returns 0, so the sentinel is the only reliable verdict). If any stage FAILS, read `/tmp/opencode/wt_align_smoke.txt` (the harness prints this path), fix the issue (common fixes: fixture path wrong → use `c14.paths.data_path`; `cmd.iterate` space-dict pattern wrong → verify against `tmp/pymol-src/modules/pymol/editing.py` (iterate is at `editing.py:1490`); `cmd.translate` signature → verify against `tmp/pymol-src/modules/pymol/editing.py`), and re-run. Iterate until `SMOKE_RESULT: PASS`.
3. Confirm the AST gate still passes (no new domain-tier pymol import — the smoke is in `tools/` and uses `pymol.cmd` directly, which is allowed for headless smokes; it must NOT be imported by `c14/` domain tier): `python3.6 tools/check_imports.py` (exit 0).

Do NOT add the `op="align"` dispatch to `c14/pymol_layer/molops.py` (deferred to Phase 6 per convention §6/§8 Q3). This smoke calls `cmd.super` DIRECTLY. If the human overrides Q3 at the checkpoint (Task 3) to pull the dispatch into 5.3, a follow-up revision task will be added — do NOT preemptively add it here.
  </action>
  <verify>
`python3.6 -m py_compile tools/wt_align_smoke.py` exits 0. `bash tools/run_headless.sh tools/wt_align_smoke.py` prints `PASSED` (the harness greps `^SMOKE_RESULT: PASS`). The captured output (`/tmp/opencode/wt_align_smoke.txt`) shows `SMOKE: PASS` for every stage (load_mutant, apply_correct_edit, edit_took, load_wt, super_returns_finite_tuple, super_moved_mobile, scoped_super_matched_ca, scoped_super_moved_whole_wt) and a final `SMOKE_RESULT: PASS`. `grep -c "# src: tmp/pymol-src" tools/wt_align_smoke.py` shows >= 5 source citations (every direct cmd.* call cited). `python3.6 tools/check_imports.py` exits 0 (AST gate clean — no new domain-tier pymol import). `git diff --name-only c14/pymol_layer/molops.py` shows NO changes (the op="align" dispatch is NOT added in 5.3 — deferred to Phase 6).
  </verify>
  <done>
`tools/wt_align_smoke.py` exists, passes `python3.6 -m py_compile`, and runs headlessly via `bash tools/run_headless.sh tools/wt_align_smoke.py` printing `SMOKE_RESULT: PASS` with all 8 stage checks passing (load mutant, apply correct edit, edit took, load WT, super returns finite tuple with matched atoms > 0, super moved the mobile WT coords, scoped super matched CA atoms, scoped super moved the whole WT object). Every direct cmd.* call carries a `# src:` citation. The AST gate stays clean. `molops.py` is NOT modified (the op="align" dispatch is deferred to Phase 6).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human design-review checkpoint of the convention spec + the headless prototype (SC3)</name>
  <what-built>
Plan 01 delivered `05.3-CONVENTION.md` — the WT-aligned structure load convention spec (when it fires: branch-node on_enter after the reverse-mutation edit, NOT the restore safety-net; what it loads: the aligned WT experimental PDB as `<enzyme>_wt`; how it aligns: `cmd.super` on `align_sele` default `name CA`, transform=1 moves the whole object; how it's presented: overlay for major conformational-difference mutants, replace for minor/none or the approach-a restore shortcut, side-by-side deferred to Phase 6; conditionality on mutant-reveal approach + per-enzyme conformational_difference; the `op="align"` MolAction shape with `target`=mobile/`args["reference"]`=fixed; the downstream contracts for 5.4/6/7; the 8 open questions).

Plan 02 Task 2 delivered `tools/wt_align_smoke.py` + two PDB fixtures, running headlessly with `SMOKE_RESULT: PASS` — proving the `cmd.super` MECHANISM on a placeholder (load mutant → apply correct edit → load + align WT → finite RMSD + matched atoms + mobile coords changed + substructure-scoped align moves the whole WT object). This proves the MECHANISM, NOT real science (placeholder coords are arbitrary; a real RMSD-quality gate is Phase 7 content).
  </what-built>
  <how-to-verify>
This is a DESIGN-REVIEW checkpoint (the real in-game human-verify is Phase 6). Review the convention DECISIONS + confirm the headless mechanism proof passed. Steps:

1. **Read the convention spec:** open `.planning/phases/05.3-wt-aligned-structure-load-convention/05.3-CONVENTION.md` and review each section against the "Review checklist" at the end of the doc:
   - §2 where-it-fires: is placing the WT-aligned load in the branch-node `on_enter` AFTER the reverse-mutation `edit` (option iii) sound? Is the correction of the 05.1-06 Note 2 imprecision (restore-safety-net vs branch-node-on_enter) acceptable?
   - §3 conditionality: does conditional firing (always specified; fires per-enzyme under approach (b) + major-conformational-difference; restore shortcut suffices under (a)) honor OQ-DM2 (does NOT pre-decide the mutant-reveal approach)?
   - §4 substructure-align: is active-site-core `cmd.super` for domain-shift mutants sound? Is `align_sele` as a per-enzyme Phase 7 field the right boundary?
   - §5 presentation + narrative: is overlay (major) / replace (minor or approach-a) the right default? Is side-by-side deferred to Phase 6 acceptable? Does the `text_teaching` template overclaim? Does any framing wrongly imply the C14 carbon becomes ATP? (At `pyr.pdh` the carbon IS correctly shed as CO2; the reveal is about the ENZYME's health — confirm the separation is clean.)
   - §6 MolAction shape: is `target`=mobile + `args["reference"]`=fixed (the naming resolution) correct? Is the molops dispatch pseudocode sound?
   - §7 downstream contracts: are the 5.4/6/7 contracts sufficient AND minimal?

2. **Confirm the headless prototype passed:** the executor ran `bash tools/run_headless.sh tools/wt_align_smoke.py` and reported `SMOKE_RESULT: PASS` with all 8 stage checks green. You may re-run it yourself if you want to see the raw output: `bash tools/run_headless.sh tools/wt_align_smoke.py` (from the repo root, in a WSL shell). The output lands in `/tmp/opencode/wt_align_smoke.txt`. Confirm the finite-RMSD + matched-atoms + coords-changed post-conditions align with the empirical findings (the smoke proves the MECHANISM, not real science).

3. **Decide the 8 open questions (§8 of the convention):** for each Q1-Q8, accept the research recommendation OR specify an override. The most consequential is **Q3 — where the `op="align"` molops dispatch lands**: the research recommends Phase 6 (5.3 stays a pure design+prototype phase; the smoke calls `cmd.super` directly). The alternative is the 04-05 precedent (add the `op="align"` dispatch to `c14/pymol_layer/molops.py` + a unit test in `tests/test_molops.py` in 5.3 alongside the smoke). If you override Q3 to "add in 5.3", a follow-up revision task will be needed (the dispatch branch + MockCmd unit test verifying `cmd.super`/`cmd.align`/`cmd.cealign` arg order + `align_sele` scoping composition). The other open questions (Q1 always-vs-conditional, Q2 overlay-vs-replace default, Q4 align_sele boundary, Q5 side-by-side scope, Q6 approach-a redundancy, Q7 PDB sourcing boundary, Q8 conformational_difference source) are design choices documented with recommendations.

4. **Approve or specify adjustments:** if the convention + prototype are scientifically accurate + narratively clear + the contracts are sufficient for 5.4/6/7, approve. If any decision needs changing (e.g. override Q3, change the presentation default, adjust the text_teaching wording), describe the adjustment — the executor will revise `05.3-CONVENTION.md` (and, if Q3 is overridden, add the molops dispatch + unit test as a follow-up).
  </how-to-verify>
  <resume-signal>Type "approved" to accept the convention as-is (all 8 open questions accepted at their research recommendations, including Q3 = defer the op="align" molops dispatch to Phase 6). OR describe specific adjustments (e.g. "Q3: override to add in 5.3; Q2: replace as sole default; revise §5 text_teaching to ..."). The executor will apply the adjustments before completing the phase.
  </resume-signal>
</task>

</tasks>

<verification>
- Two PDB fixtures exist (`_wt_align_mut.pdb`, `_wt_align_wt.pdb`) in `c14/data/assets/bundled/`, valid, network-free, not gitignored.
- `tools/wt_align_smoke.py` passes `python3.6 -m py_compile` and runs headlessly with `SMOKE_RESULT: PASS` (all 8 stage checks green) — SC2 delivered.
- Every direct `cmd.*` call in the smoke carries a `# src:` citation (source-citation convention held).
- The AST gate (`tools/check_imports.py`) stays clean (no new domain-tier pymol import).
- `c14/pymol_layer/molops.py` is NOT modified (the `op="align"` dispatch is deferred to Phase 6 per convention §6/§8 Q3; the smoke calls `cmd.super` directly).
- The human design-review checkpoint (Task 3) is reached + the human approves or specifies adjustments — SC3 delivered.
- No fabricated science: fixtures are placeholder peptides (ALA-GLY / GLY-GLY); no real PDB IDs, DOIs, or disease claims introduced.
</verification>

<success_criteria>
- SC2 delivered: the convention is headless-prototyped on a placeholder (load mutant → apply correct edit → load + align WT → verify alignment via finite-RMSD + matched-atoms>0 + mobile coords changed + substructure-scoped align), with `SMOKE_RESULT: PASS`.
- SC3 delivered: the convention is human-reviewed for scientific accuracy + narrative clarity (design-review checkpoint); the human approves or specifies adjustments, and the 8 open questions (incl. Q3 dispatch deferral) are decided.
- The prototype proves the `cmd.super`/`cmd.align` MECHANISM on a placeholder, NOT real science (Phase 7 sources real structures + a real RMSD-quality gate).
- No code outside `tools/wt_align_smoke.py` + the two fixtures is modified; `molops.py` untouched (dispatch deferred to Phase 6 unless the human overrides Q3).
</success_criteria>

<output>
After completion (including the checkpoint resolution), create `.planning/phases/05.3-wt-aligned-structure-load-convention/05.3-02-SUMMARY.md`. If the human overrode Q3 to "add the op="align" dispatch in 5.3", note the follow-up work needed (molops.py edit + test_molops.py unit test) in the SUMMARY so the phase verifier / Phase 6 can pick it up.
</output>
