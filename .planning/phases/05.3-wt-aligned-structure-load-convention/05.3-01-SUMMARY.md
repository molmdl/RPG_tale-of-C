---
phase: 05.3-wt-aligned-structure-load-convention
plan: 01
subsystem: design
tags: [wt-aligned-structure-load, restoration-reveal, convention, pymol-align, cmd-super, cmd-align, molops, op-align, hero-identity, oq-dm2]

# Dependency graph
requires:
  - phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts
    provides: the FROZEN 55-node glucose skeleton + the 14 edit-allowed-node contract (12 DIS-*-cand disease-mutant + tca.citrate_synthase edit:structural exception + tca.shuffle shared) + the 6CFO/6CER pyr.pdh cast-PDB decision (OQ-DM2 deferred to Phase 7) + the restoration narrative frame (hero as restorer)
  - phase: 04-editing-protonation-restore-safety-net
    provides: the EditOps backup/restore safety-net path (where the WT-aligned load does NOT fire — it's a branch-node on_enter concern) + the apply_edit alter→sort→rebuild sanctioned path (the reverse-mutation edit)
  - phase: 03-pymol-cmd-layer
    provides: the MolOps per-action dispatch pattern (load/edit/restore/protonate + direct cmd.* ops) that the new op="align" extends + the source-citation convention
provides:
  - 05.3-CONVENTION.md — the SC1 design artifact (the WT-aligned structure load convention spec: when/what/how-align/how-presented + the op="align" MolAction shape + downstream contracts for 5.4/6/7 + 8 open questions + review checklist)
  - The resolution of the naming disagreement between the two research files (ADOPT target=mobile, args["reference"]=fixed)
  - The 05.1-06 Note 2 imprecision correction (restore safety-net vs branch-node on_enter — a correct edit does NOT trigger the restore safety net)
  - The conditionality contract (approach × conformational_difference; OQ-DM2 honored — convention complements whichever approach Phase 7 picks)
  - The 8 open questions Q1-Q8 surfaced for the Plan 02 human design-review checkpoint
affects: [05.4-cast-reveal-scene-templates, 06-qt-ui-mvp, 07-content-authoring, 05.3-02-headless-prototype]

# Tech tracking
tech-stack:
  added: []  # pure design writing — NO code, NO new libraries
  patterns:
    - "op="align" MolAction shape: target=mobile (moved), args["reference"]=fixed (stays) — resolves the PyMOL target=fixed naming collision + the args.mobile/args.target redundancy"
    - "Branch-node on_enter canonical order for restoration reveal: edit (reverse-mutation) → load (WT PDB as <enzyme>_wt) → align (super on align_sele) → present (5.4 layers)"
    - "Substructure superposition for domain-shift mutants: cmd.super on a stable-core align_sele (default name CA; per-enzyme Phase 7 field), transform=1 transforms the WHOLE _wt object — the shifted domain shows as the visible pedagogical payload"
    - "Conditionality matrix: mechanism always specified (capability), firing is per-enzyme Phase 7 decision gated on mutant_reveal_approach × conformational_difference"
    - "Presentation default: overlay for major-conformational-difference, replace for minor/none/restore-shortcut; side-by-side multi-viewport deferred to Phase 6 UI"

key-files:
  created:
    - .planning/phases/05.3-wt-aligned-structure-load-convention/05.3-CONVENTION.md  # the SC1 design artifact (415 lines, 10 H2 sections: SC checklist + §1-§8 + review checklist)
  modified: []  # pure design writing — NO code files touched (c14/pymol_layer/molops.py NOT modified; dispatch is specified-only, deferred to Phase 6)

key-decisions:
  - "ADOPT the API researcher's MolAction naming (target=mobile, args[\"reference\"]=fixed) over the design researcher's (args.mobile/args.target) — avoids BOTH the redundancy with MolAction.target AND the PyMOL target=fixed collision"
  - "Option iii placement (branch-node on_enter AFTER reverse-mutation edit) — REJECT option i (narratively backwards) + option ii (pre-decides OQ-DM2 by converging only under approach a)"
  - "Correction of the 05.1-06 Note 2 imprecision: a correct edit routes to a branch node (NOT the restore safety net); the WT-aligned load is a branch-node on_enter concern, NOT a restore-safety-net concern"
  - "cmd.super default (NOT cmd.align) for domain-shift mutants — per fitting.py:316-320 (align is for >30% seq identity; super is sequence-independent)"
  - "op=\"align\" molops dispatch DEFERRED to Phase 6 (Q3 flagged for human override at Plan 02 checkpoint — both 5.3-pure and 5.3-adds-op are mechanically identical)"
  - "Convention is CONDITIONAL (approach × conformational_difference); OQ-DM2 honored — convention complements whichever approach Phase 7 picks, does NOT pre-decide (a) or (b)"
  - "Overlay default for conformational_difference:major, replace fallback for minor/none/restore-shortcut; side-by-side deferred to Phase 6 UI (5.3 only guarantees two objects exist + are aligned)"
  - "align_sele is a per-enzyme Phase 7 metadata field (source-approved stable-core selection), default name CA; conformational_difference is also Phase 7 source-approved (major is a structural-science claim needing a citation)"
  - "8 open questions Q1-Q8 surfaced with research recommendations + tradeoffs for the Plan 02 human design-review checkpoint"
  - "Hero-identity anti-confusion NOTE (MANDATORY): the restoration reveal is about the ENZYME's health, NOT the hero carbon's fate; at pyr.pdh the carbon IS shed as CO2 (electrons continue via NADH→ETC→ATP); the reveal must NOT imply the carbon becomes ATP"
  - "No fabricated science: only 6CFO/6CER appear (pyr.pdh example, marked pending Phase 7 per-claim approval); all other PDB IDs/disease/align_sele are PLACEHOLDERS (<WT_PDB>, <mutant_pdb>, <align_sele>, <disease_resi>, <WT_resn>, <resolution>)"

patterns-established:
  - "WT-aligned structure load convention: branch-node on_enter sequence (edit→load→align→present); <enzyme>_wt naming for the separately-loaded WT experimental object; op=\"align\" MolAction (target=mobile, args[\"reference\"]=fixed)"
  - "Substructure superposition convention: cmd.super on align_sele (default name CA, per-enzyme Phase 7 stable-core override for domain-shift); transform=1 transforms the whole _wt object; the domain shift is the visible pedagogical payload"
  - "Restoration-reveal honesty template: visualization aid NOT proof; cmd.alter changes residue identity but does NOT repack side chains (approximate coordinates); show the experimentally determined WT as ground truth"
  - "Convention conditionality pattern: mechanism always specified (capability), firing is per-enzyme downstream decision gated on metadata fields (approach × conformational_difference) — honors deferred decisions (OQ-DM2) without pre-deciding them"

# Metrics
duration: 6 min
completed: 2026-08-22
---

# Phase 5.3 Plan 01: WT-Aligned Structure Load Convention Spec Summary

**WT-aligned experimental structure load convention spec — branch-node on_enter AFTER reverse-mutation edit, cmd.super on align_sele (default name CA), overlay/replace presentation, op="align" MolAction shape (target=mobile, args["reference"]=fixed) with molops dispatch deferred to Phase 6; 8 open questions + review checklist for the Plan 02 checkpoint**

## Performance

- **Duration:** 6 min
- **Started:** 2026-08-22T16:33:59Z
- **Completed:** 2026-08-22T16:40:58Z
- **Tasks:** 2
- **Files modified:** 1 (the new convention spec; 0 code files)

## Accomplishments
- Wrote the SC1 design artifact `05.3-CONVENTION.md` (415 lines, 10 H2 sections: SC checklist + §1-§8 + review checklist) — the WT-aligned structure load convention spec consumed by Phases 5.4 / 6 / 7 WITHOUT re-designing it
- Specified WHEN it fires (branch-node on_enter AFTER reverse-mutation edit, option iii; 3 placement options analyzed; 05.1-06 Note 2 imprecision corrected; approach-(a) restore-shortcut exception; canonical on_enter order: edit→load→align→present)
- Specified the CONDITIONALITY (mechanism always specified as a capability; firing is per-enzyme Phase 7 decision gated on mutant_reveal_approach × conformational_difference; OQ-DM2 honored — convention complements whichever approach Phase 7 picks)
- Specified HOW it aligns (cmd.super default NOT cmd.align for domain-shift; substructure superposition on align_sele, default name CA, per-enzyme Phase 7 field; transform=1 transforms whole _wt object; the target=fixed/mobile=moved arg-order gotcha documented)
- Specified HOW it is presented (overlay/replace/side-by-side table; overlay default for major, replace fallback for minor/none/restore-shortcut; side-by-side deferred to Phase 6 UI; present directive for 5.4)
- Wrote the honest text_teaching template (visualization aid NOT proof; approximate-coords caveat) + the MANDATORY hero-identity anti-confusion NOTE (enzyme health, not carbon fate; pyr.pdh carbon shed as CO2, electrons continue via NADH→ETC→ATP)
- Specified the op="align" MolAction shape (target=mobile, args["reference"]=fixed — resolves the naming disagreement between the two research files) + the molops dispatch pseudocode (SPEC only, DEFERRED to Phase 6 per Q3)
- Wrote the downstream contracts for 5.4 (object set + <enzyme>_wt naming + alignment state + present directive), 6 (one new op="align" dispatch deferred, flat dispatch surface), 7 (7-field per-enzyme metadata table + 3 branch-node on_enter templates: overlay/replace/restore-shortcut)
- Surfaced all 8 open questions Q1-Q8 with research recommendations + tradeoffs + why-it-matters for the Plan 02 human design-review checkpoint
- Wrote the 7-criterion review checklist for the human at the Plan 02 checkpoint
- Verified NO fabricated science (only 6CFO/6CER as real PDB IDs, both marked pending Phase 7; all other PDB IDs/disease/align_sele are PLACEHOLDERS) + NO code modified (c14/pymol_layer/molops.py NOT touched — pure design writing phase)

## Task Commits

Each task was committed atomically:

1. **Task 1: Write the core convention (§1-§5)** — `ca50bed` (docs)
2. **Task 2: Write the integration contracts + open questions (§6-§8 + review checklist)** — `a0bdb85` (docs)

**Plan metadata:** (pending — final commit after SUMMARY + STATE)

## Files Created/Modified
- `.planning/phases/05.3-wt-aligned-structure-load-convention/05.3-CONVENTION.md` — the SC1 design artifact (the WT-aligned structure load convention spec: §1 purpose/scope, §2 when-it-fires, §3 conditionality, §4 substructure align, §5 presentation + honest narrative + hero-identity NOTE, §6 op="align" MolAction shape + molops dispatch SPEC, §7 downstream contracts 5.4/6/7, §8 8 open questions, review checklist)

## Decisions Made
- **Naming resolution:** ADOPT the API researcher's recommendation (target=mobile, args["reference"]=fixed) over the design researcher's (args.mobile/args.target) — avoids both the redundancy with MolAction.target and the PyMOL target=fixed collision
- **Placement:** Option iii (branch-node on_enter AFTER reverse-mutation edit) — rejects option i (narratively backwards) and option ii (pre-decides OQ-DM2 by converging only under approach a)
- **Default align method:** cmd.super (NOT cmd.align) — per fitting.py:316-320, super is sequence-independent (right for domain-shift); align is for >30% seq identity
- **op="align" dispatch deferral:** DEFERRED to Phase 6 (Q3 flagged for human override at Plan 02 — both 5.3-pure and 5.3-adds-op are mechanically identical)
- **Presentation default:** overlay for conformational_difference:major, replace for minor/none/restore-shortcut; side-by-side deferred to Phase 6 UI
- **align_sele + conformational_difference authoring:** per-enzyme Phase 7 metadata fields (source-approved); defaults name CA + minor respectively

## Deviations from Plan

None — plan executed exactly as written. The plan was pure design writing (0 code changes); both tasks wrote the convention spec file as specified. The only minor adjustment was expanding two sections by ~5 lines total to meet the >=200-line minimum for Task 1 (the §4 align_sele-default rationale note + the §5 PDB-ID boundary explicit statement — both substantive content additions, not padding).

## Issues Encountered
None.

## User Setup Required
None — this is a pure design writing phase; no external service configuration required.

## Next Phase Readiness
- **SC1 DELIVERED:** the WT-aligned structure load convention is specified (when/what/how-align/how-presented) and documented for review in `05.3-CONVENTION.md`. The convention is compatible with both mutant-reveal approaches (OQ-DM2 honored), conditionally fires per-enzyme, is scientifically honest (visualization aid not proof; substructure align for domain shifts), and respects hero-identity (enzyme health, not carbon fate).
- **Ready for Plan 02 (SC2 + SC3):** the convention spec is the reviewable design artifact the Plan 02 headless prototype + human design-review checkpoint consume. Plan 02 will: (a) write `tools/wt_align_smoke.py` + 2 PDB fixtures (`_wt_align_mut.pdb`, `_wt_align_wt.pdb`); (b) prototype `cmd.super` directly (NOT via molops — the dispatch is deferred to Phase 6 per Q3, unless the human overrides at the checkpoint); (c) verify via SMOKE_RESULT: PASS (finite-RMSD + matched-atoms + mobile coords changed + substructure-scoped align); (d) run the checkpoint:human-verify design review against the 7-criterion review checklist + the 8 open questions.
- **Ready for Phase 5.4:** the §7.1 downstream contract (object set + <enzyme>_wt naming + alignment state + present directive) tells 5.4 exactly what it layers on top (show_as/color/zoom/viewport) WITHOUT re-deciding the object set or re-aligning.
- **Ready for Phase 6:** the §7.2 downstream contract (one new op="align" dispatch, flat dispatch surface, no new reveal super-op) tells Phase 6 exactly what to implement (the §6 elif branch + unit test).
- **Ready for Phase 7:** the §7.3 downstream contract (7-field per-enzyme metadata table + 3 on_enter templates) tells Phase 7 exactly what to author per enzyme (wt_pdb, mutant_pdb, mutant_reveal_approach, conformational_difference, align_method, align_sele, present).
- **Blockers/concerns:** none. The 8 open questions (§8) are surfaced for the Plan 02 checkpoint — none block Plan 02's prototype; Q3 (where op="align" dispatch lands) is the only one that could change Plan 02's task list (if the human overrides to "add in 5.3", Plan 02 gains a molops.py edit + unit test task).

---
*Phase: 05.3-wt-aligned-structure-load-convention*
*Completed: 2026-08-22*
