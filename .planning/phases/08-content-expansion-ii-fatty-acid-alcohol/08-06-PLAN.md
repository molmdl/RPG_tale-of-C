---
phase: 08-content-expansion-ii-fatty-acid-alcohol
plan: 06
type: execute
wave: 3
depends_on: [01, 02, 03, 04, 05]
files_modified: [data/story_glucose/manifest.json, data/story_glucose/intro.json, data/story_glucose/fa_path.json, data/story_glucose/alcohol_path.json, rpg/data/edits.json, rpg/data/cast.json, tests/test_glucose_reachability.py, tests/test_glucose_content.py, tests/test_story_editor_lint.py, tests/test_story_editor_lifecycle.py, tests/test_bulk_download.py, tools/story_editor_lint.py, tools/story_graph_viewer.py, tools/story_editor_assets/20_validate.js, tools/story_editor_assets/30_graph.js, tools/story_editor_assets/45_lifecycle.js, story_editor.html, .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-GRAPH-DIAGRAM.md]
autonomous: true
must_haves:
  truths:
    - "Both new story files exist with ALL skeleton nodes and the COMPLETE edge topology (forks, loop, cond-free O2 choices, edit:offer choices, endings); content plans afterwards change ZERO topology"
    - "fa.stub/alc.stub are replaced in place by intro.shell_fa/intro.shell_alc; intro.select is char-cond-gated with engine strings; PLACEHOLDER_PHASE8 is gone; gate exits 0"
    - "edits.json gains fa.mcad + alc.aldh2 buckets (signatures per 08-04 probes); cast.json gains the 2 entries; relationship pins hold (cast 14 ⊂ edits 15 == tags 16 − {tca.citrate_synthase})"
    - "ALL pinned counts re-pinned in this ONE plan: tests + lint + viewer + editor assets; story_editor.html + diagram regenerated"
  artifacts:
    - path: "data/story_glucose/fa_path.json"
      provides: "11 skeleton nodes: fa.start/activation/cpt1/carnitine/shuttle/cycle/mcad/mcad_restored/acetyl_handoff + fa.good.storage + fa.bad.o2_crisis"
      contains: "fa.mcad_restored"
    - path: "data/story_glucose/alcohol_path.json"
      provides: "11 skeleton nodes: alc.start/adh/acetaldehyde/aldh2/aldh2_restored/nadh_burden/branch_fat_vs_burn/lipogenesis/acetate/acss2 + alc.bad.o2_crisis"
      contains: "alc.aldh2_restored"
    - path: "rpg/data/edits.json"
      provides: "15 buckets (13 + fa.mcad + alc.aldh2 with per-enzyme pool [bad.cofactor_lost])"
      contains: "fa.mcad"
    - path: "rpg/data/cast.json"
      provides: "14 entries (12 + fa.mcad/1EGE/fatty_acid + alc.aldh2/1O02/alcohol)"
      contains: "1EGE"
  key_links:
    - from: "intro.select choices"
      to: "intro.shell_glucose / intro.shell_fa / intro.shell_alc"
      via: "cond char=='&lt;engine string&gt;' on all three; stub gotos gone"
      pattern: "intro\\.shell_fa"
    - from: "fa.mcad / alc.aldh2 edit:offer choices"
      to: "edits.json branch_node"
      via: "known-fix routing to fa.mcad_restored / alc.aldh2_restored"
      pattern: "mcad_restored|aldh2_restored"
    - from: "manifest.files"
      to: "loader StoryGraph.load"
      via: "fa_path.json + alcohol_path.json appended; single merged bundle"
      pattern: "fa_path\\.json"
---

<objective>
THE atomic topology + re-pin plan (the 07-12 mirror): build both new story files with complete skeleton topology, replace the stubs with shell wiring, land the 2 edits buckets + 2 cast entries, and re-pin EVERY count across tests/lint/viewer/editor in this one plan.
Purpose: content plans (08-07..08-13) then author pure text/claims/scenes with zero topology movement; the suite is never transiently red across plans.
Output: 9-file merged bundle at 79 nodes / 24 endings; all gates green.
</objective>

<context>
@.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-01-SUMMARY.md (DC ledger + BINDING COUNTS)
@.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-03-SUMMARY.md (alc.bad.o2_crisis ratification — counts below assume RATIFIED; if vetoed, drop that node and use 78/23/16B everywhere)
@.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-04-SUMMARY.md (verified chain ids + residue identities for the edits signatures)
@.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-RESEARCH-MECHANICS.md (§6 lines 124-160 the complete pin enumeration; §8 lines 186-197 tool table)
@.planning/phases/07-content-expansion-i-glucose/07-12-PLAN.md (the precedent shape)
Topology (edges, from FA research §2.1/§3 + ALC research §2.1/§4): intro.select 3 char-cond choices → intro.shell_glucose/shell_fa/shell_alc. FA: shell→fa.start→fa.activation→fa.cpt1 (2 choices: "Enter the shuttle — be oxidized"→fa.carnitine / "Let the cell bank you — be stored as triglyceride"→fa.good.storage)→fa.carnitine→fa.shuttle→fa.cycle (loop choice "Another lap — the chain shortens"→fa.cycle + "The chain is spent — 8 acetyl-CoA stand ready"→fa.acetyl_handoff + O2 choice "The tissue runs out of oxygen — the spiral stalls"→fa.bad.o2_crisis, all choices cond-FREE)→fa.acetyl_handoff→tca.entry. fa.mcad: reached from fa.cycle? NO — fa.mcad sits on the spiral lap: fa.cycle's "Another lap" → fa.mcad (the lap's first strike) → fa.cycle (continue the lap)? SIMPLER per research §2.1 row 7: fa.mcad is the edit-allowed disease node ON the spiral — wire fa.cycle→fa.mcad→fa.cycle (the lap passes through the dehydrogenase every cycle; the player may attempt the restoration when the disease story fires). fa.mcad edit:offer→edit.prompt; known fix → fa.mcad_restored (choices Continue/Examine→fa.cycle; ROUTER_ONLY). fa.good.storage + fa.bad.o2_crisis: is_ending "good"/"bad". ALC: shell→alc.start→alc.adh→alc.acetaldehyde→alc.aldh2 (edit:offer→edit.prompt; known fix K487E→alc.aldh2_restored; ROUTER_ONLY; per-enzyme pool)→alc.nadh_burden (2 cond-free choices: "The flood must go somewhere — push onward"→alc.branch_fat_vs_burn / "The tissue runs out of oxygen — the flood stalls"→alc.bad.o2_crisis)→alc.branch_fat_vs_burn (2 choices: "Bank the flood as fat"→alc.lipogenesis / "Push the acetate toward the great wheel"→alc.acetate)→alc.lipogenesis→end.good.fatty_acid (EXISTING); alc.acetate→alc.acss2→tca.entry. Skeleton text: one honest non-empty line per layer, NO science claims, NO "TBD", no claim_ids; on_enter minimal ([hide_all] only). Serializer house style byte-exact (tools/story_editor_json.py round-trip).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author both story files (skeletons + full topology) + intro.json + manifest</name>
  <files>data/story_glucose/fa_path.json, data/story_glucose/alcohol_path.json, data/story_glucose/intro.json, data/story_glucose/manifest.json</files>
  <action>
    - fa_path.json: 11 nodes per the context topology (fa.start, fa.activation, fa.cpt1, fa.carnitine, fa.shuttle, fa.cycle, fa.mcad, fa.mcad_restored, fa.acetyl_handoff, fa.good.storage, fa.bad.o2_crisis). Tags: stage:fa_path on pathway nodes; edit:enzyme:fa.mcad on fa.mcad; edit:offer choice on fa.mcad → edit.prompt; is_ending on the 2 ending nodes. fa.mcad_restored: ROUTER_ONLY (NO incoming goto from any node — reached only via EditRouter), choices Continue/Examine→fa.cycle, NOT edit-allowed.
    - alcohol_path.json: 11 nodes per the context topology (alc.start, alc.adh, alc.acetaldehyde, alc.aldh2, alc.aldh2_restored, alc.nadh_burden, alc.branch_fat_vs_burn, alc.lipogenesis, alc.acetate, alc.acss2, alc.bad.o2_crisis). Tags: stage:alcohol; edit:enzyme:alc.aldh2 + edit:offer on alc.aldh2; alc.acss2 narrative-only (no tag, no edit:offer); is_ending on alc.bad.o2_crisis. alc.lipogenesis → end.good.fatty_acid (existing node — cross-file goto resolves in the merged bundle). alc.aldh2_restored ROUTER_ONLY, choices → alc.nadh_burden.
    - intro.json: rename keys fa.stub→intro.shell_fa, alc.stub→intro.shell_alc (replace in place; drop PLACEHOLDER_PHASE8 claim_id + stub tags); intro.select: 3 choices with cond "char=='glucose'" / "char=='fatty_acid'" / "char=='alcohol'" (engine strings; dict-method-safe bare name), gotos intro.shell_glucose/intro.shell_fa/intro.shell_alc, labels lose "(coming in Phase 8)" (e.g. "Fatty acid — enter the beta-oxidation path").
    - manifest.json: files += "fa_path.json", "alcohol_path.json" (order: after intro.json, before tca.json is fine — use manifest order = viewer column order: append fa_path + alcohol_path after intro.json). start stays intro.preface.
    - Skeleton text per node: text_dramatic = one honest atmospheric line naming the beat (no science claims); text_teaching = "Teaching text lands in the Phase 8 content pass." NO "TBD", no claim_ids, on_enter = [{"op": "hide_all"}] only. Write through tools/story_editor_json.py house style (round-trip must be byte-exact).
  </action>
  <verify>python3.6 -c "import sys; sys.path.insert(0,'tools'); import story_editor_json as j; raw=open('data/story_glucose/fa_path.json').read(); assert j.dumps(j.loads(raw))+'\n'==raw, 'fa roundtrip'; raw=open('data/story_glucose/alcohol_path.json').read(); assert j.dumps(j.loads(raw))+'\n'==raw, 'alc roundtrip'; print('roundtrip-ok')"</verify>
  <done>Both files exist with complete topology + skeletons; intro shells replace stubs; manifest has 9 files; byte-exact house style.</done>
</task>

<task type="auto">
  <name>Task 2: Land the edits.json buckets + cast.json entries</name>
  <files>rpg/data/edits.json, rpg/data/cast.json</files>
  <action>
    - edits.json: add bucket "fa.mcad" — signature {"op":"point_mutation","target":"resi 304 and chain &lt;chain from 08-04 probe&gt;","args":{"new_res":"LYS"}} (the known FIX of K304E), branch_node "fa.mcad_restored", claim_id "DIS-ACADM-01-cand"; global pool serves (NO per-enzyme pool override). Add bucket "alc.aldh2" — signature target "resi 487 and chain &lt;chain from 08-04 probe&gt;", new_res "GLU" (the fix of E487K), branch_node "alc.aldh2_restored", claim_id "DIS-ALDH2-01-cand", per-enzyme bad_ending_pool ["bad.cofactor_lost"] (08-01 DC-M). Lowercase targets; new_res args key; no known-wrong entries (M5); version + global pool otherwise untouched.
    - cast.json: add {"id":"fa.mcad","label":"Medium-chain acyl-CoA dehydrogenase","source":"download","pdb_id":"1EGE","character":"fatty_acid","claim_id":"CAST-FA-MCAD-PDB-01-cand"} and {"id":"alc.aldh2","label":"Aldehyde dehydrogenase 2, mitochondrial","source":"download","pdb_id":"1O02","character":"alcohol","claim_id":"CAST-ALC-ALDH2-PDB-01-cand"}. Engine character strings "fatty_acid"/"alcohol" (achievements-mandated). NO entries for 9PDQ/6VYI/1U3U (08-01 DC-E: on_enter loads, not cast — coverage would demand invented signatures).
  </action>
  <verify>python3.6 tools/check_edit_coverage.py; echo "exit=$?"</verify>
  <done>15 buckets / 14 cast entries; coverage green (every cast id has a bucket); relationship shape preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Re-pin ALL counts + retire the sanctioned residual + regen artifacts</name>
  <files>tests/test_glucose_reachability.py, tests/test_glucose_content.py, tests/test_story_editor_lint.py, tests/test_story_editor_lifecycle.py, tests/test_bulk_download.py, tools/story_editor_lint.py, tools/story_graph_viewer.py, tools/story_editor_assets/20_validate.js, tools/story_editor_assets/30_graph.js, tools/story_editor_assets/45_lifecycle.js, story_editor.html, .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-GRAPH-DIAGRAM.md</files>
  <action>
    Use the mechanics §6 enumeration as the checklist (post values: nodes 79, endings 24 = 1T+4G+2N+17B, edit-allowed 17 = old 15 + {fa.mcad, alc.aldh2}, tags 16, edits 15, cast 14):
    - tests/test_glucose_reachability.py: 57→79 count test (rename test_manifest_loads_all_57_nodes), 21→24 tier test, edit-allowed exact frozenset 15→17 (D5 discipline — same-plan), intro-topology test, single-continue predicate unaffected.
    - tests/test_glucose_content.py: relationship pins (cast 12→14, edits 13→15, tags 14→16), gate-residual tests — test_gate_residual_exactly_two_documented_stubs → test_gate_residual_zero (exit 0, 0 MISSING, 0 UNAPPROVED), test_placeholder_phase8_only_on_documented_stubs → test_no_placeholder_anywhere; the 13-signature round-trip count → 15.
    - tests/test_story_editor_lint.py + tools/story_editor_lint.py: PINNED_NODE_COUNT 79, PINNED_TIER_COUNTS, PINNED_EDIT_ALLOWED (17-id set); RETIRE PHASE8_STUB_NODES/PHASE8_CLAIM + the exact-pair sanctioned-residual rule (placeholder_misuse now fires on ANY PLACEHOLDER* use); update the battery's sanctioned-residual tests accordingly.
    - tools/story_graph_viewer.py: EXPECTED_NODES/TIER_COUNTS/EDIT_ALLOWED; FILE_COL += fa_path→6, alcohol_path→7 (N_COLS 7→8); STUB_IDS retired (the stub column repurposed for fa_path — verify no col-6 collision remains for unmapped files).
    - tools/story_editor_assets/20_validate.js: PINNED_NODE_COUNT 79 + tier/edit-allowed pins + the sanctioned-residual rule retirement (JS port mirrors the Python lint rule-for-rule, Pitfall S4). 30_graph.js: FILE_COL += the 2 new files; stub-column comments updated. 45_lifecycle.js: PINNED_NODES 79.
    - tests/test_bulk_download.py: expected_download_characters {"glucose"} → {"glucose","fatty_acid","alcohol"}.
    - Regenerate: python3.6 tools/story_editor.py (committed story_editor.html); python3.6 tools/story_graph_viewer.py (dev/story_graph_viewer.html — integrity self-check is a HARD gate); python3.6 tools/render_story_graph.py (diagram artifacts).
  </action>
  <verify>python3.6 -m unittest discover -s tests 2>&1 | tail -3 && python3.6 tools/story_editor_lint.py; echo "lint=$?" && python3.6 tools/check_citations.py; echo "gate=$?"</verify>
  <done>Suite green at 79/24/17; lint exit 0 with zero count_shift notices; gate exit 0 residual 0; story_editor.html + viewer + diagram regenerated and committed.</done>
</task>

</tasks>

<verification>
python3.6 -m unittest discover -s tests 2>&1 | tail -3
python3.6 tools/check_citations.py; echo "gate=$?"
python3.6 tools/check_edit_coverage.py; echo "coverage=$?"
python3.6 tools/story_editor_lint.py; echo "lint=$?"
python3.6 -c "import json,glob;d=[json.load(open(f)) for f in glob.glob('data/story_glucose/*.json') if not f.endswith('manifest.json')];print('nodes',sum(len(x['nodes']) for x in d))"
</verification>

<success_criteria>
79 nodes / 24 endings (1T+4G+2N+17B) / 17 edit-allowed / 15 edits buckets / 14 cast entries / 9-file manifest; stubs retired + gate exit 0; all pins re-pinned in ONE commit event; artifacts regenerated; suite + lint + coverage + gate green.
</success_criteria>

<output>.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-06-SUMMARY.md</output>
