---
phase: 08-content-expansion-ii-fatty-acid-alcohol
plan: 14
type: execute
wave: 7
depends_on: [07, 08, 09, 10, 11, 12, 13]
files_modified: [tests/test_multichar_reachability.py]
autonomous: true
must_haves:
  truths:
    - "The 12-assertion matrix is green: check_reachability from intro.shell_glucose / intro.shell_fa / intro.shell_alc each reaches all 4 ending tiers (3 chars x 4 tiers)"
    - "Per-character seeded determinism is green: each character's True route walks to the SHARED tca.shuffle; seed 42 yields the documented fate (identical to glucose's — the shuffle is shared)"
    - "Save/load round-trips are green for fatty_acid + alcohol: character preserved, state equality, MolAction replay, ending reachable after load"
    - "Cross-cutting content assertions are green: all 79 nodes two-layer non-empty; every referenced claim approved; count invariants 79/24/17"
  artifacts:
    - path: "tests/test_multichar_reachability.py"
      provides: "The STORY-02 12-assertion battery + per-char determinism + save/load round-trips + cross-cutting sweeps"
      contains: "intro.shell_fa"
  key_links:
    - from: "tests/test_multichar_reachability.py"
      to: "rpg/story/validate.py check_reachability"
      via: "BFS from each character entry node (no new algorithm — validate.py:167 takes any start)"
      pattern: "check_reachability"
    - from: "per-char determinism walks"
      to: "tca.shuffle"
      via: "the shared weighted node; same documented fate for all 3 characters (mechanics 6.3)"
      pattern: "tca\\.shuffle"
---

<objective>
The STORY-02 test battery (the v1 success measure): 12-assertion cross-character reachability + per-character determinism + save/load round-trips + the cross-cutting content sweeps, all in ONE new test file.
Purpose: ROADMAP SC2 ("12 assertions green — the v1 success measure") + SC4's automated half become machine-pinned here; the human-verify half is plan 15's checkpoint.
Output: tests/test_multichar_reachability.py green.
</objective>

<context>
@.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-RESEARCH-MECHANICS.md (6.2 lines 145-148 the 12-assertion shape; 6.3 lines 150-152 determinism; 6.4 lines 153-156 save/load shape)
@.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-03-SUMMARY.md (HF-9 determinism per character)
@.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-06-SUMMARY.md (final counts: 79 nodes / 24 endings / 17 edit-allowed)
Patterns to parametrize: check_reachability(nodes, start_id) (rpg/story/validate.py:167 — pure, stateless, cond-blind BFS = the semantics the glucose suite relies on); TestSeededDeterminismDesignB (tests/test_glucose_content.py:485-537: seed 42 → tca.co2_turn2, other fate tca.co2_turn1, both-fates-over-30-seeds) + the only-weighted-node pin; save/load round-trip (tests/test_engine.py:125-186 + tests/test_integration.py:254-334). Entry nodes: intro.shell_glucose / intro.shell_fa / intro.shell_alc. Per-char True-route walk helpers needed (FA/ALC branches are player choices, NOT index-0 chains — walk by choice label/destination per character's route; FA passes fa.cpt1-shuttle + the spiral; ALC passes adh→aldh2→nadh_burden→burn→acetate→acss2).
</context>

<tasks>

<task type="auto">
  <name>Task 1: 12-assertion reachability + per-char determinism + save/load</name>
  <files>tests/test_multichar_reachability.py</files>
  <action>
    - TestMulticharReachability: load ONE graph; for entry in (intro.shell_glucose, intro.shell_fa, intro.shell_alc): report = check_reachability(g.all_nodes(), entry); subTest per (character, tier) asserting every ending of that tier is reachable — 12 subTest assertions total; PLUS the union BFS from intro.preface stays green (all 24 endings) — mirroring the existing all-character test.
    - TestMulticharDeterminism: parametrized over the 3 characters — engine.start(character, seed=42); walk the character's True route to tca.shuffle (per-char walk helper by choice destination); assert the documented fate (tca.co2_turn2 for seed 42 — the SAME fate for all 3, the shuffle is shared); assert the OTHER fate node exists on the 30-seed sweep; keep the only-weighted-node pin referenced (import or re-assert: tca.shuffle is the bundle's only weighted node).
    - TestMulticharSaveLoad: parametrized over (fatty_acid, alcohol) — start → advance >=2 nodes (per-char route) → save → load → assert state equality (current_node, character, flags, counters, visits, rng state) + replayed current-node on_enter + the character string preserved + an ending still reachable after load. Zero engine change makes these pure test additions.
  </action>
  <verify>python3.6 -m unittest tests.test_multichar_reachability -v 2>&1 | tail -6</verify>
  <done>12/12 tier assertions green; determinism green per character; both save/load round-trips green.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-cutting content sweeps</name>
  <files>tests/test_multichar_reachability.py</files>
  <action>
    - TestPhase8ContentSweep: every node across the 9-file bundle has non-empty text_dramatic + text_teaching (edit.prompt's sanctioned stub marker exempt — the ONLY exemption); every claim_id referenced anywhere (nodes' claim_ids) resolves to approval_status=="approved" (rpg/citations.py strict predicate); count invariants: 79 nodes / 24 endings (1T+4G+2N+17B) / 17 edit-allowed nodes / tca.shuffle the only weighted node; the fa/alc endings' is_ending tier values correct; PLACEHOLDER sweep: zero PLACEHOLDER* anywhere.
    - Also assert the per-character edit floor: fa.mcad + alc.aldh2 carry edit:enzyme tags + edit:offer choices (the bad-tier BFS keystone, mechanics B-C3).
  </action>
  <verify>python3.6 -m unittest tests.test_multichar_reachability -v 2>&1 | tail -6 && python3.6 -m unittest discover -s tests 2>&1 | tail -3</verify>
  <done>Cross-cutting sweeps green; full suite green (all batteries, zero failures).</done>
</task>

</tasks>

<verification>
python3.6 -m unittest tests.test_multichar_reachability -v 2>&1 | tail -8
python3.6 -m unittest discover -s tests 2>&1 | tail -3
</verification>

<success_criteria>
The 12-assertion matrix green (the v1 success measure, machine-pinned); per-char determinism + save/load green; cross-cutting sweeps green; full suite green.
</success_criteria>

<output>.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-14-SUMMARY.md</output>
