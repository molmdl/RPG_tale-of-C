---
phase: 08-content-expansion-ii-fatty-acid-alcohol
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
must_haves:
  truths:
    - "All 7 Phase 8 PDBs are in the local gitignored cache and load headlessly"
    - "Residue identities + chain ids verified empirically: 1EGE resi 304 = LYS; 1ZUM resi 487 = LYS; 1O02 resi 487 = GLU"
    - "Chain ids + align_sele recommendations for both restoration reveals are recorded in the SUMMARY for plan 06 (edits signatures) and plans 10/13 (reveal ops)"
  artifacts:
    - path: ".planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-04-SUMMARY.md"
      provides: "PDB acquisition log + headless probe results (residue identities, chain ids, align_sele)"
      contains: "resi 304"
  key_links:
    - from: "08-04-SUMMARY.md probe results"
      to: "plan 06 (edits.json signatures) + plans 10/13 (restoration reveal ops)"
      via: "verified residue/chain data consumed when signatures + sele strings are written"
      pattern: "resi (304|487)"
---

<objective>
Acquire the Phase 8 cast/scene structures into the local cache and run headless PyMOL residue-identity probes BEFORE any signature or sele string is written (the 07-12 aconitase-mapping precedent: never write an edit sele from memory).
Purpose: plans 06/10/13 write edits.json signatures + on_enter/reveal ops against EMPIRICALLY verified residue numbers and chain ids.
Output: gitignored downloaded PDBs + 08-04-SUMMARY.md probe record.
</objective>

<context>
@.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-RESEARCH-FA.md (§5.1 lines 125-133 K304E numbering; §6 lines 169-199 PDB table)
@.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-RESEARCH-ALC.md (§6.1 lines 156-164 E504K↔E487K numbering; §7 lines 182-207)
@.planning/phases/07-content-expansion-i-glucose/07-12-SUMMARY.md (the mapping-probe precedent)
Structures: 1EGE (fa.mcad WT cast, X-ray 2.75 Å), 4P13 (optional mutant ref), 1O02 (alc.aldh2 WT cast, 1.90 Å), 1ZUM (ALDH2\*2 mutant, 2.1 Å), 9PDQ (fa.carnitine scene, cryo-EM), 6VYI (fa.good.storage scene, cryo-EM), 1U3U (alc.adh scene, X-ray). Research predictions to CONFIRM (not trust): 1EGE PDB resi 304 = LYS (mature numbering, offset −25 from UniProt 329); 1ZUM PDB resi 487 = LYS (E487K); 1O02 PDB resi 487 = GLU (WT).
Headless pattern (AGENTS.md, verified): `timeout 90 cmd.exe /c "C:\\src\\run-conda-pymol.bat -cq path\\to\\script.py" 2>&1 | tail -50` — cmd-only scripts, no Qt. 9PDQ/6VYI are cryo-EM (large files — allow longer timeout, e.g. timeout 300).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Acquire the 7 structures into the local cache</name>
  <files>tmp/fetch_phase8_pdbs.py (gitignored tmp/ — cmd-only PyMOL script), downloaded/*.pdb (gitignored cache — the 7 fetched structures), .planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-04-SUMMARY.md (cache paths + file sizes)</files>
  <action>
    - Write a cmd-only PyMOL script (under tmp/, gitignored) that fetches all 7 IDs into the gitignored local cache (downloaded/ per the Phase 7 07-15 convention) via cmd.fetch, then prints per-structure: object name, atom count, chain list.
    - Run headlessly via the run-conda-pymol.bat pattern (cryo-EM entries get timeout 300). If the Windows env is unreachable, STOP and record the blocker in the SUMMARY — do not fall back to memory or partial data.
    - Do NOT commit the cache (gitignored). Record the cache paths + file sizes in the SUMMARY.
  </action>
  <verify>ls -la downloaded/ 2>/dev/null | grep -cE "1EGE|4P13|1O02|1ZUM|9PDQ|6VYI|1U3U" || true; # plus the script's own per-structure printout captured in the SUMMARY</verify>
  <done>7/7 structures present in the cache and load headlessly with printed chain lists.</done>
</task>

<task type="auto">
  <name>Task 2: Headless residue-identity probes + align_sele recording</name>
  <files>tmp/probe_phase8_residues.py (gitignored tmp/ — cmd-only PyMOL script), .planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-04-SUMMARY.md (probe results + align_sele recommendations)</files>
  <action>
    - Probe script 2 (cmd-only): for 1EGE — cmd.iterate "resi 304" over all chains → print (chain, resn) — EXPECT LYS on the chain(s) the cast uses; confirm FAD HET visibility (research already verified; re-print the HET count).
    - For 1ZUM — iterate "resi 487" → EXPECT LYS (the E487K mutant). For 1O02 — iterate "resi 487" → EXPECT GLU (WT). Print (chain, resn) for every matching chain; the summary records WHICH chain id the edits.json signature target must name (e.g. "resi 487 and chain a" — exact chain from the probe, never assumed).
    - Determine + record the align_sele recommendation for each restoration reveal (05.3: mobile = &lt;enzyme&gt;_wt, fixed = &lt;enzyme&gt;; start from "name CA" as 07-12 did unless the probe shows a reason to scope tighter).
    - Record ALL results (raw iterate output + conclusions) in 08-04-SUMMARY.md. If any expectation FAILS (e.g. 1EGE resi 304 is not LYS), STOP — record the discrepancy and flag plans 06/10/13 as blocked pending re-verification against UniProt; do NOT write a wrong signature downstream.
  </action>
  <verify>grep -c "resn" .planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-04-SUMMARY.md</verify>
  <done>Probe results recorded: resi 304 = LYS in 1EGE; resi 487 = LYS in 1ZUM; resi 487 = GLU in 1O02; chain ids + align_sele recorded; any discrepancy escalated loudly.</done>
</task>

</tasks>

<verification>
grep -E "304|487" .planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-04-SUMMARY.md | head -10
ls downloaded/ 2>/dev/null | head
</verification>

<success_criteria>
7/7 PDBs cached (gitignored); residue identities + chain ids empirically confirmed matching the research predictions; align_sele recommendations recorded; SUMMARY consumable by plans 06/10/13.
</success_criteria>

<output>.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-04-SUMMARY.md</output>
