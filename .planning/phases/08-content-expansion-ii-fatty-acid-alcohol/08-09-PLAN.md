---
phase: 08-content-expansion-ii-fatty-acid-alcohol
plan: 09
type: execute
wave: 4
depends_on: [06]
files_modified: [rpg/ui/main_window.py, rpg/achievements.py, tests/test_achievements.py]
autonomous: true
must_haves:
  truths:
    - "StartDialog offers all 3 characters enabled; accept() returns the checked radio's character string (glucose/fatty_acid/alcohol)"
    - "ACHIEVEMENT_CATALOG_V1 contains fatty_acid_tried + alcohol_tried; the controller's generic _unlock path fires for them"
    - "Achievement tests updated: the previously-pinned no-op behavior for the 2 ids flips to unlockable; unknown-character no-op coverage preserved"
  artifacts:
    - path: "rpg/ui/main_window.py"
      provides: "CHAR-01 Qt surface: enabled radios + real tooltips + result_character from the checked radio (mechanics section 5: the ONLY required Qt edit)"
      contains: "fatty_acid"
    - path: "rpg/achievements.py"
      provides: "ACHIEVEMENT_CATALOG_V1 extended with the 2 reserved ids (the :66-71 comment's Phase 8 promise honored)"
      contains: "fatty_acid_tried"
  key_links:
    - from: "StartDialog.result_character"
      to: "controller.start_game / engine.start(character)"
      via: "existing character-generic plumbing (zero controller change — mechanics 2.2)"
      pattern: "result_character"
    - from: "controller achievements unlock"
      to: "ACHIEVEMENT_CATALOG_V1"
      via: "_unlock(character + '_tried') for ANY character (achievements.py:208-211)"
      pattern: "_tried"
---

<objective>
CHAR-01 code surface (mechanics H10 — exactly two small edits + one test update): enable the StartDialog radios, return the checked character, extend the achievements catalog.
Purpose: the new-game screen offers 3 characters (ROADMAP SC1) — the Qt visual verification itself is the human-verify checkpoint in plan 15.
Output: enabled 3-character start screen + catalog extension; engine/controller/state untouched.
</objective>

<context>
@.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-RESEARCH-MECHANICS.md (section 5 lines 107-122 the complete CHAR-01 surface list; section 2.2 lines 33-41 plumbing evidence)
@.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-06-SUMMARY.md (topology landed — selecting FA/ALC now reaches real shell nodes)
Current code: rpg/ui/main_window.py:128-138 (radios; FA+Alcohol setEnabled(False) + "Available in a future phase." tooltips), :171-173 (accept() hardcodes result_character="glucose"); rpg/achievements.py:63-71 (catalog; comment names the 2 Phase 8 ids), :208-211 (records characters_tried + _unlock(character + "_tried") generically). Character STRINGS mandated: "fatty_acid" and "alcohol" (snake_case; matches cast.json entries + achievements ids). Qt is NOT unit-testable in WSL (no display) — the code edit is verified by py_compile + import safety + plan 15's human-verify.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable StartDialog radios + return the checked character</name>
  <files>rpg/ui/main_window.py</files>
  <action>
    - In StartDialog.__init__: remove setEnabled(False) from the FA + Alcohol radios; replace the "Available in a future phase." tooltips with real ones ("Play the fatty-acid character — beta-oxidation." / "Play the alcohol character — ethanol to acetyl-CoA."); keep glucose checked by default.
    - In accept(): replace the hardcoded result_character = "glucose" with the checked radio's mapping (_glucose -> "glucose", _fatty -> "fatty_acid", _alcohol -> "alcohol"). Keep the seed-validation veto logic untouched.
    - NO other Qt/controller/engine changes (mechanics section 5 "Explicitly NOT needed" list is binding).
  </action>
  <verify>python3.6 -m py_compile rpg/ui/main_window.py && python3.6 -m unittest tests.test_controller -v 2>&1 | tail -3</verify>
  <done>Radios enabled with real tooltips; accept() maps the checked radio to the engine character string; no other UI code touched.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the achievements catalog + update its tests</name>
  <files>rpg/achievements.py, tests/test_achievements.py</files>
  <action>
    - Append to ACHIEVEMENT_CATALOG_V1: ("fatty_acid_tried", "Fatty Acid", "Began a playthrough as the fatty acid") and ("alcohol_tried", "Alcohol", "Began a playthrough as alcohol") — matching the existing tuple shape and label style.
    - Update tests/test_achievements.py: the unknown-character no-op tests that pin fatty_acid_tried NOT in the catalog (lines ~13, ~209-216) flip — the ids are now IN the catalog and _unlock fires for them; keep a no-op pin for a genuinely-unknown character string (e.g. "unknown_char") to preserve the no-op coverage.
    - Achievements unlock/persistence are generic (no schema change) — verify via the achievements + persist batteries.
  </action>
  <verify>python3.6 -m unittest tests.test_achievements tests.test_persist -v 2>&1 | tail -4</verify>
  <done>Catalog has the 2 new ids; unlock fires for FA/ALC starts; unknown-character no-op coverage preserved; batteries green.</done>
</task>

</tasks>

<verification>
python3.6 -m unittest discover -s tests 2>&1 | tail -3
python3.6 -m py_compile rpg/ui/main_window.py rpg/achievements.py
</verification>

<success_criteria>
3-character start screen code-complete (human-verify deferred to plan 15 per ROADMAP SC1); achievements catalog extended + tests flipped; zero engine/controller/state changes; suite green.
</success_criteria>

<output>.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-09-SUMMARY.md</output>
