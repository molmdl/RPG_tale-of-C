---
phase: 08-content-expansion-ii-fatty-acid-alcohol
plan: 12
type: execute
wave: 6
depends_on: [10]
files_modified: [data/story_glucose/fa_path.json]
autonomous: true
must_haves:
  truths:
    - "fa.good.storage + fa.bad.o2_crisis carry full two-layer text grounded ONLY in approved claims"
    - "fa.good.storage on_enter loads pdb:6VYI (object dgat1); the storage Good ending owns the carbon-retained semantics (no soul-claim on this road)"
    - "The fa.bad.o2_crisis teaching carries the FA-specific honesty: fat is the fuel that demands breath (FA-O2-01-cand + approved AN-ETC-02/BAD-HOST-01)"
    - "fa.cycle's O2 choice target resolves (wired in 06); topology FROZEN"
  artifacts:
    - path: "data/story_glucose/fa_path.json"
      provides: "Authored FA ending nodes with approved claims + 6VYI scene"
      contains: "FA-TAG-STORAGE-01-cand"
  key_links:
    - from: "fa.cpt1 storage choice"
      to: "fa.good.storage"
      via: "the CPT-1 fork's terminus (topology from 06; text here)"
      pattern: "fa\\.good\\.storage"
    - from: "fa.good.storage claim_ids"
      to: "data/citations.json"
      via: "approved claims only (FA-TAG-STORAGE-01-cand + CAST-DGAT1-PDB-01-cand per 08-02 verdicts)"
      pattern: "CAST-DGAT1-PDB-01-cand"
---

<objective>
FA content C — the two FA endings: fa.good.storage (TAG storage — the FA-canonical Good) and fa.bad.o2_crisis (the framing-(d) Bad trigger).
Purpose: FA's four-tier story completes — carbon retained as triglyceride vs the spiral seizing without breath.
Output: authored ending nodes; no topology change.
</objective>

<context>
@.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-RESEARCH-FA.md (§2.1 rows 9-10 lines 60-62; §4.2 lines 100-101; §4.4 lines 108-116)
@.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-02-SUMMARY.md (approved claims — FA-TAG-STORAGE-01-cand, CAST-DGAT1-PDB-01-cand, optional CAST-GPAT1, FA-O2-01-cand high-stakes wording as approved)
Claim map: fa.good.storage→FA-TAG-STORAGE-01-cand + CAST-DGAT1-PDB-01-cand (+ CAST-GPAT1-PDB-01-cand only if the batch approved 8E4Y — default deferred Phase 9); fa.bad.o2_crisis→FA-O2-01-cand + reuse AN-ETC-02 + BAD-HOST-01. Good semantics (PROJECT.md row 76): retained is the CARBON BODY; NO soul-claim on the storage road (anti-confusion). The O2 teaching: beta-oxidation banks NADH every lap; the mitochondrion's NADH is re-oxidized ONLY by the ETC; the NAD+ pool collapses; long-chain fatty acids cannot be burned anaerobically (the approved AN-G-04 fermentation facts apply to glycolysis's CYTOSOLIC NADH — the mitochondrial pool has no such recycle; state exactly this scope); the J&F wrong-CO2 sentence never quoted.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author fa.good.storage (the TAG Good ending)</name>
  <files>data/story_glucose/fa_path.json</files>
  <action>
    - Dramatic: banked, not burned — the cell esterifies the hero's fatty chain into a triglyceride droplet; whole, unspent, stored for tomorrow.
    - Teaching: "Triglycerides are the main constituents of body fat in humans" (WIKIPEDIA-TRIGLYCERIDE verbatim grounding); esterification via GPAT and DGAT acyltransferases; the cast shows DGAT1 (6VYI 3.0 Å cryo-EM, oleoyl-CoA ligand visible — Sui et al. 2020); exploration cast note (DGAT1's known variants are truncating/LoF — no disease arc, citrate-synthase precedent).
    - Good-semantics line: the carbon is RETAINED, pre-oxidation; no soul-claim on this road (the carbon body is what is banked).
    - on_enter: [hide_all, {"op":"load","target":"pdb:6VYI","args":{"object":"dgat1"}}] (op vocabulary check).
    - Scope line per H6 if approved ("your 16-carbon chain burns clean — odd chains and very-long chains are other roads, taught another day").
  </action>
  <verify>python3.6 tools/story_editor_lint.py; echo "lint=$?"</verify>
  <done>Storage Good ending authored with approved claims + 6VYI scene; Good semantics honest; lint green.</done>
</task>

<task type="auto">
  <name>Task 2: Author fa.bad.o2_crisis (the framing-(d) Bad trigger)</name>
  <files>data/story_glucose/fa_path.json</files>
  <action>
    - Dramatic: the air is gone — the electron-carts pile up with nowhere to unload; the spiral seizes; the great furnace starves; the host with it.
    - Teaching: FA-O2-01-cand's approved wording (the spiral's NADH is re-oxidized only by the ETC; without oxygen the mitochondrial NAD+ pool cannot be regenerated; the spiral stalls; long-chain fatty acids cannot be burned anaerobically — no O2, no ETC); the scope honesty (fermentation recycles glycolysis's CYTOSOLIC NADH — approved AN-G-04 facts; the mitochondrial pool has no such route); sustained failure starves the host (BAD-HOST-01 reuse).
    - The framing-(d) rule is visible in the text: no anaerobic route anywhere toward True — the O2 choice is the Bad trigger only.
    - on_enter: [{"op":"hide_all"}] (ending shape per 5.4; no loads).
    - Grep the file for banned strings ("106 ATP", "7n−6", "8.1", ROS, carbon-becomes-ATP phrasing) — absent.
  </action>
  <verify>python3.6 -c "import sys; sys.path.insert(0,'tools'); import story_editor_json as j; raw=open('data/story_glucose/fa_path.json').read(); assert j.dumps(j.loads(raw))+'\n'==raw; print('roundtrip-ok')" && python3.6 tools/check_citations.py; echo "gate=$?" && python3.6 -m unittest discover -s tests 2>&1 | tail -3</verify>
  <done>O2 Bad ending authored; framing-(d) honored; byte-exact; gate + suite green.</done>
</task>

</tasks>

<verification>
python3.6 tools/story_editor_lint.py; echo "lint=$?"
python3.6 tools/check_citations.py; echo "gate=$?"
python3.6 -m unittest discover -s tests 2>&1 | tail -3
</verification>

<success_criteria>
Both FA endings authored with approved claims; storage Good + O2 Bad complete FA's four tiers; topology frozen; suite + gate + lint green.
</success_criteria>

<output>.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-12-SUMMARY.md</output>
