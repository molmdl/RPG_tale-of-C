---
phase: 08-content-expansion-ii-fatty-acid-alcohol
plan: 15
type: execute
wave: 8
depends_on: [14]
files_modified: [tools/controller_integration_smoke.py]
autonomous: false
must_haves:
  truths:
    - "Full automated battery green: suite, citation gate exit 0 residual 0, edit coverage, lint, viewer integrity, zip sanity"
    - "Headless restoration replay passes for BOTH new arcs (fa.mcad_restored + alc.aldh2_restored) — edits land, WT loads, aligns, reveals"
    - "The controller-integration smoke's expectation-drift FAILs are fixed; any behavioral FAIL is documented as flagged (engine untouched)"
    - "ONE human-verify checkpoint covers ROADMAP SC1 + SC4: 3-character select, FA + ALC playthroughs to >=1 ending each, save/load round-trip"
  artifacts:
    - path: "tools/controller_integration_smoke.py"
      provides: "Re-aligned expectations for the 3 pre-existing FAILs + the new-cast drift (behavioral FAILs documented, not engine-fixed)"
      contains: "expected"
    - path: ".planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-15-SUMMARY.md"
      provides: "The gate battery record + the human checkpoint verdict"
      contains: "SMOKE_RESULT"
  key_links:
    - from: "headless replay script"
      to: "fa.mcad_restored / alc.aldh2_restored on_enter"
      via: "controller-faithful MolOps replay through run-conda-pymol.bat (the 07-17 pattern)"
      pattern: "run-conda-pymol"
    - from: "human checkpoint"
      to: "ROADMAP SC1 + SC4"
      via: "CHAR-01 select + save/load round-trip verified in a real Windows PyMOL session"
      pattern: "approved"
---

<objective>
Phase 8 final verification (the 07-18 mirror): run the complete automated gate battery, re-align the controller smoke, replay both new restoration arcs headlessly, then ONE human-verify checkpoint covering ROADMAP SC1 (3-character select) + SC4 (save/load round-trip) in a real Windows PyMOL session.
Purpose: the phase closes only when the v1 success measure (12-assertion matrix) is machine-green AND the human has seen the 3-character roster play.
Output: 08-15-SUMMARY.md gate record + checkpoint verdict.
</objective>

<context>
@.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-14-SUMMARY.md (the test battery green baseline)
@.planning/phases/07-content-expansion-i-glucose/07-17-SUMMARY.md (the headless controller-faithful replay pattern: SMOKE_RESULT + checks)
@.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-04-SUMMARY.md (cached PDBs for the replay)
Headless pattern: timeout 90 cmd.exe /c "C:\\src\\run-conda-pymol.bat -cq path\\to\\script.py" 2>&1 | tail -50 (timeout 300 for cryo-EM loads). Known debt (STATE.md Pending Todos + mechanics section 8): controller_integration_smoke.py has 3 pre-existing FAILs; bulk_download_expected_chars_empty will drift with the new cast entries. Zip: tools/build_plugin_zip.sh copies data/story_glucose + sanity-checks the manifest — unchanged under the merged bundle.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated gate battery + smoke re-alignment + headless replays</name>
  <files>tools/controller_integration_smoke.py</files>
  <action>
    - Run + record in the SUMMARY: full suite (unittest discover); citation gate (expect exit 0, 0 MISSING, 0 UNAPPROVED); check_edit_coverage (expect 14/14); story_editor_lint (exit 0, zero count_shift); story_graph_viewer integrity; build_plugin_zip.sh + manifest sanity.
    - controller_integration_smoke.py: diagnose the 3 pre-existing FAILs; FIX expectation-drift failures (e.g. bulk_download chars now {glucose, fatty_acid, alcohol}; story counts); do NOT change engine/controller behavior — any behavioral FAIL is documented in the SUMMARY as flagged with the diagnosis (the STATE.md owed-TODO discipline).
    - Headless restoration replay (tmp/ script, cmd-only, the 07-17 pattern): for each of fa.mcad_restored + alc.aldh2_restored, replay the node's on_enter op list through the real MolOps vocabulary against the cached PDBs — assert edits land (resi 304 -> LYS on the mcad object; resi 487 -> GLU on the aldh2 object), WT loads as a separate object, super aligns (matched CA count > 0), cartoon reveals, _bak_ backups present; print SMOKE_RESULT: PASS with a check count.
    - Write the full battery table + smoke outputs into 08-15-SUMMARY.md (07-18-SUMMARY shape: gate table + checkpoint section).
  </action>
  <verify>python3.6 -m unittest discover -s tests 2>&1 | tail -3 && python3.6 tools/check_citations.py; echo "gate=$?" && python3.6 tools/check_edit_coverage.py; echo "coverage=$?" && python3.6 tools/story_editor_lint.py; echo "lint=$?"</verify>
  <done>Every automated gate green or explicitly documented-flagged; both restoration replays SMOKE_RESULT: PASS; battery table recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — 3-character select + FA/ALC playthroughs + save/load (SC1 + SC4)</name>
  <files>.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-15-SUMMARY.md</files>
  <what-built>The complete three-character roster: StartDialog offers glucose / fatty acid / alcohol; each character plays a distinct pathway (FA: warehouse → activation → CPT-1 fork → shuttle → spiral → MCAD restoration → handoff → shared TCA/ETC; ALC: liver → ADH → acetaldehyde → ALDH2*2 restoration → NADH flood → fork → acetate → ACSS2 → shared TCA/ETC); both new ending tiers (storage Good, O2-crisis Bad) authored; 12-assertion matrix machine-green.</what-built>
  <how-to-verify>
    In a real Windows PyMOL session (open the plugin):
    1. New game: the dialog offers THREE enabled characters; tooltips describe FA + alcohol. Pick "Fatty acid".
    2. Play the FA road at least to fa.mcad (see the 1EGE enzyme with FAD) and attempt the restoration: choose the edit option, apply the known fix (residue 304 back to LYS) — the restored reveal should load + align the WT overlay. Continue to at least ONE ending (storage Good via the CPT-1 fork, or the O2 Bad, or through the shared TCA to True/Normal).
    3. Save mid-playthrough; load the save; confirm position + character + scene restore.
    4. New game → "Alcohol": play to alc.aldh2 (see the 1ZUM mutant), apply the K487E fix, see the 1O02 WT reveal; continue through the NADH flood to at least ONE ending (lipogenesis Good via the fork, the O2 Bad, or the shared arc).
    5. Check the achievements screen shows Fatty Acid + Alcohol unlocked after the playthroughs.
    6. Spot-check the flush wording (ranges, no "40%") and the ClinVar drug-response honesty line at alc.aldh2.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (per-issue: node id + what was wrong). Issues route to fix-forward plans per the 07.1-11 precedent.</resume-signal>
</task>

</tasks>

<verification>
python3.6 -m unittest discover -s tests 2>&1 | tail -3
python3.6 tools/check_citations.py; echo "gate=$?"
python3.6 tools/check_edit_coverage.py; echo "coverage=$?"
python3.6 tools/story_editor_lint.py; echo "lint=$?"
</verification>

<success_criteria>
All automated gates green (or explicitly documented-flagged for pre-existing debt); both restoration arcs replay PASS headlessly; the human approves the 3-character select + playthroughs + save/load (SC1 + SC4); the phase's v1 success measure is met.
</success_criteria>

<output>.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-15-SUMMARY.md</output>
