---
phase: 08-content-expansion-ii-fatty-acid-alcohol
plan: 07
type: execute
wave: 4
depends_on: [05, 06]
files_modified: [data/story_glucose/intro.json, data/story_glucose/fa_path.json]
autonomous: true
must_haves:
  truths:
    - "intro.shell_fa + fa.start/activation/cpt1/carnitine/shuttle carry full two-layer text grounded ONLY in approved claims (07/12: '-cand' ids kept verbatim)"
    - "fa.cpt1's storage fork and fa.carnitine's OCTN2≠CACT honesty note are landed; the O2/scope guardrails from the banned register pass"
    - "fa.carnitine on_enter loads pdb:9PDQ (object carnitine_transporter); the shell loads the bundled _smoke.pdb placeholder per 08-01 DC-I"
    - "Topology FROZEN: zero nodes/choices added or removed; byte-exact house style; gate + suite green"
  artifacts:
    - path: "data/story_glucose/fa_path.json"
      provides: "Authored two-layer text + approved claim_ids + 9PDQ scene for the entry/shuttle segment"
      contains: "FA-CPT1-01-cand"
  key_links:
    - from: "fa.cpt1 choices"
      to: "fa.carnitine / fa.good.storage"
      via: "the CPT-1 reciprocal-regulation fork (topology frozen in 06; labels/text authored here)"
      pattern: "fa\\.good\\.storage"
    - from: "fa.carnitine claim_ids"
      to: "data/citations.json"
      via: "approved claims only (FA-CARNITINE-01-cand, CAST-FA-OCTN2-PDB-01-cand, DIS-SLC22A5-01-cand per 08-02 verdicts)"
      pattern: "CAST-FA-OCTN2-PDB-01-cand"
---

<objective>
FA content A — entry + shuttle segment: two-layer text + claim wiring + scenes for intro.shell_fa, fa.start, fa.activation, fa.cpt1, fa.carnitine, fa.shuttle (6 nodes).
Purpose: the FA hero's road from the fat warehouse to the mitochondrial gate — the CPT-1 fork is the character's signature regulated branch and must be honest and playable.
Output: authored segment; no topology change.
</objective>

<context>
@.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-RESEARCH-FA.md (§2.0 lines 36-41 shell; §2.1 rows 1-5 lines 46-53 node texts; §3 lines 82-91 branch discipline; §8 lines 233-244 banned register)
@.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-02-SUMMARY.md (approved claim ids + wording verdicts — cite ONLY claims recorded approved)
@.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-01-SUMMARY.md (DC-I shell asset, DC-K shared ids)
Two-layer text per the 5.4 templates + Phase 7 house style (07-06 precedent). Claim map (per 08-02 verdicts): shell→CAST-PALM-PUBCHEM-01-cand; fa.start→FA-ENTRY-01-cand; fa.activation→FA-ACTIVATION-01-cand (the shared anchor claim — its text states the ATP→AMP cost, which alc.acss2 will reference); fa.cpt1→FA-CPT1-01-cand + FA-MALONYL-01-cand (+ optional DIS-CPT1A-01-cand text color if approved — "decreased activity" wording honesty); fa.carnitine→FA-CARNITINE-01-cand + CAST-FA-OCTN2-PDB-01-cand (+ DIS-SLC22A5-01-cand as TEXT story — no on-structure edit, SIFTS unverified, PFK-pattern); fa.shuttle→FA-SHUTTLE-01-cand (+ optional DIS-CPT2-01-cand text-only). Banned: no ATP-yield numbers, no ROS, no carbon-becomes-ATP, J&F's wrong CO2 sentence never quoted, hero = one carbon of the 16-carbon palmitate blade (C14 = label only).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author two-layer text + claim swaps for the 6 nodes</name>
  <files>data/story_glucose/intro.json, data/story_glucose/fa_path.json</files>
  <action>
    - intro.shell_fa (intro.json): dramatic — the shell becomes the form (a sixteen-carbon fatty blade carried in the blood's ferry); teaching — the hero wears palmitic acid (hexadecanoic acid, C16H32O2, MW 256.42; CID 985 claim); anti-confusion: the hero is ONE of the blade's sixteen carbons (which one deliberately unspecified — tca.entry's committed simplification covers it later); on_enter = [hide_all, load bundled _smoke.pdb per 08-01 DC-I] (verify against the glucose shell's op shape and mirror it exactly).
    - fa.start: dramatic (freed from the fat warehouse) + teaching (triglycerides; lipase cleaves to glycerol + 3 fatty acids; blood transport) per FA-ENTRY-01-cand.
    - fa.activation: the toll beat — acyl-CoA synthetase, ATP spent down to AMP + PPi (the fatty acyl adenylate intermediate); narrative-only node (ACSL1 has 0 PDB cross-refs — NO load, NOT in cast); note the shared-claim wording so ALC's acss2 reference stays consistent (08-01 DC-K).
    - fa.cpt1: the BRANCH beat — CPT-1 grips the acyl chain; malonyl-CoA shuts the gate when synthesis runs ("fatty acid synthesis and fatty acid catabolism cannot occur simultaneously in any given cell" — WIKIPEDIA-BETAOX verbatim grounding); the player picks the host's metabolic state (Phase 7 cond-neutralization honesty); choices' labels polished (topology unchanged); teaching names the reciprocal switch as the real fork.
    - fa.carnitine: the ferry-supply beat — OCTN2 imports carnitine; cast = 9PDQ; HONESTY NOTE mandatory (OCTN2 is the plasma-membrane importer; CACT — the inner-membrane carrier — is a DIFFERENT protein with no structure; the cast shows the SUPPLY step); CDSP/P46S as text-only disease color if DIS-SLC22A5-01-cand approved.
    - fa.shuttle: across the inner membrane — CACT carries acylcarnitine; CPT-2 hands the acyl group back to CoA inside the matrix (J&F 17.02 verbatim grounding); optional CPT2 S113L text color (exercise-induced muscular form) if approved.
    - Wire ONLY approved claim_ids (cross-check each against 08-02-SUMMARY verdicts; skip any optional claim the batch rejected/deferred).
  </action>
  <verify>python3.6 tools/story_editor_lint.py; echo "lint=$?"</verify>
  <done>6/6 nodes two-layer authored; only approved claims referenced; honesty notes landed; lint green with zero count_shift.</done>
</task>

<task type="auto">
  <name>Task 2: Scene fills + serializer round-trip + suite</name>
  <files>data/story_glucose/fa_path.json</files>
  <action>
    - fa.carnitine on_enter: [hide_all, {"op":"load","target":"pdb:9PDQ","args":{"object":"carnitine_transporter"}}] (op vocabulary check — load/hide_all only, no invented ops; cryo-EM fetches at runtime; degrade-on-failure is the controller's existing swallow).
    - Verify every on_enter op in the segment is in the known vocabulary and matches its 5.4 scene-template type (story-node shape: hide_all + optional load; NO edits, NO aligns on non-restoration nodes).
    - Run the byte-exact round-trip check on both touched files (tools/story_editor_json.py dumps(loads(raw))+"\n" == raw) — no-op saves must reproduce the file.
    - Run the full suite + the citation gate; grep the two files for the banned strings ("106 ATP", "7n−6", "8.1", ROS words, "oxidative phosphorylation" in carbon-fate phrasing) — all must be absent.
  </action>
  <verify>python3.6 -c "import sys; sys.path.insert(0,'tools'); import story_editor_json as j; raw=open('data/story_glucose/fa_path.json').read(); assert j.dumps(j.loads(raw))+'\n'==raw; print('roundtrip-ok')" && python3.6 tools/check_citations.py; echo "gate=$?" && python3.6 -m unittest discover -s tests 2>&1 | tail -3</verify>
  <done>Scenes conform to template types; byte-exact house style; gate exit 0; suite green; banned register clean.</done>
</task>

</tasks>

<verification>
python3.6 tools/story_editor_lint.py; echo "lint=$?"
python3.6 tools/check_citations.py; echo "gate=$?"
python3.6 -m unittest discover -s tests 2>&1 | tail -3
</verification>

<success_criteria>
Entry/shuttle segment fully authored (text + claims + scenes) with approved claims only; topology byte-frozen; honesty notes (OCTN2≠CACT, hero-carbon) landed; suite + gate + lint green.
</success_criteria>

<output>.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-07-SUMMARY.md</output>
