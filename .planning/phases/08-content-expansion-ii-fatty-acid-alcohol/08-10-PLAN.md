---
phase: 08-content-expansion-ii-fatty-acid-alcohol
plan: 10
type: execute
wave: 5
depends_on: [04, 07]
files_modified: [data/story_glucose/fa_path.json, data/story_glucose/tca.json, data/story_glucose/endings.json]
autonomous: true
must_haves:
  truths:
    - "fa.cycle + fa.mcad + fa.mcad_restored + fa.acetyl_handoff carry full two-layer text grounded ONLY in approved claims"
    - "The MCAD restoration arc is implemented per 05.3 + H3: fa.mcad on_enter pre-applies K304E on the 1EGE object; fa.mcad_restored on_enter applies the LYS fix + loads/aligns the WT reveal"
    - "The shared-arc genericization (H2) lands: tca.akg_dh + end.normal.co2 glucose-specific sentences replaced with the 08-02-approved wording, claim-neutral"
    - "The True-arc teaching beat lands: fat is the electron-dense fuel — FADH2/NADH banked every lap BEFORE the wheel turns (claim-inherited text, no new claims)"
  artifacts:
    - path: "data/story_glucose/fa_path.json"
      provides: "Authored spiral + restoration + handoff nodes with approved claims + 1EGE scenes"
      contains: "DIS-ACADM-01-cand"
    - path: "data/story_glucose/tca.json"
      provides: "Genericized tca.akg_dh sentence (claim-neutral text-only edit)"
      contains: "pyruvate"
  key_links:
    - from: "fa.mcad edits.json signature"
      to: "fa.mcad_restored"
      via: "the known-fix branch (plan 06 landed the bucket; this plan lands the reveal ops)"
      pattern: "mcad_restored"
    - from: "fa.acetyl_handoff choices"
      to: "tca.entry"
      via: "the convergence beat — CONV-ACETYL-TCA-01-cand shared claim referenced"
      pattern: "CONV-ACETYL-TCA-01-cand"
---

<objective>
FA content B — the spiral + MCAD restoration + convergence: two-layer text + claim wiring + the 05.3 restoration arc for fa.cycle, fa.mcad, fa.mcad_restored, fa.acetyl_handoff, PLUS the H2 shared-arc genericization (tca.json + endings.json text-only edits).
Purpose: the spiral is the FA character's rhythmic heart; MCAD is the edit-allowed restoration carrier (the bad-tier BFS keystone); the handoff is where FA meets the shared wheel.
Output: authored segment + genericized shared sentences; no topology change.
</objective>

<context>
@.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-RESEARCH-FA.md (§2.1 rows 6-8 lines 54-59; §4.1 lines 96-98 True arc; §5.1 lines 125-133 K304E; §8 lines 233-244 banned register)
@.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-02-SUMMARY.md (approved claims; the H2 genericization replacement sentences as recorded)
@.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-04-SUMMARY.md (verified resi 304 chain + identity; align_sele)
@.planning/phases/05.3-wt-aligned-structure-load-convention/05.3-CONVENTION.md (approach (a): pre-edit apply; branch-node canonical order edit → load WT → align → present; restore shortcut)
Claim map: fa.cycle→FA-SPIRAL-01-cand + FA-SPIRAL-STOICH-01-cand; fa.mcad→FA-MCAD-01-cand + CAST-FA-MCAD-PDB-01-cand + DIS-ACADM-01-cand (+ CAST-FA-MCAD-MUT-PDB-01-cand 4P13 "researchers photographed the mutant itself" if approved); fa.acetyl_handoff→FA-PALMITATE-01-cand + CONV-ACETYL-TCA-01-cand (shared id). Banned register: NO "106 ATP"/"7n−6"/per-carbon yields (the FADH2/NADH/acetyl-CoA COUNTS are allowed — stoichiometry, not yields); the anti-confusion line: FADH2/NADH are the SOUL's carts (electrons banked per lap), the acetyl-CoA is the carbon body's next vehicle; the carbon never becomes ATP.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the spiral + handoff text/claims</name>
  <files>data/story_glucose/fa_path.json</files>
  <action>
    - fa.cycle: the spiral loop node — dramatic (four strikes per lap; the chain comes out two carbons shorter, one acetyl coin richer, two electron-carts heavier) + teaching (the four reactions verbatim-grounded: FAD-dependent dehydrogenation, hydration — L-stereospecific, NAD+-dependent oxidation, thiolysis; the per-cycle bookkeeping "shortened by two carbon atoms. Concomitantly, one molecule of FADH2, NADH and acetyl CoA are formed"); the loop choice labels polished (topology frozen); anti-confusion beat (soul's carts vs carbon body's vehicle); deterministic spiral — no RNG language anywhere (STORY-03 stays TCA-owned).
    - fa.mcad: the first-strike disease beat — MCAD deficiency "the most common fatty acid beta-oxidation disorder" (WIKIPEDIA-BETAOX clinical section verbatim grounding); symptoms 3 months–2 years, triggered by illness/fasting, hypoketotic hypoglycemia, identified by newborn screening, treated by AVOIDING FASTING (symptom granularity per the H10 batch verdict); K304E wording exactly per H10 ("classified Pathogenic/Likely pathogenic in ClinVar (VCV000003586) ... the prevalent 'G985' allele") + the triple-numbering bridge line ("residue 304 in the structure you see"; cDNA c.985 = precursor K329 = mature/PDB K304); FAD prosthetic group visible in 1EGE.
    - fa.acetyl_handoff: the convergence beat — "the entire 16:0 fatty acid is fully converted to 8 acetyl-CoAs" via 7 cycles (J&F 17.02 verbatim, minus the banned ATP clause); the cleaved acetyl-CoA enters the TCA (S-BETAOX verbatim); the hero's carbon rides INSIDE an acetyl group — exactly the glucose hero's position (which carbon is deliberately unspecified; tca.entry's committed simplification covers it); the True-arc teaching beat: fat is the electron-dense fuel — the soul's harvest begins early on this road (7 FADH2 + 7 NADH banked BEFORE the wheel turns).
    - Wire only 08-02-approved claim ids.
  </action>
  <verify>python3.6 tools/story_editor_lint.py; echo "lint=$?"</verify>
  <done>Spiral + handoff two-layer authored; K304E wording per verdict; no RNG language; approved claims only.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the MCAD restoration arc (05.3 approach (a))</name>
  <files>data/story_glucose/fa_path.json</files>
  <action>
    - fa.mcad on_enter (per 05.3 approach (a) + H3 + the 07-12/07-17 op discipline): [{"op":"hide_all"}, {"op":"load","target":"pdb:1EGE","args":{"object":"mcad"}}, {"op":"edit","target":"mcad","args":{"edit_type":"point_mutation","sele":"resi 304 and chain <chain from 08-04 probe>","new_resn":"GLU"}}] — the host's enzyme is born broken (K304E pre-applied; the pre-edit backup IS the WT per 05.3).
    - fa.mcad_restored on_enter (the known-fix branch node; canonical 07-12 template): [{"op":"edit","target":"mcad","args":{"edit_type":"point_mutation","sele":"resi 304 and chain <chain>","new_resn":"LYS"}}, {"op":"load","target":"pdb:1EGE","args":{"object":"mcad_wt"}}, {"op":"align","target":"mcad_wt","args":{"reference":"mcad","method":"super","align_sele":"<align_sele from 08-04 SUMMARY>"}}, {"op":"show_as","target":"mcad_wt","args":{"rep":"cartoon"}}] + the restoration text (05.3 honest-caveat template: cmd.alter changes identity without repacking side chains — approximate coordinates; the WT overlay is the ground truth) + the hero-identity note (the reveal is about the ENZYME's health, NOT the carbon's fate).
    - If the 08-02 batch approved CAST-FA-MCAD-MUT-PDB-01-cand (4P13): the teaching text gains the "researchers photographed the mutant itself" sentence (2VGG pattern). NO extra on_enter load unless the batch verdict says so.
    - Record the fa.mcad 05.3 7-field metadata (wt_pdb 1EGE, mutant_pdb 4P13-or-none, mutant_reveal_approach "apply_edit", conformational_difference note, align_method super, align_sele, present) in the SUMMARY for Phase 9 reuse.
  </action>
  <verify>python3.6 tools/story_editor_lint.py; echo "lint=$?" && python3.6 -c "import json,sys; sys.path.insert(0,'tools'); import story_editor_json as j; raw=open('data/story_glucose/fa_path.json').read(); assert j.dumps(j.loads(raw))+'\n'==raw; print('roundtrip-ok')"</verify>
  <done>Restoration arc ops landed in the 05.3 canonical order with probe-verified sele strings; ROUTER_ONLY intact; round-trip byte-exact.</done>
</task>

<task type="auto">
  <name>Task 3: Shared-arc genericization (H2) — tca.json + endings.json</name>
  <files>data/story_glucose/tca.json, data/story_glucose/endings.json</files>
  <action>
    - tca.json tca.akg_dh: replace the glucose-specific cross-reference sentence ("the pyruvate dehydrogenase you met at the doorway") with the 08-02-approved generic wording (claim-neutral; visible to ALL characters — the diff was presented at the batch for transparency).
    - endings.json end.normal.co2: replace "Every glucose carbon that enters respiration eventually leaves as CO2" with the approved character-neutral wording.
    - TEXT-ONLY: zero claim_ids changes, zero topology, zero other sentences. Byte-exact round-trip both files. Note the edit is claim-neutral by design — the citation gate is unaffected.
  </action>
  <verify>python3.6 -c "import sys; sys.path.insert(0,'tools'); import story_editor_json as j; raw=open('data/story_glucose/tca.json').read(); assert j.dumps(j.loads(raw))+'\n'==raw; raw=open('data/story_glucose/endings.json').read(); assert j.dumps(j.loads(raw))+'\n'==raw; print('roundtrip-ok')" && python3.6 -m unittest discover -s tests 2>&1 | tail -3</verify>
  <done>Both glucose-specific sentences genericized exactly per the recorded verdict; claim-neutral; round-trip byte-exact; suite green.</done>
</task>

</tasks>

<verification>
python3.6 tools/story_editor_lint.py; echo "lint=$?"
python3.6 tools/check_citations.py --story data/story_glucose --registry data/citations.json; echo "gate=$?"
python3.6 -m unittest discover -s tests 2>&1 | tail -3
</verification>

<success_criteria>
Spiral/MCAD/handoff authored with approved claims; restoration arc probe-verified; shared sentences genericized per verdict; topology frozen; suite + gate + lint green.
</success_criteria>

<output>.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-10-SUMMARY.md</output>
