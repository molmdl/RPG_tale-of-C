---
phase: 08-content-expansion-ii-fatty-acid-alcohol
plan: 11
type: execute
wave: 5
depends_on: [08]
files_modified: [data/story_glucose/alcohol_path.json]
autonomous: true
must_haves:
  truths:
    - "alc.nadh_burden + alc.branch_fat_vs_burn + alc.lipogenesis + alc.bad.o2_crisis carry full two-layer text grounded ONLY in approved claims"
    - "The O2 choice at alc.nadh_burden is cond-FREE with honest framing labels; alc.bad.o2_crisis is an is_ending bad node (08-03 verdict honored)"
    - "The lipogenesis Good fork lands with the DC-6 framing (the fatty-liver tension owned in teaching text) and routes to end.good.fatty_acid"
    - "The 05.4 anti-confusion template lands at every soul-transfer beat: the 2 NADH carry ELECTRONS (soul), never the carbon"
  artifacts:
    - path: "data/story_glucose/alcohol_path.json"
      provides: "Authored NADH-engine + branch + Good-fork + O2-crisis nodes with approved claims"
      contains: "ALC-LIPO-01-cand"
  key_links:
    - from: "alc.lipogenesis"
      to: "end.good.fatty_acid"
      via: "the Good fork terminus (existing shared node reused; topology frozen)"
      pattern: "end\\.good\\.fatty_acid"
    - from: "alc.nadh_burden O2 choice"
      to: "alc.bad.o2_crisis"
      via: "the framing-(d) Bad trigger, cond-free (BFS traverses it; runtime always eligible)"
      pattern: "alc\\.bad\\.o2_crisis"
---

<objective>
ALC content B — the NADH-flood engine + the fork + the O2 crisis: two-layer text + claim wiring for alc.nadh_burden, alc.branch_fat_vs_burn, alc.lipogenesis, alc.bad.o2_crisis (4 nodes).
Purpose: ethanol's drama is an electron flood, not a carbon rearrangement — this plan lands the segment's heart and the framing-(d) Bad trigger.
Output: authored segment; no topology change.
</objective>

<context>
@.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-RESEARCH-ALC.md (§3 lines 77-99 the verbatim NADH story; §2.1 rows 5-8 lines 50-54; §5.2/§5.4 lines 127-147; §9 banned register)
@.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-03-SUMMARY.md (approved claims; DC-5/DC-6 outcomes; the O2-node verdict + ALC-O2-01 wording)
Claim map: alc.nadh_burden→ALC-2NADH-01-cand + ALC-NADH-TCA-STALL-01-cand (+ ALC-O2-01-cand on the O2 choice's target node); alc.branch_fat_vs_burn→ALC-LIPO-01-cand (high-stakes wording as approved) + CONV-ACETYL-TCA-01-cand (the shared convergence id on the burn side); alc.lipogenesis→ALC-LIPO-01-cand (shared with the branch; the ending node's own approved claims stand — do NOT re-author end.good.fatty_acid); alc.bad.o2_crisis→ALC-O2-01-cand + reuse approved AN-ETC-02 + BAD-HOST-01 (and AN-G-04's NAD+-replenishment logic for the "the liver cannot ferment its way out" scope line, exactly per the recorded verdict). Banned register: no ROS; no P/O numbers; "2 NADH per ethanol" is molecule bookkeeping (allowed — the numeric-P/O ban untouched); hero-neutral; 05.4 anti-confusion template at alc.nadh_burden.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the NADH engine + O2 crisis</name>
  <files>data/story_glucose/alcohol_path.json</files>
  <action>
    - alc.nadh_burden: the flood beat — two NADH per ethanol (one at ADH, one at ALDH2); dramatic register (the liver's wheel clogged by spent electron-carts; soul-ferries stacking at the ETC's door); teaching ("Under alcoholic conditions, the citric acid cycle is stalled by the oversupply of NADH derived from ethanol oxidation" verbatim grounding); MANDATORY anti-confusion template (05.4 §3.6): the two NADH carry the hero's ELECTRONS (soul), never the hero; the carbon travels ethanol→acetaldehyde→acetate→acetyl-CoA; the carbon NEVER becomes ATP/NADH/oxphos.
    - The O2 choice (topology frozen; label/text authored here): "The tissue runs out of oxygen — the flood stalls" — cond-FREE (both choices always eligible; the player picks the host's state; Phase 7 cond-neutralization precedent).
    - alc.bad.o2_crisis: dramatic (the air is gone; the ferries pile up with nowhere to unload; the distillery seizes; the host with it) + teaching (the 2 NADH can only be re-oxidized by the ETC; without O2 the NAD+ pool cannot regenerate; ethanol metabolism stalls; sustained failure starves the host — AN-ETC-02 + BAD-HOST-01 reuse verbatim); is_ending "bad" (already set in 06).
    - Wire only 08-03-approved claim ids.
  </action>
  <verify>python3.6 tools/story_editor_lint.py; echo "lint=$?"</verify>
  <done>NADH engine + O2 crisis authored; cond-free O2 choice; anti-confusion template landed; approved claims only.</done>
</task>

<task type="auto">
  <name>Task 2: Author the fork + lipogenesis Good road</name>
  <files>data/story_glucose/alcohol_path.json</files>
  <action>
    - alc.branch_fat_vs_burn: the fork beat — the flooded liver chooses: bank the flood as fat, or push the acetate toward the great wheel; teaching grounds BOTH sides verbatim (the NADH-shifted liver moves carbon toward fatty-acid synthesis — the alcoholic-fatty-liver link; acetate → acetyl-CoA → the citric acid cycle); the player picks the host's metabolic state (cond-neutralization honesty).
    - alc.lipogenesis: the flood is banked — the carbon body saved from the fire, stored as fat (Good semantics: retained pre-oxidation); DC-6 framing owned in the teaching layer: this same diversion, chronically, IS the alcoholic fatty-liver link — "the Good ending and the disease are the same road at different speeds" (only if the DC-6 verdict approved the tension framing; otherwise the honest disease note stays factual).
    - Verify the routing target end.good.fatty_acid is untouched (no edits to endings.json here — the node's own approved claims stand).
    - Grep the file for the banned register (ROS, P/O numbers, "pathogenic", bare "40%") — absent.
  </action>
  <verify>python3.6 -c "import sys; sys.path.insert(0,'tools'); import story_editor_json as j; raw=open('data/story_glucose/alcohol_path.json').read(); assert j.dumps(j.loads(raw))+'\n'==raw; print('roundtrip-ok')" && python3.6 tools/check_citations.py --story data/story_glucose --registry data/citations.json; echo "gate=$?" && python3.6 -m unittest discover -s tests 2>&1 | tail -3</verify>
  <done>Fork + Good fork authored with the DC-6 framing; end.good.fatty_acid untouched; byte-exact; gate + suite green.</done>
</task>

</tasks>

<verification>
python3.6 tools/story_editor_lint.py; echo "lint=$?"
python3.6 tools/check_citations.py --story data/story_glucose --registry data/citations.json; echo "gate=$?"
python3.6 -m unittest discover -s tests 2>&1 | tail -3
</verification>

<success_criteria>
The NADH-flood engine + fork + O2 crisis fully authored; anti-confusion + cond-free discipline held; shared Good ending reused untouched; suite + gate + lint green.
</success_criteria>

<output>.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-11-SUMMARY.md</output>
