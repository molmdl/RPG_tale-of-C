---
phase: 08-content-expansion-ii-fatty-acid-alcohol
plan: 05
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified: [data/citations.json, data/sources.json]
autonomous: true
must_haves:
  truths:
    - "Every claim approved in the FA + ALC batches is `approved` in data/citations.json with its -cand id unchanged (M1)"
    - "Every Phase 8 source has a sources.json record (approved, or rejected-with-provenance — 1ZU5, 2H8P retained as rejections)"
    - "The shared claims exist EXACTLY once: FA-ACTIVATION-01-cand + CONV-ACETYL-TCA-01-cand (no per-character duplicates)"
    - "Citation-gate output is fully accounted: residual = the sanctioned fa.stub/alc.stub pair only (retired by plan 06)"
  artifacts:
    - path: "data/citations.json"
      provides: "Approved Phase 8 claims (review_tier/inherits_source_approval/claim_text per hybrid schema; pdb_id/resolution_angstrom for CAST-*)"
      contains: "DIS-ACADM-01-cand"
    - path: "data/sources.json"
      provides: "Phase 8 source records + rejections retained for provenance"
      contains: "WIKIPEDIA-ETHANOL-PHARM"
  key_links:
    - from: "data/citations.json"
      to: "data/sources.json"
      via: "claim.source_id references an approved source"
      pattern: "source_id"
    - from: "08-02-SUMMARY.md + 08-03-SUMMARY.md"
      to: "data/citations.json"
      via: "every recorded verdict applied; nothing approved without a ledger line"
      pattern: "VERDICT"
---

<objective>
Registry landing (the 07-05 mirror, sole writer): apply ALL recorded FA + ALC batch outcomes to data/citations.json + data/sources.json.
Purpose: content plans (waves 4+) can only cite claims that exist and are approved here; the shared-claim coordination lands exactly once.
Output: updated registry; gate re-run with residuals documented.
</objective>

<context>
@.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-01-SUMMARY.md (DC-K shared ids)
@.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-02-SUMMARY.md (FA verdicts)
@.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-03-SUMMARY.md (ALC verdicts)
@.planning/phases/07-content-expansion-i-glucose/07-05-SUMMARY.md (landing shape precedent)
SOLE WRITER: only this plan edits citations.json + sources.json in Phase 8. Schema (07-05 context, binding): sources {reference, url, license, pdb_doi?, source_type, approval_status, approved_by:"human", approved_date, notes}; claims {claim, source_type, source, claim_text, source_id, review_tier, inherits_source_approval, review_notes (required for high-stakes), pdb_id?/resolution_angstrom?, approval_status, approved_by:"human", approved_date}. M1: KEEP -cand ids verbatim — flip approval_status only. Rejections RETAINED with provenance. High-stakes entries need review_notes evidence trails.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Land all FA + ALC batch outcomes in the registry</name>
  <files>data/citations.json, data/sources.json</files>
  <action>
    For each verdict in 08-02-SUMMARY.md + 08-03-SUMMARY.md:
    - APPROVED claim: approval_status="approved", approved_by="human", approved_date=today; keep the -cand id verbatim; fill review_tier, source_id, claim_text per the hybrid schema; add pdb_id/resolution_angstrom for CAST-* claims ( resolutions from the research §6/§7 tables, e.g. 1EGE 2.75 / 9PDQ 2.72 / 6VYI 3.0 / 1U3U 1.60 / 1O02 1.90 / 1ZUM 2.10).
    - Shared claims land ONCE: FA-ACTIVATION-01-cand + CONV-ACETYL-TCA-01-cand (referenced later by both characters' nodes — no ALC-ACSS2-01 duplicate unless the ALC ledger explicitly approved a split form; follow the recorded verdicts exactly).
    - Optional-claim verdicts honored literally: approved optionals land; dropped optionals (e.g. ALC-PERIPH-ACETATE-01-cand) are NOT filed.
    - REJECTED/documented-negative sources: sources.json records with approval_status="rejected" + provenance notes — 1ZU5 (FtsY, not ALDH2), 2H8P (K+ channel, not CPT-2), and any human-rejected source; do NOT delete.
    - PLACEHOLDER_PHASE8: stays OUT of the registry (the stub pair is retired by plan 06, not by a registry entry).
    - Do not touch story JSONs, edits.json, cast.json, or tests — plan 06 owns those.
  </action>
  <verify>python3.6 -c "import json;[json.load(open(p)) for p in ('data/citations.json','data/sources.json')];print('json-ok')"</verify>
  <done>Every approved claim has status approved with the -cand id intact; shared claims exist exactly once; rejected sources retained with provenance; no other file modified.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run citation gate + document residuals</name>
  <files>data/citations.json</files>
  <action>
    Run the gate. At this point in the wave order the fa.stub/alc.stub PLACEHOLDER_PHASE8 residual STILL exists (plan 06 retires it) — the expected form is exit 1 with EXACTLY 2 MISSING (the sanctioned pair) + 0 UNAPPROVED, now with the full Phase 8 claim set approved. Write the residual table into the SUMMARY under "Gate residuals (documented)". If any residual is NOT the sanctioned pair (e.g. a story node referencing a claim this plan failed to land), fix the registry entry — do not widen the residual list.
  </action>
  <verify>python3.6 tools/check_citations.py; echo "exit=$?"</verify>
  <done>Gate output fully accounted: residual = the documented sanctioned stub pair only; zero unexplained failures; suite still green.</done>
</task>

</tasks>

<verification>
python3.6 tools/check_citations.py; echo "exit=$?"
python3.6 -m unittest tests.test_citations -v 2>&1 | tail -3
</verification>

<success_criteria>
Registry reflects 100% of FA + ALC batch outcomes; -cand ids preserved; shared claims landed once; rejections retained; gate residuals documented (sanctioned pair only); registry tests green.
</success_criteria>

<output>.planning/phases/08-content-expansion-ii-fatty-acid-alcohol/08-05-SUMMARY.md</output>
