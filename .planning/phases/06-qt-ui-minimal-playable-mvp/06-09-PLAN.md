---
phase: 06-qt-ui-minimal-playable-mvp
plan: 09
type: execute
wave: 4
depends_on:
  - 06-06
files_modified:
  - c14/ui/edit_dialog.py
autonomous: true

must_haves:
  truths:
    - "EditDialog offers a CURATED list of edit options per enzyme (read from edits.json for the current enzyme_id + a few plausible wrong options), NOT free-form residue-entry"
    - "Selecting an option builds an EditIntent(op, target, args) via controller.build_edit_intent(op, target, args) whose signature() matches an edits.json entry (known) or falls through to the bad-ending pool (unknown — a valid game outcome)"
    - "The edit dialog is opened when the player clicks an edit:offer choice: the ChoicePanel (06-08) calls controller.request_edit(enzyme_id) (06-06 stashes _pending_edit_enzyme_id + goto edit.prompt); the MainWindow render_turn (06-08) detects turn.node.id == 'edit.prompt' + opens EditDialog(controller, controller._pending_edit_enzyme_id). 06-09 ONLY provides the EditDialog class API (selected_edit) — it does NOT modify controller.py or main_window.py (Warning 4 seam: 3 parallel wave-4 plans, non-overlapping files)"
  artifacts:
    - path: "c14/ui/edit_dialog.py"
      provides: "EditDialog (curated edit options per enzyme -> EditIntent)"
      contains: "class EditDialog"
  key_links:
    - from: "c14/ui/edit_dialog.py:EditDialog"
      to: "c14/ui/controller.py:build_edit_intent"
      via: "collects op/target/args from the curated selection; controller assembles the EditIntent"
      pattern: "build_edit_intent"
    - from: "c14/ui/edit_dialog.py"
      to: "c14/data/edits.json"
      via: "reads the known edits for the current enzyme_id to build the curated option list"
      pattern: "edits\\.json"
---

<objective>
Build the EditDialog (SC1 "edit interface" + the EDIT-01/02/03 UI). The edit routing is a CURATED lookup table (PROJECT.md:80 — "the game holds a curated lookup table of known edits"; NOT a free-form chemistry engine). The dialog reads `c14/data/edits.json` for the current enzyme's known edits + offers them + a few plausible wrong options. The player picks one; the controller builds the `EditIntent(op, target, args, enzyme_id)` whose `signature()` matches the table (known -> branch) or not (unknown -> bad-ending pool — a valid but unfun outcome). NO free-form residue-entry (would always route to bad-ending).

Purpose: The edit mechanic's UI surface. The Phase 6 placeholder `edits.json` (1 enzyme `fixture_enzyme_1` with 1 known edit) is the curated content; Phase 7 fills the real per-enzyme entries. The dialog structure is the contract Phase 7 fills.
Output: c14/ui/edit_dialog.py (EditDialog). py_compile only; human-verify in 06-14.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@c14/ui/controller.py
@c14/story/model.py
@c14/edit_router.py
@c14/data/edits.json
@.planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md
@.planning/phases/06-qt-ui-minimal-playable-mvp/06-RESEARCH-ui-adapter.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create c14/ui/edit_dialog.py — EditDialog (curated edit options -> EditIntent)</name>
  <files>c14/ui/edit_dialog.py</files>
  <action>
Create `c14/ui/edit_dialog.py` (gate-EXEMPT; `from pymol.Qt import QtCore, QtGui, QtWidgets`). Implement 06-RESEARCH-ui-adapter.md Pattern 5 (EditDialog):

`EditDialog(QtWidgets.QDialog)`:
- `__init__(self, controller, enzyme_id, parent=None)`:
  - Store `self._controller`, `self._enzyme_id = enzyme_id`.
  - Load the curated options: read `c14/data/edits.json` (via `json.load(open(str(c14.paths.data_path("data","edits.json"))))`); get `enzymes[enzyme_id].edits` (the known edits for this enzyme — each has a `signature` dict {op, target, args} + a `branch_node`); ALSO add a few plausible WRONG options (hardcoded in the dialog for Phase 6 — e.g. for a point_mutation enzyme: the known "resi 1 -> GLY" + 2 wrong like "resi 1 -> ALA", "resi 2 -> GLY" that do NOT match the table -> bad-ending pool). Build a list of `(label, op, target, args)` tuples: the known edit(s) labeled "Restore <resi> to <new_res> (the correct fix)"; the wrong ones labeled with their (plausible-looking) description.
  - If the enzyme_id has NO entries in edits.json (e.g. an enzyme without a known edit yet — Phase 7 fills them), show a message "No known edits for this enzyme yet (Phase 7 content). Your edit will route to the bad-ending pool." + offer a single generic wrong option so the player can still try (routes to bad-ending — a valid outcome). Handle the placeholder `fixture_enzyme_1` (the Phase 6 fixture).
  - UI: a `QVBoxLayout` with a `QLabel("Edit {0}".format(enzyme_id))` header + a `QButtonGroup` of `QRadioButton`s (one per curated option) + OK/Cancel. Store the selected option's `(op, target, args)` on accept.
  - `self._options` = list of `(label, op, target, args)`; `self._selected` = None.
  - On OK: `idx = self._button_group.checkedId()`; if idx >= 0: `self._selected = self._options[idx]`.
- `selected_edit(self)` -> returns the selected `(op, target, args)` tuple or None.
  - A static helper `submit(controller, enzyme_id, parent=None)` that opens the dialog, on accept calls `op, target, args = dialog.selected_edit(); intent = controller.build_edit_intent(op, target, args); controller.apply_edit(intent)` (the controller reads enzyme_id from the current node's tags in build_edit_intent — pass enzyme_id for clarity / validation).

WARNING 4 FIX — file ownership / seam contract (this plan ONLY creates `c14/ui/edit_dialog.py`; it does NOT modify 06-06's controller.py or 06-08's main_window.py — those are parallel wave-4 plans with separate file ownership). The edit:offer -> edit.prompt -> EditDialog -> apply_edit flow is wired across 3 plans via a declared seam:
- **06-06 (controller.py) provides**: `controller.request_edit(enzyme_id)` (stashes `self._pending_edit_enzyme_id` + `engine.goto("edit.prompt")`) + `controller._pending_edit_enzyme_id` attribute + `build_edit_intent` fallback to `_pending_edit_enzyme_id` when `_current_enzyme_id()` is None (already specified in 06-06 Task 2 — 06-09 does NOT add it).
- **06-08 (main_window.py) provides**: the `render_turn` detection of `turn.node.id == "edit.prompt"` -> `EditDialog(self._controller, self._controller._pending_edit_enzyme_id, self)` + on accept `controller.build_edit_intent(...)` + `controller.apply_edit(intent)` (already specified in 06-08 Task 2 — 06-09 does NOT add it).
- **06-09 (this plan) provides**: the `EditDialog` class API the above two call: `EditDialog(controller, enzyme_id, parent)` + `selected_edit() -> (op, target, args)`. 06-09 ONLY declares this API + reads `c14/data/edits.json` for the curated options. It does NOT reference or modify controller.py / main_window.py (consume the seam, don't edit the provider).

So this plan's deliverable is exactly: the `EditDialog` class (curated options from edits.json + plausible wrong options -> `(op, target, args)` via `selected_edit()`). The two callers (06-06's request_edit + 06-08's render_turn) are already specified in their own plans. Do NOT add `maybe_open_edit_prompt` to MainWindow here (06-08 owns render_turn). Do NOT add request_edit to the controller here (06-06 owns it). This keeps the 3 parallel wave-4 plans non-overlapping (06-06: controller.py; 06-08: main_window.py + widgets.py; 06-09: edit_dialog.py — no shared files).

AVOID: do NOT offer free-form residue-entry / free-form mutation-text (would produce EditIntents matching nothing -> always bad-ending; unfun + not the curated-table design). do NOT call cmd.* (the controller + molops apply the edit via the routed branch's on_enter MolAction("edit",...)). do NOT re-implement signature matching (the EditRouter does it; the dialog just builds the EditIntent). do NOT use f-strings. do NOT fabricate real disease mutations (Phase 6 uses the placeholder fixture_enzyme_1; Phase 7 fills real per-enzyme edits — 05.4-CONVENTION.md §3.7 no-fabricated-science).
  </action>
  <verify>`python3.6 -m py_compile c14/ui/edit_dialog.py` exits 0 (syntax; importing in WSL fails — EXPECTED for Qt). `python3.6 tools/check_imports.py` exits 0 (c14/ui/ exempt).</verify>
  <done>EditDialog present; curated options from edits.json + plausible wrong options; builds EditIntent via controller.build_edit_intent; the edit:offer -> edit.prompt -> EditDialog seam documented. py_compile passes.</done>
</task>

</tasks>

<verification>
- `python3.6 -m py_compile c14/ui/edit_dialog.py` — syntax (WSL-verifiable; importing fails — EXPECTED for Qt).
- `python3.6 tools/check_imports.py` — AST gate clean.
- The functional edit flow (edit:offer -> edit.prompt -> EditDialog -> apply_edit -> routed branch/bad-ending) is human-verify in 06-14.
</verification>

<success_criteria>
- EditDialog offers curated edit options per enzyme (from edits.json + plausible wrong options); NO free-form entry.
- Selecting an option builds an EditIntent via controller.build_edit_intent; apply_edit routes via the EditRouter (known -> branch, unknown -> bad pool).
- The edit:offer -> edit.prompt -> EditDialog seam wired (controller.request_edit + _pending_edit_enzyme_id).
- py_compile + AST gate pass; functional human-verify in 06-14.
</success_criteria>

<output>
After completion, create `.planning/phases/06-qt-ui-minimal-playable-mvp/06-09-SUMMARY.md`
</output>
