---
phase: 06-qt-ui-minimal-playable-mvp
plan: 12
type: execute
wave: 3
depends_on:
  - 06-04
files_modified:
  - c14/ui/achievements_dialog.py
autonomous: true

must_haves:
  truths:
    - "AchievementsDialog reads AchievementBoard.data + renders the achievements_unlocked list (name + description + date) + characters_tried + endings_found + branches_discovered counts"
    - "The board is collection-based (no scores/ranks) — ACH-01; the dialog shows what was found, not a ranking"
    - "The dialog reads the persisted achievements.json (via the AchievementBoard instance from the controller) so unlocks from prior sessions show (ACH-02 cross-session)"
  artifacts:
    - path: "c14/ui/achievements_dialog.py"
      provides: "AchievementsDialog (renders the collection-based achievement board)"
      contains: "class AchievementsDialog"
  key_links:
    - from: "c14/ui/achievements_dialog.py:AchievementsDialog"
      to: "c14.achievements.AchievementBoard.data"
      via: "reads board.data['achievements_unlocked'/'characters_tried'/'endings_found'/'branches_discovered']"
      pattern: "achievements_unlocked"
---

<objective>
Build the AchievementsDialog (ACH-01/ACH-02 UI + SC5 "the achievement board shows unlocks (glucose tried + endings found so far) and persists across PyMOL sessions"). A QDialog that reads the `AchievementBoard` (06-04) data + renders the collection-based board: unlocked achievements (name + description + date), characters tried, endings found (with tier + node_id), branches discovered count. Collection-based (NOT a leaderboard — ACH-01 explicit). Reads the persisted `achievements.json` (via the board instance) so prior-session unlocks show (ACH-02 cross-session).

Purpose: SC5's achievement board UI. The board logic (06-04) owns unlock detection + persistence; this dialog is the read-only renderer.
Output: c14/ui/achievements_dialog.py. py_compile only; human-verify in 06-14.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@c14/achievements.py
@c14/ui/controller.py
@.planning/phases/06-qt-ui-minimal-playable-mvp/06-RESEARCH-persistence-achievements.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create c14/ui/achievements_dialog.py — AchievementsDialog (collection-based board renderer)</name>
  <files>c14/ui/achievements_dialog.py</files>
  <action>
Create `c14/ui/achievements_dialog.py` (gate-EXEMPT; `from pymol.Qt import QtCore, QtGui, QtWidgets`). Implement 06-RESEARCH-persistence-achievements.md Pattern 3 (the dialog):

`AchievementsDialog(QtWidgets.QDialog)`:
- `__init__(self, achievement_board, parent=None)`: store `self._board = achievement_board`; `setWindowTitle("RPG: Tale of C — Achievements")`; `setMinimumWidth(520)`. Build a `QVBoxLayout`:
  - Header: `QLabel("<b>Achievements</b>")` + a summary line `"Characters tried: {n}/3  |  Endings found: {n}  |  Branches discovered: {n}".format(...)` reading `board.data["characters_tried"]` (len), `board.data["endings_found"]` (len), `board.data["branches_discovered"]` (len).
  - "Unlocked" section: `QLabel("<b>Unlocked</b>")`; for each `a` in `board.data["achievements_unlocked"]`: a `QLabel("{name} — {description}  ({date})".format(...))` (or a QListWidget). Show locked ones greyed if desired (the catalog has 6 v1 achievements; cross-reference `ACHIEVEMENT_CATALOG_V1` to show locked vs unlocked — optional for Phase 6; at minimum show the unlocked list).
  - "Endings found" section: `QLabel("<b>Endings found</b>")`; for each `e` in `board.data["endings_found"]`: `QLabel("{tier}: {node_id} ({character}, {date})".format(...))` (tier capitalized: True/Good/Normal/Bad).
  - "Characters tried" section: a single `QLabel(", ".join(board.data["characters_tried"]))` (or "None yet").
  - Close button: `QPushButton("Close"); clicked -> self.accept()`.
  - Make the dialog scrollable if the content overflows (wrap in a `QScrollArea`).
- The dialog is read-only (no unlock buttons — unlocks happen via gameplay; the board auto-persists via 06-04). Reads `board.data` directly (the board is kept in sync by the controller's `on_turn` calls; opening the dialog shows the current state).
- For cross-session persistence (ACH-02), the `AchievementBoard` was constructed at MainWindow init (06-08) with the default `user_data_path("achievements.json")` path + `_load()`s the persisted file — so prior-session unlocks show automatically.

AVOID: do NOT make it a leaderboard (no scores/ranks/timestamps-as-scores — ACH-01; 06-RESEARCH-persistence-achievements.md Anti-Pattern). do NOT add unlock buttons (unlocks are gameplay-driven; the board auto-detects). do NOT write to the board from the dialog (read-only; the controller calls `on_turn`). do NOT use f-strings. do NOT scope the dramatic cast list / slogan (DOC-01/DOC-02 are Phase 9 — 06-RESEARCH-persistence-achievements.md "Don't scope DOC-01/DOC-02").
  </action>
  <verify>`python3.6 -m py_compile c14/ui/achievements_dialog.py` exits 0 (syntax; importing in WSL fails — EXPECTED for Qt). `python3.6 tools/check_imports.py` exits 0.</verify>
  <done>AchievementsDialog present; renders unlocked achievements + endings found + characters tried + branches count from board.data; collection-based (no scores); read-only. py_compile passes.</done>
</task>

</tasks>

<verification>
- `python3.6 -m py_compile c14/ui/achievements_dialog.py` — syntax (WSL-verifiable).
- `python3.6 tools/check_imports.py` — AST gate clean.
- The functional achievement board (unlocks show after playthroughs, persists across PyMOL restarts) is human-verify in 06-14 (SC5).
</verification>

<success_criteria>
- AchievementsDialog renders the collection-based board (unlocked + endings + characters + branches count) from AchievementBoard.data.
- Read-only; cross-session (reads persisted achievements.json via the board).
- py_compile + AST gate pass; functional human-verify in 06-14.
</success_criteria>

<output>
After completion, create `.planning/phases/06-qt-ui-minimal-playable-mvp/06-12-SUMMARY.md`
</output>
