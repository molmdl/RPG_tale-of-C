---
phase: 06-qt-ui-minimal-playable-mvp
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - c14/data/help.json
  - c14/ui/help_dialog.py
autonomous: true

must_haves:
  truths:
    - "c14/data/help.json contains editing pointers (point mutation / substrate edit / protonation change / restore safety net — the Phase 4 EDIT-01..05 mechanics in plain language) + a small curated list of PyMOL wiki links"
    - "Every wiki link URL in help.json is webfetch-verified LIVE before commit (no fabricated URLs — only the 3 already-verified 2026-08-29: Alter->Iterate, H_Add, Fetch; any additional link MUST be webfetch-verified in this plan)"
    - "HelpDialog renders help.json: the editing pointers as text + the wiki links as clickable buttons opening the user's browser via QtGui.QDesktopServices.openUrl"
    - "DOC-01/DOC-02 (dramatic cast list + slogan) are NOT scoped here — those are Phase 9 (help.json is ONLY editing pointers + wiki links, DOC-03)"
  artifacts:
    - path: "c14/data/help.json"
      provides: "Curated help content (editing pointers + verified wiki links)"
      contains: "editing_pointers"
    - path: "c14/ui/help_dialog.py"
      provides: "HelpDialog (renders help.json + clickable wiki links)"
      contains: "class HelpDialog"
  key_links:
    - from: "c14/ui/help_dialog.py:HelpDialog"
      to: "c14/data/help.json"
      via: "json.load(data_path('data','help.json')) + renders editing_pointers + wiki_links"
      pattern: "help\\.json"
    - from: "c14/ui/help_dialog.py (wiki link button)"
      to: "QtGui.QDesktopServices.openUrl"
      via: "btn.clicked -> openUrl(QUrl(link['url']))"
      pattern: "QDesktopServices"
---

<objective>
Build the in-game help (DOC-03: "molecule-editing pointers and/or PyMOL wiki links"). `c14/data/help.json` holds the curated content (editing pointers describing the Phase 4 EDIT-01/02/03/05 mechanics in plain language + a small list of PyMOL wiki links). `HelpDialog` renders it: the editing pointers as text + the wiki links as clickable buttons opening the user's browser via `QtGui.QDesktopServices.openUrl`. NO fabricated URLs — only webfetch-verified links ship (the 3 already-verified 2026-08-29: Alter→Iterate, H_Add, Fetch; any additional link MUST be webfetch-verified in this plan's Task 1).

Purpose: SC5's "in-game help includes molecule-editing pointers + PyMOL wiki links" (DOC-03). Scope guard: DOC-01/DOC-02 (dramatic cast list + slogan) are Phase 9 — NOT scoped here (the cast isn't populated yet; 06-RESEARCH-persistence-achievements.md "Don't scope DOC-01/DOC-02").
Output: c14/data/help.json + c14/ui/help_dialog.py. py_compile + webfetch-verify the links.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@c14/paths.py
@.planning/phases/06-qt-ui-minimal-playable-mvp/06-RESEARCH-persistence-achievements.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create c14/data/help.json (editing pointers + webfetch-verified wiki links)</name>
  <files>c14/data/help.json</files>
  <action>
Create `c14/data/help.json` with the FROZEN-verbatim content from 06-RESEARCH-persistence-achievements.md Pattern 6 (lines 434-456). Schema:
```json
{
  "version": 1,
  "editing_pointers": [
    {"title": "Point mutation", "body": "Swap one residue in an enzyme's active site. Use the edit panel to pick a residue + a new identity. A known reverse-mutation (matching a disease allele) routes the story to a 'you restored it' branch; an unknown edit falls through to a bad ending."},
    {"title": "Substrate edit", "body": "Add or remove a group on the small molecule (the C14 hero or an intermediate). Pick the substrate + the group change in the edit panel."},
    {"title": "Protonation change", "body": "Switch a catalytic residue's or substrate's protonation state between curated variants (e.g. HIP/HIE/HID for histidine). Defaults are physiological pH; you can adjust."},
    {"title": "Restore safety net", "body": "If an edit goes wrong, 'Reveal correct 3D model' restores the pre-edit snapshot without restarting (keeps gameplay smooth)."}
  ],
  "wiki_links": [
    {"label": "alter (residue editing)", "url": "https://pymolwiki.org/index.php/Alter", "note": "Read-write atom properties; sort after altering identifiers. Verified live 2026-08-29 (redirects to Iterate)."},
    {"label": "h_add (protonation)", "url": "https://pymolwiki.org/index.php/H_Add", "note": "Valence-only hydrogen add (NOT pH-aware) -- why the game uses curated variants. Verified live 2026-08-29."},
    {"label": "fetch (loading structures)", "url": "https://pymolwiki.org/index.php/Fetch", "note": "Downloads from PDB/PubChem; async=0 for scripting. Verified live 2026-08-29."}
  ]
}
```

The 3 wiki links were already webfetch-verified live 2026-08-29 (06-RESEARCH-persistence-achievements.md Secondary sources). To be safe + satisfy the no-fabrication rule, BEFORE committing, webfetch each of the 3 URLs to CONFIRM they still resolve (use the webfetch tool on `https://pymolwiki.org/index.php/Alter`, `https://pymolwiki.org/index.php/H_Add`, `https://pymolwiki.org/index.php/Fetch`). If any fails to resolve, drop it from the shipped list + note it in the SUMMARY. Do NOT add additional speculative links (e.g. Show, Select, Sort) UNLESS you webfetch-verify each one in this task (06-RESEARCH-persistence-achievements.md Open Question 3: "ship the 3 verified links for Phase 6 MVP; the planner adds more only with a live-fetch verification step").

The editing pointer text describes the Phase 4 MECHANICS (not chemistry claims), so no citation gate concern — but do NOT make chemistry claims beyond what Phase 4 already cited (06-RESEARCH-persistence-achievements.md Pattern 6 scope guard).

AVOID: do NOT fabricate wiki URLs (no-fabrication rule — only ship webfetch-verified links). do NOT scope the dramatic cast list / slogan (DOC-01/DOC-02 — Phase 9). do NOT add chemistry claims beyond Phase 4. do NOT use real PDB IDs.
  </action>
  <verify>`python3.6 -c "import json; d=json.load(open('c14/data/help.json')); assert 'editing_pointers' in d and 'wiki_links' in d; assert len(d['wiki_links'])==3; print('ok')"` exits 0 + prints `ok`. Webfetch each of the 3 URLs (Alter, H_Add, Fetch) to confirm they resolve; record the verification in the SUMMARY. `git diff` shows the new file only.</verify>
  <done>help.json present with 4 editing pointers + 3 webfetch-verified wiki links; valid JSON; no fabricated URLs; no DOC-01/02 scope.</done>
</task>

<task type="auto">
  <name>Task 2: Create c14/ui/help_dialog.py — HelpDialog (renders help.json + clickable wiki links)</name>
  <files>c14/ui/help_dialog.py</files>
  <action>
Create `c14/ui/help_dialog.py` (gate-EXEMPT; `from pymol.Qt import QtCore, QtGui, QtWidgets`). Implement 06-RESEARCH-persistence-achievements.md Pattern 6 (the dialog):

`HelpDialog(QtWidgets.QDialog)`:
- `__init__(self, parent=None)`: `setWindowTitle("RPG: Tale of C — Help")`; `setMinimumWidth(520)`. Load help.json: `with open(str(c14.paths.data_path("data", "help.json")), "r", encoding="utf-8") as fh: self.data = json.load(fh)`. Build a `QVBoxLayout`:
  - `QLabel("<b>Molecule editing</b>")`.
  - For each `p` in `self.data["editing_pointers"]`: `QLabel("<b>{0}</b><br/>{1}".format(p["title"], p["body"]))` (use `setText` with the rich-text form; Qt QLabel supports a subset of HTML). Add to layout.
  - `QLabel("<b>PyMOL wiki references</b>")`.
  - For each `link` in `self.data["wiki_links"]`: `btn = QPushButton(link["label"])`; `btn.clicked.connect(lambda _=False, u=link["url"]: QtGui.QDesktopServices.openUrl(QtCore.QUrl(u)))`; add to layout. (The `lambda _=False, u=...` captures the URL avoiding the late-binding closure bug; the `_` is the clicked() bool signal arg.)
  - Optionally show `link["note"]` as a tooltip (`btn.setToolTip(link["note"])`).
  - Close button: `QPushButton("Close"); clicked -> self.accept()`.
- Wrap the content in a `QScrollArea` if it overflows.

AVOID: do NOT hardcode the help text in the dialog (read from help.json so it's editable without touching Python — 06-RESEARCH-persistence-achievements.md Pattern 6). do NOT open URLs with `os.system`/`webbrowser` (use `QtGui.QDesktopServices.openUrl` — the Qt way, works cross-platform inside PyMOL). do NOT use f-strings. do NOT scope DOC-01/02.
  </action>
  <verify>`python3.6 -m py_compile c14/ui/help_dialog.py` exits 0 (syntax; importing in WSL fails — EXPECTED for Qt). `python3.6 tools/check_imports.py` exits 0.</verify>
  <done>HelpDialog present; renders help.json (editing pointers + clickable wiki links via QDesktopServices); reads from data_path('data','help.json'). py_compile passes.</done>
</task>

</tasks>

<verification>
- `python3.6 -c "import json; json.load(open('c14/data/help.json'))"` — valid JSON with editing_pointers + wiki_links.
- Webfetch the 3 wiki URLs (Alter, H_Add, Fetch) to confirm they resolve (no fabricated URLs).
- `python3.6 -m py_compile c14/ui/help_dialog.py` — syntax (WSL-verifiable).
- `python3.6 tools/check_imports.py` — AST gate clean.
- The functional help dialog (editing pointers show, wiki links open in browser) is human-verify in 06-14.
</verification>

<success_criteria>
- help.json with 4 editing pointers + 3 webfetch-verified wiki links (no fabricated URLs).
- HelpDialog renders help.json + clickable wiki links via QDesktopServices.openUrl.
- DOC-01/02 NOT scoped (Phase 9). py_compile + AST gate pass; functional human-verify in 06-14.
</success_criteria>

<output>
After completion, create `.planning/phases/06-qt-ui-minimal-playable-mvp/06-13-SUMMARY.md`
</output>
