---
phase: 06-qt-ui-minimal-playable-mvp
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - data/story_glucose/pyruvate_branch.json
  - tests/test_glucose_reachability.py
autonomous: true

must_haves:
  truths:
    - "At pyr.branch, the interpreter's _cond evaluates the aerobic choice cond to True when host_o2_low is unset (player can proceed aerobically to TCA -> True ending)"
    - "At pyr.branch, the anaerobic choice cond evaluates to False when host_o2_low is unset (the anaerobic branch is not selectable until a flag-setter exists — Phase 7 content)"
    - "The cond syntax uses flags.get('host_o2_low') (dict-method form) NOT flags.host_o2_low (dict-attribute -> AttributeError -> False -> stuck)"
    - "The structural reachability (BFS, cond ignored) is UNCHANGED — the 55-node/21-ending counts + the existing reachability tests stay green (this is a COND-SYNTAX fix, NOT a topology change)"
  artifacts:
    - path: "data/story_glucose/pyruvate_branch.json"
      provides: "Fixed cond syntax on pyr.branch's 2 choices (dict-method form)"
      contains: "flags.get('host_o2_low')"
    - path: "tests/test_glucose_reachability.py"
      provides: "Runtime-eligibility test asserting both pyr.branch conds evaluate correctly"
      contains: "pyr.branch"
  key_links:
    - from: "data/story_glucose/pyruvate_branch.json:pyr.branch choices"
      to: "c14/story/interpreter.py:_cond"
      via: "eval(cond, {flags: state.flags (dict), ...}) — flags.get(...) works; flags.attr raises AttributeError"
      pattern: "flags\\.get\\('host_o2_low'\\)"
---

<objective>
Fix SC#3 Blocker A: the `pyr.branch` node's 2 choice conds use `flags.host_o2_low` (dict-attribute access) which the interpreter's `_cond` (interpreter.py:124-133) exposes `flags` as a DICT → `dict.host_o2_low` raises `AttributeError` → caught → both choices hidden → the player CANNOT advance past pyr.branch → cannot reach the TCA → cannot reach a True ending → SC#3 BLOCKED. The fix is a 2-char-per-cond content edit: `flags.host_o2_low` → `flags.get('host_o2_low')` (dict-method form, which the interpreter's namespace supports — `tca.shuffle`'s `visits.get('tca.shuffle', 0) > 5` already uses this form and works).

This is a COND-SYNTAX fix to the FROZEN 5.1 skeleton, NOT a topology change: no nodes/edges/weights change, the structural reachability (BFS, cond ignored) is unchanged, the 55-node/21-ending counts + the existing reachability tests stay green. The skeleton is FROZEN at topology; this fixes a RUNTIME bug that makes the FROZEN topology actually playable (the 05.1-06 review didn't exercise runtime cond evaluation). STATE.md + 06-RESEARCH-ui-adapter.md Open Question 3 both flag this as a Phase 6 fix.

Purpose: Unblock SC#3 (player can reach a True ending via the aerobic path: pyr.branch → pyr.pdh → tca → etc → end.true).
Output: 2 fixed conds in pyruvate_branch.json + a runtime-eligibility test confirming the conds evaluate correctly.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@c14/story/interpreter.py
@c14/story/model.py
@c14/state.py
@data/story_glucose/pyruvate_branch.json
@data/story_glucose/tca.json
@tests/test_glucose_reachability.py
@.planning/phases/06-qt-ui-minimal-playable-mvp/06-RESEARCH-ui-adapter.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the 2 pyr.branch conds to the dict-method form (flags.get('host_o2_low'))</name>
  <files>data/story_glucose/pyruvate_branch.json</files>
  <action>
Edit `data/story_glucose/pyruvate_branch.json` `pyr.branch.choices` (lines 12-13):

- Choice 1 (aerobic): `"cond": "not flags.host_o2_low"` → `"cond": "not flags.get('host_o2_low')"`
- Choice 2 (anaerobic): `"cond": "flags.host_o2_low"` → `"cond": "flags.get('host_o2_low')"`

Rationale (cite in a trailing comment OR the commit message): the interpreter's `_cond` (interpreter.py:124-133) exposes `flags` as `state.flags` (a DICT). `flags.host_o2_low` is dict-attribute access → `AttributeError` (dict has no attribute `host_o2_low`) → `_cond`'s `except Exception: return False` → BOTH choices hidden → stuck. `flags.get('host_o2_low')` is dict-method access → returns `None` (unset) → `not None` is `True` (aerobic eligible); `bool(None)` is `False` (anaerobic hidden until a flag-setter exists). This matches `tca.shuffle`'s working cond `visits.get('tca.shuffle', 0) > 5` (pyruvate_branch's sibling node — same dict-method form, same interpreter).

Do NOT change anything else in pyruvate_branch.json (no topology, no weights, no effects, no labels, no other conds). Do NOT change the `effects: {"set": {"anaerobic": true}}` on the anaerobic choice (it sets `anaerobic`, not `host_o2_low` — that's a Phase 7 content question; Phase 6 only fixes the cond SYNTAX so the aerobic path runs). Do NOT touch the other files in `data/story_glucose/`.

Verify the JSON is still valid after the edit (no trailing commas, proper quoting — the single quotes inside the double-quoted JSON string are fine: `"cond": "not flags.get('host_o2_low')"` is valid JSON; the single quotes are part of the string value).
  </action>
  <verify>`python3.6 -c "import json; json.load(open('data/story_glucose/pyruvate_branch.json'))"` exits 0 (valid JSON); `grep -c "flags.get('host_o2_low')" data/story_glucose/pyruvate_branch.json` prints `2` (both conds fixed); `grep -c "flags.host_o2_low" data/story_glucose/pyruvate_branch.json` prints `0` (no broken conds remain).</verify>
  <done>Both pyr.branch conds use flags.get('host_o2_low'); JSON valid; no flags.host_o2_low remains; no other change to the file.</done>
</task>

<task type="auto">
  <name>Task 2: Add a runtime-eligibility test to tests/test_glucose_reachability.py</name>
  <files>tests/test_glucose_reachability.py</files>
  <action>
Extend `tests/test_glucose_reachability.py` with a new `TestPyrBranchRuntimeEligibility(unittest.TestCase)` (or add to an existing class if appropriate — but a new class is cleaner). This test exercises the interpreter's `_cond` at RUNTIME (not structural BFS — the existing reachability tests cover BFS) to confirm the fixed conds evaluate correctly.

Build a `StoryGraph.load("data/story_glucose")` (reuse the existing `_story_dir()` helper pattern in the file if one exists, or resolve `data/story_glucose` relative to the test file CWD-safely — mirror test_engine.py:24-26's `os.path.join(os.path.dirname(__file__), '..', 'data', 'story_glucose')`). Build a `StoryInterpreter()` + a `GameState` with `host_o2_low` UNSET (the default at pyr.branch — no effect sets it before pyr.branch in the aerobic path).

Tests:
1. `test_pyr_branch_aerobic_choice_eligible_when_host_o2_low_unset` — get the `pyr.branch` node; find the aerobic choice (the one with `cond` starting with `not `); `assert interpreter._cond(choice.cond, state) is True` (the player CAN proceed aerobically). Use a fresh `GameState` with empty `flags` (host_o2_low unset → `flags.get('host_o2_low')` is None → `not None` is True).
2. `test_pyr_branch_anaerobic_choice_hidden_when_host_o2_low_unset` — find the anaerobic choice (cond = `flags.get('host_o2_low')`); `assert interpreter._cond(choice.cond, state) is False` (None is falsy → anaerobic hidden until a flag-setter exists).
3. `test_pyr_branch_aerobic_hidden_when_host_o2_low_set_true` — `state.set_flag('host_o2_low', True)`; aerobic `assert interpreter._cond(...) is False`; anaerobic `assert interpreter._cond(...) is True` (the flag flips eligibility — proves the cond is actually reading the flag, not just always-True).
4. `test_pyr_branch_both_choices_not_stuck` — assert at least ONE choice is eligible when host_o2_low is unset (aerobic True) — the player can advance (regression guard: the OLD broken cond made BOTH False → stuck; this test proves the fix).
5. `test_no_broken_dict_attribute_conds_remain` — scan ALL choice conds in the glucose story (`graph.all_nodes()` → iterate `node.choices` → `choice.cond`); assert NO cond contains the broken pattern `flags.<attr>` (a bare attribute access on the flags dict). Use a regex: a cond containing `flags.` followed by a Python identifier NOT immediately followed by `(` (i.e. `flags.host_o2_low` is bad; `flags.get(...)` is good). Implement: `re.search(r"flags\.[a-zA-Z_][a-zA-Z0-9_]*(?!\()", cond)` → if matched, fail with the node_id + cond. This is a GREP-style guard ensuring the fix doesn't regress elsewhere (catches any other node with the same bug). NOTE: `tca.shuffle`'s `visits.get(...)` uses `visits.` not `flags.` so it's fine; scan only `flags.` attribute access. (If other nodes legitimately use `flags.X` they'd need the same fix — this test surfaces them.)

Use `interpreter._cond(cond, state)` directly (it's a method; the underscore is a convention, not enforced — test_interpreter.py already calls it). Build `GameState` via `GameState.new_game("glucose", 42, "intro.preface")` or `GameState()` with `flags={}`.

AVOID: do NOT change the structural reachability tests (TestGlucoseReachability etc. — those test BFS, cond ignored; they must stay green). do NOT add a host_o2_low flag-setter to the story (that's Phase 7 content — the anaerobic branch becomes reachable when Phase 7 adds a mechanism to set host_o2_low; Phase 6 only needs the aerobic path to reach True + the cycle-trap to reach Bad). do NOT touch the other story JSON files.
  </action>
  <verify>`python3.6 -m unittest tests.test_glucose_reachability -v` passes (existing reachability tests green — 55 nodes/21 endings unchanged + 5 new eligibility tests green); `python3.6 -m unittest discover -s tests` passes (full suite green).</verify>
  <done>5 new runtime-eligibility tests green: aerobic eligible when unset, anaerobic hidden when unset, flip when set, not-stuck regression guard, no-broken-dict-attribute-conds scan. Existing reachability tests untouched (structural BFS unchanged).</done>
</task>

</tasks>

<verification>
- `python3.6 -c "import json; json.load(open('data/story_glucose/pyruvate_branch.json'))"` — valid JSON.
- `grep -c "flags.get('host_o2_low')" data/story_glucose/pyruvate_branch.json` — 2 (both fixed).
- `grep -c "flags.host_o2_low" data/story_glucose/pyruvate_branch.json` — 0 (no broken conds).
- `python3.6 -m unittest tests.test_glucose_reachability -v` — existing reachability green (topology unchanged) + 5 new eligibility tests green.
- `python3.6 -m unittest discover -s tests` — full suite green.
</verification>

<success_criteria>
- pyr.branch's 2 conds use `flags.get('host_o2_low')` (dict-method form); JSON valid.
- At runtime: aerobic choice eligible when host_o2_low unset (player can proceed to TCA → True ending); anaerobic hidden until a flag-setter exists (Phase 7).
- Structural reachability unchanged (55 nodes/21 endings; existing tests green).
- 5 new runtime-eligibility tests green (incl. a no-broken-dict-attribute-conds regression scan).
</success_criteria>

<output>
After completion, create `.planning/phases/06-qt-ui-minimal-playable-mvp/06-05-SUMMARY.md`
</output>
