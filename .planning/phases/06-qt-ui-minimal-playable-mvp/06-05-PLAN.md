---
phase: 06-qt-ui-minimal-playable-mvp
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - data/story_glucose/pyruvate_branch.json
  - data/story_glucose/intro.json
  - tests/test_glucose_reachability.py
autonomous: true

must_haves:
  truths:
    - "At pyr.branch, the interpreter's _cond evaluates the aerobic choice cond to True when host_o2_low is unset (player can proceed aerobically to TCA -> True ending)"
    - "At pyr.branch, the anaerobic choice cond evaluates to False when host_o2_low is unset (the anaerobic branch is not selectable until a flag-setter exists — Phase 7 content)"
    - "The cond syntax uses flags.get('host_o2_low') (dict-method form) NOT flags.host_o2_low (dict-attribute -> AttributeError -> False -> stuck)"
    - "The structural reachability (BFS, cond ignored) is UNCHANGED — the 55-node/21-ending counts + the existing reachability tests stay green (this is a COND-SYNTAX fix, NOT a topology change)"
    - "intro.preface on_enter loads a bundled placeholder (_smoke.pdb, NO network fetch, NO fabricated PDB ID) as hero_atom + aa_cast instead of the crashing pdb:TBD_* targets — the start node does NOT crash on the first load MolAction (Blocker 1 fix b)"
    - "intro.preface on_enter contains the 6-call hero-highlight sequence from 05.4-CONVENTION.md §2.1 (set_color hero_cyan + show_as sticks + color hero_cyan on elem C + show spheres + set sphere_scale 0.3 + label YOU) on the loaded placeholder — SC2 (hero highlight) is human-verifiable at Phase 6 WITHOUT fabricating science (the placeholder is a test fixture, NOT a cited PDB)"
    - "intro.shell_glucose on_enter loads _smoke.pdb as glucose (placeholder, no TBD_GLUCOSE network fetch) so the second start-node load also does not crash"
  artifacts:
    - path: "data/story_glucose/pyruvate_branch.json"
      provides: "Fixed cond syntax on pyr.branch's 2 choices (dict-method form)"
      contains: "flags.get('host_o2_low')"
    - path: "data/story_glucose/intro.json"
      provides: "Start-node on_enter loads bundled _smoke.pdb placeholders (no TBD_* network fetch) + the 6-call hero-highlight sequence on hero_atom (SC2 verifiable at Phase 6)"
      contains: "hero_cyan"
    - path: "tests/test_glucose_reachability.py"
      provides: "Runtime-eligibility test asserting both pyr.branch conds evaluate correctly + a start-node on_enter shape test"
      contains: "pyr.branch"
  key_links:
    - from: "data/story_glucose/pyruvate_branch.json:pyr.branch choices"
      to: "c14/story/interpreter.py:_cond"
      via: "eval(cond, {flags: state.flags (dict), ...}) — flags.get(...) works; flags.attr raises AttributeError"
      pattern: "flags\\.get\\('host_o2_low'\\)"
    - from: "data/story_glucose/intro.json:intro.preface on_enter"
      to: "c14/pymol_layer/molops.py:load branch + set_color/label/show_as/color/show/set"
      via: "target='_smoke.pdb' -> load_bundled (no KeyError, no network); the 6-call hero-highlight sequence dispatches via molops.apply (06-01)"
      pattern: "_smoke\\.pdb|hero_cyan|YOU"
---

<objective>
Fix SC#3 Blocker A: the `pyr.branch` node's 2 choice conds use `flags.host_o2_low` (dict-attribute access) which the interpreter's `_cond` (interpreter.py:124-133) exposes `flags` as a DICT → `dict.host_o2_low` raises `AttributeError` → caught → both choices hidden → the player CANNOT advance past pyr.branch → cannot reach the TCA → cannot reach a True ending → SC#3 BLOCKED. The fix is a 2-char-per-cond content edit: `flags.host_o2_low` → `flags.get('host_o2_low')` (dict-method form, which the interpreter's namespace supports — `tca.shuffle`'s `visits.get('tca.shuffle', 0) > 5` already uses this form and works).

This is a COND-SYNTAX fix to the FROZEN 5.1 skeleton, NOT a topology change: no nodes/edges/weights change, the structural reachability (BFS, cond ignored) is unchanged, the 55-node/21-ending counts + the existing reachability tests stay green. The skeleton is FROZEN at topology; this fixes a RUNTIME bug that makes the FROZEN topology actually playable (the 05.1-06 review didn't exercise runtime cond evaluation). STATE.md + 06-RESEARCH-ui-adapter.md Open Question 3 both flag this as a Phase 6 fix.

Purpose: Unblock SC#3 (player can reach a True ending via the aerobic path: pyr.branch → pyr.pdh → tca → etc → end.true).
Output: 2 fixed conds in pyruvate_branch.json + a runtime-eligibility test confirming the conds evaluate correctly.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@c14/story/interpreter.py
@c14/story/model.py
@c14/state.py
@data/story_glucose/pyruvate_branch.json
@data/story_glucose/tca.json
@tests/test_glucose_reachability.py
@.planning/phases/06-qt-ui-minimal-playable-mvp/06-RESEARCH-ui-adapter.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the 2 pyr.branch conds to the dict-method form (flags.get('host_o2_low'))</name>
  <files>data/story_glucose/pyruvate_branch.json</files>
  <action>
Edit `data/story_glucose/pyruvate_branch.json` `pyr.branch.choices` (lines 12-13):

- Choice 1 (aerobic): `"cond": "not flags.host_o2_low"` → `"cond": "not flags.get('host_o2_low')"`
- Choice 2 (anaerobic): `"cond": "flags.host_o2_low"` → `"cond": "flags.get('host_o2_low')"`

Rationale (cite in a trailing comment OR the commit message): the interpreter's `_cond` (interpreter.py:124-133) exposes `flags` as `state.flags` (a DICT). `flags.host_o2_low` is dict-attribute access → `AttributeError` (dict has no attribute `host_o2_low`) → `_cond`'s `except Exception: return False` → BOTH choices hidden → stuck. `flags.get('host_o2_low')` is dict-method access → returns `None` (unset) → `not None` is `True` (aerobic eligible); `bool(None)` is `False` (anaerobic hidden until a flag-setter exists). This matches `tca.shuffle`'s working cond `visits.get('tca.shuffle', 0) > 5` (pyruvate_branch's sibling node — same dict-method form, same interpreter).

Do NOT change anything else in pyruvate_branch.json (no topology, no weights, no effects, no labels, no other conds). Do NOT change the `effects: {"set": {"anaerobic": true}}` on the anaerobic choice (it sets `anaerobic`, not `host_o2_low` — that's a Phase 7 content question; Phase 6 only fixes the cond SYNTAX so the aerobic path runs). Do NOT touch the other files in `data/story_glucose/`.

Verify the JSON is still valid after the edit (no trailing commas, proper quoting — the single quotes inside the double-quoted JSON string are fine: `"cond": "not flags.get('host_o2_low')"` is valid JSON; the single quotes are part of the string value).
  </action>
  <verify>`python3.6 -c "import json; json.load(open('data/story_glucose/pyruvate_branch.json'))"` exits 0 (valid JSON); `grep -c "flags.get('host_o2_low')" data/story_glucose/pyruvate_branch.json` prints `2` (both conds fixed); `grep -c "flags.host_o2_low" data/story_glucose/pyruvate_branch.json` prints `0` (no broken conds remain).</verify>
  <done>Both pyr.branch conds use flags.get('host_o2_low'); JSON valid; no flags.host_o2_low remains; no other change to the file.</done>
</task>

<task type="auto">
  <name>Task 2: Add a runtime-eligibility test to tests/test_glucose_reachability.py</name>
  <files>tests/test_glucose_reachability.py</files>
  <action>
Extend `tests/test_glucose_reachability.py` with a new `TestPyrBranchRuntimeEligibility(unittest.TestCase)` (or add to an existing class if appropriate — but a new class is cleaner). This test exercises the interpreter's `_cond` at RUNTIME (not structural BFS — the existing reachability tests cover BFS) to confirm the fixed conds evaluate correctly.

Build a `StoryGraph.load("data/story_glucose")` (reuse the existing `_story_dir()` helper pattern in the file if one exists, or resolve `data/story_glucose` relative to the test file CWD-safely — mirror test_engine.py:24-26's `os.path.join(os.path.dirname(__file__), '..', 'data', 'story_glucose')`). Build a `StoryInterpreter()` + a `GameState` with `host_o2_low` UNSET (the default at pyr.branch — no effect sets it before pyr.branch in the aerobic path).

Tests:
1. `test_pyr_branch_aerobic_choice_eligible_when_host_o2_low_unset` — get the `pyr.branch` node; find the aerobic choice (the one with `cond` starting with `not `); `assert interpreter._cond(choice.cond, state) is True` (the player CAN proceed aerobically). Use a fresh `GameState` with empty `flags` (host_o2_low unset → `flags.get('host_o2_low')` is None → `not None` is True).
2. `test_pyr_branch_anaerobic_choice_hidden_when_host_o2_low_unset` — find the anaerobic choice (cond = `flags.get('host_o2_low')`); `assert interpreter._cond(choice.cond, state) is False` (None is falsy → anaerobic hidden until a flag-setter exists).
3. `test_pyr_branch_aerobic_hidden_when_host_o2_low_set_true` — `state.set_flag('host_o2_low', True)`; aerobic `assert interpreter._cond(...) is False`; anaerobic `assert interpreter._cond(...) is True` (the flag flips eligibility — proves the cond is actually reading the flag, not just always-True).
4. `test_pyr_branch_both_choices_not_stuck` — assert at least ONE choice is eligible when host_o2_low is unset (aerobic True) — the player can advance (regression guard: the OLD broken cond made BOTH False → stuck; this test proves the fix).
5. `test_no_broken_dict_attribute_conds_remain` — scan ALL choice conds in the glucose story (`graph.all_nodes()` → iterate `node.choices` → `choice.cond`); assert NO cond contains the broken pattern `flags.<attr>` (a bare attribute access on the flags dict). Use a regex: a cond containing `flags.` followed by a Python identifier NOT immediately followed by `(` (i.e. `flags.host_o2_low` is bad; `flags.get(...)` is good). Implement: `re.search(r"flags\.[a-zA-Z_][a-zA-Z0-9_]*(?!\()", cond)` → if matched, fail with the node_id + cond. This is a GREP-style guard ensuring the fix doesn't regress elsewhere (catches any other node with the same bug). NOTE: `tca.shuffle`'s `visits.get(...)` uses `visits.` not `flags.` so it's fine; scan only `flags.` attribute access. (If other nodes legitimately use `flags.X` they'd need the same fix — this test surfaces them.)

Use `interpreter._cond(cond, state)` directly (it's a method; the underscore is a convention, not enforced — test_interpreter.py already calls it). Build `GameState` via `GameState.new_game("glucose", 42, "intro.preface")` or `GameState()` with `flags={}`.

AVOID: do NOT change the structural reachability tests (TestGlucoseReachability etc. — those test BFS, cond ignored; they must stay green). do NOT add a host_o2_low flag-setter to the story (that's Phase 7 content — the anaerobic branch becomes reachable when Phase 7 adds a mechanism to set host_o2_low; Phase 6 only needs the aerobic path to reach True + the cycle-trap to reach Bad). do NOT touch the other story JSON files.
  </action>
  <verify>`python3.6 -m unittest tests.test_glucose_reachability -v` passes (existing reachability tests green — 55 nodes/21 endings unchanged + 5 new eligibility tests green); `python3.6 -m unittest discover -s tests` passes (full suite green).</verify>
  <done>5 new runtime-eligibility tests green: aerobic eligible when unset, anaerobic hidden when unset, flip when set, not-stuck regression guard, no-broken-dict-attribute-conds scan. Existing reachability tests untouched (structural BFS unchanged).</done>
</task>

<task type="auto">
  <name>Task 3: Swap the start-node TBD_* load targets for the bundled _smoke.pdb placeholder + add the 6-call hero-highlight sequence (Blocker 1 fix b + Warning 3 / SC2 verifiable)</name>
  <files>data/story_glucose/intro.json, tests/test_glucose_reachability.py</files>
  <action>
Edit `data/story_glucose/intro.json` at the 2 START nodes (`intro.preface` + `intro.shell_glucose`) — the ONLY change is the `on_enter` MolAction lists (NO topology change: do NOT touch `text_dramatic`/`text_teaching`/`claim_ids`/`tags`/`choices`/`goto`/`effects`; the 55-node/21-ending structural reachability is UNCHANGED). The problem: the FROZEN on_enter uses `"target": "pdb:TBD_HERO_CARBON"` / `"pdb:TBD_AA_CAST_20"` / `"pdb:TBD_GLUCOSE"` which are NOT real PDB IDs — `molops.load`'s new target-prefix fallback (06-01) would route them to `fetch_pdb("TBD_HERO_CARBON", ...)` → a network fetch that fails → the game crashes on `intro.preface` (the manifest start node) the moment the first load dispatches. Fix (b): swap the TBD_* targets for the BUNDLED `_smoke.pdb` fixture (a 3-atom C1/O1/C2 test fixture at `c14/data/assets/bundled/_smoke.pdb` — NOT a cited PDB, so NO fabricated science; it's a test placeholder, the same fixture `tools/asset_smoke.py` + `tools/molops_smoke.py` use). The `molops.load` bare-filename target path (06-01) routes `_smoke.pdb` → `load_bundled("_smoke.pdb", <object>)` → NO network, NO crash.

**`intro.preface` on_enter** — replace the 2 `pdb:TBD_*` loads with `_smoke.pdb` AND insert the 6-call hero-highlight sequence from 05.4-CONVENTION.md §2.1 (lines 237-243 — the FROZEN sequence 06-01 proves) on `hero_atom` so SC2 ("Starting a glucose game highlights the C14 hero atom") is human-verifiable at Phase 6:
```json
"on_enter": [
  {"op": "hide_all"},
  {"op": "load", "target": "_smoke.pdb", "args": {"object": "hero_atom"}},
  {"op": "set_color", "args": {"name": "hero_cyan", "rgb": [0.0, 0.75, 0.75]}},
  {"op": "show_as", "target": "hero_atom", "args": {"rep": "sticks"}},
  {"op": "color", "target": "hero_atom", "args": {"color": "hero_cyan", "sele": "hero_atom and elem C"}},
  {"op": "show", "target": "hero_atom", "args": {"rep": "spheres", "sele": "hero_atom"}},
  {"op": "set", "target": "hero_atom", "args": {"name": "sphere_scale", "value": 0.3, "sele": "hero_atom"}},
  {"op": "label", "target": "hero_atom", "args": {"sele": "hero_atom", "text": "YOU"}},
  {"op": "load", "target": "_smoke.pdb", "args": {"object": "aa_cast"}},
  {"op": "show_as", "target": "aa_cast", "args": {"rep": "cartoon"}}
]
```
The 6-call sequence: `set_color` defines `hero_cyan` (no `target` key — `MolAction.from_dict` parses `{"op":"set_color","args":{...}}` → `MolAction("set_color", None, {...})` per model.py:51-54); `show_as sticks` + `color hero_cyan on elem C` + `show spheres` + `set sphere_scale 0.3` + `label YOU`. `_smoke.pdb` has 2 carbons (C1/C2) → the OQ-6 HeroResolver (06-06) will prompt "2 carbons, highlight the first?" at runtime (the controller's prompt_fn); the headless smoke (06-14) auto-confirms; the human-verify (06-14) sees the prompt work. This EXERCISES the HeroResolver gate at the start node — a feature, not a bug. The `aa_cast` load keeps `_smoke.pdb` as a placeholder so the later `show_as aa_cast cartoon` (here + at shell_glucose) does not reference a missing object.

**`intro.shell_glucose` on_enter** — swap ONLY the `pdb:TBD_GLUCOSE` target for `_smoke.pdb` (NO hero-highlight here — preface owns the highlight; shell_glucose just must not crash on the glucose load):
```json
"on_enter": [
  {"op": "hide_all"},
  {"op": "load", "target": "_smoke.pdb", "args": {"object": "glucose"}},
  {"op": "show_as", "target": "glucose", "args": {"rep": "sticks"}},
  {"op": "show_as", "target": "aa_cast", "args": {"rep": "cartoon"}}
]
```

Do NOT touch `intro.select` / `fa.stub` / `alc.stub` (their on_enter is `[hide_all]`-only — no load, no crash). Do NOT change any other file in `data/story_glucose/` (the other node on_enter loads — glycolysis/tca/etc — stay as their FROZEN `pdb:XXX` targets; they are NOT the start node + the controller's defensive try/except (06-06) degrades a later real-load failure gracefully rather than crashing). The 06-14 headless smoke + human-verify START at intro.preface; the start node MUST not crash, which this fix guarantees.

AVOID: do NOT use a real PDB ID as the placeholder (no-fabricated-science — `_smoke.pdb` is a test fixture, not a cited structure). do NOT change the topology (nodes/choices/goto/effects) — only `on_enter`. do NOT add the hero-highlight to shell_glucose (preface owns it; keeps the diff minimal). do NOT touch the other story JSON files. do NOT remove the `claim_ids: ["PLACEHOLDER_PHASE7"]` (the TBD text stays — Phase 7 authors the real text + real structures).

ALSO add a `TestStartNodeOnEnterShape(unittest.TestCase)` to `tests/test_glucose_reachability.py` (this test runs AFTER the intro.json edit since Task 3 edits intro.json before adding this test — tasks are sequential within the plan). Build `StoryGraph.load("data/story_glucose")` (reuse the `_story_dir()` helper). Tests:
6. `test_preface_on_enter_loads_bundled_placeholder_not_tbd` — get `intro.preface`; assert NO `on_enter` MolAction has a `target` starting with `"pdb:TBD"` (grep the load targets); assert at least one load has `target == "_smoke.pdb"`.
7. `test_preface_on_enter_has_hero_highlight_sequence` — get `intro.preface`; assert the `on_enter` contains the 6 hero-highlight ops in order: a `set_color` with `args.name == "hero_cyan"`, a `show_as` with `rep == "sticks"`, a `color` with `color == "hero_cyan"`, a `show` with `rep == "spheres"`, a `set` with `name == "sphere_scale"`, a `label` with `text == "YOU"`. Assert the sequence appears AFTER the hero_atom load (the load precedes the highlight).
8. `test_shell_glucose_on_enter_loads_bundled_not_tbd` — get `intro.shell_glucose`; assert no `pdb:TBD` target; assert a load with `target == "_smoke.pdb"` + `args.object == "glucose"`.
9. `test_start_nodes_do_not_reference_real_pdb_fetch` — assert NO start-node (`intro.preface`/`intro.shell_glucose`/`intro.select`) `on_enter` load target starts with `"pdb:"` (the start nodes use bundled placeholders only — no network fetch at game start; SC4 "small/critical bundled so the game starts instantly"). NOTE: `intro.select`/`fa.stub`/`alc.stub` have `[hide_all]`-only on_enter (no load) — they trivially pass.
10. `test_intro_topology_unchanged` — re-run the existing reachability counts (55 nodes / 21 endings) to prove the on_enter edit did NOT change topology (regression guard that the start-node fix is content-only).

Use `node.on_enter` (a list of `MolAction` objects with `.op`/`.target`/`.args` attributes per model.py:44-78). Follow the existing test style (unittest, real graph).
  </action>
  <verify>`python3.6 -c "import json; json.load(open('data/story_glucose/intro.json'))"` exits 0 (valid JSON); `grep -c "pdb:TBD" data/story_glucose/intro.json` prints `0` (no crashing TBD targets remain at the start nodes); `grep -c "_smoke.pdb" data/story_glucose/intro.json` prints `2` (preface hero_atom + aa_cast) + `1` (shell_glucose glucose) = verify the 3 start-node loads use the bundled fixture; `grep -c "hero_cyan" data/story_glucose/intro.json` prints `2` (the set_color + the color op in the preface hero-highlight); `grep -c '"text": "YOU"' data/story_glucose/intro.json` prints `1` (the label op); `python3.6 -m unittest tests.test_glucose_reachability -v` passes (the topology is unchanged → the 55-node/21-ending reachability tests stay green + the 5 eligibility tests from Task 2 + the 5 start-node shape tests here pass).</verify>
  <done>intro.preface + intro.shell_glucose on_enter load `_smoke.pdb` (bundled placeholder, no network, no crash); intro.preface has the 6-call hero-highlight sequence (set_color hero_cyan + show_as sticks + color elem C + show spheres + set sphere_scale + label YOU) so SC2 is human-verifiable; no pdb:TBD targets remain at the start nodes; no start-node load targets start with "pdb:" (no network at game start); JSON valid; topology unchanged (reachability tests green); 5 new start-node shape tests green.</done>
</task>

</tasks>

<verification>
- `python3.6 -c "import json; json.load(open('data/story_glucose/pyruvate_branch.json'))"` — valid JSON.
- `grep -c "flags.get('host_o2_low')" data/story_glucose/pyruvate_branch.json` — 2 (both fixed).
- `grep -c "flags.host_o2_low" data/story_glucose/pyruvate_branch.json` — 0 (no broken conds).
- `python3.6 -c "import json; json.load(open('data/story_glucose/intro.json'))"` — valid JSON.
- `grep -c "pdb:TBD" data/story_glucose/intro.json` — 0 (no crashing TBD targets at the start nodes).
- `grep -c "hero_cyan" data/story_glucose/intro.json` — 2 (the preface hero-highlight set_color + color ops).
- `python3.6 -m unittest tests.test_glucose_reachability -v` — existing reachability green (topology unchanged) + 5 eligibility tests (Task 2) + 5 start-node shape tests (Task 3) green.
- `python3.6 -m unittest discover -s tests` — full suite green.
</verification>

<success_criteria>
- pyr.branch's 2 conds use `flags.get('host_o2_low')` (dict-method form); JSON valid.
- At runtime: aerobic choice eligible when host_o2_low unset (player can proceed to TCA → True ending); anaerobic hidden until a flag-setter exists (Phase 7).
- Structural reachability unchanged (55 nodes/21 endings; existing tests green).
- 5 new runtime-eligibility tests green (incl. a no-broken-dict-attribute-conds regression scan).
- intro.preface + intro.shell_glucose on_enter load the bundled `_smoke.pdb` placeholder (no `pdb:TBD` network fetch, no crash at the start node — Blocker 1 fix b); intro.preface has the 6-call hero-highlight sequence so SC2 is human-verifiable at Phase 6 (no fabricated science — test fixture, not a cited PDB); 5 new start-node shape tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/06-qt-ui-minimal-playable-mvp/06-05-SUMMARY.md`
</output>
