---
phase: 06-qt-ui-minimal-playable-mvp
plan: 14
type: execute
wave: 5
depends_on:
  - 06-01
  - 06-02
  - 06-03
  - 06-04
  - 06-05
  - 06-06
  - 06-07
  - 06-08
  - 06-09
  - 06-10
  - 06-11
  - 06-12
  - 06-13
files_modified:
  - tools/controller_integration_smoke.py
autonomous: false

must_haves:
  truths:
    - "A headless integration smoke wires Controller + real molops + real cmd + a MockView + mock prompt_fn + real view callbacks; starts a glucose game; advances through intro.preface -> ... -> reaches an ending; asserts the hero-highlight dispatches fire + the scene rebuilds + the view captures/restores (SC1-3 mechanism proof, headless)"
    - "A real Windows PyMOL session: the plugin installs via Plugin Manager, the 'RPG: Tale of C' menu appears on restart, the main window opens with character selection / story text / choice panel / cast-help-save-load-achievement controls (SC1, human-verify)"
    - "Starting a glucose game highlights the C14 hero atom (cyan + YOU label) in the loaded structure; advancing shows residue representations at relevant stages (SC2, human-verify)"
    - "The player reaches a True ending AND a Bad ending for glucose via text multiple-choice; a mid-game save + load restores the exact session (story position + RNG state + loaded structures + view) (SC3, human-verify)"
    - "On first play, a one-time bulk-download prompt fetches large structures (progress + cancel + retry + offline fallback locking only affected characters); small/critical bundled so the game starts instantly (SC4, human-verify)"
    - "The achievement board shows unlocks (glucose tried + endings found) and persists across PyMOL sessions; in-game help includes molecule-editing pointers + PyMOL wiki links (SC5, human-verify)"
  artifacts:
    - path: "tools/controller_integration_smoke.py"
      provides: "Headless end-to-end smoke of the full Controller path (real cmd + MockView)"
      contains: "SMOKE_RESULT"
  key_links:
    - from: "tools/controller_integration_smoke.py"
      to: "c14.ui.controller.Controller"
      via: "constructs Controller with real molops/cmd + MockView; advances through the glucose story to an ending"
      pattern: "Controller\\("
---

<objective>
The Phase 6 capstone: (1) a HEADLESS integration smoke proving the full Controller path end-to-end (engine + real molops + real cmd + MockView + mock prompt + real view callbacks) — the SC1-3 mechanism proof that CAN run in WSL; (2) the COMPREHENSIVE human-verify checkpoint in a real Windows PyMOL session covering ALL 5 Success Criteria (install/menu, hero highlight, reach True+Bad endings + save/load round-trip, bulk-download, achievements+help). This is the FIRST human-verify milestone — the moment the game is actually played end-to-end for the first time.

Purpose: Prove the integrated whole works. The headless smoke de-risks the integration (the controller wiring, the molops dispatch, the view capture/restore, the tca.shuffle goto routing, the pyr.branch cond fix) before the human sits down. The human-verify confirms the Qt rendering + the real PyMOL session behavior the WSL agent cannot exercise.
Output: tools/controller_integration_smoke.py (headless, SMOKE_RESULT PASS) + the human-verify sign-off (typed "approved" or issues list).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@c14/ui/controller.py
@c14/pymol_layer/molops.py
@c14/pymol_layer/asset_manager.py
@c14/pymol_layer/edit_ops.py
@c14/pymol_layer/protonation.py
@c14/edit_router.py
@c14/achievements.py
@c14/paths.py
@c14/engine.py
@data/story_glucose/manifest.json
@tools/hero_highlight_smoke.py
@tools/molops_deferred_dispatch_smoke.py
@.planning/phases/06-qt-ui-minimal-playable-mvp/06-RESEARCH-ui-adapter.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tools/controller_integration_smoke.py — headless end-to-end Controller path smoke</name>
  <files>tools/controller_integration_smoke.py</files>
  <action>
Create `tools/controller_integration_smoke.py` (pure `pymol.cmd.*` + `c14` imports; NO Qt — uses a MockView). Mirror the smoke pattern from `tools/hero_highlight_smoke.py` + `tools/molops_smoke.py` (SMOKE_RESULT sentinel, check() helper, FAILS list, `pymol.finish_launching()` first, `sys.path.insert(0, os.getcwd())`, `import c14.paths`). This smoke proves the FULL controller path headlessly (the integration of 06-01 dispatches + 06-03 view injection + 06-04 achievements + 06-05 pyr.branch fix + 06-06 controller/goto + 06-10 bulk-download runner):

Stages:
1. **Setup the real molops stack:** `from pymol import cmd; from c14.pymol_layer.molops import MolOps; from c14.pymol_layer.asset_manager import AssetManager; from c14.pymol_layer.edit_ops import EditOps; from c14.pymol_layer.protonation import ProtonationManager; import c14.protonation_catalog; from c14.edit_router import EditRouter; from c14.ui.controller import Controller; from c14.achievements import AchievementBoard`. `pymol.finish_launching()`. Construct: `assets = AssetManager(cmd); editops = EditOps(cmd); protonation = ProtonationManager(cmd, editops, c14.protonation_catalog, assets); molops = MolOps(cmd, assets, editops, protonation); edit_router = EditRouter.load(str(c14.paths.data_path("data","edits.json")))` (check c14/edit_router.py for the exact load API — adjust the call to match). `achievement_board = AchievementBoard(path=str(c14.paths.user_data_path("_smoke_achievements.json")))` (temp path; delete after). `view_provider = lambda: list(cmd.get_view())`; `view_applier = lambda v: cmd.set_view(v)`. `prompt_fn = lambda msg: True` (mock — auto-confirm; the placeholder content has known selectors so the gate is a no-op, but wire it). `count_fn = lambda sele: cmd.count_atoms(sele)`.
2. **MockView:** a tiny class `class MockView: def __init__(self): self.turns=[]; def render_turn(self, turn): self.turns.append(turn)`.
3. **Construct the Controller** with `story_dir = os.path.join(os.getcwd(), "data", "story_glucose")` (the repo-root story; the smoke runs cwd=repo-root per run_headless.sh). `controller = Controller(story_dir, molops, cmd, edit_router, view=mock_view, prompt_fn=prompt_fn, count_fn=count_fn, achievement_board=achievement_board, view_provider=view_provider, view_applier=view_applier)`.
4. **Start a glucose game:** `turn = controller.start_game("glucose", seed=42)`; assert `turn.node.id == "intro.preface"` (the manifest start); assert `mock_view.turns[-1].node.id == "intro.preface"` (rendered); assert `achievement_board.data["characters_tried"] == ["glucose"]` + `first_game` unlocked. Assert the start-node hero-highlight dispatches fired (06-05 fix: intro.preface on_enter now loads the bundled `_smoke.pdb` as hero_atom + runs the 6-call hero-highlight sequence) — wrap the molops in a recording proxy OR inspect the cmd state after: assert `cmd.count_atoms("hero_atom") > 0` (the `_smoke.pdb` loaded via the 06-01 load-branch bare-filename target path — NO KeyError, NO network); assert a named color `hero_cyan` exists (`cmd.get_color_indices()` includes "hero_cyan" — the set_color dispatched); assert `cmd.count_atoms('hero_atom and label') >= 1` (the "YOU" label dispatched). This proves the FULL start-node path: the 06-05 skeleton swap + the 06-01 load-branch target-prefix fallback + the 4 new dispatches (set_color/label/set) + the controller's _dispatch_molaction forwarding — the integration the headless smoke exists to prove (NOT just "the controller forwarded something").
5. **Advance to pyr.branch + verify the cond fix (06-05):** walk the choices (Continue buttons) from intro.preface through glycolysis to `pyr.branch`; at pyr.branch assert the aerobic choice is eligible (`controller._engine.choice_cond_met(aerobic_choice) is True` — the 06-05 fix) + the player can proceed. Take the aerobic choice (`controller.choose(<aerobic index>)` or `controller.take_choice`); assert advanced to `pyr.pdh`.
6. **Advance into TCA + verify tca.shuffle goto routing (06-06 Blocker B):** walk to `tca.shuffle`; assert `controller.is_mixed_weighted_node(tca_shuffle_node) is True`; spin the wheel once (`controller.choose(0)` — RNG picks turn1/turn2); assert advanced to `tca.co2_turn1` or `tca.co2_turn2`; return to tca.shuffle via "The cycle turns again"; repeat 6+ times to bump the visit count; assert `controller._engine.choice_cond_met(cycle_trap_choice) is True` after 6 visits; `controller.take_choice(cycle_trap_choice)` -> `bad.cycle_trap_host_death`; assert `turn.node.is_ending == "bad"` (a Bad ending reached — SC3 Bad path) + `achievement_board` has `bad_ending` unlocked.
7. **Reach a True ending (the aerobic path) — PRECISE WALK ALGORITHM (Warning 5 fix):** in a fresh controller (new seed), walk from intro.preface to end.true using a BFS-distance-guided choice selection (so the walk is deterministic + assertable, NOT "many Continue clicks"). Algorithm (implement in the smoke as a helper):
   a. **Precompute `dist[id]`** = BFS shortest-path distance (cond-IGNORED — structural reachability) from every node to `end.true` over the reverse choice edges (`choice.goto -> node.id`). `dist["end.true"] = 0`; BFS backward from end.true. Nodes with no path to end.true (edit.prompt, bad.cycle_trap_host_death, end.normal.co2, end.good.*, anaer.*) get `dist = infinity` (excluded).
   b. **Walk loop** — `path = []; turn = <current TurnResult>`; while `turn.node.is_ending is None`:
      - `path.append(turn.node.id)`.
      - If `turn.node.id == "tca.shuffle"` (the mixed weighted+non-weighted node): call `turn = controller.choose(0)` (the RNG spins among the 2 weighted choices → tca.co2_turn1 or tca.co2_turn2; both have finite dist). Record the spin result. Do NOT take edit:offer or cycle-trap (those have dist=infinity / lead away from end.true).
      - Else (pure non-weighted node): compute the eligible choices = `[c for c in turn.node.choices if c.cond is None or controller._engine.choice_cond_met(c)]`. Among eligible, pick the choice `c*` with the MINIMUM `dist[c*.goto]` (break ties by list order — the first "Continue" wins over an "Examine" mc:observe that gotos the same next node since Continue is listed first). Find `index` = position of `c*` among the eligible list (the engine's `choose(index)` picks among the eligible list for non-weighted). Call `turn = controller.choose(index)`.
      - Assert `turn.node.id` advanced (changed from the previous node — no self-loop except the tca.shuffle spin which goes to co2_turn then comes back via "Continue onward"; the dist-decrease guarantee means the walk makes progress toward end.true each step).
   c. **Assert the ending:** after the loop, `assert turn.node.id == "end.true"` + `assert turn.node.is_ending == "true"` (SC3 True path). Assert `achievement_board` has `true_ending` unlocked.
   d. **Assert the recorded path** matches the expected aerobic milestone sequence (assert these node ids appear IN ORDER in `path`, allowing the RNG to pick either tca.co2_turn1 OR tca.co2_turn2 at the spin): `["intro.preface", "intro.select", "intro.shell_glucose", "gly.start", ..., "pyr.branch", "pyr.pdh", "tca.entry", "tca.citrate_synthase", "tca.aconitase", "tca.shuffle", <"tca.co2_turn1"|"tca.co2_turn2">, "tca.isocitrate_dh", "tca.akg_dh", "tca.succinyl_coa_synthetase", "tca.fumarase", "tca.malate_dh", "tca.divert_to_good", "etc.entry", "etc.complex_i", "etc.complex_ii", "etc.complex_iii", "etc.complex_iv", "etc.atp_synthase"]` then `end.true` (the final turn). The `"..."` between gly.start and pyr.branch is the glycolysis linear chain — assert `path` contains `pyr.branch` after `gly.start` (don't hardcode every glycolysis intermediate; assert the milestones in order with `path.index(x)` monotonic). Verify the key branch decisions: at `pyr.branch` the algorithm picked the aerobic choice (goto `pyr.pdh`, cond `not flags.get('host_o2_low')` eligible — the 06-05 fix); at `etc.entry` it picked "Follow the soul into the ETC" (goto `etc.complex_i`, dist finite) NOT "Let go as CO2" (goto `end.normal.co2`, dist=infinity — the normal ending); at `tca.divert_to_good` it picked "Follow the soul to the ETC" (goto `etc.entry`) NOT the good-ending diversions.
   e. Record the full `path` list + print `SMOKE_TRUE_PATH: {0}`.format(path) for diagnostics.
   NOTE: the tca.shuffle spin is the ONLY RNG-dependent step on the True path; the dist-guided algorithm deterministically exits via "Continue onward" at tca.co2_turn (dist to isocitrate_dh < dist back to tca.shuffle), so the walk reaches end.true in a bounded number of steps (no cycle-trap — the cycle-trap cond `visits.get('tca.shuffle', 0) > 5` is only 1 spin here, so it stays hidden). If the walk somehow loops > 20 steps without reaching end.true, fail the stage (a bug in the algorithm or the skeleton).
8. **Save/load round-trip with view (06-03):** mid-game (at a non-ending node), `import tempfile; path = tempfile.mktemp(suffix=".json"); controller.save(path)`; assert the file exists + `controller._engine.state.view is not None` (the view_provider captured 18 floats); `controller2 = Controller(...same args...); controller2.load(path)`; assert `controller2._engine.state.current_node == controller._engine.state.current_node` + `controller2._engine.state.seed == controller._engine.state.seed` + the rng_state matches (the next draw is identical — mirror test_engine.py's rng-state-survives-save-load) + the view was applied (the view_applier was called — record via a mock applier OR assert cmd.get_view() matches the saved view after load). Assert the scene rebuilt (on_enter replayed — the loaded objects exist in cmd).
9. **Bulk-download runner (06-10):** `from c14.ui.bulk_download import missing_large_pdbs, run_bulk_download, characters_to_lock`; assert `missing_large_pdbs(cmd)` returns [] for the Phase 6 placeholder cast (no real download enzymes — the prompt won't fire); OR with a MockCmd simulating a missing file, assert the runner returns the right {completed, failed, canceled}. (Keep this light — the full bulk-download is human-verify.)
10. **Final sentinel:** `print("SMOKE_RESULT: {0}".format("FAIL" if FAILS else "PASS"))`.

Clean up the temp achievements file + any loaded cmd objects (`cmd.delete("hero_atom")` etc.) at the end.

  AVOID: do NOT import Qt (this is a headless cmd.* smoke — the controller is Qt-free; the MockView replaces the MainWindow). do NOT use real PDB IDs beyond the bundled smoke fixtures (the start node loads `_smoke.pdb` per the 06-05 fix — NO `pdb:TBD_*` network fetch at game start; mid-game nodes still use real `pdb:XXX` targets which the controller's defensive try/except (06-06) degrades gracefully if offline — do NOT assert those mid-game loads succeed headlessly without network). DO assert the start-node hero-highlight post-conditions (06-05 added the 6-call sequence to intro.preface — `cmd.count_atoms("hero_atom") > 0` + `hero_cyan` color + `YOU` label are all assertable headlessly against the bundled fixture; this is the SC2 mechanism proof the headless smoke exists to deliver, no longer deferred to Phase 7).
  </action>
  <verify>`python3.6 -m py_compile tools/controller_integration_smoke.py` exits 0; `bash tools/run_headless.sh tools/controller_integration_smoke.py` prints `PASSED` (the harness greps `^SMOKE_RESULT: PASS`); the output shows all stage checks PASS (start, pyr.branch cond fix, tca.shuffle goto routing + Bad ending, True ending path via the BFS-distance-guided walk asserting `turn.node.id == "end.true"` + the recorded `SMOKE_TRUE_PATH` ends at end.true, save/load round-trip with view, bulk-download runner).</verify>
  <done>Headless integration smoke PASS: the full Controller path (real cmd + MockView) starts glucose, advances past pyr.branch (06-05 fix), routes tca.shuffle via goto (06-06 Blocker B fix) to reach a Bad ending, reaches a True ending via the deterministic BFS-distance-guided walk (asserts end.true + the milestone path), save/load round-trips with view capture/restore (06-03), the achievement board unlocks (06-04), the bulk-download runner is callable (06-10). SC1-3 mechanism proven headlessly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The complete Phase 6 Qt UI MVP: plugin entry point + menu registration (06-07), main window + story/choice panels (06-08), edit dialog (06-09), bulk-download (06-10), save/load dialogs (06-11), achievements dialog (06-12), help dialog (06-13), wired over the Qt-free controller (06-06) + the 4 deferred molops dispatches (06-01) + the view-matrix injection (06-03) + the achievement board (06-04) + the pyr.branch cond fix (06-05) + the user_data_path resolver (06-02). The headless integration smoke (Task 1) proved the controller path end-to-end. This checkpoint verifies the Qt rendering + the real Windows PyMOL session behavior the WSL agent cannot exercise.
  </what-built>
  <how-to-verify>
Run in a real Windows PyMOL 2.5.0 session (the WSL agent CANNOT do this — AGENTS.md "Qt/GUI tests remain human-verify checkpoints"). Steps:

**SC1 — install + menu + main window:**
1. Build the zip: in WSL, `bash tools/build_plugin_zip.sh` -> produces `dist/c14-<version>.zip`.
2. In Windows PyMOL: Plugin -> Plugin Manager -> Install New Plugin -> pick `dist/c14-<version>.zip` -> confirm "Successfully installed".
3. Restart PyMOL (close + reopen).
4. Confirm a "RPG: Tale of C" item appears under the Plugin menu. (If not: check the PyMOL console for "Unable to initialize plugin 'c14'" — the selfcheck may have failed; report the console message.)
5. Click "RPG: Tale of C" -> the main window opens with: a toolbar (New Game / Save / Load / Achievements / Help), a story panel (text), a choice panel (buttons), a status bar. SC1 PASS.

**SC2 — hero highlight + scene templates:**
6. Click "New Game" -> a start dialog (Glucose enabled; Fatty acid/Alcohol disabled). Pick Glucose, leave seed empty (random) or enter a seed, OK.
7. The story advances to intro.preface. Confirm: the C14 hero atom is highlighted (cyan + "YOU" label) in the loaded 3D structure (per the 5.4 convention; the 06-05 fix swapped the start-node TBD_* targets for the bundled `_smoke.pdb` placeholder + added the 6-call hero-highlight sequence — so the highlight IS visible at Phase 6, no longer deferred to Phase 7). NOTE: `_smoke.pdb` has 2 carbons, so the OQ-6 HeroResolver (06-06) prompts "This structure has 2 carbons. The hero (C14) is ambiguous. Highlight the first carbon as the hero?" — click Yes; the first carbon is highlighted cyan + labeled YOU (ball-and-stick). Confirm the story text + teaching text both show (stacked, not tabbed).
8. Click Continue choices to advance. At relevant stages (substrate-traversal / active-site-reveal nodes), confirm residue representations appear (the 5.4 scene templates). SC2 PASS. (Acknowledge: mid-game node on_enter still use real `pdb:XXX` targets that load via network OR degrade gracefully via the controller's defensive try/except (06-06 Blocker 1 fix c) if offline — the start-node highlight is bundled + verified here; the per-node scene content is Phase 7.)

**SC3 — reach True + Bad endings + save/load round-trip:**
9. Play through to pyr.branch -> proceed aerobically -> TCA -> tca.shuffle. At tca.shuffle confirm the UI shows a "Spin the wheel" button + an "Edit aconitase" button + (after 6 visits) a "cycle has spun too long" button.
10. Spin 6+ times (return via "The cycle turns again"), then click "cycle has spun too long" -> reach `bad.cycle_trap_host_death` (a Bad ending). Confirm the achievement "Lost Connection" unlocks. SC3 Bad PASS.
11. New game -> play the aerobic path to `end.true` (spin the wheel, follow Continue to ETC/ATP). Reach the True ending. Confirm "Soul Harvested" unlocks. SC3 True PASS.
12. Mid-game (at a non-ending node), click Save -> pick a path -> save. Note the current node + the camera view. Click Load -> pick the save -> confirm: the story position restores (same node), the RNG state restores (the next spin matches a fresh continuation), the loaded structures restore (scene rebuilds), AND the camera view restores to where you saved it (the 06-03 view injection). SC3 save/load PASS.

**SC4 — bulk-download:**
13. (If the Phase 6 cast has only PLACEHOLDER download enzymes, the bulk-download prompt may NOT fire — `missing_large_pdbs` returns []. To verify the mechanism: temporarily add a real-looking download enzyme to a temp cast.json OR confirm the runner's logic via the headless smoke Task 1 stage 9.) Confirm: small/critical structures are bundled (glucose starts instantly with no download); IF a download is triggered, confirm the progress bar advances per-file (not per-byte — this is correct, not a bug), Cancel stops after the current file, Retry re-runs skipping already-downloaded, and a simulated offline failure locks only the affected character (glucose stays playable). SC4 PASS (mechanism verified; the real large-PDB list is Phase 7).

**SC5 — achievements + help:**
14. Click Achievements -> the board shows: "First Steps" + "Glucose" unlocked (glucose tried), the endings found (True/Bad with node ids + dates), characters tried (glucose), branches discovered count. Confirm it's a collection (no scores/ranks).
15. Close PyMOL + reopen -> click Achievements again -> confirm the unlocks from the prior session PERSIST (the achievements.json at ~/.pymol/c14-tale-of-c/ or %APPDATA%/pymol/c14-tale-of-c/ survived the restart). SC5 ACH-02 PASS.
16. Click Help -> the help dialog shows the 4 editing pointers (Point mutation / Substrate edit / Protonation change / Restore safety net) + 3 clickable PyMOL wiki links. Click a wiki link -> it opens in your browser (Alter->Iterate, H_Add, Fetch). SC5 DOC-03 PASS.

Report: type "approved" if all 5 SCs pass, OR describe which SC(s) failed + what you saw (the agent will diagnose + create gap-closure plans).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (which SC failed + what you observed)</resume-signal>
</task>

</tasks>

<verification>
- `python3.6 -m py_compile tools/controller_integration_smoke.py` — syntax.
- `bash tools/run_headless.sh tools/controller_integration_smoke.py` — `PASSED` (the headless SC1-3 mechanism proof: controller path integrates 06-01..06-06 + 06-10; reaches True + Bad endings; save/load round-trips with view).
- `python3.6 -m unittest discover -s tests` — full suite green (no regression from the integration).
- `python3.6 tools/check_imports.py` — AST gate clean (the smoke is in tools/ — not gate-scanned; but controller.py + engine.py + state.py stay clean).
- Human-verify: all 5 SCs pass in a real Windows PyMOL session (the checkpoint above).
</verification>

<success_criteria>
- Headless integration smoke PASS (the controller path end-to-end: start, pyr.branch fix, tca.shuffle goto routing, True + Bad endings, save/load with view, achievements, bulk-download runner).
- Human-verify: SC1 (install/menu/window), SC2 (hero highlight + scenes), SC3 (True + Bad endings + save/load round-trip with view), SC4 (bulk-download progress/cancel/retry/offline-lock), SC5 (achievements persist + help with wiki links) — all 5 PASS in a real Windows PyMOL session.
- Phase 6 COMPLETE: the FIRST human-verify milestone — the game is playable end-to-end for the first time.
</success_criteria>

<output>
After completion, create `.planning/phases/06-qt-ui-minimal-playable-mvp/06-14-SUMMARY.md` (record the human-verify verdict + any issues).
</output>
