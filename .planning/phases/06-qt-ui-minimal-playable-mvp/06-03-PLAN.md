---
phase: 06-qt-ui-minimal-playable-mvp
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - c14/state.py
  - c14/engine.py
  - tests/test_state.py
  - tests/test_engine.py
autonomous: true

must_haves:
  truths:
    - "GameState carries a view field (list of 18 floats or None) serialized in to_dict + deserialized in from_dict with default None for old-save backward-compat"
    - "GameEngine.save(path) captures the view via an injected view_provider callable (default None = no capture; Phase 2 tests stay green)"
    - "GameEngine.load(path) restores the view via an injected view_applier callable AFTER the on_enter replay (so a node's zoom MolAction doesn't override the saved view)"
    - "engine.py stays stdlib-only (NO pymol import) — the AST gate stays clean; view capture/restore go through injected callbacks (mirrors molaction_sink injection)"
    - "Old Phase-2 saves (no view field) load without crashing (from_dict .get default None)"
  artifacts:
    - path: "c14/state.py"
      provides: "view field on GameState (3 sites: __init__, to_dict, from_dict)"
      contains: "self.view"
    - path: "c14/engine.py"
      provides: "view_provider/view_applier constructor params + save capture + load restore"
      contains: "view_provider"
    - path: "tests/test_state.py"
      provides: "view field round-trip + old-save backward-compat tests"
      contains: "view"
    - path: "tests/test_engine.py"
      provides: "save captures via mock provider + load applies AFTER replay via mock applier tests"
      contains: "view_applier"
  key_links:
    - from: "c14/engine.py:save"
      to: "view_provider (injected callable)"
      via: "self.state.view = list(self._view_provider()) before SaveStore.save"
      pattern: "_view_provider"
    - from: "c14/engine.py:load"
      to: "view_applier (injected callable)"
      via: "self._view_applier(self.state.view) AFTER _enter replay"
      pattern: "_view_applier"
---

<objective>
Close the Phase 2 view-matrix GAP that blocks SC#3 ("load restores the exact session ... + view"). `GameState` (state.py:46-64) has NO view field; PyMOL exposes the view as 18 floats via `cmd.get_view()` (viewing.py:702) + restores via `cmd.set_view(view)` (viewing.py:705). Add a `view=None` field to `GameState` + inject `view_provider`/`view_applier` callbacks into `GameEngine` (mirrors the `molaction_sink` injection — keeps engine.py gate-clean + WSL-testable). The controller (06-06) injects `lambda: list(cmd.get_view())` / `lambda v: cmd.set_view(v)`.

Purpose: SC#3 demands "load restores the exact session (story position + RNG state + loaded structures + view)". Phase 2 restored story+RNG+scene (via on_enter replay) but NOT the camera. This plan adds the view capture (pre-save) + restore (post-load-replay).
Output: view field on GameState (backward-compatible) + view_provider/view_applier on GameEngine (default None = no-op, Phase 2 tests stay green) + unit tests with mock callbacks.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@c14/state.py
@c14/engine.py
@c14/persist.py
@tests/test_state.py
@tests/test_engine.py
@.planning/phases/06-qt-ui-minimal-playable-mvp/06-RESEARCH-persistence-achievements.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add view=None field to GameState (3 sites) — backward-compatible</name>
  <files>c14/state.py</files>
  <action>
Edit `c14/state.py` `GameState` to add a `view` field (list of 18 floats or None). Three sites:

1. `__init__` (state.py:46-50): add `view=None` as a new keyword param (place it AFTER `ending_tier=None` and BEFORE `version=1` to keep the existing positional order stable, OR after `version` — but `version` has a default so order doesn't bind callers; safest: add `view=None` right after `ending_tier=None`). Add `self.view = view` in the body. Update the docstring "Attributes:" block (state.py:30-43) to document `view: list of 18 floats from cmd.get_view() or None`.
2. `to_dict` (state.py:66-90): add `"view": self.view,` to the returned dict (place it after `"ending_tier": self.ending_tier,` so the key order stays stable + diff-friendly). Update the docstring key-order comment (state.py:71-73) to list `view`.
3. `from_dict` (state.py:92-114): add `view=d.get("view"),` to the `cls(...)` call (default None via `.get` — old Phase-2 saves without a view field load without crashing).

Do NOT change `new_game` (state.py:116-132) — a new game starts with `view=None` (the default); the view is captured only on save. Do NOT bump `version` (the `.get` default handles forward-compat; 06-RESEARCH-persistence-achievements.md Open Question: ".get-with-default is enough").

AVOID: do NOT import pymol in state.py (it's gate-scanned). do NOT store the view as a numpy array or 4x4 matrix (06-RESEARCH-persistence-achievements.md Anti-Pattern: store as a plain JSON list of 18 floats; cmd.set_view accepts a sequence). do NOT change the existing field order in `to_dict` (diff-stability — add `view` at the end of the dict literal, before the closing brace, AFTER `ending_tier`).
  </action>
  <verify>`python3.6 -m py_compile c14/state.py` exits 0; `python3.6 -c "from c14.state import GameState; s=GameState(); print(s.view)"` prints `None`; `python3.6 -c "from c14.state import GameState; s=GameState(view=[0.0]*18); d=s.to_dict(); print('view' in d, d['view'] is not None); s2=GameState.from_dict(d); print(s2.view == [0.0]*18)"` prints `True True True`; `python3.6 -c "from c14.state import GameState; s2=GameState.from_dict({'seed':0}) ; print(s2.view)"` prints `None` (old-save backward-compat).</verify>
  <done>view field present in __init__/to_dict/from_dict; default None; round-trips; old saves (no view key) load with view=None.</done>
</task>

<task type="auto">
  <name>Task 2: Add view_provider/view_applier injection to GameEngine (save captures, load restores AFTER replay)</name>
  <files>c14/engine.py</files>
  <action>
Edit `c14/engine.py` `GameEngine`:

1. `__init__` (engine.py:83-90): add `view_provider=None, view_applier=None` as new keyword params AFTER `edit_router=None`. Store `self._view_provider = view_provider` + `self._view_applier = view_applier`. Update the docstring to document: `view_provider` is a callable `() -> list[18 floats]` (the controller injects `lambda: list(cmd.get_view())`); `view_applier` is a callable `(list[18 floats]) -> None` (the controller injects `lambda v: cmd.set_view(v)`); both default None = no view capture/restore (keeps Phase 2 tests green + the engine WSL-testable with mock callbacks).
2. `save` (engine.py:196-201): AFTER `self.state.rng_state = self.rng.get_state()` and BEFORE `SaveStore.save(self.state, path)`, add:
   ```python
   if self._view_provider is not None:
       try:
           self.state.view = list(self._view_provider())   # cmd.get_view() -> 18 floats
       except Exception:
           self.state.view = None                           # don't block save on view capture
   ```
   (the `try/except` ensures a view-capture failure doesn't block the save — 06-RESEARCH-persistence-achievements.md Pattern 4).
3. `load` (engine.py:203-213): AFTER `result = self._enter(self.state.current_node, record_visit=False)` (the on_enter replay that rebuilds the scene) and BEFORE `return result`, add:
   ```python
   if self._view_applier is not None and self.state.view is not None:
       try:
           self._view_applier(self.state.view)              # cmd.set_view(18 floats) -- AFTER replay
       except Exception:
           pass                                             # don't block load on view restore
   ```
   CRITICAL: the `set_view` MUST come AFTER `_enter` (the on_enter replay may include a `zoom` MolAction; the saved view overrides it — this is the desired "saved view wins" behavior, 06-RESEARCH-persistence-achievements.md Pattern 4 + Open Question 5).

AVOID: do NOT import pymol in engine.py (gate-scanned; the callbacks are injected, mirroring molaction_sink — 06-RESEARCH-persistence-achievements.md Pitfall 6). do NOT capture the view if `view_provider is None` (Phase 2 tests construct GameEngine without it — must stay no-op). do NOT apply the view if `view_applier is None` OR `state.view is None` (old saves). do NOT change `start`/`choose`/`apply_player_edit`/`_enter` (the view is only captured on save + restored on load).
  </action>
  <verify>`python3.6 -m py_compile c14/engine.py` exits 0; `python3.6 tools/check_imports.py` exits 0 (engine.py stays stdlib-only); `python3.6 -c "from c14.engine import GameEngine; e=GameEngine(None); print(e._view_provider, e._view_applier)"` prints `None None` (backward-compatible defaults).</verify>
  <done>view_provider/view_applier in __init__ (default None); save captures via provider (no-op if None); load applies via applier AFTER replay (no-op if None or view None); engine.py gate-clean.</done>
</task>

<task type="auto">
  <name>Task 3: Add unit tests for the view field + the view_provider/view_applier injection</name>
  <files>tests/test_state.py, tests/test_engine.py</files>
  <action>
Extend `tests/test_state.py` with a `TestGameStateView(unittest.TestCase)`:
1. `test_view_defaults_none` — `GameState().view is None`.
2. `test_view_round_trips` — `GameState(view=[1.0]*18).to_dict()["view"] == [1.0]*18`; `GameState.from_dict(GameState(view=[1.0]*18).to_dict()).view == [1.0]*18`.
3. `test_old_save_without_view_loads_none` — `GameState.from_dict({"seed":0,"character":"glucose","current_node":"x"}).view is None` (old Phase-2 save shape, no view key → default None, no crash).
4. `test_new_game_view_none` — `GameState.new_game("glucose", 42, "intro.start").view is None`.

Extend `tests/test_engine.py` with a `TestEngineViewCallbacks(unittest.TestCase)` (reuse the existing `_story_dir()` helper at test_engine.py:24-26 + the real `data/story` graph — the toy 2-node story; construct GameEngine with a list sink):
5. `test_save_captures_view_when_provider_injected` — `captured = []; provider = lambda: (captured.append('called') or [0.5]*18); eng = GameEngine(StoryGraph.load(self._story_dir()), molaction_sink=list.append, view_provider=provider); eng.start('glucose', 42); eng.save(tmp_path); assert eng.state.view == [0.5]*18; assert captured == ['called']`.
6. `test_save_no_capture_when_provider_none` — same but `view_provider=None`; `eng.state.view is None` after save (Phase 2 behavior preserved).
7. `test_save_view_capture_failure_does_not_block_save` — `provider = lambda: (_ for _ in ()).throw(RuntimeError('no view')); eng.save(tmp_path)` succeeds (the try/except swallows); `eng.state.view is None`.
8. `test_load_applies_view_after_replay` — use a mock applier recording the call order: `applied = []; sink = []; applier = lambda v: applied.append(('set_view', v)); eng = GameEngine(graph, molaction_sink=sink.append, view_applier=applier); eng.start(...); eng.state.view = [0.7]*18; eng.save(path); eng2 = GameEngine(graph, molaction_sink=sink.append, view_applier=applier); eng2.load(path); assert applied[-1] == ('set_view', [0.7]*18)` (the view is applied AFTER the on_enter replay dispatched to the sink — assert the sink received the on_enter actions BEFORE the applier fired: `len(sink) > 0` at apply time; to assert ordering, record a sequence: the sink appends `('molaction', action)` and the applier appends `('set_view', v)`; assert the LAST set_view comes after all molactions — use a single shared log list).
9. `test_load_no_apply_when_applier_none` — `view_applier=None`; load succeeds; the scene replays (sink has actions); no view applied (no exception).
10. `test_load_no_apply_when_view_none` — save without a provider (view stays None); load with an applier; the applier is NOT called (guard `state.view is not None`).
11. `test_load_view_apply_failure_does_not_block_load` — `applier = lambda v: (_ for _ in ()).throw(RuntimeError('bad view')); eng2.load(path)` succeeds (try/except swallows); the TurnResult is returned.

Use `tempfile.mkdtemp()` for save paths. Follow the existing test_engine.py style (unittest, real data/story graph, list sink as the mock molaction_sink — test_engine.py:64).

AVOID: do NOT import pymol in the tests (use lambdas as the mock provider/applier). do NOT break the existing TestGameEngine classes (additive). do NOT test the real cmd.get_view/set_view (that's the headless integration smoke in 06-14).
  </action>
  <verify>`python3.6 -m unittest tests.test_state tests.test_engine -v` passes (existing + 11 new tests green); `python3.6 -m unittest discover -s tests` passes (full suite green — the view injection is backward-compatible).</verify>
  <done>11 new tests green: view field round-trip + old-save backward-compat + save captures via mock provider + load applies AFTER replay via mock applier + failure-tolerant + no-op-when-None guards. Existing tests untouched.</done>
</task>

</tasks>

<verification>
- `python3.6 -m py_compile c14/state.py c14/engine.py tests/test_state.py tests/test_engine.py` — syntax.
- `python3.6 tools/check_imports.py` — AST gate clean (state.py + engine.py stay stdlib-only; no pymol import).
- `python3.6 -m unittest tests.test_state tests.test_engine -v` — all tests green (existing + 11 new).
- `python3.6 -m unittest discover -s tests` — full suite green (backward-compatible: Phase 2 tests construct GameEngine without view callbacks + pass).
</verification>

<success_criteria>
- GameState.view field (default None; round-trips; old saves load with None).
- GameEngine view_provider/view_applier (default None = no-op; save captures; load applies AFTER replay; failure-tolerant).
- engine.py + state.py gate-clean (no pymol import).
- 11 unit tests green; full suite green (no Phase 2 regression).
</success_criteria>

<output>
After completion, create `.planning/phases/06-qt-ui-minimal-playable-mvp/06-03-SUMMARY.md`
</output>
