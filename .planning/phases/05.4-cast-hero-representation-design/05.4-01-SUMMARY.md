---
phase: 05.4-cast-hero-representation-design
plan: 01
subsystem: design
tags: [cast-hero-representation, hero-highlight, scene-templates, cast-reveal, convention, pymol-representation, op-set-color, op-label, op-set, molops, soul-jump, anti-confusion, okabe-ito, colorblind-safe]

# Dependency graph
requires:
  - phase: 05.1-story-graph-design-glucose-skeleton-integration-contracts
    provides: the FROZEN 55-node glucose skeleton (all scene templates map every node id) + the 14 edit-allowed-node contract (8 PDB-bearing disease-mutant + tca.citrate_synthase edit:structural + tca.shuffle shared + 6 narrative-only) + the 4 ending tiers (1T+3G+2N+14B) + the soul-jump metamorphosis reframing (PROJECT.md row 107) + the HIGH-STAKES carbon-fate taxonomy (row 109)
  - phase: 05.3-wt-aligned-structure-load-convention
    provides: the 5.3 composition boundary (5.3 OWNS the AFTER = WT-aligned restoration reveal at branch-node on_enter; 5.4 OWNS the BEFORE = mutant cast-reveal at edit-allowed on_enter) + the <mutant_color>/<wt_color> slots 5.3 left open (05.3-CONVENTION.md:318-319) that 5.4 fills with red/green + the op-deferral-to-Phase-6 precedent (05.3 §6/§8 Q3 — 5.4 mirrors for set_color/label/set) + the anti-confusion NOTE pattern (05.3 §5)
  - phase: 03-pymol-cmd-layer
    provides: the MolOps per-action dispatch pattern (hide_all/show/show_as/select_focus/zoom/color/load/delete + edit/restore/protonate) that the 3 new ops set_color/label/set extend as peer primitives + the source-citation convention + the 3-tier testability pattern (unit tests = mapping, headless smoke = real cmd.*)
provides:
  - 05.4-CONVENTION.md — the SC1+SC2+SC3 design artifact (the Cast & Hero Representation convention spec: 751 lines, 10 H2 sections §1-§8 + SC checklist) consumed by Phases 6/7/9/12 WITHOUT re-designing it
  - The op-granularity disagreement RESOLUTION (ADOPT 3 minimal peer-primitive ops set_color/label/set + SHORT sequences, NOT the composite op="hero_highlight" super-op; the rejected super-op documented) — mirrors how 5.3 resolved its naming disagreement by adopting the API researcher's recommendation
  - The shared MolAction op vocabulary (8 existing + 3 new peer primitives) + the shared named-color palette (hero_gold/gray/green/red/orange/gray80) consistent across all 3 SCs
  - The hero-highlight convention (SC1): hybrid selection rule (hero_selector per-asset + single-C auto-derive + multi-C-no-selector FAIL LOUDLY) + 3 structural cases (hero-alone/hero-in-substrate/hero-at-enzyme-cast w/ 3a co-load 3b suspend 3c hybrid) + the 6-call semantic sequence + persistence as pure function of on_enter + the CO2-shed soul-transfer climax + the MANDATORY anti-confusion note
  - The scene-template library (SC2): 6 scene TYPES mapping all 55 skeleton nodes as MolAction sequences (NOT cmd.scene keys — save/load-safe via on_enter replay) with per-type post-conditions + 4 ending tier-templates
  - The cast-reveal convention (SC3): full-then-zoom + the cast-reveal sequence (gray cartoon → zoom active site → green catalytic sticks → red mutant SPHERES (rep redundancy=colorblind-safe) → orange cofactor → nameplate labels) + the mutant→WT visual arc composition with 5.3 (5.4 BEFORE red + 5.3 AFTER green, filling <mutant_color>/<wt_color>)
  - The 11 open questions (OQ-1 to OQ-11) surfaced for the Plan 04 human design-review checkpoint
affects: [05.4-02-headless-prototype, 05.4-03-headless-prototype, 05.4-04-human-review-checkpoint, 06-qt-ui-mvp, 07-content-authoring, 09-full-cast, 12-ending-cutscene]

# Tech tracking
tech-stack:
  added: []  # pure design writing — NO code, NO new libraries
  patterns:
    - "op=\"set_color\" / op=\"label\" / op=\"set\" MolAction shapes: 3 NEW peer-primitive ops each wrapping ONE cmd.* call (set_color→cmd.set_color viewing.py:2107 idempotent; label→cmd.label viewing.py:1332 with quoted-string expr footgun encapsulated; set→cmd.set setting.py:183 per-atom). NO composite super-op."
    - "Hero-highlight convention: a 6-call MolAction SEQUENCE (set_color→show_as spheres→set sphere_scale→color hero_gold→color gray80 scoped-dim→label C14) — NOT a super-op; per §2.2 resolution. Persistence = pure function of on_enter (re-derived each replay via the deterministic hero_selector rule; NO runtime marker alter/flag/b; cites engine.py:20-22,163-194,203-213 + persist.py:50-51)."
    - "Scene templates = MolAction SEQUENCES in on_enter, NOT cmd.scene keys (cmd.scene is session state LOST on save/load; on_enter replays via engine.py:203-213 — cmd.scene is optional Phase 6 cache only). 6 scene TYPES map all 55 skeleton nodes."
    - "Cast-reveal = full-then-zoom (full gray cartoon → zoom active site → green catalytic sticks → red mutant SPHERES (rep redundancy = colorblind-safe) → orange cofactor → nameplate+residue labels). Composes with 5.3: 5.4 BEFORE mutant red spheres + named colors; 5.3 AFTER WT-aligned reveal green."
    - "Shared named-color palette: hero_gold (Okabe-Ito [0.90,0.62,0.0] placeholder, Phase 7 source-approves) spheres / gray cartoon / green sticks (fills 5.3 <wt_color>) / red spheres (fills 5.3 <mutant_color>, OQ-2 magenta alt) / orange sticks / gray80 scoped dim / bad-ending red cartoon."
    - "CO2-shed soul-transfer climax: at tca.co2_turn1/turn2 the hero gold sphere shows ONE final time on departing CO2 then FADES; at etc.entry onward the highlight TRANSFERS to NADH (the soul=electrons); at end.true the highlight is on ATP. MANDATORY anti-confusion: transfer is a NARRATIVE DEVICE, NOT a claim the carbon becomes ATP/NADH (scientifically WRONG); C14-decay DROPPED."
    - "Hybrid selection rule: hero_selector per-asset (Phase 7-authored, deterministic) primary; single-C auto-derive (count_atoms==1) secondary; multi-C-no-selector FAIL LOUDLY tertiary (no silent best-guess — the which-carbon-is-the-hero is a HIGH-STAKES pathway-science claim, PROJECT.md row 109)."

key-files:
  created:
    - .planning/phases/05.4-cast-hero-representation-design/05.4-CONVENTION.md  # the SC1+SC2+SC3 design artifact (751 lines, 10 H2 sections: SC checklist + §1-§8)
  modified: []  # pure design writing — NO code files touched (c14/pymol_layer/molops.py NOT modified; c14/story/model.py NOT modified — confirmed via git diff --name-only; the 3 new-op dispatches are SPEC'd but DEFERRED to Phase 6 per OQ-1, mirroring 5.3-01)

key-decisions:
  - "ADOPT the minimal-orthogonal op-granularity approach: 3 NEW peer-primitive ops (set_color/label/set) each wrapping ONE cmd.* call, hero-highlight + scene templates expressed as SHORT MolAction sequences, NOT the composite op=\"hero_highlight\" super-op. Mirrors how 5.3 resolved its naming disagreement by adopting the API researcher's recommendation (05.3-CONVENTION.md §6). The rejected super-op is documented in §2.2 for the record."
  - "The op=\"set\" op is the 3rd primitive (justification for going beyond the API researcher's 2-ops recommendation): sphere_scale is a per-atom setting that show_as/color don't cover; cmd.set(name,value,sele) is a distinct primitive; a generic op=\"set\" is reusable beyond sphere_scale. 3 ops, not 2."
  - "The 3 new-op molops dispatches (set_color/label/set elif branches + MockCmd unit tests) DEFER to Phase 6 (mirrors 5.3 §6/§8 Q3 — 5.4 prototypes cmd.* directly in the smokes like wt_align_smoke.py calls cmd.super). Flagged OQ-1 for the Plan 04 checkpoint — human may override to add in 5.4 (the 04-05 precedent)."
  - "Shared named-color palette adopted: hero_gold (Okabe-Ito [0.90,0.62,0.0] PLACEHOLDER pending Phase 7 source approval) / gray / green (fills 5.3 <wt_color>) / red (fills 5.3 <mutant_color>, OQ-2 magenta alt) / orange / gray80. hero_gold is defined via cmd.set_color (idempotent); gray/green/red/orange/gray80 are standard PyMOL named colors. Do NOT assert a hardcoded color INDEX (session-allocated; assert the NAME via get_color_indices round-trip)."
  - "Hybrid selection rule (option iv) adopted: hero_selector per-asset + single-C auto-derive + multi-C-no-selector FAIL LOUDLY. The which-carbon-is-the-hero is a HIGH-STAKES pathway-science claim (PROJECT.md row 109) — no silent best-guess for real content."
  - "3 structural cases: (1) hero-alone (intro.preface, hero_atom single-atom object); (2) hero-in-substrate (glucose/multi-C, hero_selector=glucose and name C1); (3) hero-at-enzyme-cast (loaded obj is the ENZYME, hero in substrate not loaded) w/ 3 sub-options 3a co-load / 3b suspend / 3c hybrid RECOMMENDED. Case 3 co-load decision is per-enzyme Phase 7 (mirrors 5.3 OQ-DM2)."
  - "Hero-highlight = a 6-call MolAction SEQUENCE (set_color→show_as→set→color→color scoped-dim→label), NOT a super-op. The scoped dim (gray80 on non-hero atoms) is IN THE HERO-BEARING OBJECT ONLY — NOT the cast/enzyme (critical scoping). For single-atom hero_atom (case 1), step 5 dim omitted (5 calls)."
  - "Persistence = pure function of on_enter (re-derived each replay from the deterministic hero_selector rule; NO runtime marker alter/flag/b needed). Cites engine.py:20-22 (pure function docstring), :163-194 (_enter runs on_enter), :203-213 (load replays on_enter), persist.py:50-51 (saves ONLY GameState JSON, no .pse)."
  - "CO2-shed climax = soul-transfer (fade on CO2 at tca.co2_turn1/turn2 → transfer to NADH at etc.entry → ATP at end.true). The hero-highlight carries a soul mode (highlight targets a non-carbon NADH/ATP for post-shed nodes). MANDATORY anti-confusion: the transfer is a NARRATIVE DEVICE (soul=electrons per metamorphosis reframing), NOT a claim the carbon becomes ATP/NADH (scientifically WRONG); C14-decay DROPPED."
  - "Scene templates = MolAction SEQUENCES (NOT cmd.scene keys — session state LOST on save/load). 6 scene TYPES map all 55 skeleton nodes: preface / substrate-traversal / active-site-reveal=cast-reveal / branch-point / rng-shuffle / ending (4 tier-templates). cmd.scene MAY be a Phase 6 in-session performance cache but is NOT the contract."
  - "Cast-reveal = full-then-zoom (RECOMMENDED for the 8 PDB-bearing edit-allowed enzymes — the dramatic meet-the-cast-member beat). Active-site-first is a Phase 6 re-entry optimization (OQ-7). The cast-reveal sequence: full gray cartoon → zoom active site → green catalytic sticks → red mutant SPHERES (rep redundancy = colorblind-safe) → orange cofactor → nameplate+residue labels. tca.citrate_synthase structural variant omits the mutant step."
  - "5.3 composition: 5.4 OWNS the BEFORE (mutant red spheres at edit-allowed on_enter §5) + the named color values (wt_color=green, mutant_color=red FILLING 5.3 <mutant_color>/<wt_color> slots 05.3-CONVENTION.md:318-319). 5.3 OWNS the AFTER (WT-aligned reveal mechanism). 5.4 does NOT re-litigate the 5.3 align mechanism. Enzyme-health framing (cast-reveal) kept SEPARATE from carbon-fate framing (mirror 5.3 §5)."
  - "11 open questions OQ-1 to OQ-11 surfaced with research recommendations + tradeoffs + why-it-matters + status for the Plan 04 human design-review checkpoint. OQ-1 (op-granularity + dispatch landing) RESOLVED-in-spec-confirm; OQ-2 to OQ-11 OPEN."
  - "No fabricated science: ZERO real PDB IDs (only pdb:TBD_* placeholders — unlike 5.3 which had 6CFO/6CER as the pyr.pdh example). The palette RGB values are placeholders pending Phase 7 source approval. The which-carbon-is-the-hero + which-residue-is-catalytic + which-residue-is-the-disease-mutation are Phase 7 per-claim cited-science decisions."

patterns-established:
  - "Hero-highlight convention: hybrid selection rule (hero_selector + single-C auto + multi-C FAIL LOUDLY) + 3 structural cases + 6-call MolAction sequence (set_color→show_as→set→color→color scoped-dim→label) + persistence as pure function of on_enter (NO runtime marker) + CO2-shed soul-transfer climax (fade+transfer) with MANDATORY anti-confusion."
  - "Op-granularity resolution pattern: when two research tracks disagree on dispatch granularity (minimal-ops vs composite-super-op), ADOPT the minimal-orthogonal approach (peer-primitive ops wrapping one cmd.* each + sequences) over the composite super-op — mirrors the 5.3 precedent (5.3 added ONE focused op align, NOT a composite wt_reveal super-op; 5.4 adds 3 focused primitive wrappers). Rationale: minimal+orthogonal dispatch, per-step headless-testability, the 5.3 precedent."
  - "Scene-template library pattern: scene templates are MolAction SEQUENCES in on_enter (save/load-safe via engine.py replay), NOT cmd.scene keys (session state LOST on save/load). cmd.scene is optional Phase 6 cache only. 6 scene TYPES map all 55 skeleton nodes; per-type post-conditions machine-checkable via count_atoms rep keyword + iterate + named-sele residue count."
  - "Cast-reveal convention pattern: full-then-zoom (full gray cartoon → zoom active site → green catalytic sticks → red mutant SPHERES (rep redundancy=colorblind-safe) → orange cofactor → nameplate+residue labels). Composes with 5.3 via the <mutant_color>/<wt_color> color slots (5.4 BEFORE red, 5.3 AFTER green)."
  - "Shared-palette consistency pattern: a CONSISTENT named-color palette documented in the convention, consumed by Phase 6/7/9/10 — the visual language is consistent across the ~20+ enzyme cast (Phase 9) BY CONSTRUCTION (every enzyme enters via the SAME template + palette, NOT ad-hoc per node)."

# Metrics
duration: 45 min
completed: 2026-08-29
---

# Phase 5.4 Plan 01: Cast & Hero Representation Convention Spec Summary

**Cast & hero representation convention spec — SC1 hero-highlight (hybrid selection rule + hero_gold spheres + CO2-shed soul-transfer climax w/ anti-confusion) + SC2 scene-template library (6 types mapping all 55 nodes as MolAction sequences not cmd.scene) + SC3 cast-reveal (full-then-zoom + gray/green/red/orange palette + 5.3 composition filling <mutant_color>/<wt_color>); op-granularity RESOLVED adopting 3 minimal new peer-primitive ops set_color/label/set NOT the rejected super-op; 3 new-op dispatches deferred to Phase 6 (OQ-1); 11 open questions OQ-1 to OQ-11 for the Plan 04 checkpoint**

## Performance

- **Duration:** 45 min
- **Started:** 2026-08-29T09:30:13Z
- **Completed:** 2026-08-29T10:15:00Z
- **Tasks:** 2
- **Files modified:** 1 (the new convention spec; 0 code files)

## Accomplishments
- Wrote the SC1+SC2+SC3 design artifact `05.4-CONVENTION.md` (751 lines, 10 H2 sections: SC checklist + §1-§8 + review checklist) — the Cast & Hero Representation convention spec consumed by Phases 6 / 7 / 9 / 12 WITHOUT re-designing it
- Resolved the op-granularity disagreement (§2.2): ADOPTED the minimal-orthogonal approach — 3 NEW peer-primitive ops `set_color` / `label` / `set` (each wrapping ONE `cmd.*` call), hero-highlight + scene templates expressed as SHORT MolAction sequences, NOT the composite `op="hero_highlight"` super-op. Mirrors how 5.3 resolved its naming disagreement by adopting the API researcher's recommendation. The rejected super-op is documented in §2.2 for the record (pros noted, cons lose). 4-point rationale: minimal+orthogonal dispatch; per-step headless-testability; the 5.3 precedent (5.3 added ONE focused op align NOT a composite); the `set` op is the 3rd primitive (sphere_scale is a per-atom setting show_as/color don't cover)
- Specified the shared foundation (§2): §2.1 the shared MolAction op vocabulary (8 existing peer-primitive ops + 3 NEW set_color/label/set with their MolAction shapes + the molops dispatch pseudocode SPEC-only deferred to Phase 6); §2.2 the op-granularity RESOLUTION; §2.3 the shared named-color palette (hero_gold Okabe-Ito [0.90,0.62,0.0] PLACEHOLDER spheres / gray cartoon / green sticks (fills 5.3 <wt_color>) / red spheres (fills 5.3 <mutant_color>, OQ-2 magenta alt) / orange sticks / gray80 scoped dim / bad-ending red cartoon — with the named-color confidence note: gray/green/red/orange/gray80 are standard PyMOL named colors; hero_gold is defined via cmd.set_color idempotent; do NOT assert a hardcoded color INDEX)
- Specified SC1 the hero highlight (§3): §3.1 hybrid selection rule (option iv: hero_selector per-asset + single-C auto-derive + multi-C-no-selector FAIL LOUDLY — the which-carbon-is-the-hero is a HIGH-STAKES pathway-science claim PROJECT.md row 109); §3.2 the 3 structural cases (hero-alone / hero-in-substrate / hero-at-enzyme-cast w/ 3a co-load 3b suspend 3c hybrid RECOMMENDED); §3.3 the 6-call semantic sequence (set_color→show_as spheres→set sphere_scale→color hero_gold→color gray80 scoped-dim→label C14, NOT a super-op; the scoped dim is IN THE HERO-BEARING OBJECT ONLY not the cast/enzyme); §3.4 persistence as pure function of on_enter (re-derived each replay from the deterministic hero_selector rule; NO runtime marker alter/flag/b needed; cites engine.py:20-22,163-194,203-213 + persist.py:50-51); §3.5 the CO2-shed soul-transfer climax (tca.shuffle→co2_turn1/turn2→etc.*→end.true; fade on CO2 + transfer to NADH/ATP; the soul mode); §3.6 the MANDATORY anti-confusion note (the transfer is a NARRATIVE DEVICE NOT a claim the carbon becomes ATP/NADH; C14-decay DROPPED; the text_teaching contract); §3.7 no fabricated science (PLACEHOLDERS only, zero real PDB IDs)
- Specified SC2 the scene-template library (§4): §4.1 scene templates are MolAction SEQUENCES NOT cmd.scene keys (cmd.scene is session state LOST on save/load; on_enter replays via engine.py:203-213 — cmd.scene is optional Phase 6 cache only); §4.2 the 6 scene TYPES mapping all 55 skeleton nodes (preface / substrate-traversal / active-site-reveal=cast-reveal / branch-point / rng-shuffle / ending 4-tier) + the 6 narrative-only edit-allowed sub-case (Phase 7 decides per enzyme = OQ-10); §4.3 per-type draft MolAction sequences + post-conditions for all 6 types incl the 4 ending tier-templates (true/good/normal/bad)
- Specified SC3 the cast-reveal (§5): §5.1 full-then-zoom RECOMMENDED (the dramatic meet-the-cast-member beat; active-site-first = Phase 6 re-entry optimization = OQ-7); §5.2 the cast-reveal sequence (full gray cartoon → zoom active site → green catalytic sticks → red mutant SPHERES (rep redundancy=colorblind-safe) → orange cofactor sticks → nameplate+residue labels) + the tca.citrate_synthase structural variant (omit mutant step 4); §5.3 two label layers (enzyme nameplate + residue labels, headless-verifiable via cmd.label; Qt overlay is Phase 6 polish not floor); §5.4 the mutant→WT visual arc + 5.3 composition (5.4 OWNS BEFORE mutant red spheres + named colors filling 5.3 <mutant_color>/<wt_color> slots; 5.3 OWNS AFTER WT-aligned reveal mechanism; enzyme-health vs carbon-fate SEPARATE)
- Wrote the downstream contracts (§6): §6.1 Phase 6 (6 scene-type seqs + cast-reveal + hero-highlight + shared palette; UI must NOT invent colors/reps/labels; 3 new ops set_color/label/set + align deferred); §6.2 Phase 7 (7-field enzyme metadata table + 4-field hero metadata table; selectors are structural but which-residue-is-catalytic/which-carbon-is-hero ARE cited science claims HIGH-STAKES); §6.3 Phase 9 (cast-reveal + Type 3 template GUARANTEE cast consistency by construction; large-enzyme zoom is Phase 9); §6.4 Phase 12 (Type 6 ending-templates compose with cmd.ray render)
- Surfaced all 11 open questions (§7): OQ-1 (op-granularity + dispatch landing — RESOLVED-in-spec-confirm) + OQ-2 to OQ-11 (OPEN — mutant color, hero color, label wording, CO2-shed visual, multi-C fallback, cast-reveal zoom, branch-point side-by-side, ending-tier distinctness, narrative-only edit-allowed, sub-beats) each with research recommendation + tradeoff + why-it-matters + status; plus 4 RESOLVED-by-research items recorded (fixture reuse / cmd.scene rejection / PubChem atom naming Phase 7 / palette RGB Phase 7)
- Wrote the 9-criterion review checklist (§8) for the Plan 04 human design-review checkpoint (hero-selection-rule / op-granularity / scene-template completeness / cast-reveal consistency / 5.3 composition / CO2-shed climax+anti-confusion / downstream contracts / 11 OQs / no-fabricated-science)
- Verified NO fabricated science (ZERO real PDB IDs — only pdb:TBD_* placeholders, unlike 5.3 which had 6CFO/6CER; the palette RGB values are placeholders pending Phase 7 source approval) + NO code modified (c14/pymol_layer/molops.py + c14/story/model.py NOT touched — confirmed via git diff --name-only; pure design writing phase)

## Task Commits

Each task was committed atomically:

1. **Task 1: Write the shared foundation + the SC1 hero-highlight convention (§1-§3)** — `b37185e` (docs) — 325 lines (§1 purpose/scope + hero-identity anti-confusion, §2 shared foundation: op vocabulary + op-granularity RESOLUTION + shared palette, §3 SC1 hero highlight: hybrid selection rule + 3 cases + 6-call sequence + persistence + CO2-shed climax + anti-confusion + no-fabricated-science)
2. **Task 2: Write SC2 scene-template library + SC3 cast-reveal + downstream contracts + 11 open questions + review checklist (§4-§8)** — `f6697fd` (docs) — 426 lines added (751 total; §4 SC2 6-type library, §5 SC3 cast-reveal + 5.3 composition, §6 downstream contracts 6/7/9/12, §7 11 OQs, §8 review checklist)

**Plan metadata:** (pending — final commit after SUMMARY + STATE)

## Files Created/Modified
- `.planning/phases/05.4-cast-hero-representation-design/05.4-CONVENTION.md` — the SC1+SC2+SC3 design artifact (the Cast & Hero Representation convention spec: §1 purpose/scope + hero-identity anti-confusion, §2 shared foundation [op vocabulary + op-granularity RESOLUTION + shared palette], §3 SC1 hero highlight [hybrid selection rule + 3 cases + 6-call sequence + persistence + CO2-shed climax + anti-confusion + no-fabricated-science], §4 SC2 scene-template library [6 types mapping all 55 nodes, MolAction sequences not cmd.scene, per-type post-conditions], §5 SC3 cast-reveal [full-then-zoom + cast-reveal sequence + labeling + mutant→WT arc + 5.3 composition], §6 downstream contracts [6/7/9/12], §7 11 open questions OQ-1 to OQ-11, §8 review checklist)

## Decisions Made
- **Op-granularity RESOLUTION:** ADOPT 3 minimal peer-primitive ops (set_color/label/set) + SHORT sequences, NOT the composite op="hero_highlight" super-op. Mirrors 5.3 (which adopted the API researcher's recommendation + added ONE focused op align NOT a composite). The rejected super-op documented in §2.2.
- **The set op is the 3rd primitive:** justified going beyond the API researcher's 2-ops recommendation (sphere_scale is a per-atom setting show_as/color don't cover; cmd.set is a distinct primitive; generic op=set reusable beyond sphere_scale). 3 ops, not 2.
- **Dispatch deferral:** the 3 new-op molops dispatches (set_color/label/set elif branches + MockCmd unit tests) DEFER to Phase 6 (mirrors 5.3 §6/§8 Q3). Flagged OQ-1 — human may override to add in 5.4 (the 04-05 precedent).
- **Shared named-color palette:** hero_gold (Okabe-Ito [0.90,0.62,0.0] PLACEHOLDER pending Phase 7) / gray / green (fills 5.3 <wt_color>) / red (fills 5.3 <mutant_color>, OQ-2 magenta alt) / orange / gray80. Do NOT assert a hardcoded color INDEX (session-allocated; assert the NAME via get_color_indices round-trip).
- **Hybrid selection rule (option iv):** hero_selector per-asset + single-C auto-derive + multi-C-no-selector FAIL LOUDLY. The which-carbon-is-the-hero is a HIGH-STAKES pathway-science claim.
- **3 structural cases:** hero-alone / hero-in-substrate / hero-at-enzyme-cast w/ 3a co-load 3b suspend 3c hybrid RECOMMENDED. Case 3 co-load decision is per-enzyme Phase 7 (mirrors 5.3 OQ-DM2).
- **Hero-highlight = 6-call MolAction SEQUENCE** (set_color→show_as→set→color→color scoped-dim→label), NOT a super-op. Scoped dim IN THE HERO-BEARING OBJECT ONLY (not the cast/enzyme).
- **Persistence = pure function of on_enter** (NO runtime marker). Cites engine.py + persist.py.
- **CO2-shed climax = soul-transfer** (fade+transfer; the soul mode). MANDATORY anti-confusion: transfer is a NARRATIVE DEVICE NOT carbon→ATP/NADH claim; C14-decay DROPPED.
- **Scene templates = MolAction SEQUENCES** (NOT cmd.scene keys — session state LOST on save/load). 6 scene TYPES map all 55 nodes. cmd.scene is optional Phase 6 cache.
- **Cast-reveal = full-then-zoom** (RECOMMENDED; active-site-first = Phase 6 re-entry optimization OQ-7). Rep redundancy (red SPHERES vs green STICKS) = colorblind-safe.
- **5.3 composition:** 5.4 OWNS BEFORE (mutant red spheres + named colors filling <mutant_color>/<wt_color>); 5.3 OWNS AFTER (WT-aligned reveal). Enzyme-health vs carbon-fate SEPARATE.

## Deviations from Plan

None — plan executed exactly as written. The plan was pure design writing (0 code changes); both tasks wrote the convention spec file as specified. The op-granularity RESOLUTION (§2.2) was the key decision the plan required, and it was made exactly as the plan directed (ADOPT the minimal-orthogonal approach, document the rejected super-op, mirror the 5.3 precedent). No Rule 1-3 deviations arose (no bugs, no missing critical functionality, no blocking issues — the plan was self-contained design writing against 3 already-complete HIGH-confidence research files).

## Issues Encountered
None.

## User Setup Required
None — this is a pure design writing phase; no external service configuration required.

## Next Phase Readiness
- **SC1 + SC2 + SC3 DELIVERED (spec):** the Cast & Hero Representation convention is specified (hero highlight + scene-template library + cast-reveal) and documented for review in `05.4-CONVENTION.md`. The convention is internally consistent across all 3 SCs (shared op vocabulary + shared named-color palette), honors the 5.3 composition (5.4 BEFORE mutant red spheres; 5.3 AFTER WT green; fills <mutant_color>/<wt_color>), and has a MANDATORY anti-confusion note on the CO2-shed climax (soul-transfer is a narrative device, NOT carbon→ATP).
- **Ready for Plans 02 / 03 (SC headless mechanism proofs):** the convention spec is the reviewable design artifact the headless prototypes consume. Plan 02 will prototype the hero-highlight MECHANISM (the §3.3 6-call sequence on placeholder structures — count==1, elem C, rep spheres, color name round-trip, label C14). Plan 03 will prototype the scene-template + cast-reveal MECHANISM (the §4.3 / §5.2 sequences on placeholders — rep visible, color name, label present, named-sele residue count). Both prototype `cmd.*` directly (NOT via molops — the 3 new-op dispatches are deferred to Phase 6 per OQ-1, unless the human overrides at the Plan 04 checkpoint).
- **Ready for Plan 04 (SC4 human design-review checkpoint):** the §8 review checklist (9 criteria) + the 11 open questions (§7 OQ-1 to OQ-11) tell the human exactly what to assess. OQ-1 (op-granularity + dispatch landing) is the only one that could change Plans 02 / 03's task lists (if overridden to "add in 5.4", they gain molops.py edit + unit test tasks).
- **Ready for Phase 6:** the §6.1 downstream contract (6 scene-type sequences + cast-reveal + hero-highlight + shared palette; 3 new ops set_color/label/set + align deferred; UI must NOT invent colors/reps/labels) tells Phase 6 exactly what to implement.
- **Ready for Phase 7:** the §6.2 downstream contract (7-field enzyme metadata table + 4-field hero metadata table; the CO2-shed + soul-transfer nodes' on_enter sequences + anti-confusion text_teaching) tells Phase 7 exactly what to author per asset.
- **Ready for Phase 9:** the §6.3 downstream contract (cast-reveal + Type 3 template GUARANTEE cast consistency by construction; large-enzyme zoom is Phase 9) tells Phase 9 the visual language is fixed by the template + palette, NOT ad-hoc.
- **Ready for Phase 12:** the §6.4 downstream contract (Type 6 ending-templates compose with cmd.ray render; 4 distinct visual signatures) tells Phase 12 the scene CONTENT is the 5.4 template's contract.
- **Blockers/concerns:** none. The 11 open questions (§7) are surfaced for the Plan 04 checkpoint — none block Plans 02 / 03's prototypes; OQ-1 (where the 3 new-op dispatches land) is the only one that could change Plans 02 / 03's task lists.

---
*Phase: 05.4-cast-hero-representation-design*
*Plan: 01 of 04*
*Completed: 2026-08-29*
