---
phase: 05.4-cast-hero-representation-design
plan: 04
subsystem: design
tags: [cast-hero-representation, hero-highlight, scene-templates, cast-reveal, checkpoint, design-review, cyan-hero, magenta-mutant, you-label, default-warn-confirm, oq-11-future-note, soul-jump, anti-confusion, 5.3-composition, placeholder-fixtures, convention-frozen]

# Dependency graph
requires:
  - phase: 05.4-cast-hero-representation-design
    provides: the SC1+SC2+SC3 convention spec `05.4-CONVENTION.md` (Plan 01) + both headless mechanism proofs (Plan 02 hero_highlight_smoke.py 25/25 PASS + Plan 03 scene_template_smoke.py 37/37 PASS) this plan human-reviewed + adjusted
  - phase: 05.3-wt-aligned-structure-load-convention
    provides: the <mutant_color>/<wt_color> composition slots 5.4 fills (magenta/green per OQ-2 OVERRIDE) + the op-deferral-to-Phase-6 precedent (5.3 §6/§8 Q3) + the human-review-checkpoint SUMMARY shape this plan mirrors
  - phase: 03-pymol-cmd-layer
    provides: the headless harness (tools/run_headless.sh) + the SMOKE_RESULT sentinel verdict + the source-citation convention
provides:
  - SC4 DELIVERED — human design-review checkpoint APPROVED-WITH-ADJUSTMENTS (5 overrides: OQ-2 magenta, OQ-3 cyan+all-C+ball-and-stick+YOU, OQ-4 YOU, OQ-6 default+warn+confirm, OQ-11 future-note; 6 approve-rec)
  - The convention FROZEN for Phase 6/7/9/12 — `05.4-CONVENTION.md` revised with the 5 overrides applied (766 lines); both smokes re-ran SMOKE_RESULT: PASS (hero 29/29, scene 37/37) after the cyan/magenta/YOU palette swap
  - The 11 OQ decisions table (OQ | decision | rationale) — ALL 11 RESOLVED
  - The op-granularity RESOLUTION confirmed (3 minimal new ops, NOT a super-op; dispatch deferred to Phase 6 per OQ-1; molops.py UNTOUCHED)
  - The 5.3 composition COLOR contract (5.4 BEFORE mutant magenta spheres + 5.3 AFTER WT green; 5.4 fills <mutant_color>=magenta / <wt_color>=green)
  - The CO2-shed anti-confusion (soul-transfer = narrative device, NOT carbon→ATP; C14-decay DROPPED; sole timescale = cycle-trap-until-host-death)
affects: [06-qt-ui-mvp, 07-content-authoring, 09-full-cast, 12-ending-cutscene]

# Tech tracking
tech-stack:
  added: []  # no new libraries — convention revision + smoke palette swap only
  patterns:
    - "OQ-3 OVERRIDE hero design: ALL carbons in the hero-bearing molecule are hero_cyan sticks (the hero is 'one of them'); the hero is distinguished by ball-and-stick (sticks + SMALL sphere sphere_scale=0.3) + 'YOU' label; non-carbons keep element colors (NOT dimmed); the old scoped-dim gray80 was REMOVED (gray80 now used ONLY for the CO2-shed fade)"
    - "OQ-2 OVERRIDE mutant color: magenta (NOT red — red confuses with the red of oxygen atoms in the structure); rep redundancy (magenta SPHERES vs green STICKS) still provides colorblind-safety"
    - "OQ-6 OVERRIDE selection rule: multi-C-no-selector → default + warn + confirm gate (NOT fail-loudly, NOT silent heuristic); the confirm gate IS the no-fabricated-science guard; Phase 6 UI contract"
    - "cmd.show (NOT show_as) for ball-and-stick: show_as sticks first (base rep), then show spheres ON TOP (ADDS rep, turns ON only) — the hero has BOTH sticks AND spheres; show_as spheres would turn OFF sticks"

key-files:
  created:
    - .planning/phases/05.4-cast-hero-representation-design/05.4-04-SUMMARY.md  # this SUMMARY
  modified:
    - .planning/phases/05.4-cast-hero-representation-design/05.4-CONVENTION.md  # revised: 5 overrides applied (751 -> 766 lines; FROZEN)
    - tools/hero_highlight_smoke.py  # updated: hero_gold -> hero_cyan, all-C sticks + ball-and-stick + "YOU" label, scoped-dim removed for hero (kept for CO2-shed fade); 29/29 PASS
    - tools/scene_template_smoke.py  # updated: hero_gold -> hero_cyan, mutant red -> magenta, label "C14" -> "YOU", Type 6 true label -> "YOU - soul"; 37/37 PASS
    - .planning/STATE.md  # updated: Phase 5.4 COMPLETE (4/4 plans)

key-decisions:
  - "SC4 APPROVED-WITH-ADJUSTMENTS — the human reviewed the convention + both smoke outputs + the 11 OQs and decided: 5 OVERRIDES (OQ-2/3/4/6/11) + 6 APPROVE-rec (OQ-1/5/7/8/9/10)"
  - "OQ-1 APPROVE rec: 3 minimal new ops (set_color/label/set), NOT a super-op; dispatch DEFERRED to Phase 6 (molops.py UNTOUCHED — mirrors 5.3 Q3)"
  - "OQ-2 OVERRIDE to magenta: red confuses with the red of oxygen atoms in the structure; magenta is pure-hue-safe; fills 5.3's <mutant_color> slot with magenta"
  - "OQ-3 OVERRIDE to cyan + all-C-colored: gold confuses with the yellow of sulfur; ALL carbons in the hero-bearing molecule are cyan sticks (the hero is 'one of them'); the hero is ball-and-stick (sticks + small sphere sphere_scale=0.3) + 'YOU' label; non-carbons keep element colors (NOT dimmed — dimming deferred); the old scoped-dim gray80 REMOVED (gray80 now ONLY for CO2-shed fade)"
  - "OQ-4 OVERRIDE to 'YOU': the player-facing identity label (NOT 'C14' the isotope tracking label); 'C14' remains the concept (the hero's name tag, NOT its fate) but is NOT the on-screen label"
  - "OQ-5 APPROVE rec: fade + transfer (the soul-jump metamorphosis climax)"
  - "OQ-6 OVERRIDE to default + warn + confirm gate: NOT fail-loudly (endless-loop), NOT silent heuristic (fabricated-science risk); single-C auto-derive; multi-C with selector use it; multi-C without selector → auto-pick default + warn + yes/no confirm (the confirm IS the no-fabricated-science guard); Phase 6 UI contract"
  - "OQ-7 APPROVE rec: full-then-zoom default; active-site-first as Phase 6 re-entry optimization"
  - "OQ-8 APPROVE rec: data-level 2-object shape; viewport split deferred to Phase 6"
  - "OQ-9 APPROVE rec: 4 tier-templates (distinct victory/storage/release/catastrophe signatures)"
  - "OQ-10 APPROVE rec: 5.4 specs BOTH (substrate-traversal default OR active-site-reveal IF Phase 7 sources a structure); Phase 7 decides per enzyme"
  - "OQ-11 APPROVE rec + NOTE: single-node reveal for MVP; the 3-sub-beat (approach → narrative fade for catalysis → product) is a DEFERRED FUTURE ENRICHMENT, NOT an infinite skip — potential student project / future development"
  - "CO2-shed anti-confusion CONFIRMED: the highlight transfer (carbon → NADH → ATP) is a narrative/visual device (the 'soul' follows the electrons per the metamorphosis reframing), NOT a claim the carbon becomes ATP/NADH"
  - "C14-decay CONFIRMED DROPPED: the sole timescale bad-ending is 'loop until the host dies' (the cycle-trap-until-host-death from spec.md line 18); no C14 radioactive-decay timescale"
  - "Convention FROZEN for Phase 6/7/9/12 — no follow-up work owed in 5.4 (OQ-1 dispatch deferred to Phase 6; OQ-6 warn+confirm is a Phase 6 UI contract; OQ-11 3-sub-beat is a future enrichment)"

patterns-established:
  - "OQ-3 OVERRIDE all-C-colored ball-and-stick hero design: ALL carbons cyan sticks + hero ball-and-stick (show spheres ON TOP of sticks via cmd.show, NOT show_as) + small sphere (sphere_scale 0.3) + 'YOU' label; non-carbons keep element colors; the hero is 'one of them' distinguished by the ball + label, NOT by dimming"
  - "OQ-2 OVERRIDE magenta mutant: red → magenta (red confuses with oxygen's CPK color); rep redundancy (magenta SPHERES vs green STICKS) still colorblind-safe"
  - "OQ-6 OVERRIDE default+warn+confirm selection rule: the confirm gate IS the no-fabricated-science guard — avoids both the endless-loop-of-no-selector (fail-loudly) and the silent-guess risk (heuristic)"
  - "Convention revision pattern: when the human overrides at a checkpoint, revise the convention spec + update both smokes + re-run PASS, all in one plan (the post-checkpoint finalization)"

# Metrics
duration: ~30min (continuation agent)
completed: 2026-08-29
---

# Phase 5.4 Plan 04: Human Design-Review Checkpoint Summary

**SC4 DELIVERED — human design-review checkpoint APPROVED-WITH-ADJUSTMENTS (5 overrides: OQ-2 magenta mutant, OQ-3 cyan hero + all-C-colored ball-and-stick + "YOU" label, OQ-4 "YOU", OQ-6 default+warn+confirm, OQ-11 future-note; 6 approve-rec); convention FROZEN for Phase 6/7/9/12; both smokes re-ran SMOKE_RESULT: PASS after the cyan/magenta/YOU palette swap; molops.py UNTOUCHED (OQ-1 dispatch deferred to Phase 6)**

## Performance

- **Duration:** ~30 min (continuation agent — applied the human's checkpoint overrides)
- **Started:** 2026-08-29T12:50:00Z (continuation agent spawned)
- **Completed:** 2026-08-29T13:17:42Z
- **Tasks:** 2 (Task 1 = checkpoint automation [previous executor]; Task 2 = apply overrides + SUMMARY + STATE + commit [this continuation agent])
- **Files modified:** 4 (convention revised + 2 smokes updated + STATE.md); 1 created (this SUMMARY)

## Accomplishments

- **SC4 DELIVERED** — the human reviewed the convention (`05.4-CONVENTION.md` §1-§8 + the 11 OQs) + both smoke outputs (hero_highlight 25/25 PASS + scene_template 37/37 PASS) against the §8 review checklist and responded with decisions: 5 OVERRIDES (OQ-2/3/4/6/11) + 6 APPROVE-rec (OQ-1/5/7/8/9/10). The convention is FROZEN for Phase 6/7/9/12.
- **Applied the 5 overrides to `05.4-CONVENTION.md`** (751 → 766 lines):
  - **OQ-2 OVERRIDE (magenta mutant):** §2.3 palette mutant row `red` → `magenta` (red confuses with the red of oxygen atoms in the structure; magenta is pure-hue-safe); §5.2 cast-reveal step 4 `red` → `magenta`; §5.4 composition `<mutant_color>` = magenta; §6.2/§6.3 metadata + cast-consistency references updated. The bad-ending `red` cartoon stays `red` (different context — catastrophe signal, not near oxygen atoms).
  - **OQ-3 OVERRIDE (cyan hero + all-C-colored + ball-and-stick):** §2.3 palette hero row `hero_gold` → `hero_cyan` [0.0, 0.75, 0.75] PLACEHOLDER; §3.3 hero-highlight sequence REPLACED — new 6-call sequence (set_color → show_as sticks ALL atoms → color hero_cyan elem C → [non-carbons keep element colors, no call] → show spheres hero_sele [ball-and-stick ON TOP of sticks] → set sphere_scale 0.3 [small elegant sphere] → label "YOU"); the old scoped-dim gray80 step REMOVED (gray80 now ONLY for CO2-shed fade); §3.5 CO2-shed climax gold → cyan; §3.6 anti-confusion gold → cyan; §4.3 all template hero_gold → hero_cyan; §6.2 hero_color default → hero_cyan.
  - **OQ-4 OVERRIDE ("YOU" label):** §3.3 sequence label "C14" → "YOU"; §6.2 hero_label default → "YOU"; §2.1 op example label text → "YOU".
  - **OQ-6 OVERRIDE (default + warn + confirm gate):** §3.1 selection rule step 3 FAIL LOUDLY → auto-pick DEFAULT carbon + WARN + yes/no CONFIRM (the confirm IS the no-fabricated-science guard; Phase 6 UI contract; smokes use known-by-construction selectors — the warn+confirm path is NOT exercised headlessly).
  - **OQ-11 APPROVE rec + NOTE:** §4.3 Type 3 + new §6.5 — the 3-sub-beat (approach → narrative fade for catalysis → product) is a DEFERRED FUTURE ENRICHMENT, NOT an infinite skip (potential student project / future development; the real-coordinate version needing QM/literature is the harder research-grade variant; the simpler narrative-fade version is accessible enough for a future hands-on project).
  - Updated §7 OQ decisions table: ALL 11 RESOLVED (6 APPROVE-rec + 5 OVERRIDE with rationale); §8 review checklist updated (red → magenta, FAIL LOUDLY → default+warn+confirm); header status DRAFT → FROZEN; SC1-SC4 checkboxes checked.
- **Updated `tools/hero_highlight_smoke.py`** to the new OQ-3 OVERRIDE design:
  - `hero_gold` → `hero_cyan` [0.0, 0.75, 0.75]; Stage 1-3 hero-highlight sequences rewritten (show_as sticks → color hero_cyan elem C → show spheres [ball-and-stick] → set sphere_scale 0.3 → label "YOU"); scoped-dim gray80 REMOVED for the hero (KEPT for the CO2-shed fade in Stage 4); Stage 4 co2/nadh gold → cyan, label "C14 (farewell)" → "YOU (farewell)".
  - New post-conditions: `case2_all_carbons_cyan` (C2 is also hero_cyan — the hero is "one of them"); `case2_noncarbon_element_color` (O1 keeps "oxygen" element color — NOT dimmed); `case2_all_carbons_sticks` (both carbons have sticks); `case1_rep_sticks` + `case2_hero_rep_sticks` (ball-and-stick proof — hero has BOTH sticks AND spheres).
  - Re-ran `bash tools/run_headless.sh tools/hero_highlight_smoke.py` → **SMOKE_RESULT: PASS** with all 29 stage checks green.
- **Updated `tools/scene_template_smoke.py`** to match:
  - `hero_gold` → `hero_cyan` everywhere (Stage 0 palette + Types 1/2/4/5/6); mutant `red` → `magenta` (Type 3 + Stage 7 BEFORE); label `"C14"` → `"YOU"` (Types 1/2); Type 6 true label `"ATP - soul"` → `"YOU - soul"`; `get_color_indices()` → `get_color_indices(all=1)` for magenta safety; check names `t3_mutant_color_red` → `t3_mutant_color_magenta` + `comp_before_mutant_red_spheres` → `comp_before_mutant_magenta_spheres`.
  - Re-ran `bash tools/run_headless.sh tools/scene_template_smoke.py` → **SMOKE_RESULT: PASS** with all 37 stage checks green.
- **Confirmed all gates:**
  - `python3.6 tools/check_imports.py` exits 0 (AST gate clean)
  - `git diff --name-only c14/pymol_layer/molops.py` shows NO changes (OQ-1 dispatch deferred to Phase 6)
  - `git diff --name-only c14/story/model.py` shows NO changes
  - NO new fixture files created (reused `_smoke.pdb` / `_edit_smoke.pdb` / `_wt_align_wt.pdb`)
  - Zero real PDB IDs in `05.4-CONVENTION.md` (only `pdb:TBD_*` placeholders; the cyan/magenta RGB values are placeholders pending Phase 7 source approval)

## The 11 OQ Decisions Table

| OQ | Question | Decision | Rationale |
|----|----------|----------|-----------|
| **OQ-1** | Op-granularity: 3 minimal ops vs super-op | **APPROVE rec** | 3 minimal peer-primitive ops (set_color/label/set), NOT a super-op; dispatch DEFERRED to Phase 6 (molops.py UNTOUCHED — mirrors 5.3 Q3) |
| **OQ-2** | Mutant color: red vs magenta | **OVERRIDE to magenta** | Red confuses with the red of oxygen atoms in the structure; magenta is pure-hue-safe; rep redundancy (magenta SPHERES vs green STICKS) still colorblind-safe; fills 5.3's `<mutant_color>` slot with magenta |
| **OQ-3** | Hero color: gold vs cyan vs deferred | **OVERRIDE to cyan + all-C-colored** | Gold confuses with the yellow of sulfur; ALL carbons cyan sticks (hero is "one of them"); hero = ball-and-stick (sticks + small sphere 0.3) + "YOU" label; non-carbons keep element colors (NOT dimmed — dimming deferred); scoped-dim gray80 REMOVED (now ONLY for CO2-shed fade); RGB [0.0, 0.75, 0.75] PLACEHOLDER pending Phase 7 |
| **OQ-4** | Label: "C14" vs "YOU" | **OVERRIDE to "YOU"** | Player-facing identity label (more engaging than the isotope tracking label "C14"); "C14" remains the concept (the hero's name tag, NOT its fate) but is NOT the on-screen label |
| **OQ-5** | CO2-shed: fade+transfer vs fade-only vs transfer-only | **APPROVE rec** | Fade + transfer (the soul-jump metamorphosis climax); uses hero_cyan (not gold) for the highlight + gray80 for the fade |
| **OQ-6** | Multi-C fallback: fail-loudly vs heuristic | **OVERRIDE to default+warn+confirm** | NOT fail-loudly (endless-loop-of-no-selector), NOT silent heuristic (fabricated-science risk); single-C auto-derive; multi-C with selector use it; multi-C without selector → auto-pick default + warn + yes/no confirm (the confirm IS the no-fabricated-science guard); Phase 6 UI contract |
| **OQ-7** | Cast-reveal: full-then-zoom vs active-site-first | **APPROVE rec** | Full-then-zoom default; active-site-first as Phase 6 re-entry optimization |
| **OQ-8** | Branch-point: data-level vs mandate side-by-side | **APPROVE rec** | Data-level 2-object shape; viewport split deferred to Phase 6 (mirrors 5.3 §D) |
| **OQ-9** | Ending-tier: 4 templates vs 1 parametric | **APPROVE rec** | 4 tier-templates (distinct victory/storage/release/catastrophe signatures) |
| **OQ-10** | Narrative-only edit-allowed: substrate-traversal vs active-site-reveal | **APPROVE rec** | 5.4 specs BOTH; Phase 7 decides per enzyme (mirrors 5.3 OQ-DM2) |
| **OQ-11** | Sub-beats: single-node vs 3-sub-beat | **APPROVE rec + NOTE** | Single-node reveal for MVP; the 3-sub-beat is a DEFERRED FUTURE ENRICHMENT, NOT an infinite skip — potential student project / future development; the real-coordinate version needing QM/literature is the harder research-grade variant |

## The Op-Granularity RESOLUTION (confirmed at checkpoint)

The human APPROVED the §2.2 resolution (OQ-1 APPROVE rec): 3 minimal peer-primitive ops (`set_color` / `label` / `set`), each wrapping ONE `cmd.*` call, hero-highlight + scene templates expressed as SHORT MolAction sequences, NOT the composite `op="hero_highlight"` super-op. Mirrors how 5.3 resolved its naming disagreement by adopting the API researcher's recommendation. The 3 new-op molops dispatches (`elif` branches + MockCmd unit tests) are DEFERRED to Phase 6 (5.4 prototypes `cmd.*` directly in the smokes, mirroring `wt_align_smoke.py` calling `cmd.super` directly). **`molops.py` stays UNTOUCHED in 5.4** — confirmed via `git diff --name-only`.

## The 5.3 Composition (confirmed at checkpoint)

The 5.3 composition COLOR contract is confirmed:
- **5.4 OWNS the BEFORE** (mutant magenta spheres at edit-allowed `on_enter`, §5) — the active-site-reveal scene template (Type 3)
- **5.3 OWNS the AFTER** (WT-aligned restoration reveal at branch `on_enter`) — the `edit`→`load`→`align`→`present` mechanism
- **5.4 fills 5.3's `<mutant_color>` / `<wt_color>` slots** with `magenta` / `green` (OQ-2 OVERRIDE: magenta, NOT red)
- The before/after arc: **magenta → green = broken → restored**
- Enzyme-health framing (cast-reveal) kept SEPARATE from carbon-fate framing (anti-confusion, mirror 5.3 §5)

## The CO2-shed Anti-Confusion (confirmed at checkpoint)

- The highlight transfer (carbon → NADH → ATP) is a **narrative/visual device** (the "soul" follows the electrons per the metamorphosis reframing, `PROJECT.md` row 107). It does NOT assert the C14 carbon itself becomes ATP or NADH (scientifically WRONG — the carbon body is shed as CO2; only the electrons continue via NADH/FADH2 → ETC → ATP synthase).
- **C14-decay CONFIRMED DROPPED** (`PROJECT.md` row 108) — the sole timescale bad-ending is the cycle-trap `bad.cycle_trap_host_death` (spec.md line 18). No C14 radioactive-decay timescale.
- The `text_teaching` at `tca.co2_turn1`/`turn2`, `etc.entry`, `etc.atp_synthase`, and `end.true` MUST state: "Your carbon body is released as CO2; your electrons (the narrative 'soul') are carried by NADH/FADH2 to the ETC, which powers ATP synthase. The **cyan** highlight now follows your electrons, not your carbon — the metamorphosis." (Phase 7 finalizes the exact wording with citations.)

## The Dispatch Deferral to Phase 6

The 3 new-op molops dispatches (`set_color` / `label` / `set` `elif` branches + MockCmd unit tests) + the 5.3 `align` dispatch are DEFERRED to Phase 6 (OQ-1 APPROVE rec). 5.4 prototypes `cmd.*` directly in the smokes (hero_highlight_smoke.py calls `cmd.set_color`/`cmd.label`/`cmd.show`/`cmd.show_as`/`cmd.color`/`cmd.set` directly; scene_template_smoke.py likewise; both mirror `wt_align_smoke.py` calling `cmd.super` directly). **`molops.py` is UNTOUCHED** — confirmed via `git diff --name-only c14/pymol_layer/molops.py` (no changes). The AST gate stays clean.

## The Downstream Contracts (6/7/9/12)

- **Phase 6 (Qt UI MVP):** the 6 scene-type sequences + the cast-reveal sequence + the hero-highlight sequence (OQ-3 OVERRIDE: all-C cyan sticks + ball-and-stick + "YOU") + the shared palette (hero_cyan / gray / green / magenta / orange / gray80). Phase 6 lands the 3 new-op dispatches + the 5.3 `align` dispatch. Phase 6 MAY add: `cmd.scene` cache, multi-viewport side-by-side, Qt text overlays, first-visit-vs-re-entry branch, the OQ-6 warn+confirm Qt prompt.
- **Phase 7 (Content Authoring):** the 7-field enzyme metadata table + the 4-field hero metadata table (`hero_selector` / `hero_label="YOU"` / `hero_color="hero_cyan"` / `soul`). The `hero_cyan` RGB [0.0, 0.75, 0.75] is a PLACEHOLDER — Phase 7 source-approves the final palette. The CO2-shed + soul-transfer nodes' `on_enter` sequences + anti-confusion `text_teaching`. The OQ-6 warn+confirm is a Phase 6 UI contract (Phase 7 provides the `hero_selector` to avoid the fallback).
- **Phase 9 (Full Cast ~20+ enzymes):** the cast-reveal + Type 3 template GUARANTEE cast consistency by construction (every enzyme enters via the SAME template + palette, NOT ad-hoc). `hero_cyan` is the one cross-cutting color Phase 9 must keep consistent. Large-enzyme zoom composition is Phase 9.
- **Phase 12 (Ending Cutscene Rendering):** the Type 6 ending-tier templates compose with `cmd.ray` render — 4 distinct visual signatures (victory cyan / storage cyan / release gray / catastrophe red).

## No Fabricated Science

- **Zero real PDB IDs** in `05.4-CONVENTION.md` (only `pdb:TBD_*` placeholders — verified via `grep -oE '\b[1-9][0-9][A-Z0-9]{3}\b'` → empty).
- The `hero_cyan` RGB `[0.0, 0.75, 0.75]` is a **PLACEHOLDER** colorblind-safe cyan value pending Phase 7 source approval (OQ-3 OVERRIDE).
- The `magenta` / `green` / `gray` / `orange` / `gray80` are standard PyMOL named colors (HIGH confidence they dispatch correctly).
- The "which carbon is the hero at each stage" + "which residue is catalytic" + "which residue is the disease mutation" are Phase 7 per-claim cited-science decisions (HIGH-STAKES per `PROJECT.md` row 109).
- The smokes use placeholder structures with known-by-construction selectors (NOT real glucose PDBs, NOT heuristics).

## Task Commits

1. **Task 1 (previous executor): Re-run both smokes fresh + read §7/§8** — no commit (checkpoint automation only; both smokes reported SMOKE_RESULT: PASS; HEAD at `5f3bb11`)
2. **Task 2 (this continuation agent): Apply the 5 overrides + update smokes + SUMMARY + STATE + commit** — 3 commits:
   - `df3e55f` — `docs(05.4-04): apply checkpoint overrides (cyan hero, magenta mutant, YOU label, default+warn confirm, OQ-11 future note)` (convention revision)
   - `b116a0d` — `fix(05.4-04): update smokes to cyan/magenta/YOU palette + re-run PASS` (both smokes updated + re-ran PASS)
   - (pending) — `docs(05.4-04): complete design-review-checkpoint plan (SC4 APPROVED-WITH-ADJUSTMENTS)` (this SUMMARY + STATE)

## Deviations from Plan

### Auto-applied Adjustments (per the human's checkpoint overrides)

**1. Convention revision (5 overrides applied per the human's checkpoint response)**

- **Found during:** Task 2 (the human's checkpoint response contained 5 OVERRIDES)
- **Issue:** The convention spec (Plan 01) + both smokes (Plans 02/03) used the pre-checkpoint defaults (hero_gold / red mutant / "C14" label / FAIL LOUDLY / no OQ-11 future note). The human overrode 5 of the 11 OQs at the checkpoint.
- **Fix:** Revised `05.4-CONVENTION.md` (751 → 766 lines) applying all 5 overrides (OQ-2 magenta, OQ-3 cyan+all-C+ball-and-stick+YOU, OQ-4 "YOU", OQ-6 default+warn+confirm, OQ-11 future-note). Updated both smokes to match. Re-ran both smokes → SMOKE_RESULT: PASS.
- **Files modified:** `05.4-CONVENTION.md`, `tools/hero_highlight_smoke.py`, `tools/scene_template_smoke.py`
- **Verification:** Both smokes re-ran PASS (hero 29/29, scene 37/37); AST gate clean; molops.py/model.py untouched; zero real PDB IDs; no new fixtures.
- **Committed in:** `df3e55f` (convention) + `b116a0d` (smokes)

**2. Check name accuracy (Rule 1 — Bug)**

- **Found during:** Task 2 (updating the scene smoke check names)
- **Issue:** After changing the mutant color from `red` to `magenta`, two check names still contained "red" in the label string: `t3_mutant_color_red` and `comp_before_mutant_red_spheres`. The checks PASS (the color IS magenta) but the names are misleading.
- **Fix:** Renamed to `t3_mutant_color_magenta` and `comp_before_mutant_magenta_spheres` for clarity.
- **Files modified:** `tools/scene_template_smoke.py` (2 check name strings)
- **Committed in:** `b116a0d`

---

**Total deviations:** 2 (1 convention revision per the human's overrides + 1 check-name accuracy fix). No scope creep — all changes are the direct consequence of the human's checkpoint decisions. molops.py + model.py UNTOUCHED.

## Issues Encountered

None beyond the deviations above. Both smokes passed on the first headless run after the updates (no iteration needed — the OQ-3 design with `cmd.show` for ball-and-stick + `color hero_cyan elem C` for all-carbons-cyan + element-color-retention for non-carbons worked on the first try, empirically confirmed by `case2_noncarbon_element_color o1-color='oxygen'`).

## User Setup Required

None — this is a design-review + convention-revision + smoke-update phase; no external service configuration required.

## Next Phase Readiness

- **Phase 5.4 COMPLETE (4/4 plans):** all 4 success criteria delivered. SC1 (Plan 01 convention spec + Plan 02 hero-highlight smoke), SC2 (Plan 01 + Plan 03 scene-template smoke), SC3 (Plan 01 + Plan 03 cast-reveal smoke), SC4 (Plan 04 human design-review checkpoint APPROVED-WITH-ADJUSTMENTS). The convention is FROZEN for Phase 6/7/9/12.
- **Ready for Phase 6 (Qt UI MVP):** the §6.1 downstream contract (6 scene-type sequences + cast-reveal + hero-highlight OQ-3 OVERRIDE design + shared palette hero_cyan/gray/green/magenta/orange/gray80; 3 new ops set_color/label/set + align deferred; UI must NOT invent colors/reps/labels; the OQ-6 warn+confirm is a Phase 6 Qt prompt contract) tells Phase 6 exactly what to implement.
- **Ready for Phase 7 (Content Authoring):** the §6.2 downstream contract (7-field enzyme metadata + 4-field hero metadata with hero_label="YOU" / hero_color="hero_cyan"; the hero_cyan RGB is a PLACEHOLDER pending Phase 7 source approval; the CO2-shed + soul-transfer nodes' on_enter sequences + anti-confusion text_teaching) tells Phase 7 exactly what to author per asset.
- **Ready for Phase 9 (Full Cast):** the §6.3 downstream contract (cast-reveal + Type 3 template GUARANTEE cast consistency by construction; hero_cyan is the one cross-cutting color; large-enzyme zoom is Phase 9).
- **Ready for Phase 12 (Ending Cutscene):** the §6.4 downstream contract (Type 6 ending-templates compose with cmd.ray render; 4 distinct visual signatures: victory cyan / storage cyan / release gray / catastrophe red).
- **Follow-up work owed in 5.4:** NONE. OQ-1 NOT overridden — molops.py dispatch stays deferred to Phase 6. The OQ-6 warn+confirm is a Phase 6 UI contract, NOT 5.4 work. The OQ-11 3-sub-beat is a future enrichment, NOT 5.4 work.
- **Blockers/concerns:** none. The convention is FROZEN; both smokes PASS; molops.py untouched; the cyan/magenta/YOU palette is confirmed (with RGB placeholders pending Phase 7).

---
*Phase: 05.4-cast-hero-representation-design*
*Plan: 04 of 04*
*Completed: 2026-08-29*
