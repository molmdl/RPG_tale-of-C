---
phase: 05.4-cast-hero-representation-design
plan: 02
type: execute
wave: 2
depends_on:
  - 05.4-01
files_modified:
  - tools/hero_highlight_smoke.py
autonomous: true

must_haves:
  truths:
    - "The C14 hero highlight convention is headless-prototyped (SC1 mechanism) on placeholder structures: a loaded structure has the hero atom highlighted per the convention, verified by cmd.iterate on the hero selection (count == 1, expected element C, rep spheres visible, hero_gold color name round-trips via get_color_indices, label == 'C14')"
    - "All 3 structural cases are prototyped: (1) hero-alone (hero_atom single-atom object), (2) hero-in-substrate (name C1 sub-sele in a multi-atom substrate, per-atom show_as scoping leaves other atoms untouched), (3) hero-at-enzyme-cast (co-loaded substrate + placeholder enzyme; hero highlighted in the substrate while the enzyme keeps its cast cartoon gray)"
    - "The CO2-shed soul-transfer climax is prototyped on a placeholder: a co2 object with the hero highlight, then FADED (color gray80 + label cleared), then a nadh object with the highlight APPEARING (color hero_gold) — the transfer — WITHOUT asserting the carbon becomes NADH/ATP (a MANDATORY anti-confusion comment in the smoke states the transfer is a narrative device)"
    - "The headless prototype prints SMOKE_RESULT: PASS (the STDOUT sentinel; the bat always returns 0 so the sentinel is the only reliable verdict) — the 3-tier testability pattern: the smoke proves the cmd.set_color/cmd.label/cmd.show_as/cmd.color/cmd.set MECHANISM (select -> count==1 -> elem C -> spheres -> gold -> label C14 -> color-name round-trip), NOT real science (placeholder coords are arbitrary; real hero-selectors are Phase 7 content)"
  artifacts:
    - path: "tools/hero_highlight_smoke.py"
      provides: "headless hero-highlight prototype smoke (SC1 mechanism) — exercises all 3 structural cases + the CO2-shed soul-transfer on placeholder fixtures, calling cmd.set_color/cmd.label/cmd.show_as/cmd.color/cmd.set DIRECTLY (NOT via molops; the 3 new-op dispatches are deferred to Phase 6 per convention §2.2/OQ-1)"
      contains: "SMOKE_RESULT"
      min_lines: 200
  key_links:
    - from: "tools/hero_highlight_smoke.py"
      to: "tmp/pymol-src/modules/pymol/viewing.py (cmd.set_color / cmd.label / cmd.show_as / cmd.color) + setting.py (cmd.set) + querying.py (cmd.get_color_indices / cmd.count_atoms) + editing.py (cmd.iterate)"
      via: "direct cmd.* calls with # src citations on the line above each (the mechanism proof; NOT via molops.apply — the op=\"set_color\"/\"label\"/\"set\" dispatches are deferred to Phase 6 per convention §2.2/OQ-1)"
      pattern: "cmd\\.(set_color|label|show_as|color|set|get_color_indices|iterate|count_atoms)"
    - from: "tools/hero_highlight_smoke.py"
      to: "c14/data/assets/bundled/_smoke.pdb + _edit_smoke.pdb (REUSED existing fixtures, NOT new — per RESEARCH-api §I OQ-5)"
      via: "c14.paths.data_path(...) cwd-independent resolution (NOT relative paths — PyMOL resolves against cmd.exe cwd, breaks headlessly)"
      pattern: "c14\\.paths\\.data_path"
    - from: "tools/hero_highlight_smoke.py"
      to: ".planning/phases/05.4-cast-hero-representation-design/05.4-CONVENTION.md §3 (the hero-highlight sequence contract)"
      via: "the smoke implements the §3.3 semantic sequence (set_color hero_gold -> show_as spheres -> set sphere_scale -> color hero_gold -> scoped dim gray80 -> label C14) on the 3 §3.2 structural cases + the §3.5 CO2-shed climax"
      pattern: "hero_gold|sphere_scale|gray80"
---

<objective>
Headless-prototype the C14 hero highlight convention (SC1 mechanism) on placeholder structures.

Purpose: Prove the hero-highlight MECHANISM works end-to-end headlessly (set_color → show_as spheres → set sphere_scale → color → label → verify count==1/elem C/spheres/gold/C14) across all 3 structural cases (hero-alone, hero-in-substrate, hero-at-enzyme-cast) + the CO2-shed soul-transfer climax, so the convention spec from Plan 01 is backed by a runnable proof. This is the 3-tier testability pattern: the smoke proves the real PyMOL API contract on a placeholder, NOT real science (Phase 7 sources real structures + the real hero-selector per asset).

Output: `tools/hero_highlight_smoke.py` printing `SMOKE_RESULT: PASS`, REUSING the existing `_smoke.pdb` (3-atom C1/O1/C2) + `_edit_smoke.pdb` (17-atom ALA-GLY) fixtures (NO new fixtures — per RESEARCH-api §I OQ-5). Calls `cmd.set_color`/`cmd.label`/`cmd.show_as`/`cmd.color`/`cmd.set` DIRECTLY (NOT via molops — the 3 new-op dispatches are deferred to Phase 6 per convention §2.2/OQ-1, mirroring how `wt_align_smoke.py` calls `cmd.super` directly).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.4-cast-hero-representation-design/05.4-CONVENTION.md
@.planning/phases/05.4-cast-hero-representation-design/05.4-RESEARCH-pymol-representation-api.md
@.planning/phases/05.4-cast-hero-representation-design/05.4-RESEARCH-hero-highlight-design.md

# Established patterns to follow (read these before writing the smoke)
@tools/wt_align_smoke.py
@tools/molops_smoke.py
@tools/run_headless.sh
@c14/paths.py
@c14/data/assets/bundled/_smoke.pdb
@c14/data/assets/bundled/_edit_smoke.pdb
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write tools/hero_highlight_smoke.py — the 3 structural cases + the CO2-shed soul-transfer + post-conditions</name>
  <files>tools/hero_highlight_smoke.py</files>
  <action>
Create `tools/hero_highlight_smoke.py` following the `tools/wt_align_smoke.py` pattern EXACTLY (read `tools/wt_align_smoke.py` first — it is the established template for a direct-`cmd.*` smoke with deferred dispatch). The smoke is a pure `pymol.cmd.*` script (NO Qt) that calls `cmd.set_color`/`cmd.label`/`cmd.show_as`/`cmd.color`/`cmd.set`/`cmd.count_atoms`/`cmd.iterate`/`cmd.get_color_indices` DIRECTLY (NOT via `molops.apply` — the 3 new-op dispatches are deferred to Phase 6 per convention §2.2/OQ-1; this smoke proves the mechanism the dispatches will eventually wrap, mirroring `wt_align_smoke.py` calling `cmd.super` directly).

**Header + boilerplate (mirror wt_align_smoke.py):**
- Module docstring: "Phase 5.4 Plan 02 C14 hero highlight headless prototype (SC1 mechanism). Pure pymol.cmd.* script (NO Qt) that exercises the hero-highlight MECHANISM on placeholder fixtures across the 3 structural cases (hero-alone / hero-in-substrate / hero-at-enzyme-cast) + the CO2-shed soul-transfer climax. Proves the MECHANISM (select -> count==1 -> elem C -> spheres -> hero_gold -> label C14 -> color-name round-trip), NOT real science (placeholder coords are arbitrary; real hero-selectors are Phase 7 content)."
- Reuse the CRITICAL CONTRACT RULES comment block from `wt_align_smoke.py` VERBATIM (Gotcha #1: process always exits 0, SMOKE_RESULT sentinel is the only verdict; Gotcha #2: `__file__` in a PyMOL-run script is wrong, use `os.getcwd()` + `c14.paths`; Gotcha #6: `pymol.finish_launching()` before any cmd.* call).
- Add a MANDATORY anti-confusion comment block near the top (from AGENTS.md + PROJECT.md row 107): "HERO-IDENTITY ANTI-CONFUSION: the hero is a CARBON ATOM; 'C14' is its isotope tracking LABEL, not its fate. The carbon body is shed as CO2; its electrons (the 'soul') flow via NADH/FADH2 -> ETC -> ATP. The CO2-shed soul-transfer stages below are a NARRATIVE/VISUAL DEVICE — they do NOT assert the carbon becomes NADH or ATP (scientifically WRONG). The highlight on the NADH/ATP placeholder represents the ELECTRONS (the soul), not the carbon. C14-decay was DROPPED."
- `sys.path.insert(0, os.getcwd())` then `import pymol`, `from pymol import cmd`, `import c14.paths`. Then `pymol.finish_launching()`. (NO EditOps import needed — this smoke does NOT edit; it only highlights. If a co2/nadh placeholder object is needed, build it via `cmd.create` from the existing fixtures, NOT a new file.)
- `FAILS = []` + `def check(name, ok, detail=""):` printing `SMOKE: {PASS|FAIL} {name} {detail}` and appending to FAILS on failure (copy from wt_align_smoke.py).
- Add a helper `def color_name_for(sele):` that runs `cmd.iterate(sele, "cidx.append(color)", space={"cidx": []})` (use a NON-colliding collector name `cidx` — RESEARCH-api §F Pitfall 1: the collector key MUST NOT collide with an atom-property name like `color`), builds `idx2name = {i: n for (n, i) in cmd.get_color_indices()}` (RESEARCH-api §A.2), and returns the name for the first atom's color index (or None). This is the color-name round-trip post-condition primitive (RESEARCH-api §D.1/Pitfall 4: do NOT assert a hardcoded index — assert the NAME).

**Stage 0 — define the hero_gold color (the §2.3 palette + §3.3 step 1):**
- `# src: tmp/pymol-src/modules/pymol/viewing.py:2107 cmd.set_color` then `cmd.set_color("hero_gold", [0.90, 0.62, 0.0])` (Okabe-Ito orange; idempotent).
- Verify the named color round-trips: `# src: .../querying.py:843 cmd.get_color_indices` then `idx2name = {i: n for (n, i) in cmd.get_color_indices()}`; `# src: .../querying.py:851 cmd.get_color_index` then `hg_idx = cmd.get_color_index("hero_gold")`; `check("hero_gold_defined", hg_idx in idx2name and idx2name[hg_idx] == "hero_gold", "idx=%r name=%r" % (hg_idx, idx2name.get(hg_idx)))`.

**Stage 1 — Case 1 hero-alone (the intro.preface pattern; RESEARCH-api §C.1 Strategy A):**
- Load `_smoke.pdb` as `mol` (3 atoms C1/O1/C2). `# src: .../importing.py:635 cmd.load`. `check("load_smoke", cmd.count_atoms("mol") == 3, ...)`.
- Extract the hero as its OWN single-atom object: `# src: tmp/pymol-src/modules/pymol/creating.py:1001 cmd.create` then `cmd.create("hero_atom", "mol and name C1")`. (RESEARCH-api §C.1: empirically confirmed — `create("hero_atom", "mol and name C1")` makes a 1-atom object.)
- Apply the hero-highlight sequence (convention §3.3, the §3.3 step 5 dim OMITTED — a single atom has no non-hero atoms):
  - `# src: .../viewing.py:528 cmd.show_as` then `cmd.show_as("spheres", "hero_atom")`
  - `# src: tmp/pymol-src/modules/pymol/setting.py:183 cmd.set` then `cmd.set("sphere_scale", 1.0, "hero_atom")`
  - `# src: .../viewing.py:1858 cmd.color` then `cmd.color("hero_gold", "hero_atom")`
  - `# src: .../viewing.py:1332 cmd.label` then `cmd.label("hero_atom", '"C14"')` (the quoted-string expression — RESEARCH-api §F Pitfall 2)
- Post-conditions (convention §3 + RESEARCH-api §D.1):
  - `# src: .../querying.py:1412 cmd.count_atoms` then `check("case1_count_1", cmd.count_atoms("hero_atom") == 1, ...)`
  - elem C: `elems=[]; cmd.iterate("hero_atom", "elems.append(elem)", space={"elems": elems}); check("case1_elem_C", elems == ["C"], ...)` (`# src: .../editing.py:1490 cmd.iterate`)
  - rep spheres: `check("case1_rep_spheres", cmd.count_atoms("hero_atom and rep spheres") == 1, ...)` (the `rep <name>` keyword, RESEARCH-api §A.1)
  - color name round-trip: `check("case1_color_hero_gold", color_name_for("hero_atom") == "hero_gold", ...)`
  - label: `lbls=[]; cmd.iterate("hero_atom", "lbls.append(label)", space={"lbls": lbls}); check("case1_label_C14", lbls == ["C14"], ...)`

**Stage 2 — Case 2 hero-in-substrate (the intro.shell_glucose/gly.start pattern; RESEARCH-api §C.1 Strategy B + §A.1 per-atom scoping):**
- Reuse the `mol` object (3 atoms). The hero is `name C1` IN the substrate (a deterministic sub-sele, RESEARCH-api §C.1 Strategy B). Apply the hero-highlight on the SUB-SELE (per-atom scoping leaves O1/C2 untouched — empirically confirmed RESEARCH-api §A.1/§A.6):
  - `cmd.show_as("spheres", "mol and name C1")` (per-atom: only C1 → spheres; O1/C2 keep their existing rep)
  - `cmd.set("sphere_scale", 1.0, "mol and name C1")`
  - `cmd.color("hero_gold", "mol and name C1")`
  - The scoped dim (convention §3.3 step 5): `cmd.color("gray80", "mol and not name C1")` (dim O1+C2 — the non-hero atoms IN the hero-bearing object)
  - `cmd.label("mol and name C1", '"C14"')`
- Post-conditions:
  - `check("case2_hero_count_1", cmd.count_atoms("mol and name C1") == 1, ...)`
  - `check("case2_hero_elem_C", ... iterate elem == ["C"] ...)`
  - `check("case2_hero_rep_spheres", cmd.count_atoms("mol and name C1 and rep spheres") == 1, ...)`
  - `check("case2_hero_color_hero_gold", color_name_for("mol and name C1") == "hero_gold", ...)`
  - `check("case2_hero_label_C14", ... iterate label == ["C14"] ...)`
  - per-atom scoping PROOF (RESEARCH-api §A.1): `check("case2_others_not_spheres", cmd.count_atoms("mol and not name C1 and rep spheres") == 0, ...)` (O1/C2 did NOT get spheres — the show_as was scoped)
  - scoped dim PROOF: `check("case2_dim_gray80", color_name_for("mol and not name C1") == "gray80", ...)` (O1/C2 dimmed)

**Stage 3 — Case 3 hero-at-enzyme-cast (the gly.pfk/pyr.pdh pattern; RESEARCH-hero-highlight §A case 3a co-load):**
- Load `_edit_smoke.pdb` as `enzyme` (17-atom ALA-GLY — the placeholder cast/enzyme). `# src: .../importing.py:635 cmd.load`. `check("case3_load_enzyme", cmd.count_atoms("enzyme") > 0, ...)`.
- Apply the cast representation (convention §5 — the enzyme keeps its OWN color, NOT dimmed): `cmd.show_as("cartoon", "enzyme")`; `cmd.color("gray", "enzyme")`. (RESEARCH-api §A.6: `rep cartoon=17` on a 2-residue peptide — confirmed.)
- Co-load the substrate (case 3a): `cmd.create("substrate", "mol and name C1")` (reuse the C1 from Stage 1's `mol` — a 1-atom substrate containing the hero). Apply the hero-highlight on the substrate's hero (convention §3.3 — the dim scoped to the SUBSTRATE, NOT the enzyme):
  - `cmd.show_as("spheres", "substrate")` (1 atom)
  - `cmd.color("hero_gold", "substrate")`
  - `cmd.label("substrate", '"C14"')`
- Post-conditions:
  - `check("case3_enzyme_rep_cartoon", cmd.count_atoms("enzyme and rep cartoon") > 0, ...)` (the enzyme keeps its cast cartoon)
  - `check("case3_enzyme_color_gray", color_name_for("enzyme and name CA") == "gray", ...)` (the enzyme is NOT dimmed — the dim is scoped to the substrate only)
  - `check("case3_hero_in_substrate", cmd.count_atoms("substrate and elem C") == 1, ...)`
  - `check("case3_hero_spheres", cmd.count_atoms("substrate and rep spheres") == 1, ...)`
  - `check("case3_hero_color_hero_gold", color_name_for("substrate") == "hero_gold", ...)`
  - `check("case3_hero_label_C14", ... iterate label == ["C14"] ...)`

**Stage 4 — the CO2-shed soul-transfer climax (convention §3.5; the dramatic peak):**
- Build a placeholder `co2` object (the departing carbon body — the "chrysalis"): `cmd.create("co2", "mol and name C1")` (1 carbon atom). Show it with the hero highlight ONE final time: `cmd.show_as("spheres", "co2")`; `cmd.color("hero_gold", "co2")`; `cmd.label("co2", '"C14 (farewell)"')`. Verify: `check("climax_co2_highlighted", color_name_for("co2") == "hero_gold", ...)`.
- FADE the carbon body (the gold leaves the CO2): `cmd.color("gray80", "co2")`; `cmd.label("co2", '""')` (clear the label — RESEARCH-api §A.4: empty expression clears). Verify the fade: `check("climax_co2_faded", color_name_for("co2") == "gray80", ...)`; `lbls=[]; cmd.iterate("co2", "lbls.append(label)", space={"lbls": lbls}); check("climax_co2_label_cleared", lbls == [""], ...)`.
- TRANSFER the soul to the NADH/electron carrier (the gold appears on a NEW object — the electrons, NOT the carbon): `cmd.create("nadh", "mol and name C1")` (placeholder; rename conceptually — it represents the electron carrier). `cmd.show_as("spheres", "nadh")`; `cmd.color("hero_gold", "nadh")`; `cmd.label("nadh", '"soul (electrons)"')`. Verify the transfer: `check("climax_nadh_highlighted", color_name_for("nadh") == "hero_gold", ...)`.
- Add a MANDATORY inline comment at the transfer step (restate the §3.6 anti-confusion): "# ANTI-CONFUSION: the gold on `nadh` represents the hero's ELECTRONS (the narrative 'soul'), NOT the carbon atom. The carbon body (co2) was shed + faded. This is a narrative device; the carbon does NOT become NADH or ATP."

**Final verdict (copy from wt_align_smoke.py):**
```python
print("SMOKE_RESULT: {0}".format("FAIL" if FAILS else "PASS"))
if FAILS:
    print("SMOKE_FAILED_STAGES: " + ", ".join(FAILS))
```

**Source-citation convention (MANDATORY, established Phase 3):** every DIRECT `cmd.*` call (`cmd.set_color`, `cmd.label`, `cmd.show_as`, `cmd.color`, `cmd.set`, `cmd.count_atoms`, `cmd.iterate`, `cmd.get_color_indices`, `cmd.get_color_index`, `cmd.create`, `cmd.load`) carries a `# src: tmp/pymol-src/modules/pymol/<file>.py:<line> cmd.<name>` comment on the line directly above. Verify each line number against `tmp/pymol-src/modules/pymol/` before finalizing — `viewing.py:2107` set_color, `viewing.py:1332` label, `viewing.py:528` show_as, `viewing.py:1858` color, `setting.py:183` set, `querying.py:1412` count_atoms, `editing.py:1490` iterate, `querying.py:843` get_color_indices, `querying.py:851` get_color_index, `creating.py:1001` create, `importing.py:635` load.

Do NOT add the `op="set_color"`/`op="label"`/`op="set"` dispatches to `c14/pymol_layer/molops.py` (deferred to Phase 6 per convention §2.2/OQ-1). This smoke calls `cmd.*` DIRECTLY. Do NOT create new fixture files (reuse `_smoke.pdb` + `_edit_smoke.pdb` per RESEARCH-api §I OQ-5).
  </action>
  <verify>
`python3.6 -m py_compile tools/hero_highlight_smoke.py` exits 0 (pure-Python syntax valid). `grep -c "# src: tmp/pymol-src" tools/hero_highlight_smoke.py` shows >= 8 source citations (every direct cmd.* call cited). `grep -qi "ANTI-CONFUSION\|narrative device\|electrons.*not the carbon\|NOT.*becomes.*NADH\|NOT.*becomes.*ATP" tools/hero_highlight_smoke.py` matches (the mandatory anti-confusion comment is present). `grep -q "SMOKE_RESULT" tools/hero_highlight_smoke.py` matches. `grep -qi "get_color_indices\|color_name_for" tools/hero_highlight_smoke.py` matches (the color-name round-trip post-condition primitive is used — NOT a hardcoded index). `grep -qi "hero_gold\|0.90.*0.62\|Okabe" tools/hero_highlight_smoke.py` matches (the hero color is defined). Confirm NO new fixture files created: `git status --porcelain c14/data/assets/bundled/` shows no new untracked .pdb files (only `_smoke.pdb`/`_edit_smoke.pdb` reused read-only). Confirm `molops.py` NOT modified: `git diff --name-only c14/pymol_layer/molops.py` shows no changes.
  </verify>
  <done>
`tools/hero_highlight_smoke.py` exists, passes `python3.6 -m py_compile`, has >= 8 `# src:` citations, includes the MANDATORY anti-confusion comment, uses the color-name round-trip (not hardcoded index), reuses existing fixtures (no new .pdb files), and does NOT modify `molops.py`. Ready for Task 2's headless run.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the smoke headlessly + assert SMOKE_RESULT: PASS</name>
  <files>tools/hero_highlight_smoke.py</files>
  <action>
Run the smoke headlessly via the established harness + iterate until PASS. This task does NOT modify the smoke (Task 1 wrote it); it runs + diagnoses + fixes runtime issues only (common fixes listed below).

**Run + verify:**
1. Syntax check (pure-Python, WSL): `python3.6 -m py_compile tools/hero_highlight_smoke.py` (must exit 0).
2. Run headlessly via the established harness: `bash tools/run_headless.sh tools/hero_highlight_smoke.py`. The harness greps `^SMOKE_RESULT: PASS` from the captured stdout (the bat always returns 0, Gotcha #1 — the sentinel is the only reliable verdict). If any stage FAILS, read `/tmp/opencode/hero_highlight_smoke.txt` (the harness prints this path), diagnose, fix the smoke, and re-run.
3. Common runtime fixes (from RESEARCH-api §F pitfalls + the wt_align_smoke.py precedent):
   - **Pitfall 1 (iterate space collector collision):** if you see `AttributeError: 'str' object has no attribute 'append'` (or `'int'`), the collector key collides with an atom-property name. Rename the collector to a non-colliding name (`cidx`, `lbls`, `elems` — NOT `color`/`label`/`elem`/`segi`/`b`/`flags`). The `color_name_for` helper uses `cidx`; the label/elem checks use `lbls`/`elems`.
   - **Pitfall 2 (label quoted-string):** if the label is empty or raises `NameError`, the expression was a bare word. Use `cmd.label(sele, '"C14"')` (single-quoted Python string containing a double-quoted PyMOL literal). Verify against `tmp/pymol-src/modules/pymol/viewing.py:1364` ("All strings in the expression must be explicitly quoted").
   - **Pitfall 4 (color index session-unstable):** do NOT assert `hg_idx == 5388` (the index is session-allocated). Assert the NAME via the `get_color_indices` round-trip (the `color_name_for` helper). If the name round-trip fails, verify `cmd.set_color` ran before the color was applied + before `get_color_indices` was called.
   - **Pitfall 7 (deleted object raises):** if a stage deletes an object, use `count_atoms("?obj")` (the `?`-prefix is safe; returns 0 for non-existent). Bare `count_atoms("obj")` on a deleted object RAISES.
   - **`cmd.create` self-copy is destructive:** never `cmd.create("mol", "mol")`. Always create a NEW object name from a selection of a DIFFERENT object (`cmd.create("hero_atom", "mol and name C1")`).
   - **Gotcha #2 (fixture path):** use `c14.paths.data_path("data", "assets", "bundled", "_smoke.pdb")` (cwd-independent), NOT a relative path (PyMOL resolves against cmd.exe cwd).
4. Iterate until `bash tools/run_headless.sh tools/hero_highlight_smoke.py` prints `PASSED` (the harness greps `^SMOKE_RESULT: PASS`) with `SMOKE: PASS` for every stage.
5. Confirm the AST gate still passes (no new domain-tier pymol import — the smoke is in `tools/` and uses `pymol.cmd` directly, allowed for headless smokes; must NOT be imported by `c14/` domain tier): `python3.6 tools/check_imports.py` (exit 0).
6. Confirm `molops.py` was NOT modified: `git diff --name-only c14/pymol_layer/molops.py` shows no changes (the 3 new-op dispatches are deferred to Phase 6).

Do NOT add the `op="set_color"`/`op="label"`/`op="set"` dispatches to `molops.py` (deferred to Phase 6 per convention §2.2/OQ-1). If the human overrides OQ-1 at the Plan 04 checkpoint to "add in 5.4", a follow-up revision task will be added — do NOT preemptively add it here.
  </action>
  <verify>
`bash tools/run_headless.sh tools/hero_highlight_smoke.py` prints `PASSED` (the harness greps `^SMOKE_RESULT: PASS`). The captured output (`/tmp/opencode/hero_highlight_smoke.txt`) shows `SMOKE: PASS` for every stage (Stage 0 hero_gold_defined; Case 1 count_1/elem_C/rep_spheres/color_hero_gold/label_C14; Case 2 hero_count_1/hero_elem_C/hero_rep_spheres/hero_color_hero_gold/hero_label_C14/others_not_spheres/dim_gray80; Case 3 enzyme_rep_cartoon/enzyme_color_gray/hero_in_substrate/hero_spheres/hero_color_hero_gold/hero_label_C14; Stage 4 climax co2_highlighted/co2_faded/co2_label_cleared/nadh_highlighted) and a final `SMOKE_RESULT: PASS`. `python3.6 tools/check_imports.py` exits 0 (AST gate clean). `git diff --name-only c14/pymol_layer/molops.py` shows NO changes (the 3 new-op dispatches deferred to Phase 6).
  </verify>
  <done>
`tools/hero_highlight_smoke.py` runs headlessly via `bash tools/run_headless.sh tools/hero_highlight_smoke.py` printing `SMOKE_RESULT: PASS` with all stage checks passing (hero_gold defined; 3 structural cases each with count==1/elem C/rep spheres/color-name round-trip/label C14 + the per-atom-scoping + scoped-dim proofs; the CO2-shed soul-transfer fade + transfer). The AST gate stays clean. `molops.py` is NOT modified. SC1 mechanism proven headlessly on placeholders.
  </done>
</task>

</tasks>

<verification>
- `tools/hero_highlight_smoke.py` passes `python3.6 -m py_compile` and runs headlessly with `SMOKE_RESULT: PASS` (all stage checks green) — SC1 mechanism delivered.
- Every direct `cmd.*` call carries a `# src:` citation (source-citation convention held).
- The color-name round-trip via `get_color_indices` is used (NOT a hardcoded color index — Pitfall 4 avoided).
- The MANDATORY anti-confusion comment is present (the CO2-shed soul-transfer is a narrative device, NOT carbon→NADH/ATP).
- All 3 structural cases (hero-alone / hero-in-substrate / hero-at-enzyme-cast) are prototyped with post-conditions.
- The CO2-shed soul-transfer climax (fade + transfer) is prototyped on a placeholder.
- NO new fixture files created (reuse `_smoke.pdb` + `_edit_smoke.pdb` per RESEARCH-api §I OQ-5).
- The AST gate (`tools/check_imports.py`) stays clean.
- `c14/pymol_layer/molops.py` is NOT modified (the 3 new-op dispatches deferred to Phase 6 per convention §2.2/OQ-1).
- No fabricated science: placeholder structures only (no real PDB IDs, no network fetches).
</verification>

<success_criteria>
- SC1 mechanism delivered: the hero highlight is headless-prototyped — a loaded structure has the hero atom highlighted per the convention, verified by `cmd.iterate` on the hero selection (count == 1, expected element C, rep spheres visible, hero_gold color name round-trips, label == "C14").
- The 3 structural cases + the CO2-shed soul-transfer are prototyped.
- The prototype proves the `cmd.set_color`/`cmd.label`/`cmd.show_as`/`cmd.color`/`cmd.set` MECHANISM on a placeholder, NOT real science (Phase 7 sources real structures + the real hero-selector per asset).
- No code outside `tools/hero_highlight_smoke.py` is modified; `molops.py` untouched (dispatch deferred to Phase 6 unless the human overrides OQ-1 at Plan 04).
</success_criteria>

<output>
After completion, create `.planning/phases/05.4-cast-hero-representation-design/05.4-02-SUMMARY.md`
</output>
