---
phase: 05.4-cast-hero-representation-design
plan: 03
type: execute
wave: 2
depends_on:
  - 05.4-01
files_modified:
  - tools/scene_template_smoke.py
autonomous: true

must_haves:
  truths:
    - "The scene-template library is headless-prototyped (SC2 mechanism): each of the 6 scene TYPES (preface / substrate-traversal / active-site-reveal=cast-reveal / branch-point / rng-shuffle / ending-4-tiers) is a MolAction SEQUENCE applied headlessly on a placeholder/bundled structure with post-condition assertions (expected representation visible via count_atoms('obj & rep <rep>'), expected residue count in named selections, label presence via iterate, mutant red spheres rep for the cast-reveal disease variant)"
    - "The cast-reveal convention is headless-prototyped (SC3 mechanism): a placeholder enzyme enters as full gray cartoon -> zoom active site -> WT catalytic green sticks -> mutant red SPHERES (rep redundancy = colorblind-safe) -> cofactor orange sticks -> cmd.label nameplate; the structural variant (citrate_synthase, NO mutant) omits the mutant-spheres step; the 5.3 composition is demonstrated (5.4 BEFORE mutant red spheres + a loaded _wt placeholder colored green = the AFTER restoration, filling 5.3's <wt_color> slot)"
    - "The scene templates are MolAction SEQUENCES applied via direct cmd.* calls (NOT cmd.scene — session state is not save/load-safe), proving the convention §4.1 decision; the headless prototype prints SMOKE_RESULT: PASS"
    - "The 4 ending-tier templates are prototyped (true/good/normal/bad) with tier-specific signatures (true=ATP+hero_gold+soul label, good=retained+hero_gold, normal=CO2+gray, bad=broken+red+cartoon), each with post-conditions"
  artifacts:
    - path: "tools/scene_template_smoke.py"
      provides: "headless scene-template + cast-reveal prototype smoke (SC2+SC3 mechanism) — exercises all 6 scene types + the cast-reveal + the 5.3 composition on placeholder fixtures, calling cmd.* DIRECTLY (NOT via molops; the new-op dispatches are deferred to Phase 6 per convention §2.2/OQ-1)"
      contains: "SMOKE_RESULT"
      min_lines: 250
  key_links:
    - from: "tools/scene_template_smoke.py"
      to: "tmp/pymol-src/modules/pymol/viewing.py (cmd.show/show_as/color/zoom/label) + selecting.py (cmd.select) + querying.py (cmd.count_atoms) + editing.py (cmd.iterate)"
      via: "direct cmd.* calls with # src citations (the mechanism proof; NOT via molops.apply — the op=\"label\"/\"set_color\" dispatches deferred to Phase 6; scene templates are MolAction sequences NOT cmd.scene per convention §4.1)"
      pattern: "cmd\\.(show|show_as|color|zoom|label|select|count_atoms|iterate|set_color)"
    - from: "tools/scene_template_smoke.py"
      to: "c14/data/assets/bundled/_smoke.pdb + _edit_smoke.pdb + _wt_align_wt.pdb (REUSED existing fixtures; _wt_align_wt.pdb for the 5.3 composition AFTER case)"
      via: "c14.paths.data_path(...) cwd-independent resolution"
      pattern: "c14\\.paths\\.data_path"
    - from: "tools/scene_template_smoke.py"
      to: ".planning/phases/05.4-cast-hero-representation-design/05.4-CONVENTION.md §4 (scene templates) + §5 (cast-reveal) + 05.3-CONVENTION.md (the <wt_color>/<mutant_color> composition)"
      via: "the smoke implements the §4 6-type sequences + the §5 cast-reveal sequence + the 5.3 composition (mutant red spheres BEFORE + WT green AFTER)"
      pattern: "cartoon|sticks|spheres|gray|green|red|orange"
---

<objective>
Headless-prototype the scene-template library (SC2 mechanism) + the cast-reveal convention (SC3 mechanism) on placeholder structures.

Purpose: Prove the scene-template MECHANISM works end-to-end headlessly — each of the 6 scene types (preface / substrate-traversal / active-site-reveal=cast-reveal / branch-point / rng-shuffle / ending-4-tiers) applies as a MolAction SEQUENCE (NOT `cmd.scene`) producing the expected reps/colors/labels, AND the cast-reveal (full-then-zoom gray cartoon → active site → green catalytic sticks → red mutant spheres → orange cofactor → nameplate label) + the 5.3 composition (BEFORE mutant red + AFTER WT green) are demonstrated. This is the 3-tier testability pattern: the smoke proves the real PyMOL API contract on placeholders, NOT real science (Phase 7 sources real structures + the real active-site/catalytic/mutant selections per enzyme).

Output: `tools/scene_template_smoke.py` printing `SMOKE_RESULT: PASS`, REUSING the existing `_smoke.pdb` (3-atom), `_edit_smoke.pdb` (17-atom ALA-GLY), and `_wt_align_wt.pdb` (14-atom GLY-GLY, for the 5.3 composition AFTER case) fixtures (NO new fixtures). Calls `cmd.*` DIRECTLY (NOT via molops — the new-op dispatches are deferred to Phase 6 per convention §2.2/OQ-1).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.4-cast-hero-representation-design/05.4-CONVENTION.md
@.planning/phases/05.4-cast-hero-representation-design/05.4-RESEARCH-scene-templates-cast-reveal-design.md
@.planning/phases/05.4-cast-hero-representation-design/05.4-RESEARCH-pymol-representation-api.md

# The 5.3 composition boundary (5.4 fills <mutant_color>/<wt_color>; the _wt_align_wt.pdb fixture is reused for the AFTER case)
@.planning/phases/05.3-wt-aligned-structure-load-convention/05.3-CONVENTION.md

# Established patterns to follow (read these before writing the smoke)
@tools/wt_align_smoke.py
@tools/molops_smoke.py
@tools/run_headless.sh
@c14/paths.py
@c14/data/assets/bundled/_smoke.pdb
@c14/data/assets/bundled/_edit_smoke.pdb
@c14/data/assets/bundled/_wt_align_wt.pdb
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write tools/scene_template_smoke.py — the 6 scene types + the cast-reveal + the 5.3 composition + post-conditions</name>
  <files>tools/scene_template_smoke.py</files>
  <action>
Create `tools/scene_template_smoke.py` following the `tools/wt_align_smoke.py` pattern EXACTLY (read `tools/wt_align_smoke.py` first). The smoke is a pure `pymol.cmd.*` script (NO Qt) that applies each scene-template MolAction SEQUENCE via direct `cmd.*` calls (NOT via `molops.apply` — the new-op dispatches are deferred to Phase 6 per convention §2.2/OQ-1; NOT via `cmd.scene` — session state is not save/load-safe per convention §4.1).

**Header + boilerplate (mirror wt_align_smoke.py):**
- Module docstring: "Phase 5.4 Plan 03 scene-template + cast-reveal headless prototype (SC2+SC3 mechanism). Pure pymol.cmd.* script (NO Qt) that exercises all 6 scene TYPES + the cast-reveal + the 5.3 composition on placeholder fixtures. Each template is a MolAction SEQUENCE applied via direct cmd.* (NOT cmd.scene — session state is not save/load-safe). Proves the MECHANISM (rep visible + color name + label + named-selection count), NOT real science (placeholder structures; real active-site/catalytic/mutant selections are Phase 7 content)."
- Reuse the CRITICAL CONTRACT RULES comment block from `wt_align_smoke.py` VERBATIM (Gotcha #1: SMOKE_RESULT sentinel; Gotcha #2: `__file__` wrong, use `os.getcwd()` + `c14.paths`; Gotcha #6: `pymol.finish_launching()`).
- Add a hero-identity anti-confusion comment (from AGENTS.md): the cast-reveal shows the ENZYME's health (mutant vs WT), NOT the hero carbon's fate. The 5.3 composition shows the enzyme restored; at pyr.pdh the carbon IS shed as CO2 while the hero's electrons continue — keep enzyme-health and carbon-fate framing SEPARATE.
- `sys.path.insert(0, os.getcwd())` then `import pymol`, `from pymol import cmd`, `import c14.paths`. Then `pymol.finish_launching()`. (NO EditOps import — this smoke does NOT edit; it applies scene templates. The 5.3 composition AFTER case loads the pre-built `_wt_align_wt.pdb` fixture as the "restored WT" — it does NOT run the align, just demonstrates the COLOR contract.)
- `FAILS = []` + `def check(name, ok, detail=""):` (copy from wt_align_smoke.py).
- Add the `color_name_for(sele)` helper (same as Plan 02's smoke — `cmd.iterate` color-index → `get_color_indices` index→name round-trip; use NON-colliding collector `cidx`; RESEARCH-api §F Pitfall 1). Also add a `label_text_for(sele)` helper (`cmd.iterate` label → first atom's label string; collector `lbls`).

**Stage 0 — define the palette (convention §2.3):**
- `# src: .../viewing.py:2107 cmd.set_color` then `cmd.set_color("hero_gold", [0.90, 0.62, 0.0])`. (gray/green/red/orange/gray80 are standard PyMOL named colors — no set_color needed for them; verify `cmd.get_color_index("green")` is not None as a sanity check.)

**Type 1 — preface (convention §4.3 Type 1; nodes intro.preface etc):**
- Load `_smoke.pdb` → create `hero_atom` from `name C1` (`cmd.create("hero_atom", "smoke and name C1")`). Load `_edit_smoke.pdb` as `aa_cast` (the 20-AA cast placeholder). Apply: `cmd.show_as("spheres", "hero_atom")`; `cmd.color("hero_gold", "hero_atom")`; `cmd.label("hero_atom", '"C14"')`; `cmd.show_as("cartoon", "aa_cast")`; `cmd.color("gray", "aa_cast")`; `cmd.zoom("all")`.
- Post: `check("t1_hero_count_1", cmd.count_atoms("hero_atom") == 1, ...)`; `check("t1_hero_spheres", cmd.count_atoms("hero_atom and rep spheres") == 1, ...)`; `check("t1_cast_cartoon", cmd.count_atoms("aa_cast and rep cartoon") > 0, ...)`; `check("t1_cast_loaded", cmd.count_atoms("aa_cast") > 0, ...)`.

**Type 2 — substrate-traversal (convention §4.3 Type 2; nodes gly.start etc):**
- Load `_smoke.pdb` as `substrate` (3 atoms). Apply: `cmd.show_as("sticks", "substrate")`; `cmd.color("gray", "substrate")`; hero-highlight on `substrate and name C1` (`cmd.show_as("spheres", "substrate and name C1")` — per-atom scoping; `cmd.color("hero_gold", "substrate and name C1")`; `cmd.label("substrate and name C1", '"C14"')`); `cmd.zoom("substrate")`.
- Post: `check("t2_substrate_sticks", cmd.count_atoms("substrate and rep sticks") > 0, ...)`; `check("t2_hero_spheres", cmd.count_atoms("substrate and name C1 and rep spheres") == 1, ...)`; `check("t2_hero_color", color_name_for("substrate and name C1") == "hero_gold", ...)`.

**Type 3 — active-site-reveal = cast-reveal, DISEASE-MUTANT variant (convention §5.2; nodes gly.pfk etc):**
- Load `_edit_smoke.pdb` as `enzyme` (17-atom ALA-GLY). Apply the cast-reveal sequence:
  - `cmd.show_as("cartoon", "enzyme")`; `cmd.color("gray", "enzyme")` (full gray cartoon — the cast member enters)
  - `# src: .../selecting.py:48 cmd.select` then `cmd.select("enzyme_activesite", "enzyme and resi 1")` (placeholder active-site selection = resi 1); `cmd.zoom("enzyme_activesite")`
  - WT catalytic green sticks (placeholder: resi 2 = the "catalytic" residue): `cmd.show("sticks", "enzyme and resi 2")`; `cmd.color("green", "enzyme and resi 2")`
  - mutant red SPHERES (placeholder: resi 1 = the "disease" residue): `cmd.show("spheres", "enzyme and resi 1")`; `cmd.color("red", "enzyme and resi 1")`
  - nameplate label: `cmd.label("enzyme and resi 2 and name CA", '"ENZ"')` (placeholder enzyme name on the catalytic CA anchor — use ONE label per anchor; pick the nameplate)
- Post: `check("t3_enzyme_cartoon", cmd.count_atoms("enzyme and rep cartoon") > 0, ...)`; `check("t3_activesite_nonempty", cmd.count_atoms("enzyme_activesite") > 0, ...)`; `check("t3_mutant_spheres", cmd.count_atoms("enzyme and resi 1 and rep spheres") > 0, ...)`; `check("t3_mutant_color_red", color_name_for("enzyme and resi 1 and name CA") == "red", ...)`; `check("t3_catalytic_sticks", cmd.count_atoms("enzyme and resi 2 and rep sticks") > 0, ...)`; `check("t3_catalytic_color_green", color_name_for("enzyme and resi 2 and name CA") == "green", ...)`; `check("t3_nameplate_label", label_text_for("enzyme and resi 2 and name CA") == "ENZ", ...)`.

**Type 3b — active-site-reveal, STRUCTURAL variant (convention §5.2; node tca.citrate_synthase, NO mutant):**
- Reuse the `enzyme` object. Apply the SAME sequence MINUS the mutant-spheres step (no red spheres — the enzyme is NOT diseased). Reset: `cmd.hide("everything", "enzyme")`; `cmd.show_as("cartoon", "enzyme")`; `cmd.color("gray", "enzyme")`; `cmd.show("sticks", "enzyme and resi 2")`; `cmd.color("green", "enzyme and resi 2")`; `cmd.label("enzyme and resi 2 and name CA", '"CS"')`.
- Post: `check("t3b_enzyme_cartoon", cmd.count_atoms("enzyme and rep cartoon") > 0, ...)`; `check("t3b_no_mutant_spheres", cmd.count_atoms("enzyme and resi 1 and rep spheres") == 0, ...)` (NO mutant spheres — the structural variant omits step 4); `check("t3b_catalytic_sticks", cmd.count_atoms("enzyme and resi 2 and rep sticks") > 0, ...)`.

**Type 4 — branch-point (convention §4.3 Type 4; nodes pyr.branch etc):**
- Load `_smoke.pdb` as `current` (sticks, gray); load `_edit_smoke.pdb` as `alt_destiny` (sticks, hero_gold — the "where you could go" signal). Apply: `cmd.show_as("sticks", "current")`; `cmd.color("gray", "current")`; `cmd.show_as("sticks", "alt_destiny")`; `cmd.color("hero_gold", "alt_destiny")`; `cmd.zoom("current")`.
- Post: `check("t4_current_sticks", cmd.count_atoms("current and rep sticks") > 0, ...)`; `check("t4_alt_sticks", cmd.count_atoms("alt_destiny and rep sticks") > 0, ...)`; `check("t4_alt_color_hero_gold", color_name_for("alt_destiny and name CA") == "hero_gold", ...)`; `check("t4_current_color_gray", color_name_for("current and name C1") == "gray", ...)`.

**Type 5 — rng-shuffle (convention §4.3 Type 5; node tca.shuffle — the SOLE stochastic node):**
- Load `_smoke.pdb` as `citrate` (3 atoms; the 2 carbons C1/C2 are the prochiral pair placeholder). Apply: `cmd.show_as("sticks", "citrate")`; `cmd.color("gray", "citrate")`; `cmd.select("prochiral_pair", "citrate and name C1 or name C2")` (the 2 candidate carbons — a STRUCTURAL fact, Phase 7 source-approved via `TCA-RNG-CITRATE-PROCHIRALITY-01`); `cmd.show("spheres", "prochiral_pair")`; `cmd.color("hero_gold", "prochiral_pair")`; `cmd.label("prochiral_pair", '"? (RNG)"')`; `cmd.zoom("prochiral_pair")`.
- Post: `check("t5_citrate_loaded", cmd.count_atoms("citrate") > 0, ...)`; `check("t5_prochiral_pair_count_2", cmd.count_atoms("prochiral_pair") == 2, ...)` (the two candidate carbons); `check("t5_prochiral_spheres", cmd.count_atoms("prochiral_pair and rep spheres") > 0, ...)`; `check("t5_prochiral_color_hero_gold", color_name_for("prochiral_pair") == "hero_gold", ...)`.

**Type 6 — ending (4 tier-templates; convention §4.3 Type 6):**
- **true tier** (the soul-jump climax; anti-confusion §3.6): load `_smoke.pdb` as `atp`; `cmd.show_as("sticks", "atp")`; `cmd.color("hero_gold", "atp")`; `cmd.label("atp", '"ATP - soul"')`; `cmd.zoom("atp")`. Post: `check("t6_true_atp_sticks", cmd.count_atoms("atp and rep sticks") > 0, ...)`; `check("t6_true_color_hero_gold", color_name_for("atp") == "hero_gold", ...)`; `check("t6_true_label", label_text_for("atp") == "ATP - soul", ...)`. Add an inline anti-confusion comment: "# ANTI-CONFUSION: the gold on `atp` represents the hero's ELECTRONS (the soul), NOT the carbon. The carbon body was shed as CO2 at TCA. This is the metamorphosis (PROJECT.md row 107)."
- **good tier** (C retained): load `_smoke.pdb` as `product`; `cmd.show_as("sticks", "product")`; `cmd.color("hero_gold", "product")`; `cmd.label("product", '"retained"')`. Post: `check("t6_good_sticks", cmd.count_atoms("product and rep sticks") > 0, ...)`; `check("t6_good_color_hero_gold", color_name_for("product") == "hero_gold", ...)`.
- **normal tier** (CO2 release): load `_smoke.pdb` as `release`; `cmd.show_as("sticks", "release")`; `cmd.color("gray", "release")`; `cmd.label("release", '"CO2"')`. Post: `check("t6_normal_sticks", cmd.count_atoms("release and rep sticks") > 0, ...)`; `check("t6_normal_color_gray", color_name_for("release") == "gray", ...)`.
- **bad tier** (catastrophe): load `_edit_smoke.pdb` as `broken`; `cmd.show_as("cartoon", "broken")`; `cmd.color("red", "broken")`; `cmd.label("broken and name CA", '"catastrophe"')`. Post: `check("t6_bad_cartoon", cmd.count_atoms("broken and rep cartoon") > 0, ...)`; `check("t6_bad_color_red", color_name_for("broken and name CA") == "red", ...)`.

**Stage 7 — the 5.3 composition (convention §5.4 — the BEFORE/AFTER arc color contract):**
- BEFORE (5.4 owns): reuse the Type 3 `enzyme` with the mutant red spheres (already applied in Type 3 — re-assert here as the BEFORE state): `check("comp_before_mutant_red_spheres", cmd.count_atoms("enzyme and resi 1 and rep spheres") > 0 and color_name_for("enzyme and resi 1 and name CA") == "red", ...)`.
- AFTER (5.3 owns; this smoke demonstrates the COLOR contract 5.4 fills): load the pre-built `_wt_align_wt.pdb` fixture as `enzyme_wt` (the "restored WT" — the 5.3 align mechanism is NOT run here, just the color contract). `cmd.show_as("cartoon", "enzyme_wt")`; `cmd.color("green", "enzyme_wt")` (the `<wt_color>` 5.4 fills per convention §5.4). Post: `check("comp_after_wt_green", cmd.count_atoms("enzyme_wt and rep cartoon") > 0 and color_name_for("enzyme_wt and name CA") == "green", ...)`. Add a comment: "# The 5.3 composition: 5.4 OWNS the BEFORE (mutant red spheres); 5.3 OWNS the AFTER (WT-aligned reveal, colored green via 5.4's <wt_color> contract). This smoke proves the COLOR contract coheres (red->green = broken->restored); the 5.3 align MECHANISM is proven in tools/wt_align_smoke.py."

**Final verdict (copy from wt_align_smoke.py):**
```python
print("SMOKE_RESULT: {0}".format("FAIL" if FAILS else "PASS"))
if FAILS:
    print("SMOKE_FAILED_STAGES: " + ", ".join(FAILS))
```

**Source-citation convention (MANDATORY):** every DIRECT `cmd.*` call carries a `# src: tmp/pymol-src/modules/pymol/<file>.py:<line> cmd.<name>` comment on the line directly above. Verify line numbers against `tmp/pymol-src/modules/pymol/` — `viewing.py:2107` set_color, `viewing.py:528` show_as, `viewing.py:491` show, `viewing.py:568` hide, `viewing.py:1858` color, `viewing.py:65` zoom, `viewing.py:1332` label, `selecting.py:48` select, `querying.py:1412` count_atoms, `editing.py:1490` iterate, `querying.py:843` get_color_indices, `creating.py:1001` create, `importing.py:635` load.

Do NOT add the `op="set_color"`/`op="label"`/`op="set"` dispatches to `molops.py` (deferred to Phase 6). Do NOT create new fixture files (reuse `_smoke.pdb`/`_edit_smoke.pdb`/`_wt_align_wt.pdb`).
  </action>
  <verify>
`python3.6 -m py_compile tools/scene_template_smoke.py` exits 0. `grep -c "# src: tmp/pymol-src" tools/scene_template_smoke.py` shows >= 10 source citations. `grep -qi "ANTI-CONFUSION\|electrons.*not the carbon\|metamorphosis\|NOT.*becomes" tools/scene_template_smoke.py` matches (the anti-confusion comment on the true-tier + the cast-reveal). `grep -q "SMOKE_RESULT" tools/scene_template_smoke.py` matches. `grep -qi "color_name_for\|get_color_indices" tools/scene_template_smoke.py` matches (color-name round-trip, not hardcoded index). `grep -qi "cartoon\|sticks\|spheres" tools/scene_template_smoke.py` matches (the 3 reps). `grep -qi "gray\|green\|red\|orange\|hero_gold" tools/scene_template_smoke.py` matches (the shared palette). Confirm NO new fixture files: `git status --porcelain c14/data/assets/bundled/` shows no new untracked .pdb. Confirm `molops.py` NOT modified: `git diff --name-only c14/pymol_layer/molops.py` shows no changes.
  </verify>
  <done>
`tools/scene_template_smoke.py` exists, passes `python3.6 -m py_compile`, has >= 10 `# src:` citations, includes the anti-confusion comment, uses color-name round-trip, covers all 6 scene types + the cast-reveal (disease + structural variants) + the 4 ending tiers + the 5.3 composition, reuses existing fixtures, does NOT modify `molops.py`. Ready for Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the smoke headlessly + assert SMOKE_RESULT: PASS</name>
  <files>tools/scene_template_smoke.py</files>
  <action>
Run the smoke headlessly via the established harness + iterate until PASS. This task does NOT modify the smoke (Task 1 wrote it); it runs + diagnoses + fixes runtime issues only.

**Run + verify:**
1. Syntax check: `python3.6 -m py_compile tools/scene_template_smoke.py` (must exit 0).
2. Run headlessly: `bash tools/run_headless.sh tools/scene_template_smoke.py`. The harness greps `^SMOKE_RESULT: PASS`. If any stage FAILS, read `/tmp/opencode/scene_template_smoke.txt`, diagnose, fix the smoke, re-run.
3. Common runtime fixes (same as Plan 02 Task 2 — RESEARCH-api §F pitfalls):
   - **Pitfall 1 (iterate collector collision):** use non-colliding collector names (`cidx`, `lbls` — NOT `color`/`label`/`elem`).
   - **Pitfall 2 (label quoted-string):** `cmd.label(sele, '"ENZ"')` — the expression is a quoted-string literal, NOT a bare word. Verify `viewing.py:1364`.
   - **Pitfall 4 (color index session-unstable):** use `color_name_for` (the `get_color_indices` round-trip), NOT a hardcoded index.
   - **label overwrite on same atom:** if a nameplate + residue label target the same CA, the second overwrites the first. Use ONE label per anchor atom (the nameplate on resi 2 CA; do NOT also label resi 2 CA with `resn+'-'+resi`). The smoke uses the nameplate only.
   - **`cmd.show` (not `show_as`) for overlays:** Type 3 uses `cmd.show("sticks", ...)` AFTER `cmd.show_as("cartoon", ...)` — `show` ADDS sticks on top of cartoon (does NOT turn off cartoon); `show_as` would turn OFF cartoon. Verify RESEARCH-api §A.1 (show turns ON only; show_as is atomic).
   - **Gotcha #2 (fixture path):** `c14.paths.data_path("data", "assets", "bundled", "_wt_align_wt.pdb")` — the `_wt_align_wt.pdb` fixture exists from Phase 5.3 (confirmed in `c14/data/assets/bundled/`).
   - **resi selection on `_edit_smoke.pdb`:** the fixture is ALA(resi 1)-GLY(resi 2), 17 atoms. `enzyme and resi 1` = the ALA (8 atoms incl. CB/HB); `enzyme and resi 2` = the GLY (7 atoms). `name CA` picks one CA per residue. Verify atom counts if a post-condition count is off.
4. Iterate until `bash tools/run_headless.sh tools/scene_template_smoke.py` prints `PASSED` with `SMOKE: PASS` for every stage.
5. Confirm the AST gate: `python3.6 tools/check_imports.py` (exit 0).
6. Confirm `molops.py` NOT modified: `git diff --name-only c14/pymol_layer/molops.py` shows no changes.

Do NOT add the new-op dispatches to `molops.py` (deferred to Phase 6 per convention §2.2/OQ-1). If the human overrides OQ-1 at Plan 04, a follow-up revision task will be added.
  </action>
  <verify>
`bash tools/run_headless.sh tools/scene_template_smoke.py` prints `PASSED` (greps `^SMOKE_RESULT: PASS`). The captured output (`/tmp/opencode/scene_template_smoke.txt`) shows `SMOKE: PASS` for every stage (Stage 0 palette; Type 1 hero_count_1/hero_spheres/cast_cartoon/cast_loaded; Type 2 substrate_sticks/hero_spheres/hero_color; Type 3 enzyme_cartoon/activesite_nonempty/mutant_spheres/mutant_color_red/catalytic_sticks/catalytic_color_green/nameplate_label; Type 3b enzyme_cartoon/no_mutant_spheres/catalytic_sticks; Type 4 current_sticks/alt_sticks/alt_color_hero_gold/current_color_gray; Type 5 citrate_loaded/prochiral_pair_count_2/prochiral_spheres/prochiral_color_hero_gold; Type 6 true/good/normal/bad tier posts; Stage 7 comp_before_mutant_red_spheres/comp_after_wt_green) and a final `SMOKE_RESULT: PASS`. `python3.6 tools/check_imports.py` exits 0. `git diff --name-only c14/pymol_layer/molops.py` shows NO changes.
  </verify>
  <done>
`tools/scene_template_smoke.py` runs headlessly printing `SMOKE_RESULT: PASS` with all stage checks passing (6 scene types + cast-reveal disease/structural variants + 4 ending tiers + 5.3 composition). AST gate clean. `molops.py` NOT modified. SC2 + SC3 mechanisms proven headlessly on placeholders.
  </done>
</task>

</tasks>

<verification>
- `tools/scene_template_smoke.py` passes `python3.6 -m py_compile` and runs headlessly with `SMOKE_RESULT: PASS` (all stage checks green) — SC2 + SC3 mechanisms delivered.
- Every direct `cmd.*` call carries a `# src:` citation.
- The color-name round-trip via `get_color_indices` is used (NOT a hardcoded color index).
- The anti-confusion comment is present (cast-reveal = enzyme health, NOT carbon fate; the true-tier ATP = electrons/soul, NOT carbon).
- All 6 scene types + the cast-reveal (disease + structural variants) + the 4 ending tiers + the 5.3 composition are prototyped with post-conditions.
- Scene templates are MolAction sequences via direct `cmd.*` (NOT `cmd.scene` — convention §4.1).
- NO new fixture files (reuse `_smoke.pdb`/`_edit_smoke.pdb`/`_wt_align_wt.pdb`).
- The AST gate stays clean; `molops.py` NOT modified (dispatch deferred to Phase 6).
- No fabricated science: placeholder structures only.
</verification>

<success_criteria>
- SC2 mechanism delivered: the scene-template library is headless-prototyped — each of the 6 scene types applies as a MolAction sequence with post-condition assertions (rep visible, residue count, label, color).
- SC3 mechanism delivered: the cast-reveal is headless-prototyped (full-then-zoom gray cartoon → green catalytic sticks → red mutant spheres → nameplate label) + the 5.3 composition (BEFORE mutant red + AFTER WT green).
- The prototype proves the MECHANISM on placeholders, NOT real science (Phase 7 sources real structures + the real active-site/catalytic/mutant selections per enzyme).
- No code outside `tools/scene_template_smoke.py` is modified; `molops.py` untouched (dispatch deferred to Phase 6 unless the human overrides OQ-1 at Plan 04).
</success_criteria>

<output>
After completion, create `.planning/phases/05.4-cast-hero-representation-design/05.4-03-SUMMARY.md`
</output>
