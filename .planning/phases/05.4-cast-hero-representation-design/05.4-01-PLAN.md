---
phase: 05.4-cast-hero-representation-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05.4-cast-hero-representation-design/05.4-CONVENTION.md
autonomous: true

must_haves:
  truths:
    - "A C14 hero highlight convention is specified (SC1): a hybrid selection rule (per-asset hero_selector, single-C auto-derive, multi-C-without-selector FAILS LOUDLY) + hero_gold color (Okabe-Ito [0.90,0.62,0.0]) + spheres rep + 'C14' label + scoped dim + persistence as a pure function of on_enter replay (NO runtime marker, NO .pse) + the 3 structural cases (hero-alone / hero-in-substrate / hero-at-enzyme-cast with co-loaded substrate) + the CO2-shed soul-transfer climax (gold fades on CO2, transfers to NADH/ATP) with a MANDATORY anti-confusion note that the transfer is a narrative device, NOT a claim the carbon becomes ATP/NADH"
    - "A scene-template library exists (SC2): 6 scene TYPES (preface / substrate-traversal / active-site-reveal=cast-reveal / branch-point / rng-shuffle / ending with 4 tier-templates) mapping all 55 skeleton nodes, each specified as a MolAction SEQUENCE (NOT cmd.scene — session state is not save/load-safe) keyed to skeleton node ids, with per-template post-conditions"
    - "A cast-reveal convention is specified (SC3): full-then-zoom (full gray cartoon -> zoom active site -> WT catalytic green sticks -> mutant red SPHERES (rep redundancy = colorblind-safe) -> cofactor orange sticks -> cmd.label nameplate), with a consistent named-color cast palette, documented for review"
    - "The convention is internally consistent across all 3 SCs: a SHARED MolAction op vocabulary (existing hide_all/show/show_as/select_focus/zoom/color/load/delete + 3 NEW ops set_color/label/set, all dispatch DEFERRED to Phase 6) and a SHARED named-color palette (hero_gold / gray / green / red / orange / gray80, + magenta as an OQ alternative)"
    - "The op-granularity disagreement is RESOLVED: adopt the API researcher's minimal-orthogonal approach (3 new peer-primitive ops set_color/label/set, hero-highlight + scene templates expressed as SHORT MolAction sequences), NOT the hero-design researcher's composite op=\"hero_highlight\" super-op — rationale documented; the rejected super-op alternative is recorded. This mirrors the 5.3 precedent (5.3 added ONE focused op align, NOT a composite)"
    - "The 5.3 composition is honored: 5.4 OWNS the BEFORE (mutant red spheres at the edit-allowed node on_enter) + the named color values (wt_color=green, mutant_color=red); 5.3 OWNS the AFTER (WT-aligned restoration reveal at the branch node on_enter) which plugs into 5.4's color contract (fills the <mutant_color>/<wt_color> slots 5.3 left open)"
  artifacts:
    - path: ".planning/phases/05.4-cast-hero-representation-design/05.4-CONVENTION.md"
      provides: "The Cast & Hero Representation convention spec — the reviewable design artifact (SC1+SC2+SC3) consumed by Phases 6/7/9/12"
      contains: "op=\"set_color\""
      min_lines: 500
  key_links:
    - from: ".planning/phases/05.4-cast-hero-representation-design/05.4-CONVENTION.md"
      to: "data/story_glucose/*.json (the FROZEN 55-node skeleton)"
      via: "the 6 scene types map all 55 skeleton node ids (per RESEARCH-scene-templates §A table); each template is a MolAction sequence keyed to those node ids"
      pattern: "intro\\.preface|gly\\.pfk|tca\\.shuffle|end\\.true"
    - from: ".planning/phases/05.4-cast-hero-representation-design/05.4-CONVENTION.md"
      to: "Phase 6 molops op=\"set_color\"/op=\"label\"/op=\"set\" dispatch"
      via: "the shared op vocabulary + the 3 new-op dispatch pseudocode (Phase 6 implements the elif branches + MockCmd unit tests; 5.4 specifies only, prototypes call cmd.* directly)"
      pattern: "op == \"(set_color|label|set)\""
    - from: ".planning/phases/05.4-cast-hero-representation-design/05.4-CONVENTION.md"
      to: "Phase 5.3 WT-aligned restoration reveal convention (05.3-CONVENTION.md)"
      via: "the cast-reveal BEFORE (5.4, mutant red spheres at edit-allowed on_enter) composes with the 5.3 AFTER (WT-aligned reveal at branch on_enter); 5.4 fills 5.3's <mutant_color>/<wt_color> slots with red/green"
      pattern: "<mutant_color>|<wt_color>|05\\.3"
    - from: ".planning/phases/05.4-cast-hero-representation-design/05.4-CONVENTION.md"
      to: "Phase 7 per-asset content authoring (hero_selector + active_site_sele + catalytic_residues_sele)"
      via: "the per-asset metadata contract: hero_selector (which carbon is the hero — a pathway-science claim, HIGH-STAKES per-claim approval) + the active-site-reveal enzyme metadata fields"
      pattern: "hero_selector|catalytic_residues_sele|active_site_sele"
---

<objective>
Write the Cast & Hero Representation CONVENTION as a reviewable design artifact covering all 3 Phase 5.4 success criteria (SC1 hero highlight + SC2 scene-template library + SC3 cast-reveal).

Purpose: The 3D representation language — how the player sees "their" C14 carbon atom in any structure, which residues are shown at each stage type, and how an enzyme appears when first encountered — is designed as a reviewable spec so Phase 6 implements an already-reviewed visual design rather than inventing it inside the overloaded first-Qt phase. This is a 0-requirement design phase (per the 5.1/5.2/5.3 design-phase precedent); it produces a reviewed SPEC, not shipped code. It enables CHAR-02 + CAST-05 delivery in Phase 6 and CAST-01 in Phase 9.

This plan ALSO resolves the research disagreement on op granularity (API researcher: 2-3 minimal new ops + sequences; hero-design researcher: a composite `op="hero_highlight"` super-op) — adopting the minimal-orthogonal approach, mirroring how 5.3 resolved its naming conflict by adopting the API researcher's recommendation.

Output: `.planning/phases/05.4-cast-hero-representation-design/05.4-CONVENTION.md` — the convention spec document. Pure writing; NO code changes in this plan (`c14/pymol_layer/molops.py` and `c14/story/model.py` are NOT modified — the 3 new-op dispatches are SPEC'd but DEFERRED to Phase 6, mirroring 5.3-01).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.4-cast-hero-representation-design/05.4-RESEARCH-pymol-representation-api.md
@.planning/phases/05.4-cast-hero-representation-design/05.4-RESEARCH-hero-highlight-design.md
@.planning/phases/05.4-cast-hero-representation-design/05.4-RESEARCH-scene-templates-cast-reveal-design.md

# The pattern to mirror (5.3 = same shape: spec -> prototype -> review; 5.4 has 3 deliverables instead of 1)
@.planning/phases/05.3-wt-aligned-structure-load-convention/05.3-CONVENTION.md

# The frozen skeleton the scene templates map onto (55 nodes) + the model the MolAction shape extends
@data/story_glucose/manifest.json
@c14/story/model.py
@c14/pymol_layer/molops.py

# The 5.3 composition boundary (5.4 fills 5.3's <mutant_color>/<wt_color> slots)
@.planning/phases/05.3-wt-aligned-structure-load-convention/05.3-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the shared foundation + the SC1 hero-highlight convention (§1-§3)</name>
  <files>.planning/phases/05.4-cast-hero-representation-design/05.4-CONVENTION.md</files>
  <action>
Create `05.4-CONVENTION.md` in the phase directory. This is the Cast & Hero Representation convention spec — the SC1+SC2+SC3 design artifact. Write it as a structured markdown document with a header (phase, date, status=DRAFT-for-review, the 4 SCs as a checklist, the 3 source research files cited) and the sections below. Synthesize from the THREE research files (already in the phase dir); do NOT re-research — all 3 are HIGH-confidence and the API file is empirically verified (45-check headless run, §A.6). Cite the research files by section number (e.g. "per RESEARCH-pymol-representation-api §C").

**§1. Purpose & scope.** One-paragraph statement: this convention specifies the 3D representation language for the RPG — the C14 hero highlight (how the player sees "their" carbon), the per-stage-type scene templates (which residues/reps/colors at each stage), and the cast-reveal convention (how an enzyme first appears). State it is a 0-requirement design phase enabling CHAR-02 + CAST-05 (Phase 6) and CAST-01 (Phase 9). State the real in-game human-verify is Phase 6; this phase produces a reviewed SPEC + headless mechanism proofs (Plans 02/03) + a design-review checkpoint (Plan 04). Note the hero-identity anti-confusion (AGENTS.md + PROJECT.md row 107): the hero is a CARBON ATOM; "C14" is its isotope tracking LABEL, not its fate; the carbon body is shed as CO2, its electrons (the "soul") flow via NADH/FADH2 → ETC → ATP; C14-decay was DROPPED. Do NOT reintroduce "the carbon becomes ATP" — that is scientifically wrong.

**§2. Shared foundation — the op vocabulary + the named-color palette + the op-granularity disagreement RESOLUTION.** This section is the foundation ALL 3 SCs build on (the user requires internal consistency across SCs: shared op vocabulary + shared named-color palette). Document:

- **§2.1 The shared MolAction op vocabulary.** List the EXISTING ops (hide_all, show, show_as, select_focus, zoom, color, load, delete — per `molops.py:114-144`) + the 3 NEW ops this convention introduces: `set_color` (define a named color via `cmd.set_color`), `label` (set a text label via `cmd.label`), `set` (set a per-atom setting like `sphere_scale` via `cmd.set`). All 3 new ops are peer primitives wrapping ONE `cmd.*` call each (mirrors how show/show_as/color/zoom are each one `cmd.*` call). NO composite super-op. Provide the 3 new-op MolAction shapes (pure data, `MolAction(op,target,args)`-compatible — NO model change needed, `model.py:44-48` already supports arbitrary args):
  - `{"op":"set_color","target":null,"args":{"name":"hero_gold","rgb":[0.90,0.62,0.0]}}` — define a named color (idempotent overwrite; `viewing.py:2107`).
  - `{"op":"label","target":"hero_atom","args":{"sele":null,"text":"C14"}}` — set a text label (dispatch wraps `text` as the quoted-string expression `'"C14"'` to avoid the bare-word footgun, RESEARCH-api §F Pitfall 2; `viewing.py:1332`). `sele` defaults to `target`.
  - `{"op":"set","target":"hero_atom","args":{"name":"sphere_scale","value":1.0,"sele":null}}` — set a per-atom setting (dispatch calls `cmd.set(name, value, sele or target)`; `setting.py:183`). `sele` defaults to `target`.
- **§2.2 The op-granularity disagreement RESOLUTION (the key decision).** State the two research positions: the API researcher (RESEARCH-pymol-representation-api §E.4) recommends 2 new minimal ops `label`+`set_color` + reuse, expressing hero-highlight + scene templates as MolAction SEQUENCES (rejects a composite super-op); the hero-design researcher (RESEARCH-hero-highlight-design §B/§F Q5) recommends a single composite `op="hero_highlight"` super-op encapsulating set_color+show_as spheres+set sphere_scale+color+scoped-dim+label. **ADOPTED: the minimal-orthogonal approach — 3 new peer-primitive ops (`set_color`, `label`, `set`), hero-highlight + scene templates expressed as SHORT MolAction sequences, NOT a super-op.** Rationale (mirror how 5.3 resolved its naming conflict by adopting the API researcher's recommendation, per `05.3-01-SUMMARY.md`):
  1. **Keeps molops.py dispatch minimal and orthogonal** — the existing 02-04 one-MolAction-per-apply pattern (`molops.py:112-196`); each new op is one `elif` wrapping one `cmd.*` call, the same shape as the existing show/color branches. A super-op would be one op = many `cmd.*` calls (violates the precedent).
  2. **Most headless-testable per-step** — each MolAction → one `cmd.*` → one post-condition (rep visible / color name round-trip / label text). A super-op hides intermediate state inside one dispatch, making per-step post-conditions impossible.
  3. **Mirrors the 5.3 precedent** — 5.3 added ONE focused op `align` (NOT a composite "wt_reveal" super-op); 5.4 adds 3 focused primitive wrappers because the representation/color/label surface is wider. 5.3 did NOT add a composite; 5.4 follows.
  4. **The `set` op is the 3rd primitive** (justification for going beyond the API researcher's "2 ops"): `sphere_scale` is a per-atom setting that `show_as`/`color` don't cover; `cmd.set(name, value, selection)` (`setting.py:183`) is a distinct primitive. A generic `op="set"` (name/value/sele) is reusable beyond sphere_scale (future per-atom settings) and stays one-concern-per-op.
  - **Record the REJECTED alternative** (the `op="hero_highlight"` super-op): its pros (readable `on_enter`, footgun encapsulation, colorblind default baked in) are noted; its cons (couples concerns, one-op-many-cmds, harder per-step testing, breaks the 5.3 precedent) lose. Document the super-op shape the hero-design researcher proposed (`{"op":"hero_highlight","target":"<hero_obj>","args":{selector,label,color,scale,dim,soul}}`) as the rejected alternative for the record.
  - State the **dispatch deferral** (mirror 5.3 §6/§8 Q3): the 3 new-op `elif` branches + MockCmd unit tests LAND IN PHASE 6, NOT 5.4. 5.4 SPECIFIES the op shapes + prototypes `cmd.set_color`/`cmd.label`/`cmd.set`/`cmd.show_as`/`cmd.color` headlessly (direct `cmd.*` in the smokes, like `wt_align_smoke.py` calls `cmd.super` directly). Flag as **Open Question OQ-1** (§7) for the checkpoint — the human may override to "add in 5.4" (the 04-05 precedent).
- **§2.3 The shared named-color palette.** A CONSISTENT named-color palette documented now, consumed by Phase 6/7/9/10. Phase 10 owns the full palette; 5.4 picks the DEFAULTS. Table with columns: Element | Named PyMOL color | RGB (placeholder or Okabe-Ito) | Representation | Rationale | Source/approval:
  - Hero (`hero_gold`) — Okabe-Ito orange [0.90,0.62,0.0] — spheres — colorblind-safe, distinctive, "heroic" warm read; doesn't collide with green/blue cast colors nor gray dim. (RESEARCH-hero-highlight §B.)
  - Enzyme/cast (`gray`) — standard PyMOL `gray` — cartoon — neutral, recedes; the enzyme is the STAGE, lets active-site colors pop. (RESEARCH-scene-templates §C.3.)
  - WT catalytic residues (`green`) — standard `green` — sticks — "healthy/functional". FILLS 5.3's `<wt_color>` slot.
  - Disease-mutant residue (`red`) — standard `red` — **spheres** (rep redundancy = colorblind-safe) — "broken/danger". FILLS 5.3's `<mutant_color>` slot. (OQ-2: `magenta` pure-hue-safe alternative.)
  - Cofactor (`orange`) — standard `orange` — sticks — distinct third functional class.
  - Dim/non-hero (`gray80`) — standard `gray80` — (existing rep) — the scoped spotlight dim within the hero-bearing object.
  - Bad-ending broken state (`red`) — standard `red` — cartoon — the catastrophe/death signal (consistent with mutant-red danger language).
  - Named-color confidence note: `gray`/`green`/`red`/`orange`/`gray80` are standard PyMOL named colors (RESEARCH-scene-templates §C.3); `hero_gold` is defined via `cmd.set_color` (idempotent; the RGB is a placeholder Okabe-Ito value — the FINAL palette + RGB source approval is Phase 7, RESEARCH-api §I OQ-2). Do NOT assert a hardcoded color INDEX in prototypes (the index is session-allocated; assert the NAME via `get_color_indices` round-trip, RESEARCH-api §D.3/Pitfall 4).

**§3. SC1 — the C14 hero highlight convention (per RESEARCH-hero-highlight-design §A-§C + RESEARCH-pymol-representation-api §C).** Document:
- **§3.1 The hybrid selection rule (the core question — RESEARCH-hero-highlight §A).** Adopt hybrid option (iv): each hero-bearing asset carries an explicit `hero_selector` (a PyMOL selection resolving to EXACTLY ONE carbon); single-carbon objects auto-derive the unique C; multi-carbon objects WITHOUT a `hero_selector` FAIL LOUDLY at the human-verify gate (no silent best-guess — the "which carbon is the hero" is a pathway-science claim, HIGH-STAKES per-claim, PROJECT.md row 109). State the resolution rule (deterministic, re-evaluated each `on_enter`): (1) asset metadata `hero_selector` → use it; (2) else single-carbon object (`count_atoms("<obj> and elem C")==1`) → the unique C; (3) else multi-carbon no-selector → FAIL LOUDLY. The prototype uses known-by-construction selectors on placeholders (NOT a heuristic).
- **§3.2 The 3 structural cases (RESEARCH-hero-highlight §A).** Table: Case | Skeleton example | Loaded object | Where the hero is | Handling. Case 1 hero-alone (`intro.preface`, `hero_atom` single-atom object — the object IS the hero). Case 2 hero-in-substrate (`intro.shell_glucose`/`gly.start`, `glucose` multi-carbon — ONE carbon C1 IN the substrate, `hero_selector="glucose and name C1"`). Case 3 hero-at-enzyme-cast (`gly.pfk`/`pyr.pdh`/`tca.citrate_synthase`/etc — loaded object is the ENZYME, hero is in the SUBSTRATE which the skeleton does NOT load). For case 3, document the 3 sub-options (3a co-load substrate + highlight hero WHERE small enough; 3b suspend highlight; 3c hybrid RECOMMENDED — co-load where practical, suspend where impractical e.g. huge membrane proteins like 1OCC). State: the convention specifies the MECHANISM (highlight applies whenever a hero-bearing object is loaded — cases 1/2/3a) + the SUSPEND fallback (3b). Whether to co-load the substrate at a given enzyme-cast node is a Phase 7 content decision (mirrors 5.3's OQ-DM2 per-enzyme deferral). The large-enzyme visibility/zoom is Phase 9 (§6.3).
- **§3.3 Color + representation + label (RESEARCH-hero-highlight §B).** Hero = `hero_gold` SPHERES scaled up (`sphere_scale=1.0` via the new `op="set"`), label `"C14"` (the isotope tracking label — the hero's name tag, NOT its fate). The scoped dim: `color gray80` on non-hero atoms IN THE HERO-BEARING OBJECT ONLY (NOT the cast/enzyme — critical scoping). Provide the SEMANTIC hero-highlight sequence (the §3.3 contract — what the highlight does):
  ```
  1. set_color hero_gold [0.90,0.62,0.0]          (idempotent; viewing.py:2107)
  2. show_as spheres <hero_sele>                   (per-atom scoping; viewing.py:528)
  3. set sphere_scale 1.0 <hero_sele>              (setting.py:183)
  4. color hero_gold <hero_sele>                   (viewing.py:1858)
  5. color gray80 <hero_obj> and not <hero_sele>   (scoped dim — NOT the cast/enzyme)
  6. label <hero_sele> "C14"                       (viewing.py:1332; quoted-string expr)
  ```
  State this is a SHORT sequence of 6 `cmd.*` calls expressed as MolActions (set_color → show_as → set → color → color → label), NOT a super-op (per §2.2 resolution). For the single-atom `hero_atom` (case 1), step 5 (dim) is omitted (a single atom has no non-hero atoms).
- **§3.4 Persistence across MolAction replay + save/load (RESEARCH-pymol-representation-api §H + RESEARCH-hero-highlight §C).** The hero-highlight is a PURE FUNCTION of `on_enter` — re-derived from the deterministic `hero_selector` rule + re-applied by the MolAction sequence EVERY time the node is entered (on start/choose/apply_player_edit AND load). It CANNOT depend on session-only state (a `cmd.flag`/`cmd.alter`/`cmd.label` set once and relied on across saves is LOST on load — no `.pse` is saved, `c14/persist.py:50-51`; `c14/engine.py:203-213` replays `on_enter`). Cite the engine source. State: NO runtime marker (`alter`/`flag`/`b`) is needed for persistence — the selection rule IS the marker (re-evaluated each replay). A marker is only useful if it is itself a MolAction in `on_enter` (so it replays) AND you need a stable handle surviving atom reordering — but §3 recommends avoiding markers entirely (reserve `segi`-marking for a Phase 7 real-structure case where atom reordering breaks a `name C1` rule). Provide the canonical `on_enter` order for a "hero stage" node: `hide_all` → `load <hero-bearing obj>` → `show_as <base rep>` → `[hero-highlight sequence §3.3]` → `zoom`. Note this composes with the existing `intro.json` placeholder pattern.
- **§3.5 The CO2-shed climax — the soul-transfer (the dramatic peak — RESEARCH-hero-highlight §C).** Where it happens (HIGH confidence, frozen skeleton): `tca.shuffle` (the RNG-weighted 0.5/0.5 branch, `TCA-RNG-CITRATE-PROCHIRALITY-01`) → `tca.co2_turn1`/`tca.co2_turn2` (the hero carbon SHED as CO2) → `etc.entry` → `etc.complex_i/ii/iii/iv` → `etc.atp_synthase` → `end.true`. Document the soul-jump metamorphosis reframing (PROJECT.md row 107): the hero transforms, not dies — the carbon body is shed as CO2 (the chrysalis), the narrative POV follows the ELECTRONS (the "soul") via NADH/FADH2 → ETC → ATP. The visual handling: at `tca.co2_turn1`/`turn2` the hero gold sphere shows ONE final time on the departing CO2 then FADES; at `etc.entry` onward the highlight TRANSFERS to the NADH/electron carrier (gold on NADH = the soul); at `end.true` the highlight is on ATP (the soul became ATP energy). State the convention specifies a `soul` mode flag on the hero-highlight sequence (the highlight targets a non-carbon — NADH/ATP — for post-shed nodes) + the MANDATORY anti-confusion `text_teaching` contract.
- **§3.6 MANDATORY anti-confusion note (CRITICAL, from AGENTS.md + PROJECT.md row 107).** Add a boxed NOTE: the highlight TRANSFER is a NARRATIVE/VISUAL DEVICE (the "soul" follows the electrons per the metamorphosis reframing). It does NOT assert: the C14 carbon itself becomes ATP or enters oxidative phosphorylation (WRONG — the carbon body is shed as CO2 at TCA); the C14 carbon becomes NADH (WRONG — the ELECTRONS are carried by NADH, not the carbon); any C14 radioactive-decay timescale (DROPPED — PROJECT.md row 108). The `text_teaching` at `tca.co2_turn1`/`turn2`, `etc.entry`, `etc.atp_synthase`, `end.true` MUST state: "Your carbon body is released as CO2; your electrons (the narrative 'soul') are carried by NADH/FADH2 to the ETC, which powers ATP synthase. The gold highlight now follows your electrons, not your carbon — the metamorphosis." (Phase 7 finalizes wording with citations.) The highlight on NADH/ATP represents the ELECTRONS (the soul), NOT the carbon. Keep the carbon-fate framing and the electron-soul framing SEPARATE — do NOT conflate (mirror 5.3 §5 anti-confusion, `05.3-CONVENTION.md:185-189`).
- **§3.7 No fabricated science (MANDATORY).** Use PLACEHOLDERS throughout: `pdb:TBD_*`, `<hero_selector>`, `<active_site_sele>`. The "which carbon is the hero at each stage" is a Phase 7 per-claim content decision (HIGH-STAKES carbon-fate taxonomy, PROJECT.md row 109). The prototype uses placeholder structures with known-by-construction selectors. NO real PDB IDs, NO network fetches in 5.4.

Write clearly and concisely; this is a design spec for human review. Use tables where the research uses tables. Each section self-contained enough that a reviewer assesses one decision at a time.
  </action>
  <verify>
`test -f .planning/phases/05.4-cast-hero-representation-design/05.4-CONVENTION.md` succeeds; the file is >= 250 lines after Task 1 (it will grow in Task 2). `grep -c "^##" 05.4-CONVENTION.md` shows >= 3 level-2 section headers (§1-§3). `grep -q 'op="set_color"' 05.4-CONVENTION.md` matches (the new op is referenced). `grep -q 'op="label"' 05.4-CONVENTION.md` matches. `grep -q 'op="set"' 05.4-CONVENTION.md` matches. `grep -qi 'hero_highlight.*super-op\|super-op.*hero_highlight\|composite.*hero_highlight\|REJECT.*super-op\|rejected alternative' 05.4-CONVENTION.md` matches (the op-granularity disagreement is RESOLVED + the rejected super-op is documented). `grep -qi 'Okabe-Ito\|0.90.*0.62' 05.4-CONVENTION.md` matches (the hero_gold palette is documented). `grep -qi 'soul.transfer\|metamorphosis\|chrysalis\|CO2.*shed\|shed.*CO2' 05.4-CONVENTION.md` matches (the CO2-shed climax is documented). `grep -qi 'carbon becomes ATP\|NOT assert\|anti-confusion' 05.4-CONVENTION.md` matches (the anti-confusion note is present). `grep -qi 'FAIL LOUDLY\|fail loudly' 05.4-CONVENTION.md` matches (the multi-carbon no-selector policy). `grep -qi 'pdb:TBD\|<hero_selector>' 05.4-CONVENTION.md` matches (PLACEHOLDERS used, no fabricated PDB IDs). Confirm NO real PDB IDs invented: `grep -oE '\b[1-9][0-9][A-Z0-9]{3}\b' 05.4-CONVENTION.md | sort -u` should list NONE (5.4 uses ONLY placeholders — unlike 5.3 which had 6CFO/6CER; 5.4 has zero real PDB IDs).
  </verify>
  <done>
`05.4-CONVENTION.md` exists with §1 (purpose/scope + hero-identity anti-confusion), §2 (shared foundation: op vocabulary + the op-granularity RESOLUTION adopting 3 minimal new ops set_color/label/set NOT a super-op, with the rejected super-op documented + the dispatch deferral to Phase 6 + the shared named-color palette), §3 (SC1 hero highlight: hybrid selection rule + 3 structural cases + color/rep/label sequence + persistence as pure function of on_enter + CO2-shed soul-transfer climax + MANDATORY anti-confusion note + no-fabricated-science PLACEHOLDERS). No real PDB IDs; `molops.py`/`model.py` NOT modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write SC2 scene-template library + SC3 cast-reveal + downstream contracts + the 11 open questions + review checklist (§4-§8)</name>
  <files>.planning/phases/05.4-cast-hero-representation-design/05.4-CONVENTION.md</files>
  <action>
APPEND the remaining sections (§4-§8) to `05.4-CONVENTION.md` (Task 1 created §1-§3; this task extends the same file). Read the file first to find the insertion point (end of §3), then append.

**§4. SC2 — the scene-template library (per RESEARCH-scene-templates-cast-reveal-design §A/§B).** Document:
- **§4.1 Scene templates are MolAction SEQUENCES, NOT cmd.scene keys (RESEARCH-scene-templates §B + RESEARCH-api §B.4).** `cmd.scene` store/recall works headlessly (RESEARCH-api §A.6) BUT a scene is SESSION STATE — `SaveStore` saves only `GameState` JSON, no `.pse` (`c14/persist.py:50-51`); a `cmd.scene` key is LOST on game save/load. A MolAction sequence in `on_enter` IS replayed on load (`c14/engine.py:203-213`) → it IS the persistent, save/load-safe scene template (the existing `intro.json` pattern). State: the scene-template library is a set of authored `on_enter` MolAction sequences. `cmd.scene` MAY be a Phase 6 in-session PERFORMANCE CACHE (recall a stored scene on re-entry), but the MolAction sequence is the source of truth + the contract. 5.4 does NOT depend on `cmd.scene`.
- **§4.2 The 6 scene TYPES (RESEARCH-scene-templates §A).** Table mapping all 55 skeleton nodes to 6 types (within the 4-8 target): # | Scene type | Skeleton nodes (by id) | SC5 classes | Narrative purpose. Reproduce the research §A table: (1) preface — `intro.preface`/`intro.select`/`intro.shell_glucose`/`fa.stub`/`alc.stub`; (2) substrate-traversal — `gly.start`/`gly.g6p`/`gly.fbp_to_pyruvate`/`gly.pyruvate`/`tca.entry`/`tca.co2_turn1`/`tca.co2_turn2`/`tca.akg_dh`/`etc.atp_synthase` (+ conditional narrative-only edit-allowed); (3) active-site-reveal (= the cast-reveal, §5) — 8 PDB-bearing disease-mutant edit-allowed (`gly.pfk`/`gly.pyruvate_kinase`/`pyr.pdh`/`tca.aconitase`/`tca.isocitrate_dh`/`etc.complex_iv`) + `tca.citrate_synthase` (structural variant) + 6 narrative-only edit-allowed (conditional); (4) branch-point — `pyr.branch`/`anaer.entry`/`tca.divert_to_good`/`etc.entry`; (5) rng-shuffle — `tca.shuffle` (the SOLE stochastic node, the citrate prochirality); (6) ending (4 tier-templates) — true=`end.true`; good=`end.good.fatty_acid`/`end.good.amino_acid`/`anaer.lactic`; normal=`end.normal.co2`/`anaer.ethanolic`; bad=15 `bad.*` endings. Note the §A sub-case: the 6 narrative-only edit-allowed nodes (`tca.succinyl_coa_synthetase`/`tca.fumarase`/`tca.malate_dh`/`etc.complex_i/ii/iii`) carry `edit:enzyme:<id>` but have NO PDB load (skeleton `[hide_all]`-only) — 5.4 specs BOTH options (substrate-traversal default OR active-site-reveal IF Phase 7 sources a structure); Phase 7 decides per enzyme (mirrors 5.3 OQ-DM2). Flagged OQ-10.
- **§4.3 The per-type draft MolAction sequences + post-conditions.** For EACH of the 6 types, provide: the nodes, the purpose, the draft `on_enter` MolAction sequence (composing from the §2 shared op vocabulary: hide_all/load/show_as/show/color/select_focus/zoom + the 3 new ops set_color/label/set + the 5.3 `align` op deferred to Phase 6), and the post-conditions (machine-checkable via `count_atoms("obj & rep <rep>")` + `iterate` color-name/label + named-selection residue count — per RESEARCH-api §D + RESEARCH-scene-templates §D). Use `pdb:TBD_*` PLACEHOLDERS for all PDB codes + `<active_site_sele>`/`<catalytic_residues_sele>`/`<mutant_residue_sele>` for all selections. Reproduce the research §A per-type sequences:
  - **Type 1 preface:** `hide_all` → `load hero_atom` → `show_as spheres hero_atom` → `[hero-highlight §3.3]` → `load aa_cast` → `show_as cartoon aa_cast` → `color gray aa_cast` → `zoom all`. Post: `count_atoms("hero_atom")==1`; `count_atoms("hero_atom & rep spheres")>0`; `count_atoms("aa_cast & rep cartoon")>0`; `count_atoms("aa_cast")>0`.
  - **Type 2 substrate-traversal:** `hide_all` → `load substrate` → `show_as sticks substrate` → `color gray substrate` → `[hero-highlight §3.3 on the hero sub-sele]` → `zoom substrate`. Post: `count_atoms("substrate")>0`; `count_atoms("substrate & rep sticks")>0`; hero-highlight post-condition (count==1, elem C, rep spheres).
  - **Type 3 active-site-reveal (= the cast-reveal, §5 owns the convention):** the full sequence per §5 (full cartoon gray → zoom active site → catalytic green sticks → mutant red spheres → cofactor orange sticks → nameplate label). Post per §5.
  - **Type 4 branch-point:** `hide_all` → `load current` (sticks, gray) → `load alt_destiny` (sticks, hero_gold — the "where you could go" signal) → `[hero-highlight on current]` → `zoom current`. Post: `count_atoms("current")>0`; `count_atoms("alt_destiny")>0`; both `& rep sticks` visible. NOTE: 5.4 specs the 2-object MolAction SHAPE (data-level, mirrors 5.3 §D); the viewport SPLIT (side-by-side) is deferred to Phase 6 (OQ-8).
  - **Type 5 rng-shuffle:** `hide_all` → `load citrate` (sticks, gray) → `select_focus prochiral_pair` (`citrate and <prochiral_carbons_sele>`, == 2 carbons — a STRUCTURAL fact, Phase 7 source-approved via `TCA-RNG-CITRATE-PROCHIRALITY-01`) → `show spheres prochiral_pair` → `color hero_gold prochiral_pair` → `label prochiral_pair "? (RNG decides)"` → `zoom prochiral_pair`. Post: `count_atoms("citrate")>0`; `count_atoms("prochiral_pair")==2`; `count_atoms("prochiral_pair & rep spheres")>0`.
  - **Type 6 ending (4 tier-templates):** common skeleton (`hide_all` → tier-reveal → label → zoom) + tier-specific signature. true=`load atp`/sticks/hero_gold/label "ATP — your soul powers the cell" (the soul-jump climax, anti-confusion §3.6); good=`load <retained_product>`/sticks/hero_gold/label "<product> — your carbon lives on"; normal=`load release`/sticks/gray/label "CO2 — the everyday exit"; bad=`load broken`/cartoon/red/label "<bad> — the host fades". Post per tier (count>0, rep visible, color name, label present).
  - Honesty note (mirror 5.3 §E): the prototype proves the SCENE-TEMPLATE MECHANISM (the MolAction sequence produces the expected reps/colors/labels on a placeholder), NOT real science. The `== <expected_active_site_residue_count>` assertions use placeholder values; a REAL active-site-residue-count gate is Phase 7 content.

**§5. SC3 — the cast-reveal convention (per RESEARCH-scene-templates-cast-reveal-design §C).** Document:
- **§5.1 Full-then-zoom vs active-site-first (RESEARCH-scene-templates §C.1).** RECOMMEND full-then-zoom as the DEFAULT for the 8 PDB-bearing edit-allowed enzymes (the dramatic "meet the cast member" beat — the RPG cast framing, CAST-01). Tradeoff: active-site-first wins for RE-ENTRY (skip the full reveal on a re-visit); Phase 6 MAY implement a first-visit-vs-re-entry branch (track visited enzyme_ids). 5.4 specs full-then-zoom as the cast-reveal (first-appearance) contract; the re-entry optimization is Phase 6 discretion (OQ-7). Exception: the 6 narrative-only edit-allowed nodes (no PDB) — active-site-first is moot; they get substrate-traversal unless Phase 7 loads a structure.
- **§5.2 The cast-reveal sequence (the active-site-reveal Type 3 template, detailed).** Provide the full `on_enter` sequence (the BEFORE state of the mutant→WT arc): `hide_all` → (1) `load <enzyme>` → `show_as cartoon <enzyme>` → `color gray <enzyme>` (the cast member enters full-body) → (2) `select_focus <enzyme>_activesite <active_site_sele>` → `zoom <enzyme>_activesite` (camera dives to the active site) → (3) `show sticks <enzyme> and <catalytic_residues_sele>` → `color green <enzyme> and <catalytic_residues_sele>` (WT catalytic machinery, healthy) → (4) `show spheres <enzyme> and <mutant_residue_sele>` → `color red <enzyme> and <mutant_residue_sele>` (disease mutant, DANGER — rep redundancy: spheres vs sticks = colorblind-safe) → (5) `show sticks <enzyme> and <cofactor_sele>` → `color orange <cofactor_sele>` (cofactor, if any) → (6) `label <enzyme> and <label_anchor_sele> and name CA "<EnzymeName>"` (nameplate, headless-verifiable) + `label <catalytic_residues_sele> and name CA resn+'-'+resi` (residue labels) → (7) `[hero-highlight §3.3 if substrate co-loaded]` → `zoom <enzyme>_activesite`. Structural variant (`tca.citrate_synthase`, `edit:structural`, NO mutant): OMIT step 4. Post-conditions: `count_atoms("<enzyme>")>0`; `count_atoms("<enzyme> & rep cartoon")>0`; `count_atoms("<enzyme>_activesite")>0` (Phase 7 fills `== <expected>`); `count_atoms("<enzyme> and <mutant_residue_sele> & rep spheres")>0` (disease variant only); `count_atoms("<enzyme> and <catalytic_residues_sele> & rep sticks")>0`; label present (`iterate label` matches nameplate).
- **§5.3 Labeling (RESEARCH-scene-templates §C.2).** Two label layers: (1) enzyme nameplate (`cmd.label` fixed string on one anchor CA — headless-verifiable via `iterate label`); (2) residue labels (`resn+'-'+resi` on catalytic CAs — "specific residue representations shown", CAST-05). Phase 6 Qt overlay (screen-corner nameplate) is a polish layer ON TOP, NOT a replacement — the `cmd.label` is the floor (works headlessly + survives save/load via MolAction replay). The new `op="label"` dispatch wraps the quoted-string footgun (§2.1).
- **§5.4 The mutant→WT visual arc + composition with Phase 5.3 (RESEARCH-scene-templates §C.4 — the KEY composition point).** Table: Stage | Where it fires | What the player sees | Who owns it. BEFORE (mutant, broken) = edit-allowed node `on_enter` (5.4, this §5) — gray cartoon + green catalytic sticks + red mutant spheres. [player edits] = EditRouter routes (Phase 6 UI). AFTER (WT, restored) = branch node `on_enter` (5.3, the WT-aligned restoration reveal) — `edit` reverse-mutation → `load <enzyme>_wt` → `align` → present, colored `wt_color=green`. **Composition contract: 5.4 OWNS the BEFORE (mutant red spheres at edit-allowed on_enter) + the named color values (wt_color=green, mutant_color=red, filling 5.3's `<mutant_color>`/`<wt_color>` slots, `05.3-CONVENTION.md:318-319`). 5.3 OWNS the AFTER (the WT-aligned reveal mechanism). 5.4 does NOT re-litigate the 5.3 align mechanism.** The cast-reveal (BEFORE) + the restoration reveal (AFTER) use the SAME palette so the before/after arc is visually coherent (red→green = broken→restored). Hero-identity anti-confusion (AGENTS.md): the cast-reveal shows the ENZYME's health (mutant vs WT), NOT the hero carbon's fate. At `pyr.pdh` the carbon IS shed as CO2 while the hero's electrons continue — the cast-reveal shows the PDH ENZYME restored, NOT the carbon becoming ATP. Keep the enzyme-health framing and the carbon-fate framing SEPARATE (mirror 5.3 §5).

**§6. Downstream contracts (minimum surface — mirror 5.3 §7 + RESEARCH-scene-templates §E).** Keep it minimal — a convention + template library, not a framework. Four subsections:
- **§6.1 For Phase 6 (Qt UI MVP):** Phase 6 replays the per-node-type `on_enter` MolAction sequences via `molops.apply` (the 02-04 per-action contract). 5.4 contracts: the 6 scene-type sequences (§4) + the cast-reveal sequence (§5) + the hero-highlight sequence (§3.3) + the shared palette (§2.3). The UI must NOT invent colors/reps/labels (use the named palette + the templates). New molops ops Phase 6 lands: `op="set_color"` + `op="label"` + `op="set"` (§2.1, dispatch deferred from 5.4 per OQ-1) + `op="align"` (5.3 §6, already deferred). All are direct `cmd.*` calls (no new injected dependency). Phase 6 MAY add (NOT contracted): `cmd.scene` cache for re-entry; multi-viewport side-by-side (OQ-8); Qt text overlays supplementing `cmd.label`; first-visit-vs-re-entry cast-reveal branch (OQ-7).
- **§6.2 For Phase 7 (content authoring):** the per-asset metadata fields. Reproduce the RESEARCH-scene-templates §E.2 table (enzyme_pdb, active_site_sele, catalytic_residues_sele, mutant_residue_sele, cofactor_sele, label_anchor_sele, reveal_variant) + the hero metadata (RESEARCH-hero-highlight §E.2: hero_selector, hero_label, hero_color, soul). Citation-gate note: the selectors are structural facts BUT "which residue is catalytic"/"which is the disease mutation"/"which carbon is the hero" ARE cited science claims (the `DIS-*-cand` + catalytic + carbon-fate claims, HIGH-STAKES per PROJECT.md row 109). Phase 7 fills with source-approved values. The CO2-shed + soul-transfer nodes (`tca.co2_turn1`/`turn2`, `etc.*`, `end.true`): Phase 7 authors the `on_enter` sequences implementing the metamorphosis visual (§3.5) + the MANDATORY anti-confusion `text_teaching`.
- **§6.3 For Phase 9 (full cast ~20+ enzymes):** the cast-reveal convention (§5) + the active-site-reveal template (Type 3) GUARANTEE consistency across the cast BY CONSTRUCTION — every enzyme enters via the SAME template (full cartoon gray → zoom active site → catalytic green sticks → mutant red spheres → nameplate). Phase 9 fills the per-enzyme metadata (§6.2); the visual language is consistent because the template + palette are fixed, NOT ad-hoc. Phase 9 must NOT invent per-enzyme color schemes. The large-enzyme visibility/zoom (when the co-loaded substrate is tiny vs a huge enzyme like 1OCC) is Phase 9 (the `zoom` scoped to the hero + active-site residues).
- **§6.4 For Phase 12 (ending cutscene rendering):** the Type 6 ending-tier templates compose with the `cmd.ray` render workflow — the ending `on_enter` SETS the scene (load product → show_as → color → label → zoom); `cmd.ray` RENDERS it. Phase 12 renders the scene the Type 6 template produces (does NOT re-pose/re-color beyond the template + `cmd.ray` settings). The 4 tier-templates give Phase 12 4 distinct visual signatures (victory/storage/release/catastrophe).

**§7. Open questions for the human design-review checkpoint.** List the 11 open questions (5 from RESEARCH-hero-highlight §F + 6 from RESEARCH-scene-templates §F), each with: the question, the research RECOMMENDATION, the TRADEOFF/alternative, a one-line "why it matters", and a status (RESOLVED-in-spec-confirm-at-checkpoint vs OPEN). Number them OQ-1 through OQ-11:
  - **OQ-1 [RESOLVED in §2.2, confirm].** Op-granularity: 3 minimal new ops (set_color/label/set) + sequences vs the composite `op="hero_highlight"` super-op. Rec: the minimal-orthogonal approach (adopted in §2.2). Alt: the super-op. Why: keeps dispatch minimal + per-step testable + mirrors 5.3. **Also sub-decision: where the 3 new-op dispatches land** — Rec: defer to Phase 6 (5.4 prototypes cmd.* directly); Alt: the 04-05 precedent (add in 5.4). FLAG: if overridden to "add in 5.4", Plans 02/03 gain tasks to edit `molops.py` + add unit tests.
  - **OQ-2 [OPEN].** Mutant color: `red` SPHERES (rep redundancy, strong danger signal) vs `magenta` (pure-hue colorblind-safe). Rec: red spheres. Alt: magenta. Why: red=danger is the strongest intuitive signal; rep redundancy addresses colorblind-safety. FILLS 5.3's `<mutant_color>`.
  - **OQ-3 [OPEN].** Hero color: `hero_gold` (Okabe-Ito orange) vs `hero_cyan` vs Phase-10-deferred. Rec: hero_gold. Alt: cyan (collides with atom/stick colors). Why: the hero must be INSTANTLY distinguishable; colorblind-safe.
  - **OQ-4 [OPEN].** Label wording: `"C14"` (isotope tracking label) vs `"YOU"` (player-facing) vs both. Rec: "C14" primary. Alt: "YOU". Why: the hero's IDENTITY label; "C14" is canonical, "YOU" may be more engaging.
  - **OQ-5 [OPEN].** CO2-shed visual handling (the climax): fade + transfer (the soul-jump metamorphosis) vs fade-only vs transfer-only. Rec: fade + transfer. Alt: fade-only (contradicts metamorphosis). Why: the dramatic + scientific climax; the visual must match the soul-jump WITHOUT implying the carbon becomes ATP (§3.6 anti-confusion).
  - **OQ-6 [OPEN].** Multi-carbon fallback: FAIL LOUDLY (no silent best-guess) vs best-guess heuristic + warning. Rec: FAIL LOUDLY for real content. Alt: heuristic. Why: silently highlighting the wrong carbon is a fabricated-science risk; hero-identity is load-bearing.
  - **OQ-7 [OPEN].** Cast-reveal: full-then-zoom vs active-site-first. Rec: full-then-zoom default; active-site-first as Phase 6 re-entry optimization. Alt: active-site-first always. Why: the RPG cast framing depends on the enzyme-as-character beat.
  - **OQ-8 [OPEN].** Branch-point side-by-side: 5.4 specs 2-object data-level shape (Phase 6 chooses overlay/viewport) vs mandate side-by-side now. Rec: data-level shape; viewport split deferred to Phase 6 (mirrors 5.3 §D). Alt: mandate now. Why: multi-viewport is a Phase 6 UI layout concern.
  - **OQ-9 [OPEN].** Ending-tier distinctness: 4 tier-templates vs 1 parametric. Rec: 4 tier-templates (distinct victory/storage/release/catastrophe signatures). Alt: 1 + tier param. Why: the 4 tiers are narratively distinct; distinct templates make the tier immediately visually legible.
  - **OQ-10 [OPEN].** Narrative-only edit-allowed nodes (no PDB): substrate-traversal (current) or active-site-reveal? Rec: 5.4 specs BOTH; Phase 7 decides per enzyme (mirrors 5.3 OQ-DM2). Alt: mandate a structure. Why: forcing a structure may hit the no-human-PDB wall.
  - **OQ-11 [OPEN].** Sub-beats "substrate approach"/"product release": separate MolAction steps or folded into the single reveal? Rec: 5.4 specs the SINGLE-NODE reveal; sub-beats are an OPTIONAL Phase 6 enrichment. Alt: mandate 3-sub-beat. Why: the skeleton's one-node-per-step granularity doesn't need 3 sub-beats for the MVP.
  - Also note the RESOLVED-by-research items the convention adopts without an open question: fixture reuse (reuse `_smoke.pdb`/`_edit_smoke.pdb`, RESEARCH-api §I OQ-5 — RESOLVED: reuse, no new fixtures); `cmd.scene` as scene-template primitive (RESEARCH-api §I OQ-4 — RESOLVED: NOT used for templates, optional Phase 6 cache); PubChem atom naming (RESEARCH-api §I OQ-3 — Phase 7 concern, does not block 5.4); palette RGB source (RESEARCH-api §I OQ-2 — Phase 7 source/approval).

End the document with a "## §8. Review checklist for the human" section listing the review criteria for the Plan 04 checkpoint (mirror 5.3's review checklist). Criteria: (1) hero-selection-rule soundness (hybrid rule, fail-loudly, the 3 structural cases); (2) op-granularity resolution soundness (3 minimal ops vs super-op; the 5.3 precedent; per-step testability); (3) scene-template completeness (6 types map all 55 nodes; MolAction sequences not cmd.scene; save/load-safe); (4) cast-reveal consistency (full-then-zoom; gray/green/red/orange palette; rep-redundancy colorblind-safety); (5) 5.3 composition soundness (5.4 BEFORE mutant + 5.3 AFTER WT; the <mutant_color>/<wt_color> slots filled; enzyme-health vs carbon-fate separation); (6) CO2-shed climax + anti-confusion (fade+transfer metamorphosis; the MANDATORY note that the transfer is a narrative device, NOT carbon→ATP); (7) downstream contract sufficiency + minimality (6/7/9/12); (8) the 11 open questions are all surfaced with recommendations; (9) no fabricated science (PLACEHOLDERS only; zero real PDB IDs).
  </action>
  <verify>
`grep -c "^##" 05.4-CONVENTION.md` shows >= 8 level-2 headers (§1-§8). `grep -qi 'scene.template\|MolAction sequence\|cmd.scene' 05.4-CONVENTION.md` matches (SC2 scene templates documented + the cmd.scene rejection). `grep -qi 'full.then.zoom\|cast.reveal\|nameplate' 05.4-CONVENTION.md` matches (SC3 cast-reveal documented). `grep -qi 'wt_color.*green\|mutant_color.*red\|<mutant_color>\|<wt_color>' 05.4-CONVENTION.md` matches (the 5.3 composition + the color slots filled). `grep -c 'OQ-1\|OQ-2\|OQ-3\|OQ-4\|OQ-5\|OQ-6\|OQ-7\|OQ-8\|OQ-9\|OQ-10\|OQ-11' 05.4-CONVENTION.md` shows all 11 open questions present. `grep -qi 'tca.co2_turn1\|tca.shuffle\|end.true\|gly.pfk' 05.4-CONVENTION.md` matches (the scene templates key to real skeleton node ids). `grep -qi 'Phase 6\|deferred to Phase 6\|Phase 7\|Phase 9\|Phase 12' 05.4-CONVENTION.md` matches (downstream contracts documented). `grep -qi 'review checklist\|Review checklist' 05.4-CONVENTION.md` matches (the §8 checklist). Confirm NO real PDB IDs invented: `grep -oE '\b[1-9][0-9][A-Z0-9]{3}\b' 05.4-CONVENTION.md | sort -u` should list NONE. Confirm `c14/pymol_layer/molops.py` was NOT modified: `git diff --name-only c14/pymol_layer/molops.py` shows no changes. Confirm `c14/story/model.py` was NOT modified: `git diff --name-only c14/story/model.py` shows no changes (the 3 new ops are string keys + docstring mentions only; the dispatch is specified-only, deferred to Phase 6).
  </verify>
  <done>
`05.4-CONVENTION.md` complete with §4 (SC2 scene-template library: 6 types mapping all 55 nodes, MolAction sequences not cmd.scene, per-type post-conditions), §5 (SC3 cast-reveal: full-then-zoom, the cast-reveal sequence, labeling, the mutant→WT arc + 5.3 composition filling <mutant_color>/<wt_color>), §6 (downstream contracts 6/7/9/12), §7 (the 11 open questions OQ-1 to OQ-11 with recommendations + tradeoffs + statuses), §8 (the review checklist). `molops.py` + `model.py` NOT touched (dispatch deferred to Phase 6). No real PDB IDs; the convention is internally consistent across all 3 SCs (shared op vocabulary + shared palette).
  </done>
</task>

</tasks>

<verification>
- `05.4-CONVENTION.md` exists in the phase directory and is a coherent, reviewable design artifact (>= 500 lines, >= 8 sections) covering SC1 + SC2 + SC3.
- The op-granularity disagreement is RESOLVED in §2.2: 3 minimal new peer-primitive ops (set_color/label/set) + SHORT sequences, NOT a super-op; the rejected super-op alternative is documented; the rationale mirrors the 5.3 precedent.
- The shared op vocabulary + shared named-color palette are consistent across all 3 SCs.
- The 5.3 composition is honored (5.4 BEFORE mutant red spheres; 5.3 AFTER WT green; 5.4 fills the <mutant_color>/<wt_color> slots).
- The CO2-shed climax has a MANDATORY anti-confusion note (soul-transfer is a narrative device, NOT carbon→ATP).
- All 11 open questions (OQ-1 to OQ-11) are surfaced with recommendations for the Plan 04 checkpoint.
- No fabricated science: zero real PDB IDs (only `pdb:TBD_*` placeholders); no network fetches.
- `c14/pymol_layer/molops.py` and `c14/story/model.py` are NOT modified (pure design writing; the 3 new-op dispatches are SPEC'd but DEFERRED to Phase 6, mirroring 5.3-01).
- `python3.6 -m py_compile` is N/A (no .py files in this plan); no tests added (design phase).
</verification>

<success_criteria>
- SC1 delivered (spec): the C14 hero highlight convention is specified (selection rule + color + label + persistence + the 3 structural cases + the CO2-shed climax + anti-confusion) and documented for review.
- SC2 delivered (spec): the scene-template library exists (6 types mapping all 55 nodes, MolAction sequences not cmd.scene, per-type post-conditions).
- SC3 delivered (spec): the cast-reveal convention is specified (full-then-zoom, labeling, palette, 5.3 composition) and documented for review.
- The op-granularity disagreement is resolved (3 minimal new ops, not a super-op) with rationale + the rejected alternative recorded.
- The 11 open questions are surfaced for the Plan 04 human-review checkpoint.
- No code modified; no fabricated science; planning doc committed.
- SC4 (human review) is deferred to Plan 04 — this plan delivers the spec the checkpoint reviews.
</success_criteria>

<output>
After completion, create `.planning/phases/05.4-cast-hero-representation-design/05.4-01-SUMMARY.md`
</output>
