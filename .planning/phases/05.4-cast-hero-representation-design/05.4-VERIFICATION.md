---
phase: 05.4-cast-hero-representation-design
verified: 2026-08-29T21:30:00Z
status: passed
score: 4/4 success criteria verified (SC1+SC2+SC3+SC4); all must_haves met
re_verification: false
---

# Phase 5.4: Cast & Hero Representation Design — Verification Report

**Phase Goal:** The 3D representation language is designed as a reviewable artifact — the C14 hero highlight convention, the per-stage-type scene templates, and the cast-reveal convention — headless-prototyped on placeholder/bundled structures, so Phase 6 implements an already-reviewed visual design rather than inventing it inside the overloaded first-Qt phase. Resolves the user's concern about cast representation and hero representation.

**Verified:** 2026-08-29T21:30:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (the 4 Roadmap Success Criteria)

| # | Truth (SC) | Status | Evidence |
| --- | --- | --- | --- |
| 1 | **SC1** — A C14 hero highlight convention is specified (selection rule + color + label + persistence + 3 structural cases + CO2-shed climax + anti-confusion) AND headless-prototyped (count==1, elem C, rep spheres, color round-trip, label) | ✓ VERIFIED | `05.4-CONVENTION.md` §3 (766 lines total) specifies the hybrid selection rule (§3.1 w/ OQ-6 default+warn+confirm), the 3 structural cases (§3.2), the 6-call semantic sequence (§3.3 w/ OQ-3 cyan+all-C+ball-and-stick+YOU, NO scoped-dim), persistence as pure function of on_enter (§3.4), the CO2-shed climax (§3.5), the MANDATORY anti-confusion note (§3.6). `tools/hero_highlight_smoke.py` (444 lines) re-run by verifier → `SMOKE_RESULT: PASS` (29/29); `cmd.iterate` verifies count==1, elem C, color-name round-trip, label=="YOU". |
| 2 | **SC2** — A scene-template library exists (6 scene types mapping all 55 skeleton nodes as MolAction sequences NOT cmd.scene, with per-type post-conditions) AND headless-prototyped | ✓ VERIFIED | `05.4-CONVENTION.md` §4 — §4.1 (MolAction sequences NOT cmd.scene, save/load-safe), §4.2 (6 types: preface/substrate-traversal/active-site-reveal/branch-point/rng-shuffle/ending-4-tiers mapping all 55 nodes by id), §4.3 (per-type draft sequences + post-conditions). `tools/scene_template_smoke.py` (520 lines) re-run → `SMOKE_RESULT: PASS` (37/37); all 6 types exercised with rep-visible/color/label/named-selection-count post-conditions. |
| 3 | **SC3** — A cast-reveal convention is specified (full-then-zoom + gray cartoon → green catalytic sticks → mutant spheres → nameplate label + 5.3 composition) AND headless-prototyped | ✓ VERIFIED | `05.4-CONVENTION.md` §5 — §5.1 (full-then-zoom), §5.2 (cast-reveal sequence: full gray cartoon → zoom → WT green sticks → mutant **magenta** spheres → cofactor orange sticks → nameplate label), §5.4 (mutant→WT arc + 5.3 composition: BEFORE magenta mutant + AFTER WT green, fills `<mutant_color>`/`<wt_color>` slots). Scene smoke exercises Type 3 disease + structural variants + Stage 7 composition (`comp_before_mutant_magenta_spheres` + `comp_after_wt_green` both PASS). |
| 4 | **SC4** — The convention is human-reviewed for clarity (design-review checkpoint; the real in-game human-verify is Phase 6) | ✓ VERIFIED | `05.4-04-SUMMARY.md` records the checkpoint outcome APPROVED-WITH-ADJUSTMENTS (5 overrides: OQ-2/3/4/6/11; 6 approve-rec: OQ-1/5/7/8/9/10); the 11-OQ decisions table; the convention FROZEN state. `05.4-CONVENTION.md` header status = FROZEN with the 4 SC checkboxes checked. `STATE.md` records Phase 5.4 COMPLETE (4/4). |

**Score:** 4/4 success criteria verified

### Required Artifacts (three-level check: exists / substantive / wired)

| Artifact | Expected | Exists | Substantive | Wired | Status | Details |
| --- | --- | --- | --- | --- | --- | --- |
| `.planning/phases/05.4-cast-hero-representation-design/05.4-CONVENTION.md` | SC1+SC2+SC3 spec; min 500 lines; contains `op="set_color"` | ✓ 766 lines | ✓ no TODO/FIXME/stubs; 10 H2 sections §1-§8 + SC checklist + §7 OQ table + §8 review checklist | ✓ referenced by both smokes; consumed by Phases 6/7/9/12 (§6 contracts) | ✓ VERIFIED | `op="set_color"` present (§2.1 line 56); header FROZEN; all 5 overrides applied (cyan/magenta/YOU/default+warn+confirm/OQ-11 future-note) |
| `tools/hero_highlight_smoke.py` | SC1 mechanism; min 200 lines; contains `SMOKE_RESULT` | ✓ 444 lines | ✓ 29 PASS / 0 FAIL on verifier re-run; anti-confusion comment present; # src citations | ✓ calls `cmd.*` directly (NOT via molops); uses `c14.paths.data_path` for fixtures; implements §3.3 sequence | ✓ VERIFIED | `SMOKE_RESULT: PASS` (29/29) on verifier re-run; `cmd.iterate` count==1/elem C/color-round-trip/label=="YOU" |
| `tools/scene_template_smoke.py` | SC2+SC3 mechanism; min 250 lines; contains `SMOKE_RESULT` | ✓ 520 lines | ✓ 37 PASS / 0 FAIL on verifier re-run; anti-confusion comment present; 6 types + cast-reveal + composition | ✓ calls `cmd.*` directly (NOT via molops); uses `c14.paths.data_path` for 3 fixtures; implements §4/§5 sequences | ✓ VERIFIED | `SMOKE_RESULT: PASS` (37/37) on verifier re-run; all 6 types + 4 ending tiers + cast-reveal + 5.3 composition |
| `.planning/phases/05.4-cast-hero-representation-design/05.4-04-SUMMARY.md` | SC4 checkpoint outcome; contains `SC4` | ✓ 214 lines | ✓ records APPROVED-WITH-ADJUSTMENTS + 11 OQ table + FROZEN + deviations | ✓ STATE.md updated; key-links to Phase 6 | ✓ VERIFIED | `SC4` present; all 11 OQs recorded; FROZEN; 5 overrides + 6 approve-rec |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `05.4-CONVENTION.md` | 55-node skeleton (`data/story_glucose/*.json`) | 6 scene types map all 55 skeleton node ids | ✓ WIRED | §4.2 table lists every node id per type (e.g. `intro.preface`, `gly.pfk`, `tca.shuffle`, `end.true`); pattern `intro\.preface\|gly\.pfk\|tca\.shuffle\|end\.true` matches |
| `05.4-CONVENTION.md` | Phase 6 molops dispatch (`op="set_color"/"label"/"set"`) | shared op vocabulary + 3 new-op dispatch pseudocode (deferred to Phase 6) | ✓ WIRED | §2.1 defines the 3 new-op MolAction shapes; §2.2 states dispatch deferral to Phase 6 (OQ-1); pattern `op == "(set_color\|label\|set)"` matches; molops.py UNTOUCHED (git diff empty) |
| `05.4-CONVENTION.md` | 5.3 convention (`05.3-CONVENTION.md`) | cast-reveal BEFORE (5.4) composes with 5.3 AFTER; fills `<mutant_color>`/`<wt_color>` slots | ✓ WIRED | §5.4 composition section: BEFORE mutant magenta spheres + AFTER WT green; `<mutant_color>`=magenta / `<wt_color>`=green slots filled; pattern `<mutant_color>\|<wt_color>\|05\.3` matches |
| `05.4-CONVENTION.md` | Phase 7 per-asset metadata | `hero_selector` + `active_site_sele` + `catalytic_residues_sele` contract | ✓ WIRED | §6.2 metadata table: `hero_selector`/`hero_label="YOU"`/`hero_color="hero_cyan"`/`soul` + 7-field enzyme metadata; pattern `hero_selector\|catalytic_residues_sele\|active_site_sele` matches |
| `tools/hero_highlight_smoke.py` | `tmp/pymol-src/modules/pymol/*` (cmd.set_color/label/show_as/color/set/iterate) | direct `cmd.*` calls with `# src:` citations (NOT via molops) | ✓ WIRED | `from pymol import cmd`; calls cmd.set_color/label/show_as/color/set/iterate/get_color_indices/count_atoms directly; `# src: tmp/pymol-src/...` citations present |
| `tools/hero_highlight_smoke.py` | existing fixtures (`_smoke.pdb` + `_edit_smoke.pdb`) | `c14.paths.data_path(...)` cwd-independent resolution | ✓ WIRED | `c14.paths.data_path("data","assets","bundled","_smoke.pdb")` + `_edit_smoke.pdb`; NO new fixtures (git status clean) |
| `tools/hero_highlight_smoke.py` | `05.4-CONVENTION.md` §3 | implements §3.3 sequence (hero_cyan/sphere_scale/gray80 for CO2 fade) on §3.2 cases + §3.5 climax | ✓ WIRED | `hero_cyan`/`sphere_scale 0.3`/`gray80` (CO2 fade) all present; 3 cases + CO2-shed climax exercised |
| `tools/scene_template_smoke.py` | `tmp/pymol-src/modules/pymol/*` (cmd.show/show_as/color/zoom/label/select/count_atoms/iterate) | direct `cmd.*` calls with `# src:` citations (NOT via molops; NOT cmd.scene) | ✓ WIRED | direct `cmd.*` calls; `# src:` citations; NOT cmd.scene |
| `tools/scene_template_smoke.py` | existing fixtures (`_smoke.pdb` + `_edit_smoke.pdb` + `_wt_align_wt.pdb`) | `c14.paths.data_path(...)` | ✓ WIRED | 3 fixtures via `c14.paths.data_path`; `_wt_align_wt.pdb` for the 5.3 composition AFTER case; NO new fixtures |
| `tools/scene_template_smoke.py` | `05.4-CONVENTION.md` §4/§5 + 5.3 composition | implements §4 6-type sequences + §5 cast-reveal + 5.3 composition | ✓ WIRED | cartoon/sticks/spheres + gray/green/magenta/orange/hero_cyan all present; composition BEFORE magenta + AFTER green |
| `05.4-04-SUMMARY.md` | Phase 6 (Qt UI MVP) | SUMMARY records human-approved FROZEN convention; Phase 6 implements dispatches WITHOUT re-designing | ✓ WIRED | `approved`/`APPROVED`/`OQ-1`/`Phase 6`/`FROZEN` all present; "Next Phase Readiness" section details Phase 6 contract |

### The 5 Human Checkpoint Overrides (verified reflected in BOTH SUMMARY and CONVENTION spec)

| Override | SUMMARY (05.4-04) | CONVENTION spec (actual artifact) | Status |
| --- | --- | --- | ✓ |
| **OQ-2** mutant = magenta (NOT red) | ✓ key-decisions + OQ table line 111 | ✓ §2.3 palette mutant row = `magenta` (line 153); §5.2 step 4 = magenta; §5.4 `<mutant_color>`=magenta; bad-ending `red` retained (different context, line 156) | ✓ VERIFIED |
| **OQ-3** cyan + all-C-colored + ball-and-stick + YOU + NO scoped-dim | ✓ key-decisions + OQ table line 112 | ✓ §2.3 hero row = `hero_cyan` [0.0,0.75,0.75] (line 150); §3.3 sequence = all-C cyan sticks + ball-and-stick sphere_scale 0.3 + "YOU" + non-carbons keep element colors (line 217); scoped-dim REMOVED (line 247: "old scoped-dim was REMOVED"); gray80 now ONLY for CO2-shed fade | ✓ VERIFIED |
| **OQ-4** label = "YOU" (NOT "C14") | ✓ key-decisions + OQ table line 113 | ✓ §3.3 step 7 label "YOU" (line 228); §6.2 `hero_label` default = "YOU" (line 638); "C14" only as the concept (line 28 anti-confusion), NOT the on-screen label | ✓ VERIFIED |
| **OQ-6** multi-C fallback = default + warn + confirm gate | ✓ key-decisions + OQ table line 115 | ✓ §3.1 step 3 (line 183): "auto-pick a DEFAULT carbon + WARN + yes/no CONFIRM (OQ-6 OVERRIDE)"; §7 OQ-6 decision (line 700); confirm IS the no-fabricated-science guard | ✓ VERIFIED |
| **OQ-11** single-node for MVP + 3-sub-beat deferred FUTURE enrichment | ✓ key-decisions + OQ table line 120 | ✓ §4.3 Type 3 note (line 408): "DEFERRED FUTURE ENRICHMENT, NOT a permanent skip"; §6.5 (line 652); §7 OQ-11 decision (line 730) | ✓ VERIFIED |

### Cross-Cutting Constraints

| Constraint | Expected | Status | Evidence |
| --- | --- | --- | --- |
| `c14/pymol_layer/molops.py` NOT modified | dispatch deferred to Phase 6 (OQ-1) | ✓ MET | `git diff --name-only c14/pymol_layer/molops.py` → empty |
| `c14/story/model.py` NOT modified | no model change (MolAction already supports arbitrary args) | ✓ MET | `git diff --name-only c14/story/model.py` → empty |
| NO new fixture files created | reuse `_smoke.pdb`/`_edit_smoke.pdb`/`_wt_align_wt.pdb` | ✓ MET | `git status --porcelain c14/data/assets/bundled/` → empty |
| Zero real PDB IDs in CONVENTION | only `pdb:TBD_*` placeholders (no fabricated science) | ✓ MET | `grep -oE '\b[1-9][0-9][A-Z0-9]{3}\b' 05.4-CONVENTION.md \| sort -u` → empty |
| MANDATORY anti-confusion note (§3.6) | soul-transfer = narrative device, NOT carbon→ATP; C14-decay DROPPED | ✓ MET | §3.6 (lines 300-315) boxed note: "narrative/visual device"; "NOT assert the C14 carbon itself becomes ATP"; "C14 radioactive-decay DROPPED"; text_teaching contract |
| 5.3 composition honored | 5.4 BEFORE (magenta mutant) + 5.3 AFTER (WT green); slots filled | ✓ MET | §5.4 (line 584+): BEFORE mutant magenta spheres + AFTER wt_color=green; `<mutant_color>`=magenta / `<wt_color>`=green filled |
| Op-granularity RESOLUTION | 3 minimal new ops (set_color/label/set), NOT a super-op; rejected super-op documented | ✓ MET | §2.2 (lines 91-118): ADOPTED 3 peer-primitive ops; REJECTED `op="hero_highlight"` super-op documented with shape + pros + cons + explicit REJECTION |
| AST gate (`python3.6 tools/check_imports.py`) | exits 0 | ✓ MET | verifier ran → `check_imports: clean (no pymol/PyQt5 imports in c14/ domain tier)` exit=0 |

### Smoke Re-Run Results (verifier-executed, NOT trusting SUMMARY claims)

| Smoke | Command | Expected | Actual | Count |
| --- | --- | --- | --- | --- |
| Hero highlight (SC1) | `bash tools/run_headless.sh tools/hero_highlight_smoke.py` | `SMOKE_RESULT: PASS` | `SMOKE_RESULT: PASS` | 29 PASS / 0 FAIL |
| Scene template + cast-reveal (SC2+SC3) | `bash tools/run_headless.sh tools/scene_template_smoke.py` | `SMOKE_RESULT: PASS` | `SMOKE_RESULT: PASS` | 37 PASS / 0 FAIL |

Both smokes re-run by the verifier (not the executor's claimed run). The smoke output confirms the override markers are live: hero smoke `case1_color_hero_cyan`, `case1_label_YOU`, `case2_all_carbons_cyan`, `climax_co2_faded co2-faded-color='gray80'`, `climax_nadh_highlighted`; scene smoke `t3_mutant_color_magenta color='magenta'`, `comp_before_mutant_magenta_spheres`, `comp_after_wt_green`, `t6_true_label label='YOU - soul'`.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| `tools/scene_template_smoke.py` | 222-223 | Stale comment: "mutant red SPHERES" describes the OLD design, but the actual `cmd.color("magenta", ...)` (line 247) + the passing check `t3_mutant_color_magenta` enforce the OQ-2 OVERRIDE (magenta) | ⚠️ Warning | Misleading comment only; the behavior is correct (verified by the passing smoke). Not goal-blocking. Recommend a 1-line comment fix to "mutant magenta SPHERES" in a future pass. |
| `05.4-CONVENTION.md` | 652/656 | §6.5 (Future development) appears before §6.4 (Phase 12) — cosmetic section ordering | ℹ️ Info | Both sections present + substantive; no functional impact. |

### Human Verification Required

None blocking. Phase 5.4 is a DESIGN phase; the real in-game human-verify is Phase 6 (the Qt MVP), as explicitly stated in the phase goal and the §8 review checklist. The SC4 design-review checkpoint was ALREADY human-verified (APPROVED-WITH-ADJUSTMENTS, recorded in `05.4-04-SUMMARY.md`). The two headless mechanism proofs (smokes) were re-run by the verifier and PASS — they prove the PyMOL API MECHANISM on placeholders, not real science (real per-enzyme structures/hero-selectors/catalytic-residue selections are Phase 7 content).

### Gaps Summary

**No gaps found.** All 4 success criteria (SC1+SC2+SC3+SC4) are verified against the actual artifacts. All must_haves from the 4 PLAN frontmatters are met (truths, artifacts at all 3 levels, key links all wired). Both smokes re-run PASS by the verifier (29/29 hero, 37/37 scene). All 5 human checkpoint overrides are confirmed reflected in BOTH the SUMMARY and the actual CONVENTION spec (cyan hero, magenta mutant, "YOU" label, default+warn+confirm selection rule, OQ-11 future-note). All cross-cutting constraints are met (molops.py + model.py untouched, no new fixtures, zero real PDB IDs, anti-confusion note present, 5.3 composition honored, op-granularity resolved, AST gate clean).

Two minor non-blocking observations:
1. A stale comment in `scene_template_smoke.py` (line 222-223) says "mutant red SPHERES" but the code enforces magenta (the OQ-2 OVERRIDE). Behavior is correct; only the descriptive comment is stale. Recommend a 1-line comment fix.
2. §6.5 appears before §6.4 in the CONVENTION (cosmetic section ordering; both substantive).

Neither observation blocks goal achievement. The phase goal — a reviewable visual-design-language artifact backed by headless mechanism proofs and a human design-review checkpoint — is achieved. The convention is FROZEN and ready for Phases 6/7/9/12 to consume without re-designing.

---

_Verified: 2026-08-29T21:30:00Z_
_Verifier: OpenCode (gsd-verifier)_
_Method: goal-backward verification against actual codebase artifacts (CONVENTION spec + both smokes re-run + git diff + grep), NOT trusting SUMMARY claims_
