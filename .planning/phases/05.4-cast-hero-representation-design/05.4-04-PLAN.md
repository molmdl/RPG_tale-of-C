---
phase: 05.4-cast-hero-representation-design
plan: 04
type: execute
wave: 3
depends_on:
  - 05.4-02
  - 05.4-03
files_modified:
  - .planning/STATE.md
autonomous: false

must_haves:
  truths:
    - "The Cast & Hero Representation convention is human-reviewed for clarity (SC4): the human reviews 05.4-CONVENTION.md + the hero-highlight smoke output (Plan 02) + the scene-template/cast-reveal smoke output (Plan 03) against the §8 review checklist, and approves or specifies adjustments"
    - "All 11 open questions (OQ-1 to OQ-11) are decided by the human at the checkpoint (accept the research recommendations OR specify overrides), with OQ-1 (op-granularity + dispatch landing) being the most consequential — if overridden to 'add the 3 new-op dispatches in 5.4', follow-up revision tasks are noted in the SUMMARY for the molops.py edit + unit tests"
    - "The CO2-shed climax anti-confusion is confirmed by the human (the soul-transfer is a narrative device, NOT carbon→ATP) and the 5.3 composition is confirmed sound (5.4 BEFORE mutant red + 5.3 AFTER WT green; enzyme-health vs carbon-fate separation)"
  artifacts:
    - path: ".planning/phases/05.4-cast-hero-representation-design/05.4-04-SUMMARY.md"
      provides: "the post-checkpoint SUMMARY recording the human's approval + the OQ decisions + any follow-up work (e.g. if OQ-1 overridden)"
      contains: "SC4"
  key_links:
    - from: ".planning/phases/05.4-cast-hero-representation-design/05.4-04-SUMMARY.md"
      to: "Phase 6 (Qt UI MVP) — the convention is now REVIEWED + ready to consume"
      via: "the SUMMARY records the human-approved convention state; Phase 6 implements the 3 new-op molops dispatches (set_color/label/set) + the scene templates + the cast-reveal WITHOUT re-designing"
      pattern: "approved|OQ-1|Phase 6"
---

<objective>
Run the human design-review checkpoint (SC4) for the Cast & Hero Representation convention, then record the outcome + the 11 open-question decisions in the phase SUMMARY + STATE.

Purpose: Phase 5.4 is a design phase — the real in-game human-verify is Phase 6 (the Qt MVP). This plan's checkpoint is a DESIGN-REVIEW: the human reviews the convention spec (`05.4-CONVENTION.md` from Plan 01) + the two headless-prototype smoke outputs (Plan 02 hero-highlight + Plan 03 scene-template/cast-reveal) against the §8 review checklist + the 11 open questions, and approves or specifies adjustments. After approval, the convention is FROZEN for Phase 6/7/9/12 to consume without re-designing.

Output: a human-approved (or adjusted) convention + a post-checkpoint SUMMARY + STATE update recording the SC4 delivery + the OQ decisions. This plan is `autonomous: false` (has the blocking checkpoint). If the human overrides OQ-1 to "add the 3 new-op dispatches in 5.4", a follow-up revision note is added to the SUMMARY (the molops.py edit + MockCmd unit tests — NOT executed in this plan; flagged for the phase verifier / Phase 6).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.4-cast-hero-representation-design/05.4-CONVENTION.md
@.planning/phases/05.4-cast-hero-representation-design/05.4-01-SUMMARY.md
@.planning/phases/05.4-cast-hero-representation-design/05.4-02-SUMMARY.md
@.planning/phases/05.4-cast-hero-representation-design/05.4-03-SUMMARY.md

# The 5.3 checkpoint pattern to mirror (design-review checkpoint shape)
@.planning/phases/05.3-wt-aligned-structure-load-convention/05.3-02-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human design-review checkpoint of the convention spec + the two headless prototypes (SC4)</name>
  <what-built>
Plan 01 delivered `05.4-CONVENTION.md` — the Cast & Hero Representation convention spec covering SC1 (hero highlight: hybrid selection rule + hero_gold spheres + C14 label + scoped dim + persistence as pure function of on_enter + the 3 structural cases + the CO2-shed soul-transfer climax), SC2 (scene-template library: 6 types mapping all 55 nodes as MolAction sequences not cmd.scene + per-type post-conditions), SC3 (cast-reveal: full-then-zoom gray cartoon → green catalytic sticks → red mutant spheres → nameplate label + the 5.3 composition filling <mutant_color>/<wt_color>). The op-granularity disagreement is RESOLVED in §2.2 (3 minimal new ops set_color/label/set, NOT a super-op; the rejected super-op documented; dispatch deferred to Phase 6 per OQ-1). The 11 open questions (OQ-1 to OQ-11) are surfaced in §7.

Plan 02 delivered `tools/hero_highlight_smoke.py` running headlessly with `SMOKE_RESULT: PASS` — proving the hero-highlight MECHANISM across all 3 structural cases (hero-alone / hero-in-substrate / hero-at-enzyme-cast) + the CO2-shed soul-transfer (fade + transfer) on placeholder fixtures, with post-conditions (count==1, elem C, rep spheres, hero_gold color-name round-trip, label C14). Calls cmd.set_color/cmd.label/cmd.show_as/cmd.color/cmd.set DIRECTLY (dispatch deferred to Phase 6).

Plan 03 delivered `tools/scene_template_smoke.py` running headlessly with `SMOKE_RESULT: PASS` — proving the scene-template MECHANISM across all 6 scene types + the cast-reveal (disease + structural variants) + the 4 ending tiers + the 5.3 composition (BEFORE mutant red + AFTER WT green) on placeholder fixtures, with post-conditions (rep visible, color-name round-trip, label, named-selection count). Calls cmd.* DIRECTLY (NOT cmd.scene).
  </what-built>
  <how-to-verify>
This is a DESIGN-REVIEW checkpoint (the real in-game human-verify is Phase 6). Review the convention DECISIONS + confirm both headless mechanism proofs passed. Steps:

1. **Read the convention spec:** open `.planning/phases/05.4-cast-hero-representation-design/05.4-CONVENTION.md` and review each section against the "§8. Review checklist" at the end of the doc:
   - §2 shared foundation: is the op-granularity RESOLUTION sound (3 minimal new ops set_color/label/set, NOT a super-op)? Does the rationale (minimal-orthogonal + per-step testable + the 5.3 precedent) hold? Is the shared named-color palette (hero_gold/gray/green/red/orange/gray80) consistent across all 3 SCs?
   - §3 SC1 hero highlight: is the hybrid selection rule (hero_selector + single-C auto-derive + multi-C FAIL LOUDLY) sound? Are the 3 structural cases (hero-alone / hero-in-substrate / hero-at-enzyme-cast) complete? Is the CO2-shed soul-transfer climax (fade + transfer) scientifically honest + narratively clear? Does the MANDATORY anti-confusion note (§3.6) correctly separate the electron-soul from the carbon-fate (NO "carbon becomes ATP")?
   - §4 SC2 scene templates: do the 6 types map all 55 skeleton nodes? Are the templates MolAction sequences (NOT cmd.scene — save/load-safe)? Are the per-type post-conditions machine-checkable?
   - §5 SC3 cast-reveal: is full-then-zoom the right default? Is the mutant red SPHERES + WT green STICKS rep-redundancy colorblind-safe? Is the 5.3 composition sound (5.4 BEFORE mutant red + 5.3 AFTER WT green; 5.4 fills <mutant_color>/<wt_color>)? Is the enzyme-health vs carbon-fate separation clean (at pyr.pdh the carbon IS shed as CO2; the cast-reveal shows the ENZYME restored, NOT the carbon becoming ATP)?

2. **Confirm both headless prototypes passed:** the executor ran `bash tools/run_headless.sh tools/hero_highlight_smoke.py` and `bash tools/run_headless.sh tools/scene_template_smoke.py`, both reporting `SMOKE_RESULT: PASS`. You may re-run them yourself: `bash tools/run_headless.sh tools/hero_highlight_smoke.py` and `bash tools/run_headless.sh tools/scene_template_smoke.py` (from the repo root, in a WSL shell). The outputs land in `/tmp/opencode/hero_highlight_smoke.txt` and `/tmp/opencode/scene_template_smoke.txt`. Confirm the post-conditions (count==1, elem C, rep spheres, color-name round-trip, label C14 for the hero; rep visible + color + label for the scene templates) align with the empirical findings (the smokes prove the MECHANISM, not real science).

3. **Decide the 11 open questions (§7 of the convention):** for each OQ-1 to OQ-11, accept the research recommendation OR specify an override. The most consequential:
   - **OQ-1 [op-granularity + dispatch landing]:** the research recommends (a) the minimal-orthogonal 3-ops approach (adopted in §2.2, NOT the super-op) AND (b) deferring the 3 new-op dispatches to Phase 6 (5.4 prototypes cmd.* directly). The alternative for (b) is the 04-05 precedent (add the `op="set_color"`/`op="label"`/`op="set"` dispatches to `c14/pymol_layer/molops.py` + MockCmd unit tests in `tests/test_molops.py` in 5.4 alongside the smokes). **If you override OQ-1(b) to "add in 5.4"**, a follow-up revision is needed (the 3 `elif` branches + the unit tests) — note this in your response; the executor will record it in the SUMMARY for a follow-up task (NOT executed in this plan).
   - **OQ-2 (mutant color: red spheres vs magenta):** fills 5.3's `<mutant_color>` slot.
   - **OQ-3 (hero color: hero_gold vs cyan):** the hero_gold Okabe-Ito default.
   - **OQ-4 (label wording: "C14" vs "YOU"):** the hero's identity label.
   - **OQ-5 (CO2-shed visual: fade+transfer vs fade-only):** the climax.
   - **OQ-6 (multi-carbon fallback: FAIL LOUDLY vs heuristic):** the no-fabricated-science guard.
   - **OQ-7 (cast-reveal: full-then-zoom vs active-site-first):** the cast-member beat.
   - **OQ-8 (branch-point side-by-side: data-level vs mandate):** the Phase 6 deferral.
   - **OQ-9 (ending tiers: 4 templates vs 1 parametric):** the tier distinctness.
   - **OQ-10 (narrative-only edit-allowed nodes):** substrate-traversal vs active-site-reveal.
   - **OQ-11 (sub-beats approach/release):** folded vs separate.
   The other OQs are design choices documented with recommendations; accept at recs OR override.

4. **Approve or specify adjustments:** if the convention + both prototypes are scientifically accurate + narratively clear + the contracts are sufficient for 6/7/9/12, approve. If any decision needs changing (e.g. override OQ-1 to add dispatches in 5.4, change OQ-2 to magenta, adjust the §3.6 text_teaching wording), describe the adjustment — the executor will revise `05.4-CONVENTION.md` if needed (and, if OQ-1 is overridden, note the follow-up molops.py + test work in the SUMMARY).
  </how-to-verify>
  <resume-signal>Type "approved" to accept the convention as-is (all 11 open questions accepted at their research recommendations, including OQ-1 = 3 minimal new ops NOT a super-op + defer the dispatches to Phase 6). OR describe specific adjustments (e.g. "OQ-1(b): override to add the 3 new-op dispatches in 5.4; OQ-2: magenta; revise §3.6 text_teaching to ..."). The executor will apply the adjustments (revise 05.4-CONVENTION.md if needed) + record the OQ decisions in the SUMMARY before completing the phase.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record the checkpoint outcome + OQ decisions in the SUMMARY + STATE (metadata-only)</name>
  <files>.planning/STATE.md</files>
  <action>
After the human resolves the checkpoint (Task 1), record the outcome. This is a METADATA-ONLY task (no code, no convention changes unless the human requested adjustments in Task 1 — in which case apply those FIRST to `05.4-CONVENTION.md`, then record).

1. **If the human requested convention adjustments** (e.g. override an OQ, revise wording): apply them to `.planning/phases/05.4-cast-hero-representation-design/05.4-CONVENTION.md` first (edit the relevant §2/§3/§5/§7 section). Re-run the relevant smoke(s) if the adjustment changes a mechanism (rare for a design-review checkpoint — most adjustments are spec-wording or OQ-override, not mechanism changes). If OQ-1(b) was overridden to "add the 3 new-op dispatches in 5.4", do NOT execute that here — record it as follow-up work in the SUMMARY (Step 3) for a separate task / the phase verifier / Phase 6.

2. **Update `.planning/STATE.md`:** append a `[Execution 05.4-04]` decision line recording: Phase 5.4 SC4 DELIVERED (human design-review checkpoint APPROVED or APPROVED-WITH-ADJUSTMENTS); the 11 OQ decisions (accept-at-recs OR the specific overrides); the convention state (FROZEN for Phase 6/7/9/12); any follow-up work (e.g. if OQ-1 overridden, the molops.py + test_molops.py work owed). Update the "Current Position" + "Progress" lines to reflect Phase 5.4 COMPLETE (4/4 plans). Mirror the 05.3-02 STATE entry style (the 05.3 completion entry is the template).

3. **Create `.planning/phases/05.4-cast-hero-representation-design/05.4-04-SUMMARY.md`** (the post-checkpoint SUMMARY, mirroring the 05.3-02-SUMMARY.md shape). Frontmatter: phase, plan, status, depends_on, affects (Phase 6/7/9/12), subsystem (representation/cast/hero), tech-stack (unchanged). Body: what was built (the 4-plan phase summary — Plan 01 convention spec, Plan 02 hero smoke, Plan 03 scene+cast smoke, Plan 04 checkpoint); the SC1/SC2/SC3/SC4 delivery; the op-granularity RESOLUTION (3 minimal new ops, NOT a super-op, mirroring 5.3); the 11 OQ decisions table (OQ | decision | rationale); the 5.3 composition (5.4 BEFORE mutant red + 5.3 AFTER WT green); the CO2-shed anti-confusion (soul-transfer = narrative device, NOT carbon→ATP); the dispatch deferral to Phase 6 (or the follow-up note if OQ-1 overridden); the downstream contracts (6/7/9/12); the no-fabricated-science note (zero real PDB IDs, placeholders only); deviations (if any auto-fixes were needed in the smokes, like the 05.3 iterate→iterate_state fix). Mark the convention FROZEN + ready for Phase 6.
  </action>
  <verify>
`test -f .planning/phases/05.4-cast-hero-representation-design/05.4-04-SUMMARY.md` succeeds. `grep -qi "SC4\|design-review\|checkpoint" .planning/phases/05.4-cast-hero-representation-design/05.4-04-SUMMARY.md` matches. `grep -qi "approved\|APPROVED" .planning/phases/05.4-cast-hero-representation-design/05.4-04-SUMMARY.md` matches (the checkpoint outcome recorded). `grep -c "OQ-1\|OQ-2\|OQ-3\|OQ-4\|OQ-5\|OQ-6\|OQ-7\|OQ-8\|OQ-9\|OQ-10\|OQ-11" .planning/phases/05.4-cast-hero-representation-design/05.4-04-SUMMARY.md` shows the 11 OQ decisions are recorded. `grep -qi "Phase 5.4 COMPLETE\|5\.4.*COMPLETE\|4/4" .planning/STATE.md` matches (STATE updated to Phase 5.4 complete). `grep -qi "FROZEN\|frozen" .planning/phases/05.4-cast-hero-representation-design/05.4-04-SUMMARY.md` matches (the convention is frozen for Phase 6). If OQ-1 was overridden, `grep -qi "follow-up\|molops.py\|dispatch" .planning/phases/05.4-cast-hero-representation-design/05.4-04-SUMMARY.md` matches (the follow-up work is noted). Confirm `molops.py` was NOT modified by THIS plan (the dispatch deferral holds unless OQ-1 overridden + a separate follow-up task): `git diff --name-only c14/pymol_layer/molops.py` shows no changes from Plan 04.
  </verify>
  <done>
`05.4-04-SUMMARY.md` exists recording the SC4 checkpoint outcome (approved or approved-with-adjustments) + the 11 OQ decisions + the convention FROZEN state + any follow-up work. `STATE.md` updated to Phase 5.4 COMPLETE (4/4 plans). `molops.py` NOT modified by this plan (the 3 new-op dispatches remain deferred to Phase 6 unless OQ-1 was overridden + a separate follow-up task is noted). Phase 5.4 COMPLETE — all 4 SCs delivered; the convention is ready for Phase 6/7/9/12 to consume WITHOUT re-designing.
  </done>
</task>

</tasks>

<verification>
- The human design-review checkpoint (Task 1) is reached + the human approves or specifies adjustments — SC4 delivered.
- The 11 open questions (OQ-1 to OQ-11) are decided (accept-at-recs OR overrides recorded).
- The CO2-shed anti-confusion is confirmed (soul-transfer = narrative device, NOT carbon→ATP).
- The 5.3 composition is confirmed sound (5.4 BEFORE mutant red + 5.3 AFTER WT green).
- `05.4-04-SUMMARY.md` records the checkpoint outcome + OQ decisions + the FROZEN convention state.
- `STATE.md` updated to Phase 5.4 COMPLETE.
- `molops.py` NOT modified by this plan (dispatch deferred to Phase 6; if OQ-1 overridden, follow-up noted in the SUMMARY, NOT executed here).
- If the human requested convention adjustments, `05.4-CONVENTION.md` is revised accordingly before the SUMMARY is written.
</verification>

<success_criteria>
- SC4 delivered: the convention is human-reviewed for clarity (can a player actually see the hero + the relevant residues at each stage? colorblind-safe? cast-reveal consistent? CO2-shed anti-confusion honored? 5.3 composition sound?) — the human approves or specifies adjustments.
- All 11 open questions are decided.
- The convention is FROZEN + ready for Phase 6/7/9/12.
- Phase 5.4 COMPLETE — all 4 SCs delivered (SC1 hero highlight spec + mechanism, SC2 scene-template library spec + mechanism, SC3 cast-reveal spec + mechanism, SC4 human review).
- No code modified in this plan (metadata-only); `molops.py` untouched (dispatch deferred to Phase 6 unless OQ-1 overridden + noted as follow-up).
</success_criteria>

<output>
After completion, the phase is COMPLETE. The SUMMARY + STATE reflect Phase 5.4 done; the convention is ready for Phase 6 (Qt UI MVP) to consume.
</output>
