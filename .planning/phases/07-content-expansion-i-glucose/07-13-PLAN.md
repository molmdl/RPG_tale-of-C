---
phase: 07-content-expansion-i-glucose
plan: 13
type: execute
wave: 5
depends_on: [06, 12]
files_modified: [data/story_glucose/tca.json, tests/test_glucose_reachability.py]
autonomous: true
must_haves:
  truths:
    - "All 12 TCA nodes have two-layer text grounded only in approved claims"
    - "Shuffle texts use design B re-anchor semantics (0.5/0.5 weights untouched); isotope-surprise beat at the CO2 nodes"
    - "If D5 = promote: tca.akg_dh is edit-allowed AND edit-allowed count test updated 14→15 in the SAME plan"
  artifacts:
    - path: "data/story_glucose/tca.json"
      provides: "TCA texts + claim swaps + PDB loads per batch B outcomes"
      contains: "tca.aconitase_restored"
    - path: "tests/test_glucose_reachability.py"
      provides: "edit-allowed invariant per D5 outcome (14 or 15)"
      contains: "edit_allowed"
  key_links:
    - from: "tca.shuffle claim_ids"
      to: "data/citations.json"
      via: "TCA-RNG-WEIGHT-02-cand (design B) per 03 outcome"
      pattern: "TCA-RNG-WEIGHT-02-cand"
    - from: "tca.aconitase on_enter"
      to: "D6 cast outcome"
      via: "pdb:1ACO replaces pdb:TBD_ACONITASE"
      pattern: "pdb:1ACO"
---

<objective>
TCA content: 12 nodes text + claims per tca §4, shuffle design-B weights per 03, PDB loads per D3/D6/D8, CS dimer per D7, OGDH promotion per D5.
Purpose: the TCA is the game's mechanical heart (RNG shuffle + cycle trap) — text must carry the isotope-surprise teaching beat on approved claims.
Output: authored tca.json + D5-consistent invariants.
</objective>

<context>
@.planning/phases/07-content-expansion-i-glucose/07-03-SUMMARY.md (D1-D10 outcomes; approved claims)
@.planning/phases/07-content-expansion-i-glucose/07-RESEARCH-tca-rng.md (§2 lines 28-65 design B; §4 lines 89-121 per-node claims; §8 lines 204-226 data-vs-code)
Claim swap map: tca.entry/co2_turn1/co2_turn2/akg_dh/divert_to_good PLACEHOLDER_PHASE7_TCA→per §4 table (approved ids only); citrate_synthase keeps CAST-CSC-PDB-01 + TCA-RNG-CITRATE-PROCHIRALITY-01 (+ co-citation per D2); aconitase + DIS-ACO2-01-cand (keep verbatim); shuffle → TCA-RNG-WEIGHT-02-cand (+prochirality) per D1; isocitrate_dh + DIS-IDH3A-01-cand; succinyl_coa_synthetase + DIS-SUCLG1-01-cand; fumarase + DIS-FH-01-cand; malate_dh + DIS-MDH2-01-cand. IDH wording: soften "rate-limiting step" → "a key regulatory step" (16.03 not approved this phase). Cycle-trap text: N=5 is a game-design parameter, not science (D10).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author TCA texts + swaps + loads</name>
  <files>data/story_glucose/tca.json</files>
  <action>
    - Write two-layer text for all 12 nodes per tca §4 + §2 design-B framing: at isocitrate_dh/akg_dh the teaching text reveals "the CO2 leaving are NOT yours — they are the recycled oxaloacetate's"; shuffle texts re-anchored to the succinate-symmetry coin flip ("exit position" vs "retained"); weights stay 0.5/0.5 — do NOT touch weight values, conds, or node ids.
    - Loads per batch B: citrate_synthase keeps 1CSC (D7: teaching note "the functional enzyme is a homodimer" — flag-only, no assembly mechanism); aconitase pdb:TBD_ACONITASE → pdb:1ACO (D6; or 7ACN if recorded); isocitrate_dh 5GRF → 5GRE if D3 approved (else keep 5GRF with mutant disclosure); add 4WLU/2DFD load per D8 only if recorded. tca.aconitase_restored: TEXT ONLY (verify plan-12 on_enter intact, do not redefine).
    - If D5 = promote: add "edit:enzyme:tca.akg_dh" tag + an edit:offer choice (goto edit.prompt, same pattern as siblings) to tca.akg_dh; akg_dh gets the OGDH P189L claim + disease text with the evidence-tier caveat in review wording.
  </action>
  <verify>python3.6 -m unittest tests.test_glucose_reachability -v 2>&1 | tail -5</verify>
  <done>12/12 nodes authored; shuffle semantics re-anchored; loads per outcomes; restored-node on_enter untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Apply D5 outcome to the edit-allowed invariant</name>
  <files>tests/test_glucose_reachability.py</files>
  <action>
    If plan-03 recorded D5 = promote: update test_14_edit_allowed_nodes to the 15-set including tca.akg_dh (rename message accordingly). If D5 = narrative-only: no test change — assert the file still passes as-is. This is the OGDH test-update obligation the index binds to this plan ("same execution").
  </action>
  <verify>python3.6 -m unittest discover -s tests 2>&1 | tail -3</verify>
  <done>edit-allowed invariant matches the D5 outcome; full suite green.</done>
</task>

</tasks>

<verification>
python3.6 -m unittest discover -s tests 2>&1 | tail -3
python3.6 tools/check_citations.py 2>&1 | tail -5
</verification>

<success_criteria>
TCA fully authored on approved claims; design-B shuffle beat present; D5 outcome consistently applied in graph + tests; suite green.
</success_criteria>

<output>.planning/phases/07-content-expansion-i-glucose/07-13-SUMMARY.md</output>
