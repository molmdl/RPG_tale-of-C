---
phase: 07-content-expansion-i-glucose
plan: 05
type: execute
wave: 2
depends_on: [01, 02, 03, 04]
files_modified: [data/citations.json, data/sources.json]
autonomous: true
must_haves:
  truths:
    - "Every claim approved in batches A/B/C is `approved` in data/citations.json with its -cand id unchanged"
    - "Every Phase 7 source has a sources.json record (approved or rejected-with-provenance)"
    - "Citation-gate residual failures are exactly the not-yet-authored placeholder buckets, documented per bucket"
  artifacts:
    - path: "data/citations.json"
      provides: "Approved Phase 7 claims (review_tier/inherits_source_approval/claim_text per hybrid schema)"
      contains: "DIS-PDHA1-01-cand"
    - path: "data/sources.json"
      provides: "Phase 7 source records + rejections retained for provenance"
      contains: "LIBRETEXTS-CATAB-GLYCOLYSIS"
  key_links:
    - from: "data/citations.json"
      to: "data/sources.json"
      via: "claim.source_id references an approved source"
      pattern: "source_id"
    - from: "data/citations.json"
      to: "data/story_glucose/*.json"
      via: "every claim_id referenced by story nodes resolves + is approved (gate)"
      pattern: "approval_status"
---

<objective>
Registry landing: apply ALL recorded batch outcomes (plans 01-04) to data/citations.json + data/sources.json.
Purpose: the gate is the phase's release mechanism — content plans (wave 3+) can only cite claims that exist and are approved here.
Output: updated registry; gate re-run with residuals documented per bucket.
</objective>

<context>
@.planning/phases/07-content-expansion-i-glucose/07-02-SUMMARY.md
@.planning/phases/07-content-expansion-i-glucose/07-03-SUMMARY.md
@.planning/phases/07-content-expansion-i-glucose/07-04-SUMMARY.md
@.planning/phases/07-content-expansion-i-glucose/07-RESEARCH-content-mechanics.md (§8 lines 233-249 hybrid schema; §3 lines 91-122 gate contract)
Sole writer rule: ONLY plan 05 edits data/citations.json + data/sources.json this phase. Policy: KEEP -cand claim_ids when approving — flip approval_status only (M1). Approved entries copy the Phase 5 batch shapes exactly: sources {reference, url, license, pdb_doi?, source_type, approval_status, approved_by: "human", approved_date, notes}; claims {claim, source_type, source, claim_text, source_id, review_tier, inherits_source_approval, review_notes (required for high-stakes), pdb_id?/resolution_angstrom?, approval_status, approved_by: "human", approved_date}. Rejections RETAINED with provenance (LEHNINGER precedent). High-stakes = RNG weights, carbon-fate, contested (individual review_notes); routine = source-inherited fast-track.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Land all batch outcomes in the registry</name>
  <files>data/citations.json, data/sources.json</files>
  <action>
    For each verdict in 07-02/03/04-SUMMARY.md:
    - APPROVED claim: set approval_status="approved", approved_by="human", approved_date=today; keep the -cand id verbatim; fill review_tier (high-stakes claims get review_notes evidence trail), source_id, claim_text per the hybrid schema; add pdb_id/resolution_angstrom for PDB claims (CAST-* precedent).
    - NEW claims: add full entries (e.g. TCA-RNG-WEIGHT-02-cand, TCA-CARBON-FATE-01-cand, ETC-CI-01 family, BAD-* pool claims, cataplerotic TCA-CITRATE-EXPORT-01/TRANSAMINATION-01/CO2-EXIT-01/AMPHIBOLIC-01).
    - REJECTED: sources.json entry approval_status="rejected" + provenance note (e.g. LIBRETEXTS-CATAB-PYRUVATE-DH stub; 5GRF replaced by 5GRE; ETC-II outdated-yield reject if recorded); do NOT delete.
    - Batch-01 outcomes with registry impact: PLACEHOLDER_PHASE8 stays OUT (fa.stub/alc.stub documented stubs); -cand ids kept.
    Do not touch story JSONs, edits.json, cast.json, or tests — later plans own those.
  </action>
  <verify>python3.6 -c "import json;[json.load(open(p)) for p in ('data/citations.json','data/sources.json')];print('json-ok')</verify>
  <done>Every approved claim has status approved with the -cand id intact; every batch source present (approved or rejected+provenance); no other file modified.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run citation gate + document residuals per bucket</name>
  <files>data/citations.json</files>
  <action>Run the gate. Residual MISSING/PENDING entries must each map to a not-yet-authored placeholder bucket (PLACEHOLDER_PHASE7_* in story JSONs still awaiting wave-3/4 content plans, or the documented fa.stub/alc.stub PLACEHOLDER_PHASE8). Write the residual table into the SUMMARY under "Gate residuals (documented)". If a residual is NOT a known placeholder bucket, fix the registry entry — do not widen the residual list.</action>
  <verify>python3.6 tools/check_citations.py; echo "exit=$?"</verify>
  <done>Gate output fully accounted: each residual = a documented placeholder bucket; zero unexplained failures.</done>
</task>

</tasks>

<verification>
python3.6 tools/check_citations.py; echo "exit=$?"
python3.6 -m unittest discover -s tests
</verification>

<success_criteria>
Registry reflects 100% of batch A/B/C outcomes; -cand ids preserved; rejections retained; gate residuals all documented per bucket; full test suite still green (324 baseline).
</success_criteria>

<output>.planning/phases/07-content-expansion-i-glucose/07-05-SUMMARY.md</output>
