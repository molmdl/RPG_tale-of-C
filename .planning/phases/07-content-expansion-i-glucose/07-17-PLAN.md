---
phase: 07-content-expansion-i-glucose
plan: 17
type: execute
wave: 7
depends_on: [16]
files_modified: [data/story_glucose/intro.json, data/story_glucose/glycolysis.json, data/story_glucose/pyruvate_branch.json, data/story_glucose/tca.json, data/story_glucose/etc_atp.json, data/story_glucose/endings.json, data/story_glucose/bad_endings.json]
autonomous: true
must_haves:
  truths:
    - "Every node's on_enter matches its 5.4 scene-template type (6 types + 4 ending tiers)"
    - "5.3 WT-aligned reveal wiring verified on BOTH restored nodes (edit → load WT → align → reveal)"
    - "Gaps found are fixed in the same pass without touching text or claim_ids"
  artifacts:
    - path: "data/story_glucose/"
      provides: "Template-conformant on_enter sequences across all 57 nodes"
      contains: "on_enter"
  key_links:
    - from: "gly.pfk_restored / tca.aconitase_restored on_enter"
      to: "05.3-CONVENTION §3/§6"
      via: "WT-aligned reveal op sequence verified op-by-op"
      pattern: "align"
    - from: "every node on_enter"
      to: "05.4-CONVENTION.md scene templates"
      via: "type-appropriate ops (hide_all, loads, reveals, tier templates)"
      pattern: "op"
---

<objective>
5.4 template-fill verification pass: walk all 57 nodes, verify every on_enter matches its scene-template type, verify the 5.3 reveal wiring on both restored nodes, and fix gaps.
Purpose: content is now authored — this pass guarantees the VISUAL layer follows the frozen conventions so human-verify (plan 18) sees correct scenes.
Output: conformance-fixed story JSONs + verification notes.
</objective>

<context>
@.planning/phases/05.4-story-rendering-and-help-layer-glucose-phase/05.4-CONVENTION.md (scene templates: 6 types + 4 ending tiers, §3.6 anti-confusion, §6.2 fields)
@.planning/phases/05.3-pymol-action-layer-glucose-phase/05.3-CONVENTION.md (§7.3 per-enzyme 7-field metadata; align reveal)
Verification loop per node: read node tags + on_enter → identify scene-template type → check ops conform (hide_all first; loads present where the plan promised them; reveal/label ops per template; ending tiers per Type-6). Restored nodes: assert op sequence edit → load WT → align(target wt, reference enzyme) → reveal, and that Continue/Observe choices re-enter the main path. Anti-confusion: any highlight/soul-transfer wording checks stay in TEXT (already authored) — this pass only fixes STRUCTURE. Fix gaps by editing ONLY on_enter/ops/args — never text fields, claim_ids, choices, or node ids (plans 06-13 own those; suite must stay green).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify + fix template conformance across all 57 nodes</name>
  <files>data/story_glucose/intro.json, data/story_glucose/glycolysis.json, data/story_glucose/pyruvate_branch.json, data/story_glucose/tca.json, data/story_glucose/etc_atp.json, data/story_glucose/endings.json, data/story_glucose/bad_endings.json</files>
  <action>
    - Walk every node; classify by tags (stage:*, is_ending tier, edit:offer, rng:weighted, edit:prompt) → expected 5.4 type; verify on_enter conforms; fix deviations in place (add/remove/reorder ops only).
    - Restored nodes: full 5.3 reveal-wiring verification per the op sequence above; align_sele values must match 07-12-SUMMARY.
    - Keep hero-highlight + _smoke.pdb start ops byte-identical (start-shape tests).
    - Record per-node verdicts (conform / fixed) in the SUMMARY.
  </action>
  <verify>python3.6 -m unittest discover -s tests 2>&1 | tail -3</verify>
  <done>All nodes conform; any fixes kept op-level; suite green after edits.</done>
</task>

</tasks>

<verification>
python3.6 -m unittest discover -s tests 2>&1 | tail -3
python3.6 tools/check_citations.py; echo "exit=$?"
</verification>

<success_criteria>
57/57 nodes template-conformant; both restored nodes carry verified 5.3 reveal wiring; zero text/claim drift introduced.
</success_criteria>

<output>.planning/phases/07-content-expansion-i-glucose/07-17-SUMMARY.md</output>
