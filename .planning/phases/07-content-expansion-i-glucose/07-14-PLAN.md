---
phase: 07-content-expansion-i-glucose
plan: 14
type: execute
wave: 4
depends_on: [12]
files_modified: [rpg/data/edits.json]
autonomous: true
must_haves:
  truths:
    - "All 12 mutable enzyme buckets exist with ≥1 known entry; NO known-wrong entries (M5 outcome)"
    - "PFKM + ACO2 entries branch to gly.pfk_restored / tca.aconitase_restored (the plan-12 ids)"
    - "tca.citrate_synthase (edit:structural, no disease claim) deliberately has NO entry; cast-side subset handled by plan 15"
    - "No fixture entries remain; every signature uses the canonical lowercase target + new_res arg form"
  artifacts:
    - path: "rpg/data/edits.json"
      provides: "12 enzyme buckets with reverse-mutation signatures + branch routing"
      contains: "gly.pfk_restored"
  key_links:
    - from: "edits.json branch_node"
      to: "story graph"
      via: "every branch_node exists (validate_edits_table clean vs real graph)"
      pattern: "branch_node"
    - from: "edits.json enzymes keys"
      to: "graph edit:enzyme:<id> tags"
      via: "shared-manifest subset (12 buckets; tca.citrate_synthase excluded by design — edit:structural, no disease claim)"
      pattern: "edit:enzyme"
---

<objective>
edits.json authoring per the mechanics §4 schema: 12 enzyme entries (13 tagged enzymes minus tca.citrate_synthase, which is edit:structural with no disease claim and gets NO entry by design), reverse-mutation signatures from the research disease tables, branch routing to the restored nodes.
Purpose: filling edits.json IS the restoration UX — the EditDialog's "correct fix" options are built verbatim from these signatures.
Output: complete edits.json (sole writer this phase).
</objective>

<context>
@.planning/phases/07-content-expansion-i-glucose/07-12-SUMMARY.md (aconitase residue mapping + align_sele)
@.planning/phases/07-content-expansion-i-glucose/07-02-SUMMARY.md / 07-04-SUMMARY.md (CYC1 allele choice)
@.planning/phases/07-content-expansion-i-glucose/07-RESEARCH-content-mechanics.md (§4 lines 123-182 schema + authoring rules)
Scope: the graph carries 13 edit:enzyme:* buckets, but tca.citrate_synthase is tagged edit:structural with NO implementable disease claim (tca §6.1: 0 natural variants) — it gets NO edits.json entry and plan 15 excludes it from cast.json (roster invariant is cast-ids ⊆ edits-keys, a subset). Author the other 12 buckets. Schema rules: op ∈ point_mutation|substrate_edit|protonation_change; target ALREADY LOWERCASE; point_mutation args key = new_res; duplicate signatures forbidden; claim_id = the DIS-* claim (NOT gate-checked). Signatures: gly.pfk resi 209→GLY (PFKM); gly.pyruvate_kinase resi 479→ARG (PKLR); pyr.pdh resi 138 and chain a→VAL (PDHA1, PDB/mature numbering); tca.aconitase per plan-12 mapping (ACO2 S112R → reverse R112S); tca.isocitrate_dh M204I→I (or R316C per 03); tca.succinyl_coa_synthetase resi 85→GLY (SUCLG1 G85A reverse A85G); tca.fumarase resi 233→ARG (FH R233H reverse H233R); tca.malate_dh resi 37→GLY (MDH2 G37R reverse R37G); etc.complex_i NDUFS8 R102H→H (PDB chain i resi 68, PDB-numbering); etc.complex_ii SDHA R554W→W (PDB resi 512); etc.complex_iii CYC1 per 04 choice (L215F→F PDB resi 131, or W96C→C resi 12); etc.complex_iv COX4I1 "resi 130 and chain d"→PRO (P152T, PDB-numbering). bad_ending_pool: UNTOUCHED (2 entries stay). NO known-wrong 1b entries (M5). Remove fixture_enzyme_1 (its test-fixture twin lives in tests/fixtures/edit_routing/).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 12 buckets + remove fixture</name>
  <files>rpg/data/edits.json</files>
  <action>
    - Replace the file content: keep version 1 + bad_ending_pool exactly as-is; enzymes = the 12 real mutable buckets (all EXCEPT tca.citrate_synthase).
    - gly.pfk entry: branch_node "gly.pfk_restored", claim_id DIS-PFKM-01-cand. tca.aconitase entry: branch_node "tca.aconitase_restored", claim_id DIS-ACO2-01-cand, target sele EXACTLY as documented in 07-12-SUMMARY (do not re-derive).
    - Exploration enzymes (pyr.pdh, isocitrate_dh, succinyl_coa_synthetase, fumarase, malate_dh, complex_i/ii/iii/iv): known CORRECT entries only; branch_node = the enzyme node's own main-path successor (existing node; re-entering fires its on_enter — acceptable per mechanics §4.4 routing), recorded per-bucket in SUMMARY.
    - Every signature: lowercase target, new_res arg, reverse-mutation value per the table above; claim_id = matching DIS-* id verbatim (-cand kept).
    - Delete fixture_enzyme_1. NO new ops, NO engine references, NO known-wrong entries, NO tca.citrate_synthase key.
  </action>
  <verify>python3.6 -m unittest discover -s tests 2>&1 | tail -3</verify>
  <done>12 buckets present; CS absent by design; fixture gone; every signature canonical; bad pool untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Validate against the real graph</name>
  <files>rpg/data/edits.json</files>
  <action>Run the edit-coverage tool (it will flag cast.json gaps — expected until plan 15; record as documented-pending). Separately verify structure: parse JSON; assert 12 keys (CS absent); assert branch_node values exist in the loaded StoryGraph (a throwaway python3.6 -c check using rpg.story.model — durable test coverage stays in plan 16).</action>
  <verify>python3.6 tools/check_edit_coverage.py; echo "exit=$?"</verify>
  <done>JSON valid; 12 buckets; branch nodes all resolve in the graph; cast-side failures documented as pending-plan-15.</done>
</task>

</tasks>

<verification>
python3.6 -m unittest discover -s tests 2>&1 | tail -3
python3.6 tools/check_edit_coverage.py 2>&1 | tail -10
</verification>

<success_criteria>
12 real mutable-enzyme buckets with canonical signatures; restored-node routing for PFK/aconitase; no fixture/known-wrong/CS entries; cast-side coverage documented as plan-15 pending.
</success_criteria>

<output>.planning/phases/07-content-expansion-i-glucose/07-14-SUMMARY.md</output>
