---
phase: 07-content-expansion-i-glucose
plan: 06
type: execute
wave: 4
depends_on: [05, 12]
files_modified: [data/story_glucose/pyruvate_branch.json, tests/test_glucose_reachability.py]
autonomous: true
must_haves:
  truths:
    - "All 7 pyruvate-branch nodes have non-empty two-layer text grounded only in approved claims"
    - "pyr.branch choices are cond-neutralized with honest O2-framing labels; both paths always eligible"
    - "anaer.ethanolic uses the yeast-counterfactual framing (mammalian host does not run this road)"
    - "TestPyrBranchRuntimeEligibility matches the new always-eligible semantics in the SAME plan"
  artifacts:
    - path: "data/story_glucose/pyruvate_branch.json"
      provides: "Two-layer text + approved claim_ids + cond-neutralized branch + 6CFO/6CER on_enter per outcomes"
      contains: "PYR-PDH-01"
    - path: "tests/test_glucose_reachability.py"
      provides: "Updated runtime-eligibility tests for cond-neutralized pyr.branch"
      contains: "TestPyrBranchRuntimeEligibility"
  key_links:
    - from: "pyr.branch choices"
      to: "pyr.pdh / anaer.entry"
      via: "both choices eligible (no cond) with O2-framing labels"
      pattern: "host_o2_low"
    - from: "pyr.pdh claim_ids"
      to: "data/citations.json"
      via: "approved claims only (keep -cand ids verbatim)"
      pattern: "DIS-PDHA1-01-cand"
---

<objective>
Pyruvate-branch content: two-layer text for all 7 nodes, claim swaps, PDH cast on_enter per batch-A outcome, anaerobic treatment per plan-01 outcome (cond-neutralize + O2 labels), OQ1 ethanolic per plan-02 outcome (yeast counterfactual).
Purpose: the O2 branch is the story's first real fork; it must be honest and always playable.
Output: authored pyruvate_branch.json + updated runtime-eligibility tests.
</objective>

<context>
@.planning/phases/07-content-expansion-i-glucose/07-01-SUMMARY.md (host_o2_low + OQ-K outcomes)
@.planning/phases/07-content-expansion-i-glucose/07-02-SUMMARY.md (DC-4/DC-5, OQ1, OQ-E outcomes; approved claims)
@.planning/phases/07-content-expansion-i-glucose/07-RESEARCH-glycolysis-pyruvate.md (§7 lines 186-217; §8 lines 218-240; §3 lines 74-105)
Claim swap map: pyr.branch PLACEHOLDER_PHASE7_ANAEROBIC→AN-G-01+AN-G-05; pyr.pdh PLACEHOLDER_PHASE7→PYR-PDH-01 (keep DIS-PDHA1-01-cand + CAST-PDH-WT-PDB-01-cand ids verbatim — pinned tests assert them); anaer.entry→AN-G-04; anaer.ldh→AN-G-02(+CAST-LDH-PDB-01); anaer.lactic→AN-G-02; anaer.ethanolic→AN-G-03+AN-G-05 (DELETE the HOST-TENSION FLAG line once counterfactual framing lands); anaer.crisis→AN-G-04+AN-ETC-02. NO topology change (graph is 57 nodes / 21 endings after plan 12; this plan adds zero nodes and must keep pyruvate_branch.json at exactly 7 nodes).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author two-layer text + claim swaps + branch treatment</name>
  <files>data/story_glucose/pyruvate_branch.json</files>
  <action>
    - Write text_dramatic + text_teaching for all 7 nodes per gly §8 authoring bullets (PDH: hero-neutral carbon wording — never assert the hero is the PDH-CO2 carbon; note TPP/Mg2+ visible in 6CFO; V138M breaks the Mg2+ cradle).
    - Apply the claim swap map above; keep every existing -cand id verbatim.
    - Per 01 outcome: REMOVE both cond keys on pyr.branch's choices; reword labels to honest O2 framing (e.g. "Proceed aerobically — the tissue is oxygen-rich" / "The air runs out — ferment (anaerobic)"); teaching text explains O2 as a real tissue condition. Keep dict-method cond form ONLY if any cond remains (none should).
    - Per 02 outcome: pyr.pdh on_enter — if DC-5 chose (a): keep pdb:6CFO load as-is; if (b): add pdb:6CER mutant load/reveal per 05.3. anaer.ldh keeps 5W8J + names the bound inhibitor (OQ-E).
    - Do NOT add/remove nodes or choices (topology frozen here).
  </action>
  <verify>python3.6 -m unittest tests.test_glucose_reachability -v 2>&1 | tail -5</verify>
  <done>Graph totals 57 nodes / 21 endings (post-plan-12) with pyruvate_branch.json still exactly 7 nodes / zero added; pyr.pdh loads 6CFO (never 2OZL); claim ids resolve to approved registry entries.</done>
</task>

<task type="auto">
  <name>Task 2: Update TestPyrBranchRuntimeEligibility to always-eligible semantics</name>
  <files>tests/test_glucose_reachability.py</files>
  <action>
    Rewrite the 5 runtime-eligibility tests: with conds removed, BOTH pyr.branch choices are eligible regardless of flags (aerobic-eligible-when-unset / anaerobic-hidden-when-unset / flip-when-set become both-eligible assertions); keep test_no_broken_dict_attribute_conds_remain green (it passes trivially once conds are gone). Do not touch other test classes. This is the OQ-K obligation recorded by plan 01 — same-plan execution is mandatory.
  </action>
  <verify>python3.6 -m unittest tests.test_glucose_reachability -v 2>&1 | tail -5</verify>
  <done>All runtime-eligibility tests green under the new semantics; full reachability class green.</done>
</task>

</tasks>

<verification>
python3.6 -m unittest discover -s tests 2>&1 | tail -3
python3.6 tools/check_citations.py 2>&1 | tail -5
</verification>

<success_criteria>
7/7 nodes two-layer authored with approved claims only; pyr.branch cond-free with honest labels; ethanolic counterfactual framing landed; runtime-eligibility tests updated and green; suite green.
</success_criteria>

<output>.planning/phases/07-content-expansion-i-glucose/07-06-SUMMARY.md</output>
