---
phase: 07-content-expansion-i-glucose
plan: 11
type: execute
wave: 3
depends_on: [05]
files_modified: [data/story_glucose/bad_endings.json]
autonomous: true
must_haves:
  truths:
    - "All 14 bad endings + edit.prompt have text per the etc §6 mapping; bad.proton_leak describes PROTONS, never electrons"
    - "No PLACEHOLDER_PHASE7* ids remain in bad_endings.json (mechanic-narrative nodes carry [] or registered generic claims)"
    - "cycle_trap death sentence grounded in BAD-HOST-01; denature_ph text stays claim-light unless BAD-PH-01 approved"
  artifacts:
    - path: "data/story_glucose/bad_endings.json"
      provides: "Bad-pool texts + claims per mapping; edit.prompt stub cleared"
      contains: "bad.proton_leak"
  key_links:
    - from: "bad.wrong_substrate_trapped"
      to: "PDB 1ZP0"
      via: "1ZP0 anchor per batch-C outcome (optional on_enter load)"
      pattern: "pdb:1ZP0"
    - from: "bad_endings claim_ids"
      to: "data/citations.json"
      via: "approved BAD-* claims only"
      pattern: "BAD-HOST-01"
---

<objective>
Bad-ending pool content: 15 nodes text + claims per the etc §6 mapping.
Purpose: bad endings teach real failure modes (inhibitors, misfolding, uncoupling) instead of generic loss text.
Output: authored bad_endings.json.
</objective>

<context>
@.planning/phases/07-content-expansion-i-glucose/07-04-SUMMARY.md (bad-pool approvals; 1ZP0; no-ROS-v1)
@.planning/phases/07-content-expansion-i-glucose/07-RESEARCH-etc-endings.md (§6 lines 156-183 per-node mapping; §7 lines 184-226)
Mapping highlights: bad.lost_connection/released_from_host = mechanic-narrative → Pitfall-10 explanatory template, claim_ids→[] (no science claimed); bad.cycle_trap_host_death → BAD-HOST-01 death sentence (energy-harvest failure is lethal; loop mechanics stay TCA-framed, no futile-cycling claim); wrong_substrate_trapped → BAD-INHIB-01 + optional pdb:1ZP0 load per 04; proton_leak → ETC-UCP-01 + FIX the dramatic text (currently "your electrons leak to nowhere" — rewrite to proton/gradient bleed; no-ROS-v1); denature_ph → BAD-PH-01 only if approved in 05, else claim-light text framing it as a pathology/experimental condition (matrix ~pH 7.8; protonation tie-in); misfold/aggregate texts must DIFFERENTIATE (collapse = enzyme-level loss of folded state; aggregate = proteostasis/clearance). edit.prompt → claim_ids [] (structural stub; runtime bypasses via EditRouter).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author all bad-pool texts + claim swaps</name>
  <files>data/story_glucose/bad_endings.json</files>
  <action>
    - Write two-layer text for the 14 bad endings per the mapping above; keep each node's tier/tag/on_enter/choices EXACTLY as-is (frozen pool; the parametrized convention test auto-covers).
    - Swap PLACEHOLDER_PHASE7_BAD: registered claims where approved; [] for the two mechanic-narrative endings; edit.prompt PLACEHOLDER_PHASE7_EDIT→[] (stub carries no science).
    - bad.proton_leak dramatic text: gradient bleeds away, proton motive force dissipates as heat, ATP synthesis falls — NO electron-leak/ROS wording.
    - bad.denature_ph: extreme-pH framing as pathology/experimental condition; salt-bridge disruption wording per approved claim (or generic if pending).
    - Optional per 04: add {"op":"load","target":"pdb:1ZP0","args":{"object":"sdh_inhibited"}} to wrong_substrate_trapped on_enter if the 1ZP0 anchor was approved; otherwise [hide_all] stays.
  </action>
  <verify>python3.6 -m unittest tests.test_glucose_reachability -v 2>&1 | tail -5</verify>
  <done>15/15 nodes authored; 0 PLACEHOLDER_PHASE7* in the file; proton_leak text proton-correct; convention test green.</done>
</task>

</tasks>

<verification>
python3.6 -m unittest discover -s tests 2>&1 | tail -3
python3.6 tools/check_citations.py 2>&1 | tail -5
grep -c "PLACEHOLDER_PHASE7" data/story_glucose/bad_endings.json
</verification>

<success_criteria>
Every bad node's text matches its §6 failure mode; claims approved-only or []; electron-leak wording gone; suite green.
</success_criteria>

<output>.planning/phases/07-content-expansion-i-glucose/07-11-SUMMARY.md</output>
