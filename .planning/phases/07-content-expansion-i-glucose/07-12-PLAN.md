---
phase: 07-content-expansion-i-glucose
plan: 12
type: execute
wave: 3
depends_on: [01, 05]
files_modified: [data/story_glucose/glycolysis.json, data/story_glucose/tca.json, tests/test_glucose_reachability.py, .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md, .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-GRAPH-DIAGRAM.md]
autonomous: true
must_haves:
  truths:
    - "gly.pfk_restored + tca.aconitase_restored exist with the 05.3 on_enter shape and 2 choices back to the main path"
    - "Counts are 57 nodes / 21 endings; restoration nodes are NOT edit-allowed; all reachability tests updated + green"
    - "Aconitase human↔bovine residue mapping is established before its align/edit sele is written (documented in SUMMARY)"
  artifacts:
    - path: "data/story_glucose/glycolysis.json"
      provides: "gly.pfk_restored node (structure only — text lands in plan 08)"
      contains: "gly.pfk_restored"
    - path: "data/story_glucose/tca.json"
      provides: "tca.aconitase_restored node (structure only — text lands in plan 13)"
      contains: "tca.aconitase_restored"
    - path: "tests/test_glucose_reachability.py"
      provides: "Count invariants 57/21; 14 edit-allowed unchanged"
      contains: "57"
  key_links:
    - from: "gly.pfk_restored / tca.aconitase_restored"
      to: "rpg/data/edits.json (plan 14)"
      via: "these ids are the fixed branch_node targets"
      pattern: "gly.pfk_restored|tca.aconitase_restored"
    - from: "restored-node on_enter"
      to: "05.3-CONVENTION"
      via: "edit → load WT → align → reveal op sequence"
      pattern: "\"op\": \"align\""
---

<objective>
Restoration topology per plan-01 outcome (ADD): add gly.pfk_restored + tca.aconitase_restored, update counts/tests, sync 05.1 DESIGN + regenerate the graph diagram.
Purpose: creates the two branch targets every restoration edit routes to; the only sanctioned topology change of the phase (55→57).
Output: structural nodes + green updated invariants + refreshed design docs.
</objective>

<context>
@.planning/phases/07-content-expansion-i-glucose/07-01-SUMMARY.md (DC-A outcome)
@.planning/phases/07-content-expansion-i-glucose/07-RESEARCH-content-mechanics.md (§4 lines 123-182 — schema + node shape + test impact)
@.planning/phases/07-content-expansion-i-glucose/07-RESEARCH-tca-rng.md (§6.7 lines 173-178 aconitase mapping caveat)
Node shape (mechanics §4.4 + 05.3): restored node on_enter = [MolAction(op="edit", target=<enzyme obj>, args={"edit_type":"point_mutation","sele":"resi <N> and chain <c>","new_resn":"<WT>"}), MolAction(op="load","pdb:<WT>",{"object":"<enzyme>_wt"}), MolAction(op="align",target:"<enzyme>_wt",args={"reference":"<enzyme>","method":"super","align_sele":"<per-enzyme sele>"}), ...reveal ops] + choices Continue/Observe back to the main path. PFK: pre-edit applyEdit on 4PFK is OMITTED per DC-3 text-only fallback (OQ-B: never invent a bacterial residue mapping); PDH keeps its own arc (not a restored node). Aconitase: human S112R ↔ bovine 1ACO residue number MUST come from a real sequence alignment + headless cmd.iterate probe (run-conda-pymol.bat pattern) BEFORE writing the sele — record the mapping + align_sele in SUMMARY for plan 14 to reuse.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add both restored nodes + wire main-path re-entry</name>
  <files>data/story_glucose/glycolysis.json, data/story_glucose/tca.json</files>
  <action>
    - glycolysis.json: add node "gly.pfk_restored" (tags stage:glycolysis; NOT edit-allowed; on_enter per the shape above — PFK edit op uses the edits.json signature target per plan 14's table, resi 209→GLY framing as recorded; text fields left "TBD — Phase 7 text (plan 08)"; choices → back to gly.fbp_to_pyruvate).
    - tca.json: add node "tca.aconitase_restored" (same shape; aconitase edit op sele from the alignment mapping; reference the 1ACO WT cast per D6; text "TBD — plan 13"; choices → back to tca.shuffle).
    - Source nodes stay as-is except: gly.pfk/tca.aconitase keep their edit:offer choices (runtime routes known fixes to the restored nodes via plan 14's branch_node).
    - Do NOT touch texts of existing nodes (plans 08/13 own them) and do NOT touch other files.
  </action>
  <verify>python3.6 -c "import json;[json.load(open('data/story_glucose/'+f)) for f in ('glycolysis.json','tca.json')];print('json-ok')</verify>
  <done>Both nodes exist with the 05.3 shape; existing nodes' text/choices unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update count invariants + DESIGN + diagram</name>
  <files>tests/test_glucose_reachability.py, .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-DESIGN.md, .planning/phases/05.1-story-graph-design-glucose-skeleton-integration-contracts/05.1-GRAPH-DIAGRAM.md</files>
  <action>
    - tests: 55→57 in test_manifest_loads_all_55_nodes (rename/update message), topology test (57 nodes/21 endings), reachability 4-tier (restoration nodes reachable, non-ending); test_14_edit_allowed_nodes unchanged (restored nodes are not edit-allowed); no test adds claim assertions here.
    - DESIGN §5/§6: fill the "branch node (Phase 7)" rows with the two fixed ids + on_enter shape + aconitase mapping note.
    - Regenerate the diagram: python3.6 tools/render_story_graph.py (then read the diff header to confirm 57 nodes).
  </action>
  <verify>python3.6 -m unittest discover -s tests 2>&1 | tail -3</verify>
  <done>Suite green with 57/21 invariants; DESIGN rows filled; diagram regenerated.</done>
</task>

</tasks>

<verification>
python3.6 -m unittest discover -s tests 2>&1 | tail -3
python3.6 -c "import json;import itertools;d=[json.load(open('data/story_glucose/'+f)) for f in ('intro.json','glycolysis.json','pyruvate_branch.json','tca.json','etc_atp.json','endings.json','bad_endings.json')];print('nodes',sum(len(x['nodes']) for x in d))"
</verification>

<success_criteria>
57 nodes / 21 endings; both restored nodes structurally complete; invariants green; design docs + diagram in sync; aconitase mapping documented.
</success_criteria>

<output>.planning/phases/07-content-expansion-i-glucose/07-12-SUMMARY.md</output>
