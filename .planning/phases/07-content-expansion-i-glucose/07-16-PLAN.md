---
phase: 07-content-expansion-i-glucose
plan: 16
type: execute
wave: 6
depends_on: [06, 07, 08, 09, 10, 11, 13]
files_modified: [tests/test_glucose_content.py, tests/test_glucose_reachability.py]
autonomous: true
must_haves:
  truths:
    - "Every one of the 57 nodes has non-empty text_dramatic AND text_teaching (two-layer invariant)"
    - "Every story claim_id resolves to an APPROVED registry entry; 0 PLACEHOLDER_PHASE7* remain (fa.stub/alc.stub PLACEHOLDER_PHASE8 excepted)"
    - "Citation gate exits 0; seeded determinism test pins a documented shuffle fate under design B"
  artifacts:
    - path: "tests/test_glucose_content.py"
      provides: "Two-layer, claim-hygiene, gate, coverage, round-trip, determinism tests"
      contains: "def test_"
    - path: "tests/test_glucose_reachability.py"
      provides: "Final count invariants 57/21/4-tier + edit-allowed per D5"
      contains: "test_reachability_green_all_four_tiers"
  key_links:
    - from: "tests/test_glucose_content.py"
      to: "data/citations.json + data/story_glucose/*"
      via: "subprocess gate run + registry cross-check on real data"
      pattern: "check_citations"
    - from: "seeded determinism test"
      to: "tca.shuffle weighted choices"
      via: "fixed seed → documented fate (design B final)"
      pattern: "seed"
---

<objective>
Cross-cutting test pass: author the new content-invariant suite and tighten the reachability invariants so the phase's release conditions are mechanically enforced.
Purpose: turns "no fabricated science + no placeholder residue" from a review promise into CI.
Output: tests/test_glucose_content.py (new) + final reachability invariant updates; all green.
</objective>

<context>
@.planning/phases/07-content-expansion-i-glucose/07-RESEARCH-content-mechanics.md (§10 lines 285-305 test strategy table)
Test homes: new invariants → tests/test_glucose_content.py (pattern source: test_glucose_reachability setUp/StoryGraph.load + test_citations.py TestCheckCitationsExitCodes + test_edit_router.py:175-192). Required tests: (1) two-layer non-empty on all 57 non-stub glucose nodes (fa.stub/alc.stub exempted as documented stubs); (2) no "TBD" residue in story text fields; (3) no PLACEHOLDER_PHASE7* claim_ids anywhere (PLACEHOLDER_PHASE8 on fa.stub/alc.stub allowed); (4) citation gate exit 0 via subprocess on the real bundle; (5) edit coverage exit 0 + scan_edit_coverage green + validate_edits_table(edits, real graph) clean; (6) known-edit round-trip: every edits.json signature rebuilt as EditIntent routes to an EXISTING branch node (mock sink); (7) seeded determinism: fixed seed → documented shuffle fate per design B + different-seed variation; (8) restoration-arc integration: known edit at gly.pfk routes to gly.pfk_restored whose on_enter carries edit+align ops (mock sink op sequence). Reachability file: final invariants 57 nodes / 21 endings / 4 tiers / edit-allowed per D5 outcome — verify current state, adjust ONLY if a prior plan left a count stale.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author tests/test_glucose_content.py</name>
  <files>tests/test_glucose_content.py</files>
  <action>
    Implement the 8 test groups listed in context, mirroring existing patterns (StoryGraph.load setUp; subprocess for the gate with the three-way exit contract; EditIntent/EditRouter imports for round-trip + restoration-arc with mock sinks; RngEngine-seeded determinism with the documented fate from 07-13-SUMMARY). No engine changes; tests only. python3.6 compatible (no f-strings if the suite avoids them — match suite style).
  </action>
  <verify>python3.6 -m unittest tests.test_glucose_content -v 2>&1 | tail -5</verify>
  <done>All new tests pass against the real data; any failure indicates a real content gap → fix data per owning plan's scope or report.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize reachability invariants</name>
  <files>tests/test_glucose_reachability.py</files>
  <action>
    Confirm/assert: 57 nodes, 21 endings, 4 tiers green, edit-allowed count per D5 outcome, pinned -cand assertions intact (complex_i exact list, pyr.pdh contains). Add nothing speculative — this task is convergence of existing invariants with the 57-node reality.
  </action>
  <verify>python3.6 -m unittest discover -s tests 2>&1 | tail -3</verify>
  <done>Full suite green (324 baseline + restored-node updates + new content tests).</done>
</task>

</tasks>

<verification>
python3.6 -m unittest discover -s tests 2>&1 | tail -3
python3.6 tools/check_citations.py; echo "exit=$?"
python3.6 tools/check_edit_coverage.py; echo "exit=$?"
</verification>

<success_criteria>
All 8 new test groups green; 57/21/4-tier invariants green; gate + coverage exit 0; zero undocumented PLACEHOLDER residue.
</success_criteria>

<output>.planning/phases/07-content-expansion-i-glucose/07-16-SUMMARY.md</output>
