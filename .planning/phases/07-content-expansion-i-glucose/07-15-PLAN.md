---
phase: 07-content-expansion-i-glucose
plan: 15
type: execute
wave: 5
depends_on: [05, 14]
files_modified: [rpg/data/cast.json]
autonomous: true
must_haves:
  truths:
    - "cast.json lists the real story enzymes (12 mutable buckets; fixture + PLACEHOLDER entries gone)"
    - "Every cast id has ≥1 edits.json entry — check_edit_coverage exits 0"
    - "pdb:TBD_ACONITASE replacement (1ACO per D6) is reflected in cast + bulk-download list"
  artifacts:
    - path: "rpg/data/cast.json"
      provides: "Real enzyme entries per 05.4 conventions (id/label/source/pdb_id/character/claim_id)"
      contains: "1ACO"
  key_links:
    - from: "cast.json ids"
      to: "rpg/data/edits.json keys"
      via: "cast-ids ⊆ edits-keys (subset invariant, tool-checked)"
      pattern: "enzymes"
    - from: "cast.json pdb_id set"
      to: "rpg/data/assets/downloaded/"
      via: "bulk-download list pre-populates the gitignored cache"
      pattern: "download"
---

<objective>
Cast + assets: replace the fixture/placeholder cast entries with the real story enzymes, fix the aconitase cast per D6, and verify the bulk-download list for offline pre-population.
Purpose: missing_large_pdbs pre-fetches every download cast entry before play; wrong ids here break first-run on fresh installs.
Output: final cast.json + verified download list; coverage invariant green.
</objective>

<context>
@.planning/phases/07-content-expansion-i-glucose/07-14-SUMMARY.md (12 edits.json keys)
@.planning/phases/07-content-expansion-i-glucose/07-03-SUMMARY.md (D6/D8 casts) + 07-02-SUMMARY.md (DC-4/DC-5 PDBs) + 07-04-SUMMARY.md (ETC PDBs)
@.planning/phases/07-content-expansion-i-glucose/07-RESEARCH-content-mechanics.md (§5 lines 183-205 pipeline; §4.5 coverage invariants)
Conventions (mechanics §5.2/§5.4): entry shape {id, label, source: "download"|"bundled", pdb_id, character, claim_id}; every load-bearing PDB needs its approved CAST-* claim (plan 05 landed them). Cast set per recorded outcomes: 4PFK (pfk), 7FS3 (+2VGG if DC-4 adopted), 6CFO (+6CER if DC-5 b), 5W8J (ldh), 1CSC (cs), 1ACO (aconitase — replaces pdb:TBD_ACONITASE per D6), 5GRE-or-5GRF (idh per D3), 4WLU-or-2DFD (mdh per D8), 1ZOY (complex_ii), 1BGY (complex_iii), 1OCC (complex_iv), 1E79-or-1BMF (atp_synthase per batch C), 1ZP0 only if the bad-ending anchor was approved. EXCLUDE tca.citrate_synthase? NO — CS IS a cast (1CSC teaches structure) but has NO edits entry; roster invariant is cast-ids ⊆ edits-keys... CS in cast would VIOLATE subset. Resolution: cast.json lists the 12 enzymes that have edits entries; CS's 1CSC load stays wired in tca.json via its own pdb:1CSC on_enter (loads do not require cast membership) and CS's teaching cast is documented in the SUMMARY.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill cast.json with real entries</name>
  <files>rpg/data/cast.json</files>
  <action>
    - Remove fixture_enzyme_1 + PLACEHOLDER_large_enzyme entirely (M3 disposal per mechanics rec).
    - Add one entry per edits.json key (12 enzymes): id = the bucket id (gly.pfk, gly.pyruvate_kinase, pyr.pdh, tca.aconitase, tca.isocitrate_dh, tca.succinyl_coa_synthetase, tca.fumarase, tca.malate_dh, etc.complex_i..iv), label, source "download", pdb_id per the recorded outcomes, character "glucose", claim_id = the matching CAST-* claim id from the registry.
    - species note stays honest: bovine/porcine structures + human disease genetics (etc OQ#5 wording) goes in labels' teaching side only if space allows — data fields stay machine-clean.
    - Do NOT touch edits.json, story JSONs, or engine files.
  </action>
  <verify>python3.6 -c "import json;c=json.load(open('rpg/data/cast.json'));e=json.load(open('rpg/data/edits.json'));ids={x['id'] for x in c['enzymes']};assert ids<=set(e['enzymes']),ids-set(e['enzymes']);print('subset-ok',len(ids))"</verify>
  <done>12 real entries; subset invariant green; no fixture/placeholder remnants.</done>
</task>

<task type="auto">
  <name>Task 2: Verify bulk-download list + coverage</name>
  <files>rpg/data/cast.json</files>
  <action>
    - Run the edit-coverage tool — must exit 0 now (cast ids all covered).
    - Cross-check the download list: every cast source="download" pdb_id is a real 4-char RCSB id (no PLACEHOLDER_PDB) so missing_large_pdbs will fetch them; list any already-present in rpg/data/assets/downloaded/ (6 dev-cached) vs to-fetch in the SUMMARY. The downloaded/ cache is gitignored — do NOT commit it.
  </action>
  <verify>python3.6 tools/check_edit_coverage.py; echo "exit=$?"</verify>
  <done>Coverage exit 0; download list complete; cache untouched in git.</done>
</task>

</tasks>

<verification>
python3.6 tools/check_edit_coverage.py; echo "exit=$?"
python3.6 -m unittest discover -s tests 2>&1 | tail -3
</verification>

<success_criteria>
Cast = 12 real enzymes matching edits keys; aconitase 1ACO recorded; download list verified; coverage tool exit 0; suite green.
</success_criteria>

<output>.planning/phases/07-content-expansion-i-glucose/07-15-SUMMARY.md</output>
