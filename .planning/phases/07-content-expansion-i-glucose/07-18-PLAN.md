---
phase: 07-content-expansion-i-glucose
plan: 18
type: execute
wave: 7
depends_on: [15, 17]
files_modified: []
autonomous: false
must_haves:
  truths:
    - "Full test suite green; citation gate exit 0 with residual = documented stubs only; edit coverage green"
    - "Reachability green at 57 nodes / 21 endings / 4 tiers; determinism test present and green"
    - "Human approves the batch content review (two-layer samples + claim spot-check)"
  artifacts:
    - path: ".planning/phases/07-content-expansion-i-glucose/07-18-SUMMARY.md"
      provides: "Final verification record + human-verify outcome"
      contains: "VERIFIED"
  key_links:
    - from: "07-18-SUMMARY.md"
      to: "phase 7 completion"
      via: "all gates + human sign-off recorded"
      pattern: "VERIFIED"
---

<objective>
Final phase verification: run every automated gate, then ONE human-verify checkpoint for the batch content review.
Purpose: phase exit requires green automation + human eyes on the science text (GUI/scene behavior stays human-verified per AGENTS.md).
Output: 07-18-SUMMARY.md with full gate record + approval.
</objective>

<context>
@.planning/phases/07-content-expansion-i-glucose/07-16-SUMMARY.md (test inventory + documented fate)
@.planning/phases/07-content-expansion-i-glucose/07-15-SUMMARY.md (asset/download state)
Automated pre-checks (run all, record outputs): full suite; citation gate (residual must be ONLY documented fa.stub/alc.stub stubs); edit coverage; reachability invariants (57/21/4 tiers); determinism test presence. NO engine surgery; no zip rebuild required by the index (flag only if a rebuild is wanted — out of roster scope).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full automated gate battery</name>
  <files>.planning/phases/07-content-expansion-i-glucose/07-18-SUMMARY.md</files>
  <action>
    Run: python3.6 -m unittest discover -s tests; python3.6 tools/check_citations.py; python3.6 tools/check_edit_coverage.py. Record pass/fail + counts in the SUMMARY. Any red = fix within data/content/tests scope (owning plan's rules) and re-run before the checkpoint.
  </action>
  <verify>python3.6 -m unittest discover -s tests 2>&1 | tail -3</verify>
  <done>Suite green; gate exit 0 (stub residual documented); coverage exit 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human batch content review</name>
  <files>.planning/phases/07-content-expansion-i-glucose/07-18-SUMMARY.md</files>
  <what-built>All Phase 7 content: two-layer text across 57 nodes, approved-claims-only wiring, restored-node structure, cast/edits/registry landing.</what-built>
  <how-to-verify>
    1. Skim 2-3 two-layer text samples per segment (intro, glycolysis, pyruvate/anaerobic, TCA, ETC/endings, bad pool) in the story JSONs.
    2. Spot-check 5 claims: pick any 5 claim_ids in story text → confirm approved + wording matches in data/citations.json.
    3. Confirm the soul-jump wording in end.true (electrons = soul; carbon = CO2; C14 = label only).
    4. Optional GUI pass in a real Windows PyMOL session (Qt scenes are human-verify per AGENTS.md): load the plugin, walk intro → glycolysis → PFK edit → restoration node.
  </how-to-verify>
  <resume-signal>Type "approved" or list specific text/claim fixes needed (each fix routes back to the owning content plan's scope).</resume-signal>
</task>

</tasks>

<verification>
python3.6 -m unittest discover -s tests 2>&1 | tail -3
python3.6 tools/check_citations.py; echo "exit=$?"
python3.6 tools/check_edit_coverage.py; echo "exit=$?"
</verification>

<success_criteria>
All automated gates green with only documented stub residual; human checkpoint approved (or fixes routed + re-verified); 07-18-SUMMARY.md records the phase as verified.
</success_criteria>

<output>.planning/phases/07-content-expansion-i-glucose/07-18-SUMMARY.md</output>
